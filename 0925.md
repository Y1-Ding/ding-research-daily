# Daily Research - 2026-09-25 (21:30)

## 0) 今日3条结论

- **LangSmith「agent-addressed」追踪在未配置 project 时不应再隐式挂 `"default"`**（LangChain #**40832**, created **2026-09-25**）→ **skill/评测可观测性**：API 同时带 agent+project 会 400 丢 trace，agent eval 门禁要先确认 `LANGSMITH_AGENT_*` 与 project 配置是否互斥。
- **GLM-5.3-Flash 稀疏 MLA 的 `index_kpool` 配错会在 >2048 ctx 漏读 1–3 个 query token**（vLLM #**58704** / #**58663**, created **2026-09-25**）→ **Talk ≥3 turns + 长上下文 tool agent**：GSM8K 测不出，GPQA/34k+ tool 会话会出现 **损坏 tool call**；eval 须按 **ctx 长度 × tool 密度** 分桶。
- **FlashInfer / AITER sparse MLA metadata 去掉 async 路径上的 `seq_lens` D2H**（#**58684** / #**58710**）→ **Attention 调度 + token 成本**：MTP 每 draft 步都建 metadata 时，async 曾比 `--no-async-scheduling` 更慢；多轮 agent **TPOT 与 serving 默认** 强相关。

## 1) Agent工程化（high-signal）

- [fix(core): don't send a default project with agent-addressed runs（LangChain PR #40832, created **2026-09-25**）](https://github.com/langchain-ai/langchain/pull/40832)
  - 摘要: 仅设 `LANGSMITH_AGENT_ID` / `LANGSMITH_AGENT_ENVIRONMENT` 时，tracer 曾附加隐式 `"default"` project；API 拒绝同时命名 agent 与 project，trace **400 丢失**。现仅在用户显式配置 project 时才附带。
  - 为什么重要(对我): **评测/trace 契约**——agent 流水线若「以为在记 log」实则静默失败，L2 门禁会假绿。
  - 明日动作(<=20min): 写 **是/否**：当前是否仅用 agent env 未设 project（是/否/未知）。

- [fix(core): preserve additional_kwargs in message_to_events finish event（LangChain PR #40829, created **2026-09-25**）](https://github.com/langchain-ai/langchain/pull/40829)
  - 摘要: `message_to_events` 重放 `message-finish` 时曾丢弃 `additional_kwargs`；流式两路径已保留，replay/cache 与 live **行为不一致**（fixes #40788）。
  - 为什么重要(对我): **skill 回放/eval 复现**——provider 侧状态在重放轨迹里消失会破坏 **A/B 与缓存命中** 对照。
  - 明日动作(<=20min): 写 **是/否**：是否用 `message_to_events` 做离线 eval（是/否/未知）。

- [fix(groq): raise ValueError when bind_tools is called on a model that does not support tool calling（LangChain PR #40828, created **2026-09-25**）](https://github.com/langchain-ai/langchain/pull/40828)
  - 摘要: `bind_tools()` 读取 model profile 的 `tool_calling=False` 时 **fail-fast `ValueError`**，而非 Groq HTTP 400（fixes #40771）。
  - 为什么重要(对我): **skill 落地**——工具绑定应在 **构图时** 失败，避免 eval 跑一半才爆。
  - 明日动作(<=20min): 写 **1 条** gold：不支持 tools 的模型期望异常类型（≤10 词）。

- [fix(tools): keep Annotated Field constraints in function tool schemas（LlamaIndex PR #23237, created **2026-09-25**）](https://github.com/run-llama/llama_index/pull/23237)
  - 摘要: `create_schema_from_function` 重建 `FieldInfo` 时丢失 `ge`/`le`/`min_length` 等 **Annotated 约束**，工具 JSON schema 比 Python 签名更宽（fixes #23235）。
  - 为什么重要(对我): **skill schema = 合约**——宽松 schema 会让模型合法调用 **越界参数**，评测假阴性。
  - 明日动作(<=20min): 写 **是/否**：工具参数是否用 `Annotated[..., Field(...)]`（是/否/未知）。

- [fix(core): preserve source order across adjacent neighbor windows（LlamaIndex PR #23229, created **2026-09-25**）](https://github.com/run-llama/llama_index/pull/23229)
  - 摘要: `PrevNextNodePostprocessor` 在检索顺序与文档顺序不一致时，相邻窗口会拼成 `[B,C,D,E,A]` 而非 `[A,B,C,D,E]`；改为沿 prev/next 链遍历输出。
  - 为什么重要(对我): **RAG agent eval**——上下文块顺序错误会系统性改 **引用与推理链**。
  - 明日动作(<=20min): 写 **是/否**：是否用 prev/next 扩窗（是/否/未知）。

- [fix(core): preserve relationships and offsets when resplitting nodes（LlamaIndex PR #23226, created **2026-09-25**）](https://github.com/run-llama/llama_index/pull/23226)
  - 摘要: 对已含 source 的节点再切分时保留父节点身份与 **NEXT/PREV 对称**、字符 offset 映射（fixes #23224）。
  - 为什么重要(对我): **ingest pipeline + eval gold**——多 splitter 后关系断裂会让 **cite 评测** 对不上原文。
  - 明日动作(<=20min): 写 **是/否**：pipeline 是否多次 resplit（是/否/未知）。

- [fix(agentchat): sanitize auto-generated Handoff tool names（AutoGen PR #8285, created **2026-09-25**）](https://github.com/microsoft/autogen/pull/8285)
  - 摘要: 默认 `transfer_to_{target}` 未校验；目标含空格时构造成功但 OpenAI client 首调失败（`Invalid name`，要求 `^[a-zA-Z0-9_-]+$`）。
  - 为什么重要(对我): **multi-agent skill 命名**——handoff 是 **图边**，命名应在 **Handoff 构造时** 失败。
  - 明日动作(<=20min): 写 **1 条**：handoff 目标命名规则（≤12 词）。

- [fix(core): preserve strict in FunctionTool component config（AutoGen PR #8284, created **2026-09-25**）](https://github.com/microsoft/autogen/pull/8284)
  - 摘要: `FunctionTool(strict=True)` 经 `dump_component`/`load_component` 曾丢失 strict，schema 静默变 non-strict；旧 config 默认 `False` 兼容。
  - 为什么重要(对我): **skill 配置即代码**——序列化回归应覆盖 **strict tool + structured output** 组合。
  - 明日动作(<=20min): 写 **是/否**：是否序列化 AutoGen FunctionTool（是/否/未知）。

- [Bugfix] Mark tool_result is_error in Anthropic converter（vLLM PR #58700, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58700)
  - 摘要: `AnthropicContentBlock.is_error` 曾解析未用，失败与成功 tool 结果在内部 `tool` 消息 **不可区分**（#58647 item 9）。
  - 为什么重要(对我): **L2 tool eval**——模型无法区分「文件不存在」与「文件内容」，会污染 **重试策略** 评测。
  - 明日动作(<=20min): 写 **是/否**：是否经 vLLM Anthropic 兼容层跑 tool loop（是/否/未知）。

- [Perf][Frontend] Linearize GLM tool-result rendering（vLLM PR #58686, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58686)
  - 摘要: GLM-5.3 chat template 对大块合法 tool-result 做嵌套扫描，渲染可达 **秒级**；改为线性路径对齐 HF early-break 思路。
  - 为什么重要(对我): **token 预算前置**——渲染慢会被误判为模型慢，**TTFT/首 token** agent 指标失真。
  - 明日动作(<=20min): 写 **是/否**：是否 GLM-5.x + 大 tool payload（是/否/未知）。

- [Bugfix][Core] Uncount stop-trimmed spec tokens from num_computed_tokens（vLLM PR #58668, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58668)
  - 摘要: speculative decode 在 stop 处 trim 后续 token，但 `num_computed_tokens` 仍计数；**可恢复 streaming-input** 下下一 chunk fold 边界错位。
  - 为什么重要(对我): **多轮 + spec decode eval**——KV/折叠边界错会改 **Talk ≥3 turns** 轨迹。
  - 明日动作(<=20min): 写 **是/否**：是否 streaming-input + spec decode（是/否/未知）。

- [Frontend][Spec Decode] Populate metrics.speculative_decoding on /v1/responses（vLLM PR #58656, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58656)
  - 摘要: chat/completions 已有 `metrics.speculative_decoding`，`/v1/responses` 在 `--per-request-spec-decode-metrics` 下曾为 **null**；与 chat 路径对齐填充。
  - 为什么重要(对我): **评测统一入口**——Responses API agent 若缺 spec 指标，**$/task 与 acceptance** 无法同帧。
  - 明日动作(<=20min): 写 **是/否**：eval 是否走 `/v1/responses`（是/否/未知）。

- [Bugfix][Rust Frontend] Dedupe chat top_logprobs and reject an over-budget max_tokens（vLLM PR #58711, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58711)
  - 摘要: Rust 前端 `top_logprobs` 重复采样 token；显式 `max_tokens` 超出剩余 context 时 **拒绝** 而非静默截断（fixes #58680，对齐 Python）。
  - 为什么重要(对我): **logprob judge + token 门禁**——重复项与静默截断都会污染 **L3 评分**。
  - 明日动作(<=20min): 写 **是/否**：是否 Rust frontend eval（是/否/未知）。

- [Bugfix][Structured Output] Preserve literal values in Guidance disable_additional_properties（vLLM PR #58709, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58709)
  - 摘要: Guidance 后端在 `disable_additional_properties=True` 时误改 `const`/`enum`/`default` 内嵌对象，导致 **合法 literal 被拒**（fixes #58695）。
  - 为什么重要(对我): **structured tool/skill 输出**——schema 编译与运行时 literal 不一致会制造 **假失败**。
  - 明日动作(<=20min): 写 **是/否**：是否 Guidance + disable_additional_properties（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Bugfix][GLM-5.3-Flash] SM90 sparse MLA: index_kpool mismatch leads to corruption via unread query token（vLLM PR #58704, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58704)
  - 摘要: 稀疏 indexer 的 `index_kpool=4` 与主 MLA `tokens_per_state=1` 混用，>2048 ctx 时 **每 4 步中 3 步漏读 1–3 个 query token**；34k/42k 重 tool 会话可见损坏 tool call。
  - 为什么重要(对我): **Attention 索引与 agent 行为**——长上下文 benchmark 须覆盖 **>2048 + tools**。
  - 明日动作(<=20min): 写 **≤15 词**：是否 GLM-5.3-Flash serving（是/否/未知）。

- [Bugfix][Attention] Fix index_kpool handling in FlashInfer SM90 sparse MLA（vLLM PR #58663, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58663)
  - 摘要: 从 model config 读取 `index_kpool`（默认 1）；修正传给 FlashInfer 的有效长度计算；GPQA Diamond **0.5455→0.7626**（同设置 H200 TP8）。
  - 为什么重要(对我): **稀疏 MLA 数值契约**——错误 `kv_len` 直接改 **下游 logits** 而非仅 perf。
  - 明日动作(<=20min): 写 **1 行**：你记录的 sparse MLA 后端名（≤10 词）。

- [Perf][Attention] Remove D2H sync from FlashInfer SM90 sparse MLA plan under async scheduling（vLLM PR #58684, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58684)
  - 摘要: async + MTP 每步 metadata 的 blocking D2H 使 GLM-5.3-Flash TPOT **11.1ms vs 7.7ms**（async 更慢）；改为 CPU upper bound + GPU `_clamp_work_kv_end` 夹紧。
  - 为什么重要(对我): **Attention 调度与 agent 逐步延迟**——metadata 构建是 **隐藏同步点**。
  - 明日动作(<=20min): 写 **是/否**：是否 `--async-scheduling` + sparse MLA（是/否/未知）。

- [ROCm][Perf] Skip the seq_lens D2H sync in the AITER sparse MLA metadata key（vLLM PR #58710, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58710)
  - 摘要: ROCm `ROCM_AITER_MLA_SPARSE` 每步 `seq_lens.cpu()` 仅用于 metadata **cache key**；无 spec decode 时用 `seq_lens_cpu_upper_bound` 避免 D2H（#58684 的 ROCm 对应）。
  - 为什么重要(对我): **跨平台 Attention 路径**——同一模型在 CUDA/ROCm 的 **多轮延迟分布** 可能不同。
  - 明日动作(<=20min): 写 **是/否**：是否 ROCm sparse MLA（是/否/未知）。

- [ROCm][DSv4.1] Paged MXFP4 sparse indexer on aiter's MQA-logits kernel（vLLM PR #58671, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58671)
  - 摘要: DSv4.1 indexer 支持 **MXFP4 K cache**；可选 `indexer_sparse_logits` 用 block max 候选池替代全上下文打分（长 ctx 收益大）。
  - 为什么重要(对我): **稀疏 Attention + KV 量化**——agent 长对话的 **indexer 算力** 与精度边界。
  - 明日动作(<=20min): 写 **≤15 词**：是否 DSv4.1 + sparse indexer（是/否/未知）。

- [ROCm][Perf] Fuse the HyperConnection low-rank mix at decode widths（vLLM PR #58706, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58706)
  - 摘要: Qwen3.8-Flash-Next 每层 HyperConnection 在 decode batch≤5 将 down+SiLU、up+gate 各 **2 kernel 并 1**；decode trace 四 kernel ~**19%** 步时间、多为 dispatch。
  - 为什么重要(对我): **小 batch decode（agent 典型）**——残差门控路径的 **固定 launch 税** 会累积 Talk ≥3 turns。
  - 明日动作(<=20min): 写 **是/否**：是否 Qwen3.8-Flash-Next 在线（是/否/未知）。

- [Test][Hybrid] Add TP2 Mamba2 prefix-cache hit regression（vLLM PR #58676, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58676)
  - 摘要: `Zyphra/Zamba2-1.2B-instruct` TP=2 冷/热/bypass 三路径回归：要求 warm **非空 cache hit**，并比对 warm vs bypass 的 token id 与 logprobs。
  - 为什么重要(对我): **Hybrid SSM + prefix cache**——多轮 agent 的 **TP>1 cache 语义** 需单测锚定。
  - 明日动作(<=20min): 写 **是/否**：是否 hybrid 模型 + TP>1（是/否/未知）。

## 3) 热门项目/可试个人项目

- [Model] Support MiniCPM-V 4.7（vLLM PR #58674, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58674)
  - 摘要: vLLM 接入即将发布的 **MiniCPM-V 4.7**；新增 canvas 3D M-RoPE（缩略图按 slice 几何重采样），视频占位符不再发 `<image_id>` local id。
  - 为什么重要(对我): **低成本多模态 agent 试跑**——小 VLM + vLLM 可对照自有 **tool+vision** eval 栈。
  - 明日动作(<=20min): 查 release 权重是否公开，记 **最小显存** 与一条 smoke prompt（3 行）。
  - 估计复现成本: **中**（拉权重 + vLLM nightly，~1–3h，视 GPU）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Claude discovers a novel enzyme system with CRISPR-like repeats（Anthropic News, **2026-09-23**）](https://www.anthropic.com/news/claude-discovers-novel-enzyme-system)
  - 机会信号: ~**950 agents / 21h / 210M tokens** 基因组挖掘 → **「假设生成 + 人审 + 湿实验」** 产品形态；对外可卖 **垂直 agent eval + 科学 taste 沉淀**（非纯模型 API）。
  - 风险信号: 功能未完全阐明；**生命科学合规与数据边界** 高；易被简化为「AI 又发现 CRISPR」叙事。
  - 我可执行的一步(<=30min): 写 **3 行**：你若做「科研 agent」会固定的 **人审闸门**（每条 ≤12 词，可脱敏）。

- [Docs] Document DP-aware routing with vLLM Router in the DP deployment guide（vLLM PR #58702, created **2026-09-25**）](https://github.com/vllm-project/vllm/pull/58702)
  - 机会信号: **consistent_hash + `X-data-parallel-rank`** 可把会话钉在同一 DP rank → **prefix/KV 复用**；出海交付 **对话型 agent SLA** 时的标准话术与架构图素材。
  - 风险信号: 文档级，需自建 Router/K8s；**会话粘滞** 与故障域扩大。
  - 我可执行的一步(<=30min): 画 **5 框** ASCII：client → router → DP rank → vLLM → cache 命中点（≤5 词/框）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[1096. Brace Expansion II](https://leetcode.com/problems/brace-expansion-ii/)（**2026-09-25 Daily Challenge**，Hard，LeetCode GraphQL `activeDailyCodingChallengeQuestion`）：集合展开 + 递归/栈；与 **状态空间枚举 invariant** 同构。明日动作：一题或写 **≤15 词**「Hard 日题」对你周计划意味着什么。

- **IELTS（≤1 条）**：[IELTS Reading practice tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/reading)：0924 为 Listening，今日 **Reading 一组（~20min 计时）**。明日动作：写 **2 个** 失误类型（定位/同义替换）；**≤15 词** 最弱 Reading 题型。

## 6) 噪音过滤记录

- **LangChain #40833/#40834/#40831**：model profile refresh / fireworks release / Perplexity `stop` → 过滤。
- **LlamaIndex #23233/#23232/#23231/#23230**：deps bump → 过滤。
- **LlamaIndex #23238/#23228/#23227**：delete_ref_doc / mbox / CSV 读者修复 → RAG niche，未进主报告。
- **vLLM #58712/#58705/#58707/#58708/#58701**：logging/docs/CI → 过滤。
- **vLLM #58682**：Anthropic system 块分隔符 → agent 相关但当日 §1 已满密度，可自行跟进（#58647 族）。
- **vLLM #58669**：/tokenize 501 → token 门禁 niche，未进 §1。
- **LangGraph**：**2026-09-25** 无新 PR → 不收录（#9073 为 **2026-09-24**，已在邻日报告策略外）。
- **OpenAI Blog / DeepMind / Lil'Log / Simon Willison**：未发现 **2026-09-25** 可验证新文 → 不收录。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-25** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill → 未调用；白名单 GitHub + Anthropic 新闻当日 **≥8** 条主信号，无需 skill 补充。
- **openai/evals**：**2026-09-25** 无新 PR → 不收录。

## 7) 本周累计计数

- Agent: **1120**（`0924.md` §7: 1106 + 本日 §1: 14）
- Theory: **517**（`0924.md` §7: 510 + 本日 §2: 7）
- Career/Business: **164**（`0924.md` §7: 162 + 本日 §4: 2）
- LC: **138**（`0924.md` §7: 137 + 本日 §5: 1）
- IELTS: **138**（`0924.md` §7: 137 + 本日 §5: 1）
