# Daily Research - 2026-08-05 (21:30)

## 0) 今日3条结论

- **Middleware 内部 LLM 调用会「污染 messages + stream + tracing」**（LangChain #39252/#39245, LangGraph #8523, Aug 4–5）：`SummarizationMiddleware`/`LLMToolSelector` 等 bookkeeping 调用与主 agent turn 同节点执行 → `run.messages` 与 `stream_events` 混入内部 token；tracing 全量 state 可拖慢 500–1000ms/middleware → **eval 的 messages 投影与 $/task 计量必须排除 internal calls**。
- **DeltaChannel checkpoint 仍有「首写丢失 + 安全默认收紧」**（LangGraph #8526/#8530, Aug 5）：plain-value seed 迁移后 `get_delta_channel_history` 跳过首条 write；`LANGGRAPH_STRICT_MSGPACK` 默认 true（GHSA-g48c-2wqr-h844）→ **trajectory replay eval 需覆盖迁移线程 + 自定义 checkpoint 类型 allowlist**。
- **Token 计量三层缺口同日补齐**：Anthropic reasoning tokens 进 `usage_metadata`（#39257）、vLLM bench 可报 `cached_tokens`（#51155）、oaieval 展平 nested usage details（#1650）→ **agent eval 的 cost/latency 面板需同时追踪 reasoning + cached + nested details**。

## 1) Agent工程化（high-signal）

- [feat(langchain): filter internal middleware model calls from `messages` projection（LangChain PR #39252, updated **2026-08-05**）](https://github.com/langchain-ai/langchain/pull/39252)
  - 摘要: `SummarizationMiddleware`、`LLMToolEmulator`、`LLMToolSelectorMiddleware` 在 agent 同节点内发起 bookkeeping LLM 调用；`MessagesTransformer` 仅按 checkpoint namespace 过滤，无法区分内部/主 turn → 内部 token 泄漏进 `run.messages` 与 `stream_events(v3)`。Fix：三处 call site 在 `config["metadata"]` 打 internal-call marker + 新增 `InternalCallTransformer` 过滤。
  - 为什么重要(对我): **token 优化 + 评测**——middleware ablation 若 messages 含 summarization 中间态，grading 输入与真实用户所见不一致；$/task 虚高。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否用 `run.messages` 或 `stream_events` 做 trajectory grading（是/否/未知）。

- [fix(langchain): prevent middleware model calls from leaking into the parent stream（LangChain PR #39245, updated **2026-08-05**）](https://github.com/langchain-ai/langchain/pull/39245)
  - 摘要: Fixes #34139/#34382/#34358。内部 middleware LLM 调用通过移除 streaming callbacks（保留 tracing callbacks）不再泄漏到用户 stream；同时对 malformed structured-output 增加一次 retry 后抛清晰 `ValueError`。
  - 为什么重要(对我): **skill 落地 + 评测**——HITL/streaming UI 若混入 summarization chunk，用户与 eval 同步观测全错；structured-output retry 影响 pass^k 的 timeout 分布。
  - 明日动作(<=20min): 写 **是/否**：你是否在 streaming agent 上叠 SummarizationMiddleware（是/否/未知）。

- [fix(langchain): clear stale `structured_response` between checkpointed turns（LangChain PR #39248, updated **2026-08-05**）](https://github.com/langchain-ai/langchain/pull/39248)
  - 摘要: Fixes #36957。checkpointer 下 `_build_commands` 仅在模型产出 structured_response 时写入；上轮残留值留在 checkpoint → routing edge 只查 key 存在性 → 本轮 structured-output 校验失败却提前 exit。Fix：无产出时显式清 `None`；edge 改查 `is not None`。
  - 为什么重要(对我): **评测 + skill 落地**——multi-turn structured agent eval 的假 pass = 上轮答案被误当本轮；与 0804 ReAct parser 假失败形成「进/出」双侧。
  - 明日动作(<=20min): 写 **是/否**：你 multi-turn eval 是否测过「上轮有 structured_response、本轮 retry」路径（是/否/未知）。

- [fix(checkpoint): collect writes at plain-value seed in delta channel history（LangGraph PR #8526, updated **2026-08-05**）](https://github.com/langchain-ai/langgraph/pull/8526)
  - 摘要: Fixes #8384。`InMemorySaver.get_delta_channel_history` 在 seed blob 为 plain value（非 `_DeltaSnapshot`）时跳过该祖先的 writes → 迁移到 `DeltaChannel` 后首条 write 静默丢失。Fix：移除错误区分；终止 walk 已保证更老祖先被 seed 覆盖。
  - 为什么重要(对我): **评测**——与 0804 #8520（空 hydrate）同属 DeltaChannel conformance；迁移线程的 `get_state_history` 首步缺失 = trajectory eval ground truth 断裂。
  - 明日动作(<=20min): 写 **是/否**：你是否从非 Delta 通道迁移过生产线程（是/否/未知）。

- [feat(langgraph): expose `trace_policy` on `add_node`（LangGraph PR #8523, updated **2026-08-04**）](https://github.com/langchain-ai/langgraph/pull/8523)
  - 摘要: 新增 `TracePolicy(process_inputs, tags)`，经 `add_node` 暴露；动机是 LangChain middleware hooks 当前 trace 全量 state，deepagents eval 中每个 middleware 增加 500–1000ms tracing 延迟。允许 per-node 变换 trace 输入。
  - 为什么重要(对我): **token 优化 + 评测**——长跑 eval 的 wall-clock 与 LangSmith trace 成本被 tracing payload 绑架；与 #39252 的 messages 泄漏是同一「内部调用膨胀」族。
  - 明日动作(<=20min): 写 **≤15 词**：你 LangSmith trace 是否因长 message history 拖慢 eval（是/否/未知）。

- [fix(checkpoint): default LANGGRAPH_STRICT_MSGPACK to true（LangGraph PR #8530, updated **2026-08-05**）](https://github.com/langchain-ai/langgraph/pull/8530)
  - 摘要: Fixes #8522。`LANGGRAPH_STRICT_MSGPACK` 默认 **true**（GHSA-g48c-2wqr-h844 msgpack 构造器 allowlist）；需 permissive 行为可设 `=false` 或显式 allowlist。
  - 为什么重要(对我): **skill 落地**——自定义 state/checkpoint 类型若未加入 allowlist，升级后 checkpoint 读写 silent fail；eval 环境需 pin 或文档化 opt-out。
  - 明日动作(<=20min): 写 **是/否**：你 state 是否含自定义 Python 类进 checkpoint（是/否/未知）。

- [fix(core): keep tool call pairs during token truncation（AutoGen PR #8025, updated **2026-08-05**）](https://github.com/microsoft/autogen/pull/8025)
  - 摘要: Fixes #7955。`TokenLimitedChatCompletionContext` 从中间逐条删 message 时，可能拆开 assistant tool_call 与 execution result → 孤儿 message 破坏协议。Fix：相邻 tool-call/result 对作为不可分割单元截断；覆盖 `token_limit` 与 `remaining_tokens` 两模式。
  - 为什么重要(对我): **token 优化 + 评测**——长上下文 agent 靠 truncation 省 token 时，eval 假失败（tool 协议断裂）比 OOM 更难查。
  - 明日动作(<=20min): 写 **是/否**：你是否用 AutoGen `TokenLimitedChatCompletionContext`（是/否/未知）。

- [fix(core): forward `feedback_config` through `EvaluatorCallbackHandler._log_evaluation_feedback`（LangChain PR #39253, updated **2026-08-05**）](https://github.com/langchain-ai/langchain/pull/39253)
  - 摘要: Fixes #31802。`EvaluatorCallbackHandler` 调 `client.create_feedback` 时丢弃 `EvaluationResult.feedback_config` → LangSmith 侧 feedback 元数据（如评分 rubric、来源标签）静默丢失。Fix：单参数转发 + 首组 unit test。
  - 为什么重要(对我): **评测**——LLM-as-judge eval 的 feedback 链路是 pass^k 审计 ground truth；config 丢失 = 无法复现 grading 条件。
  - 明日动作(<=20min): 写 **是/否**：你是否在 eval 中用 `EvaluatorCallbackHandler` + 自定义 `feedback_config`（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[CPU] Add MLA backend so DeepSeek-V2/V3 can run on CPU（vLLM PR #49453, updated **2026-08-05**）](https://github.com/vllm-project/vllm/pull/49453)
  - 摘要: 新增 `CPUMLABackend`/`CPUMLAImpl`：prefill 用 PyTorch SDPA 慢路径，decode 走 `mla_decode_kvcache` CPU kernel（`block_size=16`）；含 smoke test（dummy weights DeepSeek-V2-Lite）。目标：无 GPU 也能本地验证 MLA 正确性。
  - 为什么重要(对我): **Talk ≥3 turns + MLA/Attention**：MLA 压缩 KV 是长上下文 agent 基础设施；CPU 参考实现 = 本地 debug attention 路径而不依赖 GPU worker pool。
  - 明日动作(<=20min): 写 **≤15 词**：你是否需要在无 GPU 环境验 MLA decode（是/否/未知）。

- [[Bugfix][Core] Fix request-bound KV cache sizing（vLLM PR #46841, updated **2026-08-05**）](https://github.com/vllm-project/vllm/pull/46841)
  - 摘要: 混合模型（token-proportional attention KV + request-bound KV 如 Mamba）启动 sizing 未扣 shared BlockPool 中 request-bound 固定块 → auto-fit 容量高于 runtime admission。Fix：sizing 路径计入 request-bound blocks + 保留 null block；74 tests pass。
  - 为什么重要(对我): **Talk ≥3 turns + KV/Attention**：hybrid SSM+Attention 是下一代长上下文架构；KV 高估 = agent 长 session eval 随机 OOM 或 silent admission reject。
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 是否 hybrid Mamba+Attention 模型（是/否/未知）。

- [Karpathy: Opus 5 用 1M token（~$10）把《魔戒》首段渲染为 Three.js 3D 世界（**2026-08-02**）](https://x.com/karpathy/status/2083749667410727319)
  - 摘要: ~2h 自主执行、5500 行 JS；模型需自行放置 (x,y,z) 资产并动画化。Karpathy 指出：**评测范式正从「画 pelican SVG」转向长时程 agentic 任务**；弱点是模型无法原生感知/审计 3D 输出，只能靠截图慢速自检 → jank 累积。他称之为 vibe check 而非 hard benchmark。
  - 为什么重要(对我): **Talk ≥3 turns + Agent 原理**：长时程 eval 的核心瓶颈从「单次输出质量」转向「感知-修正闭环」；对你 L2–L3 eval 意味着需加 **artifact 自检通道**（截图/headless browser）而非只看最终文本。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent eval 是否含「模型自检自己产出」步骤（是/否/未知）。

- [fix(anthropic): include reasoning tokens in `output_token_details` usage metadata（LangChain PR #39257, updated **2026-08-05**）](https://github.com/langchain-ai/langchain/pull/39257)
  - 摘要: Fixes #39249。Anthropic SDK 的 `output_tokens_details.thinking_tokens` 此前未映射到 `UsageMetadata.output_token_details["reasoning"]` → AIMessage/Chunk 缺 reasoning token 计数。Fix：invoke + streaming 双侧补齐 + unit test。
  - 为什么重要(对我): **Talk ≥3 turns + token 计量**：extended thinking 模型的 eval cost 若不计 reasoning tokens，$/task 与 cache ablation 全偏。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否用 Anthropic extended thinking 模型（是/否/未知）。

- [Handle nested token usage details in oaieval（openai/evals PR #1650, updated **2026-08-05**）](https://github.com/openai/evals/pull/1650)
  - 摘要: Fixes #1582。`add_token_usage_to_result` 假设 usage 值为标量；新 SDK 的 `completion_tokens_details`/`prompt_tokens_details` 为嵌套对象 → 聚合崩溃。Fix：展平嵌套 payload 为 `usage_completion_tokens_details_reasoning_tokens` 等叶子指标。
  - 为什么重要(对我): **Talk ≥3 turns + eval 基础设施**：oaieval 是开源 eval 参考实现；nested usage 与 LangChain #39257 同属「token 明细 schema 演进」族，自建 eval harness 需同步。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否解析 `prompt_tokens_details.cached_tokens`（是/否/未知）。

## 3) 热门项目/可试个人项目

- [[Benchmark] Add cached tokens to vllm bench serve（vLLM PR #51155, updated **2026-08-05**）](https://github.com/vllm-project/vllm/pull/51155)
  - 摘要: `vllm bench serve` 新增 `--report-cached-tokens`（默认 off）；服务端需 `--enable-prompt-tokens-details`。捕获 `usage.prompt_tokens_details.cached_tokens` → 控制台与 JSON 输出 `total_cached_tokens`。
  - 为什么重要(对我): **token 优化杠杆**——prefix cache / prompt cache ablation 终于有了 bench 级可复现指标，不必手写 curl 解析 usage。
  - 明日动作(<=20min): 对一条 agent eval prompt 跑 `vllm bench serve --report-cached-tokens`，记录 cached/total 比。
  - 估计复现成本: **低**（已有 vLLM server + 单条 bench 命令，≤20min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Eval Engineering: A Beginner's Guide（Opinion AI Substack, **2026-08-04**）](https://emergingai.substack.com/p/eval-engineering-a-beginners-guide)
  - 机会信号: 用生产案例说明「agent 每步都完成但任务仍失败」——response quality 83.9% vs faithfulness 32.3%；主张 eval engineering 是独立技能层（定义 promise → 三文件 eval → Harbor/LangChain skill → trace→eval → worker/judge 分离）。与 0804 AI startup hiring 的「agent eval stream」可直接对接。
  - 风险信号: 指南偏概念框架，落地需配套 trace 基础设施；「五类必测」清单未给具体 YAML，易停留在读后感。
  - 我可执行的一步(<=30min): 从你当前 agent 选 **1 条真实 trace**，写 **3 句 eval promise**（输入/期望证据/不可接受路径），不碰代码。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[95. Unique Binary Search Trees II](https://leetcode.com/problems/unique-binary-search-trees-ii/)（**Day 35, Aug 2026 Daily Challenge**；承接 0804 Combination Sum 的 backtracking 链，今日切 BST 结构生成）：给定 `n`，返回所有结构互异的 BST（节点 1..n）。明日动作：模板化 `dfs(start, end)` 分治建树；写 **≤15 词**：BST 分治与 agent eval「子任务分解」的类比。

- **IELTS（≤1 条）**：[IELTS Writing Task 2 官方样题与评分标准（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-writing-task-2)：0804 已轮换 Reading Mini Mock，今日换 **Writing Task 2**（25 分钟列提纲 + 写 introduction 段，不限字数）。明日动作：选 1 道 agree/disagree 题，写 **thesis + 2 topic sentences**；写 **≤15 词** 你最易犯的衔接词错误。

## 6) 噪音过滤记录

- **LangGraph #8529/#8528 cryptography deps bump**：安全依赖升级，非 agent 行为变更 → 过滤。
- **LangGraph #8525 docs 404、#8524 Windows Redis probe**：非当前 focus → 过滤。
- **LangChain #39256 model profile refresh、#39254 git tag sanitize**：infra/chore → 过滤。
- **LlamaIndex #22593 OTEL、#22592 pickle __getstate__、#22526 PII postprocessor**：重要但偏 observability/serialization，今日 §1 已满 8 条 → 过滤（#22592 可并入 eval 稳定性专题）。
- **AutoGen #8026 MCP tool override collision、#8021 MCP SDK cap**：重要但今日 §1 已代表 tool 协议族 → 过滤。
- **vLLM #51165 ZoomKV、#50289 Rust frontend、#51158 CUTLASS MoE**：与 Attention/token focus 弱相关 → 过滤。
- **Karpathy LoTR secondary coverage**（the-decoder、Times of India）：已有一手 X 链接 → 不重复收录二手源。
- **Simon Willison smevals（Jul 31）、langchain-skills（0804 已引）**：非 Aug 5 新内容 → 不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-08-04~08-05** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Aug 4–5 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **670**（`0804.md` §7: 662 + 本日 §1: 8）
- Theory: **290**（`0804.md` §7: 285 + 本日 §2: 5）
- Career/Business: **112**（`0804.md` §7: 111 + 本日 §4: 1）
- LC: **94**（`0804.md` §7: 93 + 本日 §5: 1）
- IELTS: **94**（`0804.md` §7: 93 + 本日 §5: 1）
