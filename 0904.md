# Daily Research - 2026-09-04 (21:30)

## 0) 今日3条结论

- **LlamaIndex SQL 三条 sync/async 分叉同日修复**（#22952/#22953/#22955, created **2026-09-04**）：`NLSQLTableQueryEngine` 的 `markdown_response`、`NLSQLRetriever` 的 `sql_only` metadata、`NLStructStoreQueryEngine` 的 response synthesis 均在 `query()` 与 `aquery()` 间行为不一致 → **RAG/SQL agent eval gold 须对 sync/async 双路径断言同一输出格式与 metadata 键。**
- **AutoGen 三条 agent runtime 缺陷同日暴露**（#8185/#8182/#8191, created **2026-09-04**）：`tool_choice` 未进 cache key 导致跨 phase 脏读；`CancelledError` 使 `on_messages_stream` 永久挂起；MCP SDK 2.x snake_case 属性不兼容 → **multi-phase agent eval 须覆盖 tool_choice 切换、mid-tool cancellation、MCP SDK 2.x schema 三条负例。**
- **vLLM sparse attention 两条确定性/初始化缺陷**（#55314/#55299, created **2026-09-04**）：DSA indexer radix top-k 在 threshold bin 溢出时静默丢候选；DSv4 prefill sparse index workspace 未 seed `-1` sentinel → 多 chunk prefill + CUDA graph 启动即 illegal memory access → **sparse attention eval 须标 top-k tie 行为 + workspace 初始化 sentinel。**

## 1) Agent工程化（high-signal）

- [draft: keyed subgraphs through send（LangGraph PR #8819, created **2026-09-04**）](https://github.com/langchain-ai/langgraph/pull/8819)
  - 摘要: Draft PR，为 LangGraph `Send` 引入 keyed subgraph 机制，使 subagent/subgraph 调用可携带稳定 key 用于路由与 replay；改动覆盖 `pregel/_algo.py`、`types.py`、checkpoint serde 与 `_replay.py`；附注 deepagents 需在 tool call 中传递新 send arg。
  - 为什么重要(对我): **skill 落地 + subagent 架构**——multi-agent skill 落地时 subagent 隔离与结果回传依赖 Send 语义；keyed subgraph 是 deepagents 并行 subagent 的可复现路由基础。
  - 明日动作(<=20min): 写 **是/否**：你是否用 LangGraph `Send` 做 subagent 并行委派（是/否/未知）。

- [feat(langchain): add `allow_list` in `ShellToolMiddleware`（LangChain PR #40196, created **2026-09-04**）](https://github.com/langchain-ai/langchain/pull/40196)
  - 摘要: `ShellToolMiddleware` 新增可选 `allow_list` 参数，在 dangerous pattern validator 之后再做命令白名单检查；设计参考 deepagents 的 `ShellAllowListMiddleware`。
  - 为什么重要(对我): **skill 落地 + 安全门禁**——agent skill 若含 shell 执行，allowlist 是 eval 的安全边界；与 0903 HITL `args_schema` 形成「人审 + 命令白名单」双层 gate。
  - 明日动作(<=20min): 写 **≤15 词**：你的 agent shell tool 有无 allowlist（有/无/未知）。

- [fix(agents, anthropic): Fix Filesystem middleware path validation on Windows（LangChain PR #40195, created **2026-09-04**）](https://github.com/langchain-ai/langchain/pull/40195)
  - 摘要: Fixes #40062/#40063。Filesystem middleware 用 `str()` 转相对路径，Windows 产生反斜杠，导致 `allowed_prefixes` 虚拟路径校验失败。Fix：改用 `.as_posix()` 确保虚拟路径始终用正斜杠。
  - 为什么重要(对我): **skill 落地**——deepagents / Cursor plugin 的 filesystem skill 在 Windows 环境 silent fail = eval 跨平台不可复现。
  - 明日动作(<=20min): 写 **是/否**：你是否在 Windows 上跑 agent filesystem skill（是/否/未知）。

- [fix(core): honor markdown_response in NLSQLTableQueryEngine.aquery（LlamaIndex PR #22952, created **2026-09-04**）](https://github.com/run-llama/llama_index/pull/22952)
  - 摘要: Fixes #16720（2024-10 报告，stale bot 关闭未修）。`BaseSQLTableQueryEngine._query` 在 `synthesize_response=False` + `markdown_response=True` 时返回 markdown table，但 `_aquery` 跳过该分支，返回原始 tuple string。Fix：在 `_aquery` 应用相同 `markdown_response` 检查。
  - 为什么重要(对我): **评测 + RAG agent**——SQL agent eval 若只测 sync `query()`，async `aquery()` 路径系统性返回不同格式 → false pass。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `NLSQLTableQueryEngine` + `markdown_response=True`（是/否/未知）。

- [fix(core): return sql_only result metadata from NLSQLRetriever.aretrieve_with_metadata（LlamaIndex PR #22953, created **2026-09-04**）](https://github.com/run-llama/llama_index/pull/22953)
  - 摘要: `sql_only=True` 时，`retrieve_with_metadata` 返回 `{"sql_query", "result"}`，但 `aretrieve_with_metadata` 仅返回 `{"sql_query"}` → `aquery()` 后 `response.metadata["result"]` 抛 `KeyError`。Fix：async `sql_only` 分支补齐 `"result"` 键。
  - 为什么重要(对我): **评测**——agent eval 断言 metadata 键时，async 路径 silent `KeyError` = eval harness 与 production async 路径脱节。
  - 明日动作(<=20min): 写 **是/否**：你是否在 eval 断言 `response.metadata["result"]`（是/否/未知）。

- [fix(core): synthesize responses in NLStructStoreQueryEngine._aquery（LlamaIndex PR #22955, created **2026-09-04**）](https://github.com/run-llama/llama_index/pull/22955)
  - 摘要: `NLStructStoreQueryEngine._query` 在 `synthesize_response=True`（默认）时运行 response synthesis prompt，但 `_aquery` 直接返回 raw SQL result string。Fix：mirror sync 分支，在 `_aquery` 中按 `synthesize_response` 决定是否 `apredict`。
  - 为什么重要(对我): **评测 + skill 落地**——struct store SQL agent 的 async 路径 silent 跳过 synthesis = eval 测到的是 SQL 原始输出而非 agent 最终回答。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `NLStructStoreQueryEngine` + 默认 `synthesize_response`（是/否/未知）。

- [feat: add gen_ai.agent.action_ref to trace_tool_span（AutoGen PR #8187, created **2026-09-04**）](https://github.com/microsoft/autogen/pull/8187)
  - 摘要: Fixes #7850。新增 `gen_ai.agent.action_ref` OTel 属性，通过 `derive_action_ref(agent_id, action_type, scope, timestamp_ms)` 生成确定性 SHA-256 跨 producer 关联键；`trace_tool_span()` 新增可选 `action_ref` 参数。
  - 为什么重要(对我): **评测 + trace 关联**——multi-agent eval 跨 runtime 比对 tool span 时，producer-scoped `tool.call.id` 不够；`action_ref` 是可复算的 audit correlation key。
  - 明日动作(<=20min): 写 **≤15 词**：你的 agent eval 是否跨 runtime 关联 tool span（是/否/未知）。

- [fix: include tool_choice in ChatCompletionCache cache key（AutoGen PR #8185, created **2026-09-04**）](https://github.com/microsoft/autogen/pull/8185)
  - 摘要: Fixes #7968。`ChatCompletionCache` cache key 仅含 `messages/tools/json_output/extra_create_args`，不含 `tool_choice` → `tool_choice="auto"` vs `"none"` 碰撞，第二调用 silent 返回 stale cached response。`AssistantAgent` 在 tool-call phase 与 reflection phase 间切换 `tool_choice`，尤为致命。
  - 为什么重要(对我): **评测 + token 优化**——cached agent eval 若未区分 `tool_choice`，multi-phase agent 系统性 false pass；cache key 设计是 eval reproducibility 的前置条件。
  - 明日动作(<=20min): 写 **是/否**：你是否在 agent eval 启用 `ChatCompletionCache`（是/否/未知）。

- [fix: prevent deadlock when tool call is cancelled during on_messages_stream（AutoGen PR #8182, created **2026-09-04**）](https://github.com/microsoft/autogen/pull/8182)
  - 摘要: Fixes #7956。`CancellationToken` 在 tool call in-flight 时 cancel，`CancelledError`（Python 3.8+ 继承 `BaseException`）逃出 workbench → `asyncio.gather` 在 put sentinel 前 raise → `on_messages_stream` 永久挂起，`stop_when_idle()` 也挂。Fix：`return_exceptions=True` + `finally` 保证 sentinel + 异常转 `FunctionExecutionResult(is_error=True)`。
  - 为什么重要(对我): **评测 + skill 落地**——long-running agent eval 的 timeout/cancel 路径是 liveness gate；deadlock = eval CI 挂死。
  - 明日动作(<=20min): 写 **是/否**：你是否在 eval 测试 mid-tool cancellation（是/否/未知）。

- [fix: use snake_case attributes for MCP tool and result objects（AutoGen PR #8191, created **2026-09-04**）](https://github.com/microsoft/autogen/pull/8191)
  - 摘要: Fixes #7129。MCP Python SDK 2.x 使用 `tool.input_schema` / `result.is_error`（snake_case），AutoGen 仍用 camelCase `inputSchema`/`isError` → `mcp_server_tools()` 抛 `AttributeError`。Fix：全量改为 snake_case。
  - 为什么重要(对我): **skill 落地 + MCP eval**——与 0903 LlamaIndex MCP `is_error` fix 同构；MCP SDK 版本升级是 agent skill eval 的 breaking change gate。
  - 明日动作(<=20min): 写 **是/否**：你是否用 MCP Python SDK 2.x + AutoGen（是/否/未知）。

- [[Bugfix] Set stop_sequence explicitly in streaming message_delta event（vLLM PR #55325, created **2026-09-04**）](https://github.com/vllm-project/vllm/pull/55325)
  - 摘要: Fixes #55324。`/v1/messages` streaming 终端 `message_delta` 在非 stop-sequence 分支未设 `stop_sequence`，`model_dump_json(exclude_unset=True)` 将其从 wire 丢弃 → schema-validating proxy 在完整 answer 交付后倒数第二个 event 拒绝 stream。Fix：`else` 分支显式设 `stop_sequence=None`。
  - 为什么重要(对我): **agent serving eval**——Anthropic-compatible agent harness 的 streaming eval 若用 schema validator，此 bug 导致系统性 stream reject。
  - 明日动作(<=20min): 写 **是/否**：你是否用 vLLM `/v1/messages` streaming + schema validator（是/否/未知）。

- [Add hash-only decode validator to GenerateRequest protocol（vLLM PR #55333, created **2026-09-04**）](https://github.com/vllm-project/vllm/pull/55333)
  - 摘要: Closes #55332。`token_in_token_out` `GenerateRequest` 接受 metadata-only multimodal request（`features.kwargs_data is None`）无校验 → worker 构造 `mm_kwargs[modality]=[None]*len(hashes)` 转发，下游 opaque `GenerationError`/`MultiModalCacheMissError`。Fix：新增 `_validate_hash_only_decode` validator，仅 local cache hit 或 `kv_transfer_params`/`ec_transfer_params` 存在时允许 hash-only decode。
  - 为什么重要(对我): **token 优化 + agent serving**——PD disaggregation 的 hash-only decode 是 prefix caching 路径；early reject = eval 错误消息可断言。
  - 明日动作(<=20min): 写 **是/否**：你是否用 hash-only multimodal decode + KV transfer（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix][Kernel] Fix top-k selection when the radix threshold bin overflows the smem stash（vLLM PR #55314, created **2026-09-04**）](https://github.com/vllm-project/vllm/pull/55314)
  - 摘要: DSA sparse-attention indexer（`persistent_topk`/`cooperative_topk`/`top_k_per_row`）的 radix-select top-k 在 stage-1 coarse threshold bin 超过 shared memory stash 容量时，按 atomic 到达顺序静默丢弃候选 → 选中集可与 `torch.topk` 不一致。Fix：oversized bin 继续 descend FP32 key bytes 直到 participant set  fit stash；选择状态标量无条件 reset；所有 stash/selection store 加 capacity guard；underfilled slot 发 `-1` 而非 stale index。覆盖 4 条 selection path（`histogram_2048/256/4096_topk` + `FilteredTopKUnifiedKernel`）。
  - 为什么重要(对我): **Talk ≥3 turns + sparse attention/indexer**——与 0903 `persistent_topk` deterministic fix 互补：前者修 tie 顺序 nondeterminism，今日修 stash overflow silent drop；sparse indexer eval 须双覆盖。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serving DSA sparse indexer + top-k tie eval（是/否/未知）。

- [[Bugfix][DSv4] Seed the -1 sentinel in the prefill sparse index workspace（vLLM PR #55299, created **2026-09-04**）](https://github.com/vllm-project/vllm/pull/55299)
  - 摘要: `combine_topk_swa_indices` 仅写 valid prefix，padding 依赖 `-1` sentinel。`out=None` 路径用 `torch.full(fill_value=-1)` seed；caller-supplied workspace 不 seed → 多 chunk prefill + workspace 轮换到未写 slot 时，padding 为未初始化内存，sparse attention kernel 将其当 KV index 读取。Repro：DeepSeek-V4-Flash warmup 256 requests + CUDA graph → `idx_max=2113501039` vs `kv_rows=2`，404,096 次 illegal access。Fix：caller-supplied path 同样 seed `-1`。
  - 为什么重要(对我): **Talk ≥3 turns + sparse attention prefill**——sparse index workspace 初始化是 attention 读路径的前置条件；uninitialized padding = long-context prefill eval 在 CUDA graph 下 crash。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serving DeepSeek-V4 sparse prefill + CUDA graph（是/否/未知）。

- [[Qwen3.8-Flash-Next] Fuse PLE residual and QSA output gate（vLLM PR #55309, created **2026-09-04**）](https://github.com/vllm-project/vllm/pull/55309)
  - 摘要: 将 PLE outer residual add 融合进 short-convolution kernel；将 QSA output gate 融合进 single-split attention epilogue 与 split-K merge kernel；保留 BF16/FP16 rounding boundary。移除 Qwen4Exp NVIDIA path 上两个 standalone pointwise op。B200 microbenchmark：PLE residual fused path 显著降低 kernel launch overhead。
  - 为什么重要(对我): **Talk ≥3 turns + QSA/sparse attention epilogue**——QSA 是 Qwen sparse attention 的输出门控；epilogue fusion 影响 decode latency，是 agent serving token 成本的关键路径。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serving Qwen3.8-Flash-Next QSA path（是/否/未知）。

- [fix: handle multiple KV groups in _update_requests_with_invalid_blocks（vLLM PR #55321, created **2026-09-04**）](https://github.com/vllm-project/vllm/pull/55321)
  - 摘要: `Scheduler._update_requests_with_invalid_blocks` 用 `(req_block_ids,) = get_block_ids(req_id)` 单值 unpack，但 hybrid model（attention + Mamba/GDN）返回多个 KV cache group 的 block-id list → `ValueError: too many values to unpack` → `EngineDeadError` 杀死整个 engine。触发路径：KV-connector reload/eviction + Mooncake store PD disaggregation on hybrid models。
  - 为什么重要(对我): **Talk ≥3 turns + hybrid attention/Mamba KV**——与 0903 Mamba preemption handoff 同族；hybrid KV group 是 attention 与 SSM state 的联合内存模型。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 hybrid KV cache + KV connector PD disaggregation（是/否/未知）。

- [fix: fix Heterogeneous TP bug of Mamba2 conv state（vLLM PR #55306, created **2026-09-04**）](https://github.com/vllm-project/vllm/pull/55306)
  - 摘要: Fixes #55208。NIXL 对所有 Mamba2 conv projection 按 TP ratio 缩放；`n_groups=1` 时 B/C replicated、x sharded → 一 rank 读 C 当 B，两 rank 读 temporal state 当 C。Fix：从 advertised conv size 推导 remote projection boundary；reverse gather 保留 replicated B/C；pull/push 模式各选单一 B/C writer。Falcon-H1-0.5B-Base P TP=1 D TP=2 验证通过。
  - 为什么重要(对我): **Talk ≥3 turns + Mamba conv state / attention 边界**——Mamba2 conv state 是 hybrid model 中与 attention KV 并列的状态载体；heterogeneous TP 下 silent 错读 = eval 仅 uniform TP 下 pass。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 heterogeneous TP serving Mamba2 model（是/否/未知）。

- [[Perf] Enable efficient SDPA for Granite Speech block attention（vLLM PR #55303, created **2026-09-04**）](https://github.com/vllm-project/vllm/pull/55303)
  - 摘要: Granite Speech block attention 传 5D Q/K/V 给 SDPA 并强制 math backend；PyTorch fused SDPA 要求 4D input。Fix：SDPA 前 flatten batch×block 维度，请求 `EFFICIENT_ATTENTION` + `MATH` fallback，之后 restore 原维度。Granite Speech 3.3 2B generation test 与 Transformers reference 一致。
  - 为什么重要(对我): **Talk ≥3 turns + block attention layout**——block attention 是 speech/multimodal 的 attention 变体；5D→4D reshape 是 fused kernel 可达性的前置条件，影响 multimodal agent serving 延迟。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serving Granite Speech multimodal agent（是/否/未知）。

## 3) 热门项目/可试个人项目

- [langchain-ai/langchain-skills（@hwchase17 ecosystem, updated **2026-09-04**）](https://github.com/langchain-ai/langchain-skills)
  - 摘要: LangChain 官方 21 个 agent skills 合集（**1,196 stars**），含 `eval-engineering`（Harbor Task 设计与 audit）、`langgraph-fundamentals`、`langgraph-human-in-the-loop` 等；`npx skills add langchain-ai/langchain-skills --skill eval-engineering` 一键安装。今日 repo 有 activity（`updated_at: 2026-09-04`）。
  - 为什么重要(对我): **skill 落地 + 评测**——`eval-engineering` skill 直接对标你的 agent eval playbook；21 个 skill 的结构（SKILL.md + 场景预设 + Harbor 集成）是可复用模板。
  - 明日动作(<=20min): 读 `eval-engineering` skill 的 SKILL.md 前 50 行，写 **2 条** 与你 eval playbook 的接点（各 ≤15 词）。
  - 估计复现成本: **低**（读 SKILL.md + 写 2 条接点，~15min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [hwchase17/autoresearch-agents（@hwchase17, **148 stars**）](https://github.com/hwchase17/autoresearch-agents)
  - 机会信号: @hwchase17 将 @karpathy `autoresearch` 模式移植到 agent 优化——固定 `run_eval.py` + `dataset.json`，AI coding agent 自主修改 `agent.py`（prompts/tools/architecture），LangSmith 评分，keep/discard 循环。与 0903 Karpathy autoresearch 报道形成「ML training → agent eval」完整迁移路径；是 agent eval 自动化的可 fork 起点。
  - 风险信号: 仅 148 stars，生态早期；依赖 LangSmith + 可计算 scalar metric（Karpathy 明确指出 soft eval 不适用）；需自备 eval dataset 与 evaluators。
  - 我可执行的一步(<=30min): fork repo，读 `program.md` + `run_eval.py`，写 **3 条** 你的 eval 场景能否映射到此 loop（各 ≤15 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[2950. Smallest Stable Index I](https://leetcode.com/problems/smallest-stable-index-i/)（**Day 65, Sep 2026 Daily Challenge**，承接 0903 的 2949「模 √k 约束前缀哈希」，今日 **Easy**「Array + Prefix Sum，找最小 stable index」）：关键洞察——前缀和判定 stability 条件，与 agent eval「前缀状态等价类」同构。明日动作：手写前缀和解法；写 **≤15 词**：「stable index」对你 eval 状态终止条件意味着什么。

- **IELTS（≤1 条）**：[IELTS Listening Practice Tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/listening)：0903 已轮换 Reading，今日换 **Listening**（选 1 套 Section，限时 ~10min）。明日动作：完成 1 次 Listening Section，记录 **≥2 个** 拼写/同音词失误；写 **≤15 词** 你最弱的 Listening 维度（Numbers / Spelling / Paraphrase / Speed）。

## 6) 噪音过滤记录

- **chore(model-profiles) PR（LangChain #40198）**：model profile 数据刷新，非工程信号 → 过滤。
- **fix(huggingface) translation PRs（LangChain #40194/#40199）**：HuggingFacePipeline 翻译任务，与当前 focus 弱相关 → 过滤。
- **fix(core): Support plus bullet in MarkdownListOutputParser（LangChain #40192/#40193）**：output parser 边界，偏 infra → 过滤。
- **fix(openai): Azure AD auth（LangChain #40190）**：auth fix，与当前 focus 弱相关 → 过滤。
- **fix(core): forward insert_kwargs / NLStructStore（LlamaIndex #22954）**：index insert 路径，与当前 focus 弱相关 → 过滤。
- **docs PRs（AutoGen #8184/#8181/#8180）**：CancellationToken guide / PZERO example / TLS guide → 过滤。
- **fix: utf-8 encoding / get_message_thread / http_client（AutoGen #8179/#8178/#8189）**：infra 边界 → 过滤。
- **fix: streaming tool_calls None guards（AutoGen #8188）**：#7130 OpenAI client 同 fix 的 Azure 版，signal 弱于 #8185/#8182 → 过滤。
- **fix: serialize dict/list tool outputs（AutoGen #8186）**：serialization 边界 → 过滤。
- **fix: http/https URL as image_url（AutoGen #8192）**：multimodal 边界 → 过滤。
- **[[Kernel] Gemma RMSNorm FlashInfer（vLLM #55329）**：norm kernel perf，非 attention 核心路径 → 过滤（#55311 Triton 版已被 supersede）。
- **[[Kernel][Perf] Register-resident 8-bit quant（vLLM #55330）**：quant kernel perf，偏 infra → 过滤。
- **[[Rust Frontend] max_model_len RoPE scaling（vLLM #55328）**：frontend config，偏 infra → 过滤。
- **0903 已收录项**：persistent_topk deterministic #55122、MCP is_error #22944、HITL args_schema #40165、checkpoint stale write #8811 → semantic duplicate，不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-04** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **884**（`0903.md` §7: 872 + 本日 §1: 12）
- Theory: **394**（`0903.md` §7: 388 + 本日 §2: 6）
- Career/Business: **135**（`0903.md` §7: 134 + 本日 §4: 1）
- LC: **117**（`0903.md` §7: 116 + 本日 §5: 1）
- IELTS: **117**（`0903.md` §7: 116 + 本日 §5: 1）
