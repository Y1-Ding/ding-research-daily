# Daily Research - 2026-09-16 (21:30)

## 0) 今日3条结论

- **Responses 须在渲染前预算 flatten 后的 tool 名总长度**（#57147, created **2026-09-16**）：namespace 工具名过长时，`\u003cnamespace\u003e__\u003cfunction\u003e` 复制可使 ~464 KiB 请求在进模型前膨胀 ~272 MiB → **skill/MCP 注册表 + token 预算** 的 admission-gold；eval 须含「超长 namespace 应 400 而非 OOM」。
- **streaming/non-streaming 在 tool 边界对 content 白空格处理须一致**（#57137 / #57120, created **2026-09-16**）：deferred content 与 FunctionGemma 尾部文本在两条路径上被剥掉 → **tool-call eval gold**（同一 completion，`stream=true/false` 须同 content/tool 结构）。
- **CUDA graph capture 后 null KV block 须清零**（#57158, created **2026-09-16**）：block 0 若含 NaN，`0 * NaN` 可污染整批 decode → 与 **#57094**（`seq_len=0` pad 槽 MLA decode）并列，构成 **Attention serving 的「pad/null 槽」几何-gold**。

## 1) Agent工程化（high-signal）

- [[Frontend][Responses] Reject namespace tool names that exceed the input budget（vLLM PR #57147, created **2026-09-16**）](https://github.com/vllm-project/vllm/pull/57147)
  - 摘要: 在构建 flatten 工具名之前累加投影长度；超过 `max_input_tokens × max_chars_per_token` 则 **400 拒绝**，避免 namespace 长名在多 function 上重复复制导致渲染内存爆炸（文中 ~464 KiB → ~272 MiB 例）。
  - 为什么重要(对我): **skill 落地 + token 优化**——MCP/namespace 工具注册是 agent harness 的 DoS 面；须纳入 L2「恶意/膨胀 tool schema」用例。
  - 明日动作(<=20min): 写 **是/否**：你的 harness 是否在客户端预检 flatten 后 tool 名总长度（是/否/未知）。

- [[Parser] Fix: strip content whitespace at tool-call boundaries in streaming path（vLLM PR #57137, created **2026-09-16**）](https://github.com/vllm-project/vllm/pull/57137)
  - 摘要: 非流式在 tool 边界用 `_strip_content_whitespace` 剥尾部空格；流式 `_events_to_delta` 在 `finish_streaming()` 直接吐出 deferred content，未走同一规则 → `stream` 开关改变可见 content（fixes #49412）。
  - 为什么重要(对我): **评测 + tool 落地**——streaming gold 须与 batch 对齐，否则 eval 假阳性/假阴性。
  - 明日动作(<=20min): 写 **是/否**：你的 tool eval 是否同时跑 stream/non-stream 对同一 prompt（是/否/未知）。

- [[Bugfix][Tool Parser] Keep content after FunctionGemma tool calls in non-streaming parse（vLLM PR #57120, created **2026-09-16**）](https://github.com/vllm-project/vllm/pull/57120)
  - 摘要: `FunctionGemmaToolParser` 原只保留首个 `\u003cstart_function_call\u003e` 之前文本，call 之后或 call 之间的自然语言被丢弃；流式路径曾发出完整 deltas → 同模型输出两条路径不一致。
  - 为什么重要(对我): **skill/评测**——多 tool + 后置说明是常见 agent 话术；parser gold 须覆盖「call 后仍有 content」。
  - 明日动作(<=20min): 列 **1 个** 你 gold 集里「tool 后还有一句」的样例 ID（或写「无」）。

- [[BugFix][KV Connector] Fix Deadlock with KVConnector AsyncLoad + MTP under KV Pressure（vLLM PR #57104, created **2026-09-16**）](https://github.com/vllm-project/vllm/pull/57104)
  - 摘要: async KV load 不含 lookahead slots，admission 未计入；promotion 再要 `1 + num_spec_tokens` 块 → 池耗尽后 head 每步 `allocate_slots()` 失败，后续 load 永不推进，**调度死锁**。
  - 为什么重要(对我): **长上下文 agent + spec decode + 外部 KV**——压测须观测「KV 压力下 MTP 是否 wedged」。
  - 明日动作(<=20min): 写 **是/否**：你的 serving eval 含 KV connector + spec token 组合压测（是/否/未知）。

- [[Bugfix] MambaManager.find_longest_cache_hit ignores drop_eagle_block（vLLM PR #57128, created **2026-09-16**）](https://github.com/vllm-project/vllm/pull/57128)
  - 摘要: hybrid Mamba/GDN + `--enable-prefix-caching` + native MTP/EAGLE 时，参数 `drop_eagle_block` 未生效 → 含被拒 draft 的 recurrent 块仍被 prefix 命中复用，**静默状态腐化**。
  - 为什么重要(对我): **评测 + spec decode**——prefix cache + draft 的 teardown-gold，与 0915 DSpark/EAGLE 区分逻辑（#57110）同族。
  - 明日动作(<=20min): 写 **是/否**：你是否 hybrid Mamba + prefix cache + spec decode（是/否/未知）。

- [fix(core): stop `UsageMetadataCallbackHandler` from dropping usage without `model_name`（LangChain PR #40513, created **2026-09-16**）](https://github.com/langchain-ai/langchain/pull/40513)
  - 摘要: `on_llm_end` 仅在 `response_metadata` 含 `model_name` 时记账；自定义/第三方模型缺该字段时 **token usage 静默丢失** → 现回落到 `"unknown"` 桶。
  - 为什么重要(对我): **评测 + token 成本**——agent eval 若靠 callback 聚合 cost，须测「无 model_name」路径是否仍计数。
  - 明日动作(<=20min): 写 **是/否**：你的 eval 是否断言每步 usage 非空（是/否/未知）。

- [fix(graph_stores): dedupe triplets across depths in SimplePropertyGraphStore.get_rel_map（LlamaIndex PR #23082, created **2026-09-16**）](https://github.com/run-llama/llama_index/pull/23082)
  - 摘要: `seen_triplets` 未收录首跳，环图/双向边导致各 depth 重复收集同一 triplet（fixes #23073）；与 0915 #22972「首跳占满 limit」互补，覆盖 **cycle 重复**。
  - 为什么重要(对我): **RAG/agent 评测**——graph retrieval 的 cycle + limit 双 gold。
  - 明日动作(<=20min): 写 **是/否**：graph eval 是否含 **2-node 环** 用例（是/否/未知）。

- [Claude Managed Agents：`auto` 权限策略与 `evaluation` 字段（Anthropic Platform Release Notes, **2026-09-10**）](https://docs.anthropic.com/en/release-notes/api)
  - 摘要: `permission_policy` 新增 **`auto`**：服务端逐次评估 agent/MCP tool call，执行/拒绝/暂停待批；`agent.tool_use` / `agent.mcp_tool_use` 事件带 **`evaluation` + `evaluated_permission`**，可审计每次判定。
  - 为什么重要(对我): **skill/MCP 落地 + 评测**——托管 harness 的 permission eval 可对标你本地「tool allowlist + human-in-the-loop」矩阵。
  - 明日动作(<=20min): 写 **3 条** 你本地 tool policy 能否映射为 `auto` 的 allow/deny/pause 三类（各 ≤8 词）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix][Attention] Zero the null KV block after CUDA graph capture（vLLM PR #57158, created **2026-09-16**）](https://github.com/vllm-project/vllm/pull/57158)
  - 摘要: capture 用全零 block table，block 0（`BlockPool.null_block`）可能残留非有限值；masked gather 仍会因 `0*NaN` 污染 value 累加。capture 后 **显式清零 null block**（fixes #57156）。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**——CUDA graph pad 与 null block 语义是 decode 正确性 gold。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 `cudagraph` + 多 seq batch decode（是/否/未知）。

- [[Bugfix][Attention] Avoid NaN from empty seq_len in Triton MLA decode（vLLM PR #57094, created **2026-09-16**）](https://github.com/vllm-project/vllm/pull/57094)
  - 摘要: CUDA graph 将 3-seq batch pad 到 4，`seq_len=0` 槽在 `triton_decode_attention` stage2 做 `acc/e_sum` 且 `e_sum=0` → **NaN logits**（DeepSeek-V2-Lite FP8 + TRITON_MLA；fixes #57017）。
  - 为什么重要(对我): **Talk ≥3 turns + MLA/Attention**——与 #57158 同属「无效槽」；eval 须测 `max-num-seqs≥3` graph 路径。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 FP8 + TRITON_MLA + cudagraph（是/否/未知）。

- [[GLM-5.3-Flash] Rework kpool compress and dual-stream indexer scheduling（vLLM PR #57161, created **2026-09-16**）](https://github.com/vllm-project/vllm/pull/57161)
  - 摘要: `kpool_compress` 改 **online softmax**（每 pool gate 只读一次）、支持 flat token layout（`row_offset = kpool-1`）；并重排 dual-stream indexer 调度以匹配 SGLang「assemble」语义。
  - 为什么重要(对我): **Talk ≥3 turns + sparse MLA/索引 Attention**——GLM-5.3 类模型的 kpool 是长上下文 agent 的 prefill/indexer 成本核心。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serving GLM-5.3 sparse MLA（是/否/未知）。

- [[Bugfix][MLA] Size the profile-run chunked-prefill workspace from the active backend's builder（vLLM PR #57170, created **2026-09-16**）](https://github.com/vllm-project/vllm/pull/57170)
  - 摘要: `chunked_prefill_workspace_size` 原总用 dense MLA 公式，与实际 backend builder 不一致 → profile run 过度分配 workspace；改为 **按活跃 backend 的 builder 取值**。
  - 为什么重要(对我): **Talk ≥3 turns + chunked prefill**——profile 与 runtime 几何不一致会误导 capacity planning。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 MLA + chunked prefill 长 prompt（是/否/未知）。

- [[KV Cache] GLM-5.3-Flash: generic packed KV layout; CircularBufferSpec tail with speculative ring slots（vLLM PR #57169, created **2026-09-16**）](https://github.com/vllm-project/vllm/pull/57169)
  - 摘要: 重构 GLM-5.3 KV 为 generic packed layout；composite sparse-MLA metadata 传播物理 block stride；**CircularBufferSpec** tail 支持 speculative ring slots（GB200 4× 端到端验证）。
  - 为什么重要(对我): **Talk ≥3 turns + KV/Attention 寻址**——spec ring + packed block 是 agent 长对话 + draft 的内存模型 gold。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 GLM-5.3 + DFlash2/spec ring（是/否/未知）。

## 3) 热门项目/可试个人项目

- [JudgmentLabs/judgeval — Continuous-Improvement Stack for Agents（~1062 stars, updated **2026-09-15**）](https://github.com/JudgmentLabs/judgeval)
  - 摘要: 开源 Python SDK：`@Tracer.observe()`（OTel）、**agent judges**（prompt scorers → 结构化 behaviors）、在线监控与 Slack 告警；集成 LangGraph / Claude Agent SDK 等。
  - 为什么重要(对我): **评测杠杆**——「生产 trace → judge → 回归集」可映射 map_prompt_test 的 collect→eval 闭环（非白名单来源，≤20% 配额内）。
  - 明日动作(<=20min): 读 README **Quickstart** 一节，写 **1 句** 能否替换你现有 L2 judge 入口（≤25 词）。
  - 估计复现成本: **中**（需 Judgment 账号 + 接一条现有 agent trace，~2h）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Claude Platform：`ant apply` 与 `claude-lock.json`（Anthropic Release Notes, **2026-09-03**）](https://docs.anthropic.com/en/release-notes/api)
  - 机会信号: **`ant apply`** 从仓库文件创建/更新 agents、environments、**skills**、memory stores、deployments；**lockfile** 保证本机/CI 更新同一资源 → **agent 产品可 GitOps**，利于个人公司卖 vertical harness + 可复现部署。
  - 风险信号: 绑定 Anthropic 托管面；与 LangChain Deep Agents Deploy（0428）等开源替代品的 **model/host 中立性** 须并行维护。
  - 我可执行的一步(<=30min): 列 **2 个** 你仓库里最适合迁到「文件描述 + apply」的 artifact（skill 定义 / eval task 各 1，≤15 词/条）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[Number of Sets of K Non-Overlapping Line Segments](https://leetcode.com/problems/number-of-sets-of-k-non-overlapping-line-segments/)（**2026-09-16 Daily Challenge**，LeetCode GraphQL `activeDailyCodingChallengeQuestion`，Medium）：区间 DP / 组合计数；与 agent eval「选 K 条非重叠子轨迹最大化覆盖」同构。明日动作：完成 Medium 一题或写状态定义草图；**≤15 词**「非重叠线段集」对你 trace 切片意味着什么。

- **IELTS（≤1 条）**：[IELTS Writing practice tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/writing)：0915 为 Listening，今日 **Writing Task 2**（选 1 题，~30min）。明日动作：写 1 段 introduction（≥150 词）；标 **2 个** 语法/衔接失误类型；**≤15 词**你最弱的 Writing 维度。

## 6) 噪音过滤记录

- **test-only / chore（LangChain #40514–#40516、#40494–#40497、#40500；AutoGen #8241/#8242）**：单测与文档链接 → 无当日 agent 主信号 → 过滤。
- **ROCm/XPU/MM/模型上新杂项（vLLM #57154–#57172 中 LayerNorm SYCL、Pixtral compile、MoE routing 等）**：非当前 focus → 过滤。
- **LangGraph 当日无 agent 向 PR**；仅 deps bot merge（#7192/#6814）→ 过滤。
- **openai/evals 当日无 PR** → 过滤。
- **0909–0915 已收录项**：vLLM compaction #56970、MCP cleanup #56988、Triton metadata #56996、LlamaIndex #22972/#22621、OpenAI Astra skills 博文、Anthropic alignment Sep-9、Simon Sep-12 compaction 等 → semantic duplicate，不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-16** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + Anthropic Docs ≥8 条主信号，无需 skill 补充。

## 7) 本周累计计数

- Agent: **993**（`0915.md` §7: 985 + 本日 §1: 8）
- Theory: **458**（`0915.md` §7: 453 + 本日 §2: 5）
- Career/Business: **148**（`0915.md` §7: 147 + 本日 §4: 1）
- LC: **129**（`0915.md` §7: 128 + 本日 §5: 1）
- IELTS: **129**（`0915.md` §7: 128 + 本日 §5: 1）
