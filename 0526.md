# Daily Research - 2026-05-26 (21:30)

> 采集口径：优先 **X_WHITELIST / YOUTUBE_WHITELIST / GITHUB_WHITELIST / BLOG_WHITELIST / CAREER_BUSINESS_WHITELIST**；评分 `relevance(40)+actionability(25)+credibility(20)+freshness(15)`，正文主段 **≥70**（职业池 **≥65** 且具可执行信号）。与 `0525.md` 及更早文件做 **URL 去重** + **语义去重**。本日 **`[OUTSIDE_WHITELIST]` 0 条** → 主链 **0%（≤20%）**。  
> **自动化提示（给编排器）**：若 Cron 仍为 UTC 触发，请将等价目标对齐 **Asia/Shanghai 每日 21:30**；失败 **重试 1 次**；连续 **2** 个日历日失败再发告警（本仓库文件不承载告警状态机）。

## 0) 今日3条结论

- **流式治理进入「顺序契约」时代**：LangChain 允许 **middleware 注册 `StreamTransformer`**（[`langchain-ai/langchain#37591`](https://github.com/langchain-ai/langchain/pull/37591)），LangGraph 再补 **`before_builtins`** 让 **PII/内容过滤** 能在 `MessagesTransformer` 固化投影 **之前**改写 `delta.text`（[`langchain-ai/langgraph#7882`](https://github.com/langchain-ai/langgraph/pull/7882)）——你做 **可观测性 + 合规** 时，要把「谁能先看到明文 token」写成 **显式顺序表**，而不是只靠 `wrap_model_call`。
- **评测基础设施也会吃供应链攻击面**：`openai/evals` 的 Flask/Playwright 路径里，用户可控 `command` 曾直接进入日志与异常参数，存在 **log injection** 风险（[`openai/evals#1671`](https://github.com/openai/evals/pull/1671) 描述）——你自建 **sandbox/eval runner** 时，应默认假设 **任何用户字段**都可能进 **日志/指标/错误聚合**。
- **「实验室外监督」正在成为可引用的公共叙事**：Anthropic 同日释放两条互补证据链——**Chris Olah 在梵蒂冈的完整讲稿**（[`anthropic.com/news/chris-olah-pope-leo-encyclical`](https://www.anthropic.com/news/chris-olah-pope-leo-encyclical)）与 **扩大对话** 文章里对 **道德形成 / Constitution / 评测** 的闭环表述（[`anthropic.com/news/widening-conversation-ai`](https://www.anthropic.com/news/widening-conversation-ai)）——对你意味着：产品层面的 **价值对齐** 不能再只写成内部 wiki，而要能映射到 **可审计的外部对话记录**。

## 1) Agent工程化（high-signal）

- [feat(langgraph): add `before_builtins` opt-in for stream transformers（`langgraph` PR #7882）](https://github.com/langchain-ai/langgraph/pull/7882)
  - 摘要: 为 `StreamTransformer` 增加 `before_builtins: ClassVar[bool]`；当为 `True` 时，**mux 先把该 transformer 注册到内置 transformer 之前**，从动机上解决 **PII 脱敏/内容过滤** 发生在 `MessagesTransformer` 已快照 `delta.text`/`delta.reasoning` 之后的「太晚」问题；并文档化 **可安全原地改写** 的字段边界与 foot-gun（以 PR 描述为准）。
  - 为什么重要(对我): 你把 **流式输出**接进 **客服/金融/医疗** 场景时，「先投影后脱敏」等价于 **合规事故已发生**；这条 PR 把问题从 prompt 层推进到 **运行时事件顺序**。
  - 明日动作(<=20min): 用中文写 **≤40 词**：列出你当前 stack 里 **1 条**会消费 `run.messages`/`messages` 流式事件的下游（UI/日志/SDK 三选一），并标注它是否 **假设**已脱敏（是/否）。

- [feat(langchain): register stream transformers on middleware（`langchain` PR #37591）](https://github.com/langchain-ai/langchain/pull/37591)
  - 摘要: `AgentMiddleware` 增加 `transformers` 属性；`create_agent` 在编译期合并 **middleware 注册的 transformer 工厂** 与调用方传入的 `transformers=`；并给出相对 `ToolCallTransformer`、middleware、caller 的 **合并顺序**（以 PR 描述为准）。
  - 为什么重要(对我): 让你把「横切关注点」（脱敏、审计、采样、限长）从 **散落的 callback** 收敛为 **与 middleware 同生命周期的流式组件**，降低 **多团队改同一 agent** 时的组合爆炸。
  - 明日动作(<=20min): 画 **1 条**从 `HumanMessage` 到「最终 SSE」的箭头链，标出你愿意放在 **middleware transformer** vs **LangGraph node** 的 **唯一分界理由**（中文 ≤35 词）。

- [fix(langgraph): keep tool results out of v3 messages（`langgraph` PR #7838）](https://github.com/langchain-ai/langgraph/pull/7838)
  - 摘要: 在 v3 `run.messages` 流式路径中 **过滤 `ToolMessage`**，避免工具结果以 **chat text delta** 形式泄漏；并规范化 `ToolCallStream.output` 的序列化/反序列化语义，附 **可复现脚本** 与回归测试说明（以 PR 正文为准）。
  - 为什么重要(对我): 工具返回 **JSON/隐私字段** 若混进「模型 token 流」，会同时污染 **用户可见 UI** 与 **LLM-as-judge / 安全监控** 的输入分布。
  - 明日动作(<=20min): 在你的 **1 条**最小 agent trace 里，标注 **tool output** 当前从哪个 channel 读出（`tool_calls` / `messages` / 自定义 event 三选一）。

- [fix: sanitize user-supplied command before logging to prevent log injection（`openai/evals` PR #1671）](https://github.com/openai/evals/pull/1671)
  - 摘要: 针对 `evals/elsuite/multistep_web_tasks/docker/flask-playwright/app.py`：`_execute_command()` 将请求体里的 `command` **原样写入日志**可能导致 **换行注入伪造日志行**；补丁以 `safe_command` 形式 **替换 `\n`/`\r`** 后再记录，同时保留原始 `command` 给 API 响应（以 PR 描述为准）。
  - 为什么重要(对我): 你的 **agent benchmark / 远程浏览器评测** 若复用同类模式，会把 **供应链最薄弱环节** 从模型移到 **runner 可观测性**。
  - 明日动作(<=20min): 检查你任一服务里 **1 处** `logger.*(f"...{user_input}")`，判断是否存在 **换行/控制字符** 风险（只写结论：需修/可接受 + **1 句**理由）。

- [Find and fix your agent's failures with LangSmith Engine（LangSmith 文档）](https://docs.langchain.com/langsmith/engine)
  - 摘要: 描述 **LangSmith Engine（Beta）** 如何把生产 trace **聚类为 issue**、给出根因诊断、提议 **evaluator/数据集补丁**，并在连接仓库时支持 **生成 PR**；并提示 **trace 数据会进入托管推理** 等合规开关（以页面章节为准）。
  - 为什么重要(对我): 这是把 **「监控 → 变更 → 防回归 eval」**写成闭环产品能力的入口页，适合你对照自研 **issue miner** 缺哪一段（聚类/证据包/PR 门禁）。
  - 明日动作(<=20min): 用中文写 **≤35 词**：定义你团队里 **issue** 的最小字段集（标题/严重级别/repro trace id/**owner** 四选三必填）。

- [Running agents in production with Google’s Gemini Managed Agents（W&B Report）](https://wandb.ai/byyoung3/bug-finder/reports/Running-agents-in-production-with-Google-s-Gemini-Managed-Agents--VmlldzoxNjk1NDIyMQ)
  - 摘要: 以 **Gemini Managed Agents** 搭建 **bug-finder/repo auditor** 类 agent；强调 **真实仓库 + 真命令** 才能把失败变成可诊断 trace，并用 **Weave** 把长时运行过程 **结构化记录**（以报告小节为准；页面标注 **Last updated: May 21, 2026**）。
  - 为什么重要(对我): 把「**托管 agent + 证据链 trace**」落到 **可抄作业** 的端到端范式，便于你对照 **LangSmith / 自研 orchestrator** 的缺口。
  - 明日动作(<=20min): 列出你 **1 个**内部仓库上最适合做 **只读 auditor** 的检查项（依赖漏洞/PII 模式/数据泄漏 三选一），写 **≤20 词**成功判据。

## 2) 原理深化（Attention/Transformer/Agent）

- [Anthropic co-founder Chris Olah's remarks on Pope Leo XIV's encyclical "Magnifica humanitas"（Anthropic News）](https://www.anthropic.com/news/chris-olah-pope-leo-encyclical)
  - 摘要: **2026-05-25**；Olah 以实验室共同创始人身份陈述：**前沿实验室的激励结构会与「做对的事」冲突**，因此需要 **激励体系之外** 的监督者与批评者；并描述 **AI 并非像桥梁那样被完全设计理解**、而是 **在近似脑的结构上「生长」**，从而把问题从纯 CS 扩展到 **人文/哲学/社会**（以页面全文为准）。
  - 为什么重要(对我): **三回合追问链**：(1) 你的 **安全/对齐预算**是否默认「实验室自我监督足够」？(2) 你是否把 **模型内部机制不可完全解释**写进了 **产品责任边界**（对客条款/事故响应）？(3) 你对 **全球南方收益分配** 是否只有 PR 叙事而没有 **工程/数据路径**？
  - 明日动作(<=20min): 从文中抄 **1 句**英文原话（≤25 词）作为你 README 的 **「外部监督」** epigraph，并加 **1 句**中文注释说明其映射到你系统的 **具体外部方**（法务/红队/用户理事会 三选一）。

- [Widening the conversation on frontier AI（Anthropic News）](https://www.anthropic.com/news/widening-conversation-ai)
  - 摘要: **2026-05-19**；阐述 Anthropic 如何与 **15+** 宗教/跨文化群体对话，并把议题连接到 **Claude Constitution、训练塑造的 character、以及评测集应覆盖的行为空间**；文中给出一个 **实验性**方向：让模型在关键行动前可调用工具 **回顾自身伦理承诺**，并在若干 **internal alignment evaluations** 上报告 **misaligned behavior** 比率下降（以页面表述为准，属早期实验叙事）。
  - 为什么重要(对我): **三回合追问链**：(1) 你的 agent 是否只有 **静态 system prompt** 而没有 **可版本化的「价值检查点」**？(2) 你是否能把「价值观提醒」落实为 **可观测事件**（tool call / structured log）而非玄学 prompt？(3) 你的 offline eval 是否覆盖 **sycophancy / conflict of interest** 类压力场景？
  - 明日动作(<=20min): 给你现有 agent 增加 **1 个**「低成本自检」接口（伪代码或中文 ≤40 词）：输入=即将执行的高风险动作描述；输出=**允许/拒绝/需人类** 三态之一。

## 3) 热门项目/可试个人项目

- [Add Logic Stress Stress-test Suite (v2, v3)（`openai/evals` PR #1622）](https://github.com/openai/evals/pull/1622)
  - 摘要: 提案新增 **Logic Stress** 两个数据集；其中 **Variant 3 (Contradiction)** 声称评估 **「Logic Inertia」**：模型在 **显式矛盾规则** 下仍盲跟规则的现象（以 PR 标题与首段描述为准）；PR 仍处于 **open** 状态，且作者在 **2026-05-22** 评论中解释 CI 失败与 **Python 3.9 / spacy / thinc** 依赖链相关，并引用 **#1567/#1648**（以 PR 讨论为准）。
  - 为什么重要(对我): 这类 **「规则-例外」鲁棒性** benchmark 可直接映射到你 agent 的 **policy layer** 与 **tool permission** 设计。
  - 明日动作(<=20min): 用中文写 **≤30 词**：举 **1 个**你业务里可能出现的 **「书面规则 vs 明显例外」**场景（退款/权限/合规 三选一占位）。
  - 估计复现成本: **低**（仅阅读 PR 与数据结构）；若要 **本地跑通 `openai/evals` CI**，则 **中–高**（依赖链/解释器版本可能卡住）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [The Top 100 Gen AI Consumer Apps — 6th Edition（a16z）](https://a16z.com/100-gen-ai-apps-6/)
  - 摘要: a16z 对 **消费级 GenAI 应用排行** 的第六版叙事：强调 **horizontal agents**、**计算机使用/连接器**、以及 **分发与增长**正在与基础模型能力 **解耦**（以文章章节为准；具体排名数字若引用请以页面为准）。
  - 机会信号: **「执行层 agent」**与 **「分发/工作流入口」**正在分层；对个人 builder，仍存在 **垂直场景 + 强工作流** 的卡位窗口，而不必卷 **通用聊天**。
  - 风险信号: **横向 agent** 易陷入 **平台政策/自动化滥用** 与 **获客成本飙升**；文章也暗示 **开源爆款 → 被收购 → 产品化路线不确定** 的路径依赖。
  - 我可执行的一步(<=30min): 打开该文 **Agents** 小节，记录 **2** 个产品名（原样英文），并各写 **1 句**「它解决的 **workflow** 是 ______」（中文 ≤40 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：阅读 LeetCode Explore 卡片 [`Binary Search`](https://leetcode.com/explore/learn/card/binary-search/) 的 **Introduction**：把 **「单调性 + 不变区间收缩」**当作你调试 **agent 决策搜索** 的心智模型。明日动作：用中文写 **≤30 词**：把 **search space** 映射为你系统里的 **1 个**对象（**hyperparameter** / **retrieval k** / **tool 候选集大小** 三选一）。

- **IELTS（≤1 条）**：浏览官方聚合页 [IELTS preparation resources](https://ielts.org/take-a-test/preparation-resources)（页面列有 **sample tests / webinars / apps / videos** 等入口）。明日动作：用中文写 **≤35 词**：从页内任选 **1 类**资源（**sample tests** / **webinars** / **apps** 三选一），写你下周要完成的 **1 个**最小动作（例如 **完成 1 套限时 Academic sample**）。

## 6) 噪音过滤记录

- **`https://github.com/langchain-ai/langgraph/releases/tag/checkpoint%3D%3D4.1.1`**、**`https://github.com/langchain-ai/langgraph/pull/7892`**：与 `0525.md` §1 **URL 去重**（checkpoint `lc:2` 安全面已在昨日收口）。
- **`https://alignment.anthropic.com/2026/sleight-bench/`**、**`https://genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026/`**：与 `0525.md` §2 **URL 去重**（监控盲区 + OWASP agentic 仍有效，但不在本日复读占带宽）。
- **`https://www.youtube.com/watch?v=jWy39wavbjY`**（Interrupt 26 Keynote）：与 `0525.md` §1 **URL 去重**。
- **`https://www.langchain.com/blog/introducing-langsmith-engine`**、**`https://www.langchain.com/blog/how-we-built-langsmith-engine-our-agent-for-improving-agents`**：与 `0515.md`/`0524.md` **URL/语义去重**（本日改以 **官方 Engine 文档页**承接「可执行入口」增量）。
- **`https://github.com/vllm-project/vllm/releases/tag/v0.21.0`**、**`https://github.com/run-llama/llama_index/releases/tag/v0.14.22`**：与 `0515.md`–`0522.md` **URL 去重**。
- **`https://openai.com/index/model-disproves-discrete-geometry-conjecture/`**：与 `0522.md` §2 **URL 去重**（离散几何突破叙事本周已读则优先转向 **流式治理 + eval runner 安全**）。

## 7) 本周累计计数

- Agent: **176**（`0525.md` §7: 170 + 本日 §1: 6）
- Theory: **69**（`0525.md` §7: 67 + 本日 §2: 2）
- Career/Business: **44**（`0525.md` §7: 43 + 本日 §4: 1）
- LC: **28**（`0525.md` §7: 27 + 本日 §5: 1）
- IELTS: **28**（`0525.md` §7: 27 + 本日 §5: 1）
