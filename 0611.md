# Daily Research - 2026-06-11 (21:30)

## 0) 今日3条结论

- **流式 tool call 进入「可测契约」阶段**：`langchain-core 1.4.5` 在 `ModelProfile` 新增 **`tool_call_streaming`** 并写入标准测试（#34707）；**1.4.6** 同日修复 OpenAI Chat Completions 在 **`output_version="v1"`** 下流式 tool call 从 `.content_blocks` 丢失（#35983）——你的 agent eval 应把 **「chunk 级 tool_call_chunk」** 与 **「最终聚合 tool_call」** 分开测。
- **Frontier safeguard 从「静默降效」改回「可见 fallback」**：Anthropic 在 **2026-06-11** 宣布 Fable 5 对 **frontier LLM 开发类请求** 的干预将 **可见地 fallback 到 Opus 4.8**（Simon Willison 转述 Wired / @ClaudeDevs）——对你做 **训练/infra agent eval** 意味着：需记录 **fallback 事件** 而非把降效误判为模型能力不足。
- **HITL 工具链参考实现落地**：`datasette-agent 0.2a0` 发布 **`context.ask_user()`**（暂停 turn、持久化到 DB、重启可恢复）+ **`save_query` 需人工批准**（#20）——与 LangChain **SummarizationMiddleware 触发器**、**ProviderToolSearchMiddleware 延迟加载工具** 形成 **「长对话 + 大工具集 + 人工闸门」** 同日拼图。

## 1) Agent工程化（high-signal）

- [langchain-core==1.4.5：validate tool call chunks during streaming（GitHub Release / #34707，**Published 2026-06-11**）](https://github.com/langchain-ai/langchain/releases/tag/langchain-core%3D%3D1.4.5)
  - 摘要: **`langchain-core 1.4.5`** 合并 **#34707**：在 **`ModelProfile`** 增加 **`tool_call_streaming`** 能力位；标准 **`test_tool_calling`** 在 profile 开启时验证流式 chunk 的 **`content_blocks`** 至少含 **1 个 `tool_call_chunk`**，且最终消息仍有合法 **`tool_calls`**。已 smoke 通过并 opt-in：**OpenAI、Anthropic、Fireworks、HuggingFace、OpenRouter、DeepSeek、xAI**；**MistralAI** 因 translator 暴露完整 `tool_call` 而非 `tool_call_chunk` 暂排除；**Perplexity Responses** 通过 smoke 但 profile 策略留 follow-up。
  - 为什么重要(对我): 你若有 **跨 provider 流式 coding agent**，此前 **「UI 看得到 tool chunk、eval 只测最终 message」** 会漏检 **半段 JSON args**；现可用 **profile 门控** 把 **streaming tool fidelity** 纳入 **langchain-tests 回归**。
  - 明日动作(<=20min): 列出你主模型 **1 个** provider 名；查其 **`ModelProfile.tool_call_streaming`** 是否为 true（`pip show langchain-core` + 文档或 profile 表）；写 **是/否/未查**。

- [langchain-core==1.4.6：normalize v1 streamed tool calls + tracing 版本元数据（GitHub Release / #35983、#35295，**Published 2026-06-11**）](https://github.com/langchain-ai/langchain/releases/tag/langchain-core%3D%3D1.4.6)
  - 摘要: **`langchain-core 1.4.6`**：**#35983** 修复 OpenAI Chat Completions 在 **`output_version="v1"`** 时，流式 chunk 的 **`tool_call_chunks`** 未进入最终 **`.content_blocks`**（因 v1 短路把 `content=""` 当已规范化）；改为 **仅当 `content` 已是 block list 才短路**，否则走 provider translator。**#35295** 在 tracing metadata 写入 **package version**，便于 LangSmith 排障跨版本行为差异。
  - 为什么重要(对我): 若你用 **v1 content-block 接口** 写 **统一 streaming UI/eval**，此前 OpenAI 路径会 **「tool_calls 有值、content_blocks 无 tool_call」**——与 Anthropic 路径不一致；升级后应跑 **1 条** 流式 bind_tools smoke。
  - 明日动作(<=20min): 查项目是否设 **`output_version="v1"`**（是/否）；若 **是** 且用 **ChatOpenAI 流式**，记当前 **`langchain-core` 版本** 是否 **≥1.4.6**。

- [langchain==1.3.7：`ProviderToolSearchMiddleware`（GitHub Release / #37969，**Published 2026-06-10**）](https://github.com/langchain-ai/langchain/releases/tag/langchain%3D%3D1.3.7)
  - 摘要: **`langchain 1.3.7`** 合并 **#37969**：新增 **`ProviderToolSearchMiddleware`**，让 agent 把选定工具 **延迟加载（defer）** 到 **OpenAI/Anthropic provider-native tool search** 之后；校验 searchable tool 名、仅在有 deferred tool 时注入 provider search tool、对不支持 provider **启动即 fail-fast**；保留既有 **`extras={"defer_loading": True}`** 语义；由 **Open SWE** 贡献，配套 [docs PR #4355](https://github.com/langchain-ai/docs/pull/4355)。
  - 为什么重要(对我): **大工具集 agent**（>20 tools）的 **context 污染** 与 **错误 tool 选择** 是 planning eval 常客；provider-native search 让你把 **「全量 bind」→「按需发现」** 写进 middleware 栈，而非自建 RAG-over-tools。
  - 明日动作(<=20min): 数你当前 agent **bind 了几个 tool**（整数）；若 **>10**，写 **≤20 词** 哪 **1 个** tool 最适合标 **`defer_loading: True`**。

- [datasette-agent 0.2a0：`ask_user()` 暂停恢复 + `save_query` 人工批准（GitHub Release / #20，**Published 2026-06-10**）](https://github.com/datasette/datasette-agent/releases/tag/0.2a0)
  - 摘要: **`datasette-agent 0.2a0`**：**`ToolContext.ask_user()`** 支持 yes/no、多选、自由文本；未回答时 **agent turn 挂起**，表单持久化到内部 DB，**服务重启可恢复**；回答后工具 **从头重放** 并回放已存答案（故 **`ask_user()` 须在副作用前调用**）。内置 **`save_query`** 保存 SQL 为 Datasette stored query **必须人工点 Yes**。依赖 **`llm>=0.32a3`** 的 **`PauseChain`** 与 chain resume，恢复时 SSE 事件与 live 一致。
  - 为什么重要(对我): 这是 **「工具中途问人 + 副作用闸门」** 的可运行参考，比抽象 HITL middleware 更具体——你可对照自己的 **LangGraph interrupt / LangChain HumanInTheLoopMiddleware** 检查 **是否避免 placeholder ToolMessage**。
  - 明日动作(<=20min): 读 [datasette-agent #20](https://github.com/datasette/datasette-agent/pull/20) 标题与 release note **1 次**；用中文写 **≤25 词**：你的 agent **暂停时是否持久化用户问题**（是/否/未实现）。

- [fix(core,openai): normalize v1 streamed tool calls（#35983，merged 2026-06-11）](https://github.com/langchain-ai/langchain/pull/35983)
  - 摘要: PR 阐明根因：usage-only chunk 在 v1 用 **`content=[]`** 而普通 chunk 用 **`content=""`**，聚合类型不一致；**`AIMessage.content_blocks`** 对任意 **`output_version="v1"`** 短路，即使 Chat Completions 仍返回 string content。修复后流式 OpenAI tool call 的最终聚合应含规范化 **`type: tool_call`** block。
  - 为什么重要(对我): 与 **#34707 streaming 标准测试** 互补——前者定 **「测什么」**，本 PR 定 **「OpenAI v1 为何曾测不过」**；eval harness 升级时应 **两条一起锁版本**。
  - 明日动作(<=20min): 若你有 OpenAI 流式 tool 单测，标 **是否断言 `.content_blocks`**（是/否）；否 → 加 **1 条** 最小断言草稿（≤15 词）。

- [langgraph-cli==0.4.28：部署时追踪 ADK 等库版本（GitHub Release / #7939，**Published 2026-06-10**）](https://github.com/langchain-ai/langgraph/releases/tag/cli%3D%3D0.4.28)
  - 摘要: **`langgraph-cli 0.4.28`** 含 **#7939**：在 revisions 表 metadata 记录 **Google ADK version** 等 **`tracked_packages`**，便于 LangGraph Platform 识别 **ADK/其他框架混部** 部署（类比 langchainplus #22087）。
  - 为什么重要(对我): 若你用 **LangGraph CLI 部署** 且实验 **ADK 与 LangGraph 共存**，升级 CLI 后可在 **revision metadata** 核对 **实际运行时依赖**，减少 **「本地能跑、平台缺包」** 的 eval 漂移。
  - 明日动作(<=20min): 若用 LangGraph 部署，查 **`langgraph-cli` 版本**；写 **是/否** 使用 Google ADK（各 1 词）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Anthropic 撤回 Fable 5「不可见 frontier 开发 safeguard」（Simon Willison Blog，**2026-06-11**）](https://simonwillison.net/2026/Jun/11/anthropic-walks-back-policy/)
  - 摘要: Simon 转述 **Wired（Maxwell Zeff）** 与 **@ClaudeDevs**：Anthropic 将把 Fable 5 对 **frontier LLM 开发**（预训练管线、分布式训练、加速器设计等）的干预改为 **可见**；**本周起** 触发请求将 **可见 fallback 到 Opus 4.8**（同 cyber/bio 路径）；API 将返回 **拒绝原因**（server-side fallback **数日内**补齐）。公司承认 **「不可见 safeguard 是错误权衡」**。
  - 为什么重要(对我): **Agent 原理层**：模型输出 = **能力 × 策略函数 × 隐藏干预**；**0610** 已记 **steering vectors/PEFT 静默降效**，本日变更是 **可观测性回归**——**talk ≥3 turns**：(1) 你的 **训练类 prompt** eval 是否记录 **model_id / fallback 标记**？(2) **同 prompt 在 fallback 前后** 方差是否可解释？(3) 是否仍需 **Mythos/无 safeguard 通道** 作对照？
  - 明日动作(<=20min): 写 **≤35 词**：若 API 返回 **fallback 原因**，你现有日志 **能否入库**（能/不能 + 1 字段名或「无」）。

- [Running untrusted queries: Datasette/SQLite vs psycopg/PostgreSQL（Simon Willison Research，**2026-06-11**）](https://simonwillison.net/2026/Jun/11/)
  - 摘要: Simon **2026-06-11** 研究条目：对比 **Datasette+SQLite** 与 **psycopg+PostgreSQL** 执行不可信 SQL 的安全边界。**SQLite 侧**：硬只读文件模式 + VM progress handler 超时；**PostgreSQL 侧**：权限系统限制 **SELECT-only** + **`statement_timeout`** 取消睡眠/重查询。结论：两者均可防 **数据破坏与资源耗尽**，但机制不同。
  - 为什么重要(对我): **SQL tool agent** 属于经典 **「私有数据 + 不可信输入」** 组合（Simon 所称 **lethal trifecta** 子集）；原理上 **attention 窗口外的「工具沙箱」** 必须 **引擎级** 而非仅靠 prompt——与 **RAG agent** 的 **检索边界** 同构。
  - 明日动作(<=20min): 若你有 **SQL/code tool**，写 **≤25 词** 当前 **超时 + 只读** 各由 **哪一层**  enforce（DB/进程/prompt 选 1）。

- [Fable 5 safeguard 设计权衡：可见 vs 不可见（Simon 转述 @ClaudeDevs，**2026-06-11**）](https://simonwillison.net/2026/Jun/11/anthropic-walks-back-policy/)
  - 摘要: Anthropic 解释原设计：**可见 safeguard 可被 probe**，需更长时间做 robust；**不可见** 可 **更窄 targeting、更快 ship、更少 false positive**——但对 **frontier 研究用户** 造成 **「模型 rug-pull」** 感知。政策回调 **未取消该类限制**，仅 **恢复用户可见性**；Simon 认为 **最好完全取消该拒绝类**。
  - 为什么重要(对我): **多轮 agent** 中，用户会 **根据上一轮输出调整 prompt**；**不可见降效** 破坏 **马尔可夫对话假设**（用户以为状态可解释）。设计你自己的 **agent safety** 时，应默认 **任何降效/拒绝可审计**。
  - 明日动作(<=20min): 为你 **1 个** agent 写 **≤20 词** **拒绝/降效** 时用户会看到什么（弹窗/日志/无）。

## 3) 热门项目/可试个人项目

- [google/diffusiongemma-26B-A4B-it（Apache 2，Simon Willison **2026-06-10** 实测）](https://huggingface.co/google/diffusiongemma-26B-A4B-it)
  - 摘要: Google 将去年实验性 **Gemini Diffusion** 研究开源为 **Gemma Diffusion** 权重；Simon 通过 **NVIDIA NIM 免费 API** 生成文本，**~4.4s / 2409 tokens（≥500 tok/s）**。与自回归 Transformer 不同的 **扩散式 token 生成** 路径，适合探索 **非 AR 推理栈**。
  - 为什么重要(对我): 作为 **技术探索池**：若你关注 **推理延迟/并行解码**，可用 **NIM 端点** 做 **与 vLLM AR 模型** 的 **同 prompt 延迟对照**（无需本地 26B）。
  - 明日动作(<=20min): 打开 HuggingFace 模型卡 **1 次**，记 **许可证** 与 **推荐推理入口**（各 ≤5 词）。
  - 估计复现成本: **低**（云 API smoke）；本地权重 **高**（26B MoE 级资源）。

- [datasette-agent 0.2a0 — LLM agent for Datasette（GitHub，**2026-06-10**）](https://github.com/datasette/datasette-agent/releases/tag/0.2a0)
  - 摘要: 在 Datasette 上跑 **SQL 问答 + 工具链**；**0.2a0** 加 **HITL ask_user、save_query 批准、工具流式 SSE**。依赖 Datasette 1.0 alpha 的 **stored query / 权限** 能力。
  - 为什么重要(对我): **低成本的「数据 agent」个人项目**：比从零写 LangGraph **SQL harness** 更快验证 **untrusted SQL 边界 + HITL**（与今日 §2 PostgreSQL 研究同 tema）。
  - 明日动作(<=20min): `pip install datasette-agent` 或读 README **5 分钟**；写 **是否已有 Datasette 实例**（是/否）。
  - 估计复现成本: **中**（需 Datasette + LLM key；无 GPU）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Introducing Claude Corps（Anthropic Newsroom，**2026-06-11**）](https://www.anthropic.com/news/claude-corps)
  - 机会信号: Anthropic 投入 **$150M**，招募 **1000 名** 早期职业 fellow（**$85k/年 + 福利 + Claude token 预算**），**12 个月全职驻场** 美国非营利组织；与 **CodePath、Social Finance** 合作；强调 **AI 冲击劳动力市场时「收益广泛共享」**——头部 lab 正把 **「AI 技能下沉到社区」** 当 **政策/品牌资产**。
  - 风险信号: **美国本土、需工作授权、7/17 截止首批 100 人**——对 **出海/海外求职** 直接路径有限；更像 **观察 AI 劳动市场叙事** 而非远程岗位；**非营利驻场** 与 **AI solo 公司** 商业模式不同。
  - 我可执行的一步(<=30min): 用中文写 **≤40 词**：该计划对你意味着 **「lab 在买社会许可」还是「真实岗位机会」**（二选一 + 1 理由）；列 **1 个** 你可复制的 **「AI 赋能本地组织」** 微行动（≤12 词，不必在美国）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 1146. Snapshot Array](https://leetcode.com/problems/snapshot-array/)：练 **版本化数组 + 按版本二分查询**——对照 **LangGraph checkpoint 版本** 与 **datasette-agent 挂起状态持久化**。明日动作：实现后写 **≤20 词**：**`snap()` 与 `get(index, snap_id)`** 各 **O(?)**（各 1 词）。

- **IELTS（≤1 条）**：[IELTS — Prepare for your test](https://ielts.org/take-a-test/prepare-for-your-test)：官方备考资源含 **题型说明与样题**；与 **机考 1–5 天出分 / One Skill Retake 须同 delivery mode**（见 `0610.md`）连贯。明日动作：选 **Listening 或 Reading** 做 **1 套官方样题中的 1 节**（≤15 min），写 **≤15 词** 错因类型（词汇/定位/速度 选 1）。

## 6) 噪音过滤记录

- **`langchain 1.3.6` SummarizationMiddleware、`langchain-openai apply_patch`、`langchain-perplexity 1.4.0`**：与 `0610.md` **URL/语义去重**。
- **Fable 5 初体验、静默 safeguard system card 长文**：`0610.md` 已深度覆盖；本日仅收录 **2026-06-11 政策回调** 新进展。
- **`langgraph 1.2.4`、Custom Agent Harness 博客**：`0610.md` 已收录 → 丢弃。
- **Anthropic「Policy on the AI Exponential」**：新闻室列表可见但本环境 **404 正文** → 不收录。
- **LangChain「Missing Link Between Agents and Applications」博客（2026-06-10）**：slug **404** → 丢弃。
- **Wired 原文直链**：本环境 **404**；改用 **Simon Willison Blog（BLOG_WHITELIST）** 转述。
- **X_WHITELIST / YOUTUBE_WHITELIST / Bilibili**：无法稳定抓取原帖 → 不收录。
- **vLLM 0.22.1 / llama_index 5 月发布**：无 **2026-06-11** 新鲜信号 → 丢弃。

## 7) 本周累计计数

- Agent: **269**（`0610.md` §7: 263 + 本日 §1: 6）
- Theory: **107**（`0610.md` §7: 104 + 本日 §2: 3）
- Career/Business: **61**（`0610.md` §7: 60 + 本日 §4: 1）
- LC: **43**（`0610.md` §7: 42 + 本日 §5: 1）
- IELTS: **43**（`0610.md` §7: 42 + 本日 §5: 1）
