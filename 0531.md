# Daily Research - 2026-05-31 (21:30)

> 采集口径：优先 **X_WHITELIST / YOUTUBE_WHITELIST / GITHUB_WHITELIST / BLOG_WHITELIST / CAREER_BUSINESS_WHITELIST**；评分 `relevance(40)+actionability(25)+credibility(20)+freshness(15)`，正文主段 **≥70**（职业池 **≥65** 且具可执行信号）。与 `0530.md` 及更早文件做 **URL 去重** + **语义去重**。本日 **`[OUTSIDE_WHITELIST]` 0 条** → 主链 **0%（≤20%）**。  
> **自动化提示（给编排器）**：Cron 触发时区若仍为 UTC，请将等价目标对齐 **Asia/Shanghai 每日 21:30**；失败 **重试 1 次**；连续 **2** 个日历日失败再发告警（本仓库文件不承载告警状态机）。

## 0) 今日3条结论

- **vLLM ACE Phase 3 已接入推理路径**：Open PR [#42645](https://github.com/vllm-project/vllm/pull/42645) 在 **2026-05-31** 将 `req_id → session_id` 注册表与 `model_runner.execute_model()` 前后 hook 打通，使 **累积 attention 权重** 可驱动多轮 `role=tool` 消息行级淘汰（FlashAttention 默认仍回退 BM25）。
- **RAG/结构化输出观测链同日补强**：LlamaIndex [#21754](https://github.com/run-llama/llama_index/pull/21754) 保留 `StructuredLLM` 的 **provider 原始响应与 usage**；[#21812](https://github.com/run-llama/llama_index/pull/21812) 为 OpenAI Responses 补 **reasoning/thinking 独立 delta 通道**，避免思考 token 与 answer 混流。
- **LangGraph 1.2.3 / SDK 0.4.1 发布链在排队**：Open 的 [#7944](https://github.com/langchain-ai/langgraph/pull/7944) + [#7945](https://github.com/langchain-ai/langgraph/pull/7945) 将 **langgraph-sdk ≥0.4.1** 设为 RemoteGraph v3 的硬依赖——升级窗口已可提前做 **依赖与调用点清单**。

## 1) Agent工程化（high-signal）

- [feat(ace): wire Phase 3 attention hooks into model_runner（`vllm` PR #42645，**Updated 2026-05-31**）](https://github.com/vllm-project/vllm/pull/42645)
  - 摘要: **ACE（Attention-Weighted Context Eviction）** 在 chat 模板渲染前压缩旧 `role=tool` 消息行；三档评分：**累积 attention（Mode 3）> BM25 查询相关（Mode 2，默认）> 启发式（Mode 1）**。5/31 提交 `b3b583a` 新增 `ace_req_registry.py`，在 `serving.py` 注册 `request_id↔session_id`，于 `model_runner.execute_model()` 安装/刷新 `ACEModelHook`。**限制**：FlashAttention2/FlashInfer 不返回 softmax 权重 → 自动回退 BM25；CUDA graph FULL 模式跳过 hook。
  - 为什么重要(对我): 长程 agent 的 **工具结果膨胀** 可从「盲截断」升级为 **内容感知 +（在可采集 attention 时）模型自身信号**；与 `0530.md` 的 **KV 多层 offload** 互补（文本层 vs GPU KV 层）。
  - 明日动作(<=20min): 在你方 **1 个** 多轮 tool-heavy 会话，估算 **tool 消息字符占比**；写是否值得试 `context_compression="ace"` + `conversation_id`（是/否/未知）。

- [Add objectstore as a secondary tier to multi-tier kv cache offloading（`vllm` PR #41968，**Updated 2026-05-31**）](https://github.com/vllm-project/vllm/pull/41968)
  - 摘要: 在既有 **CPU + 文件系统** 多层 KV offload 上，经 **NVIDIA NIXL** 增加 **object store 二级层**（S3 兼容）；通过 `kv_connector_extra_config.secondary_tiers` 配置 bucket/endpoint/credentials，与 `0530.md` v0.22.0 的 **TieringOffloadingSpec** 叙事衔接。
  - 为什么重要(对我): 托管/混合云 agent 可将 **冷 KV** 落到 **对象存储** 而非仅本地盘，影响 **长会话恢复成本与 tail latency** 建模。
  - 明日动作(<=20min): 若你用 vLLM KV connector，列 **当前 tier 列表**（CPU/FS/其他）并标 **是否已有 S3 类后端**（有/无）。

- [Preserve raw provider responses in StructuredLLM chat（`llama_index` PR #21754，**Updated 2026-05-31**）](https://github.com/run-llama/llama_index/pull/21754)
  - 摘要: `StructuredLLM.chat()`/`achat()` 此前把 `ChatResponse.raw` 换成 **仅 Pydantic 解析结果**，丢失 **token usage 与 provider 原生对象**；现用内部 `StructuredPredictionResult` 保留源响应，公开 `structured_predict()` API 不变。覆盖 OpenAI chat-completions 与 **Responses `responses.parse`** 结构化路径。
  - 为什么重要(对我): **结构化 agent 输出** 的 **计费/调试/回放** 需与 **解析后对象** 并存——否则 eval harness 无法对齐 provider 侧日志。
  - 明日动作(<=20min): 在你方 **1 条** `StructuredLLM` 调用链，查 `response` 是否仍含 **usage**；若无，记文件路径待升级 llama-index。

- [feat(OpenAIResponse): Add reasoning delta handling（`llama_index` PR #21812，**Updated 2026-05-31**）](https://github.com/run-llama/llama_index/pull/21812)
  - 摘要: OpenAI Responses API 的 `ResponseReasoningTextDeltaEvent` / `ResponseReasoningSummaryTextDeltaEvent` 此前被忽略；现于 `process_response_event` 写入独立 **`thinking_delta`** 通道（并 emit `ThinkingBlock`），与 answer delta **严格分离**，且 **按事件** 写入 `additional_kwargs` 防止 reasoning 渗入后续 answer chunk。同步/异步流均覆盖。
  - 为什么重要(对我): **可观测的「思考流」** 是 agent UI 与 **eval 归因**（planning vs final answer）的基础能力。
  - 明日动作(<=20min): 若你用 Responses 流式 API，画 **2 通道** 数据流草图：`thinking_delta` vs `delta`（各 ≤1 句）。

- [fix(ollama): preserve streaming thinking chunks（`llama_index` PR #21820，**Updated 2026-05-31**）](https://github.com/run-llama/llama_index/pull/21820)
  - 摘要: Ollama 流式 chunk 可带 **`thinking` 无 `content`**；旧逻辑会丢弃，导致 **思考过程不可见**；修复后保留 thinking-only chunk，仍跳过无 text 无 thinking 的心跳包。Fixes #21232。
  - 为什么重要(对我): 本地/边缘 **Ollama 推理 agent** 与云端 Responses 的 **thinking 通道** 应对齐观测语义。
  - 明日动作(<=20min): 若栈含 Ollama，对 **1 个** thinking 模型跑流式并确认 UI/日志是否出现 **非空 thinking 段**。

- [release(sdk-py): 0.4.1 + release(langgraph): 1.2.3（`langgraph` PR #7944/#7945，**Open，Updated 2026-05-29**）](https://github.com/langchain-ai/langgraph/pull/7945)
  - 摘要: **#7944** 计划发布 `langgraph-sdk` **0.4.1**（自 0.4.0 的 patch）；**#7945** 发布 `langgraph` **1.2.3** 并将 sdk 约束升为 **`>=0.4.1,<0.5.0`**（RemoteGraph v3 流式依赖）。说明要求 **先合并 #7944** 再发 1.2.3。
  - 为什么重要(对我): 与 `0530.md` 已合并的 v3 decoder/RemoteGraph 形成 **可安装的版本闭环**；宜在合并前完成 **依赖矩阵** 检查。
  - 明日动作(<=20min): `pip show langgraph langgraph-sdk`（或 poetry lock），记下 **当前版本** 与 **是否使用 RemoteGraph.stream_events(v3)**。

## 2) 原理深化（Attention/Transformer/Agent）

- [ACE — Attention-Weighted Context Eviction（`vllm` PR #42645，**Updated 2026-05-31**）](https://github.com/vllm-project/vllm/pull/42645)
  - 摘要: 文内将 ACE 与 **H2O / Scissorhands（KV 级 attention 淘汰）** 对照：ACE 在 **tokenization 前的 tool 文本行** 操作；Mode 3 用 **前向 pass 累积的 per-token attention** 评分行重要性。作者称 SWE-bench Lite 上 **KV 左截断** 与 **no-evict** 准确率相近，ACE 曾因空 query BM25 bug 落后 **0.7%**（已修）。
  - 为什么重要(对我): **三回合追问链**：(1) 你的 context 压缩发生在 **哪一层**（prompt / KV / 两者）？(2) Mode 3 在你后端是否 **has_data**（SDPA/CPU vs FA2）？(3) eval 是否区分 **压缩导致的任务遗忘** vs **模型能力**？
  - 明日动作(<=20min): 写 **≤40 词** 定义你方「长会话记忆」策略：**截断 / 摘要 / ACE 类行级压缩 / RAG** 四选一为主。

- [fix(langgraph): merge instead of overwrite in ensure_config（`langgraph` PR #7926，**Merged 2026-05-29**）](https://github.com/langchain-ai/langgraph/pull/7926)
  - 摘要: `ensure_config` 对 `callbacks`/`tags`/`metadata`/`configurable` 曾 **整字段覆盖**，导致 `.with_config()` 绑定在 `Pregel.stream(..., config)` 合并时 **静默丢失**；现与 `merge_configs` 一致做 **按 key 合并**，并抽取共享 `_merge_callbacks`。
  - 为什么重要(对我): **Agent 可观测性（callbacks）与元数据** 是多 config 源叠加时的常见坑；属于 **编排层确定性** 而非模型概率问题。
  - 明日动作(<=20min): 在你方 **1 个** `graph.stream` 调用，列出 **config 来源数量**（图默认 / with_config / 调用时 config）；标 callbacks 是否可能丢失（是/否）。

## 3) 热门项目/可试个人项目

- [feat(autogen-ext): TorchAgenticPlannerAgent（`autogen` PR #7775，**Updated 2026-05-30**）](https://github.com/microsoft/autogen/pull/7775)
  - 摘要: 将 [torchagentic](https://github.com/liodon-ai/torchagentic) 的可微规划原语（`ValueIteration`、`MCTSPlanner`）封装为 `BaseChatAgent`；`planner_type="vi"|"mcts"`，当前 **随机 reward 占位**，意图对接 **可学习 reward model**。
  - 为什么重要(对我): 在 AutoGen 多 agent 图中插入 **显式规划模块**，探索 **规划与 LLM 对话环** 的分工（非端到端黑盒）。
  - 明日动作(<=20min): 读 PR **Usage** 代码块，写 **≤25 词**：该 planner 在你栈中更适合作 **子 agent** 还是 **工具节点**。
  - 估计复现成本: **中**（需 `torchagentic` extra + PyTorch；reward 仍为占位）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Anthropic defines “run-rate revenue” in two parts（Simon Willison Blog → Reuters Breakingviews，**2026-05-31**）](https://simonwillison.net/2026/May/31/)
  - 机会信号: Reuters 援引知情人士：Anthropic **run-rate** = **最近 28 天 consumption 销售 × 13** + **月订阅收入 × 12**——比单纯「最近一月 ×12」更贴近 **混合计费** SaaS；利于 **出海 B2B** 谈判时用 **同一口径** 解释客户账单与估值叙事。
  - 风险信号: 对外披露 run-rate **≠ GAAP 收入**；consumption 窗口波动会使 **年化数字敏感**；与 `0529.md` Series H **$47B run-rate** 混谈时易 **过度解读增速**。
  - 我可执行的一步(<=30min): 用中文写 **≤50 词**：为你目标客户的 **AI 用量报价** 标 **consumption vs 订阅** 占比，并写你更信 **MRR** 还是 **28 天年化**（二选一）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：LeetCode Explore [`Binary Search`](https://leetcode.com/explore/learn/card/binary-search/) 中 **Search in Rotated Sorted Array**：把「**单调段判定 + 缩区间**」映射为 agent **「在不确定状态空间里收缩搜索」**（如日志时间窗、版本区间）。明日动作：用中文写 **≤30 词**：为你 **1 个** 调试场景标 **left/right 不变量** 各是什么（各 ≤12 词）。

- **IELTS（≤1 条）**：浏览 [IELTS 官方 — Writing test format](https://ielts.org/take-a-test/test-format/academic-writing) 中 **Task 2**（discursive/argument）时长与字数要求。明日动作：用中文写 **≤35 词**：把 **Task 2 立场句** 与你 **技术方案 trade-off 段** 的 **1 个** 共用句式（自定）。

## 6) 噪音过滤记录

- **`https://www.anthropic.com/engineering/how-we-contain-claude`**：与 `0527.md`–`0529.md` **URL 去重**（Simon 5/30 转述不重复收录）。
- **`https://github.com/vllm-project/vllm/releases/tag/v0.22.0`**、**`https://github.com/langchain-ai/langgraph/pull/7928`**、**`https://github.com/langchain-ai/langgraph/pull/7935`**、**`https://github.com/langchain-ai/langgraph/pull/7927`**、**`https://github.com/langchain-ai/langgraph/pull/7923`**：与 `0530.md` §1 **URL/语义去重**。
- **`https://openai.com/index/trustworthy-third-party-evaluations-foundations/`**、**`https://www.anthropic.com/news/series-h`**：与 `0528.md`–`0530.md` **去重**。
- **`https://github.com/langchain-ai/langchain/pull/37791`**、**`#37802`**：仅 model profile 自动刷新，无 agent 架构信号 → 丢弃。
- **`https://github.com/microsoft/autogen/pull/7679`** 等 docs/encoding 修复：低信号 → 丢弃。
- **`https://github.com/openai/evals/commits/main`**：最近实质 commit **2026-04-14 量级**，无 **2026-05-31** 高信号 → 丢弃。
- **X_WHITELIST / YOUTUBE_WHITELIST / Bilibili 当日帖文**：本环境 **无法稳定抓取原帖 URL/正文** → 不收录。
- **DeepMind Blog / Lil'Log**：无 **2026-05-31** 可核验新篇单链 → 丢弃。

## 7) 本周累计计数

- Agent: **203**（`0530.md` §7: 197 + 本日 §1: 6）
- Theory: **79**（`0530.md` §7: 77 + 本日 §2: 2）
- Career/Business: **49**（`0530.md` §7: 48 + 本日 §4: 1）
- LC: **33**（`0530.md` §7: 32 + 本日 §5: 1）
- IELTS: **33**（`0530.md` §7: 32 + 本日 §5: 1）
