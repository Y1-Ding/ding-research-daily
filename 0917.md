# Daily Research - 2026-09-17 (21:30)

## 0) 今日3条结论

- **Skill 路由可进 TypeSafe 中间件栈**（LangChain PR #40544, created **2026-09-17**）：实验性 `SkillsMiddleware` 用 classifier 选 skill → **skill 落地** 从「prompt 里写目录」升级为 **可测的中间件契约**；eval 应覆盖「错分类 / 无 skill / 多 skill 冲突」。
- **HITL 拒绝不能被 `jump_to="tools"` 旁路**（LangChain PR #40540, created **2026-09-17**）：第二段 `after_model` 中间件直跳 ToolNode 会执行已拒绝的 tool call → **agent 评测 gold** 须含「HITL reject + 其他中间件 jump_to=tools」组合回归。
- **FlashInfer 去掉每步 GPU `seq_lens` 回拷**（vLLM PR #57352, created **2026-09-17**）：Attention metadata 构建不再 sync 等 GPU → **长上下文 agent 的 CPU 调度与 prefill/decode 交错** 少一类隐性 stall；与 token 预算优化同属 serving 杠杆。

## 1) Agent工程化（high-signal）

- [feat(typesafe): experimental classifier based `SkillsMiddleware`（LangChain PR #40544, created **2026-09-17**）](https://github.com/langchain-ai/langchain/pull/40544)
  - 摘要: TypeSafe 栈新增实验性 **基于分类器的 SkillsMiddleware**，与同日 #40543 `ModelRouterMiddleware`、#40545 `AutoModeMiddleware` 等同批 landing，把 skill 选择纳入 agent 中间件层而非仅依赖静态 prompt。
  - 为什么重要(对我): **skill 落地 + 评测**——skill 激活变成可插拔、可单测的中间件；L2 可测「输入意图 → 加载哪份 SKILL」。
  - 明日动作(<=20min): 写 **1 句** 你当前 harness 的 skill 选择是 prompt-only / 路由中间件 / 未知。

- [fix(langchain): don't let `jump_to="tools"` bypass HITL rejections（LangChain PR #40540, created **2026-09-17**）](https://github.com/langchain-ai/langchain/pull/40540)
  - 摘要: `HumanInTheLoopMiddleware` 用合成 `ToolMessage` 保留被拒 tool call 以维持消息合法性，但其他 `after_model` 返回 `jump_to="tools"` 会绕过过滤直进 ToolNode → 被拒 call 仍可能执行（fixes #40492）；修复后所有进 tools 的路径统一走 pending-tool 过滤。
  - 为什么重要(对我): **评测 + skill/MCP 落地**——多中间件图是生产常态；HITL 与自动路由的 **组合回归** 是门禁缺口。
  - 明日动作(<=20min): 写 **是/否**：你的 agent 图是否存在「HITL + 第二段 after_model」叠加（是/否/未知）。

- [fix(core): preserve parallel tool calls without indexes（LangChain PR #40538, created **2026-09-17**）](https://github.com/langchain-ai/langchain/pull/40538)
  - 摘要: 无 index 的并行 tool call 仅以位置为 key，跨 chunk 会互相覆盖；现 **优先用 tool call id** 作稳定 key（fixes #40392）。
  - 为什么重要(对我): **评测 + token 流式**——streaming tool eval 的 gold 结构依赖 id 稳定性。
  - 明日动作(<=20min): 写 **是/否**：你的 gold 是否断言并行 call 的 id 唯一（是/否/未知）。

- [fix(core): preserve name in on_tool_start and response in on_llm_error for AsyncBaseTracer（LangChain PR #40547, created **2026-09-17**）](https://github.com/langchain-ai/langchain/pull/40547)
  - 摘要: 异步 tracer 曾丢失自定义 tool run `name` 与流式 LLM 错误时的 partial `response`；与 sync 路径对齐（fixes #40501）。
  - 为什么重要(对我): **评测 + token 成本**——async agent 占主导时，trace 缺字段会导致 usage/行为 eval 假阴性。
  - 明日动作(<=20min): 写 **是/否**：你的 eval 是否跑在 async tracer 路径（是/否/未知）。

- [[Frontend] Support CustomToolCall in the non-Harmony Responses API（vLLM PR #57322, created **2026-09-17**）](https://github.com/vllm-project/vllm/pull/57322)
  - 摘要: `type=custom` 工具经统一 function-tool shim 进入 parser：`custom_tool_parameters` 生成 schema、grammar `format` 写入 description，xgrammar / `tool_choice` 与 custom 类型一致（#56285 拆分首 PR）。
  - 为什么重要(对我): **skill 落地 + MCP 形态**——custom tool 是 Codex/Responses 侧与 OpenAI function tool 的桥；harness 须覆盖 custom vs function 双路径 eval。
  - 明日动作(<=20min): 列 **1 个** 你若接 Responses API 会用 custom tool 的场景（或写「无」）。

- [[Frontend] Replay Responses reasoning via encrypted_content blobs（vLLM PR #57323, created **2026-09-17**）](https://github.com/vllm-project/vllm/pull/57323)
  - 摘要: `include=["reasoning.encrypted_content"]` 时 reasoning 以 opaque blob 附在 item 上并在下轮 prompt 回放，支持 `store=false` 多轮（Codex 式）而不明文落库 reasoning 文本。
  - 为什么重要(对我): **多轮 agent + 评测**——reasoning 状态与可见 content 分离；eval 须定义 blob 回放 vs 明文 reasoning 的等价标准。
  - 明日动作(<=20min): 写 **≤15 词**：你是否需要 `store=false` 多轮 reasoning（是/否/未知）。

- [[V1][Perf] Cap detokenizer prompt-prime window for long prompts（vLLM PR #57330, created **2026-09-17**）](https://github.com/vllm-project/vllm/pull/57330)
  - 摘要: `FastIncrementalDetokenizer` 原用全 prompt 初始化 `DecodeStream`（首 token 解码 O(prompt_len)）；现仅保留尾部 **MAX_PROMPT_PRIME_TOKENS（128）** 用于增量解码上下文。
  - 为什么重要(对我): **token 优化 + 长上下文 agent**——首 token 延迟与 prompt 长度解耦，压测长 system+skill 目录时须重测 TTFT。
  - 明日动作(<=20min): 写 **是/否**：你的 agent 是否常 >8k token system prompt（是/否/未知）。

- [[Frontend] Keep output_text logprobs on streamed Responses items（vLLM PR #57326, created **2026-09-17**）](https://github.com/vllm-project/vllm/pull/57326)
  - 摘要: 流式 `output_text.delta` 累积 per-token logprobs 并挂到 done item，使 `include=["message.output_text.logprobs"]` 在流式与非流式一致（#56285 拆分）。
  - 为什么重要(对我): **评测**——confidence / 校准类 L2 judge 依赖 logprob；流式路径以前可能缺字段。
  - 明日动作(<=20min): 写 **是/否**：你的 eval 是否用 output logprobs（是/否/未知）。

- [fix(autogen-ext): enforce trust boundary on Docker volume mounts (OWASP ASI10)（AutoGen PR #8245, created **2026-09-17**）](https://github.com/microsoft/autogen/pull/8245)
  - 摘要: `DockerCommandLineCodeExecutor` 的 `extra_volumes` 可在容器内挂载宿主机敏感路径；`start()` 前新增 allowlist（work_dir）+ 敏感目录拒绝 + `..` 逃逸检测（fixes #7917）。
  - 为什么重要(对我): **skill 落地 + 代码执行 agent**——ASI10 是 agent harness 的 trust boundary gold；eval 应含恶意 volume 请求。
  - 明日动作(<=20min): 写 **是/否**：你的 code-exec sandbox 是否校验 mount 路径（是/否/未知）。

- [Demote system messages in handoff context（AutoGen PR #8244, created **2026-09-17**）](https://github.com/microsoft/autogen/pull/8244)
  - 摘要: `HandoffMessage` 携带的对端 `SystemMessage` 曾以 system 权威注入目标 agent；现于接收边界转为 `UserMessage(source="handoff")`，防止 peer 注入系统指令。
  - 为什么重要(对我): **多 agent 评测**——handoff 是常见 pattern；gold 须覆盖「对端 system 不得提升权限」。
  - 明日动作(<=20min): 写 **是/否**：你是否 multi-agent handoff（是/否/未知）。

- [fix(core): tolerate None and dict tool arguments in token counting（LlamaIndex PR #23104, created **2026-09-17**）](https://github.com/run-llama/llama_index/pull/23104)
  - 摘要: `TokenCounter.estimate_tokens_in_messages` 对 `arguments=None` 或 dict 会 `TypeError`；现 coerce 为可 tokenize 文本。
  - 为什么重要(对我): **token 优化 + 评测**——流式 tool call 中间态很常见；预算 agent 须在计数阶段容忍 partial args。
  - 明日动作(<=20min): 写 **是/否**：你是否在流式中途做 token 预算（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Perf][Attention] FlashInfer: plan without copying seq_lens from the GPU（vLLM PR #57352, created **2026-09-17**）](https://github.com/vllm-project/vllm/pull/57352)
  - 摘要: FlashInfer metadata 构建每步从 GPU 拷贝 `seq_lens` 并 sync 全部排队 GPU 工作，阻塞 CPU 准备下一步；改为用 CPU 侧 bounds 规划，消除该拷贝（Ampere/Ada 每 build；H100 等有 prefill 行时亦如此）。
  - 为什么重要(对我): **Talk ≥3 turns + Attention serving**——decode 调度与 Attention kernel 准备是交错瓶颈；agent 高 QPS 时 CPU stall 放大。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 FlashInfer + fp8 KV（是/否/未知）。

- [[Bugfix][torch.compile] Honor an explicit RoPE+KV-cache fusion request（vLLM PR #57349, created **2026-09-17**）](https://github.com/vllm-project/vllm/pull/57349)
  - 摘要: 用户设 `fuse_rope_kvcache=True` 曾被静默改回 `False`（因 `unified_kv_cache_update` 被 split 出图导致 fusion pass 无法匹配）；现尊重显式 fusion 请求并文档化与 piecewise compile 的权衡。
  - 为什么重要(对我): **Talk ≥3 turns + RoPE/KV**——compile 路径下 RoPE 与 KV 写回融合影响 prefill/decode 算子边界与正确性。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 `torch.compile` + RoPE KV fusion（是/否/未知）。

- [[Feat][Mamba2] Enable internal prefill checkpoints（vLLM PR #57329, created **2026-09-17**）](https://github.com/vllm-project/vllm/pull/57329)
  - 摘要: 将 Kimi-K3 的 internal prefill checkpoint 优化移植到 Mamba2：`mamba_cache_mode="align"` 时避免为对齐 block 边界而 **额外整段 attention forward**。
  - 为什么重要(对我): **Talk ≥3 turns + hybrid 状态/Attention**——Mamba+Attention hybrid 的 prefill 切分是长对话成本核心。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 hybrid Mamba2 serving（是/否/未知）。

- [[ROCm][DSv4.1] Read the NVFP4 compressed KV cache in the gfx950 sparse decode（vLLM PR #57344, created **2026-09-17**）](https://github.com/vllm-project/vllm/pull/57344)
  - 摘要: DeepSeek-V4.1 压缩 KV 为 NVFP4（288 B/token）+ fp8 sliding window；原仅 FlashMLA SM100 sparse decode 可读，ROCm gfx950 新增 NVFP4 row loader，按 cache spec 宽度选择记录格式。
  - 为什么重要(对我): **Talk ≥3 turns + 压缩 KV/Attention decode**——多格式 KV 是 MLA 类模型的寻址与带宽 gold。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 DSv4.1 / nvfp4 KV（是/否/未知）。

- [[Metrics] Add `vllm:kv_cache_token_usage_perc`（vLLM PR #57336, created **2026-09-17**）](https://github.com/vllm-project/vllm/pull/57336)
  - 摘要: 现有 `kv_cache_usage_perc` 按 **block** 计数，短 prompt 下单 token 占块与满块不可区分，可近 **2× 高估** 真实 KV 占用；新指标按 token 计真实 keys/values 占用率。
  - 为什么重要(对我): **Talk ≥3 turns + KV 几何**——agent 多短会话时 block 指标误导容量规划与 autoscale。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 block 指标做 agent 容量告警（是/否/未知）。

- [[Rust Frontend] Build full-output grammars from initialized reasoning parsers（vLLM PR #57340, created **2026-09-17**）](https://github.com/vllm-project/vllm/pull/57340)
  - 摘要: 严格 tool grammar 原在引擎判定 reasoning 结束后才开始；现从已初始化 Rust reasoning parser 构建 reasoning 阶段 grammar 并与可见输出 grammar 组合，支持 **FromTokenZero** 约束（叠 #55269）。
  - 为什么重要(对我): **Talk ≥3 turns + 结构化生成**——reasoning 与 tool 边界是 token 级 Attention/解码约束问题，非仅后处理。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 xgrammar 约束 reasoning+tool（是/否/未知）。

## 3) 热门项目/可试个人项目

- [langchain-ai/langchain-skills — Agent Skills for LangChain / LangGraph / Deep Agents（官方仓库, README 含 eval-engineering）](https://github.com/langchain-ai/langchain-skills)
  - 摘要: 21 个可 `npx skills add` 的技能包，含 **eval-engineering**（Harbor Task、trace 审计）、langgraph 系列与 Deep Agents quickstart；与 Cursor/Windsurf 等 Agent Skills 规范对齐。
  - 为什么重要(对我): **skill 落地 + 评测杠杆**——与 #40544 中间件路线互补：仓库侧 skill 内容 + 运行时 classifier 路由可组成可复用 harness 模板。
  - 明日动作(<=20min): `npx skills add langchain-ai/langchain-skills --skill eval-engineering --yes` 是否可跑通（是/否/未试），写 **1 句** Harbor 是否已在你的 eval 栈。
  - 估计复现成本: **低**（CLI 安装 + 读 eval-engineering SKILL，~30–60min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Native is now the future of mobile at Shopify（Shopify Engineering, **2026-09-10**）](https://shopify.engineering/back-to-native)
  - 机会信号: **coding agents 使「双端原生」成本低于维护 RN 共享栈**；业务逻辑 headless + CLI 供 agent 毫秒级迭代、Helix 式检查点与对抗审查 → **个人公司可卖「agent-native 架构迁移」方法论**（移动/垂域均可类比）。
  - 风险信号: 大厂工程预算与 Shop 12 周迁移不可直接复制；agent 产出仍须人类验收与合规（对照 Simon Willison 转引 Boris Cherny：**Claude 产码应比人类更高门禁**，2026-09-11）。
  - 我可执行的一步(<=30min): 写 **3 条** 你当前项目里哪些逻辑可抽成 headless CLI 供 agent（每条 ≤12 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[Find Two Non-overlapping Sub-arrays Each With Target Sum](https://leetcode.com/problems/find-two-non-overlapping-sub-arrays-each-with-target-sum/)（**2026-09-17 Daily Challenge**，Medium）：前缀和 + 滑窗/双段不重叠子数组；与 agent eval「两段不重叠子轨迹各命中目标指标」同构。明日动作：完成一题或写状态定义；**≤15 词**「两段不重叠」对你 trace 切片意味着什么。

- **IELTS（≤1 条）**：[IELTS Reading practice tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/reading)：0916 为 Writing，今日 **Reading**（选 1 套，限时 60min 或做 Section 1）。明日动作：完成 Section 1 并标 **2 个** T/F/NG 失误类型；**≤15 词**你最弱的 Reading 子技能。

## 6) 噪音过滤记录

- **test-only / chore（LangChain #40541 model profiles、#40539 FewShot；LlamaIndex #23107–#23112 等批量 parser 容错；vLLM #57367–#57380 ROCm/CI/XPU）**：无当日 agent/theory 主信号 → 过滤。
- **vLLM #57370/#57366 Kimi K3 reasoning 非流式截断**：偏 parser/产品边界，与 0916 tool 边界族相关但非当日 focus 增量 → 过滤。
- **vLLM #57325 joint GPU/CPU prefix lookup、#57358 sparse KV events**：运维向，非原理/评测主杠杆 → 过滤。
- **LangGraph #8948 traceback 过滤**：内部 DX → 过滤。
- **openai/evals、langgraph 当日无 agent 向 feature PR** → 过滤。
- **0909–0916 已收录项**：Anthropic alignment Sep-9、Claude managed agents Sep-10、vLLM tool whitespace #57137、null KV #57158、LangChain usage #40513 等 → semantic duplicate，不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-17** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub 当日 ≥8 条主信号，无需 skill 补充。
- **agentevals #73（graph trajectory subset/superset）**：OPEN 非合并、非当日创建 → 仅观察，未入主报告。

## 7) 本周累计计数

- Agent: **1004**（`0916.md` §7: 993 + 本日 §1: 11）
- Theory: **464**（`0916.md` §7: 458 + 本日 §2: 6）
- Career/Business: **149**（`0916.md` §7: 148 + 本日 §4: 1）
- LC: **130**（`0916.md` §7: 129 + 本日 §5: 1）
- IELTS: **130**（`0916.md` §7: 129 + 本日 §5: 1）
