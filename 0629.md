# Daily Research - 2026-06-29 (21:30)

## 0) 今日3条结论

- **`ProviderToolSearchMiddleware` 的 defer 工具反而多耗 24% input tokens（#38544，2026-06-29）**：flat `defer_loading` 未包 `namespace` 时 OpenAI Responses API 仍计费完整 schema；正确 `namespace` 封装可 **−47%**——token 优化不是「开了 defer 就行」，要验 `usage.input`。
- **LlamaIndex 补齐 tool 执行审计回调（#22185，2026-06-29）+ AutoGen `action_ref` 跨 producer 关联（#7893，2026-06-28）**：两条线都在把 **tool call → 可签名 receipt / OTel span** 做成一等公民，与 governance eval 的 ALLOW/DENY/ESCALATE 决策链可直接对接。
- **Attention 侧新信号：AsymCache 用 Multi-Segment Attention 把 KV eviction 与 GPU kernel 延迟对齐（arXiv 2606.02964）**：lossless KV 管理不再只看 LRU/位置启发式，而是 joint optimize hit rate + position-aware recomputation cost；agent serving（Continuum）上 job latency **↓18.1%**。

## 1) Agent工程化（high-signal）

- [`ProviderToolSearchMiddleware` defers tools flat 而非 `namespace` 封装——无 token 节省（#38544 opened **2026-06-29**）](https://github.com/langchain-ai/langchain/issues/38544)
  - 摘要: 14 个 tool 的 A/B/C 对照：`gpt-5.4-mini` Responses API 下 inline tools `usage.input=1992`；middleware 当前发出的 flat `defer_loading` + `tool_search` **2469（+24%）**；正确 `namespace` 封装 **1063（−47%）**。功能上 `tool_search_call` 正常，但 billed input 反而更大。
  - 为什么重要(对我): **token 优化**——skill 落地若依赖 deferred tool loading，必须验 payload 形态与 `usage.input`，否则 eval 成本与生产账单双漂移。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `ProviderToolSearchMiddleware`（是/否/未知）。

- [feat(core): add tool execution audit callbacks（LlamaIndex #22185 opened **2026-06-29**）](https://github.com/run-llama/llama_index/pull/22185)
  - 摘要: 新增 core `TOOL` callback event，payload 含 tool name、input kwargs、tool call id、output text、error status；经 shared helper 路由现有 LLM selected-tool 执行路径。Fixes #21317；workflow-agent direct tool 执行留后续 PR。
  - 为什么重要(对我): **skill 落地 + eval**——tool 执行审计是 F/C 规则引擎与 L2 grading 的落点；compact receipt 比 scrape workflow stream 更稳。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 是否记录每次 tool call 的 input/output（记/不记/未知）。

- [feat(autogen-core): add `gen_ai.agent.action_ref` to trace_tool_span（#7893 opened **2026-06-28**）](https://github.com/microsoft/autogen/pull/7893)
  - 摘要: 新增 `derive_action_ref()`：对 `agent_id, action_type, scope, timestamp_ms` 做 canonical JSON + SHA-256，生成 64-char hex；`trace_tool_span(action_ref=...)` 写入 OTel span attribute。可重算、跨 producer 一致、与 `gen_ai.tool.call.id` 互补。Closes #7850。
  - 为什么重要(对我): **eval + audit**——multi-agent / multi-runtime 场景下，同一 action 的 trace 可跨 producer 关联，是 L3 eval 门禁与 incident 溯源的基础键。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent trace 是否有跨 step 的稳定 action id（有/无/未知）。

- [fix(memory): count tokens for ToolCallBlock, ThinkingBlock, CitableBlock, CitationBlock（LlamaIndex #22153 updated **2026-06-28**）](https://github.com/run-llama/llama_index/pull/22153)
  - 摘要: `Memory._estimate_token_count()` 原先排除 `ToolCallBlock`（与 `CachePoint` 同滤），且未计 `ThinkingBlock`/`CitableBlock`/`CitationBlock`。tool-heavy agent 的 FIFO 队列实际 token 远超 `token_limit`，最终触发 provider HTTP 400 "prompt is too long"。Fixes #21950。
  - 为什么重要(对我): **token 优化 + eval**——memory 预算低估会导致 eval 与生产在相同 turn 数下行为分叉；tool_kwargs 大的 agent 首当其冲。
  - 明日动作(<=20min): 写 **是/否**：你是否监控 agent memory 的 estimated vs actual tokens（是/否/未知）。

- [[Spec Decode] Avoid redundant hidden-states gather in draft prefill（vLLM #46968 merged **2026-06-29**）](https://github.com/vllm-project/vllm/pull/46968)
  - 摘要: 部分 EAGLE/MTP draft model 复用同一 hidden states 做 sampling 与 next-step input；旧代码对 last hidden states 做两次 `torch.gather`。Fix 跳过冗余 gather。
  - 为什么重要(对我): **token 优化（spec decode）**——agent serving 的 TTFT/TPS 杠杆在 draft path；小优化在 batch 下可累积。
  - 明日动作(<=20min): 写 **是/否**：你是否用 EAGLE/MTP spec decode（是/否/未知）。

- [langchain stream_events v3 final response tokens not streamed in real-time（#38484 updated **2026-06-29**）](https://github.com/langchain-ai/langchain/issues/38484)
  - 摘要: `stream_events` v3 路径下 final response tokens 非实时流出，而是一次性到达末尾；影响依赖 streaming 做中间态 eval / UI 的 agent harness。
  - 为什么重要(对我): **eval harness**——若 L2 eval 依赖 stream chunk 时序做 early-stop 或 latency 度量，此 bug 会使 eval 与生产 streaming 契约不一致。
  - 明日动作(<=20min): 写 **是/否**：你的 eval 是否依赖 `stream_events` v3 实时 token（是/否/未知）。

- [[Bug] merge_dicts concatenates identical string metadata across streaming chunks（#38366 第六日仍 open）](https://github.com/langchain-ai/langchain/issues/38366)
  - 摘要: OpenRouter 双 terminal chunk 导致 `model_name`/`finish_reason` 翻倍（`stopstop`）；issue **2026-06-22 起 open**，**2026-06-29** 仍无 merge 的 fix PR。十余个 contributor PR 均 auto-close（external + missing-issue-link）。
  - 为什么重要(对我): **eval 元数据污染**——streaming 聚合的 `response_metadata` 若含翻倍字段，L2 grading 与 cost tracking 不可信。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否读 `response_metadata.finish_reason`（读/不读/未知）。

- [tools: add GuardrailProvider protocol for tool call interception（AutoGen #7881 ready for review，**2026-06-28** 无新 update）](https://github.com/microsoft/autogen/pull/7881)
  - 摘要: `ALLOW`/`DENY`/`MODIFY` 三态 + per-tool guardrail chain，在 `run_json` 执行前拦截。**2026-06-29** PR 状态无新变化，仍 ready for review。
  - 为什么重要(对我): **skill 落地 + eval**——与 healthcare governance eval 三元决策线对齐；持续追踪 merge 状态。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent tool 拦截层映射到哪三态（ALLOW/DENY/其他/无）。

## 2) 原理深化（Attention/Transformer/Agent）

- [AsymCache: Multi-Segment Attention for Efficient KV-Cache Management（arXiv 2606.02964）](https://arxiv.org/html/2606.02964)
  - 摘要: lossless KV 管理三组件：(1) **Multi-Segment Attention (MSA)** 将 attention 分解为多个非连续 segment，精细控制哪些 KV block 参与 GPU 计算；(2) **Computational-Aware Block Evictor** 联合优化 hit rate 与 position-aware recomputation cost；(3) **Chunking Scheduler** 动态调整 prefill chunk。相对 SOTA：TTFT **1.90–2.03×**、TPOT **1.62–1.71×**；接入 Continuum agent serving 后 job latency **↓18.1%**。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) 为何 LRU/位置启发式 eviction 忽略 block 对 attention kernel 延迟的边际贡献？(2) non-contiguous segment attention 与 vLLM prefix cache 的 block 粒度如何共存？(3) agent 多 session 并发下，eviction 决策是否应纳入 L3 eval 门禁？
  - 明日动作(<=20min): 写 **≤10 词**：你 agent serving 是否做 KV eviction（是/否/未知）。

- [Recent Developments in LLM Architectures: KV Sharing, mHC, and Compressed Attention（Sebastian Raschka，2026）](https://magazine.sebastianraschka.com/p/recent-developments-in-llm-architectures)
  - 摘要: 梳理 DeepSeek V4 等模型的 **shared KV cache**（later layer 复用 earlier layer 的 K/V）与 **CSA/HCA compressed attention**（沿 sequence 维度压缩 KV entry 数，区别于 MLA 的 per-token 压缩）。agent/reasoning 长 trace 下 KV 内存与 attention 成本是主约束。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) shared-KV 与 sequence-compression 分别压缩什么维度？(2) 对你的 agent eval，prefill vs decoding 阶段哪侧 KV 压力更大？(3) 模型侧压缩与 serving 侧 AsymCache/IntentKV 是正交还是互斥？
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否区分模型侧 vs serving 侧 KV 优化（区分/不区分/未知）。

## 3) 热门项目/可试个人项目

- [langchain-ai/agentevals](https://github.com/langchain-ai/agentevals)
  - 摘要: LangChain 官方 agent eval 工具包：`create_trajectory_match_evaluator` 支持 strict/unordered/subset/superset 模式；`create_trajectory_llm_as_judge` 做 trajectory 质量评判。输入可为 OpenAI dict 或 LangChain `BaseMessage` 列表。
  - 为什么重要(对我): **eval harness 参考**——trajectory match + LLM-as-judge 可直接挂到你的 collect→test→eval 流水线，与 F/C 规则 eval 互补。
  - 明日动作(<=20min): `pip install agentevals`；写 **≤15 词** 你想先测 strict 还是 subset 模式。
  - 估计复现成本: **低**（纯 Python，无 GPU）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Human Agent in the loop — Jon Udell 引述（Simon Willison Blog，**2026-06-28**）](https://simonwillison.net/2026/Jun/28/jon-udell/)
  - 机会信号: **agent 协作范式**——反对 "human in the loop" 的被动叙事，主张 "our loop, we invite agents in"；agent-assisted 不必是黑箱 prompt→feature，可保持人类可审查的 org code 结构。
  - 风险信号: 若 agent 产出 unreviewable PR / 不可审计 trace，人类反而被排除在 loop 外；与当前 industry "全自主 coding loop" 叙事存在张力。
  - 我可执行的一步(<=30min): 写 **≤20 词** 你当前 agent 产出是否可被人类逐步审查（能/不能/部分）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 117 Populating Next Right Pointers in Each Node II](https://leetcode.com/problems/populating-next-right-pointers-in-each-node-ii/)：任意二叉树每层 `next` 指向右邻；O(1) 空间用已建立的 `next` 指针逐层连接（与 116 完美二叉树版对比）。明日动作：手写 O(1) 空间版（≤20min）；写 **≤15 词**：非完美树逐层 `next` 对你 agent 状态图意味着什么。

- **IELTS（≤1 条）**：[IELTS Speaking — Describe a skill you want to learn（British Council 样题）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-practice-tests/speaking)：Part 2 用 1 分钟准备 + 2 分钟陈述（what / why / how / feeling）；fluency 与 coherence 权重高于词汇炫技。明日动作：用 "agent eval methodology" 脱敏练 2 分钟独白（≤20min）；写 **≤15 词** 弱项（fluency/词汇/结构 选 1）。

## 6) 噪音过滤记录

- **LangChain #38510/#38512/#38511**：`0628.md` 已追踪 auto-close 未 merge；**2026-06-29** 无新 PR → 不重复收录细节。
- **vLLM #45237 spec-dec padding**：`0621.md` 已收录；**2026-06-29** 仅 metadata update → 不重复。
- **openai/evals #1684**：pip install 文档澄清，与 agent eval 主线弱相关 → 丢弃。
- **openai/evals #1683**：化学方程式 label 修正 → 丢弃。
- **Self-Harness（hwchase17，Jun 23）**：`0623–0626` 多日已覆盖 weakness-mining loop → 不重复。
- **Datasette Agent 0.3a0（Simon Willison，Jun 15）**：无 **2026-06-29** 新 release → 不重复。
- **Karpathy 10-rule CLAUDE.md 传闻（Jun 28 媒体）**：出处未获 Karpathy 本人确认 → 不收录。
- **bullpeng72/Agent-Evaluator**：非 whitelist repo，信号与 agentevals 重叠 → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-06-29** 原帖 → 不收录。
- **@gefei55 职业内容**：第三方聚合无法验证原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **418**（`0628.md` §7: 410 + 本日 §1: 8）
- Theory: **175**（`0628.md` §7: 173 + 本日 §2: 2）
- Career/Business: **79**（`0628.md` §7: 78 + 本日 §4: 1）
- LC: **61**（`0628.md` §7: 60 + 本日 §5: 1）
- IELTS: **61**（`0628.md` §7: 60 + 本日 §5: 1）
