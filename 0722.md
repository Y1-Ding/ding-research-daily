# Daily Research - 2026-07-22 (21:30)

## 0) 今日3条结论

- **AutoGen 7/22 三连 bug 暴露 multi-turn eval 脆弱面**：`TokenLimitedChatCompletionContext` 截断后 orphan `FunctionExecutionResultMessage`（#7986）、cancel 时 `CancelledError` 未捕获导致 consumer deadlock（#7987）、`MessageFilterAgent` 按 config 而非时间序返回（#7985）——三者叠加会让 eval harness 在「长对话 + 工具取消 + 消息过滤」场景产生假阴/假阳；collect 层应断言 tool call/result 成对与 cancel 后 loop 终止。
- **vLLM sparse MLA attention 修复链推进**：#49294（empty MLA context chunk merge 用 `-inf` LSE mask）已于 **7/22 merged**；#47629（`TRITON_MLA_SPARSE` SM80/SM121 fallback）同日 rebase 更新——承接 0721 #49196 revert 后的 correctness 修复方向；长上下文 agent serving 应单独跟踪 PCP/MLA 路径回归。
- **Claude Code 团队 dogfood 信号：lean prompt = +10–15% eval & -41–66% tokens**：Simon Willison 7/21 对谈披露——系统 prompt 瘦身 80%、减少 "don't do X" 硬约束、用 eval 信任阶梯替代全人工 review；对你：skill 落地优先减 prompt 体积 + 建 behavioral eval，而非堆示例。

## 1) Agent工程化（high-signal）

- [fix(core): support positional-only parameters in tools（LangChain PR #39012, updated **2026-07-22**）](https://github.com/langchain-ai/langchain/pull/39012)
  - 摘要: PEP 570 位置仅限参数（`def f(a, /, b)`）注册为 tool 时，`create_schema_from_function` 会泄漏 phantom `v__positional_only` 字段到 schema，LLM 可能尝试填充不存在的参数；调用时 keyword 传参触发 `TypeError`。Fix：strip phantom 字段 + `_split_positional_only` 在 `_run`/`_arun` 中将位置仅限参数转为 positional 传递。
  - 为什么重要(对我): **skill 落地 + 评测**——skill tool 函数若用位置仅限参数（常见 Python 风格），schema 污染 = eval 假阳性（模型填 phantom 参数）或 invoke 崩溃；harness 应覆盖 positional-only tool 注册路径。
  - 明日动作(<=20min): 写 **是/否**：你 skill tools 是否有 positional-only 参数（是/否/未知）。

- [feat(openai): expose vLLM `stop_reason` metadata（LangChain PR #39008, updated **2026-07-22**）](https://github.com/langchain-ai/langchain/pull/39008)
  - 摘要: vLLM OpenAI-compatible Chat Completions 响应含 per-choice `stop_reason`，但 `ChatOpenAI` 此前仅复制 `finish_reason`。Fix：streaming/non-streaming 均将 `stop_reason` 写入 `AIMessage.response_metadata`，保留 token ID `0` 与显式 `None`。
  - 为什么重要(对我): **评测 + token 优化**——stop sequence/token 是可观测信号，multi-turn eval 可区分「自然结束 vs stop token 截断 vs length limit」；此前丢失 = eval 归因盲区。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否记录 `stop_reason`（是/否/未知）。

- [Fix SlidingWindowRateLimiter wait time calculation for accumulated token usage（LlamaIndex PR #22419, updated **2026-07-22**）](https://github.com/run-llama/llama_index/pull/22419)
  - 摘要: `SlidingWindowRateLimiter._wait_time()` 在累计 token 用量超过 `allowed_tokens` 时，若 `_token_usage` 中无足够 entry 覆盖剩余 needed tokens，会返回 `wait=0.0` 而非等待 entry 过期。Fix：添加 fallback wait 计算 + 单元测试 `test_sliding_window_wait_time_accumulated_tokens`（46 passed）。
  - 为什么重要(对我): **token 优化**——rate limiter 静默返回 0 wait = 突发超配额请求不被节流 = eval batch 跑时 token 成本/429 不可预测；agent eval harness 若用 sliding window limiter 应测 accumulated overflow 路径。
  - 明日动作(<=20min): 写 **是/否**：你是否用 sliding window token rate limiter（是/否/未知）。

- [fix(agent): copy initial_state in AgentWorkflow to prevent cross-run leaks（LlamaIndex PR #21800, updated **2026-07-22**）](https://github.com/run-llama/llama_index/pull/21800)
  - 摘要: `AgentWorkflow._init_context` 直接存储 `self.initial_state` 引用，agent run 中 state dict 变异会泄漏回 `initial_state`，后续 run 以脏状态启动。Fix：`.copy()` 对齐 `BaseWorkflowAgent._init_context` 已有模式 + 测试 `test_initial_state_not_leaked_across_runs`。
  - 为什么重要(对我): **skill 落地 + 评测**——multi-run eval（同一 workflow 跑 N case）若 state 跨 run 泄漏 = case 间污染 = 假相关/不可复现；harness 应断言每 run 初始 state 一致。
  - 明日动作(<=20min): 写 **是/否**：你 multi-run eval 是否复用同一 workflow 实例（是/否/未知）。

- [[BUG] fix TokenLimitedChatCompletionContext orphaning FunctionExecutionResultMessage mid-list（AutoGen PR #7986, updated **2026-07-22**）](https://github.com/microsoft/autogen/pull/7986)
  - 摘要: `get_messages()` 截断循环从列表中间移除 message；移除含 `FunctionCall` 的 `AssistantMessage` 后，配对的 `FunctionExecutionResultMessage` 留在其他位置成为 orphan。此前 fix 仅检查 index 0。Fix：截断后全表扫描，移除 call_id 无匹配 `FunctionCall` 的 orphan result。
  - 为什么重要(对我): **token 优化 + 评测**——token-limited context 是长对话 agent 标配；orphan tool result = 模型看到无因之果 = eval 行为漂移；collect 层应断言 tool call/result 成对。
  - 明日动作(<=20min): 写 **是/否**：你是否用 token-limited context 截断（是/否/未知）。

- [[BUG] fix deadlock when cancelling in-flight tool calls（AutoGen PR #7987, updated **2026-07-22**）](https://github.com/microsoft/autogen/pull/7987)
  - 摘要: cancel token 在 tool call 期间触发时，`CancelledError`（继承自 `BaseException` 非 `Exception`）穿透 `except Exception` 传播到 consumer loop，`_SENTINEL` 永不执行 → 死锁。Fix：catch `CancelledError` 格式化为 error message + `finally` 保证 sentinel 执行。
  - 为什么重要(对我): **skill 落地 + 评测**——eval harness 超时 cancel 是常见模式；deadlock = eval worker 挂死 = CI 假超时；应测 cancel 后 loop 终止。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否用 cancel/timeout 中断 tool call（是/否/未知）。

- [[BUG] fix MessageFilterAgent ordering filtered messages by config not chronological（AutoGen PR #7985, updated **2026-07-22**）](https://github.com/microsoft/autogen/pull/7985)
  - 摘要: `MessageFilterAgent` 过滤后的消息按 config 顺序而非原始时间序返回，破坏 multi-turn 对话因果链。Fix：按原始 chronological order 返回。
  - 为什么重要(对我): **评测**——消息顺序错乱 = 模型/tool 看到错误因果 = eval 不可复现；multi-turn skill eval 应断言时间序。
  - 明日动作(<=20min): 写 **是/否**：你是否用 message filter agent（是/否/未知）。

- [[BUG] fix Tool return_value_as_string using Python repr instead of JSON（AutoGen PR #7984, updated **2026-07-22**）](https://github.com/microsoft/autogen/pull/7984)
  - 摘要: tool 返回 plain dict/list 时 `return_value_as_string` 用 Python `str()` repr 而非 `json.dumps()`，产生非合法 JSON（单引号、`None` 等）。Fix：dict/list 走 `json.dumps()`。
  - 为什么重要(对我): **评测 + skill 落地**——下游 judge/structured parser 期望 JSON tool result；repr 输出 = silent parse failure = eval 假阴性。
  - 明日动作(<=20min): 写 1 条你域内 tool 返回 dict 被 repr 污染的真实样例（≤10 行）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix][Attention] Ignore empty MLA context chunks during merge（vLLM PR #49294, merged **2026-07-22**）](https://github.com/vllm-project/vllm/pull/49294)
  - 摘要: 宽 PCP batch 将 cached context 切成 workspace-sized chunk 后，后续 chunk 可能对某些 prefill row 无 KV token；backend 返回有限 LSE → `merge_attn_states` 把未定义输出当有效 attention 污染 prefill。Fix：零-context row 在 merge 前用 `-inf` LSE mask；单 in-place Triton kernel，B300 上 2K–131K balanced/skewed 均 ~12–13μs。Fixes #49334，承接 #49196 revert 后的替代修复。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：empty context chunk 是 PCP 分裂的结构性产物；merge correctness = 长上下文 multi-turn agent 召回/生成质量；#49196 revert 后此 PR merged = 修复方向确认。
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 是否启用 PCP（是/否/未知）。

- [[Attention] TRITON_MLA_SPARSE backend for SM80/SM121 sparse MLA（vLLM PR #47629, updated **2026-07-22**）](https://github.com/vllm-project/vllm/pull/47629)
  - 摘要: rebase & takeover #38476：为 DSA sparse-MLA 模型（DeepSeek-V3.2、GLM-5.x）在 DeepGEMM/FlashMLA-Sparse 不可用的 SM80（A100）和 SM121（GB10/DGX Spark）上提供 Triton fallback backend。`sparse_attn_indexer.py` 三路 dispatch：`is_xpu() → DeepGEMM → Triton fallback`；SM90+ 保持 FlashMLA 优先，`TRITON_MLA_SPARSE` 排在 `FLASH_ATTN_MLA_SPARSE`/`FLASHMLA_SPARSE` 之后。
  - 为什么重要(对我): **Attention 机制**：sparse MLA indexer 是长上下文 agent 的 attention 路由核心；此 backend 让 A100/消费级 GPU 也能跑 sparse MLA agent，但需与 #49294 correctness fix 联调。
  - 明日动作(<=20min): 写 **≤15 词**：你部署 GPU 架构是 SM80/SM90/SM121 哪个（填 1 个）。

- [[KV Offloading] Add per-request lookup_scope to skip secondary tier lookups（vLLM PR #48123, updated **2026-07-22**）](https://github.com/vllm-project/vllm/pull/48123)
  - 摘要: `kv_transfer_params` 新增 `lookup_scope` key：`"primary"` 跳过 secondary tier lookup（无 promotion），默认 `"all"`（现有行为）。`TieringOffloadingManager.lookup` 3 行短路 + enum/constant in `tiering/base.py`。
  - 为什么重要(对我): **Attention + token 优化**：multi-turn agent 的 KV cache tiering 中，secondary lookup promotion 有 latency 成本；per-request scope 让 eval 可测「仅 primary tier」vs「全 tier」的 TTFT/召回差异。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 KV offloading tiering（是/否/未知）。

## 3) 热门项目/可试个人项目

- [Cassie07/AgentSkill_Survey（GitHub, updated **2026-07-21**）](https://github.com/Cassie07/AgentSkill_Survey)
  - 摘要: arXiv 2606.11435 配套仓库：系统梳理 agent skill 评测与进化的 4 类范式（execution feedback、trajectory distillation、compression、RL）+ 6 类 skill-centric benchmark 分类，指出 longitudinal eval、token cost/latency、retrieval coverage 等结构性缺口。
  - 为什么重要(对我): **skill 落地 + 评测杠杆**——比单篇论文更可复用：benchmark 分类表可直接映射你的 L1–L3 eval 分层与 collect 层指标缺口清单。
  - 明日动作(<=20min): clone 仓库，读 Table 1 benchmark 分类；写 1 条你可映射的缺口（≤10 行）。
  - 估计复现成本: **低**（读 survey + 填映射表，≤30min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [A Fireside Chat with Cat and Thariq from the Claude Code team（Simon Willison Blog, **2026-07-21**）](https://simonwillison.net/2026/Jul/21/cat-and-thariq/)
  - 机会信号: Claude Code 团队 dogfood 数据——Claude Tag 落地 65% product eng PR；系统 prompt 瘦身 80% 后 internal eval **+10–15%**、token **-41–66%**、cost **-33–67%**；code review 信任阶梯（outer layer 全自动 review + incident PR 进 eval set）；behavioral eval 补 capability eval 盲区（如「不要说该睡觉了」）。→ **harness/评测/skill 精简** 是可对外 signature 的杠杆。
  - 风险信号: 访谈偏 Anthropic 内部实践，非可直接复制的工具链；「tool design 是 biology 不是 physics」= 难标准化；auto mode 安全 claim 待公开 eval 验证。
  - 我可执行的一步(<=30min): 写 **≤8 行**：你当前 1 个 skill/agent 的 system prompt 中，哪 1 段可删（示例/硬约束）并设计 1 条 behavioral eval 验证删后不退化。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 3804 Maximize Active Section with Trade II](https://leetcode.com/problems/maximize-active-section-with-trade-ii/)（**Day 22, Jul 2026, Hard**）：二进制串 + 至多一次 trade（先灭 surrounded `1` 块再亮 surrounded `0` 块）+ 多 query 子串最优。明日动作：理解 augmented `'1'+s+'1'` 技巧；写 **≤15 词**：agent 状态翻转与 binary trade 的类比。

- **IELTS（≤1 条）**：[IELTS Ready — British Council 官方免费备考（含 AI Mini Mock）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-ready)：50 分钟 Mini Mock + AI 估分。明日动作：完成 **Reading 1 节 Mini Mock**；写 **≤15 词** 错因类型（定位/同义替换/推理 选 1）。

## 6) 噪音过滤记录

- **AEVAL 2607.16345、SkillCoach 2607.01874、SkillHone 2606.08671**：`0719–0721.md` 已收录 → 不重复。
- **LangGraph #8396/#8395/#8392/#7273、LangChain #38993/#38996/#38997**：`0720–0721.md` 已收录 → 不重复。
- **vLLM #49196 revert、#47279 indexer topk**：`0721.md` 已收录 → 今日 §2 改收 #49294 merged + #47629 rebase。
- **LangGraph #8410 PurePath serde、#8407 store __init__**：checkpoint 边缘 case，今日 §1 已满 8 条 → 待观察。
- **LangGraph #8403 TracePolicy revert**：trace 策略回滚，偏内部 infra → 待观察。
- **LangChain #39010 model profiles refresh**：模型 catalog 更新，无直接 eval/skill 动作 → 待观察。
- **LlamaIndex #21899 vector store filter hardening、#21818 empty context fallback**：安全/RAG 相关但非今日 focus → 待观察。
- **vLLM #48597 Blackwell decode perf、#49445 queue caps**：#49445 偏 serving QoS，今日 §2 已满 → 待观察。
- **openai/evals #1657/#1694**：`0720–0721.md` 已标注待观察 → 不重复。
- **Anthropic Demystifying evals（2026-01）**：高价值但非 07-22 新鲜信号 → 记入待观察。
- **Simon Willison Jul 2 cognitive debt、Jul 16 Kimi K3 pelican**：`0721.md` 或更早已收录/非今日 focus → 不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-22** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Jul 22 + arXiv survey 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **582**（`0721.md` §7: 574 + 本日 §1: 8）
- Theory: **239**（`0721.md` §7: 236 + 本日 §2: 3）
- Career/Business: **101**（`0721.md` §7: 100 + 本日 §4: 1）
- LC: **83**（`0721.md` §7: 82 + 本日 §5: 1）
- IELTS: **83**（`0721.md` §7: 82 + 本日 §5: 1）
