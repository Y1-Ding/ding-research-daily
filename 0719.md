# Daily Research - 2026-07-19 (21:30)

## 0) 今日3条结论

- **skill eval 终于有「过程四维」标准**：SkillCoach 把 skill selection / following / composition / reflection 与外部 verifier 解耦——你的 L2/L3 不能只记 pass bit，应加「选对 skill 但跳过关键步」的 distractor 测试。
- **checkpoint 与 tracing 有两类 silent failure**：LangGraph #8380 的 `Counter`/`OrderedDict` 经 msgpack 变 plain dict；#8362 显示 middleware trace 可拖慢 500–1000ms/层——eval harness 要在 restore 后断言类型 + 对 trace 做 `trace_inputs` 裁剪。
- **Attention 微调可极瘦**：AHR 每 head 一个 scalar（~0.0001% 参数）在 few-shot 分类上可超 LoRA——垂域 agent 的轻量 adapter 路线值得与 skill routing head 分析并列实验。

## 1) Agent工程化（high-signal）

- [fix: properly sync AsyncPipeline on exit to prevent SSL connection closure（LangGraph PR #8381, updated **2026-07-19**）](https://github.com/langchain-ai/langgraph/pull/8381)
  - 摘要: `AsyncPostgresSaver` 在 `pipeline=True` 时，上下文退出（尤其内部抛异常或 LLM 调用延迟）未 `pipe.sync()`，pipeline 缓冲未刷盘即关连接 → `SSL connection has been closed unexpectedly` 与 checkpoint 损坏。Fix：在 `from_conn_string` 的 `finally` 中显式 sync pipeline。
  - 为什么重要(对我): **skill 落地 + eval**——与 0718 #8372 同族但覆盖「异常退出」路径；multi-turn eval 回放若间歇丢步，应查 pipeline sync + 异常路径测试。
  - 明日动作(<=20min): 写 **是/否**：你 Postgres checkpoint 是否在 `try/except` 内仍可能提前退出上下文（是/否/未知）。

- [fix(checkpoint): preserve Counter and OrderedDict type through msgpack round-trip（LangGraph PR #8380, updated **2026-07-19**）](https://github.com/langchain-ai/langgraph/pull/8380)
  - 摘要: `JsonPlusSerializer` 经 msgpack 往返将 `Counter`/`OrderedDict` 静默降为 `dict`，写入成功但 restore 后 `.most_common()`/`move_to_end()` 失效——典型「works until restore」失败。Fix：加 `OPT_PASSTHROUGH_SUBCLASS` 使 dict 子类走 `_msgpack_default` 保留类型。
  - 为什么重要(对我): **skill 落地 + eval**——agent state 若用 `Counter` 计 tool 频次或 `OrderedDict` 保 skill 优先级，eval 绿但 resume 后行为漂移；collect 层应加 round-trip 类型断言。
  - 明日动作(<=20min): 写 **是/否**：你 graph state 是否含 dict 子类（Counter/OrderedDict/defaultdict）（是/否/未知）。

- [feat(langgraph): expose `trace_inputs` on `add_node`（LangGraph PR #8362, updated **2026-07-19**）](https://github.com/langchain-ai/langgraph/pull/8362)
  - 摘要: 将 node `RunnableSeq` 的 `trace_inputs` 经 `PregelNode`/`StateNodeSpec`/`add_node` 透出。动机：LangChain middleware node hooks（`before/after_*`）默认 trace 全量 state，长 message history + 深 middleware 栈在 deepagents eval 中每层额外 **500–1000ms**。
  - 为什么重要(对我): **token 优化 + eval**——tracing 成本与 inference 同级时可污染 bench；skill 模板应为 middleware 节点配置 `trace_inputs` 裁剪（只留 tool name / turn id）。
  - 明日动作(<=20min): 写 **≤15 词**：你是否对 LangSmith/trace 做过 latency A/B（是/否/未知）。

- [fix(agent): fix _TracerCore copy, default_tools mutation, and LogStream send return（LangChain PR #38964, updated **2026-07-19**）](https://github.com/langchain-ai/langchain/pull/38964)
  - 摘要: (1) `_TracerCore.__copy__` 返回 `self` 致并发 chain run trace 互污；(2) `create_agent()` 的 `default_tools` 被 `model_node`/`amodel_node` 共享引用，middleware 变异跨调用泄漏；(3) `LogStreamCallbackHandler.send()` 在 stream 关闭后仍返回 `True`。Fix：深拷贝 tracer 字段、每次 `list(default_tools)`、关闭 stream 返回 `False`。
  - 为什么重要(对我): **评测 + skill 落地**——并行 eval worker + middleware 动态改 tools 时，trace 与 tool 列表串线 = 假阴性/假阳性；harness 应并行跑 2 agent 断言 trace 隔离。
  - 明日动作(<=20min): 写 **是/否**：你是否并行跑多个 `create_agent` 实例（是/否）。

- [fix(sdk): reject reserved x-api-key header case-insensitively（LangGraph PR #8379, updated **2026-07-19**）](https://github.com/langchain-ai/langgraph/pull/8379)
  - 摘要: Python SDK 对 `RESERVED_HEADERS` 做大小写敏感检查，`X-API-Key` 等变体可绕过并与自动配置的 `x-api-key` 共存，导致双 header 认证混乱。Fix：比较前 case-fold 自定义 header 名。
  - 为什么重要(对我): **skill 落地**——部署 LangGraph Cloud/SDK 的 skill agent 时，自定义 header 与 API key 冲突是隐蔽集成 bug；CI 应 parametrized 测三种大小写。
  - 明日动作(<=20min): 写 **是/否**：你是否在 LangGraph SDK 传过自定义 auth header（是/否/未知）。

- [[Bugfix] Fix MTP/EAGLE prefix-cache correctness for hybrid Mamba models（vLLM PR #47861, updated **2026-07-19**）](https://github.com/vllm-project/vllm/pull/47861)
  - 摘要: hybrid full-attention + Mamba/GDN 模型在 MTP/EAGLE prefix cache 路径上错误套用 token-KV 的 peek-and-drop，Mamba  recurrent state 不可像 KV block 回滚 → cache hit 时 tool-call 泄漏、召回失败、退化生成。Fix：token-KV 与 Mamba state 分组不变量分离；`mamba_cache_mode="align"` 仅在 block 边界缓存 state。
  - 为什么重要(对我): **token 优化 + eval**——Qwen3.5/3.6 类 hybrid 做 agent serving 时，prefix cache 是降本杠杆但 correctness 风险高；bench 应含「cache hit + multi-turn tool」回归集。
  - 明日动作(<=20min): 写 **是/否**：你 serving 是否用 MTP/EAGLE + prefix cache（是/否/未知）。

- [Include tool_choice in ChatCompletionCache's cache key（AutoGen PR #7969, updated **2026-07-18**）](https://github.com/microsoft/autogen/pull/7969)
  - 摘要: `ChatCompletionCache` 缓存键未含 `tool_choice`，`auto` 与 `none`（如 `AssistantAgent._reflect_on_tool_use_flow` 强制纯文本反思）可碰撞 → 后调用静默吃到前一次缓存响应。Fix：将 `tool_choice` 序列化进 hash（forced `Tool` 用 `.schema`）。
  - 为什么重要(对我): **评测 + token 优化**——reflect-on-tool-use 是常见 skill 模式；cache 污染 = eval 把「禁止 tool」路径记成「曾调用 tool」；任何 completion cache 应把 `tool_choice` 列入 key。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否在 reflect 阶段切换 `tool_choice`（是/否/未知）。

- [SkillCoach: Self-Evolving Rubrics for Evaluating and Enhancing Agentic Skill-Use（arXiv **2607.01874**）](https://arxiv.org/abs/2607.01874)
  - 摘要: 针对 enterprise skill 库重叠与 distractor skill，提出四维过程 rubric：**skill selection / following / composition / grounded reflection**，与外部 verifier 分离；从 gold skill 派生初始 rubric，经 evidence-grounded judging + validation-gated patch 自进化，用于诊断与 trajectory 筛选训练。实验：进化 rubric 显著优于 outcome-only 过滤，暴露 final accuracy 隐藏的失败。
  - 为什么重要(对我): **skill 落地 + 评测**——与 0718 Double Ratchet 互补：后者共进化 metric+skill，SkillCoach 专注 **skill-use 过程**；你的 eval playbook 可先实现四维 checklist + distractor 库。
  - 明日动作(<=20min): 写 1 条你域内「选对 skill 但跳过关键步」的失败样例（≤10 行）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Data-Efficient Adaptation of LLMs via Attention Head Reweighting（arXiv **2607.13425**, submitted **2026-07-15**）](https://arxiv.org/abs/2607.13425)
  - 摘要: **Attention Head Reweighting (AHR)**：每个 attention head 学一个 scalar 放大/抑制其对 residual 的贡献，仅更新 **~0.0001%** 参数（较 LoRA 少 200–1000×），在多样 text-classification few-shot 上可超 LoRA；权重可解释，揭示哪些 head 驱动 ICL。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：(1) 垂域 agent 轻量适配不必全量 LoRA；(2) head 权重可映射到「哪类 multi-turn 模式被放大」；(3) 与 0718 sink/routing head 分析可并联——AHR 给出可学习的路由系数。
  - 明日动作(<=20min): 写 **≤15 词**：你是否有 <100 样本的垂域分类/路由子任务（是/否）。

- [Training, Reading, and Editing Legible Transformers（arXiv **2607.08946**）](https://arxiv.org/abs/2607.08946)
  - 摘要: 在 legible-by-construction transformer 上，variance-floor loss 防止 crispness penalty 把 detector 塌成常数；深层的 crisp-and-contextual attention head 比例从 18% 升至 **78%**，单 head 编辑站点比 neuron 局部 **50–184×**；decorrelation 可在不损质量下换电路独立性，支持外科手术式编辑 named conjunction。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：≥3 turn agent 的「哪层 head 在路由 vs 阻尼」可用 legible 训练目标显式化；eval 可测 edit 后 multi-turn 行为是否按预期改变（因果干预而非相关性）。
  - 明日动作(<=20min): 写 **≤15 词**：你是否做过 attention head 级干预实验（是/否/未知）。

- [[Attention] TRITON_MLA_SPARSE backend for SM80/SM121 sparse MLA（vLLM PR #47629, updated **2026-07-19**）](https://github.com/vllm-project/vllm/pull/47629)
  - 摘要: 为 DeepSeek-V3.2/GLM-5.x 等 DSA sparse-MLA 在 **SM80 (A100)** 与 **SM121 (GB10)** 上补 Triton sparse attention backend（`TRITON_MLA_SPARSE`），在 SM90+ 仍优先 FlashMLA/DeepGEMM。含 `use_fused_indexer_q` 在 SM80 上 fp8e4nv 编译失败的 gate fix。
  - 为什么重要(对我): **Attention + token 优化**：sparse MLA 是长上下文 agent 的 attention 算子层杠杆；部署栈应记录「GPU 代际 → sparse backend 回退链」，避免误以为 dense fallback 是同等 SLA。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent serving 目标 GPU 代际（A100/H100/GB10/其他）。

## 3) 热门项目/可试个人项目

- [darkrishabh/agent-skills-eval（GitHub, last push **2026-07-15**）](https://github.com/darkrishabh/agent-skills-eval)
  - 摘要: **agentskills.io** 规范下的 skill 测试 runner：每条 eval 跑 `with_skill` vs `without_skill` 双轨，judge model 打分 + tool-call 断言，输出 JSON/JSONL 与静态 HTML 报告；TypeScript SDK + CLI，默认 OpenAI-compatible API。
  - 为什么重要(对我): **skill 落地 + 评测杠杆**——直接回答「这个 SKILL.md 有没有 measurable lift」；可 fork 其 with/without 模式进 map_prompt_test collect 层，比手写 rubric 更快出 baseline。
  - 明日动作(<=20min): 本地 `npx agent-skills-eval`（或读 README）对 1 个现有 skill 跑 1 条 eval；记录 with vs without  pass 差。
  - 估计复现成本: **低**（单 skill + 单 prompt，≤30min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [How agents are transforming work（OpenAI, **2026-06-25**）](https://openai.com/index/how-agents-are-transforming-work/)
  - 机会信号: OpenAI 公开 Codex 使用数据——2026-06 日活用户 **P99 单日 >60 小时** agent turns（并行多 agent）；知识工作单元从「单次对话」变为「委托长程任务」→ **harness/评测/编排** 技能比「会调 API」更稀缺，适合出海 remote 的 signature 方向。
  - 风险信号: 数据来自 OpenAI 内部/heavy user 分布，不代表 median；「并行 agent」也放大 cache/trace/cost 类 bug（见今日 §1）。
  - 我可执行的一步(<=30min): 写 **≤8 行** 你当前工作中哪 1 个环节最适合改成「委托式长程 agent」（含成功标准 1 条）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 435 Non-overlapping Intervals](https://leetcode.com/problems/non-overlapping-intervals/)（**Day 19, Jul 2026, Medium**）：给定区间列表，求移除最少区间使剩余互不重叠（等价于区间调度/贪心按 end 排序）。明日动作：手写贪心 15 行；写 **≤15 词**：agent 工具调用去重与区间调度贪心的类比。

- **IELTS（≤1 条）**：[IELTS Ready — British Council 官方免费备考（含 AI Mini Mock）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-ready)：50 分钟 Mini Mock + AI 估分、2 套完整模考与技能短课；报名 BC 考试可解锁 Premium（40 套 + Express Mock）。明日动作：完成 **Listening 或 Reading 1 节 Mini Mock**；写 **≤15 词** 错因类型（词汇/定位/速度 选 1）。

## 6) 噪音过滤记录

- **LangGraph #8371 undeclared state warn、#8372 pipeline context、LangChain #38929 empty tool args、#38932 summarization、#38922 ModelRetry**：`0718.md` 已收录 → 不重复。
- **vLLM #48295 llama3_json/jamba tool parser、#48641 logits fp32、#49018 logprobs padding**：`0717–0718.md` 已收录 → 不重复。
- **Double Ratchet 2607.12790、Attention Drift 2605.09992、Transient Reserves 2607.09279**：`0717–0718.md` 已收录 → 不重复。
- **AgentLens 2607.06624、MemoHarness 2607.14159**：前者 `0712.md` 已收；后者与 SkillCoach 同族，今日 §1 已收 SkillCoach → MemoHarness 待观察。
- **LangChain #38966 mustache falsy、#38963 CancelledError、#38965 texts_ materialize**：重要但偏模板边缘/infra → 待观察（#38964 已覆盖更关键的 agent tracer 并发 bug）。
- **run-llama #22389 score 0.0→1.0、#21671 SSRF**：RAG 相关但今日 §1 已满 8 条 → 记入待观察（security 优先排期）。
- **vLLM #48382 weight reload、#48984 pooling migration**：perf/API 迁移，与今日 focus 弱于 #47861 → 待观察。
- **Indie Hackers 多篇 AI solo 帖（2026-02–03）**：非当日新鲜信号 → 不收录；职业池改用 OpenAI 06-25 数据帖。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-19** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Jul 19 + arXiv 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **558**（`0718.md` §7: 550 + 本日 §1: 8）
- Theory: **230**（`0718.md` §7: 227 + 本日 §2: 3）
- Career/Business: **98**（`0718.md` §7: 97 + 本日 §4: 1）
- LC: **80**（`0718.md` §7: 79 + 本日 §5: 1）
- IELTS: **80**（`0718.md` §7: 79 + 本日 §5: 1）
