# Daily Research - 2026-06-06 (21:30)

> 采集口径：优先 **X_WHITELIST / YOUTUBE_WHITELIST / GITHUB_WHITELIST / BLOG_WHITELIST / CAREER_BUSINESS_WHITELIST**；评分 `relevance(40)+actionability(25)+credibility(20)+freshness(15)`，正文主段 **≥70**（职业池 **≥65** 且具可执行信号）。与 `0605.md` 及更早文件做 **URL 去重** + **语义去重**。本日 **`[OUTSIDE_WHITELIST]` 0 条** → 主链 **0%（≤20%）**。  
> **自动化提示（给编排器）**：Cron 触发时区若仍为 UTC，请将等价目标对齐 **Asia/Shanghai 每日 21:30**；失败 **重试 1 次**；连续 **2** 个日历日失败再发告警（本仓库文件不承载告警状态机）。

## 0) 今日3条结论

- **Agent 代码执行沙箱的可行路径：MicroPython + wasmtime + 有状态 session**：Simon Willison Blog **2026-06-06** [Running Python code in a sandbox with MicroPython and WASM](https://simonwillison.net/2026/Jun/6/micropython-in-a-sandbox/) 发布 **`micropython-wasm` 0.1a2**（[PyPI](https://pypi.org/project/micropython-wasm/) / [GitHub](https://github.com/simonw/micropython-wasm)）：**362KB WASI blob**、**内存 + fuel CPU 限制**、**host function 白名单**、**`MicroPythonSession` 跨调用保留变量**；已接入 **Datasette Agent** 插件且 **GPT-5.5 xhigh 暂未突破沙箱**（作者自述 alpha，非生产背书）。对你：**工具型 agent 的 code interpreter** 可优先评估 **WASM 解释器 + 确定性 egress 控制**，而非裸 `exec()`。
- **LangGraph 两条生产级漏洞仍在 Open，社区修复 PR 同日被自动关闭**：**2026-06-06** [#8012](https://github.com/langchain-ai/langgraph/issues/8012)（**`DeepAgentState` 新线程 `update_state` 首条 `messages` 丢失**）与 [#7845](https://github.com/langchain-ai/langgraph/issues/7845)（**流式 tool-call 边界 token 泄漏为用户可见文本**）均 **Open**；对应修复 [#8014/#8016](https://github.com/langchain-ai/langgraph/pull/8016)、[#8015/#8017](https://github.com/langchain-ai/langgraph/pull/8017) **2026-06-06 因 `missing-issue-link` 关闭、未合并**。与 `0605.md` 的 **#8009 缓存键碰撞** 并读：**checkpoint / 流式多通道 / ToolNode** 三条 harness 边界需纳入发布门禁。
- **langchain-core 1.4.1 补齐 v3 流式「推理 + 工具」并存时的状态组装**：**2026-06-05** [langchain-core==1.4.1](https://github.com/langchain-ai/langchain/releases/tag/langchain-core%3D%3D1.4.1) 合入 **#37434/#37435**——修复 **无 `index` 的 provider（如 Gemini）在 `stream_events(v3)` 下 reasoning 与 tool_call 同桶覆盖**；与 **#7845** 的 **边界泄漏** 是同一「多通道 decode 契约」问题的不同切面。

## 1) Agent工程化（high-signal）

- [Running Python code in a sandbox with MicroPython and WASM（Simon Willison Blog，**2026-06-06**）](https://simonwillison.net/2026/Jun/6/micropython-in-a-sandbox/)
  - 摘要: **`micropython-wasm`** 用 **wasmtime** 跑定制 **MicroPython WASI**；支持 **PyPI 安装**、**fuel/内存上限**、**受控文件与网络（经 host）**、**持久 `MicroPythonSession`**（队列 + `eval()` 循环）；**0.1a2** 新增 **CLI**（`uvx micropython-wasm -c '...'`）；配套 **[datasette-agent-micropython](https://github.com/datasette/datasette-agent-micropython)** 已上线。
  - 为什么重要(对我): **Code-execution tool** 是 agent 栈最高风险面之一；该方案把 **隔离层放在 WASM 而非 prompt**，与 OpenAI **Lockdown Mode**（见 §4）同属 **切断 lethal trifecta 外泄腿** 的工程思路。
  - 明日动作(<=20min): 本地跑 `uvx micropython-wasm -c 'print(1+1)'`；用中文写 **≤25 词**：你若给 agent 加代码工具，**WASM / 容器 / 无执行** 三选一及理由。

- [`update_state` on fresh `DeepAgentState` thread drops first `messages` write（Issue #8012，**Opened 2026-06-05**，**2026-06-06** 更新）](https://github.com/langchain-ai/langgraph/issues/8012)
  - 摘要: 新线程 **`saved is None`** 时 **`put_writes` 跳过**，**`DeltaChannel.checkpoint()`** 返回 **MISSING** → **`aupdate_state` 写入的 `messages` 读回为空**；**`ainvoke` 与 `MessagesState` 正常**。社区 **#8014/#8016** 提案 **`create_checkpoint(channels_to_snapshot=...)`** 但 **未合并**。
  - 为什么重要(对我): **人工注入历史 / HITL 预写消息 / 外部编排写状态** 若走 **`update_state`**，在 **DeepAgents** 上会 **静默丢首条**——eval 与生产 onboarding 会系统性偏乐观。
  - 明日动作(<=20min): 若你用 **DeepAgents + checkpoint**，测 **1 次**「仅 `aupdate_state` 写首条 message」是否可读回（是/否）；若否，临时改用 **`ainvoke` 种子**（≤15 词记录）。

- [Streaming agents leak malformed tool-call payloads（Issue #7845，仍 Open；PR #8015/#8017 **2026-06-06** 关闭）](https://github.com/langchain-ai/langgraph/issues/7845)
  - 摘要: **`astream_events(..., stream_mode=["messages"])`** 下，模型在 **tool-call 边界** 偶发 **CJK 等 stray token** → 解析失败 → **`to=functions.*` JSON 经 `on_chat_model_stream` 泄漏给用户**；issue 指向 **langchain-core `stream_v2`/content-block** 与 **LangGraph 侧缓冲** 两条修复线，**主线均未闭合**。
  - 为什么重要(对我): 与 `0605.md` **vLLM `include_reasoning=false`** 同类——**每一输出通道需定义「解析失败时零泄漏」**；否则 **工具名/参数形状** 泄露且 **工具永不执行**。
  - 明日动作(<=20min): 查你流式 UI 是否订阅 **`on_chat_model_stream` 裸文本**（是/否）；若是，加 **≤20 词** 的 **tool-boundary 正则丢弃规则** 或切 **stream v2/v3**。

- [langchain-core==1.4.1 发布（GitHub Release，**Published 2026-06-05**）](https://github.com/langchain-ai/langchain/releases/tag/langchain-core%3D%3D1.4.1)
  - 摘要: 补丁版含 **#37434**（**v3 流组装保留 reasoning∥tool_call**）、**#37435**（**`additional_kwargs` 跨 chunk 保留**）、**#37456**（**`_convert_to_message` 接受 Serializable envelope**）等；面向 **`astream_events(version="v3")`** 与多 provider **content-block** 路径。
  - 为什么重要(对我): 升级 **langchain-core≥1.4.1** 可缓解 **Gemini 等无 index block** 的 **状态丢失**；**不能**替代 **#7845** 的 **协议形文本泄漏** 修复。
  - 明日动作(<=20min): 若你用 **LangGraph + Gemini/多通道流**，记录 **`langchain-core` 版本**；若 **<1.4.1**，列 **1 个** 需验证的 **reasoning+tool 同轮** 用例（≤20 词）。

- [ToolNode accepts ToolMessage results bound to sibling tool_call_id（Issue #7989，**2026-06-06** 更新）](https://github.com/langchain-ai/langgraph/issues/7989)
  - 摘要: 并行 **`ToolNode`** 下，恶意/缺陷工具可返回 **绑定兄弟 `tool_call_id` 的 `ToolMessage`**，与真实工具结果 **并存**；草稿 **#7997** 仅覆盖 **top-level 返回**，**`Command(update={...})` 路径** 仍可由 **#7989 线程内 repro** 复现，待 follow-up。
  - 为什么重要(对我): **多工具并行** 时 **tool 返回值 = 写共享状态的 API**；缺少 **call_id 严格绑定** 等同 **未校验的跨通道写入**。
  - 明日动作(<=20min): 若你有 **并行 tool calls**，写 **≤20 词**：是否信任 **工具实现方**（自研/第三方/MCP）；若第三方，是否已有 **结果 schema 校验**（有/无）。

- [[KV offload] Parallel-agnostic fs-tier cache（vLLM PR #44733，**Opened 2026-06-06**）](https://github.com/vllm-project/vllm/pull/44733)
  - 摘要: 为 **单 full-attention 组** 提供 **与 TP 无关的 fs-tier KV 缓存**；**4×H100** 验证 **Qwen2.5-7B store TP=2 → load TP=4** 可 **400/403 token 命中** 且输出一致；**MLA/GQA** 路径 **short read → recompute**。
  - 为什么重要(对我): **多节点 agent 推理** 常遇 **扩缩容/换 TP**；**KV 可迁移** 降低 **长上下文 agent** 的 **冷启动成本**。
  - 明日动作(<=20min): 若你跑 **vLLM multi-node**，记 **是否启用 KV offload / 文件层缓存**（是/否/未知）；写 **≤15 词** 扩缩容时 **会话是否可续**。

- [[Feature Proposal] Standard Reducers Library（Issue #7271，**2026-06-06** 讨论更新）](https://github.com/langchain-ai/langgraph/issues/7271)
  - 摘要: 并行 superstep 默认 **last-write-wins** 导致 **静默丢写**；提案 **`smart_merge_dict` / `combine_distinct` / `first_wins`**（PR **#7239** 仍阻塞）。**2026-06-06** 线程补充：**reducers 解决 merge 语义，runtime 检测「无 reducer 多写者」** 是互补问题（社区 **agent-coherence** 回调示例）。
  - 为什么重要(对我): **Map-Reduce / 并行 RAG** 图若未显式 **Annotated reducer**，生产并行一切换即 **数据丢失**——应在 **图编译期或 CI** 扫描。
  - 明日动作(<=20min): 为你 **1 个** LangGraph 列 **≥2 个节点会写的 state key**；标 **是否有 reducer**（有/无/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [DeltaChannel 首写与 checkpoint 快照语义（Issue #8012 + PR #8016，**2026-06-06**）](https://github.com/langchain-ai/langgraph/issues/8012)
  - 摘要: **`DeltaChannel`** 在非 snapshot 步 **`checkpoint()`→MISSING**；**`update_state`** 路径依赖 **`saved` 存在才 `put_writes`** → **首写只活于内存通道、不进 blob**。修复思路：**无 prior checkpoint 时 force-snapshot Delta 通道**。
  - 为什么重要(对我): **三回合追问**：(1) 你的 **增量状态** 是否区分 **delta vs snapshot**？(2) **out-of-band 写** 是否走与 **invoke** 相同持久化路径？(3) eval 是否包含 **「仅 update_state 种子线程」**？
  - 明日动作(<=20min): 画 **≤4 节点**：`update_state` → `saved?` → `put_writes` / `create_checkpoint` → `read back`（每节点 ≤8 词）。

- [v3 流式 content-block 桶键与 reasoning∥tool_call 共存（langchain-core #37434，已入 **1.4.1**）](https://github.com/langchain-ai/langchain/pull/37434)
  - 摘要: provider **无 `index`** 时 compat 层用 **位置 `i` 作桶键** → **reasoning 与 tool_call 互斥覆盖**；修复为 **`("__lc_no_index__", block_type, i)`** 分桶，使两通道均到达 **`content-block-finish`**。
  - 为什么重要(对我): 类比 **Attention**：**桶键 = 路由索引**；缺 **type 维度** 会把 **正交信息通道** 错误 **merge 为单一槽位**。
  - 明日动作(<=20min): 列你用的 **provider** 是否在流式块中带 **`index`**（是/否/未知）；若无，确认 **core≥1.4.1**（是/否）。

- [流式 tool-call 边界的「可见性 vs 解析失败」（Issue #7845，**2026-06-06**）](https://github.com/langchain-ai/langgraph/issues/7845)
  - 摘要: 社区 PR 提案在 **`StreamMessagesHandler(V2)`** **缓冲** 匹配 **tool-boundary 模式** 的 content，**确认 tool call 则丢弃缓冲**，否则 **flush**；与 **InvalidToolCallBlock（stream_v2）** 形成 **「失败→类型化事件」** 而非 **「失败→用户可见 JSON」**。
  - 为什么重要(对我): **Agent decode** 需定义 **失败模式枚举**：**重试 / 丢弃 / 类型化错误事件 / 泄漏**——默认应是 **后两者之一**，不是 **泄漏**。
  - 明日动作(<=20min): 为你流式栈写 **≤30 词** 的 **tool 解析失败策略**（四选一 + 一句理由）。

## 3) 热门项目/可试个人项目

- [micropython-wasm 0.1a2（Simon Willison / PyPI，**2026-06-06**）](https://github.com/simonw/micropython-wasm/releases/tag/0.1a2)
  - 摘要: Alpha 库 + **CLI**；**Datasette Agent** 插件 **[datasette-agent-micropython](https://github.com/datasette/datasette-agent-micropython)** 演示 **agent 生成并执行 MicroPython**；作者 **[越狱测试 gist](https://gist.github.com/simonw/5de497c44d25f9fd459c8aa2c959fe4a)** 暂未成功。
  - 为什么重要(对我): 个人项目可 **1 小时内** 验证「**WASM 沙箱 + 自有 host API**」是否满足 **内网数据 agent** 的 **最低执行隔离**。
  - 明日动作(<=20min): `uvx micropython-wasm -c 'import sys; print(sys.platform)'` 截图或粘贴 **stdout**（1 行即可）。
  - 估计复现成本: **低**（无 GPU；需接受 **alpha 安全未审计**）。

- [feat: add FallbackLLM for built-in provider failover（llama_index PR #21894，**Opened 2026-06-06**）](https://github.com/run-llama/llama_index/pull/21894)
  - 摘要: **`FallbackLLM`** 按优先级包装多 **LLM**；**429/5xx/超时** 切换下一 provider，**4xx 鉴权/策略** 立即失败；覆盖 **chat/complete/stream 及 async**；**32** 个单测。
  - 为什么重要(对我): **Agent 工程** 的 **模型中立 + 运行期降级** 可先在 **RAG/单 agent** 层用 **薄封装** 验证，再上升到 **LangGraph 步骤级路由**。
  - 明日动作(<=20min): 写 **≤20 词**：你若试 failover，**主/备** 各选哪两家（自定）。
  - 估计复现成本: **中**（需 **≥2** provider API key + LlamaIndex 管道）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Multi-agent systems need a 'mission keeper' role（microsoft/autogen Issue #7487，**2026-06-06** 活跃）](https://github.com/microsoft/autogen/issues/7487)
  - 机会信号: 社区用 **生产数据** 论证 **规划 agent 兼管协调与目标** 时 **goal drift 静默**（有 thread 引 **19 天未检出** 案例）；**「Mission Keeper / 外部验证」** 可作为 **咨询交付物**（合规、审计、长跑 agent）的 **可售卖模块**。
  - 风险信号: 讨论混杂 **未验证营销回复**；落地需 **可测 drift 注入 benchmark**，否则易沦为 **又一个「监督 agent」prompt**。
  - 我可执行的一步(<=30min): 用中文写 **≤40 词**：为你当前 **1 个** 多 agent 流程，**原始目标** 在多少步后 **不再被显式引用**（估步数即可）。

- [OpenAI Help: Lockdown Mode（经 Simon Willison Blog 引用，**2026-06-05**）](https://help.openai.com/en/articles/20001061-lockdown-mode)
  - 机会信号: **确定性限制出站网络** 切断 **lethal trifecta** 的 **外泄腿**（[Simon 解读](https://simonwillison.net/2026/Jun/5/openai-help-lockdown-mode/)）；对 **出海 ToB agent** 是 **可向客户讲述的默认安全姿态**（即使模型被 prompt injection 影响，**仍难外传数据**）。
  - 风险信号: **不防注入本身**；且文档暗示 **默认 ChatGPT 设置对坚定外泄攻击保护不足**——你若做 **类似能力**，需 **明确默认开/关** 与 **误杀率**。
  - 我可执行的一步(<=30min): 列 **3 条** 你方 agent **出站请求**（工具/检索/回调）；标 **哪些应在 lockdown 下默认禁止**（各 ≤8 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：LeetCode Explore [`Graph`](https://leetcode.com/explore/learn/card/graph/) 中 **Number of Islands**：二维网格 **连通分量计数**——映射 **并行 agent superstep** 中 **「无 reducer 的共享 key」** 实为 **隐式合并同一「岛」**。明日动作：用中文写 **≤30 词**：为你图中 **1 个** 多写者 key 标 **应独立成岛（拆分 key）还是加 reducer（合并规则）**。

- **IELTS（≤1 条）**：浏览 [IELTS 官方 — Speaking test format](https://ielts.org/take-a-test/test-format/academic-speaking) 三部分（intro / long turn / discussion）。明日动作：用中文写 **≤35 词**：把 **Part 2「1 分钟准备 + 2 分钟陈述」** 映射为你 **下周 1 次** 英文 **技术观点 oral drill**（自定话题）。

## 6) 噪音过滤记录

- **`https://openai.com/index/chatgpt-memory-dreaming/`**、**`v0.22.1`**、**`#8009`**、**`#44391`**、**Anthropic Partner Hub**、**`#7985`**：与 `0605.md` **URL/语义去重**。
- **`#8014`/`#8015`/`#8017`**：**未合并**（`missing-issue-link`）→ 仅作 **阻塞信号**，不记为已修复。
- **`https://github.com/microsoft/autogen/pull/7730`**：**2026-06-06 作者关闭**，**非 merged** → MCP `ensure_ascii` 修复仍未入主链。
- **`https://simonwillison.net/2026/Jun/4/ai-enthusiasts-ai-skeptics/`**、**Uber AI caps**：与 `0603.md`–`0605.md` **去重**。
- **`https://github.com/run-llama/llama_index/issues/21882`**（TealTiger）：`0605.md` 已收录 → 本日不重复。
- **X_WHITELIST / YOUTUBE_WHITELIST / Bilibili**：无法稳定抓取原帖 → 不收录。
- **`openai/evals`**、**`#1674 REFUTE benchmark`**：边缘分、无 **`0606`** 新合并 → 丢弃。
- **DeepMind Blog**：抓取超时 → 无 **`0606`** 可核验新文 → 丢弃。

## 7) 本周累计计数

- Agent: **235**（`0605.md` §7: 228 + 本日 §1: 7）
- Theory: **92**（`0605.md` §7: 89 + 本日 §2: 3）
- Career/Business: **56**（`0605.md` §7: 54 + 本日 §4: 2）
- LC: **38**（`0605.md` §7: 37 + 本日 §5: 1）
- IELTS: **38**（`0605.md` §7: 37 + 本日 §5: 1）
