# Daily Research - 2026-09-03 (21:30)

## 0) 今日3条结论

- **MCP / HITL / checkpoint 三条 silent success 同日暴露**（LlamaIndex #22944, LangChain #40165, LangGraph #8811, created **2026-09-03**）：MCP server 返回 `is_error` 被当成功且 `return_direct` 直接结束；HITL review payload 丢失 `args_schema`；cancelled run 的 stale shallow checkpoint 可回滚 replacement run → **agent skill eval gold 须覆盖 MCP is_error、HITL schema 完整性、checkpoint 并发写顺序三条负例。**
- **Tool-call serving 两条 token/解析缺陷**（vLLM #55141/#55115, created **2026-09-03**）：DeepSeek-V3.2 把 `## Tools` 渲染到 system prompt 之前；step3 streaming parser 在单 delta 含 name+args 时不推进 `position` → 客户端收到空 arguments 或丢失 tool call → **function-call eval 须标注 tokenizer 渲染顺序 + streaming delta 边界。**
- **Attention/serving 两条确定性缺陷**（vLLM #55122/#55150, created **2026-09-03**）：`persistent_topk` 因 `atomicAdd` 使 sparse indexer 同输入不同输出；encoder-only model 接受 quantized KV cache 却在 backend init 才 fail → **sparse attention eval 须标 deterministic flag；encoder embedding eval 须在 config 层拒绝 quant KV。**

## 1) Agent工程化（high-signal）

- [fix(checkpoint-postgres): reject stale shallow checkpoint writes（LangGraph PR #8811, created **2026-09-03**）](https://github.com/langchain-ai/langgraph/pull/8811)
  - 摘要: Fixes #8810。`durability="exit"` 的 cancelled run 可在 replacement run 已保存更新 state 后，完成一次 delayed shallow checkpoint write，把 thread 静默回滚到旧值（repro：`{'value': 'old'}` 覆盖 `{'value': 'new-done'}`）。Fix：shallow upsert 比较 monotonic checkpoint ID，拒绝更旧 ID 的写入，accepted update 在同一事务提交。
  - 为什么重要(对我): **skill 落地 + 评测**——multi-turn agent 用 shallow checkpoint 做 fast resume 时，stale write = eval 读到旧 state 且无 error；与 0902 的 reducer default discard 同属 silent state corruption。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `durability="exit"` + shallow checkpoint 做 agent resume（是/否/未知）。

- [fix(checkpoint-sqlite): respect per-op TTL refresh flags in batched reads（LangGraph PR #8812, created **2026-09-03**）](https://github.com/langchain-ai/langgraph/pull/8812)
  - 摘要: Fixes #8806。SQLite store 两条 bug：(1) batch GET 在 namespace 内任一 `GetOp` 设 `refresh_ttl=True` 时，对整个 namespace 所有 key 做 TTL refresh，即使 sibling op 设了 `refresh_ttl=False`；(2) store-level `ttl_config` 被二次检查，与 `BaseStore` 已解析的 per-op flag 冲突，导致文档默认 `refresh_on_read=True` 时 refresh 被静默禁用。Fix：仅对 opted-in keys 做 refresh UPDATE；移除二次检查，对齐 PostgresStore。
  - 为什么重要(对我): **skill 落地 + 评测**——agent memory store 的 TTL refresh 是 long-running skill 的 liveness 前提；batch read 中 opt-out 被污染 = eval 断言 expiry 不可复现。
  - 明日动作(<=20min): 写 **是/否**：你是否在 batch `store.get` 中对部分 key 设 `refresh_ttl=False`（是/否/未知）。

- [fix(langchain): propagate `args_schema` to HITL review config（LangChain PR #40165, created **2026-09-03**）](https://github.com/langchain-ai/langchain/pull/40165)
  - 摘要: Fixes #40164。`HumanInTheLoopMiddleware` 接受 `InterruptOnConfig.args_schema`，但构造 `ReviewConfig` 时遗漏该字段 → human reviewer 看不到 tool argument schema，无法做结构化 review/edit。Fix：保留用户显式配置的 schema 到 interrupt payload；不改 inference 或 edited-call validation。
  - 为什么重要(对我): **skill 落地 + 评测**——HITL 是 agent skill 的安全门禁；review payload 缺 schema = eval 无法断言 reviewer 收到的 argument 结构与 model 输出一致。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `HumanInTheLoopMiddleware` + 自定义 `args_schema`（是/否/未知）。

- [fix(tools/mcp): carry MCP tool failures into ToolOutput.is_error（LlamaIndex PR #22944, created **2026-09-03**）](https://github.com/run-llama/llama_index/pull/22944)
  - 摘要: Closes #22943。MCP server 用 result `is_error` 报告失败而非 raise；`McpToolSpec` 原样透传，`ToolOutput.is_error` 默认 `False` → agent 把失败当成功。更糟：`base_agent.py` / `multi_agent_workflow.py` 在 `return_direct` tool 上 gate `not tool_output.is_error`，失败时 error text 被直接作为 final answer 输出，agent 无 recovery 机会。Fix：`async_callback` 在 failure 时设 `is_error=True` 并用 `_result_text` 填 output。
  - 为什么重要(对我): **skill 落地 + MCP eval**——Harness Cursor plugin 与 agent skill 均走 MCP；`is_error` 是 MCP 标准失败信号，silent success = eval 系统性 false positive。
  - 明日动作(<=20min): 写 **是/否**：你是否在 MCP tool eval 断言 `ToolOutput.is_error`（是/否/未知）。

- [fix: bound API retries and drop dead error-dict checks（openai/evals PR #1821, created **2026-09-03**）](https://github.com/openai/evals/pull/1821)
  - 摘要: Fixes #1818。共享 model-I/O retry helper 原 unbounded predicate loop；Fix：exception-based backoff + 可配置 `EVALS_API_RETRY_MAX_TIME`（默认 300s），耗尽后 re-raise 最后异常；移除 OpenAI/Anthropic SDK wrapper 中 stale error-dict checks。9 个回归 test 覆盖 zero-budget exhaustion、falsey success、retry recovery、non-retryable propagation。
  - 为什么重要(对我): **评测基础设施**——agent eval 跑 openai/evals 或自建 retry wrapper 时，unbounded retry = eval run 挂死或 cost 失控；bounded budget 是 eval CI 的 liveness gate。
  - 明日动作(<=20min): 写 **≤15 词**：你的 eval retry 有无 wall-clock budget（有/无/未知）。

- [[Tokenizer] Fix tools rendering before system prompt in DeepSeek-V3.2（vLLM PR #55141, created **2026-09-03**）](https://github.com/vllm-project/vllm/pull/55141)
  - 摘要: Fixes #54911。Companion to #52255（V4 已修）。`deepseek_v32.py` 在 request-level tools 时 `messages.insert(0, {"role": "system"})` 并 attach tools → `## Tools` block 渲染在现有 system prompt **之前**，偏离 reference encoding `{system_content}\n\n## Tools ...`。Fix：attach 到已有 leading system/developer message；仅无 system message 时才 insert。3 个回归 test 镜像 V4 suite。
  - 为什么重要(对我): **skill 落地 + token 优化**——tool schema 位置影响 model 对 system instruction 的注意力分配；eval 须按 tokenizer 渲染顺序标注 prompt layout。
  - 明日动作(<=20min): 写 **是/否**：你是否 serving DeepSeek-V3.2 + request-level tools（是/否/未知）。

- [[Bugfix][Tool Parser] Make step3 streaming independent of delta boundaries（vLLM PR #55115, created **2026-09-03**）](https://github.com/vllm-project/vllm/pull/55115)
  - 摘要: Fixes #55082。`extract_tool_calls_streaming` 解析到 function name 后立即 return 但不推进 `self.position` → 单 delta 同时含 invoke tag + parameters + end tag 时，arguments 永不 emit；leading text + tool call 的 delta 则完全丢失 tool call。生产常态：`--stream-interval > 1`、spec decode、scheduler batching 均产生 multi-token delta。Fix：parse loop 内累积 content + tool calls 一次 emit；hold back trailing partial `<|tool_calls_begin|>` token。
  - 为什么重要(对我): **评测 + token 优化**——streaming function-call eval 若只在单-token delta 下 pass，生产 multi-token delta 会系统性 empty arguments；与 0902 Granite 4 double-encode 形成上下游同构 bug class。
  - 明日动作(<=20min): 写 **是/否**：你是否用 step3/hermes streaming tool parser + multi-token delta（是/否/未知）。

- [[Bugfix][OpenAI] Aggregate cached tokens for multi-prompt completions（vLLM PR #55120, created **2026-09-03**）](https://github.com/vllm-project/vllm/pull/55120)
  - 摘要: Completion API `prompt=[p1, p2]` 时，每个 prompt 独立 `RequestOutput` + cache-hit count；serving 代码只报告一个 output 的 count（non-streaming 取 last，streaming 取 first interleaved result）→ `usage.prompt_tokens_details.cached_tokens` 依赖 prompt 顺序/scheduling 而非整请求。A100 实验：两 prompt 分别 176/256 cached，parent 返回 176 或 256（期望 432）。Fix：两路径均按 prompt 聚合一次。
  - 为什么重要(对我): **token 优化 + agent serving eval**——multi-prompt batch eval 的 cache hit metric 若不可复现，prefix caching ROI 评估系统性偏差。
  - 明日动作(<=20min): 写 **是/否**：你是否用 Completion API multi-prompt + `--enable-prefix-caching`（是/否/未知）。

- [[Bugfix][KV Offload] Don't pop a verified full-attn eagle prefix chunk（vLLM PR #55118, created **2026-09-03**）](https://github.com/vllm-project/vllm/pull/55118)
  - 摘要: `OffloadingConnector` 在 full-attention eagle group prefix scan 时，对 unverified group 无条件 `num_hit_chunks -= 1`；compensating over-query 仅 SWA 有 → full-attn eagle 的 pop 移除已匹配 chunk，stored prefix ≤1 chunk 时 `return 0` 整请求 abort。`GPU_to_CPU` 增长、`CPU_to_GPU` 恒 0。Fix：decrement 与 over-query 同 gate（仅 `sliding_window_size_in_chunks is not None`）。
  - 为什么重要(对我): **token 优化**——multi-turn agent KV offload 的 hit rate eval 若系统性 0%，cost benchmark 低估 offload 收益。
  - 明日动作(<=20min): 写 **是/否**：你是否用 CPU KV offload + eagle full-attn prefix（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Kernel] Make persistent_topk deterministic（vLLM PR #55122, created **2026-09-03**）](https://github.com/vllm-project/vllm/pull/55122)
  - 摘要: Fixes #54521。`persistent_topk`（QSA / sparse-indexer block selection）同输入不同输出：output slot 由 `atomicAdd` 按 thread 到达顺序分配，threshold tie 时 first-come 选取 → sparse attention 求和顺序不同 → greedy decode fork。Qwen3.8-Flash-Next 7.5k-token forward bit-bisection 确认 indexer 是第一个输出 diverge 的模块。Fix：single-CTA 行用 `det_select_row`（radix select + index-ordered block scan）；multi-CTA 行保留 radix rounds 但 emission 改为 deterministic（per-CTA count → prefix → index rank）。
  - 为什么重要(对我): **Talk ≥3 turns + sparse attention/indexer**——sparse indexer 是 attention 子系统的 pre-attention 路由；nondeterminism 使 temperature=0 eval 的 logprob assertion 不可复现。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serving Qwen3.8 sparse indexer + temperature=0 eval（是/否/未知）。

- [[Bugfix] Reject quantized KV cache for encoder-only attention（vLLM PR #55150, created **2026-09-03**）](https://github.com/vllm-project/vllm/pull/55150)
  - 摘要: Fixes #55048。encoder-only model（如 `BAAI/bge-large-en-v1.5`）与 quantized KV cache dtype 各自独立被接受，但组合 unsupported，直到 attention backend init/execute 才 fail。Fix：在 `VllmConfig` 构造期用 canonical V1 KV-cache quantization classifier（FP8/per-token-head/NVFP4/TurboQuant）拒绝 `encoder_only + quantized`；decoder model 不受影响。
  - 为什么重要(对我): **Talk ≥3 turns + Attention KV layout**——encoder embedding serving 的 KV cache 与 decoder 不同；late fail = RAG eval 在 config 层无法 gate。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 encoder-only model + 非 auto KV cache dtype（是/否/未知）。

- [[Bugfix] Fix DeepSeek V4 K-cache gather for strided block tables（vLLM PR #55109, created **2026-09-03**）](https://github.com/vllm-project/vllm/pull/55109)
  - 摘要: `_dequantize_and_gather_k_kernel` 接受 `block_table` tensor view 但用 `block_table.shape[-1]` 跨行前进；narrowed view 保留 backing table 更大 `stride(0)` → 第二行起读错误 physical block ID，可能 OOB。Fix：传 `block_table.stride(0)` 到 kernel；contiguous table 时 `stride(0)==shape[-1]` 行为不变。
  - 为什么重要(对我): **Talk ≥3 turns + MLA/Attention K-cache**——compressed KV gather 是 attention 读路径核心；strided view bug = long-context eval 仅首 request 正确。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 narrowed block_table view serving DeepSeek V4（是/否/未知）。

- [[Bugfix][Core] Hand off a producer's partial-tail boundary state on preemption（vLLM PR #55142, created **2026-09-03**）](https://github.com/vllm-project/vllm/pull/55142)
  - 摘要: KV-producer request 在 Mamba "align" mode + `--prefix-match-unit` 下，prefill 停在 last hash boundary 时保留 partial-tail SSM state 并 hand off block 给 KV connector 供后续 hybrid (attention + Mamba) prefix hit。Preemption 路径既不触发 next chunk CoW 也不走 `_connector_finished` → blocks 被 free、pending producer tail 被 drop，resume 后不再拥有 boundary block。Disaggregated prefill + 小 KV pool 下这是常见 case。
  - 为什么重要(对我): **Talk ≥3 turns + hybrid attention/Mamba**——hybrid model 的 prefix cache 跨 attention/Mamba 边界；preemption 丢 state = long-context agent eval 在 KV 压力下不可复现。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 disaggregated prefill + Mamba align mode（是/否/未知）。

- [[Bugfix][GLM] Invalidate merged KDA convolution weights after refit（vLLM PR #55144, created **2026-09-03**）](https://github.com/vllm-project/vllm/pull/55144)
  - 摘要: Fixes #55087。GLM-5.3 KDA 懒缓存 concatenated q/k/v conv weights 于 `_merged_conv_weight`；online-RL weight refit 后 underlying conv weights 更新但 merged tensor 仍 stale → inference 继续用 dummy/previous weights。Fix：refit 前显式 invalidate KDA cache；覆盖 direct model loading 与 CausalLM loading 两路径。
  - 为什么重要(对我): **Talk ≥3 turns + KDA/conv attention**——KDA 是 GLM attention 变体的核心算子；stale merged weight = RL eval 在 refit 后 silent regression。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 online-RL refit GLM-5.3 KDA model（是/否/未知）。

## 3) 热门项目/可试个人项目

- [helloianneo/ian-xiaohei-illustrations（@ianneo_ai, GitHub trending **2026-09**）](https://github.com/helloianneo/ian-xiaohei-illustrations)
  - 摘要: @ianneo_ai 的 Codex Skill，指导 AI Agent 为中文文章生成 16:9 白底手绘怪诞配图（视觉 IP「小黑」）；先提取认知锚点（判断/流程/结构/隐喻）再出 shot list + PNG。GitHub **10.9k+ stars**，本周 trending #2。作者定位 product designer / 一人公司实践者。
  - 为什么重要(对我): **skill 落地参考**——完整的 skill 结构（SKILL.md + 风格约束 + shot list workflow + QA checklist）可直接对照你的 agent eval playbook 的 skill 模板；一人公司 + skill 出海的可见性案例。
  - 明日动作(<=20min): 读 repo 的 `SKILL.md` 结构，写 **3 条** 你的 skill 缺什么 vs 此 repo（各 ≤15 词）。
  - 估计复现成本: **低**（读 SKILL.md + 写 3 条对照，~15min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [@ianneo_ai: Skill ranked #2 on GitHub trending（**2026-09**）](https://github.com/helloianneo/ian-xiaohei-illustrations)
  - 机会信号: 一人公司 builder 用 **可复用 Codex Skill**（非 SaaS）拿到 GitHub trending #2 + 10k stars → 「方法论/视觉 IP + skill 分发」是 AI solo company 的低代码杠杆路径；作者同时维护 `awesome-claude-code-skills`（455 stars）和 `obsidian-ai-second-brain`（245 stars）形成 skill 矩阵。
  - 风险信号: 插画 skill 壁垒在视觉 IP 与中文内容审美，非通用 agent 工程；trending 热度≠持续收入；X 原帖无法稳定抓取验证 trending 时间戳。
  - 我可执行的一步(<=30min): 写 **3 条** 你的 agent eval/skill 经验能否裁剪成类似「可安装 skill + 可见 demo」（各 ≤15 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[2949. Count Beautiful Substrings II](https://leetcode.com/problems/count-beautiful-substrings-ii/)（**Day 64, Sep 2026 Daily Challenge**，承接 0902 的 2948「值域连续段 DSU/排序分组」，今日 **Hard**「vowels×consonants 为完全平方数 + 元音数≡0(mod √k) → 前缀 (vowels, vowels−consonants) 哈希计数」）：关键洞察——与 2947 I 版同构但 k 可到 1000，须先求 √k 的 square root 再模运算；与 agent eval「带模约束的状态等价类计数」同构。明日动作：手写前缀哈希解法；写 **≤15 词**：「模 √k 约束」对你 eval 状态空间意味着什么。

- **IELTS（≤1 条）**：[IELTS Reading Practice Tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/reading)：0902 已轮换 Writing，今日换 **Reading**（选 1 套 passage，限时 ~20min）。明日动作：完成 1 次 Reading passage，记录 **≥2 个** 定位/同义替换失误；写 **≤15 词** 你最弱的 Reading 维度（Skimming / Scanning / T/F/NG / Vocabulary）。

## 6) 噪音过滤记录

- **chore(deps) PRs（LangGraph #8804, LangChain #40162/#40171）**：依赖 bump / model profile 刷新，非工程信号 → 过滤。
- **docs PRs（LangChain #40173/#40175, LlamaIndex #22946, vLLM #55148）**：文档补充，与当前 focus 弱相关 → 过滤。
- **core: fix _convert_to_v1_from_anthropic mutating message.content（LangChain #40169）**：0902 已收录 #40138 同 fix → semantic duplicate，不重复。
- **fix(core): restore unit tests for list output parsers（LangChain #40170）**：test-only restore，#40096 已覆盖 parser fix → 不重复展开。
- **fix(checkpoint-sqlite): honor fields index configuration（LangGraph #8809）**：index config fix，与当前 focus 弱相关 → 过滤。
- **fix(core): reject nested dataclasses in generic fields（AutoGen #8164）**：serialization 边界，偏 infra → 过滤。
- **[[Memory][GDN] Bound FLA chunk-scan workspace（vLLM #55149）**：KV sizing infra，与当前 focus 弱相关 → 过滤。
- **[[ROCm][Bugfix] topk_ids width（vLLM #55147）**：ROCm-specific → 过滤。
- **0901/0902 已收录项**：ToolMessage call ID #8788、Granite double-encode #54964、PP tensor send #54962、FlashInfer MoE nondeterministic #54948 → semantic duplicate，不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-03** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + @ianneo_ai repo ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **872**（`0902.md` §7: 863 + 本日 §1: 9）
- Theory: **388**（`0902.md` §7: 383 + 本日 §2: 5）
- Career/Business: **134**（`0902.md` §7: 133 + 本日 §4: 1）
- LC: **116**（`0902.md` §7: 115 + 本日 §5: 1）
- IELTS: **116**（`0902.md` §7: 115 + 本日 §5: 1）
