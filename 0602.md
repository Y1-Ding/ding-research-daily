# Daily Research - 2026-06-02 (21:30)

> 采集口径：优先 **X_WHITELIST / YOUTUBE_WHITELIST / GITHUB_WHITELIST / BLOG_WHITELIST / CAREER_BUSINESS_WHITELIST**；评分 `relevance(40)+actionability(25)+credibility(20)+freshness(15)`，正文主段 **≥70**（职业池 **≥65** 且具可执行信号）。与 `0601.md` 及更早文件做 **URL 去重** + **语义去重**。本日 **`[OUTSIDE_WHITELIST]` 0 条** → 主链 **0%（≤20%）**。  
> **自动化提示（给编排器）**：Cron 触发时区若仍为 UTC，请将等价目标对齐 **Asia/Shanghai 每日 21:30**；失败 **重试 1 次**；连续 **2** 个日历日失败再发告警（本仓库文件不承载告警状态机）。

## 0) 今日3条结论

- **LangGraph 1.2.3 已正式发布**：**2026-06-01** 的 [release 1.2.3](https://github.com/langchain-ai/langgraph/releases/tag/1.2.3) 落地 **RemoteGraph v3 流式**、**WebSocket/SSE 传输**、**interleave_projections** 与 **sdk≥0.4.2** 硬绑定——`0601.md` 中「PR 未合并」的阻塞已解除，应立刻做依赖矩阵与回滚演练。
- **自托管 LangGraph 的 `tools`/`lifecycle` 流在 API 层被误杀**：**2026-06-02** 的 [#7986](https://github.com/langchain-ai/langgraph/issues/7986) 暴露 **OpenAPI 校验器与运行时 `StreamMode` 不一致**，导致 `useStream().toolProgress` 在 `langgraph dev` 上 **422**——agent UI 进度条与 deepagents 工具流在本地栈上可能「静默失效」。
- **防御侧规模化正在制度化**：Anthropic **2026-06-02** [Expanding Project Glasswing](https://www.anthropic.com/news/expanding-project-glasswing) 将 Mythos 扫描扩至约 **150** 家关键基础设施伙伴；OpenAI **2026-06-01** [Frontier models and Codex on AWS](https://openai.com/index/openai-frontier-models-and-codex-are-now-available-on-aws/) 把 frontier 能力嵌入企业既有 **治理/采购** 路径——agent 工程的主战场从「能跑 demo」转向 **可审计的生产接入 + 安全闭环**。

## 1) Agent工程化（high-signal）

- [langgraph==1.2.3 正式发布（GitHub Release，**Published 2026-06-01**）](https://github.com/langchain-ai/langgraph/releases/tag/1.2.3)
  - 摘要: **1.2.3** 合并 [#7945](https://github.com/langchain-ai/langgraph/pull/7945) 路线：**RemoteGraph v3 streaming**（#7927）、**interleave_projections**（#7938）、**tool-dispatched subagents 命名**（`lc_agent_name`）、**WebSocket stream transports** 与 **messages/tool call projections**（sdk-py #7818–#7830）；修复 **ensure_config 合并而非覆盖**、**用户取消 vs 其他取消** 区分、**ProtocolEvent.event_id** 与 wire 字段对齐；**langgraph-sdk 地板升至 ≥0.4.2**（`thread_id` 路径编码修复）。
  - 为什么重要(对我): 远程托管 agent 的 **流式协议 v3** 已是默认路径；与 `0601.md` 的「待合并」状态相比，本日是 **可执行的升级窗口**。
  - 明日动作(<=20min): `pip install -U 'langgraph==1.2.3' 'langgraph-sdk>=0.4.2'` 后在 **1 条** RemoteGraph 调用上验证 `stream_mode` 含 `messages` + `tools`（或你方等价模式）；记录版本号。

- [langgraph-api 0.9.0：`stream_mode` 含 `tools`/`lifecycle` 时 422（Issue #7986，**Opened 2026-06-02**）](https://github.com/langchain-ai/langgraph/issues/7986)
  - 摘要: **请求体验证器**（bundled `openapi.json`）的 `stream_mode` 枚举 **缺少 `tools` 与 `lifecycle`**，但 `langgraph_api.schema.StreamMode` 与 **streaming runtime** 均接受；访问 `useStream().toolProgress` 会 **自动注册 `tools` 模式** 并触发 **422**，阻断 deepagents **0.6** 的 `tool_calls` 进度 UX 与自托管 `langgraph dev`。
  - 为什么重要(对我): 本地/自托管 agent 栈的 **可观测性与前端进度** 依赖这些 stream mode；validator 不同步会造成 **「代码正确、线上 422」** 类故障。
  - 明日动作(<=20min): 若你用自托管 LangGraph API，用 issue 内 **3 行** `RunCreateStateful.is_valid` 复现脚本测 `["tools"]`；结果记 **True/False** 并标当前 `langgraph-api` 版本。

- [ToolNode 无法规范化 MCP 返回的 content block list（Issue #7985，**Opened 2026-06-02**）](https://github.com/langchain-ai/langgraph/issues/7985)
  - 摘要: `langchain-mcp-adapters` 按 LangChain Core 标准返回 **`list[{"type":"text",...}]`**，但 `ToolNode._normalize_tool_response` 仅接受 **Command/ToolMessage 列表**，在 deepagents 等路径下抛 **`expected all Command or ToolMessage`**；同文件 `msg_content_output` **已识别** content block 格式，形成 **内部矛盾**。
  - 为什么重要(对我): **MCP 是 LangGraph 官方推荐的工具接入方式**；该缺口直接影响 **多工具 agent + MCP** 生产路径的可靠性。
  - 明日动作(<=20min): 若你接 MCP，列 **1 个** 工具是否可能返回 content block list（是/否）；若是，在 issue 下用 **≤20 词** 备注你的 middleware 路径。

- [[KV Offload] `on_schedule_end()` 已合并（vLLM PR #44206，**Merged 2026-06-02**）](https://github.com/vllm-project/vllm/pull/44206)
  - 摘要: `OffloadingManager` / `SecondaryTierManager` 新增 **`on_schedule_end()`**，由 `OffloadingConnectorScheduler.build_connector_meta()` 在 **每个 scheduler step 结束** 调用，将 **finished jobs、batched promotions、per-step gate 重置** 与 **`take_events()` 纯观测** 分离（`0601.md` 记为 Open；本日 **已合并**）。
  - 为什么重要(对我): 长会话 agent 使用 **多层 KV offload** 时，**步边界副作用** 现在有单一钩子，便于写 **集成测试与 tracing**。
  - 明日动作(<=20min): 若用 vLLM KV connector，更新 **1 条** 内部 runbook：在 `on_schedule_end` 与 `take_events` 各写 **≤8 词** 职责。

- [OpenAI frontier models and Codex on AWS（OpenAI Blog，**2026-06-01**）](https://openai.com/index/openai-frontier-models-and-codex-are-now-available-on-aws/)
  - 摘要: OpenAI **frontier 模型** 与 **Codex** 在 AWS **GA**：经 **Amazon Bedrock** 提供模型 API，Codex 作为 **软件工程 agent** 进入 AWS 环境；强调企业通过既有 **安全、合规、采购、计费** 工作流落地；并预告 **Daybreak**（含 cyber 模型与 Codex Security）未来经 AWS 供给安全团队。
  - 为什么重要(对我): **企业 agent 部署** 的摩擦点在 **云市场与治理**，而非模型能力本身；对你方 **出海/ToB agent** 架构选型有参照。
  - 明日动作(<=20min): 用中文写 **≤25 词**：对比你方若上 AWS，**Bedrock OpenAI** vs **自托管 vLLM+LangGraph** 的 **1 个** 决策维度（成本/合规/延迟三选一）。

- [Hackers Simply Asked Meta AI…（Simon Willison Blog，**2026-06-01**）](https://simonwillison.net/2026/Jun/1/hackers-simply-asked-meta-ai/)
  - 摘要: 多源证实攻击者通过 **Meta AI 支持聊天机器人** 对话即可将 **高粉 Instagram** 账户恢复流程 **跳步绑定攻击者邮箱**——属于 **支持 bot 权限过大 + 账户接管** 类 agent 安全事故，而非复杂 prompt injection。
  - 为什么重要(对我): **客服/支持类 agent** 的工具权限边界是 **一等安全需求**；对你设计 **human-in-the-loop 恢复流程** 有直接警示。
  - 明日动作(<=20min): 为你方 **1 个** 支持类 agent 草拟 **禁止单轮完成的敏感操作清单**（≤3 条，每条 ≤12 词）。

## 2) 原理深化（Attention/Transformer/Agent）

- [LangGraph 1.2.3：v3 流式 wire 契约与 `event_id`（Release / PR #7942）](https://github.com/langchain-ai/langgraph/releases/tag/1.2.3)
  - 摘要: **ProtocolEvent.eventId → event_id** 等与 wire 字段对齐；v3 流式引入 **messages/tool call projections** 与 **interleave**——本质是 **多通道 token/事件流** 在 HTTP/WebSocket 上的 **序列化不变量** 问题。
  - 为什么重要(对我): **三回合追问**：(1) 你的客户端是否假设 **单通道 JSON delta**？(2) **interleave** 下如何保证 **tool call 与 message chunk 的因果序**？(3) 协议字段重命名时如何做 **向后兼容测试**？
  - 明日动作(<=20min): 画 **≤4 节点** 序列图：`RemoteGraph.stream` → transport → client reducer（每节点 ≤6 词）。

- [MCP content block list vs ToolNode 归一化（Issue #7985，**2026-06-02**）](https://github.com/langchain-ai/langgraph/issues/7985)
  - 摘要: LangChain Core 将 **content block list** 视为合法 `ToolMessage.content`，但 `ToolNode` 在 **归一化层** 未统一——触及 **Agent 工具环** 中 **「工具输出类型」与「消息类型」** 的接口不变量。
  - 为什么重要(对我): 与 **多模态 tool output**、**推理态分流** 同属 **类型系统/契约** 问题，而非单点 bug。
  - 明日动作(<=20min): 列出你方 tool 返回类型 **3 类**（string / ToolMessage / content blocks）及当前处理方（各 ≤8 词）。

## 3) 热门项目/可试个人项目

- [Tool: Pasted File Editor（Simon Willison Blog + 在线 demo，**2026-06-02**）](https://simonwillison.net/2026/Jun/2/pasted-file-editor/)
  - 摘要: 仿 Claude **大段粘贴自动转附件**：超过 **1000 字符** 的粘贴或文件拖入会在编辑器下方生成 **附件预览**（含图片缩略图），编辑器正文不变；在线工具 [tools.simonwillison.net/pasted-file-editor](https://tools.simonwillison.net/pasted-file-editor)，原型由 **Codex desktop** 构建。
  - 为什么重要(对我): 对 **长 context agent UI**（日志/代码块/文档）是 **低成本 UX 模式**，可嵌入自建 chat 前端。
  - 明日动作(<=20min): 打开 demo，用中文写 **≤20 词**：你若复现，第一个要测的粘贴场景是什么（日志/代码/截图三选一）。
  - 估计复现成本: **低**（纯前端；无 GPU）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Expanding Project Glasswing（Anthropic News，**2026-06-02**）](https://www.anthropic.com/news/expanding-project-glasswing)
  - 机会信号: 在首批约 **50** 家伙伴已用 **Claude Mythos Preview** 扫出 **万级高/严重漏洞** 基础上，再扩 **~150** 家跨 **电力/水务/医疗/通信/硬件** 等关键基础设施；并发布面向可信安全团队的 **扫描工具** 与 **Claude Security** 产品——**AI 辅助安全** 正在形成 **行业标准与采购品类**。
  - 风险信号: Anthropic 预判 **6–12 个月内** 多家公司将拥有 **Mythos 级 cyber 模型** 且可能 **无足够防护** 公开发布；攻击面扩大要求 **防御方规范与补丁吞吐** 同步升级——对 **小型 AI 公司** 意味着 **合规与安全能力** 成为出海门槛。
  - 我可执行的一步(<=30min): 阅读文中 **「bottleneck 在验证/披露/修补」** 段，用中文写 **≤40 词**：你若做 agent 产品，**1 个** 可对外承诺的 **安全披露/补丁 SLA** 应如何表述（自定）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：LeetCode Explore [`Binary Search`](https://leetcode.com/explore/learn/card/binary-search/) 中 **Search Insert Position**：把「**有序数组上维护不变区间 [left,right]**」映射为 agent **「在候选工具/文档索引上做单调收敛搜索」**。明日动作：用中文写 **≤30 词**：为你 **1 个** RAG 检索参数（top_k 或 score 阈值）标 **left/right 各代表什么**（各 ≤10 词）。

- **IELTS（≤1 条）**：浏览 [IELTS 官方 — Writing test format (Academic)](https://ielts.org/take-a-test/test-format/academic-writing) 中 **Task 1（图表描述，150 词，20 分钟）** 的结构要求。明日动作：用中文写 **≤35 词**：把 **Task 1 四段式（概述→关键特征→对比→总结）** 与你 **英文 agent 评测报告** 的 **1 个** 共用段落模板（自定）。

## 6) 噪音过滤记录

- **`https://github.com/langchain-ai/langgraph/pull/7945`（未发布叙事）**：`0601.md` §1 已述「未合并」；本日以 [release 1.2.3](https://github.com/langchain-ai/langgraph/releases/tag/1.2.3) **替代**，避免重复 PR 条目。
- **`https://github.com/vllm-project/vllm/pull/44206`（Open 状态）**：`0601.md` §1 **语义去重**；本日仅保留 **Merged 2026-06-02** 新信号。
- **`https://github.com/run-llama/llama_index/pull/21843`**：与 `0601.md` §1/§2 **URL 去重**（仍 Open，无 6/2 实质更新）。
- **`https://simonwillison.net/2026/May/30/pyodide-asgi-browser/`**、**`datasette 1.0a32`**：与 `0601.md` §1/§3 **去重**。
- **`https://openai.com/index/strengthening-societal-resilience-with-rosalind-biodefense/`** 等 **5 月下旬** OpenAI 文：与 `0528.md`–`0531.md` **去重**。
- **`https://github.com/run-llama/llama_index/pull/21850`（draft notebook）**：信号偏 **文档示例**，评分边缘；本日 **未入主段**（可在下周扩 whitelist 采样时再评）。
- **X_WHITELIST / YOUTUBE_WHITELIST / Bilibili 当日帖文**：无法稳定抓取原帖 URL/正文 → 不收录。
- **`https://github.com/openai/evals/commits/main`**：最近实质 commit **2026-04-14 量级** → 丢弃。
- **`https://github.com/microsoft/autogen`**：`merged:>2026-06-01` **0 条** → 无高信号。

## 7) 本周累计计数

- Agent: **214**（`0601.md` §7: 208 + 本日 §1: 6）
- Theory: **83**（`0601.md` §7: 81 + 本日 §2: 2）
- Career/Business: **51**（`0601.md` §7: 50 + 本日 §4: 1）
- LC: **35**（`0601.md` §7: 34 + 本日 §5: 1）
- IELTS: **35**（`0601.md` §7: 34 + 本日 §5: 1）
