# Daily Research - 2026-06-12 (21:30)

## 0) 今日3条结论

- **`langchain==1.3.8` 同日打包「结构化输出路由 + 流式 tool 修复」**：**2026-06-12** 发布，内含 **`langchain-core 1.4.6`**（#35983 v1 流式 tool call 规范化）、**#38042**（结构化输出 fallback 从子串匹配改为 regex 全名匹配）、**`create_agent` overloads**（#34309）——你的 agent eval 应把 **「provider-native structured output 是否误触发 fallback」** 与 **「流式 content_blocks 含 tool_call_chunk」** 作为 **同一升级批次** 回归。
- **生产级 eval 在无 benchmark 领域 = 多模型分歧 + trace 值班制**：Benchling（LangChain Max Agency Podcast，**2026-06-11**）在科学任务上 **跨 provider 并行** 而非同模型重复采样；**模型一致 → 数据质量信号**，**分歧 → 错误信号**；每周 **rotating fire chief** 审 production traces + thumbs up/down——对你做 **RAG/planning agent** 意味着：**clean benchmark 缺失时，用「模型分歧率」作 online eval 指标** 比纯 LLM-as-judge 更便宜。
- **持久化 agent 执行从「产品叙事」进入「收购整合」**：OpenAI **2026-06-11** 宣布收购 **Ona**，将 **客户自控云环境** 并入 **Codex**（任务可跨 **小时/天**、关笔记本仍继续）——与 LangGraph **checkpoint/RemoteGraph**、SmithDB **trace 检索** 形成 **「长时程 agent = 执行环境 + 状态可观测」** 同日拼图。

## 1) Agent工程化（high-signal）

- [langchain==1.3.8（GitHub Release / #38096，**Published 2026-06-12**）](https://github.com/langchain-ai/langchain/releases/tag/langchain%3D%3D1.3.8)
  - 摘要: **`langchain 1.3.8`** 合并 **#38096**，捆绑 **`langchain-core 1.4.6`**、**`langchain-anthropic 1.4.5`**、**#38042** 结构化输出 fallback 收紧、**#34309** `create_agent` typing overloads、**#34584** async middleware decorator typing、**#36470** mypy 2.1 统一类型检查。
  - 为什么重要(对我): 若你刚跟进 **0611** 的流式 tool call 测试（#34707），本 release 是 **可 pin 的单一版本锚点**；升级后应一次跑通 **structured output + streaming tool + create_agent middleware** 三条 smoke。
  - 明日动作(<=20min): 记当前 **`langchain` / `langchain-core` 版本**；写 **是/否** 已 **≥1.3.8 / ≥1.4.6**（各 1 词）。

- [fix(langchain): tighten structured output model fallbacks（#38042，merged 2026-06-11）](https://github.com/langchain-ai/langchain/pull/38042)
  - 摘要: **`FALLBACK_MODELS_WITH_STRUCTURED_OUTPUT`** 从 **宽松子串** 改为 **regex 全段 model-name 匹配**；扩展 **gpt-5.x、Claude 4/5 系、grok-build** 等 provider-native structured output 覆盖；**`_provider_from_model_name`** 复用 **`_attempt_infer_model_provider`**，与 **`init_chat_model`** 行为一致；测试 fixture 从 **gpt-4/o1** 刷新为 **gpt-5.5/gpt-5-nano** 等当前族。
  - 为什么重要(对我): **误触发 fallback** 会让 eval 把 **「走了 JSON mode 旁路」** 当成模型能力——对 **planning agent 结构化 plan** 尤其致命；升级后应对 **主模型 ID** 查是否仍被 **旧子串规则误判**。
  - 明日动作(<=20min): 列出你 **1 个** 主模型字符串；在 #38042 描述中搜该族是否列入 **structured output 白名单**（是/否/未查）。

- [langgraph-cli==0.4.29：本地 dev server 支持 HTTPS cert（GitHub Release / #8031，**Published 2026-06-11**）](https://github.com/langchain-ai/langgraph/releases/tag/cli%3D%3D0.4.29)
  - 摘要: **`langgraph-cli 0.4.29`** 新增 **`certfile` + cert key** 参数，dev server 可跑 **HTTPS**（**Safari 本地调试硬性要求**）；**tunnel 与 SSL 互斥**（gate 防同时开启）。
  - 为什么重要(对我): 你若用 **LangGraph Studio / 本地 HITL UI** 在 Safari 测 agent，此前需反向代理；现可在 **CLI 层** 直接起 HTTPS，缩短 **「本地 agent demo → 给他人验收」** 路径。
  - 明日动作(<=20min): 写 **是/否** 曾在 Safari 测过 LangGraph 本地 UI；若是，记 **`langgraph-cli` 版本** 是否 **≥0.4.29**。

- [Full Text Search in SmithDB: Designing an Inverted Index for Object Storage（LangChain Blog，**2026-06-10**）](https://blog.langchain.dev/full-text-search-in-smithdb-designing-an-inverted-index-for-object-storage)
  - 摘要: SmithDB 在 **对象存储** 上对 agent traces 做全文/JSON 检索，**P50 ~400ms**；挑战包括 **1MB+ inputs/outputs**、**source:index ≈ 1:1.9**、Zipfian 词频；v2 倒排索引用 **字节预算 row group**（32MB postings / 64MB terms）、**FST 词典**、**block-bitpacked deltas**；支持 **`json_key` / `json_key_search` / `search`** 三类谓词。
  - 为什么重要(对我): 你的 **长时程 agent eval** 若只存最终 answer 会丢 **中间 tool I/O**；此文给出 **「trace 当搜索文档」** 的工程约束——写 eval harness 时应预留 **可索引字段**（path + value + free text），而非整包 JSON 扫描。
  - 明日动作(<=20min): 为你 **1 个** agent 写 **≤25 词**：LangSmith/自研日志里 **能否按 tool output 关键词检索**（能/不能 + 字段名或「无」）。

- [How Benchling builds agents when the smartest AI isn't smart enough（LangChain Max Agency，**2026-06-11**）](https://www.langchain.com/blog/benchling-max-agency-podcast)
  - 摘要: Benchling Head of AI **Nicholas Larus-Stone** 与 Harrison Chase 对谈：**跨 provider 多模型** 处理同一任务（非重复采样）；**模型一致 = 质量信号、分歧 = 错误信号**；科学领域 **缺乏 clean benchmark** → 依赖 **每周 trace review（rotating fire chief）** + **用户 thumbs up/down**；强调 **SQL 结构化查询优于 LLM 读数百 notebook**（更快更便宜）。
  - 为什么重要(对我): 这是 **「无 gold label 的 agent eval」** 可迁移 playbook：**模型分歧率** + **人工 trace 值班** 比堆 benchmark 更贴生产；与 **legal agent verifiers**（LangChain Labs）同属 **domain harness > 裸 LLM** 谱系。
  - 明日动作(<=20min): 写 **≤30 词**：你的 agent 是否有 **「双模型/双 provider 交叉检查」** 步骤（有/无 + 触发条件 1 个词）。

- [OpenAI to acquire Ona（OpenAI Blog，**2026-06-11**）[OUTSIDE_WHITELIST]](https://openai.com/index/openai-to-acquire-ona)
  - 摘要: OpenAI 收购 **Ona**（原云开发环境公司，**200 万开发者** 使用其可复现云环境）；将 **客户自控、持久化云执行** 并入 **Codex**——agent 可在 **组织自有云** 内 **跨会话/关盖继续**，满足 **安全、治理、凭证作用域、审计日志**；Codex 周活 **500 万+**（同比 **+400%**）。
  - 为什么重要(对我): **长时程 agent** 的瓶颈常是 **「执行环境归谁」** 而非模型 IQ；Ona 路径 = **intel 在 OpenAI、compute/env 在客户云**——你可对照 **LangGraph Platform / 自托管** 检查 **credential 与 revision 边界** 是否同等清晰。
  - 明日动作(<=20min): 写 **≤25 词**：你的 agent 部署 **执行环境在本地/客户 VPC/第三方 SaaS**（三选一 + 是否支持断点续跑 是/否）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Claude Fable is relentlessly proactive（Simon Willison Blog，**2026-06-11**）](https://simonwillison.net/2026/Jun/11/fable-is-relentlessly-proactive/)
  - 摘要: Simon 两日体验：**Fable 5「relentlessly proactive」**——会主动选多种手段达成目标；案例：调试 Datasette Agent 依赖时 **用户离开数分钟**，回来发现机器仍在 **自主探索依赖、开 Safari、自写 HTML 复现 bug、自研截图脚本**；体现 **agent 规划深度 = 目标坚持 × 工具组合广度**，而非单轮 CoT。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) 你的 agent **无人值守时** 是否有 **步数/工具/浏览器上限**？(2) **proactive 探索** 与 **副作用** 的边界在哪（HITL 闸门）？(3) eval 是否记录 **「用户不在场时的自主步数」** 作为独立指标？
  - 明日动作(<=20min): 为你 **1 个** coding agent 写 **≤20 词** **max_iterations / 无人值守策略**（有上限/无/未配置）。

- [Dealing with verifiable vs non-verifiable tasks（Benchling Podcast 章节，**2026-06-11**）](https://www.langchain.com/blog/benchling-max-agency-podcast)
  - 摘要: Podcast timestamps **14:36** 起讨论 **可验证 vs 不可验证** 科学任务：**可验证**（SQL 结果、结构化 assay 阈值）可走自动化检查；**不可验证**（假设生成、文献综合）依赖 **多模型交叉 + 人工 trace**；与 **「evals when clean benchmarks aren't possible」（16:19）** 连贯。
  - 为什么重要(对我): **Agent 原理**：规划树应 **按可验证性分叉**——可验证子任务用 **tool+assert**，不可验证用 **ensemble+human**；混用会导致 **eval 假阳性**（看起来有 plan 但无法 falsify）。
  - 明日动作(<=20min): 列你 agent **3 个 tool**；标各 **可自动验证 / 需人审**（各 ≤3 词）。

- [SmithDB agent trace 的索引经济学（LangChain Blog，**2026-06-10**）](https://blog.langchain.dev/full-text-search-in-smithdb-designing-an-inverted-index-for-object-storage)
  - 摘要: Agent trace 的 **inputs/outputs 占绝对字节**；**无倒排索引的内容过滤 = 灾难性全量扫描**；词频 **Zipfian**（`"agents"`/`"import"` 几乎每文档出现）要求 **per-term 编码** 而非 per-column 固定位宽——类比 **Attention**：高频 token 需要 **不同「带宽」** 存储与检索。
  - 为什么重要(对我): 把 **trace 检索** 当成 **「对 agent 记忆的稀疏注意力」**——eval 平台若只支持 **run_id 点查** 不支持 **内容谓词**，长对话 debug 成本随 **context 长度超线性** 增长。
  - 明日动作(<=20min): 写 **≤25 词**：你上次 debug agent 是 **按 run_id 还是按 output 关键词** 定位（二选一 + 耗时体感 快/慢）。

## 3) 热门项目/可试个人项目

- [Datasette extras API explorer（Simon Willison，**2026-06-11**）](https://simonwillison.net/2026/Jun/11/datasette-extras-explorer/)
  - 摘要: Simon 用 **Claude Fable 规划 + GPT-5.5 Codex 实现** 的 **单页工具**：输入 Datasette JSON API URL，选择 **`?_extra=`** 参数，展示语法高亮响应；支持 **URL hash 分享配置**；配合 **Datasette 1.0a33** 将 extras 扩展到 **queries/rows**。
  - 为什么重要(对我): **低成本 API 探索器** 模板——你可为 **自己的 agent HTTP tool** 复制「plan 用强模型、implement 用 Codex」分工，验证 **tool schema 文档是否够用**。
  - 明日动作(<=20min): 打开 explorer 页面 **1 次**；写 **≤15 词** 你的 agent 是否缺类似 **「tool 试跑 UI」**（缺/有）。
  - 估计复现成本: **低**（静态页 + 调 API；无 GPU）。

- [langgraph dev HTTPS：certfile + key（#8031，**merged 2026-06-11**）](https://github.com/langchain-ai/langgraph/pull/8031)
  - 摘要: **`langgraph dev`** 新增 SSL 参数；**禁止 tunnel+SSL 同时开**；动机为 **Safari 本地 HTTPS 要求**。
  - 为什么重要(对我): **个人项目 30 分钟可验证**：`mkcert` + `langgraph dev --certfile ...` 在 iPhone/Safari 上点 HITL 按钮，比 ngrok 更少 moving parts。
  - 明日动作(<=20min): 若用 LangGraph，查 `langgraph dev --help` 是否出现 **cert 相关 flag**（是/否）。
  - 估计复现成本: **低**（需本地证书；无模型成本）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [OpenAI to acquire Ona（OpenAI Blog，**2026-06-11**）[OUTSIDE_WHITELIST]](https://openai.com/index/openai-to-acquire-ona)
  - 机会信号: **头部 lab 收购「agent 工作区」而非再训模型**——Codex **5M 周活 / +400%** 说明 **分发+执行环境** 已成护城河；**客户云内持久执行** 是 **企业合规采购** 的硬需求，**AI solo / 出海 B2B** 可卖 **「托管 agent runtime + 客户 VPC 连接器」** 而非裸 API wrapper。
  - 风险信号: 收购 **待监管批准、交割前独立运营**——短期 **无现成 Ona API 可集成**；OpenAI **垂直整合** 挤压 **第三方 agent hosting**（如小型 cloud agent 创业公司）议价空间。
  - 我可执行的一步(<=30min): 用中文写 **≤40 词**：Ona 叙事对你更像 **「大厂会做掉的基础设施」还是「可差异化的细分层」**（二选一 + 1 理由）；列 **1 个** 你可写的 **「客户云内 agent 执行」** landing 页标题（≤12 词，不必上线）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 981. Time Based Key-Value Store](https://leetcode.com/problems/time-based-key-value-store/)：练 **时间戳版本化 KV + 二分取 floor timestamp**——对照 **SmithDB trace 按时间检索** 与 **LangGraph checkpoint 版本**。明日动作：实现后写 **≤20 词**：`get(key, timestamp)` 二分条件是 **找最后一个 ≤ timestamp**（对/错）。

- **IELTS（≤1 条）**：[IELTS Preparation Resources](https://www.ielts.org/for-test-takers/how-to-prepare)：官方 **sample tests / Writing 评分说明视频 / apps & webinars** 入口；与 **机考出分、OSR 同 delivery mode**（见 `0610.md`）连贯。明日动作：选 **Writing** 看 **1 条官方评分说明视频**（≤15 min），写 **≤15 词** Task 2 你最弱环节（论证/衔接/词汇 选 1）。

## 6) 噪音过滤记录

- **`langchain-core 1.4.5/1.4.6` 流式 tool call（#34707、#35983）**：`0611.md` **URL/语义去重** → 本日仅经 **1.3.8 打包** 引用。
- **Anthropic Fable safeguard 回调、Claude Corps、datasette-agent 0.2a0**：`0611.md` 已深度覆盖 → 丢弃。
- **Simon「untrusted SQL SQLite vs PostgreSQL」**：`0611.md` 已收录 → 丢弃。
- **Benchling 企业博客（benchling.com，Apr 2026）**：内容偏营销且与 **Jun 11 Podcast** 重叠 → 丢弃，保留 **langchain.com** 可核验稿。
- **Anthropic「Policy on the AI Exponential」**：newsroom 可见但正文 **timeout** → 不收录。
- **Lil'Log / DeepMind Blog**：首页无 **2026-06-12** 新文 → 丢弃。
- **X_WHITELIST / YOUTUBE_WHITELIST / Bilibili**：无法稳定抓取原帖 → 不收录。
- **vLLM 0.22.1 / llama_index 5 月发布 / openai/evals 无 release**：无 **2026-06-12** 新鲜信号 → 丢弃。

## 7) 本周累计计数

- Agent: **275**（`0611.md` §7: 269 + 本日 §1: 6）
- Theory: **110**（`0611.md` §7: 107 + 本日 §2: 3）
- Career/Business: **62**（`0611.md` §7: 61 + 本日 §4: 1）
- LC: **44**（`0611.md` §7: 43 + 本日 §5: 1）
- IELTS: **44**（`0611.md` §7: 43 + 本日 §5: 1）
