# Daily Research - 2026-07-24 (21:30)

## 0) 今日3条结论

- **LangGraph `Runtime.merge` 会把显式 falsy 值当缺失**（LangGraph #8428, **2026-07-24**）：`context={}` 等合法空值被 `or` fallback 吞掉 → nested graph 子运行时丢配置；skill 落地若依赖 runtime context 传参，eval 必须测「显式空 dict/list/0 是否保留」。
- **`ChatOpenAI` 会原地污染共享 `model_kwargs["text"]`**（LangChain #39049, **2026-07-24**）：structured-output 调用把 `format` merge 进浅拷贝后的同一嵌套 dict → 后续普通调用仍 stuck 在旧 schema；agent eval 若复用同一 model 实例测多 schema，会出现不可复现假阳性。
- **vLLM 实验性 ZoomKV 后端落地**（vLLM #49719, **2026-07-24**）：hierarchical Quest chunk filtering + sparse KIVI reranking + 4-bit Key summary + KV CPU offload —— 长上下文 Attention 预算从 dense 全量 KV 转向可配置稀疏检索；agent serving eval 应单独测 recall vs latency tradeoff。

## 1) Agent工程化（high-signal）

- [fix(langgraph): preserve falsy values in runtime merge（LangGraph PR #8428, updated **2026-07-24**）](https://github.com/langchain-ai/langgraph/pull/8428)
  - 摘要: `Runtime.merge` 原先对 falsy 值（`{}`、`[]`、`0`、`False`）做 `or` fallback，显式传入的空 `context` 等被当作未提供而丢弃。Fix：仅对 `None` fallback；补全所有 nullable runtime 字段单测 + nested-graph 显式空 context 回归测试。Fixes #8406。
  - 为什么重要(对我): **skill 落地 + 评测**——子 graph / middleware 常靠 runtime 传配置；空 context 被吞 = 子 agent 行为漂移且 eval 难复现。
  - 明日动作(<=20min): 写 **是/否**：你 nested graph 是否传过显式空 `context`/`store`（是/否/未知）。

- [fix(autogen-ext): skip LangChain callback-manager when inferring tool args schema（AutoGen PR #7994, updated **2026-07-24**）](https://github.com/microsoft/autogen/pull/7994)
  - 摘要: `LangChainToolAdapter` 从无 `args_schema` 的 LangChain tool 推断 pydantic args model 时，把 `_run`/`_arun` 注入的 `run_manager`（`CallbackManagerForToolRun`）也当字段 → pydantic 无法生成 schema 直接 crash（如 `GoogleDriveSearchTool`）。Fix：推断时跳过 callback-manager 类型参数（含 `Optional`/`Union` unwrap + `run_manager` 命名 fallback）。Fixes #6385。
  - 为什么重要(对我): **skill 落地**——把 LangChain tool 包进 AutoGen agent 是常见互操作路径；schema 推断失败 = 整条 tool eval 管线阻断。
  - 明日动作(<=20min): 写 **是/否**：你是否把 LangChain Community tool 直接挂进 AutoGen（是/否/未知）。

- [fix(core): await tool retriever in async query（LlamaIndex PR #22370, updated **2026-07-24**）](https://github.com/run-llama/llama_index/pull/22370)
  - 摘要: `ToolRetrieverRouterQueryEngine._aquery()` 在异步路径仍调用同步 `retrieve()`，远程 I/O retriever 会阻塞 event loop。Fix：异步路径改 `await ObjectRetriever.aretrieve()`，同步 `_query()` 保持 `retrieve()`。含回归测试防 `aquery()` 触碰 sync API。Fixes #22369。
  - 为什么重要(对我): **skill 落地 + 评测**——tool routing agent 的 async eval harness 若 event loop 被 sync I/O 卡住，latency/token 指标全失真。
  - 明日动作(<=20min): 写 **是/否**：你 tool router 是否走 `aquery()` 异步路径（是/否/未知）。

- [fix(openai-responses): accumulate streamed text across chunks（LlamaIndex PR #22450, updated **2026-07-24**）](https://github.com/run-llama/llama_index/pull/22450)
  - 摘要: `OpenAIResponses.stream_chat()` / `astream_chat()` 每个 `ResponseTextDeltaEvent` 都把 `blocks` 重置为仅含当前 delta 的 `TextBlock`，中间 chunk 的 `ChatResponse.message` 不含累积文本，只有 terminal `ResponseCompletedEvent` 才正确。Fix：跨 chunk 累积 streamed text。
  - 为什么重要(对我): **评测 + token 优化**——streaming eval 若读中间 chunk 做 early-stop / partial judge，会得到截断假输出；trace 解析也会错位。
  - 明日动作(<=20min): 写 **是/否**：你 streaming eval 是否读非 terminal chunk 内容（是/否/未知）。

- [fix(openai): stop mutating shared `model_kwargs["text"]`（LangChain PR #39049, updated **2026-07-24**）](https://github.com/langchain-ai/langchain/pull/39049)
  - 摘要: `ChatOpenAI` 构建 Responses API payload 时浅拷贝 `model_kwargs`，但 `payload["text"]` 与 `model_kwargs["text"]` 仍共享同一嵌套 dict；structured-output 调用把 `format`/`verbosity` merge 进 `payload["text"]` 会永久污染实例 → 后续普通调用 stuck 在旧 format。Fix：深拷贝或隔离 merge 目标。Closes #38869。
  - 为什么重要(对我): **评测**——同一 model 实例跑多轮 eval（plain + structured + 换 schema）会出现跨 case 污染，门禁阈值无意义。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否复用同一 `ChatOpenAI` 实例测多种 output format（是/否/未知）。

- [fix(core): produce UUID document IDs for all `key_encoder` algorithms（LangChain PR #39051, updated **2026-07-24**）](https://github.com/langchain-ai/langchain/pull/39051)
  - 摘要: `index()` 推荐从默认 `sha1` 迁移，但 `sha256`/`sha512`/`blake2b` 三个 encoder 产出 raw hex digest 而非 UUID → Qdrant 等要求 UUID record ID 的 vector store 每次 indexing 失败。Fix：三 encoder 统一走 UUIDv5 wrap（与 `sha1` 一致）。Breaking：旧 ID 变更，incremental/full cleanup 会删旧记录。Fixes #39047。
  - 为什么重要(对我): **skill 落地 + RAG eval**——RAG agent collect 层换 encoder 后 silent indexing 失败 = gold 集检索全 miss。
  - 明日动作(<=20min): 写 **是/否**：你 vector store 是否要求 UUID document ID（是/否/未知）。

- [fix(core): make similarity_top_k=0 return no results instead of all（LlamaIndex PR #22425, updated **2026-07-24**）](https://github.com/run-llama/llama_index/pull/22425)
  - 摘要: `get_top_k_embeddings` / `get_top_k_mmr_embeddings` 中 `similarity_top_k=0` 因 falsy 判断被当成「取全部」：`if similarity_top_k and len(...) > similarity_top_k` 永不 trim，`similarity_top_k_count = similarity_top_k or embedding_length` 把 0 静默变 embedding 全长。Fix：`top_k=0` 返回 `[]`。
  - 为什么重要(对我): **评测 + token 优化**——用 `top_k=0` 做 ablation / 空检索 baseline 时结果非空 = eval 对照组污染。
  - 明日动作(<=20min): 写 **是/否**：你 RAG eval 是否用过 `similarity_top_k=0` 作对照（是/否/未知）。

- [feat(benchmark): support custom system prompt, request_id tracking, and length filtering controls in bench serve（vLLM PR #43904, updated **2026-07-24**）](https://github.com/vllm-project/vllm/pull/43904)
  - 摘要: `vllm bench serve` 新增：`--system-prompt` 前置 system 指令；保留并追踪 dataset 级 `request_id` 写入最终结果；`--disable-len-filter` / `--max-prompt-len` / `--max-total-len` 控制或绕过 prompt 长度过滤。
  - 为什么重要(对我): **评测 + token 优化**——agent serving batch eval 需要可复现 request_id 关联 trace；system prompt 固定 + 长度过滤可控 = 压测基线更干净。
  - 明日动作(<=20min): 写 **≤15 词**：你 serving bench 是否记录 per-request `request_id`（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Attention] Add experimental ZoomKV backend（vLLM PR #49719, updated **2026-07-24**）](https://github.com/vllm-project/vllm/pull/49719)
  - 摘要: 实验性 V1 ZoomKV attention backend：hierarchical Quest parent/child chunk filtering、dense/sparse KIVI token reranking、physical-block min/max/centroid + 4-bit Key summary、可选 K+V CPU offload（warm/cold/restore）+ prefix-cache 恢复、CUDA/Triton kernel + recall instrumentation。限制：mixed prefill/decode 与 speculative decoding 回退 dense；CUDA Graphs / KV connectors 未支持。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：长上下文 agent 的 KV 预算核心在「哪些 token 进入 attention」；ZoomKV 把稀疏检索引入 serving 层 —— 理解 recall instrumentation 与 dense fallback 边界 = 评测 agent 长对话时的 latency/质量 tradeoff。
  - 明日动作(<=20min): 写 **≤15 词**：你长上下文 eval 是否测 KV 截断 recall（是/否/未知）。

- [[Bugfix] Support non-uniform page sizes in KVBlockZeroer（vLLM PR #49704, updated **2026-07-24**）](https://github.com/vllm-project/vllm/pull/49704)
  - 摘要: 多 KV cache group 不同维度（如 GLM-5.2 的 MLA + DSA indexer attention）产生不同 page size；`KVBlockZeroer` 原先断言单一 uniform page size → init crash。Fix：按 page size 分组 segment，每组 launch 一次 Triton zeroing kernel。Fixes #49696。
  - 为什么重要(对我): **Attention 机制**：heterogeneous attention（MLA + indexer）意味着 KV cache 物理布局非均匀；zeroing 路径不跟 = 多 head 架构 silent init 失败。
  - 明日动作(<=20min): 写 **≤15 词**：你关注 heterogeneous KV cache layout 吗（是/否/未知）。

- [[Render][2/n] Paged shared memory storage for mm tensor ipc（vLLM PR #49667, updated **2026-07-24**）](https://github.com/vllm-project/vllm/pull/49667)
  - 摘要: 基于 #48088，对比 multimodal tensor IPC 三种路径性能：paged shared memory（本 PR）、direct_rpc（msgspec 序列化）、torch_shm（零拷贝）。当前主要为 benchmark，强调 low-copy transfer + 全异步 pipeline 方向；注：LRU/SHM multimodal processor cache 均非 thread-safe。
  - 为什么重要(对我): **Attention + token 优化**：multimodal agent 的 image tensor IPC 成本直接影响 prefill latency；paged SHM vs RPC 选型 = 理解 mm prefill 在 attention 前的数据通路瓶颈。
  - 明日动作(<=20min): 写 **≤15 词**：你 mm agent serving 是否 profiling 过 IPC 开销（是/否/未知）。

## 3) 热门项目/可试个人项目

- [langchain-ai/deepagents — per-leaf LangSmith token & cost analysis（commit 5c9f750, **2026-07-23**）](https://github.com/langchain-ai/deepagents/commit/5c9f750543a49301b277bf05881dea3dadfa1eb6)
  - 摘要: deepagents unified eval workflow 新增 `collect_langsmith_usage.py`：从 LangSmith 拉取 per-leaf token & cost，集成进 `aggregate_shards.py` / `aggregate_unified.py` 与 CI workflow（`unified_evals.yml`、`_harbor_run.yml`），含 500+ 行单测。
  - 为什么重要(对我): **评测 + token 优化杠杆**——agent eval 不只 pass/fail，还要 per-step token/cost 归因；可直接借鉴其 shard 聚合模式进你的 collect→eval pipeline。
  - 明日动作(<=20min): 读 `collect_langsmith_usage.py` 前 50 行；写 1 条你可映射的 per-leaf metric 名（≤10 词）。
  - 估计复现成本: **中**（需 LangSmith 账号 + 现有 eval shard 结构，≤2h 移植核心逻辑）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Milestone #1 — I built an AI workforce to run my SaaS in 3 days（Indie Hackers, Ryven）](https://www.indiehackers.com/post/milestone-1-i-built-an-ai-workforce-to-run-my-saas-in-3-days-heres-exactly-what-i-built-and-why-66d9ecfd98)
  - 机会信号: 三层 agentic marketing（Orchestrator → Marketing Director → Specialist agents）+ Supabase 共享 memory；**approval flow 不可协商**——任何 post/spend/publish 必须 Slack 显式 approve/reject；3 天 evenings 用 Claude Code 搭完，正用自家 Crevaxo 验证后开放 Ryven early access。
  - 风险信号: 案例自述难独立验证外部品牌效果；三层编排 maintenance 成本高；无公开 eval harness / incident 复盘；国内出海支付/合规路径不同。
  - 我可执行的一步(<=30min): 写 **≤8 行**：你当前 1 个 agent 工作流中，哪 1 步最适合加「Slack approve + eval 门禁」而非全自主执行。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 3514 Number of Unique XOR Triplets II](https://leetcode.com/problems/number-of-unique-xor-triplets-ii/)（**Day 24, Jul 2026, Hard**；承接 0723 的 3513 I）：求不同 `(i,j,k)` 三元组 XOR 值种类数（II 版约束更强）。明日动作：对比 3513/3514 差异；写 **≤15 词**：XOR 状态空间与 agent 离散 action 组合的类比。

- **IELTS（≤1 条）**：[IELTS Ready — British Council 官方免费备考（含 AI Mini Mock）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-ready)：50 分钟 Mini Mock + AI 估分。明日动作：完成 **Reading 1 节 Mini Mock**；写 **≤15 词** 错因类型（定位/同义替换/推理 选 1）。

## 6) 噪音过滤记录

- **LangChain #39000 SummarizationMiddleware、LlamaIndex #22441 multimodal token、vLLM #49445 queue caps**：`0723.md` 已收录 → 不重复（07-24 仅 rebase/merge 更新）。
- **LangGraph #8416 join_stream v2、#8418 PgBouncer SSL**：`0723.md` 已收录 → 不重复。
- **LangChain #39037/#39038 streaming/tool_calls、LlamaIndex #21708 empty choices**：`0723.md` 已收录 → 不重复。
- **vLLM #48804 Eagle warmup、#49347 MXFP4、#49590 qk_norm layout**：`0723.md` 已收录 → 不重复。
- **darkrishabh/agent-skills-eval**：`0719.md`/`0723.md` 已收录 → 今日 §3 改收 deepagents per-leaf token eval。
- **Indie Hackers Polsia $500k**：`0723.md` §4 已收录 → 不重复。
- **Simon Willison Jul 21 Cat/Thariq、Jul 22 OpenAI HF cyberattack**：`0722–0723.md` 已收录或待观察 → 不重复。
- **Lil'Log Harness Engineering（Jul 4）**：高价值但非 07-24 新鲜信号 → 待观察。
- **LangChain #39050 model profiles refresh、#39053 mypy→ty**：内部 tooling/catalog，无直接 eval 动作 → 待观察。
- **LlamaIndex #22448 truncate_text_middle**：偏 utils helper，已入 §1 边缘；单独不够 theory → 未重复收录。
- **LlamaIndex #22446 arbitrary file read、#22447 Tennis API**：安全/示例集成，非今日 focus → 待观察。
- **vLLM #48295 llama3_json tool parser**：`0717–0723.md` 已标注不重复 → 今日仅 rebase。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-24** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Jul 24 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **598**（`0723.md` §7: 590 + 本日 §1: 8）
- Theory: **245**（`0723.md` §7: 242 + 本日 §2: 3）
- Career/Business: **103**（`0723.md` §7: 102 + 本日 §4: 1）
- LC: **85**（`0723.md` §7: 84 + 本日 §5: 1）
- IELTS: **85**（`0723.md` §7: 84 + 本日 §5: 1）
