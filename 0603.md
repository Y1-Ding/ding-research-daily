# Daily Research - 2026-06-03 (21:30)

> 采集口径：优先 **X_WHITELIST / YOUTUBE_WHITELIST / GITHUB_WHITELIST / BLOG_WHITELIST / CAREER_BUSINESS_WHITELIST**；评分 `relevance(40)+actionability(25)+credibility(20)+freshness(15)`，正文主段 **≥70**（职业池 **≥65** 且具可执行信号）。与 `0602.md` 及更早文件做 **URL 去重** + **语义去重**。本日 **`[OUTSIDE_WHITELIST]` 0 条** → 主链 **0%（≤20%）**。  
> **自动化提示（给编排器）**：Cron 触发时区若仍为 UTC，请将等价目标对齐 **Asia/Shanghai 每日 21:30**；失败 **重试 1 次**；连续 **2** 个日历日失败再发告警（本仓库文件不承载告警状态机）。

## 0) 今日3条结论

- **MITRE ATT&CK 难以刻画「agent 编排型」攻击**：Anthropic **2026-06-03** [What we learned mapping a year's worth of AI-enabled cyber threats](https://www.anthropic.com/news/AI-enabled-cyber-threats-mitre-attack) 对 **832** 个封禁账号做映射——AI 使用从 **初始入侵** 转向 **横向移动/账户发现** 等 **攻后阶段**；**技术数量** 与 **平台（Claude Code/API/聊天）** 不再可靠区分风险，**高 scaffolding 的自主链** 才是 durable 信号。
- **LangGraph ToolNode × MCP 契约正在快速闭合**：**2026-06-02–03** 的 [#7985](https://github.com/langchain-ai/langgraph/issues/7985) → [#7994](https://github.com/langchain-ai/langgraph/pull/7994) / [#8000](https://github.com/langchain-ai/langgraph/pull/8000) 修复 **content block list** 归一化；同期 **1.2.4**（[#7991](https://github.com/langchain-ai/langgraph/releases/tag/1.2.4)）修补 **`_on_started` 向后兼容**——与 `0602.md` 的 **OpenAPI `tools`/`lifecycle` 422** 并读，本地 agent UI 栈应做 **版本+schema 双检查**。
- **企业 agent 编码工具进入「人均预算」时代**：Simon Willison（**2026-06-03**）转述 Bloomberg：Uber 对 **Cursor / Claude Code** 等 **agentic 编码工具** 设 **每工具每月 $1,500** 上限（工具间预算独立）——约等于美区工程师 **median 包 ~11%** 的年度 token 天花板，是 **PMF 后的治理模板**，而非单纯「砍 AI」。

## 1) Agent工程化（high-signal）

- [What we learned mapping a year's worth of AI-enabled cyber threats（Anthropic Policy，**2026-06-03**）](https://www.anthropic.com/news/AI-enabled-cyber-threats-mitre-attack)
  - 摘要: 基于 **2025-03 至 2026-03** 间 **832** 个可评估封禁账号（亦贡献 Verizon **2026 DBIR**）：**67.3%** 用 AI 写恶意软件；**6.5%** 辅助横向移动；**中高风险占比** 从首期 **33%** 升至后期 **56%**；AI 辅助 **账户发现 +8.9%**、**钓鱼 -8.6%**；**MITRE ATT&CK 缺少 agentic 编排**（如 **2025-11** 国家级行动：模型 **30 技术/13 战术** 仍被低估为中等风险）。
  - 为什么重要(对我): **生产 agent** 的安全评测不能只数「调用了多少工具」；需把 **自主链长度、攻后阶段能力、human-in-the-loop 断点** 写入威胁模型与 eval。
  - 明日动作(<=20min): 为你方 **1 个** agent 写 **≤3 条**「攻后等价能力」（如批量读凭证/横向扫描）及对应 **阻断点**（各 ≤12 词）。

- [langgraph==1.2.4 正式发布（GitHub Release，**Published 2026-06-02**）](https://github.com/langchain-ai/langgraph/releases/tag/1.2.4)
  - 摘要: **1.2.4** 含 **factory-graph 集成测试**（#7978，覆盖 server factory 路径）与 **`_on_started` 向后兼容**（#7987：不破坏 **predating cause** 的 override）。相对 `0602.md` 的 **1.2.3**，本版是 **补丁级** 但影响 **自定义 lifecycle hook** 的生产图。
  - 为什么重要(对我): 若你在 **Pregel/RemoteGraph** 上覆写 **`_on_started`**，升级 **1.2.4** 前需确认 **cause 参数** 语义未变。
  - 明日动作(<=20min): `pip install -U 'langgraph==1.2.4'` 后跑 **1 条** 带自定义 `_on_started` 的 smoke test；记录 pass/fail。

- [fix: accept raw tool content blocks（langgraph PR #7994，**Opened 2026-06-02**；Fixes #7985）](https://github.com/langchain-ai/langgraph/pull/7994)
  - 摘要: `ToolNode._normalize_tool_response` 现接受 **LangChain content block list**，包装为带外层 **tool_call_id/name** 的 **ToolMessage**；补 **raw-content-block** 回归测试。与 `0602.md` §1 的 **MCP 返回 list 抛 TypeError** 为 **同一根因的修复 PR**（发布前需盯 merge 状态）。
  - 为什么重要(对我): **MCP + ToolNode** 是默认工具路径；未合并前生产应 **pin 版本** 或 **middleware 预包装** content blocks。
  - 明日动作(<=20min): 对你 **1 个** MCP 工具打印返回类型（string / list[blocks] / ToolMessage）；若 list[blocks]，标 **是否已装含 #7994 的分支**（是/否/未知）。

- [fix(prebuilt): handle content block tool responses（langgraph PR #8000，**Opened 2026-06-03**；Fixes #7985）](https://github.com/langchain-ai/langgraph/pull/8000)
  - 摘要: **prebuilt** 路径显式将 **raw content block list** 归一化为 **ToolMessage**；保留 **Command/ToolMessage list** 校验与 **invalid/empty list** 错误路径；测试覆盖 **raw_content_block_list_return** 等关键字用例。
  - 为什么重要(对我): **deepagents / prebuilt ToolNode** 与 **libs/prebuilt** 发布节奏可能 **晚于** 核心 `langgraph`——需分别跟踪。
  - 明日动作(<=20min): 确认你用的是 **`langgraph.prebuilt.ToolNode`** 还是自建节点；记下 **import 路径**（≤15 词）。

- [ToolNode accepts ToolMessage results bound to sibling tool_call_id values（Issue #7989，**Opened 2026-06-02**）](https://github.com/langchain-ai/langgraph/issues/7989)
  - 摘要: 工具可返回 **绑定到 sibling `tool_call_id`** 的顶层 **ToolMessage**；**list-return** 只计匹配 terminator，**单 ToolMessage** 路径 **原样放行**——可导致 **并行工具调用** 下 **消息图污染**。[#7997](https://github.com/langchain-ai/langgraph/pull/7997)（**2026-06-03**）曾提案校验但被 **missing-issue-link** 自动关闭，issue **仍 Open**。
  - 为什么重要(对我): **多工具 agent** 的 **call_id 绑定** 是 **状态机不变量**；比 content-block 更隐蔽。
  - 明日动作(<=20min): 若你并行 ToolNode，写 **≤25 词**：工具返回 **ToolMessage** 时是否 **强制校验 call_id==当前调用**（是/否/未查）。

- [[Bugfix] Fix unstreamed tool call args dropped in Responses API streaming（vLLM PR #44348，**Merged 2026-06-03**）](https://github.com/vllm-project/vllm/pull/44348)
  - 摘要: **tool parser** 流式增量解析时，末帧 **`parse_delta(finished=True)`** 需 **`_append_unstreamed_tool_args()`** 刷出 **未流式化的参数尾部**；Chat Completions 已有，本 PR **补齐 Responses API 路径**，避免客户端收到 **截断/空 tool arguments**。
  - 为什么重要(对我): **OpenAI Responses + vLLM** 托管 agent 时，**tool call JSON** 完整性直接影响 **下游执行**。
  - 明日动作(<=20min): 若你用 vLLM Responses 流式 + tools，对 **1 个** 大参数 tool call 比对 **末事件 args** 与 **非流式完整 args**（一致/不一致）。

- [Codex for every role, tool, and workflow（OpenAI Blog，**2026-06-02**）](https://openai.com/index/codex-for-every-role-tool-workflow/)
  - 摘要: Codex 推出 **6 个角色插件**（聚合约 **62** 个业务应用、**110** 项 skills）、**Annotations**（文档/表格/幻灯片就地精修）、**Sites** 预览（Business/Enterprise 用自然语言生成 **可分享托管站点/应用**）；非开发者占周活 **~20%** 且增速约为工程师 **3×**（文内数据）。
  - 为什么重要(对我): **agent 产品形态** 从 IDE 扩展到 **知识工作 orchestration**；对你 **ToB agent / 插件目录** 设计有参照。
  - 明日动作(<=20min): 用中文写 **≤30 词**：你若做 agent 产品，**1 个** 可复制的「角色插件」边界（含什么工具/不含什么）。

## 2) 原理深化（Attention/Transformer/Agent）

- [AI-enabled 攻击与 MITRE ATT&CK 缺口（Anthropic Policy，**2026-06-03**）](https://www.anthropic.com/news/AI-enabled-cyber-threats-mitre-attack)
  - 摘要: **Agent 编排**（顺序执行命令、利用漏洞、偷凭证、战术决策、极少人工）**无 ATT&CK ID**；传统用 **技术计数** 评估风险会 **系统性低估** 自主 agent 威胁。
  - 为什么重要(对我): **三回合追问**：(1) 你的 agent **eval 指标** 是否仍等价于「调用了几种 ATT&CK 技术」？(2) **攻后能力** 是否单独打分？(3) **scaffolding**（记忆/规划/工具环）是否进入 **红队剧本**？
  - 明日动作(<=20min): 列 **3 个** 你方 agent eval 指标，各标注 **映射 ATT&CK / 无法映射 / 需新 taxonomy**（各 ≤8 词）。

- [Responses API 流式 tool args 的「末帧 flush」（vLLM PR #44348，**Merged 2026-06-03**）](https://github.com/vllm-project/vllm/pull/44348)
  - 摘要: 增量解析下 **已解析参数 ⊃ 已流式参数** 的差集必须在 **finished=True** 时追加——属于 **流式解码器与完整语义对象** 的不变量，与 **thinking_delta / message delta** 分流同类。
  - 为什么重要(对我): **多通道流** 每一通道都需定义 **终止时的完整性契约**。
  - 明日动作(<=20min): 画 **≤3 节点** 图：stream delta → buffer → final flush（每节点 ≤6 词）。

- [ToolNode `tool_call_id` 绑定与并行工具图（Issue #7989，**2026-06-02**）](https://github.com/langchain-ai/langgraph/issues/7989)
  - 摘要: **消息图** 中 **ToolMessage** 必须 **唯一绑定** 触发它的 **tool_call**；允许 sibling id 会破坏 **并行 reduce** 的 **因果序**。
  - 为什么重要(对我): 与 **Transformer 侧 multi-head 输出路由** 类似：**输出端口必须与请求 id 对齐**。
  - 明日动作(<=20min): 为你 **1 个** 并行工具图写 **≤20 词**「错误 call_id 会导致什么状态损坏」（自定）。

## 3) 热门项目/可试个人项目

- [datasette-agent-micropython 0.1a0（Simon Willison Blog，**2026-06-02**）](https://simonwillison.net/2026/Jun/2/datasette-agent-micropython/)
  - 摘要: **Datasette Agent** 新 alpha：用 **定制 WASM MicroPython + wasmtime** 作为 **安全执行 Python 的工具**；作者称 **GPT-5.5** 尚未突破沙箱；与同日 [micropython-wasm 0.1a1](https://simonwillison.net/2026/Jun/2/micropython-wasm/) 配套。
  - 为什么重要(对我): **数据 agent** 的 **代码执行层** 可用 **轻量 WASM** 而非全容器——适合 **内网 demo / 个人知识库**。
  - 明日动作(<=20min): 读 release 说明，用中文写 **≤20 词**：你若试，第一个要跑的 **Datasette SQL/插件 API** 是什么。
  - 估计复现成本: **中**（需接 Datasette Agent + wasmtime；无 GPU）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Uber Caps Usage of AI Tools Like Claude Code to Manage Costs（Simon Willison Blog → Bloomberg，**2026-06-03**）](https://simonwillison.net/2026/Jun/3/uber-caps-usage/)
  - 机会信号: **$1,500/工具/月** 仍允许重度用户（Simon 估算个人 **~$1k/提供商/月** 用量在企业 cap 内尚有 **~$500 余量**）——说明 **agentic 编码** 已被视为 **与薪酬包同量级的经常性投入**；对 **AI 工具出海/企业销售** 是 **预算科目已成立** 的信号。
  - 风险信号: **按工具独立 cap** 可能推高 **多工具并行** 总成本；若无法证明 **feature 产出线**（Uber COO 亦承认难量化），**CFO 复审** 会反复发生——**小型 AI 公司** 需准备 **ROI / 治理** 叙事。
  - 我可执行的一步(<=30min): 为你目标客户写 **≤40 词**「**每工程师每月 agent 工具预算**」建议区间（自定币种）及 **1 个** 可观测 ROI 指标（如 merged PR /  incident MTTR）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：LeetCode Explore [`Binary Tree`](https://leetcode.com/explore/learn/card/data-structure-tree/) 中 **Maximum Depth of Binary Tree**：把「**递归/迭代向下探深度**」映射为 agent **「从当前状态向下展开工具链的最大步数/成本」**。明日动作：用中文写 **≤30 词**：为你 **1 个** LangGraph 图标 **max_depth 等价物**（步数/ token / 工具调用次数三选一）及 **剪枝条件**（≤12 词）。

- **IELTS（≤1 条）**：浏览 [IELTS 官方 — Listening test format](https://ielts.org/take-a-test/test-format/listening) 中 **四段式结构（日常→教育→学术对话→学术讲座）** 与 **题量递增**。明日动作：用中文写 **≤35 词**：把 **「先易后难四段听力」** 与你 **英文技术 onboarding 文档** 的 **1 个** 章节顺序对齐（自定）。

## 6) 噪音过滤记录

- **`https://github.com/langchain-ai/langgraph/releases/tag/1.2.3`**：与 `0602.md` §1 **语义去重**；本日仅保留 **1.2.4** 新信号。
- **`https://github.com/langchain-ai/langgraph/issues/7986`**、**`#7985` 重复叙事**：`0602.md` §1 已述；本日以 **#7994/#8000 修复进展** 与 **#7989 新 issue** 替代。
- **`https://github.com/vllm-project/vllm/pull/44206`**：与 `0602.md` §1 **URL 去重**（已 merged）。
- **`https://www.anthropic.com/news/expanding-project-glasswing`**、**`https://openai.com/index/openai-frontier-models-and-codex-are-now-available-on-aws/`**：与 `0602.md` §1/§4 **去重**。
- **`https://simonwillison.net/2026/Jun/2/pasted-file-editor/`**、**`microsofts-new-models`**：与 `0602.md` §3 / 未收录但 **6/2 已覆盖窗口** → 本日 **未重复**。
- **`https://github.com/langchain-ai/langgraph/pull/7997`**：**2026-06-03** 自动关闭（**missing-issue-link**），**未合并** → 仅保留 **#7989** 问题描述。
- **`https://github.com/run-llama/llama_index/pull/21859`**：Open **extra_body** 便利修复，评分边缘 → **未入主段**。
- **X_WHITELIST / YOUTUBE_WHITELIST / Bilibili 当日帖文**：无法稳定抓取原帖 URL/正文 → 不收录。
- **`https://github.com/openai/evals/commits/main`**：最近实质 commit **2026-04-14 量级** → 丢弃。
- **`https://github.com/microsoft/autogen`**：`merged:>2026-06-02` **0 条**高信号 → 丢弃。

## 7) 本周累计计数

- Agent: **221**（`0602.md` §7: 214 + 本日 §1: 7）
- Theory: **86**（`0602.md` §7: 83 + 本日 §2: 3）
- Career/Business: **52**（`0602.md` §7: 51 + 本日 §4: 1）
- LC: **36**（`0602.md` §7: 35 + 本日 §5: 1）
- IELTS: **36**（`0602.md` §7: 35 + 本日 §5: 1）
