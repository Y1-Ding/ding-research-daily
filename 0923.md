# Daily Research - 2026-09-23 (21:30)

## 0) 今日3条结论

- **LangGraph store 的 per-item `index=["$"]` 曾被 `tokenize_path` 当成字面键 `$`，整文档不进向量索引**（#**9060** / #**9061**, created **2026-09-23**）→ **skill 记忆 + RAG eval 须覆盖「根路径 sentinel」与 put 级 override**，否则 semantic search gold 静默全错。
- **OpenAI Responses 的 shell + Programmatic Tool Calling 现可经 LangChain `bind_tools` 完整转换**（#**40783**）+ **Opus 5.5 / GPT-6 on Bedrock 的 forced tool / temperature 约束已在 LangChain/LlamaIndex 同日 fail-fast**（#**40778**, #**23208**, #**23212**）→ **L2 eval 要分「模型族 × 路由层」断言 tool_choice 与采样参数**。
- **packed KV 下 SWA 块若仍用 16-token 内核块会极度浪费容量**（vLLM #**58345**）+ **sparse-MLA decode 元数据在 ROCm 仍每步 scratch+copy**（#**58352**）→ **长上下文 agent 的 prefill/verify 与 SWA 层 eval 须按 serving 配置分桶**，不可沿用 uniform-page 假设。

## 1) Agent工程化（high-signal）

- [fix(openai): support GPT-6 shell and programmatic tool calling（LangChain PR #40783, created **2026-09-23**）](https://github.com/langchain-ai/langchain/pull/40783)
  - 摘要: `convert_to_openai_tool()` 现识别官方 shell 与 Programmatic Tool Calling；`bind_tools` 经 provider-agnostic 转换后保留 Responses 的 program/shell 输出项、`allowed_callers` 与多轮 continuation（含 streaming）。
  - 为什么重要(对我): **skill 落地**——云 shell + programmatic call 是 **Codex/Agents API 形态** 在 LangChain 栈的契约层。
  - 明日动作(<=20min): 写 **是/否**：是否 bind OpenAI programmatic/shell tools（是/否/未知）。

- [fix(anthropic): validate Claude Opus 5.5 request constraints（LangChain PR #40778, created **2026-09-23**）](https://github.com/langchain-ai/langchain/pull/40778)
  - 摘要: Opus 5.5 **thinking 不可关**；forced `tool_choice` 在最终 payload 降级为 `auto` 并 warn；覆盖 `model_kwargs` / adaptive thinking。
  - 为什么重要(对我): **评测 fail-fast**——避免 HTTP 400 后才暴露 **eval harness 配置与模型族不兼容**。
  - 明日动作(<=20min): 写 **是/否**：Opus 5.5 eval 是否仍 force tool（是/否/未知）。

- [Keep empty group_ids meaningful in InMemoryRecordManager（LangChain PR #40784, created **2026-09-23**）](https://github.com/langchain-ai/langchain/pull/40784)
  - 摘要: `group_ids=[]` 不再被 truthiness 当成「未过滤」；空列表 **过滤为空** 且 update 时触发长度校验（fixes #40745）。
  - 为什么重要(对我): **RAG ingest eval**——「显式空组」是 **隔离/清空** 的常见 API 语义。
  - 明日动作(<=20min): 写 **1 条** gold：`group_ids=[]` 时期望 0 条还是全量（≤12 词）。

- [fix(checkpoint): honor root path in per-item InMemoryStore index（LangGraph PR #9060, created **2026-09-23**）](https://github.com/langchain-ai/langgraph/pull/9060)
  - 摘要: per-item `index=["$"]` 与 store 级 `fields: ["$"]` 一致，**不再 tokenize `$` 为字面字段**；修复整对象未嵌入、语义检索失真（fixes #9059）。
  - 为什么重要(对我): **skill 记忆 store**——put 级 index override 是 **opt-in 向量** 的产品路径。
  - 明日动作(<=20min): 写 **是/否**：是否用 per-item `index=["$"]`（是/否/未知）。

- [fix(store): respect root-path sentinel "$" in per-item index overrides（LangGraph PR #9061, created **2026-09-23**）](https://github.com/langchain-ai/langgraph/pull/9061)
  - 摘要: `_extract_texts` 对 `$` 应用与 `__init__` 相同的 sentinel 规则；sync/async 路径一致（同族 #9059）。
  - 为什么重要(对我): **RAG + agent checkpoint**——与 #9060 同属 **静默索引失败** 类 bug，宜合并进回归套件。
  - 明日动作(<=20min): 写 **是/否**：LangGraph store 是否跑 semantic search gold（是/否/未知）。

- [fix(core): fail fast when one request exceeds a rate limiter's token capacity（LlamaIndex PR #23203, created **2026-09-23**）](https://github.com/run-llama/llama_index/pull/23203)
  - 摘要: Token bucket / sliding window 在 **单次请求 > TPM 容量** 时不再死锁或 silently 超限，改为 **`ValueError`**（fixes #23202）。
  - 为什么重要(对我): **token + agent 批跑**——长跑 eval 须 **可观测失败** 而非 hang。
  - 明日动作(<=20min): 写 **是/否**：是否用 LlamaIndex rate limiter（是/否/未知）。

- [fix(bedrock-converse): add Claude Opus 5.5 and stop forcing toolChoice（LlamaIndex PR #23208, created **2026-09-23**）](https://github.com/run-llama/llama_index/pull/23208)
  - 摘要: Bedrock Opus 5.5 入模型表；forced tool 改 `{"auto": {}}`；`tool_required` / structured_predict  live 验证通过（fixes #23207）。
  - 为什么重要(对我): **skill 落地 × 多云**——同一 gold 在 Bedrock 与直连 Anthropic **路由语义不同**。
  - 明日动作(<=20min): 写 **是/否**：是否 Bedrock + forced tool eval（是/否/未知）。

- [fix(bedrock-converse): add OpenAI GPT-6 Sol/Luna and stop sending temperature（LlamaIndex PR #23212, created **2026-09-23**）](https://github.com/run-llama/llama_index/pull/23212)
  - 摘要: `openai.gpt-6-sol/luna` 入 Bedrock 表；二者 **任意 temperature 均被拒**，加入 `BEDROCK_NO_TEMP_MODELS`（fixes #23211）。
  - 为什么重要(对我): **token/成本 eval**——新模型族默认采样参数会 **直接打爆请求**。
  - 明日动作(<=20min): 写 **是/否**：GPT-6 Sol/Luna 是否在 Bedrock 路径测过（是/否/未知）。

- [fix(ingestion): keep transformation-cache keys unambiguous across node boundaries（LlamaIndex PR #23209, created **2026-09-23**）](https://github.com/run-llama/llama_index/pull/23209)
  - 摘要: `get_transformation_hash` 对节点内容 **长度前缀**，修复 `ab|c` 与 `a|bc` 同 hash 导致 **错用缓存节点列表**（refs #23002）。
  - 为什么重要(对我): **ingest pipeline eval**——边界歧义会 **交叉污染 ref_doc_id**。
  - 明日动作(<=20min): 写 **是/否**：是否启用 transformation cache（是/否/未知）。

- [fix: preserve retrieved scores during prev-next expansion（LlamaIndex PR #23201, created **2026-09-23**）](https://github.com/run-llama/llama_index/pull/23201)
  - 摘要: `PrevNextNodePostprocessor` 在 neighbor 扩展后 **恢复原始检索分**；重叠扩展不再丢分（fixes #23199）。
  - 为什么重要(对我): **RAG eval**——postprocessor 链是 **L2 检索 gold** 常见组成。
  - 明日动作(<=20min): 写 **是/否**：是否 prev/next 扩展（是/否/未知）。

- [[Bugfix][Tool Parser] Fix ValueError on second action block in InternLM2 streaming（vLLM PR #58346, created **2026-09-23**）](https://github.com/vllm-project/vllm/pull/58346)
  - 摘要: 第二段 `<|action_start|><|plugin|>` 时 `split` 返回 3 段导致 **unpack 崩溃**（异常在 try 外）；单 stream 多 tool 即触发。
  - 为什么重要(对我): **L2 streaming tool eval**——并行/连续 tool 块须 **断言不 abort stream**。
  - 明日动作(<=20min): 写 **是/否**：是否 InternLM2 + streaming tools（是/否/未知）。

- [[Bugfix][Frontend] Preserve MiniMax M2's unclosed final tool parameter（vLLM PR #58349, created **2026-09-23**）](https://github.com/vllm-project/vllm/pull/58349)
  - 摘要: 模型关闭 `</invoke>` 但未关末个 `</parameter>` 时 **仍保留 trailing argument**（#57827 MiniMax M2 部分；含 stream/non-stream 回归）。
  - 为什么重要(对我): **tool 参数 gold**——不完整 XML 是 **真实模型输出** 分布。
  - 明日动作(<=20min): 写 **1 条**：eval 是否含「缺闭合 parameter」样例（是/否/未知）。

- [[Rust Frontend] Token-aware marker parsing for Kimi K3（vLLM PR #58358, created **2026-09-23**）](https://github.com/vllm-project/vllm/pull/58358)
  - 摘要: K3 通道标记若由 **普通 BPE 拼出** 不再误关 think 通道；仅 **预期 special token ID** 计为结构（stack #58357 UTF-8 anchor）。
  - 为什么重要(对我): **skill 落地 + 多通道输出**——reasoning 引号内 XTML 曾 **静默丢 response**。
  - 明日动作(<=20min): 写 **是/否**：是否 Kimi K3 + unified parser（是/否/未知）。

- [[Bugfix][Frontend][Rust Frontend] Update DeepSeek V4.1 Flash reasoning effort mappings（vLLM PR #58316, created **2026-09-23**）](https://github.com/vllm-project/vllm/pull/58316)
  - 摘要: `low/high/max` 映射对齐 HF 官方 commit（如 `low: 50`, 去掉旧 `xhigh` 档）；与 TR 一致。
  - 为什么重要(对我): **评测可复现**——effort 档位变化会 **整体漂移 agent 轨迹**。
  - 明日动作(<=20min): 写 **是/否**：DS V4.1 Flash eval 是否 pinned effort（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix] Scale sliding-window block size in packed KV cache grouping（vLLM PR #58345, created **2026-09-23**）](https://github.com/vllm-project/vllm/pull/58345)
  - 摘要: packed KV 路径在 unify 前将 **SlidingWindowSpec 块扩到最大 attention 块**；修复 SWA 组 block=16 占满 packed block 导致 **KV 容量暴跌**（Qwen3.6 + DFlash 实测 ~3.5× token 容量差）。
  - 为什么重要(对我): **Talk ≥3 turns + SWA**——spec decode 下 SWA draft 组与 full attn **块对齐** 决定 agent 能否跑长上下文。
  - 明日动作(<=20min): 写 **≤15 词**：是否 packed KV + SWA spec（是/否/未知）。

- [[Bugfix][Attention] Fix FA4 FP8 descale strides for singleton KV heads（vLLM PR #58304, created **2026-09-23**）](https://github.com/vllm-project/vllm/pull/58304)
  - 摘要: TP=2 每 rank 单 KV head 时 `scalar.expand` 的 **stride 0** 触发 FA4 CuTe descale 崩溃；对不兼容 2-D descale **`clone(contiguous)`**（Nemotron-H on B200）。
  - 为什么重要(对我): **Talk ≥3 turns + FP8 KV**——小 batch agent prefill 是 **常见部署形态**。
  - 明日动作(<=20min): 写 **是/否**：是否 FA4 + FP8 KV + TP（是/否/未知）。

- [[Perf][SM120][MLA] Use compact FP8 KV rows for NoPE models（vLLM PR #58305, created **2026-09-23**）](https://github.com/vllm-project/vllm/pull/58305)
  - 摘要: NoPE 模型在 FlashInfer SM120 用 **528-byte** `fp8_ds_mla` 行（512 FP8 latent + scales），无 RoPE payload；按 `kv_lora_rank=512` & `qk_rope_head_dim=0` 选择。
  - 为什么重要(对我): **Talk ≥3 turns + MLA KV 布局**——行宽直接影响 **长对话 KV 上限与带宽**。
  - 明日动作(<=20min): 写 **≤15 词**：是否 NoPE MLA 部署（是/否/未知）。

- [[ROCm][DSV4][Perf] Pack sparse-MLA ragged decode metadata into graph buffers（vLLM PR #58352, created **2026-09-23**）](https://github.com/vllm-project/vllm/pull/58352)
  - 摘要: ROCm 上 sparse-MLA decode 每步 **scratch pack + copy 到 graph buffer**；V4.1 SWA 路径可达 **7 calls/step**；draft 改为直接写入 capture 地址（stack #52628）。
  - 为什么重要(对我): **Talk ≥3 turns + sparse Attention**——decode 元数据是 **多轮 latency** 隐藏项。
  - 明日动作(<=20min): 写 **是/否**：是否 ROCm + DSv4 sparse MLA（是/否/未知）。

- [[Core][ROCm][Perf] Kimi-K3 enable prefill checkpoints on ROCm（vLLM PR #58344, created **2026-09-23**）](https://github.com/vllm-project/vllm/pull/58344)
  - 摘要: `align` mamba 模式下 KDA prefill checkpoint 使 **非块对齐 context 免二次 forward**；并修 scheduler 与 KV manager **块边界不一致** 导致的浪费/丢 cache。
  - 为什么重要(对我): **Talk ≥3 turns + 长 prefill**——Mamba/KDA 与 Transformer 混部 agent 的 **TTFT** 边界。
  - 明日动作(<=20min): 写 **≤15 词**：是否 Kimi-K3 ROCm prefill（是/否/未知）。

- [[SpecDecode] Support quantized DFlash2 selectors and owned DFlash/DSpark heads（vLLM PR #58343, created **2026-09-23**）](https://github.com/vllm-project/vllm/pull/58343)
  - 摘要: DFlash2 selector 与 owned Qwen3 draft head 可接 **draft 量化配置**；缺 `lm_head.*` 的 owned head checkpoint **显式拒绝**；量化 scale 不等价时不再静默共享。
  - 为什么重要(对我): **Talk ≥3 turns + spec verify**——draft/target **权重等价性** 是 acceptance rate eval 前提。
  - 明日动作(<=20min): 写 **是/否**：是否 DFlash2 量化 draft（是/否/未知）。

- [[ROCm][Perf] Drop two dead per-step ops from the AITER sparse MLA metadata build（vLLM PR #58354, created **2026-09-23**）](https://github.com/vllm-project/vllm/pull/58354)
  - 摘要: sparse MLA metadata 构建去掉 **每步无效 op**，降低 ROCm decode 固定开销（与 #58352 同族优化）。
  - 为什么重要(对我): **Talk ≥3 turns + MLA**——agent serving 的 **逐步算子数** 影响 p99 多轮延迟。
  - 明日动作(<=20min): 写 **≤15 词**：是否 profiling sparse MLA metadata（是/否/未知）。

## 3) 热门项目/可试个人项目

- [feat(examples): add Adam Network agent integration example（AutoGen PR #8274, created **2026-09-23**）](https://github.com/microsoft/autogen/pull/8274)
  - 摘要: 新增 **Adam Network** 与 AutoGen agent 集成示例（示例级 PR，便于对照多 agent 网络拓扑接线）。
  - 为什么重要(对我): **低成本的 multi-agent 接线模板**——可对照自有 service_agent 规则引擎拆角色。
  - 明日动作(<=20min): clone 示例目录，**标 3 个** 与你当前 agent 图差异最大的节点（每点 ≤8 词）。
  - 估计复现成本: **中**（读 example + 跑通 demo，~1–2h，视依赖而定）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [SF October 14th: A Birds of a Feather Session on Agentic Engineering（Simon Willison Blog, **2026-09-23**）](https://simonwillison.net/2026/Sep/23/)
  - 机会信号: SF **agentic show-and-tell**（非 pitch，偏早期实验）→ 出海社区 **弱关系 + 未公开实践** 的交换面；与 Jesse Vincent 共办。
  - 风险信号: 线下活动 **不可远程复用**；需自备可展示但未产品化的 agent 实验。
  - 我可执行的一步(<=30min): 写 **5 条 bullet**：你若参加会展示的 **unfinished agent 实验**（每条 ≤12 词，可脱敏）。

- [Announcing GPT-6 Sol and Luna in the API, Codex and ChatGPT（OpenAI Developer Community, **2026-09-22** 帖；**2026-09-23** 仍有成本/额度讨论）](https://community.openai.com/t/announcing-gpt-6-sol-and-gpt-6-luna-in-the-api-codex-and-chatgpt/1399925)
  - 机会信号: 官方称 **API 价较 GPT-5.6 促销价降 50%** + 社区 Codexometer 显示 **Luna high 在 agent 式任务上 PASS 且更便宜** → 个人/小团队 **迭代 eval 与 Codex 循环** 成本下探。
  - 风险信号: **banked reset** 非自动到账；AutomationBench 等榜单上 **Sol max 未必线性更好**（社区帖 _j 提醒）。
  - 我可执行的一步(<=30min): 写 **3 行**：默认 eval 模型 / 是否迁 Luna / 月度 token 预算敏感度（每行 ≤10 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[1658. Minimum Operations to Reduce X to Zero](https://leetcode.com/problems/minimum-operations-to-reduce-x-to-zero/)（**2026-09-23 Daily Challenge**，Medium，LeetCode GraphQL `activeDailyCodingChallengeQuestion`）：双端队列 + 前缀和变体；与 **窗口/抵消** 类 invariant 同构。明日动作：一题或写 **≤15 词**「Medium 日题」对你周计划意味着什么。

- **IELTS（≤1 条）**：[IELTS Writing practice tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/writing)：0922 为 Reading，今日 **Writing Task 2 一篇（~25min 计时）**。明日动作：写 **2 个** 失误类型（任务回应/连贯）；**≤15 词** 最弱 Writing 维度。

## 6) 噪音过滤记录

- **LangChain #40779/#40781/#40780**：chore / OpenWiki / model profiles → 过滤。
- **LangGraph #9058**：移除 Claude 说明 → 过滤。
- **LlamaIndex #23204 docs landing / #23206 embedding 标题含糊 / #23200 与 #23201 同族**：保留 #23201，其余过滤。
- **LlamaIndex #23210 data URL decode**：多模态 niche → 未进主报告（可自行跟进）。
- **vLLM #58351/#58348/#58347 watermark/ROCm preset**：CI/仪表盘/水印 → 过滤。
- **vLLM #58356 ngram_gpu `.item()`**：perf micro → 过滤。
- **vLLM #58355 与 #58358 重复标题**：同主题保留 #58358（更完整描述）。
- **OpenAI Blog Agents API / GPT-Live（Sep 10）**：非今日新鲜度 → 过滤（职业池用社区 Sol/Luna 成本角）。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-23** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + Simon Willison + 社区帖当日 ≥8 条主信号，无需 skill 补充。
- **openai/evals**：2026-09-23 无新 PR → 不收录。
- **Lil'Log / Anthropic Blog**：未发现 **2026-09-23** 可验证新文 → 不收录。

## 7) 本周累计计数

- Agent: **1092**（`0922.md` §7: 1078 + 本日 §1: 14）
- Theory: **503**（`0922.md` §7: 496 + 本日 §2: 7）
- Career/Business: **160**（`0922.md` §7: 158 + 本日 §4: 2）
- LC: **136**（`0922.md` §7: 135 + 本日 §5: 1）
- IELTS: **136**（`0922.md` §7: 135 + 本日 §5: 1）
