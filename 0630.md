# Daily Research - 2026-06-30 (21:30)

## 0) 今日3条结论

- **LangGraph HITL 路径：`GraphInterrupt` 在 `awrap_tool_call` 包装下被 `handle_tool_errors` 吞掉（#8217，2026-06-30）**：`interrupt()` 工具应挂起图执行，实际却变成 `ToolMessage(status='error')`——skill 落地若用 middleware + HITL，eval harness 必须区分「可恢复错误」与「预期中断」。
- **SkillsBench 实证：curated Skills 平均 +16.2pp，但 16/84 任务为负 delta（arXiv 2602.12670）**：skill 评测不能只看 aggregate pass rate；2–3 模块的 focused skill 优于百科全书式文档，self-generated skill 无平均收益。
- **Attention 架构层 KV 减半：Keyless Attention 去掉 K 投影，Value-Only Cache 精确减 50% KV（arXiv 2606.21848）**：与 serving 侧 eviction（AsymCache）正交——模型侧从 bilinear form 深度分解入手，5 模型中 4 个 perplexity 不输标准 QKV。

## 1) Agent工程化（high-signal）

- [GraphInterrupt Not Re-raised in awrap_tool_call Wrapper Path（LangGraph #8217 opened **2026-06-29**，updated **2026-06-30**）](https://github.com/langchain-ai/langgraph/issues/8217)
  - 摘要: 最小复现：`ToolNode([interrupt_tool], awrap_tool_call=middleware, handle_tool_errors=...)` 调用 `interrupt()` 时，期望图挂起；实际 `GraphInterrupt` 被 `handle_tool_errors` 捕获，返回 `ToolMessage(status='error', content="Tool call failed (GraphInterrupt): ...")`，图继续执行而非 suspend。
  - 为什么重要(对我): **skill 落地 + eval**——HITL / approval-gated tool 是 governance eval 的核心模式；此 bug 使 eval 与生产中断语义分叉，L2 grading 会把「待审批」误判为 tool error。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否在 `ToolNode` 使用 `handle_tool_errors`（是/否/未知）。

- [fix(agent): always include model in model_to_tools_destinations（LangChain #38569 merged **2026-06-30**）](https://github.com/langchain-ai/langchain/pull/38569)
  - 摘要: `create_agent` 条件边在部分配置下未将 `loop_entry_node` 纳入 destination set，但 `_make_model_to_tools_edge` 在 `wrap_model_call` middleware 注入 synthetic `ToolMessage`（缓存/HITL/replay 文档模式）时仍会返回该 destination → LangGraph `KeyError('model')`。Fix：无条件包含 `loop_entry_node`。
  - 为什么重要(对我): **skill 落地**——middleware 注入 synthetic tool message 是 skill replay / cache 的常见模式；此 fix 解除一类 silent graph crash。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 middleware 注入 synthetic ToolMessage（用/不用/未知）。

- [[Bug]: AgentWorkflow stores agents by reference; shared BaseTool leaks per-tool mutations（LlamaIndex #22146 updated **2026-06-30**）](https://github.com/run-llama/llama_index/issues/22146)
  - 摘要: `AgentWorkflow.__init__` 以引用存储 agent config，无 deep copy；`BaseWorkflowAgent.validate_tools` 复用同一 `BaseTool` 实例。多 agent 共享 tool 时，一个 agent 对 tool 的 mutation（如 runtime state）会泄漏到其他 agent。
  - 为什么重要(对我): **skill 落地 + eval**——multi-agent workflow 是 skill 编排常见形态；共享 tool 实例导致 eval 非确定性（同 seed 不同 agent 顺序结果分叉）。
  - 明日动作(<=20min): 写 **≤15 词**：你 multi-agent 是否共享 tool 实例（共享/隔离/未知）。

- [feat(property_graph): add raise_on_error to LLM path extractors（LlamaIndex #22195 opened **2026-06-30**）](https://github.com/run-llama/llama_index/pull/22195)
  - 摘要: `DynamicLLMPathExtractor` / `SchemaLLMPathExtractor` / `SimpleLLMPathExtractor` 原先 broadly catch 异常并返回 `[]`，下游无法感知 API timeout 或 validation 失败。新增 `raise_on_error: bool = False`（默认保持 batch 容错语义），`True` 时 bubble up。
  - 为什么重要(对我): **eval harness**——silent failure 是 L2 eval 的假阴性来源；opt-in `raise_on_error` 与 F/C 规则「失败必须可见」对齐。
  - 明日动作(<=20min): 写 **是/否**：你 pipeline 是否容忍 extractor 静默返回空（容忍/不容忍/未知）。

- [fix(checkpoint-postgres): add serde passthrough to from_conn_string（LangGraph #8233 opened **2026-06-30**）](https://github.com/langchain-ai/langgraph/pull/8233)
  - 摘要: `PostgresSaver.from_conn_string` 新增可选 `serde` 参数并转发给构造函数，与 `AsyncPostgresSaver.from_conn_string` 行为对齐；同步修复 `ShallowPostgresSaver.from_conn_string`。Fixes #8116。
  - 为什么重要(对我): **skill 落地**——自定义 serde（如加密/压缩 checkpoint）是长会话 agent 持久化的杠杆；此前 sync path 无法注入 serde，prod/eval 环境分叉。
  - 明日动作(<=20min): 写 **是/否**：你是否用自定义 checkpoint serde（是/否/未知）。

- [Add healthcare_ai_governance eval（openai/evals #1682 opened **2026-06-27**，**2026-06-30** 仍 open）](https://github.com/openai/evals/pull/1682)
  - 摘要: 测试 governed healthcare agent 在 permission / PHI / clinical-safety 场景下输出 `ALLOW` / `DENY` / `ESCALATE` 三元决策，exact match grading。覆盖越权、PHI 暴露、高风险临床操作等 failure mode。
  - 为什么重要(对我): **eval + skill 落地**——与 AutoGen `GuardrailProvider` 三态、你 F/C 规则引擎直接同构；可作为 skill governance eval 的参考 gold 集结构。
  - 明日动作(<=20min): 读 PR 中 3 条 sample prompt；写 **≤15 词**：你 governance eval 是否覆盖 ESCALATE（覆盖/不覆盖/未知）。

- [SkillsBench: Benchmarking How Well Agent Skills Work Across Diverse Tasks（arXiv 2602.12670）](https://arxiv.org/abs/2602.12670)
  - 摘要: 86 tasks × 11 domains，三种条件（no Skills / curated / self-generated），7 agent-model × 7,308 trajectories。Curated Skills 平均 +16.2pp（SE +4.5pp ~ Healthcare +51.9pp），16 tasks 负 delta；self-generated 无平均收益；2–3 模块 focused skill 优于长文档；小模型+Skills 可匹配大模型无 Skills。
  - 为什么重要(对我): **skill 评测方法论**——直接回答「skill 落地后怎么验」；必须 per-task 报告 delta 而非只看 aggregate，负 delta 任务应 rollback。
  - 明日动作(<=20min): 写 **≤15 词**：你是否有 per-skill A/B eval（有/无/仅 aggregate）。

- [Expose Deep Agent Harness as Composable LangGraph Primitives（LangChain #36310 updated **2026-06-30**）](https://github.com/langchain-ai/langchain/issues/36310)
  - 摘要: Feature request：将 Deep Agent Harness（planning / subagent dispatch / filesystem / sandbox）拆为可组合 LangGraph primitives（`PlanningNode`、`SubAgentDispatchNode`、`FileSystemNode` 等），而非 monolithic `create_deep_agent`。社区持续 +1（**2026-06-30** 仍有 activity）。
  - 为什么重要(对我): **skill 落地架构**——composable primitives 是 skill 可复用性的前提；monolithic harness 难拆成独立 skill 模块做 per-component eval。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 图是否可拆为独立可测节点（可/不可/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Keyless Attention: Value-Space Routing and Value-Only Caching for Efficient Transformers（arXiv 2606.21848，submitted **2026-06-23**）](https://arxiv.org/abs/2606.21848)
  - 摘要: 完全去掉 K 投影，用 value-space routing matrix 替代；提出 Depth-m Attention Factorization（标准 attention 为 depth-2，Keyless 实例化 m=3 的 QVV）。Value-Only Cache 精确减 50% KV memory + access；5 模型 4 架构（GPT-2 280M/557M、Pythia 410M、Qwen2 1.5B、Llama 3.2 1B）中 4 个 perplexity 不输 QKV；GPT-2 557M zero-shot 5 benchmark 中 4 个更优。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) 去掉 K 后 routing 与 retrieval 如何耦合？(2) 50% KV 削减与 MLA/GQA 的 per-head 压缩是叠加还是替代？(3) agent 长 trace 下 Value-Only Cache 对 prefill vs decode 哪侧收益更大？
  - 明日动作(<=20min): 写 **≤10 词**：你 eval 是否区分 K/V cache 占比（区分/不区分/未知）。

- [Towards Tight Bounds for Streaming Attention（arXiv 2606.07205，submitted **2026-06-05**）](https://arxiv.org/abs/2606.07205)
  - 摘要: 将 KV cache compression 形式化为 streaming attention approximation，给出 nearly tight space complexity bounds。算法侧融合 discrepancy coreset、polynomial method、space partitioning 三种 KDE 技术；下界侧用 INDEX problem + large side information 新技术。闭合了 Haris et al. COLT'25 与 Kochetkova et al. NeurIPS'25 的 precision-dependent gap。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) streaming attention 近似误差与 KV eviction 启发式的理论关系？(2) precision parameter 进入 space bound 意味着什么工程含义？(3) 对你的 agent eval，KV 压缩算法的 worst-case guarantee 是否应纳入 L3 门禁？
  - 明日动作(<=20min): 写 **≤15 词**：你 KV 优化是否有理论误差界（有/无/未知）。

## 3) 热门项目/可试个人项目

- [langchain-ai/agentevals](https://github.com/langchain-ai/agentevals)
  - 摘要: LangChain 官方 agent eval 工具包：`create_trajectory_match_evaluator`（strict/unordered/subset/superset）+ `create_trajectory_llm_as_judge`。输入支持 OpenAI dict 或 LangChain `BaseMessage` 列表；可与 SkillsBench 的 per-task delta 思路组合。
  - 为什么重要(对我): **eval harness 参考**——trajectory match 可直接挂 collect→test→eval；与今日 SkillsBench 结论互补（match 模式选 strict vs subset）。
  - 明日动作(<=20min): 对一个已有 agent trace 跑 `create_trajectory_match_evaluator(mode="subset")`（≤20min）。
  - 估计复现成本: **低**（纯 Python，无 GPU）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Building agents with the Claude Agent SDK（Anthropic Engineering Blog，**2026-06-17**）](https://www.anthropic.com/engineering/building-agents-with-the-claude-agent-sdk)
  - 机会信号: **agent harness 产品化**——Claude Code SDK 重命名为 Claude Agent SDK，明确「同一 harness 可驱动 coding 以外 agent」；best practices 来自 Anthropic 内部部署，含 subagent、MCP、permission model。
  - 风险信号: SDK 绑定 Anthropic 生态；自建 agent 若只做 thin wrapper 难差异化；permission/containment 设计成本高（参见同月 containment 文章）。
  - 我可执行的一步(<=30min): 读 SDK docs 的 permission model 一节；写 **≤20 词** 对比你 agent 权限模型与其三 pattern（sandbox/HITL/VM）差异。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 118 Populating Next Right Pointers in Each Node](https://leetcode.com/problems/populating-next-right-pointers-in-each-node/)：完美二叉树每层 `next` 连接；O(1) 空间用已建立 `next` 逐层串联（116 的完美树版）。明日动作：手写 O(1) 空间版（≤20min）；写 **≤15 词**：完美树 vs 117 非完美树对你 agent DAG 拓扑意味着什么。

- **IELTS（≤1 条）**：[IELTS Speaking — Describe a piece of technology you find useful（British Council 样题）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-practice-tests/speaking)：Part 2 用 agent eval tooling（脱敏）练 2 分钟陈述 what/why/how/feeling。明日动作：录音 2 分钟自评 fluency（≤20min）；写 **≤15 词** 弱项（fluency/词汇/结构 选 1）。

## 6) 噪音过滤记录

- **LangChain #38544 defer_loading namespace**：`0629.md` 已收录；**2026-06-30** 无 fix PR → 不重复细节。
- **LlamaIndex #22185 tool audit callbacks / #22153 token counting**：`0629.md` 已收录；**2026-06-30** 无新 merge → 不重复。
- **AutoGen #7881 GuardrailProvider / #7893 action_ref**：`0628–0629` 已追踪；**2026-06-30** 无新 update → 不重复。
- **vLLM #46968 spec decode gather**：`0629.md` 已收录 merged → 不重复。
- **AsymCache MSA（arXiv 2606.02964）**：`0629.md` 已收录 serving 侧 KV → 本日改收架构侧 Keyless Attention。
- **openai/evals #1684/#1683**：文档/label 修正，与主线弱相关 → 丢弃。
- **LlamaIndex #22147 PaddleOCR reader**：OCR 集成，与当前 focus 弱相关 → 丢弃。
- **LangChain #38549 filesystem double-dot**：路径验证 bug，与 agent eval 主线弱相关 → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-06-30** 原帖 → 不收录。
- **@gefei55 / @ianneo_ai 职业内容**：第三方聚合无法验证原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **426**（`0629.md` §7: 418 + 本日 §1: 8）
- Theory: **177**（`0629.md` §7: 175 + 本日 §2: 2）
- Career/Business: **80**（`0629.md` §7: 79 + 本日 §4: 1）
- LC: **62**（`0629.md` §7: 61 + 本日 §5: 1）
- IELTS: **62**（`0629.md` §7: 61 + 本日 §5: 1）
