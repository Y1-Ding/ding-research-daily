# Daily Research - 2026-08-09 (21:30)

## 0) 今日3条结论

- **async ToolNode 终于与 sync 对齐 `max_concurrency`**（LangGraph #8576, Aug 9）：`_afunc` 原用 `asyncio.gather` 无视 config 并发上限 → eval 压测时 async graph 的 tool 洪峰与 sync 不可比；fix 改用 `gather_with_concurrency` → **skill 落地若混用 sync/async ToolNode，eval 须分路径测并发与 rate-limit**。
- **`filter_messages(exclude_tool_calls=…)` 只删 `tool_calls` 不删 content block 是 silent poison**（LangChain #39359, Aug 9）：OpenAI `function_call` / 标准 `tool_call` block 残留 → provider 仍收到被排除的 tool call → **token 优化与 trajectory 裁剪须在 middleware 边界同时清 `tool_calls` + content blocks**。
- **KV offload 进入「跨 TP 拓扑可复用」阶段**（vLLM #48414, Aug 9）：canonical CPU layout 将 per-rank fragment scatter 到 parallelism-free 位置，MLA/GQA replicated heads 只存一份 → **长 session agent 的 prefix cache + CPU tiering 成本模型须按 canonical vs legacy layout 分开算**。

## 1) Agent工程化（high-signal）

- [Fix ToolNode to respect max_concurrency in async execution（LangGraph PR #8576, updated **2026-08-09**）](https://github.com/langchain-ai/langgraph/pull/8576)
  - 摘要: Fixes #8517。`ToolNode._afunc` 用 `asyncio.gather(*coros)` 全并发，忽略 config 的 `max_concurrency`；sync `_func` 已用 `get_executor_for_config` 限流。Fix：改用 `langchain_core.runnables.utils.gather_with_concurrency`；新增回归测试验证 `max_concurrency=2` 最多 2 并发、`=1` 串行、未设置时行为不变。
  - 为什么重要(对我): **评测 + skill 落地**——parallel tool call eval 若 async path 无限并发，会触发真实 API rate-limit 或与 sync baseline 不可比。
  - 明日动作(<=20min): 写 **是/否**：你 eval harness 是否区分 sync/async ToolNode 并发语义（是/否/未知）。

- [fix(core): remove excluded tool call blocks from message content（LangChain PR #39359, updated **2026-08-09**）](https://github.com/langchain-ai/langchain/pull/39359)
  - 摘要: Fixes #36492。`filter_messages(..., exclude_tool_calls=[...])` 只从 `AIMessage.tool_calls` 删除，但 content 中 OpenAI `function_call`（`call_id`）和标准 `tool_call`（`id`）block 仍残留 → message 声称无 tool call 却仍将调用发给 provider。Fix：按 block type→ID key 映射统一删除。
  - 为什么重要(对我): **token 优化 + skill 落地**——trajectory 裁剪/压缩是 eval 与生产共用路径；半删 = checkpoint poison + 虚增 token。
  - 明日动作(<=20min): 写 **是/否**：你 middleware 裁剪 tool call 时是否同时清 content blocks（是/否/未知）。

- [Fix tool-call delta empty string overwriting id/name in streaming（LangChain PR #39357, updated **2026-08-09**）](https://github.com/langchain-ai/langchain/pull/39357)
  - 摘要: Fixes #39335。部分 provider 流式 tool call delta 在后续 chunk 发空字符串 `""` 的 `id`/`name`，原逻辑 `is not None` 让 `""` 覆盖已累积值 → tool call 身份丢失。Fix：`_merge_block_delta_into_store` 与 `_compat_bridge._accumulate` 将空串与 `None` 同等跳过。
  - 为什么重要(对我): **评测**——streaming eval 的 tool call 匹配依赖稳定 id/name；空串覆盖 = 假 fail 或错绑 tool result。
  - 明日动作(<=20min): 写 **是/否**：你 streaming eval 是否断言 tool call id 全程非空（是/否/未知）。

- [fix(core): finalize chain-group run on cancellation (BaseException)（LangChain PR #39360, updated **2026-08-09**）](https://github.com/langchain-ai/langchain/pull/39360)
  - 摘要: Fixes #39163。`trace_as_chain_group` / `atrace_as_chain_group` 仅 `except Exception`，`asyncio.CancelledError`（派生自 `BaseException`）跳过 error/end 回调 → ASGI/WebSocket 客户端断开时 parent run 永久 pending。Fix：边界扩至 `BaseException`，记录 error 后 re-raise。
  - 为什么重要(对我): **评测 + token 优化**——长跑 eval 中断后 LangSmith pending run 污染成本统计与 ablation 对比。
  - 明日动作(<=20min): 写 **是/否**：你 eval 取消时是否检查 tracer 无 pending parent run（是/否/未知）。

- [fix(perplexity): capture num_search_queries in usage_metadata for cost tracking（LangChain PR #39351, updated **2026-08-08~09**）](https://github.com/langchain-ai/langchain/pull/39351)
  - 摘要: Fixes #31647。Perplexity 定价依赖 search query 次数；`num_search_queries` 曾在 `response_metadata` 但不在 `UsageMetadata.output_token_details` → LangSmith/Braintrust 成本低估可达一个数量级。Fix：Completions 与 Responses API 路径均写入 `output_token_details`。
  - 为什么重要(对我): **token 优化 + 评测**——RAG agent 用 Perplexity 作 search tool 时，eval $/task 须含 search query 维度。
  - 明日动作(<=20min): 写 **是/否**：你 cost tracker 是否读 `output_token_details` 非 search 字段（是/否/未知）。

- [Add OpenEval adapter for AutoGen evals（AutoGen PR #8009, updated **2026-08-09**）](https://github.com/microsoft/autogen/pull/8009)
  - 摘要: Fixes #8005。新增 `autogenstudio.eval.openeval` 模块，提供 `to_openeval()` / `from_openeval()` 与 OpenEval（`evalport-sdk` 1.0.0）互转；含 round-trip 回归测试。
  - 为什么重要(对我): **评测杠杆**——AutoGen ↔ OpenEval 标准格式打通，eval dataset 可跨框架复用，减少「换框架重标」成本。
  - 明日动作(<=20min): 读 `autogenstudio/eval/openeval.py` 签名，写 **≤15 词** 你现有 gold 集最接近哪种 OpenEval task shape。

- [fix: add SSRF protection to resolve_binary() for URL-based content blocks（LlamaIndex PR #21976, updated **2026-08-09**）](https://github.com/run-llama/llama_index/pull/21976)
  - 摘要: `resolve_binary()` 对用户提供的 Image/Audio/Video URL 无校验直接 `requests.get()` → 可打 cloud metadata（`169.254.169.254`）、loopback 等，响应 base64 回注 LLM。Fix：限 http/https、解析 hostname 拒私有/保留地址、每跳 redirect（≤5）重验。
  - 为什么重要(对我): **skill 落地**——multimodal agent 接受用户 URL 是常见 pattern；eval sandbox 须覆盖 SSRF 向量。
  - 明日动作(<=20min): 写 **是/否**：你 agent pipeline 是否接受用户提供的 media URL（是/否/未知）。

- [fix: Disable pipeline mode for connection poolers with idle timeout（LangGraph PR #8578, updated **2026-08-09**）](https://github.com/langchain-ai/langgraph/pull/8578)
  - 摘要: Closes #5675。`AsyncPostgresSaver` 的 `pipeline=True` 在 Supabase pooler 等 idle timeout 连接池上，长 LLM 调用后连接 stale → `SSL connection has been closed unexpectedly`。Fix：检测 `pooler.supabase.com` 等 pooler host 自动禁用 pipeline mode。
  - 为什么重要(对我): **skill 落地**——durable agent 用托管 Postgres checkpointer 是常见部署；pipeline 默认开启 = 生产间歇性 checkpoint 失败。
  - 明日动作(<=20min): 写 **是/否**：你是否用 Supabase/PgBouncer 等 pooler 作 checkpointer 后端（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[KV Connector] Canonical CPU layout for parallelism-agnostic KV offload（vLLM PR #48414, updated **2026-08-09**）](https://github.com/vllm-project/vllm/pull/48414)
  - 摘要: Stacked on #48408。offloaded KV 按 canonical（parallelism-free）layout 存储：每 worker scatter fragment 到共享 CPU area 的 canonical 位置；MLA latent 与 replicated GQA heads 只存一份。配置 `kv_connector_extra_config: {"canonical_layout": true}`；不可认证拓扑 startup fail 而非静默降级。E2E：Qwen2.5 tp1/tp2/tp4 + dcp2 greedy token 完全一致。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/KV**：跨 TP 切换或 CPU tier reload 时 legacy layout 重复存 head = 隐藏 DRAM 成本；agent 长 session prefix cache 须按 layout 建模。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 vLLM KV offload + TP>1（是/否/未知）。

- [[BugFix] Reserve the bonus query slot in DFlash scheduling budget（vLLM PR #51256, updated **2026-08-09**）](https://github.com/vllm-project/vllm/pull/51256)
  - 摘要: DFlash 每 request 有 K+1 query tokens（非通用 parallel drafting 的 K-1）。旧计算 `max_num_scheduled_tokens = 2048 - 7*256 = 256` 但满批需 `256*(8+1)=2304` → 超 `max_num_batched_tokens`。Fix：为 DFlash bonus slot 预留调度预算。
  - 为什么重要(对我): **Talk ≥3 turns + spec decode**：DFlash spec decode agent 低延迟路径的 scheduling 边界条件；预算不足 = silent batch 截断或 OOM。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serve DFlash spec decode（是/否/未知）。

- [[Bugfix][Parser] Emit REASONING_END for Inkling tool calls that follow no thinking block（vLLM PR #50528, updated **2026-08-09**）](https://github.com/vllm-project/vllm/pull/50528)
  - 摘要: Fixes #50512。`--tool-call-parser inkling --reasoning-parser inkling` 下，若 turn 直接开 tool block 无 reasoning，`REASONING_END` 永不触发 → `should_transition` 不 fire，tool engine 收不到输入，整段 markup 泄漏到 `delta.content`。Fix：`TOOL_START` 在 `MESSAGE_HEADER` 路径也 emit `REASONING_END`（#49876 后 `TOOL_START` 走 forwarding gate 需额外处理）。
  - 为什么重要(对我): **Talk ≥3 turns + Agent 推理边界**：reasoning→tool 状态机是 agent serving 栈核心；无 thinking 直出 tool 是常见 fast-path，parser 须覆盖。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 reasoning+tool 双 parser 部署 agent 模型（是/否/未知）。

- [[KV-offload][FS]: Batching for read/write threads（vLLM PR #49225, updated **2026-08-09**）](https://github.com/vllm-project/vllm/pull/49225)
  - 摘要: #49152 的 request-level batching 导致多 request 的 read job 在磁盘交错，单 request 总 read 时间上升。Fix：按 request FCFS，单 request 拆成与线程数相等的 batch 均分；benchmark 覆盖 block size 1024/16、8/16 read threads。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/KV**：长上下文 agent 的 CPU/NVMe KV tier 延迟直接决定 TTFT；per-request 并行化是结构性优化。
  - 明日动作(<=20min): 写 **≤15 词**：你 profiling 是否拆分 KV reload vs prefill（是/否/未知）。

- [[ROCm] Portable Triton paged-MQA-logits kernel for DeepSeek-V4 sparse indexer（vLLM PR #51555, updated **2026-08-09**）](https://github.com/vllm-project/vllm/pull/51555)
  - 摘要: DeepSeek-V4 sparse indexer 的 paged-MQA logits 在 ROCm 上若 Triton 不支持 fp8 `tl.dot`（如 gfx1151），只能走 pure-python torch reference，每 seq/KV block 一次 `.item()` host sync → 每层 pipeline stall。Fix：向量化 torch fallback + 新增 `portable_fp8_paged_mqa_logits` Triton kernel（kernel 内 dequant fp8→bf16 再 dot）。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：sparse attention indexer 是下一代长上下文架构；host sync per block = agent 多轮推理的隐藏延迟。
  - 明日动作(<=20min): 写 **≤15 词**：你是否关注 sparse indexer / MQA logits 路径（是/否/未知）。

## 3) 热门项目/可试个人项目

- [feat: add fastCRW web reader (CrwWebReader)（LlamaIndex PR #21980, updated **2026-08-09**）](https://github.com/run-llama/llama_index/pull/21980)
  - 摘要: 新增本地 `CrwWebReader`（对标 `FireCrawlWebReader`）。fastCRW 单 ~8MB Rust binary、AGPL，本地 stealth/JS 渲染/BYO-proxy；自托管 Firecrawl 的 stealth engine 为 cloud-only。benchmark：truth-recall 63.74% vs Firecrawl 56.04%，p50 ~1.9s vs ~2.3s；search 层基于 SearXNG + query expansion + content rerank。
  - 为什么重要(对我): **RAG/skill 落地**——agent 数据采集链可完全本地化，降低 token 预处理成本（对比 PDF→图片链）且可自托管。
  - 明日动作(<=20min): `pip install` readers-web 包，用 1 个 JS-heavy URL 对比 CrwWebReader vs 现有 reader 的 token 输出长度。
  - 估计复现成本: **低**（单 binary + LlamaIndex reader 接口，首跑 ~20min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [ian-xiaohei-illustrations — Codex Skill 正文配图生成（@ianneo_ai, GitHub **2026**）](https://github.com/helloianneo/ian-xiaohei-illustrations)
  - 机会信号: 9k+ stars 的 Skill 产品化路径清晰——从自用 Notion 配图 → 开源 Skill → GitHub trending；核心不是「画图 prompt」而是「认知锚点→shot list→16:9 配图」workflow，与一人公司「方法论→可复用 artifact」杠杆模型一致。
  - 风险信号: 视觉 IP 依赖单一角色风格；Codex/Claude Skill 生态变化快，分发靠平台而非 SEO。
  - 我可执行的一步(<=30min): 读 `SKILL.md` 前 50 行，写 **3 条** 你可从该 Skill 结构借鉴的「eval rubric → artifact」映射（各 ≤20 词，不抄视觉）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[3348. Smallest Divisible Digit Product II](https://leetcode.com/problems/smallest-divisible-digit-product-ii/)（**Day 39, Aug 2026 Daily Challenge**；承接 0806–0808 数位/频率链，今日 Hard 切质因数分解 + 贪心构造）：给定字符串 `num` 与整数 `t`，求 ≥`num` 的最小 zero-free 数使其各位乘积被 `t` 整除，不存在返回 `"-1"`。明日动作：模板化 prime factor(2,3,5,7) + 右扫增大 digit；写 **≤15 词**：「质因数背包」与 agent eval「约束可满足构造」的类比。

- **IELTS（≤1 条）**：[IELTS Academic Writing Task 1（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/writing/academic/task-1)：0808 已轮换 Reading，今日换 **Academic Writing Task 1**（20 分钟完成 1 篇图表描述 + 对照 model answer）。明日动作：记录 **≥3 个** 句式模板（overview/comparison/trend）；写 **≤15 词** 你最弱的 Task 1 题型（折线/柱状/流程图）。

## 6) 噪音过滤记录

- **LangGraph #8577/#8571**：`adelete_thread` 缺 `setup()` — infra fix，非当前 focus → 过滤。
- **LangGraph #8574/#8573**：0808 已列 SSL pipeline/OTel OOM → 不重复。
- **LangGraph #8569 bytecode subgraph**：0808 已收录 → 不重复。
- **LangChain #39358 PydanticUndefined、#39342 generator inputs**：边缘序列化 → 过滤。
- **LangChain #39361 langchain-rustchain**：新 partner 集成，与当前 focus 弱相关 → 过滤。
- **AutoGen #7981/#8026 message order/MCP override**：0808 已收录 → 不重复。
- **LlamaIndex #21968~#21981 async offload/readers 批量 PR**：chore/边缘 reader，§1 已满 → 过滤。
- **vLLM #51545/#51534/#51422**：MoE pad/FP8 quant/CI bump — 与 Attention focus 弱相关 → 过滤。
- **openai/evals #1700 RPI**：0808 已收录 → 不重复。
- **Simon Willison Blog Aug 7 Tokenpocalypse/timeline**：0808 已收录 → 不重复；Aug 9 无新帖。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-08-08~09** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Aug 8–9 高信号 ≥8 条，无需 skill 补充。
- **Prime Agent blog（非白名单）**：RLM harness 有趣但非 career 白名单且与 §1 重叠 → 过滤。

## 7) 本周累计计数

- Agent: **694**（`0808.md` §7: 686 + 本日 §1: 8）
- Theory: **305**（`0808.md` §7: 300 + 本日 §2: 5）
- Career/Business: **115**（`0808.md` §7: 114 + 本日 §4: 1）
- LC: **97**（`0808.md` §7: 96 + 本日 §5: 1）
- IELTS: **97**（`0808.md` §7: 96 + 本日 §5: 1）
