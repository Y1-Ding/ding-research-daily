# Daily Research - 2026-08-10 (21:30)

## 0) 今日3条结论

- **`UntrackedValue` 在 Send checkpoint 后 resume 会丢 key**（LangGraph #8584, Aug 10）：含 `UntrackedValue` 的 Send 任务 checkpoint 后重试输入结构变化 → **parallel skill/eval 若用 Send + 非持久化字段，resume 路径须单独回归**。
- **`create_react_agent` 把 thinking block 回灌模型触发 HTTP 400**（LangGraph #8580, Aug 9）：DeepSeek thinking mode 等 provider 拒绝 input 中的 `{"type":"thinking"}` → **token 优化/trajectory 裁剪须区分「持久化 state」与「模型 input view」**。
- **MTP spec-decode + P/D 外部 KV load 多 reserve 1 block**（vLLM #50173, Aug 10）：sync 路径 lookahead + `pad_spec_decode` 在 `num_external_computed_tokens>0` 时仍多分配 → `assert len(local) <= len(remote)` 失败 → **长 session agent 的 disaggregated serving eval 须覆盖首步 external-KV-load**。

## 1) Agent工程化（high-signal）

- [fix(langgraph): restore UntrackedValue input to Send tasks on resume（LangGraph PR #8584, updated **2026-08-10**）](https://github.com/langchain-ai/langgraph/pull/8584)
  - 摘要: Fixes #8582。Send 任务 arg 含 `UntrackedValue` 时，checkpoint 后该 key 丢失，resume 输入与原始结构不一致。Fix：sanitizer 在持久化 Send 中保留稳定 marker，原始值仅进程内保留（不写 checkpoint），同进程重试时恢复原始输入。
  - 为什么重要(对我): **skill 落地 + 评测**——parallel map-reduce / dynamic Send 是常见 agent pattern；resume 输入漂移 = eval 假 fail 或 silent 行为变化。
  - 明日动作(<=20min): 写 **是/否**：你 graph 是否用 Send + 非 checkpoint 字段（是/否/未知）。

- [fix(prebuilt): strip thinking blocks from model input in create_react_agent（LangGraph PR #8580, updated **2026-08-09**）](https://github.com/langchain-ai/langgraph/pull/8580)
  - 摘要: Fixes #6521。`create_react_agent` 将模型返回的 `{"type":"thinking"}` content block 回灌后续 model call，DeepSeek thinking mode（OpenAI-compatible）等 provider 报 HTTP 400。Fix：sync/async/structured-output 路径过滤 thinking block 出 model input view，但 state 仍保留原始输出；`reasoning` / `reasoning_content` 不受影响。
  - 为什么重要(对我): **token 优化 + skill 落地**——thinking 块占 token 且部分 provider 禁 input；middleware 须双视图：持久化全量 vs 模型可见子集。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否在 model input 前 strip thinking blocks（是/否/未知）。

- [fix: use deep-agent template with OpenTelemetry dependency fix（LangGraph PR #8585, updated **2026-08-10**）](https://github.com/langchain-ai/langgraph/pull/8585)
  - 摘要: Closes #8409。`langgraph new` Deep Agent 模板 pin `opentelemetry-exporter-prometheus>=0.58b0,<0.59` 与 langgraph-api 0.11.* 不兼容，并发运行时 telemetry/metrics loop 导致 OOM。Fix：模板 URL 指向已修正 OTel 依赖（`>=0.62b0`）的分支。
  - 为什么重要(对我): **skill 落地**——`langgraph new` 是 agent 项目 bootstrap 路径；默认模板 OOM = 新 skill 项目首跑即踩坑。
  - 明日动作(<=20min): 写 **是/否**：你 deep-agent 项目是否 pin OTel exporter >=0.62（是/否/未知）。

- [fix(core): handle PydanticUndefined default for default_factory fields in _try_neq_default（LangChain PR #39358, updated **2026-08-10**）](https://github.com/langchain-ai/langchain/pull/39358)
  - 摘要: Closes #39157。Pydantic 2.14+ `FieldInfo.get_default()` 对 `default_factory` 字段返回 `PydanticUndefined` 而非 `None`；`_try_neq_default()` 误判为非默认 → `output_version=None` 泄漏进 repr 并改变 `_get_llm_string()` cache key。Fix：遇 `PydanticUndefined` + `default_factory` 时调用 `get_default(call_default_factory=True)`。
  - 为什么重要(对我): **评测 + token 优化**——LLM cache key 漂移 = eval ablation 不可复现、重复计 token。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否依赖 LangChain LLM cache（是/否/未知）。

- [Fix #20386: Deterministic tool I/O pre/post-processing middleware for agents（LlamaIndex PR #22621, updated **2026-08-10**）](https://github.com/run-llama/llama_index/pull/22621)
  - 摘要: `FunctionTool` 新增 `pre_processor` / `async_pre_processor` / `post_processor` / `async_post_processor` hook，在 tool 执行前后确定性变换 I/O；motivating case 为 MCP tool 标准化输入输出。
  - 为什么重要(对我): **skill 落地**——MCP tool 接入常需 schema 归一化/PII 脱敏；middleware hook 是可复用 pattern，避免每个 tool 重复包装。
  - 明日动作(<=20min): 列 **1 个** 你现有 tool 可用 pre/post hook 解决的 I/O 不一致（≤15 词）。

- [Fix similarity_top_k=0 returning all results（LlamaIndex PR #22623, updated **2026-08-10**）](https://github.com/run-llama/llama_index/pull/22623)
  - 摘要: Fixes #22508。`get_top_k_embeddings` / `get_top_k_mmr_embeddings` 用 truthy check 判断 `similarity_top_k`，使 `0` 与 `None`（无限制）不可区分 → `top_k=0` 返回全部 embedding。Fix：改为 `is not None` 判断。
  - 为什么重要(对我): **评测**——RAG eval 用 `top_k=0` 作 dry-run / 空检索 baseline 时会 silent 污染结果。
  - 明日动作(<=20min): 写 **是/否**：你 RAG eval 是否用 `top_k=0` 作空检索对照（是/否/未知）。

- [fix(agentchat): SelectorGroupChat fallback must not return excluded previous speaker（AutoGen PR #7936, updated **2026-08-10**）](https://github.com/microsoft/autogen/pull/7936)
  - 摘要: Fixes #7471。`allow_repeated_speaker=False` 时，selector 模型重试耗尽后 fallback 仍返回 `_previous_speaker`（已被排除）→ 双人 team **livelock**。Fix：gate fallback 于 `allow_repeated_speaker`，否则选第一个非 previous speaker。
  - 为什么重要(对我): **评测**——multi-agent eval 的 termination 条件须覆盖 selector fallback；livelock = 无限 token 消耗。
  - 明日动作(<=20min): 写 **是/否**：你 multi-agent eval 是否设 max_turn + livelock 检测（是/否/未知）。

- [[Bugfix] Resolve $ref/$defs in tool schemas and preserve definitions（vLLM PR #50933, updated **2026-08-10**）](https://github.com/vllm-project/vllm/pull/50933)
  - 摘要: Fixes #46924。Pydantic v2 tool schema 用 `$defs`/`$ref` 定义嵌套类型；`find_tool_properties()` 不 resolve ref 且 `_get_tool_schema_defs()` 用 `pop("$defs")` 破坏原始 dict → 嵌套 object（如 `{"kind":"week"}`）被 double-encode 为 JSON string。Fix：`_resolve_refs()` inline ref + `get` 替代 `pop`。
  - 为什么重要(对我): **skill 落地 + 评测**——复杂 tool schema 的 serving 层解析错误 = agent 传参 silent 损坏。
  - 明日动作(<=20min): 写 **是/否**：你 tool eval 是否含嵌套 object 参数（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix][Core][Spec Decode] Skip spec-decode block reservation on external-KV-load step（vLLM PR #50173, updated **2026-08-10**）](https://github.com/vllm-project/vllm/pull/50173)
  - 摘要: MTP spec-decode + P/D disaggregation（sync MoRIIO READ）下，decode 节点在首步 external-KV-load 多 reserve 1 block（lookahead + `pad_spec_decode`），超过 prefill 发送的 remote block 数 → `assert len(local) <= len(remote)`。Fix：当 `num_external_computed_tokens > 0` 时 gate 两者，与 async 路径对齐。
  - 为什么重要(对我): **Talk ≥3 turns + KV/Attention**：disaggregated serving 是长 session agent 扩容路径；首步 block 计数 off-by-one = 硬 crash 或 silent OOM。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 P/D disaggregation + spec decode（是/否/未知）。

- [[CPU][MLA] Fix prefill backend selection so MLA runs end-to-end on CPU（vLLM PR #51471, updated **2026-08-10**）](https://github.com/vllm-project/vllm/pull/51471)
  - 摘要: #49453 后续。DeepSeek-V2-Lite MLA 在 CPU 上无法跑完整 external-hit prefill（如 LMCache reload）。Fix：CPU MLA prefill 路由到 CPU-native backend；实现 `return_softmax_lse=True` suffix prefill 与 `run_prefill_context_chunk` contextful prefill。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/MLA**：MLA 是下一代 KV 压缩架构；CPU tier prefill 是边缘/成本优化 agent 部署的关键路径。
  - 明日动作(<=20min): 写 **≤15 词**：你是否关注 MLA CPU offload 路径（是/否/未知）。

- [[Perf][Hybrid] 3D-grid tiling of the state-copy Triton kernels（vLLM PR #49436, updated **2026-08-10**）](https://github.com/vllm-project/vllm/pull/49436)
  - 摘要: Follow-up #48110。Hybrid model（Mamba/SD）state copy kernel 小 batch 时 SM 利用率低且要求 8B 对齐。Fix：新增 `program_id(2)` 3D grid 分片 temporal copy；SD conv 共享 fused launch；head/body/tail 模式解除 8B 硬对齐。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/state**：hybrid SSM+Attention 是长上下文 agent 架构趋势；state copy 是 decode 隐藏瓶颈。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serve hybrid（Mamba/Jamba）模型（是/否/未知）。

- [[FEAT] Support fast engine recovery through weight cache（vLLM PR #49879, updated **2026-08-10**）](https://github.com/vllm-project/vllm/pull/49879)
  - 摘要: 引擎重启 dominated by disk weight load。Proposal：per-GPU daemon 持久持有 post-quantized TP-sharded weights；重启时通过 CUDA IPC zero-copy map，跳过 disk reload。
  - 为什么重要(对我): **Talk ≥3 turns + serving**：agent 服务频繁滚动升级时，冷启动延迟直接决定 failover SLA；weight cache 改变 agent 部署成本模型。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent serving 冷启动是否 >30s（是/否/未知）。

- [[Bugfix] Account for NUMA memory policy when sizing available RAM（vLLM PR #51584, updated **2026-08-10**）](https://github.com/vllm-project/vllm/pull/51584)
  - 摘要: `_get_available_ram_bytes()` 报告 host-wide free memory，但 `MPOL_BIND` 进程无法使用其他 node 内存 → checkpoint prefetch 高估可用 RAM → SIGKILL 无诊断。Fix：按 bound node 计算可用内存并 warn checkpoint 是否 fit。
  - 为什么重要(对我): **Talk ≥3 turns + KV/memory**：大模型 agent serving 在 NUMA 机器上常见；内存 sizing 错误 = 间歇性 OOM 难归因。
  - 明日动作(<=20min): 写 **≤15 词**：你部署环境是否设 NUMA policy（是/否/未知）。

## 3) 热门项目/可试个人项目

- [Add llama-index-tools-olostep integration（LlamaIndex PR #22624, updated **2026-08-10**）](https://github.com/run-llama/llama_index/pull/22624)
  - 摘要: 新增 `OlostepToolSpec`，提供 `scrape` / `answer` / `crawl` / `map_site` 四工具，返回 `Document` 对象直连 LlamaIndex agent/indexing；PyPI `pip install llama-index-tools-olostep`。
  - 为什么重要(对我): **RAG/skill 落地**——web data collection 是 agent 常见依赖；Olostep 对标 Tavily 但结构更轻，适合本地 eval 对比。
  - 明日动作(<=20min): `pip install llama-index-tools-olostep`，用 1 个 URL 跑 `scrape` 对比现有 reader 的 token 输出长度。
  - 估计复现成本: **低**（PyPI 包 + 标准 ToolSpec 接口，首跑 ~15min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [A quote from OpenClaw — agent 实战发现 API 零鉴权（Simon Willison Blog, **2026-08-10**）](https://simonwillison.net/2026/Aug/10/openclaw/)
  - 机会信号: OpenClaw agent 在澳大利亚健身房预约网站发现 cancel API 无鉴权，可取消他人预约并改变 waitlist 排名——**真实世界 agent 安全审计**是 emerging 服务品类；一人公司可用「agent 渗透测试」切入 SMB。
  - 风险信号: 未经授权测试可能违法；quote 非完整 case study，缺 remediation 细节。
  - 我可执行的一步(<=30min): 读 [OpenAI×HF 事故时间线](https://simonwillison.net/2026/Aug/7/openai-timeline/) 前 5 分钟，写 **3 条**「agent 授权边界」checklist（各 ≤20 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[3349. Adjacent Increasing Subarrays Detection I](https://leetcode.com/problems/adjacent-increasing-subarrays-detection-i/)（**Day 40, Aug 2026 Daily Challenge**；承接 0806–0809 数位/构造链，今日 Easy 切递增子数组相邻检测）：给定 `nums` 与 `k`，判断是否存在两个长度为 `k`、严格递增、相邻（`b=a+k`）的子数组。明日动作：单遍维护 `pre`/`cur` 递增段长度，更新 `mx=max(mx, cur//2, min(pre,cur))`；写 **≤15 词**：「相邻段检测」与 agent trace「连续合法 action 段」的类比。

- **IELTS（≤1 条）**：[IELTS Practice Speaking Test - Part 2（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/speaking/part-2)：0809 已轮换 Writing Task 1，今日换 **Speaking Part 2**（1 分钟准备 + 1–2 分钟独白 + rounding-off questions）。明日动作：选 1 个 cue card 录音自评，记录 **≥3 个** 连接词（furthermore/however/in terms of）；写 **≤15 词** 你最弱的 Part 2 话题类型（人物/地点/物品/事件）。

## 6) 噪音过滤记录

- **LangGraph #8583**：checkpoint-postgres `ssl` vs `sslmode` 文档澄清 — 运维文档，非当前 focus → 过滤。
- **LangGraph #8581**：scalar resume 多 interrupt 拒绝 — 0809 前后已覆盖 interrupt 语义 → 边缘 case 过滤。
- **LangChain #39388 ShellSession newline**：agent shell 边缘 fix，与当前 focus 弱相关 → 过滤。
- **LangChain #39430 model-profiles refresh**：自动化数据刷新，无 actionable insight → 过滤。
- **AutoGen #8033 tool error docs**：文档向，#7936 已覆盖 eval 侧 → 过滤。
- **AutoGen #8009 OpenEval adapter**：0809 已收录 → 不重复。
- **LlamaIndex #21980 fastCRW**：0809 已收录 → 不重复。
- **LlamaIndex #22622 asyncio_run errors**：边缘 async fix → 过滤。
- **vLLM #51657/#50020/#43680**：multimodal hardening/encoder stats/routing sim — 与 Attention focus 弱相关 → 过滤。
- **vLLM #51654/#51675/#51678**：frontend API fix — 非原理 focus → 过滤。
- **openai/evals #1707–#1711**：community 模板 PR，可信度不足 → 过滤。
- **Simon Willison Aug 7–8 帖**：0808–0809 已收录 → 不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-08-10** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Aug 10 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **702**（`0809.md` §7: 694 + 本日 §1: 8）
- Theory: **310**（`0809.md` §7: 305 + 本日 §2: 5）
- Career/Business: **116**（`0809.md` §7: 115 + 本日 §4: 1）
- LC: **98**（`0809.md` §7: 97 + 本日 §5: 1）
- IELTS: **98**（`0809.md` §7: 97 + 本日 §5: 1）
