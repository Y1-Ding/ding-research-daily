# Daily Research - 2026-09-14 (21:30)

## 0) 今日3条结论

- **LangGraph `stream_mode="messages"` 可服务端限定 state keys**（#8868, updated **2026-09-14**）：`__pregel_stream_messages_keys` 只扫描列出的字段 → **agent 对外流式须默认 pin `["messages"]`，评测要测 internal channel（如 `agent_messages`）不被泄露。**
- **Adaptive DSpark 在 TP 上须 rank-0 广播 draft budget**（vLLM #56831, created **2026-09-14**）：rank-local confidence 分歧会导致 collective hang → **spec-decode + 多卡 serving eval 必须含 TP≥2 的 consensus 用例。**
- **零范数 embedding 的 cosine 相似度应从 `nan` 改为 `0.0`**（LlamaIndex #23050, created **2026-09-14**）：`nan` 会污染 top-k heap → **RAG/agent retrieval eval 须含空文本/零向量 gold，断言不进入 top-k。**

## 1) Agent工程化（high-signal）

- [feat(langgraph): restrict stream_mode="messages" to specific state keys（LangGraph PR #8868, updated **2026-09-14**）](https://github.com/langchain-ai/langgraph/pull/8868)
  - 摘要: Closes #6798。`stream_mode="messages"` 原先扫描 state 中全部 `BaseMessage`，内部 reasoning channel（如 `agent_messages`）会泄露到客户端。新增 `configurable["__pregel_stream_messages_keys"]`：设置后仅扫描所列 state keys；未设置行为与旧版一致（含 backwards-compat test）。`Pregel.stream()` / `astream()` 读取 `CONFIG_KEY_STREAM_MESSAGES_KEYS` 并传给 `StreamMessagesHandler(V2)`。
  - 为什么重要(对我): **skill 落地 + 评测**——对外 message stream 的 allowlist 是 agent platform 的 privacy/trace-contract gold；须测未列 key 不出现在 client stream。
  - 明日动作(<=20min): 写 **是/否**：你的 LangGraph agent 是否在 server config 里 pin `__pregel_stream_messages_keys`（是/否/未知）。

- [fix(checkpoint): serialize datetime64 NumPy arrays（LangGraph PR #8916, created **2026-09-14**）](https://github.com/langchain-ai/langgraph/pull/8916)
  - 摘要: Fixes #8689。`JsonPlusSerializer` 对 C-contiguous NumPy 走 `memoryview` 快路径，但 `datetime64`/`timedelta64` 不支持 buffer view，序列化报 `TypeError: not msgpack serializable`。Fix 将这两类 dtype 路由到已有 `tobytes` 安全路径；`test_jsonplus.py` 增加 round-trip 回归。
  - 为什么重要(对我): **评测 + skill 落地**——checkpoint/store 对时序特征的 serde 是 long-running agent eval 的 persistence gold；须测含 `datetime64` 的 state 可恢复。
  - 明日动作(<=20min): 写 **是/否**：你的 agent checkpoint eval 是否含 NumPy 时间序列字段（是/否/未知）。

- [fix(sdk-py): keep locking SyncStreamController reconnect（LangGraph PR #8918, created **2026-09-14**）](https://github.com/langchain-ai/langgraph/pull/8918)
  - 摘要: Fixes #8896。`SyncStreamController` 存在 shadowed `_reconnect_shared_stream`，后定义覆盖 #7829 的 locking 实现。Fix 合并为单一方法（保留 backoff、old-handle close、lock-guarded state）；回归 `test_sync_reconnect_shared_stream_defined_once` 与 backoff test。
  - 为什么重要(对我): **skill 落地 + 评测**——同步流重连竞态是 client-side agent harness 的 reliability gold；须测断线重连不丢锁、不双定义。
  - 明日动作(<=20min): 写 **是/否**：你是否用 LangGraph Python SDK 同步流式（是/否/未知）。

- [fix(core): preserve exception propagation in run_async_tasks with show_progress=True（LlamaIndex PR #23048, created **2026-09-14**）](https://github.com/run-llama/llama_index/pull/23048)
  - 摘要: Fixes #22493。`show_progress=True` 时 `except Exception: pass` 吞掉协程异常，再 await 已消费协程并抛出误导性 `RuntimeError: Detected nested async...`。Fix 重构使成功/失败路径与 `show_progress=False` 一致；参数化单测覆盖两模式。
  - 为什么重要(对我): **评测 pipeline**——批量 async ingest/eval 在 progress UI 下须 fail-fast；否则 eval job 假绿。
  - 明日动作(<=20min): 写 **是/否**：你的 collect/eval 是否用 `run_async_tasks(..., show_progress=True)`（是/否/未知）。

- [fix(core): return 0.0 similarity for zero-norm embeddings instead of nan（LlamaIndex PR #23050, created **2026-09-14**）](https://github.com/run-llama/llama_index/pull/23050)
  - 摘要: Fixes #23049。默认 cosine `similarity()` 在零范数向量时返回 `nan`，`get_top_k_embeddings` 因 `nan` 比较恒 false 把零向量节点错误保留在 top-k。Fix：任一侧 norm 为 0 时返回 `0.0`（对齐 sklearn 惯例）；DOT_PRODUCT/EUCLIDEAN 不变。
  - 为什么重要(对我): **评测 + RAG agent**——空 chunk/空 query embedding 是 retrieval eval 的 edge-case gold。
  - 明日动作(<=20min): 写 **是/否**：你的 RAG eval 是否含空文本 embedding 用例（是/否/未知）。

- [fix(mcp): expand URI-template variables for dynamic resource tools（LlamaIndex PR #23011, updated **2026-09-14**）](https://github.com/run-llama/llama_index/pull/23011)
  - 摘要: Fixes #23001。`McpToolSpec.to_tool_list_async()` 将 `ResourceTemplate` 暴露为无参 tool，以字面 URI（如 `repo://{owner}/{name}/README.md`）调用 `read_resource()` 导致永远无法解析。Fix：检测 `uri_template`，从 `{var}` 建 Pydantic schema，展开 URI 后 `read_resource()`；无 `uri`/`uri_template` 的资源跳过并 warning。
  - 为什么重要(对我): **skill 落地（MCP）+ 评测**——动态 MCP resource template 是 agent tool surface 的 contract gold；须测参数绑定后 resource 可读。
  - 明日动作(<=20min): 写 **是/否**：你的 MCP agent eval 是否覆盖 URI template 参数化（是/否/未知）。

- [fix(openrouter): properly disable sdk retries when max_retries=0（LangChain PR #40442, created **2026-09-14**）](https://github.com/langchain-ai/langchain/pull/40442)
  - 摘要: Fixes #40379。`ChatOpenRouter(max_retries=0)` 省略 `retry_config` 时 OpenRouter SDK 将 `UNSET` 解释为默认最长约 **1 小时**指数退避。Fix：`max_retries<=0` 时显式传 `retry_config=None`；并修正此前断言错误行为的单测。
  - 为什么重要(对我): **token 优化 + 评测**——eval 须 pin latency/cost 上界；`max_retries=0` 必须零隐式重试。
  - 明日动作(<=20min): 写 **是/否**：你的 agent eval 是否断言 OpenRouter `max_retries=0` 无 SDK backoff（是/否/未知）。

- [Eval Engineering Skill: Build Evals From Repo Context and Traces（LangChain Blog, **2026-07-22**；配套 repo 更新 **2026-09-14**）](https://www.langchain.com/blog/towards-automating-eval-engineering)
  - 摘要: 发布 **eval-engineering** skill：读 repo（prompts/tools/skills/hooks）+ 可选 LangSmith traces，与用户访谈后产出 Harbor 格式 task（`evals/<name>/task.toml|instruction.md|environment|tests`）；强调 verifier 与 agent trajectory 双侧检视以防 reward hack。安装：`npx skills add langchain-ai/langchain-skills --skill eval-engineering`（[langchain-skills](https://github.com/langchain-ai/langchain-skills) 于 **2026-09-14** 仍有活跃更新）。
  - 为什么重要(对我): **skill 落地 + 评测**——「trace → Harbor task → 并行跑配置」与 map_prompt_test collect→test→eval 同构；可复用为内部 eval 脚手架 v0.1。
  - 明日动作(<=20min): 读 blog「Eval Design is iterative」一节，写 **1 条** verifier trajectory 对你 L2 eval 的接点（≤20 词）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix][Spec Decode] Synchronize adaptive DSpark decisions across TP ranks（vLLM PR #56831, created **2026-09-14**）](https://github.com/vllm-project/vllm/pull/56831)
  - 摘要: Adaptive DSpark 的 verification budget 来自 rank-local confidence，TP 间微小差异可导致不同 total budget / per-request boundary → collective hang。Fix：rank 0 广播选定 draft budget 与 partial-allocation capacities。TP1/2/4/8 单测 + H100 NCCL TP2/4（Qwen3-4B + `dspark_qwen3_4b_block7`）；注入 rank drift 时 patched 代码 0 consensus mismatch（原 20）。
  - 为什么重要(对我): **Talk ≥3 turns + spec-decode/Attention**——多卡 draft verification 一致性是 serving eval 的 distributed-gold。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 TP + speculative decode（是/否/未知）。

- [[Bugfix][SparseMLA] Fix piecewise cudagraph capture crash in index group（vLLM PR #56825, created **2026-09-14**）](https://github.com/vllm-project/vllm/pull/56825)
  - 摘要: sparse-MLA 模型（如 GLM-5.3-Flash hybrid KDA + sparse-MLA）在 **piecewise** cudagraph decode capture 时，`index_group.py` 的 `side_stream.wait_event` 触发 `CUDA error: invalid argument`，引擎启动失败。Fix 针对 index group 在 capture 路径的事件同步。
  - 为什么重要(对我): **Talk ≥3 turns + sparse MLA Attention**——sparse top-k index + cudagraph 是长上下文 Attention 后端 eval 的 startup-gold。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 sparse-MLA + piecewise cudagraph（是/否/未知）。

- [[Bugfix] Bill hidden-state extraction at its own KV cache group width（vLLM PR #56822, created **2026-09-14**）](https://github.com/vllm-project/vllm/pull/56822)
  - 摘要: Fixes #56774。hybrid Mamba/MLA 上 `extract_hidden_states` 的 1-layer group 按最宽 peer group（如 24 MLA layers）计费，~5.4 GiB 被计为 ~130 GiB 导致启动失败。Fix：存在 hidden-state cache 时将各 group 拆为单层，使 pool width 与 admission 按 extraction 自身 page 计费（仅 `extract_hidden_states` 路径）。
  - 为什么重要(对我): **Talk ≥3 turns + KV/Attention memory**——heterogeneous KV group 计费是 hybrid model serving eval 的 memory-gold。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 hybrid Mamba/MLA hidden-state 提取（是/否/未知）。

- [[Bugfix][GLM-5.3-Flash] Fix fp8_fp4_paged_mqa_logits MQA kpool varlen path for speculative decode（vLLM PR #56811, created **2026-09-14**）](https://github.com/vllm-project/vllm/pull/56811)
  - 摘要: kpool sparse attention indexer decode 路径未把 `decode_metadata.indices` 传入 `fp8_fp4_paged_mqa_logits`：metadata 可走 varlen（`is_varlen=true`），但 DeepGEMM kernel 以 `is_varlen=false, next_n=1` 实例化——正确性不受影响，但 flattened spec-decode 每 query row 独立处理，性能大幅下降（SM100 特有）。
  - 为什么重要(对我): **Talk ≥3 turns + MQA/paged Attention**——spec-decode flatten + varlen grouping 是 throughput eval 的 kernel-contract gold。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 GLM-5.3-Flash kpool + spec decode（是/否/未知）。

- [[Bugfix][KV Offload] Skip non-prefix-cacheable groups in SimpleCPUOffload（vLLM PR #56810, created **2026-09-14**）](https://github.com/vllm-project/vllm/pull/56810)
  - 摘要: GLM-5.3-Flash 的 kpool-tail scratch group（`prefix_cacheable=False`, `block_size=4`）在 `SimpleCPUOffloadScheduler` 仍被当作可 offload：对 tail group 做 `resolve_block_hashes(..., hash_block_size=1152, block_size=4)` 触发 divisibility assert（`--kv-events-config` 开启 KV events 时）。Fix 与 core/Mooncake 一致跳过 non-prefix-cacheable groups。
  - 为什么重要(对我): **Talk ≥3 turns + KV cache / prefix Attention**——多 group KV 的 prefix-cache 语义是 long-context agent serving eval 的 offload-gold。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 CPU KV offload + hybrid MLA（是/否/未知）。

## 3) 热门项目/可试个人项目

- [langchain-ai/langchain-skills — Agent skills for LangChain/LangGraph/Deep Agents（~1215 stars, GitHub API updated **2026-09-14**）](https://github.com/langchain-ai/langchain-skills)
  - 摘要: MIT；21 个 skills（含 **eval-engineering**、langgraph-fundamentals、deep-agents 等）；`npx skills add langchain-ai/langchain-skills --skill <name>` 安装；eval-engineering 依赖 Harbor，从 repo + traces 生成 `evals/` Harbor tasks。
  - 为什么重要(对我): **skill 落地 + 评测**——与 Cursor/Harness 插件 skill 规范同轨；可对照你现有 agent eval playbook 做 skill→Harbor 映射表。
  - 明日动作(<=20min): `npx skills add langchain-ai/langchain-skills --skill eval-engineering --yes` 后读 `eval-engineering/SKILL.md` 目录结构，写 **1 条** 可迁入你 repo 的字段（≤20 词）。
  - 估计复现成本: **低**（只读 skill 文档 + 本地 `harbor` dry-run，~30min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Building agents with Skills: Equipping agents for specialized work（Anthropic Blog）](https://claude.com/blog/building-agents-with-skills-equipping-agents-for-specialized-work)
  - 机会信号: Anthropic 将 **Agent Skills 发布为开放标准**（与 MCP 类比），强调 progressive disclosure 降 context 成本 → **垂域 skill 包 + 跨平台可移植** 可作独立交付物（评测/合规/行业 workflow），对齐「AI 个人公司」产品化而非纯模型 API。
  - 风险信号: skills 生态快速同质化；平台仍控制 runtime/计费；无 verifier 的 skill 易沦为 prompt 碎片。
  - 我可执行的一步(<=30min): 列 **3 个** 你现有项目可拆成公开 skill 的能力边界（各 ≤15 词），标「需 Harbor eval / 否」。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[836. Rectangle Overlap](https://leetcode.com/problems/rectangle-overlap/)（**2026-09-14 Daily Challenge**，verified via LeetCode GraphQL `activeDailyCodingChallengeQuestion`，Easy）：关键洞察——两轴对齐矩形是否相交可化为区间重叠判定（`max(left) < min(right)` 两维）；与 agent eval「bounding-box / 工具输出区域冲突检测」同构。明日动作：完成 Easy 一题；写 **≤15 词**：区间重叠对你 tool-args 校验意味着什么。

- **IELTS（≤1 条）**：[IELTS Reading practice tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/reading)：0913 为 Listening，今日 **Reading**（选 1 套，限时 ~60min）。明日动作：完成 1 套 Reading；记录 **≥2 个**失误类型（T/F/NG 混淆 / 定位慢 / 同义替换）；写 **≤15 词**你最弱的 Reading 题型。

## 6) 噪音过滤记录

- **fix(core): strip UTF-8 BOM in Blob（#40439）**：ingestion 边缘 → 弱于当日 stream/MCP 主题 → 过滤。
- **test MCPAdapter Windows / chore model-profiles / Redis test skip（#40437/#40436/#8917）**：test/chore → 过滤。
- **fix: use dtype vs torch_dtype（#56833）**：deprecation cleanup → 过滤。
- **Marlin ReLU2 / ROCm GEMM / ViT CUDA graph / watermarking 系列（#56827/#56812/#56820/#56806–#56807）**：kernel/watermark → 弱于 Attention/KV focus → 过滤。
- **Social impact RAG example / GoogleSheetsReader（#23047/#23046）**：example/reader bug → 过滤。
- **0909–0913 已收录项**：LangChain #40427/#40430、LangGraph #8905、LlamaIndex #23038/#22740/#22249、AutoGen #8206、vLLM #56692/#56698/#56673、Simon Sep-12 compaction 等 → semantic duplicate，不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-14** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + Anthropic Blog ≥8 条主信号，无需 skill 补充。
- **LangChain Harbor 集成长文（2026-06-30）**：与 eval-engineering 博文部分重叠且非当日 → 不单独收录（见 §1 eval-engineering 链）。

## 7) 本周累计计数

- Agent: **977**（`0913.md` §7: 969 + 本日 §1: 8）
- Theory: **448**（`0913.md` §7: 443 + 本日 §2: 5）
- Career/Business: **146**（`0913.md` §7: 145 + 本日 §4: 1）
- LC: **127**（`0913.md` §7: 126 + 本日 §5: 1）
- IELTS: **127**（`0913.md` §7: 126 + 本日 §5: 1）
