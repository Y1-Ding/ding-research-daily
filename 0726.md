# Daily Research - 2026-07-26 (21:30)

## 0) 今日3条结论

- **MCP skill 落地必须隔离 workflow 实例**（LlamaIndex #22466, **2026-07-26**）：`workflow_as_mcp` 原先复用同一 `Workflow` 实例，跨调用共享 `self` 可变状态 → eval/多租户场景会出现串话。新增 `workflow_factory` 每次调用创建新实例，是 skill 封装的最低生命周期要求。
- **短 prefill 应跳过 sparse indexer 评分**（vLLM #48407, **2026-07-26**）：GLM-5.2 sparse attention 在 dense 短 prefill 路径上 indexer scoring 是死计算；跳过后在 ISL=1024 基准上 TTFT P95 **−2.70%**、吞吐 **+2.54%** → agent gateway 的短 prompt 预检/路由应区分 sparse vs dense 路径。
- **Vision window attention 的 sink 权重曾被静默丢弃**（vLLM #49815, **2026-07-26**）：MiMo-V2.5 24/28 个 window block 的 `attn.sinks` 从 checkpoint 加载后从未参与 `_forward_window_attn` → 多模态 agent 的 vision encoder 输出与官方实现不一致；用 softmax LSE 闭式修正 key-0 bias，无需重跑 flash kernel。

## 1) Agent工程化（high-signal）

- [fix(mcp): support per-invocation workflow factories（LlamaIndex PR #22466, updated **2026-07-26**）](https://github.com/run-llama/llama_index/pull/22466)
  - 摘要: `workflow_as_mcp` 原先 close over 单个 `Workflow` 实例，每次 MCP tool 调用复用同一对象，mutable state on `self` 跨 caller 泄漏。本 PR 新增可选 `workflow_factory`，每次 invocation 创建 fresh compatible workflow；无 factory 时保持旧行为。Fixes #22071；`llama-index-tools-mcp` 版本 0.4.8→0.4.9；含 factory isolation 回归测试（53 passed）。
  - 为什么重要(对我): **skill 落地**——MCP 是 Cursor/Harness skill 的标准暴露面；实例复用 = eval 串状态、prod 多用户污染。factory 模式应成为所有 workflow-as-tool 的默认模板。
  - 明日动作(<=20min): 写 **是/否**：你现有 MCP skill 是否每次调用新建 workflow 实例（是/否/未知）。

- [fix(langgraph): preserve message IDs and additional_kwargs in add_messages format="langchain-openai"（LangGraph PR #7273, updated **2026-07-26**）](https://github.com/langchain-ai/langgraph/pull/7273)
  - 摘要: `add_messages(format="langchain-openai")` 的 `_format_messages()` 在 OpenAI 格式 round-trip 时丢失 message ID（未传 `include_id=True`）和 custom `additional_kwargs`（UI widget、metadata 等）。Fix：传 `include_id=True` + 按 ID 保存/恢复 `additional_kwargs`。Fixes #7272；19 个 `test_messages_state.py` 测试通过。
  - 为什么重要(对我): **skill 落地 + 评测**——multi-turn agent state 若 ID/metadata 在 format 转换中丢失，eval replay 与 prod trace 无法对齐；OpenAI-compat 路径是主流 serving 接口。
  - 明日动作(<=20min): 写 **是/否**：你 graph state 是否用 `format="langchain-openai"` 做 message 序列化（是/否/未知）。

- [fix(ollama): stop deduping streamed tool calls by name+arguments（LlamaIndex PR #22465, updated **2026-07-26**）](https://github.com/run-llama/llama_index/pull/22465)
  - 摘要: `stream_chat()`/`astream_chat()` 用 `(name, arguments)` 做 content-based dedup，Ollama 客户端 pydantic 丢弃 wire-level `id`/`index` 后，同名同参的两次 tool call（如连续调两次无参工具）被合并为一次。非 streaming `chat()` 无此问题。Fix：去掉 dedup，无条件累积每个 chunk 的 tool call。Fixes #22464。
  - 为什么重要(对我): **评测**——tool-calling eval 若 streaming 路径丢 call，pass rate 与 function call 计数全失真；local Ollama 是低成本 eval 后端。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否在 streaming 路径测 duplicate same-name tool calls（是/否/未知）。

- [fix(anthropic): add usage metadata to non-streaming chat() and achat()（LlamaIndex PR #22463, updated **2026-07-26**）](https://github.com/run-llama/llama_index/pull/22463)
  - 摘要: Anthropic 集成的 sync `chat()`/`achat()` 此前不返回 usage metadata，caller 无法获 token 计数除非手动解析 `response.raw`。本 PR 在 `additional_kwargs["usage"]` 附加 `input_tokens`/`output_tokens`/`cache_creation_input_tokens`/`cache_read_input_tokens`，与 streaming 路径（#22311）对齐。含 4 个 mock 单测。
  - 为什么重要(对我): **token 优化 + 评测**——非 streaming batch eval 若缺 usage，cost attribution 和 cache hit 分析断裂；prompt cache 是长 context agent 降本核心杠杆。
  - 明日动作(<=20min): 写 **是/否**：你 batch eval 是否记录 `cache_read_input_tokens`（是/否/未知）。

- [[Core] Offload raw-prompt preprocessing to renderer thread pool in AsyncLLM（vLLM PR #49608, updated **2026-07-26**）](https://github.com/vllm-project/vllm/pull/49608)
  - 摘要: `AsyncLLM.add_request()` 在 asyncio event loop 上同步执行 `InputProcessor.process_inputs()`，raw prompt 路径含 tokenization + HF 多模态预处理（image resize/patchify），并发时串行阻塞 scheduler 前，GPU 空转。OpenAI server 路径已有 renderer thread pool offload，但 RL/scoring 框架直接调 `AsyncLLM.generate()` 会踩坑。Fix：`process_inputs_async` 复用 renderer 现有 thread pool（`renderer_num_workers` 限并发）。Fixes #49317。
  - 为什么重要(对我): **token 优化 + 评测**——multimodal agent eval 的吞吐瓶颈常在 CPU 预处理而非 GPU inference；RL rollout 框架是典型受害者。
  - 明日动作(<=20min): 写 **是/否**：你 multimodal eval 是否 profiling 过 CPU 预处理 vs GPU 占比（是/否/未知）。

- [fix(core): avoid inherited docstrings for tools（LangChain PR #38672, updated **2026-07-26**）](https://github.com/langchain-ai/langchain/pull/38672)
  - 摘要: Pydantic 类 tool 的 schema description 推断用 `inspect.getdoc` 会沿 MRO 继承父类 docstring，子类无自有 docstring 时 LLM 看到错误 tool 描述。Fix：只读直接定义的 docstring；无 docstring 的 Pydantic 类 tool 允许空 description（函数 tool 仍报错）。Fixes #32066。
  - 为什么重要(对我): **skill 落地**——@tool 装饰的 Pydantic 子类若描述串味，agent 会误选 tool；eval 的 tool selection 准确率被 schema 噪声污染。
  - 明日动作(<=20min): 写 **是/否**：你 skill tools 是否有 Pydantic 子类未写独立 docstring（是/否/未知）。

- [fix: use reentrant lock for pipeline mode to prevent SSL connection drops（LangGraph PR #8452, updated **2026-07-26**）](https://github.com/langchain-ai/langgraph/pull/8452)
  - 摘要: `AsyncPostgresSaver` + `pipeline=True` 时，`asyncio.Lock` 非可重入，pipeline flush/execute 在同 async context 内阻塞 → `psycopg.OperationalError: SSL connection has been closed unexpectedly`。Fix：检测 `AsyncPipeline` active 时跳过 lock（pipeline 自带同步），用 no-op context manager。Closes #5675。
  - 为什么重要(对我): **skill 落地**——LangGraph checkpoint 是长运行 agent 的持久化 backbone；SSL drop = silent state 丢失，eval 长跑后不可复现。
  - 明日动作(<=20min): 写 **是/否**：你 prod agent 是否用 `AsyncPostgresSaver(pipeline=True)`（是/否/未知）。

- [fix(runtime): preserve explicitly falsy context and store in merge（LangGraph PR #8450, updated **2026-07-26**）](https://github.com/langchain-ai/langgraph/pull/8450)
  - 摘要: `Runtime.merge()` 用 `or` 判断 incoming value，`{}`/`""`/`0` 等合法 falsy 值被错误 fallback 到 `self.*`。Fix：对 `context`/`store`/`execution_info`/`server_info`/`control` 改用 `is not None` 检查。Fixes #8406。
  - 为什么重要(对我): **skill 落地 + 评测**——空 dict context 是常见「显式清空」语义；merge 误判 = 子 graph 继承不该有的 store 状态，A/B eval 不可比。
  - 明日动作(<=20min): 写 **是/否**：你 subgraph 是否传空 `{}` 作为显式 context reset（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Attention] Skip sparse indexer scoring for dense short prefills（vLLM PR #48407, updated **2026-07-26**）](https://github.com/vllm-project/vllm/pull/48407)
  - 摘要: GLM-5.2 sparse attention 在 dense 短 prefill 前仍跑 indexer scoring，是纯死计算。本 PR 在 dense short prefill 路径跳过 indexer scoring。基准（ISL=1024, OSL=1, concurrency=1, 128 req, MTP=3）：吞吐 5.998→6.150 req/s（**+2.54%**），median TTFT 167.43→163.22 ms（**−2.52%**），P95 TTFT 168.76→164.21 ms（**−2.70%**）。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：sparse attention 的 indexer 是 prefill 额外开销；理解「何时 indexer 该跳过」= 长上下文 agent 的 TTFT 优化杠杆。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent prefill 是否区分 sparse/dense 路径（是/否/未知）。

- [[Bugfix][MiMo] Apply vision attention sinks in the window attention path（vLLM PR #49815, updated **2026-07-26**）](https://github.com/vllm-project/vllm/pull/49815)
  - 摘要: `MiMoVisionAttention` 仅在非 `fullatt_block_indexes` 的 window block 分配 `self.sinks`，但 `_forward_window_attn` 从未读取 → XiaomiMiMo/MiMo-V2.5 的 24/28 个 window block 的 sink 权重加载后被丢弃。Fix：用 softmax LSE 闭式修正 key-0 logit bias（`out' = (out + p0*(exp(s)-1)*v0)/(1+p0*(exp(s)-1))`），仅影响每个 sequence 前 `window_size+1` 个 query；`cu_seqlens` 在 device 上构建 index，无 host sync。
  - 为什么重要(对我): **Attention 机制**：attention sink 是 window attention 稳定长序列的关键技术；加载了不用 = vision encoder 输出系统性偏移，多模态 agent 的 grounding 质量受损。
  - 明日动作(<=20min): 写 **≤15 词**：你了解 attention sink 在 window attention 中的作用吗（是/否/未知）。

- [[Bugfix][KV Offload][P2P] Scope serve state to fetch rounds（vLLM PR #49877, updated **2026-07-26**）](https://github.com/vllm-project/vllm/pull/49877)
  - 摘要: P2P KV offload server 每 `kv_request_id` 仅一个 outbound slot，但 prefix 增长会触发多轮 lookup/fetch；round 重叠时完成一轮会 wipe 下一轮已 pin 的 supply → 下一 fetch 要么 stall 30s load timeout 要么触发 duplicate-fetch guard 终止 session。Fix：server 分离 current fetch round 与 not-yet-fetched supply；client 每 id 仅一个 in-flight FetchMsg；symmetric mode 加 per-request suffix 防 router 分配重复 id。Fixes #49820。
  - 为什么重要(对我): **Talk ≥3 turns + KV/Attention**：长上下文 multi-turn agent 的 prefix 逐轮增长是 P/D disaggregation 的核心场景；fetch round 状态机错误 = silent 30s stall，eval 长跑难复现。
  - 明日动作(<=20min): 写 **≤15 词**：你 disaggregated serving 是否测过多轮 prefix 增长（是/否/未知）。

- [[Perf] Split-reduction batch-invariant log_softmax for small-batch decode（vLLM PR #49367, updated **2026-07-26**）](https://github.com/vllm-project/vllm/pull/49367)
  - 摘要: batch-invariant `log_softmax` 每行一个 Triton program（`grid=(n_rows,)`），decode 时 `n_rows`=batch size < SM 数（~108 on A100）导致大部分 SM 空闲，每 program 串行扫 128k–152k vocab。该 kernel 在 `Sampler.compute_logprobs` 路径上，是 RL rollout 热路径。Fix：按 vocab 维度 split reduction——stats kernel `grid=(n_rows, n_splits)` + finalize kernel 合并；`n_splits` 仅依赖 vocab width，与 batch size 无关，保持 batch invariance。
  - 为什么重要(对我): **Attention 后处理 + token 优化**：logits→log_softmax 是 sampling 前最后一步；small-batch decode 的 vocab 并行化 = agent RL eval 吞吐瓶颈之一。
  - 明日动作(<=20min): 写 **≤15 词**：你 RL eval 是否 profiling 过 logprob 计算耗时（是/否/未知）。

- [fix: handle Inkling non-reasoning responses（vLLM PR #49876, updated **2026-07-26**）](https://github.com/vllm-project/vllm/pull/49876)
  - 摘要: `--reasoning-parser inkling` + `--tool-call-parser inkling` 时，`reasoning_effort=none`/`minimal` 的非 reasoning 输出以裸 text 开头，parser 停在 `MESSAGE_HEADER` 状态丢弃 text，reasoning adapter 反而 surfacing `<|end_message|>` 为 content。Fix：Inkling-only plain-text mode for no-tools + low reasoning effort；adapter context forwarding opt-in 仅 Inkling。989 parser 测试通过。
  - 为什么重要(对我): **Talk ≥3 turns**：reasoning parser 与 tool parser 的状态机交互是多模态/reasoning model serving 的常见坑；token 计数和 content 提取在错误 parser 状态下全失真。
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 是否同时启用 reasoning + tool parser（是/否/未知）。

## 3) 热门项目/可试个人项目

- [run-llama/llama_index — `workflow_as_mcp` per-invocation factory（PR #22466, updated **2026-07-26**）](https://github.com/run-llama/llama_index/pull/22466)
  - 摘要: MCP tool 暴露 workflow 时，用 `workflow_factory: Callable[[], Workflow]` 替代单例实例；README 文档化两种 lifecycle mode；`llama-index-tools-mcp` 0.4.9。
  - 为什么重要(对我): **skill 落地最小模板**——10 行 factory wrapper 即可让你的 agent workflow 通过 MCP 安全暴露，避免 eval 状态泄漏。
  - 明日动作(<=20min): 读 PR 中 `test_workflow_as_mcp.py` 的 factory isolation 测试；写 1 条你的 workflow 哪些 state 必须 per-invocation 隔离（≤10 词）。
  - 估计复现成本: **低**（读测试 + 加 factory 参数，≤30min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Harness Engineering for Self-Improvement（Lil'Log, Jul 2026）](https://lilianweng.github.io/posts/2026-07-04-harness/)
  - 机会信号: Lilian Weng 系统梳理 harness 设计模式（workflow loop、filesystem memory、sub-agent 并行）和优化路径（context engineering → workflow → harness code → optimizer code）。关键论点：**harness 的 observability 是自我改进瓶颈**——rollout 失败时需定位哪个组件负责，每次 edit 需 evidence-grounded（呼应 AHE、DGM、Hyperagents 等方向）。对你：agent eval 方法论可沉淀为「harness 可观测性 checklist」，有复用杠杆。
  - 风险信号: 综述性质，非产品级落地指南；self-improvement/RSI 叙事偏研究前沿，短期变现路径不清晰；与日常 deliverable 易脱节。
  - 我可执行的一步(<=30min): 写 **≤8 行**：你当前 1 个 agent workflow，按 Lil'Log 三模式（workflow loop / file memory / sub-agent）标出已有/缺失项。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 3516 Find Closest Person](https://leetcode.com/problems/find-closest-person/)（**Day 26, Jul 2026, Easy**；日历日切换，承接 0725 的 3515 Hard 系列）：数轴上两人同速走向第三人，比较距离判谁先到达。明日动作：O(1) 模板写 1 次；写 **≤15 词**：「比较两路径代价选优」与 agent tool routing 的类比。

- **IELTS（≤1 条）**：[IELTS Ready — British Council 官方免费备考（含 AI Mini Mock）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-ready)：50 分钟 Mini Mock + AI 估分。明日动作：完成 **Reading 1 节 Mini Mock**；写 **≤15 词** 错因类型（词汇/定位/推理 选 1）。

## 6) 噪音过滤记录

- **vLLM #49794 input_tokens preflight、#49741 owner-sharded PCP、AutoGen #7991 cancel hang**：`0725.md` 已收录 → 不重复。
- **deepagents #4893 reward-gated retry**：`0725.md` 已收录 → 不重复。
- **LangGraph #8437 AsyncThreadStream close、#7996 cache_policy opt-out**：`0725.md` 已收录 → 不重复。
- **LlamaIndex #22419 SlidingWindowRateLimiter**：`0722–0723.md` 已收录 → 不重复。
- **Anthropic Project Pilot / Drone-Bench（Jul 24）**：高价值但偏 red-team 物理世界 eval，非今日 focus（skill/评测/token）→ 待观察。
- **Simon Willison Opus 5 / Boris Cherny PI eval（Jul 24–25）**：模型发布 + 安全属性，非 07-26 新鲜工程信号 → 待观察。
- **LangChain #39050 model profiles refresh、LangGraph #8451 UUID rename、#6719 deps bump**：内部 tooling → 过滤。
- **LlamaIndex #22460 docs typo、#21653 deps bump**：纯文档/依赖 → 过滤。
- **vLLM #49862 A8 Marlin alignment、#48761 XPU yaml、#49853 speech CI**：低相关或 infra → 过滤。
- **AutoGen #7999 Jupyter UTF-8**：低信号 → 过滤。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-26** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Jul 26 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **614**（`0725.md` §7: 606 + 本日 §1: 8）
- Theory: **255**（`0725.md` §7: 250 + 本日 §2: 5）
- Career/Business: **105**（`0725.md` §7: 104 + 本日 §4: 1）
- LC: **87**（`0725.md` §7: 86 + 本日 §5: 1）
- IELTS: **87**（`0725.md` §7: 86 + 本日 §5: 1）
