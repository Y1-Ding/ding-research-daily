# Daily Research - 2026-06-13 (21:30)

## 0) 今日3条结论

- **Fable 5 / Mythos 5 全球下架是「监管召回」而非产品迭代**：**2026-06-12** 美国政府以出口管制令要求 Anthropic **立即切断所有用户**（含境外用户与外籍员工）对 **Fable 5 / Mythos 5** 的访问；Simon Willison **2026-06-12 18:59 PT** 实测 API 返回 404——对你做 **proactive coding agent** 意味着：**前沿 agent 模型可用性 = 政策风险**，eval 基线应 **多 provider 冗余**，且 **勿把单一「最强 agent 模型」绑进生产 SLA**。
- **同日两条「agent 安全边界」工程修复值得一起升级**：**langchain 1.3.9**（#38106）收紧 **FilesystemFileSearchMiddleware 路径逃逸** 与 **anthropic `allowed_prefixes` 段边界**；**langchain-openai 1.3.2**（#38128/#38129）修复 **Codex 流式 async token 路径**——路径 confinement 与 **OAuth 流式 header** 同属 **「不可信输入 → 工具执行」** 防线，应作为 **同一回归批次** 跑 smoke。
- **vLLM 0.23.0 把 serving 层推向「统一解析 + 默认 MRv2」**：**2026-06-12** 发布 **408 commits**；**reasoning + tool-call 统一 `Parser.parse()`**、**Llama/Mistral dense 默认 Model Runner V2**、**多 tier KV offload**——若你用 vLLM 自托管 agent backend，升级后应重点测 **structured output / tool streaming** 与 **prefix cache** 行为变化。

## 1) Agent工程化（high-signal）

- [fix(openai): avoid sync token reads in Codex streaming（#38128，merged **2026-06-13**）](https://github.com/langchain-ai/langchain/pull/38128)
  - 摘要: **`_ChatOpenAICodex._astream`** 改为在 **async token 路径** 构建请求头，避免先 async refresh、后在 payload 构造时 **sync 读 token**；保留 **`ChatGPT-Account-Id`** 与 **`originator`** header；同日发布 **`langchain-openai==1.3.2`**（#38130）。
  - 为什么重要(对我): 你若用 **LangChain + Codex** 做 **长时程流式 agent**，sync token 读会 **阻塞 event loop** 或导致 **竞态 401**——这是 **生产级 streaming harness** 的隐性 bug，比模型 IQ 更常导致 **间歇性断流**。
  - 明日动作(<=20min): 查当前 **`langchain-openai` 版本**；写 **是/否 ≥1.3.2**；若用 Codex streaming，记 **上次断流是否伴随 token refresh**（是/否/未观测）。

- [fix(openai): build Codex async headers off the event loop in `_agenerate`（#38129，merged **2026-06-13**）](https://github.com/langchain-ai/langchain/pull/38129)
  - 摘要: 与 #38128 配套，**`_agenerate`** 同样在 event loop 外构建 Codex async headers，消除 **非流式 Codex 调用** 的 sync token 路径。
  - 为什么重要(对我): agent **plan 步常走非流式、tool 步走流式**——两条路径需 **一致 auth 语义**；只修 `_astream` 会留下 **「plan 成功、execute 偶发失败」** 的 split-brain。
  - 明日动作(<=20min): 列你 agent **plan vs tool** 各用的 **stream 模式**（流式/非流式各 1 词）；写 **是/否** 两路径共用同一 OpenAI/Codex client 实例。

- [langchain==1.3.9 + fix: confine file-search results & tighten `allowed_prefixes`（#38106，merged **2026-06-12**）](https://github.com/langchain-ai/langchain/pull/38106)
  - 摘要: **`FilesystemFileSearchMiddleware`**：拒绝含 **`..`/leading `/`** 的 glob、**`followlinks=False`** 遍历、**ripgrep 结果 resolve 后重检 root 包含**；**`langchain-anthropic`**：`allowed_prefixes` 从 **字符串前缀** 改为 **路径段边界**（防 `/memories2` 误匹配 `/memories`）；捆绑 **`langchain-anthropic 1.4.6`**。
  - 为什么重要(对我): **RAG/agent file tool** 的最大事故面是 **symlink/glob 逃逸**——与 **prompt injection 诱导 LLM 读 `/etc/passwd`** 直接相关；这是你 **middleware 安全 checklist** 的可复制条目。
  - 明日动作(<=20min): 为你 **1 个** file-search tool 写 **≤20 词**：是否有 **root_path + resolve 后 containment 复检**（有/无/未查）。

- [fix(langgraph): merge `lc_versions` config metadata（#8052 → **langgraph 1.2.5**，merged **2026-06-12**）](https://github.com/langchain-ai/langgraph/pull/8052)
  - 摘要: **`merge_configs()` / `ensure_config()`** 对 **`metadata["lc_versions"]`** 做 **一层 package-version 累加**（非递归替换）；用户自定义 **`metadata["versions"]`** 仍 **last-writer-wins**；**`langchain-core` 下限升至 ≥1.4.7**；修复 stream event 测试中 **task path metadata 泄漏** 问题。
  - 为什么重要(对我): 生产 trace 里 **「哪版 langchain-core 跑了这条边」** 对 **eval 复现** 至关重要——bound config + invoke config 双贡献时旧逻辑会 **丢版本指纹**。
  - 明日动作(<=20min): 在 LangSmith/日志里搜 **1 条** run 是否含 **`lc_versions`** 字段（有/无）；若无，记 **langgraph 版本** 是否 **≥1.2.5**。

- [Building Box AI: Enterprise Content Platform Went AI-Native with Deep Agents（LangChain Blog，**2026-06-12**）](https://blog.langchain.dev/building-box-ai-how-an-enterprise-content-platform-went-ai-native-with-deep-agents/)
  - 摘要: Box **Global Agent** 用 **parent/child 皆为 Deep Agent**：简单任务 parent 直做，复杂任务 **运行时动态 spawn child**（非硬编码 search/QA/compose 三角）；child 以 **tool 形式** 暴露给 parent；**middleware** 并行做 **citation streaming、prompt caching、>170k token 自动摘要**；**全量 tool registry** 优于静态子集路由；新 agent **数周** 可 ship（旧架构 **~3 月**）。
  - 为什么重要(对我): 这是 **「规划深度 = 动态子 agent 拓扑」** 的企业级样板——你的 multi-agent 若仍 **硬编码子角色**，可对标 Box 的 **「简单不走 plan、复杂才 fan-out」** 延迟优化。
  - 明日动作(<=20min): 写 **≤25 词**：你的 agent 子任务分发是 **静态角色 / 运行时动态 spawn**（二选一）；列 **1 个** 适合「parent 直做」的简单 query 样例（≤12 词）。

- [How to Choose the Right Sandbox for Your Agent（LangChain Blog，**2026-06-12**）](https://blog.langchain.dev/how-to-choose-the-right-sandbox-for-your-agent/)
  - 摘要: 引用 Simon Willison **「lethal trifecta」**（敏感数据 + 不可信内容 + 外联能力）与 Meta **Rule of Two**；sandbox 需 **隔离 FS、限网、资源上限、可控复用、内核级隔离（microVM）**；**LangSmith Sandboxes** 用 **per-sandbox microVM + auth proxy 注入凭证（secrets 不进 sandbox）**。
  - 为什么重要(对我): 你若给 agent **code execution / bash tool**，此文是 **采购/自研 sandbox 的验收清单**——许多自称 sandbox 的方案 **缺 kernel isolation**。
  - 明日动作(<=20min): 为你 code-exec tool 勾 **5 项 checklist**（FS/网/资源/复用/内核）各 **有/无**；标最弱 **1 项**。

- [vLLM v0.23.0 Release（GitHub Release，**Published 2026-06-12**）](https://github.com/vllm-project/vllm/releases/tag/v0.23.0)
  - 摘要: **408 commits / 200 contributors**；**reasoning + tool-call 统一 `Parser.parse()`**（#44267）；**Llama/Mistral dense 默认 Model Runner V2**（#43458）；**多 tier KV cache offload（含 object-store 二级）**；**Anthropic Messages API structured output + effort**（#42396）；**DeepSeek-V4 / Gemma 4** 大量 backend 硬化。
  - 为什么重要(对我): 自托管 agent 的 **tool streaming / structured output** 行为高度依赖 **parser 与 MRv2**——升级 0.23.0 后应把 **tool_choice=required 流式** 与 **prefix cache hit rate** 纳入 eval。
  - 明日动作(<=20min): 写 **是/否** 使用 vLLM 作推理后端；若是，记当前版本与 **0.23.0** 差距（同版/落后/未用）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Statement on the US government directive to suspend Fable 5 and Mythos 5（Anthropic Blog，**2026-06-12**）](https://www.anthropic.com/news/fable-mythos-access)
  - 摘要: 政府 **2026-06-12 17:21 ET** 下达出口管制令；Anthropic 称依据为 **「窄域 non-universal jailbreak」**（让模型读 codebase 找漏洞），但 **GPT-5.5 等公开模型亦可**；Fable 采用 **defense-in-depth**（强 safeguard + 30 天数据留存 + 监控），**无 universal jailbreak** 被证实；Anthropic **不同意「窄域 jailbreak = 召回商用模型」** 标准但 **依法合规下架**。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) **agent 安全 eval** 应区分 **universal vs narrow jailbreak** 威胁模型？(2) **proactive agent**（如 Fable 自主探索）的 **监管敏感度** 是否高于 chat-only？(3) 你的 agent 是否记录 **「模型不可用时的 fallback 策略」**？
  - 明日动作(<=20min): 写 **≤30 词** 你 agent 的 **主模型 + fallback 模型**（各 1 个名或「无 fallback」）；标 **Fable 类 proactive 能力** 在你栈中是 **必需/可选/禁用**。

- [Statement on the US government directive…（Simon Willison Blog，**2026-06-13**）](https://simonwillison.net/2026/Jun/13/fable/)
  - 摘要: Simon 用脚本 **每分钟 ping `claude-fable-5`**；**2026-06-12 18:59 PT（attempt 37）** API 返回 **404 `not_found_error`**，提示改用 **Opus 4.8**；政府令 **5:21pm ET** 下达，**实际切断约 4 小时后**；Simon 质疑 **「窄域 jailbreak」证据** 与 **GPT-5.5 同等能力** 论点。
  - 为什么重要(对我): 这是 **「agent 能力回归测试」可观测性** 的极简模板——你可对 **任一关键模型 endpoint** 做 **cron healthcheck + 版本 pin 告警**，避免 **静默降级**。
  - 明日动作(<=20min): 写 **≤20 词** 你是否对 **主 LLM endpoint** 有 **定时可用性探测**（有/无 + 频率若有可能 1 词）。

- [Box Deep Agent：动态 child spawn vs 硬编码子 agent（LangChain Blog，**2026-06-12**）](https://blog.langchain.dev/building-box-ai-how-an-enterprise-content-platform-went-ai-native-with-deep-agents/)
  - 摘要: 早期 **search/QA/compose 三子 agent** 即使用例简单也 **强制走 plan → 延迟**；现 **parent 按意图分类**：简单搜索 parent 直调 BM25/vector，复杂合同风险分析才 **并行 spawn 检索/规则/综合 child**；child **共享全 tool registry**，由模型 **自选工具** 优于静态路由。
  - 为什么重要(对我): **Agent 规划原理**：规划开销应 **与任务复杂度成正比**（类似 **adaptive computation / early exit**）；eval 若一律测 **多步 plan**，会 **高估简单 query 延迟、低估复杂 query 质量**。
  - 明日动作(<=20min): 为你 agent 写 **≤25 词**：是否存在 **「跳过 planner 的 fast path」**（有/无 + 触发条件 1 个词若有可能）。

- [Lethal trifecta 与 sandbox 边界（LangChain Blog，**2026-06-12**）](https://blog.langchain.dev/how-to-choose-the-right-sandbox-for-your-agent/)
  - 摘要: 当 agent 同时满足 **(1) 敏感数据 (2) 不可信内容 (3) 外联** 时，prompt injection 可导致 **数据外泄**；Meta **Rule of Two** 建议三者 **最多同时满足两项**；sandbox **缩小 FS 暴露面与外联目标**，使 injection 风险 **可运营**。
  - 为什么重要(对我): 类比 **Attention**：sandbox 是对 agent **「可写/可读 memory 的稀疏 mask」**——不是消除 trifecta，而是 **把 attention 限制在可审计子空间**。
  - 明日动作(<=20min): 列你 agent **3 个 tool**；标各是否触及 **敏感数据 / 外联**（是/否各 1 词）；判断 trifecta **三全/缺 1/缺 2+**。

## 3) 热门项目/可试个人项目

- [vLLM 0.23.0 Rust frontend：`/generate` streaming + tool parsers（GitHub Release，**2026-06-12**）](https://github.com/vllm-project/vllm/releases/tag/v0.23.0)
  - 摘要: 实验性 **Rust frontend** 新增 **streaming `generate`**（#43779）、**dynamic LoRA**（#43778）、**InternLM2/hy_v3/Phi-4-mini/Gemma4 tool parsers**；与 Python 前端 **统一 parser 接口** 对齐。
  - 为什么重要(对我): 若你做 **自托管 agent API**，Rust 路径可能是 **更低延迟 tool streaming** 的试金石——适合 **个人 1 日 spike** 对比 Python 前端 TTFT。
  - 明日动作(<=20min): 读 release **Rust frontend** 小节 **1 次**；写 **是/否** 愿意用 **1 小时** 跑官方 docker + `/generate` smoke。
  - 估计复现成本: **中**（需 GPU + 新镜像；无训练）。

- [Simon Fable API 断供监测脚本（Simon Willison Blog，**2026-06-13**）](https://simonwillison.net/2026/Jun/13/fable/)
  - 摘要: 用 **`uv run llm -m claude-fable-5 hi`** 每分钟探测；日志含 **精确断供时间戳** 与 **404 error body**——零依赖 UI 的 **model deprecation 告警器**。
  - 为什么重要(对我): **≤30 行** 可复用到你的 **OpenAI/Anthropic/Google** 主 endpoint；与 LangSmith **eval 版本 pin** 互补。
  - 明日动作(<=20min): fork 思路写 **伪代码 5 行**（endpoint + interval + on_fail alert）；不必真部署。
  - 估计复现成本: **低**（curl/llm CLI；无 GPU）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Results from the first Anthropic Public Record（Anthropic Blog，**2026-06-12**）](https://www.anthropic.com/news/anthropic-public-record)
  - 机会信号: **52k 美国人调研（2025 Q4）**：**48%** 盼 AI 治病、**64%** 惧失业（跨党派一致）；**71%** 支持政府监管 AI、**仅 15%** 信任 AI 公司——**「合规 + 可问责 agent」** 是 **regulated industry（金融/医疗/公共部门）** 采购硬门槛；同日 **TCS 5 万员工用 Claude**、**DXC 多行业集成** 印证 **B2B 出海卖「治理型 agent」** 窗口。
  - 风险信号: **Fable 召回** 显示 **政府可强制下架 frontier agent 模型**——面向海外客户若主打 **「最强 proactive agent」**，需准备 **模型切换与合规叙事**；公众 **不信任 AI 公司** 使 **纯 API wrapper** 难溢价。
  - 我可执行的一步(<=30min): 用中文写 **≤40 词**：你的目标客群更像 **「怕失业的终端用户」还是「要合规的 B2B 采购」**（二选一 + 1 理由）；列 **1 个** landing 页副标题强调 **audit trail / human-in-the-loop**（≤15 词，不必上线）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 388. Longest Absolute File Path](https://leetcode.com/problems/longest-absolute-file-path/)：练 **栈模拟目录深度 + 路径拼接**——对照 **#38106 file-search root confinement** 与 Box **enterprise path 权限**。明日动作：提交后写 **≤15 词**：路径分隔用 **`\n` + `\t` 深度** 还是 **显式栈**（二选一）。

- **IELTS（≤1 条）**：[IELTS Test Format and Scoring](https://www.ielts.org/for-test-takers/test-format)：官方 **四科时长、题型、band 描述** 入口；与 **学术/培训类选型、机考流程** 连贯。明日动作：打开 **Speaking** 格式页，写 **≤15 词** Part 2 准备时间（**1 min / 2 min / 无** 三选一）。

## 6) 噪音过滤记录

- **`langchain==1.3.8` / structured output #38042 / streaming tool #35983**：`0612.md` **URL/语义去重** → 本日仅经 **1.3.9 升级链** 引用新修复。
- **`langgraph-cli 0.4.29` HTTPS（#8031）**：`0612.md` 已收录 → 丢弃。
- **SmithDB 倒排索引、Benchling Podcast、OpenAI 收购 Ona**：`0611–0612` 已深度覆盖 → 丢弃。
- **Anthropic TCS/DXC 合作稿**：与 Public Record **同日** 但 **偏 PR 重复** → 职业池仅保留 **Public Record 可核验数据**。
- **OpenAI Academy courses（Jun 12）**：fetch **timeout** → 不收录。
- **Lil'Log / DeepMind Blog**：无 **2026-06-13** 新文 → 丢弃。
- **X_WHITELIST / YOUTUBE_WHITELIST / Bilibili**：无法稳定抓取原帖 → 不收录。
- **llama_index / autogen / openai/evals**：无 **2026-06-13** 新鲜 release → 丢弃。
- **vLLM Jun 13 security commits（#45252/#45118）**：无独立 release note → 经 **0.23.0** 安全小节间接引用，不单列。

## 7) 本周累计计数

- Agent: **282**（`0612.md` §7: 275 + 本日 §1: 7）
- Theory: **114**（`0612.md` §7: 110 + 本日 §2: 4）
- Career/Business: **63**（`0612.md` §7: 62 + 本日 §4: 1）
- LC: **45**（`0612.md` §7: 44 + 本日 §5: 1）
- IELTS: **45**（`0612.md` §7: 44 + 本日 §5: 1）
