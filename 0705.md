# Daily Research - 2026-07-05 (21:30)

## 0) 今日3条结论

- **LangGraph `awrap_tool_call` 路径把 `GraphInterrupt` 当普通 tool error 吞掉（#8217 / #8218，updated 2026-07-05）**：HITL skill 在 tool 内 `interrupt()` 时，有 middleware 的 `ToolNode` 把可恢复控制流压成 `tool-error` 字符串 → L2 trajectory eval 与 prod 暂停/恢复语义分叉，需 wrapper/no-wrapper 对称 contract test。
- **新 Claude 模型对非原生 tool schema 更差（Armin 2026-07-04 / Pi PR #5434）**：Opus 4.8 / Sonnet 5 在第三方 harness（Pi）的 `edit` tool 上发明 `edits[]` 额外键；Pi 已放宽 inner schema `additionalProperties` → skill 落地要区分「schema 严格」与「handler 容错」，eval 需覆盖 invented-key 噪声。
- **Tool Attention 把 Attention 范式外推到 tool 层（arXiv 2604.21816）**：两阶段 lazy schema loading 在 120-tool 模拟中 per-turn tool token 47.3k→2.4k（-95%）→ MCP/skill 栈的 token 预算应优先做 protocol 层 gating，而非只扩 context window。

## 1) Agent工程化（high-signal）

- [GraphInterrupt Not Re-raised in awrap_tool_call Wrapper Path（LangGraph #8217 updated **2026-07-05**）](https://github.com/langchain-ai/langgraph/issues/8217)
  - 摘要: `ToolNode` 配 `awrap_tool_call` 时，tool 内 `interrupt()` 抛出的 `GraphInterrupt`（`GraphBubbleUp` 子类）被 wrapper 路径的 `except Exception` 捕获，经 `handle_tool_errors` 转成 `"Tool call failed (GraphInterrupt): ..."` 字符串；图不暂停、payload 丢失。直接执行路径有 `except GraphBubbleUp: raise` 守卫，wrapper 路径缺失。PR #8224 已备 fix，待 maintainer assign。
  - 为什么重要(对我): **skill 落地 + eval**——HITL / client-side delegation 是 skill stack 标配；middleware 把可恢复 interrupt 误分类为 terminal failure，L3 eval 出现「应暂停却继续」假阴性。
  - 明日动作(<=20min): 写 **是/否**：你 `ToolNode` 是否用 `awrap_tool_call` middleware（是/否/未知）。

- [interrupt() inside a tool is reported as a `tool-error` on the tools stream（LangGraph #8218 updated **2026-07-05**）](https://github.com/langchain-ai/langgraph/issues/8218)
  - 摘要: 与 #8217 同源但症状在 streaming 层：`tools` stream channel 把 `GraphInterrupt` 报为 `tool-error` event，`message=str(error)` 销毁结构化 `Interrupt` 对象；resume 协议无法重建。社区提出 wrapper/no-wrapper 对称 contract test 作为 CI 门禁。
  - 为什么重要(对我): **eval + skill**——`stream_mode="debug"` trajectory eval 依赖 tools channel 事件分类；pause 被标为 error 使 L2 step-level grading 与 prod 行为分叉。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否校验 interrupt 事件类型（是/否/仅 end-state）。

- [fix(sdk-py): encode stream scoped path params（LangGraph PR #8284 updated **2026-07-05**）](https://github.com/langchain-ai/langgraph/pull/8284)
  - 摘要: `AsyncPostgresSaver` 之外，`langgraph_sdk` 的 async/sync stream helper 对 `assistant_id` / `thread_id` path segment 未 URL-encode；含 traversal-like 字符的 ID 可导致 REST 路径解析错误。Fix：复用 `_quote_path_param`，加 regression test。Fixes #8222。
  - 为什么重要(对我): **skill 落地**——LangGraph Cloud / remote graph 的 thread ID 常含特殊字符；SDK 层路径编码 bug 使 collect→test 的 remote eval 与本地 invoke 分叉。
  - 明日动作(<=20min): 写 **是/否**：你是否用 LangGraph SDK remote stream（是/否/仅 local）。

- [fix(core): set additionalProperties=false in $defs/definitions for strict JSON schemas（LangChain PR #38671 updated **2026-07-05**）](https://github.com/langchain-ai/langchain/pull/38671)
  - 摘要: `convert_to_openai_function(..., strict=True)` 的递归 walker 只 descend `properties`/`items`/`anyOf`，不进入 `$defs`/`definitions`；嵌套 Pydantic model 的 inner object 缺 `additionalProperties: false` → OpenAI strict-mode HTTP 400。Fix：walker 进入 `$defs`/`definitions`。Fixes #38223。
  - 为什么重要(对我): **skill 落地 + eval**——skill 输出 schema 常嵌套 Pydantic；strict JSON schema 是 Responses API / structured output eval 的 L2 gate，$defs 漏设使 prod 400 而 mock 通过。
  - 明日动作(<=20min): 写 **是/否**：你 structured output 是否用 `strict=True`（是/否/仅 OpenAI）。

- [fix(core): refresh agent state prompt after tool updates（LlamaIndex PR #22249 updated **2026-07-05**）](https://github.com/run-llama/llama_index/pull/22249)
  - 摘要: `FunctionAgent` / `AgentWorkflow` 用 `initial_state` + `state_prompt` 时，首步格式化 state 后设 `formatted_input_with_state=True`；tool 变异 `ctx.store["state"]` 后下一步仍跳过 state 重格式化，LLM 收到过期 state。Fix：tool results 处理后 reset flag。Fixes #22248。
  - 为什么重要(对我): **skill 落地 + eval**——multi-step skill 的 state 在 tool 间传递是 collect→test 核心路径；stale state 使 L2 trajectory match 假阳性/假阴性。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否在每 tool 步后刷新 state prompt（是/否/未知）。

- [feat: add VerificationQueryEngine component（LlamaIndex PR #21302 updated **2026-07-05**）](https://github.com/run-llama/llama_index/pull/21302)
  - 摘要: 新增 `VerificationQueryEngine`：包装任意 `BaseQueryEngine`，draft response 生成后 secondary LLM 作 adversarial judge，严格对照 `source_nodes` 验证 claims；metadata 增 verification 结果，strict mode 可 block hallucinated response。Fixes #21213。
  - 为什么重要(对我): **eval**——Post-RAG verification 是 L3 guardrail 的可复用组件；adversarial judge 模式可直接嵌入 map_prompt_test 的 collect→test pipeline。
  - 明日动作(<=20min): 写 **≤15 词**：你 RAG eval 是否有 claim-level verification（有/无/仅 end-state）。

- [fix(agentchat): persist GraphFlow triggered_activation_groups across save/load_state（AutoGen PR #7916 merged **2026-07-04**）](https://github.com/microsoft/autogen/pull/7916)
  - 摘要: 循环 `GraphFlow`（如 `A→B→A`）pause 后 `save_state`→`load_state`，因未持久化 `_triggered_activation_groups`，resume 立即报 `"Digraph execution is complete"` 永不达 exit node。Fix：save/load 补全该 dict。Closes #7043。
  - 为什么重要(对我): **skill 落地 + eval**——multi-agent cyclic workflow 的 checkpoint/resume 是长会话 eval 基础；state 缺字段使 parallel eval run 不可恢复。
  - 明日动作(<=20min): 写 **是/否**：你 multi-agent graph 是否有 cyclic edge（是/否/未知）。

- [Better Models: Worse Tools（Simon Willison Blog → Armin 原文 **2026-07-04**）](https://simonwillison.net/2026/Jul/4/better-models-worse-tools/)
  - 摘要: Armin 报告 Pi 的 `edit` tool：Opus 4.8 / Sonnet 5 在 `edits[]` 内发明 `type`/`id`/`newText_variant` 等额外键，`additionalProperties: false` 导致硬拒绝；旧模型无此问题。假说：新模型 RL 针对 Claude Code 原生 edit schema 训练，第三方 harness 受损。Pi PR #5434 已放宽 inner schema 容错。
  - 为什么重要(对我): **skill 落地 + eval**——skill tool schema 设计与 model-specific RL prior 冲突；L2 tool-call grading 需区分「语义正确但 schema 噪声」vs「真失败」。
  - 明日动作(<=20min): 写 **≤15 词**：你 tool schema 对 nested object 是否 strict（strict/容错/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Tool Attention Is All You Need: Dynamic Tool Gating and Lazy Schema Loading（arXiv **2604.21816**）](https://arxiv.org/abs/2604.21816)
  - 摘要: 将「Attention Is All You Need」范式从 token self-attention 外推到 tool gated attention：Phase-1 保持 compact summary pool，Phase-2 仅 promote top-k gated tools 的 full JSON schema；结合 Intent-Schema Overlap (ISO) score + state-aware gating。120-tool 六-server 模拟：per-turn tool token 47.3k→2.4k（-95%），effective context utilization 24%→91%。代码：https://github.com/asadani/tool-attention
  - 为什么重要(对我): **Talk ≥3 turns**：(1) MCP Tax 是否是你 multi-skill agent 的 hidden token sink？(2) ISO gating 能否替代全量 schema 注入做 L1 latency gate？(3) Phase-1 summary pool 放 stable prefix 对 prompt cache 命中意味着什么？
  - 明日动作(<=20min): 写 **≤15 词**：你 skill 栈单 turn 注入多少 tool schema token（≤5k / 5–20k / >20k）。

- [Latent Reasoning with Normalizing Flows / NF-CoT（arXiv **2606.06447**）](https://arxiv.org/abs/2606.06447)
  - 摘要: NF-CoT 在 LLM backbone 内嵌 TARFlow-style normalizing flow 建模 continuous thoughts；continuous-thought 位置由 NF head 生成，text 位置由标准 LM head 生成，同一 causal stream 内交错。保留 left-to-right 生成、KV-cache 兼容、tractable likelihood；支持 latent space 直接 policy-gradient。代码生成 benchmark 上 pass rate 优于 explicit-CoT 且大幅降低 intermediate reasoning cost。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) latent thought 能否替代 ≥3 turn agent 的中间 CoT 降 token？(2) KV-cache 兼容 vs ALAR 的 latent block 哪个更适合 serving+eval？(3) exact likelihood 能否做 L2 per-step confidence grading？
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 是否区分 reasoning token vs action token（是/否/仅 outcome）。

- [[RFC]: Unifying Tool Calling via Region-Scoped Guided Decoding（vLLM #39848）](https://github.com/vllm-project/vllm/issues/39848)
  - 摘要: vLLM tool calling 架构 RFC：从 `generation→parsing→correction` 转向 `generation(with explicit structured regions)→extraction`。Region-scoped guided decoding 在 trigger token 后进入 constrained mode，grammar 约束 tool name/arguments，end token 后恢复 free-form；`tool_choice=required` 不再强制全局 JSON array。PR #36891 已在 Kimi K2 上验证 schema accuracy 75%→100%。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) decode-time constraint vs post-hoc parser 哪个更适合 agent serving eval 一致性？(2) boundary tracking 能否映射到你 L2 tool-call region grading？(3) 与 Tool Attention 的 protocol 层 gating 是互补还是替代？
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 层 tool call 是 parse 还是 constrain（parse/constrain/混合）。

## 3) 热门项目/可试个人项目

- [asadani/tool-attention — MCP Tax 消除中间件（GitHub，arXiv 2604.21816 配套实现）](https://github.com/asadani/tool-attention)
  - 摘要: drop-in middleware：`IntentRouter.route()` + 两阶段 lazy schema loader；Phase-1 summary pool 放 stable prefix（prompt-cache 友好），Phase-2 按 top-k 动态注入 full schema；支持 `precondition_check` 做 state-aware gating。参考实现测得 summary pool -98.6%、marginal schema cost -91.9%。
  - 为什么重要(对我): **token 优化杠杆**——可直接挂 MCP/skill 栈做 A/B；与 Cursor/Harness skill 的 tool 注入模式同构，适合 collect→test 的 per-turn token 门禁。
  - 明日动作(<=20min): `pip install` 或 clone README；写 **≤15 词** 你 MCP 部署有几个 server（0 / 1–3 / >3）。
  - 估计复现成本: **低**（纯 Python middleware，无 GPU；需自备 MCP catalog 做对比实验）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [sqlite-utils 4.0rc2, mostly written by Claude Fable (for about $149.25)（Simon Willison Blog **2026-07-05**）](https://simonwillison.net/2026/Jul/5/sqlite-utils-fable/)
  - 机会信号: **一人公司 agent 交付成本锚点**——37 prompts / 34 commits / 30 files，主会话 $141 + 4 个 review subagent ~$8；作者自省应更多用便宜 subagent。transaction 语义 bug（`delete_where` 静默丢数据）被 agent 在 release blocker review 中发现 → agent 可做「责任半径」内的质量门禁。
  - 风险信号: Fable 订阅 7 月 7 日后需付 full API cost；长会话 agent 若不用 subagent 模型分层，major release 成本不可预测（$149/次量级）。
  - 我可执行的一步(<=30min): 读 AgentsView 用法；写 **≤30 词** 你最近一次 agent 任务的模型/subagent 成本结构（有追踪/无追踪/仅直觉）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 122 Best Time to Buy and Sell Stock II](https://leetcode.com/problems/best-time-to-buy-and-sell-stock-ii/)：允许多次买卖，贪心累加所有上涨区间 `profit += max(0, prices[i] - prices[i-1])`。明日动作：手写 O(n) 一遍版（≤20min）；写 **≤15 词**：与 agent 多步决策「每步取局部最优」的类比。

- **IELTS（≤1 条）**：[IELTS Speaking — Describe a skill you learned（British Council 样题）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-practice-tests/speaking)：Part 2 用「学 Agent eval 方法论」脱敏练 2 分钟（what/when/how/why）。明日动作：录音 2 分钟自评结构（≤20min）；写 **≤15 词** 弱项（fluency/词汇/结构 选 1）。

## 6) 噪音过滤记录

- **LangGraph #8240 / PR #8270 / #8273 / #8272**：`0703.md` 已收录 → 不重复。
- **LangChain #38642 / LlamaIndex #22076 / vLLM #47555 / #47556**：`0703.md` 已收录 → 不重复。
- **FlowTracer（2606.10646）/ Agent-Radar（2605.30136）/ BOSCH（2604.05942）**：`0703.md` 已收录 → 不重复。
- **ALAR（2606.02871）**：`0626.md` 已收录 → 不重复。
- **LangChain PR #38670（Anthropic content block guard）**：opened 2026-07-05 但 16 分钟内因 `missing-issue-link` 自动关闭，非 merged fix → 仅作跟踪，不进主报告。
- **vLLM #47608（CLI human-readable int）/ #45022（Voxtral OOM）**：serving infra，与当前 focus 弱相关 → 丢弃。
- **AutoGen #7917/#7918（security ASI10）**：安全面有价值但与今日 focus（skill/eval/token/attention）间接 → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-05** 原帖 → 不收录。
- **@gefei55 职业内容**：第三方聚合无法验证 **2026-07-05** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv + Simon Willison 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **458**（`0703.md` §7: 450 + 本日 §1: 8）
- Theory: **188**（`0703.md` §7: 185 + 本日 §2: 3）
- Career/Business: **84**（`0703.md` §7: 83 + 本日 §4: 1）
- LC: **66**（`0703.md` §7: 65 + 本日 §5: 1）
- IELTS: **66**（`0703.md` §7: 65 + 本日 §5: 1）
