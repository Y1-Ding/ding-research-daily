# Daily Research - 2026-07-15 (21:30)

## 0) 今日3条结论

- **LangGraph #8339 堵住 subgraph 对未触碰 key 的 reducer 二次执行**：compiled subgraph 每次 invoke 回传 schema 全量 key，父图把「echo 但未写入」的 key 当真实 write → 共享 counter 在两 subgraph 循环中每轮 +2 而非 +1；multi-agent eval harness 应 assert reducer 调用次数与「实际写入 key」一致。
- **AutoGen #7958 暴露 token 截断后 orphan tool result 静默污染 trace**：`TokenLimitedChatCompletionContext` 按中点 pop 时，若 AssistantMessage 含 FunctionCall 被删而其 paired FunctionExecutionResultMessage 留在列表，旧逻辑只查 `messages[0]` → eval 出现「无 call 有 result」假 trace；长上下文 agent 的 collect 阶段应校验 call_id 配对完整性。
- **Long-Horizon-Terminal-Bench（arXiv 2607.08964）用 dense partial-credit 评长程 terminal agent**：46 任务平均 9.8M tokens / 239 episodes / 88.9min，最强 Grok 4.5 仅 28.3% pass@0.95——你的 L2/L3 eval 应借鉴「未完成也有中间分」而非 binary pass/fail。

## 1) Agent工程化（high-signal）

- [fix(langgraph): stop subgraph nodes from re-running reducers on untouched keys（LangGraph PR #8339 merged **2026-07-15**）](https://github.com/langchain-ai/langgraph/pull/8339)
  - 摘要: compiled subgraph invoke 后回传 output schema 全部 key，即使本 run 内无 node 写入；父图把这些 echo key 当 write → 自定义 reducer channel 对未产出值再跑 reducer。Issue #6290：两 subgraph 共享 counter、仅其一更新时，每 loop counter +2。
  - 为什么重要(对我): **skill 落地 + eval**——与 0714 #8335（write 侧 alias）同属「框架层 state 语义」族；multi-subgraph agent eval 若测共享 state 计数，需 assert reducer 仅在实际 write 时触发。
  - 明日动作(<=20min): 写 **是/否**：你 LangGraph 是否用 compiled subgraph 作 node 且共享带 reducer 的 state key（是/否/未知）。

- [fix(prebuilt): allow return_direct tools to run at remaining_steps=1（LangGraph PR #8343 merged **2026-07-15**）](https://github.com/langchain-ai/langgraph/pull/8343)
  - 摘要: `create_react_agent` 的 `_are_more_steps_needed` 在 `remaining_steps < 2 && has_tool_calls` 时一律 abort，误杀 `return_direct=True` 工具（仅需 1 step：tool execute → done）。Fix：区分 direct vs 需二次 LLM 的 tool call。
  - 为什么重要(对我): **skill 落地 + eval**——`return_direct` 是常见 skill 模式（查 DB 直接返回答案）；step budget=1 时 eval 假阴性「need more steps」；harness 应单列 direct-tool scenario。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否有 `return_direct` tool 且 `remaining_steps` 常设 ≤2（是/否）。

- [core: prevent docstring inheritance from parent BaseModel classes（LangChain PR #38857 merged **2026-07-15**）](https://github.com/langchain-ai/langchain/pull/38857)
  - 摘要: `@tool` 装饰的 Pydantic 子类若无自有 docstring，`inspect.getdoc()` 沿 MRO 继承父类 docstring → `ChildTool.description` 错误显示为 `"Parent Tool"`。Fix：尊重 Pydantic 对子类设 `__doc__ = None` 的语义。
  - 为什么重要(对我): **skill 落地**——skill/tool 描述是 model 选 tool 的核心信号；继承污染 = 错误 routing + eval 中 tool selection 假阳性/阴性；skill 模板应显式写 description 或测 `ChildTool.description != Parent.description`。
  - 明日动作(<=20min): 写 **是/否**：你 `@tool` 类是否继承带 docstring 的 BaseModel 父类（是/否）。

- [fix(tools-mcp): resolve draft-07 "definitions" refs in create_model_from_json_schema（LlamaIndex PR #22364 opened **2026-07-15**）](https://github.com/run-llama/llama_index/pull/22364)
  - 摘要: `McpToolSpec.create_model_from_json_schema` 只认 draft 2020-12 的 `$defs` 和 `#/$defs/X` 指针；draft-07（`zod-to-json-schema` 等 TypeScript MCP server 常用）用 `"definitions"` + `#/definitions/X` → required object 参数静默变 `"type": "null"`，tool args 全破。
  - 为什么重要(对我): **skill 落地**——MCP skill 接入 TS server 时 schema 版本不匹配 = 线上 tool 全 null；collect 阶段应对每个 MCP tool 做 args schema smoke test（非 null 类型断言）。
  - 明日动作(<=20min): 写 **是/否**：你 MCP tool 来源是否含 TypeScript/zod server（是/否/未知）。

- [fix: remove orphaned FunctionExecutionResultMessages after truncation（AutoGen PR #7958 opened **2026-07-15**）](https://github.com/microsoft/autogen/pull/7958)
  - 摘要: `TokenLimitedChatCompletionContext.get_messages()` 按 `len//2` 反复 pop 至 token budget；旧逻辑仅检查 `messages[0]` 是否为 orphan result。若含 FunctionCall 的 AssistantMessage 被删而 paired FunctionExecutionResultMessage 留列表 → tool result 无 matching call。
  - 为什么重要(对我): **token 优化 + eval**——长对话 agent 截断后 trace 语义断裂；L2 eval 若数 tool call 会误计；harness 应 post-truncation assert 每个 result 的 call_id 有对应 AssistantMessage FunctionCall。
  - 明日动作(<=20min): 写 **是/否**：你 agent context 是否做 token 截断且保留 tool message（是/否/未知）。

- [[KV Offloading] Add per-request lookup_scope to skip secondary tier lookups（vLLM PR #48123 opened **2026-07-15**）](https://github.com/vllm-project/vllm/pull/48123)
  - 摘要: `kv_transfer_params` 新增 `lookup_scope`：`"primary"` 跳过 secondary tier lookup（无 promotion），默认 `"all"` 保持原行为；3 行 short-circuit 于 `TieringOffloadingManager.lookup`。
  - 为什么重要(对我): **token 优化**——multi-tenant agent 若仅需 primary cache hit、不需跨 tier promotion，可避免 secondary lookup 延迟与错误 hit；与 0714 #48566（cache_salt 隔离）互补——一个管租户，一个管 tier 策略。
  - 明日动作(<=20min): 写 **是/否**：你 vLLM 部署是否用 multi-tier KV offload（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Long-Horizon-Terminal-Bench: Testing the Limits of Agents on Long-Horizon Terminal Tasks with Dense Reward-Based Grading（arXiv **2607.08964**, submitted **2026-07-13**）](https://arxiv.org/abs/2607.08964)
  - 摘要: 46 个长程 terminal 任务（9 类：交互游戏、实验复现、SWE、科学计算等），每任务细分子任务 + dense intermediate reward；平均 9.8M tokens / 239 episodes / 88.9min；Grok 4.5 最强仅 28.3%@0.95 partial、19.6%@1.0 full pass，均值 6.4% / 3.2%。
  - 为什么重要(对我): **Talk ≥3 turns + Agent 原理**：(1) ≥3 turn agent eval 应报告 partial progress 而非仅 final bit；(2) token 消耗是长程能力的一等指标（与 0714 KV offload focus 直连）；(3) dense grader 可映射你的 L2 过程分（子目标完成度）。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否有 partial-credit 维度（有/无/仅 L1）。

- [ResonatorLM: Causal Resonant Field Mixing for Efficient Long-Context Language Modeling（arXiv **2607.05583**, v2 **2026-07**）](https://arxiv.org/abs/2607.05583)
  - 摘要: 用阻尼谐振子因果混合替代 self-attention dot-product：token 序列视为 1D 驱动场，per-head damped resonator 做 causal mixing；6M 匹配规模下 32K decode 6.47× speedup、WikiText 61.31% vs transformer 55.32%；训练/prefill 走 parallel conv，decode 走 fixed-size recurrent state。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：(1) attention 非唯一长上下文算子——resonator 用物理阻尼替代 softmax 几何；(2) agent backbone 选型若考虑 linear/subquadratic，应对比 effective context vs KV 生命周期；(3) 与 0713 linear attention 论文（frozen-backbone 线性化）不同轴——此为架构替换非 post-hoc 近似。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent serving 是否评估过 non-attention backbone（是/否/仅读论文）。

- [Harness Engineering for Self-Improvement（Lil'Log, **2026-07-04**）](https://lilianweng.github.io/posts/2026-07-04-harness/)
  - 摘要: 综述 harness 自进化：Lin et al. (2026) 解耦 **harness-updating**（写有用 harness 编辑）vs **harness-benefit**（利用更新后 harness 解题）；Qwen3.5-9B 与 Claude Opus 4.6 的 updating 能力相近；AHE 强调 observability——rollout 失败需定位责任组件；Self-Harness / DGM / Hyperagents 等让 agent 编辑自身 harness 代码。
  - 为什么重要(对我): **Talk ≥3 turns + Agent 原理**：(1) skill 落地 = harness 工程，非仅 prompt；(2) eval 应分层测 updating vs benefit——小模型可写 skill 但未必会 invoke；(3) 与 map_prompt_test collect→test 循环同构：trace → 定位断点 → harness 编辑。
  - 明日动作(<=20min): 写 **≤15 词**：你 skill 迭代是否记录「哪层 harness 改了什么」（有/无）。

## 3) 热门项目/可试个人项目

- [Add external governance checkpoint sample（AutoGen PR #7960 opened **2026-07-15**）](https://github.com/microsoft/autogen/pull/7960)
  - 摘要: AgentChat 样例：tool call 前经外部 governance checkpoint（action envelope + hash → mock 本地或 `EXTERNAL_GOVERNANCE_URL` verdict）；仅 verdict + 本地 policy 允许时执行高风险 export。默认 `ReplayChatCompletionClient`，无需 live API key。
  - 为什么重要(对我): **skill 落地杠杆**——可 10 分钟内跑通「tool 前 gate」模式，映射你垂域 agent 的 approval/freeze 逻辑；与 #7962 structured authority denial JSON 互补。
  - 明日动作(<=20min): clone AutoGen repo；跑 external governance sample；写 **≤15 词** 你最该 gate 的 1 个高风险 tool。
  - 估计复现成本: **低**（无 API key + ReplayChatCompletionClient）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [How to manage AI investments in the agentic era（OpenAI Blog, **2026-07-14**）](https://openai.com/index/managing-ai-investments-in-agentic-era/)
  - 机会信号: OpenAI 提出 **cost per accepted outcome**（非 token 单价）——engineering 场景是「通过 review 的 tested change」；强调 eval 反映真实任务 + 定义 good enough 再测全成本（attempts、latency、human review）；共享 eval/observability/model routing 应中央资助 → 出海 signature 可打在「垂域 agent ROI 度量 + 可复用 eval harness」。
  - 风险信号: 无 usage 可见性则账单难解读；frontier model 滥用于可用小模型任务；governance 滞后于 Computer Use / connector 扩展。
  - 我可执行的一步(<=30min): 选 1 条 priority workflow；写 **accepted outcome** 定义 + 当前 cost 估算模板（≤5 行）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 210 Course Schedule II](https://leetcode.com/problems/course-schedule-ii/)：在 #207 判环基础上返回拓扑序；Kahn BFS 出队顺序即答案，节点数 < numCourses 返回 `[]`。明日动作：先写 #207 核心 15 行再扩展返回 list（≤25min）；写 **≤15 词**：agent tool 依赖调度与拓扑序的类比。

- **IELTS（≤1 条）**：[IELTS test delivery updates for 2026（IDP IELTS, **2026-03-05**）](https://ielts.idp.com/about/news-and-articles/article-updates-to-ielts-test-delivery)：mid-2026 起全球停纯纸笔，全面机考；选定市场新增 **Writing on Paper** 混合模式（L/R 机考、Writing 手写、Speaking 面对面）；One Skill Retake 须与原 delivery mode 一致。明日动作：确认你目标市场是否开放 Writing on Paper；写 **≤15 词** 你更适机写还是手写（选 1）。

## 6) 噪音过滤记录

- **LangGraph #8335 copy-on-write、#48596 KV last block offload、#48566 cache_salt**：`0714.md` 已收录 → 不重复。
- **vLLM #48295 llama3_json/jamba/ernie45 单 delta tool parser**：`0711.md` 已作为 #48348 同族提及 → 不重复。
- **arXiv 2607.06621 Fingerprint / 2607.04319 Legible / 2607.07706 linearization / 2607.11881 Metacognition**：`0708–0714.md` 已收录 → 不重复。
- **AgentLens 2607.06624、OpenAI SWE-Bench Pro audit**：`0711–0712.md` 已收录 → 不重复。
- **LangGraph #8342 typo、#8341 InMemoryStore created_at、#8344 get_config async guard**：#8344 偏 Python<3.11 infra；#8341 store 语义弱于今日 subgraph/reducer focus → 丢弃或不单列。
- **LangChain #38856 model profile refresh**：数据 chore → 丢弃。
- **LlamaIndex #22363 SelectionOutputParser 多选丢失**：与 Router 相关但弱于 MCP schema focus → 记入噪音（可下周跟进）。
- **AutoGen #7959 asyncio.gather 并行 tool、#7961 docs**：perf/docs → 丢弃。
- **vLLM #48736 multimodal attention recompile、#47677 XPU speculative**：kernel/infra → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-15** 原帖 URL → 不收录。
- **@gefei55 / @ianneo_ai 职业帖**：无法验证当日原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv + Lil'Log + OpenAI 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **527**（`0714.md` §7: 521 + 本日 §1: 6）
- Theory: **218**（`0714.md` §7: 215 + 本日 §2: 3）
- Career/Business: **94**（`0714.md` §7: 93 + 本日 §4: 1）
- LC: **76**（`0714.md` §7: 75 + 本日 §5: 1）
- IELTS: **76**（`0714.md` §7: 75 + 本日 §5: 1）
