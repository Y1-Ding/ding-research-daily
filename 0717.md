# Daily Research - 2026-07-17 (21:30)

## 0) 今日3条结论

- **AgentAbstain 证明「会做事」≠「知道何时不做」**：17 个 frontier 模型、4 种 harness 下最佳 paired accuracy 仅 59.5%，且 abstention 能力与任务解题能力弱相关——你的 L2/L3 eval 必须加入 paired should-act / should-abstain 与 commit-level trace 检查，不能只盯最终成功。
- **高并发下 spec decode 的瓶颈从 draft 变成 verify**：D-cut 在 batched serving 中按置信度跨请求裁剪 verify 深度，dense 配置下把 speedup 从 1.26× 拉到 1.65×，MoE 最高 3.0×——agent serving 的 token 优化要按 batch size 分档测，而非只看单请求 MAT。
- **T²MLR 把中间层表征跨 token 复用**：仅 20% 中间层做 temporal recurrence 往往优于全层 loop，且可 retrofit 1.7B 预训练模型（GSM8K 35.8→39.9）——≥3 turn agent 的中间态持久化，可能比单纯加 KV cache 更贴近「推理链不断裂」。

## 1) Agent工程化（high-signal）

- [fix(langgraph): deep copy channel_values in copy_checkpoint to prevent mutation cross-contamination（LangGraph PR #7006, updated **2026-07-17**）](https://github.com/langchain-ai/langgraph/pull/7006)
  - 摘要: `copy_checkpoint()` 对 `channel_values` 仅 `dict.copy()` 浅拷贝；`__pregel_tasks` 等嵌套可变对象在 background checkpoint save 与主 loop 间共享引用，in-place append/clear 会同时污染已保存快照与 live checkpoint。Fix：`copy.deepcopy(channel_values)` 全隔离。
  - 为什么重要(对我): **评测 harness 可靠性**——multi-turn agent eval 若异步落盘 checkpoint 供回放/断点续跑，浅拷贝 bug = trace 与 state 分叉、L2 断言假失败；harness 应在 teardown 对比 live vs saved checkpoint 哈希。
  - 明日动作(<=20min): 写 **是/否**：你 LangGraph eval 是否依赖 `copy_checkpoint` 做后台持久化（是/否/未知）。

- [fix(checkpoint): replay migrated delta writes through a plain seed（LangGraph PR #7957, updated **2026-07-17**）](https://github.com/langchain-ai/langgraph/pull/7957)
  - 摘要: `InMemorySaver.get_delta_channel_history` 在 migration-boundary 的 plain（pre-delta）seed 上错误跳过 pending writes，导致 post-migration 写入 reload 后消失、但 live invoke 仍可见。Fix：移除「非 `_DeltaSnapshot` seed 则跳过 writes」守卫，与 `BaseCheckpointSaver` 参考路径对齐；含两条回归测试。
  - 为什么重要(对我): **skill 落地 + eval**——checkpoint 格式升级后，旧 harness 读新 trace 会「幽灵丢步」；版本迁移应是 eval 门禁项，而非上线后才发现 pass rate 漂移。
  - 明日动作(<=20min): 写 **是/否**：你 agent state 是否经历过 checkpoint schema 迁移（是/否/未知）。

- [fix(langchain): respect middleware `tool_choice` override with structured output tools（LangChain PR #38848, closed **2026-07-17**）](https://github.com/langchain-ai/langchain/pull/38848)
  - 摘要: `create_agent` + `ToolStrategy`/`response_format` 时，`_get_bound_model` 强制 `tool_choice="any"`，覆盖 middleware 经 `request.override(tool_choice=...)` 设置的值；导致 `ModelCallLimitMiddleware(exit_behavior="end")` 等无法在最后一轮定向 structured-output tool，run 无法干净结束并填充 `structured_response`。
  - 为什么重要(对我): **skill 落地**——structured output + middleware 是常见 skill 栈；框架层覆盖 `tool_choice` = 线上「有响应但 schema 空」；skill 模板应记录 middleware 与 `tool_choice` 的优先级契约。
  - 明日动作(<=20min): 写 **是/否**：你是否用 structured output + exit middleware 控制 agent 终止（是/否）。

- [fix(langchain): ModelRetryMiddleware propagates non-retryable exceptions（LangChain PR #38894, closed **2026-07-17**）](https://github.com/langchain-ai/langchain/pull/38894)
  - 摘要: `ModelRetryMiddleware` 对不匹配 `retry_on` 的异常走 `_handle_failure` + 默认 `on_failure="continue"`，伪造 error `AIMessage` 而非上抛；`ToolRetryMiddleware` 已在 #38845 修复同类问题，model 侧遗漏。Fix：与 #38845 对齐，bare `raise`。
  - 为什么重要(对我): **评测 + skill 落地**——eval 中 auth/validation 类不可重试错误被吞掉 = 过程分误判为「模型回复了错误」而非「应 fail fast」；collect 层应对 non-retryable 异常分类统计。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否区分 retryable vs non-retryable model 错误（是/否/未知）。

- [[Bugfix][Tool Parser] Preserve whitespace in parameter values（vLLM PR #48846, updated **2026-07-17**）](https://github.com/vllm-project/vllm/pull/48846)
  - 摘要: MiniMax M2 / Qwen3 / MiniCPM5 XML 等 parser 对参数值 `str.strip()`，删除首尾空白；coding agent 的 `old_string` 若含缩进/尾换行会被篡改，client 端 exact match 失败，多轮对话逐轮复利失真。Fix：保留合法首尾空白。
  - 为什么重要(对我): **skill 落地 + token 优化**——tool call 参数是 agent 与环境的契约；serving 层静默改字符串 = L2 tool 断言系统性假阴性；eval 应含「含缩进/换行的 edit tool」gold case。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否有依赖 exact whitespace 的 edit/search-replace tool（是/否）。

- [[Bugfix][Tool Parser] Stream tool calls that arrive whole in a single delta（vLLM PR #48295, updated **2026-07-17**）](https://github.com/vllm-project/vllm/pull/48295)
  - 摘要: 当整条 tool-call 在单个 stream delta 到达（`stream_interval>1`、async scheduling、短消息）时，`llama3_json`/`jamba` 记录状态却返回 `None` 不 emit；`ernie45` 只 emit 第一个 `<tool_call>` block。与 #47907 同类 bug，非流式路径正常。
  - 为什么重要(对我): **skill 落地**——agent serving 常调大 `stream_interval` 降 token 开销；此 bug = 客户端收到空 tool call；harness 应测「单 delta 完整 JSON tool」与「多 delta 增量」两条路径。
  - 明日动作(<=20min): 写 **是/否**：你 vLLM 部署是否用 `stream_interval>1` 或 async scheduling（是/否/未知）。

- [AgentAbstain: Do LLM Agents Know When Not to Act?（arXiv **2607.10059**）](https://arxiv.org/abs/2607.10059)
  - 摘要: 首个 **agentic abstention** 系统评测：263 paired tasks × 42 MCP sandbox，8 类 abstention 场景（pre-execution + runtime）；每对含 should-act / should-abstain 受控扰动。最佳 Gemini 3.1 Pro 仅 **59.5% paired accuracy**；常见 **post-hoc abstention**（先执行不可逆 commit 再口头拒绝）。配套自动化合成管线 AbstainGen；开源 https://agentabstain.github.io 。
  - 为什么重要(对我): **评测**——与 0716 AgentCompass「怎么测」互补：AgentAbstain 定义「何时不应 act」的 paired 指标 + commit check；你的 gold 集应至少覆盖 1 条 irreversible-tool paired case。
  - 明日动作(<=20min): 从 8 类 abstention 场景中选 1 类；写 1 条你域内的 should-act / should-abstain 配对草图（≤5 行）。

- [LLM Evaluators are Biased across Languages（arXiv **2607.14480**, announced **2026-07-17**）](https://arxiv.org/abs/2607.14480)
  - 摘要: 23 种语言上语义相同的 instruction-response pair，multilingual LLM-as-Judge / reward model 仍给出显著不同分数；高 pairwise accuracy **不蕴含**跨语言评分中立。揭示 judge 语言选择与训练语料偏置对 agent eval 的系统性影响。
  - 为什么重要(对我): **评测**——若用 LLM judge 做 L2/L3，中文 prompt + 英文 judge（或反之）可能引入语言维度噪声；eval 报告应固定 judge 语言并做跨语言对照子集。
  - 明日动作(<=20min): 写 **是/否**：你当前 judge 与 agent 是否同语言（是/否/混用）。

## 2) 原理深化（Attention/Transformer/Agent）

- [T²MLR: Transformer with Temporal Middle-Layer Recurrence（arXiv **2607.15178**, announced **2026-07-17**）](https://arxiv.org/abs/2607.15178)
  - 摘要: 将**上一 token 的中间层表征**经 gated pathway 注入**当前 token 更早层**，使 latent reasoning 跨解码步持续，避免每步把高维状态压回 one-hot token 空间。仅 **20% 中间层** recurrence 常优于全层 loop；可 retrofit SmolLM2-1.7B（GSM8K 35.8→39.9，MATH500 12.8→18.0），推理开销约 +8%。
  - 为什么重要(对我): **Talk ≥3 turns + Transformer**：(1) ≥3 turn agent 的「中间推理态」不必只靠显式 CoT token；(2) 中间层 recurrence 与 Attention/KV 正交——选型时可问「缺的是跨步 latent 还是跨步 context」；(3) retrofit 路径降低实验成本。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 失败更像「中间步丢目标」还是「最后一步算错」（选 1）。

- [[2/N][Attention] Enable masked MHA for sparse MLA prefills（vLLM PR #48770, updated **2026-07-17**）](https://github.com/vllm-project/vllm/pull/48770)
  - 摘要: 短序列 prefill 下，masked MHA 模拟 DSA 比 sparse MQA 更快（DeepSeek V3.2 论文提及）；在 #47327「seq_len < topk 走 dense MHA」基础上，实现完整 masked MHA pathway 扩展该优化。含 DP/TP 多配置 benchmark 图。
  - 为什么重要(对我): **Attention + token 优化**：sparse attention 在**短上下文**与**长上下文**应走不同 kernel path；agent 多轮对话常混合短 prefill + 长 KV——serving eval 应按 prompt 长度分桶测 latency，而非只看平均。
  - 明日动作(<=20min): 写 **是/否**：你是否按 prompt 长度分桶统计 agent serving latency（是/否）。

- [D-cut: Adaptive Verification Depth Pruning for Batched Speculative Decoding（arXiv **2607.14647**, announced **2026-07-17**）](https://arxiv.org/abs/2607.14647)
  - 摘要: 高并发 batched spec decode 中，长 draft 大量 token 被拒却仍参与 verify，DFlash 在 bs=64 可低于 AR baseline。D-cut 按 drafter 置信度**跨请求**裁剪 verify 深度，并结合启动时 profile 的 runtime cost model 自适应选深。dense 上 speedup 1.26×→**1.65×**；MoE 最高 **3.0×** over AR；training-free、分布无损。
  - 为什么重要(对我): **Talk ≥3 turns + token 优化**：(1) agent 在线 serving 的 token 成本由 **batch concurrency** 主导，单请求 bench 会高估 spec decode 收益；(2) verify 阶段与 attention 同属「一次 forward 处理多 token」——理解其 compute-bound 转折有助于定 KV/草稿策略；(3) 与 0716 KV dtype 解耦同属「按部署条件调优」族。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 部署的典型并发 batch size 区间（若未知写「未知」）。

## 3) 热门项目/可试个人项目

- [SEED: Self-Evolving On-Policy Distillation for Agentic RL（arXiv **2607.14777** / GitHub **jinyangwu/SEED**）](https://github.com/jinyangwu/SEED)
  - 摘要: 将 on-policy 轨迹转为 hindsight skills，再用 skill-augmented vs ordinary context 的 on-policy distillation 信号与 outcome RL 联合优化；缓解长程 agent RL 的 sparse reward / trajectory neglect。文本与视觉 agent 任务上提升性能与 sample efficiency。
  - 为什么重要(对我): **训练侧杠杆**——若你做 agent 微调，可把现有失败 trace 自动提炼为「应避免/应遵循」skill 文本，对比纯 outcome RL 的 step-level 学习曲线。
  - 明日动作(<=20min): 读 README 的 skill 格式；写 **≤15 词** 你域内 1 条可写成 hindsight skill 的失败模式。
  - 估计复现成本: **中**（需 agent 环境 + RL 栈）。

- [DeepStress: Stress-Testing Deep Search Agents（arXiv **2607.13920**, submitted **2026-07-15**）](https://arxiv.org/abs/2607.13920)
  - 摘要: 用可控合成环境替换 retrieval 模块，沿 **trustworthiness / relevance / factuality** 三轴调节劣质证据频率；在 HotpotQA、BrowseCompPlus 上显示 search agent 对不可靠信息处理能力差异显著，并提出更好刻画 parametric vs retrieved knowledge 冲突的指标。
  - 为什么重要(对我): **评测杠杆**——RAG agent 的「干净 bench 高分」不等于「脏证据下稳健」；可借鉴框架给现有 collect 集加 1 维 evidence corruption knob。
  - 明日动作(<=20min): 写 1 条你 RAG pipeline 的「低质证据」注入方式（≤10 行）。
  - 估计复现成本: **中**（需接 search agent + 合成 retriever）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [How Anthropic runs large-scale code migrations with Claude Code（Anthropic Blog, **2026-07-16**）](https://claude.com/blog/ai-code-migration)
  - 机会信号: Anthropic 公开 **六步大规模代码迁移** playbook——Bun Zig→Rust 百万行两周内 CI 全绿（merge 后 19 个回归已修）；Mike Krieger Python→TS 16.5 万行周末完成，含 **adversarial review 三轮 + 输出 parity diff**。证明「AI agent 代码迁移」可产品化为咨询/工具链 signature（出海企业 modernization 刚需），比通用 chat 更易定价。
  - 风险信号: 依赖 Claude Fable/Opus 级模型与成熟 agent loop；迁移后回归仍会发生；客户环境难复制 Anthropic 内部 gate 密度。
  - 我可执行的一步(<=30min): 选你熟悉的一个 500–2000 行模块；写「迁移前必须通过的 3 条 parity 检查」清单（输入/输出/副作用各 1 条）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 3312 Sorted GCD Pair Queries](https://leetcode.com/problems/sorted-gcd-pair-queries/)（**Day 17, Jul 2026, Hard**）：给定数组，回答多个查询——第 k 小的数对 GCD 值。数论 + 枚举/筛法/二分。明日动作：先 brute-force 数对 GCD 验证小数据，再查「GCD 频次前缀和」优化思路（≤25min）；写 **≤15 词**：agent 工具调用排序与「第 k 小统计」的类比。

- **IELTS（≤1 条）**：[IELTS: Updates to test delivery（官方, **2026-03-05**）](https://ielts.org/news-and-insights/updates-to-ielts-test-delivery)：2026 年中起全球逐步取消纸笔，全面机考；选定市场可选 **Writing on Paper**（L/R 机考 + Writing 手写），分数可比性已做研究验证；UKVI 仅全数字格式。明日动作：确认你目标考区时间表；写 **≤15 词** 你需否 Writing on Paper 选项（是/否/待查）。

## 6) 噪音过滤记录

- **AgentCompass 2607.13705、COBS 2607.09052、Transforming Rank 2607.14018、STAPO 2607.04963**：`0716.md` 已收录 → 不重复。
- **Long-Horizon-Terminal-Bench 2607.08964、GPT-Red Jul 15**：`0715–0716.md` 已收录 → 不重复。
- **India IELTS Sep 2026 机考新闻**：`0716.md` 已收录 → 不重复；本日改收全球官方政策页。
- **LeetCode 3867 Day 16**：`0716.md` 已收录 → 不重复。
- **LangChain #38905 model profile refresh、#38897 marketplace cookbook**：数据/docs → 丢弃。
- **vLLM #48938 logo、#48133 compile log UX、#46871 XPU MoE**：infra/UX → 丢弃。
- **vLLM #48852 Jamba/InternLM2 dropped args**：与 #48295 同族但 #48295 更通用 → 记入待观察。
- **AutoGen #7966 utf-8 Windows、#7691 Synap memory sample**：平台/示例，弱于今日 eval/harness focus → 丢弃。
- **LangGraph #8361 pydantic defaults、#8359 msgpack PurePath**：重要但偏 state schema 边缘 → 待观察（今日 §1 已满）。
- **Claude Code v2.1.212 subagent/web search caps**：产品运维向，无开源 harness 细节 → 不收录主报告。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-17** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv Jul 17 公告 + Anthropic 博客当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **542**（`0716.md` §7: 534 + 本日 §1: 8）
- Theory: **224**（`0716.md` §7: 221 + 本日 §2: 3）
- Career/Business: **96**（`0716.md` §7: 95 + 本日 §4: 1）
- LC: **78**（`0716.md` §7: 77 + 本日 §5: 1）
- IELTS: **78**（`0716.md` §7: 77 + 本日 §5: 1）
