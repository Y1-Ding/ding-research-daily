# Daily Research - 2026-09-06 (21:30)

## 0) 今日3条结论

- **LangGraph `update_state` 在 config-injected checkpointer 下会静默抹除 `DeltaChannel` 历史**（#8828, created **2026-09-06**）：Platform 场景 `get_state`/`update_state` 未把 config 解析出的 checkpointer 传入 channel hydration → `messages` 等 delta 历史被覆盖 → **HITL / time-travel eval 须测「pause → update_state → resume」后 channel 完整性。**
- **LlamaIndex `SimpleVectorStore` 同日三连修暴露 RAG eval 系统性盲区**（#22964/#22966/#22967, created **2026-09-06**）：`query.mode`、`doc_ids`、`mmr_threshold` 均被忽略或错误转发 → **RAG agent eval gold 须覆盖 learner 模式、源文档过滤、MMR 多样性三条独立断言，不能只测 default SVM。**
- **vLLM sparse attention 栈三条 SM120/integrated GPU 边界同日收敛**（#55572/#55563/#55557, created **2026-09-06**）：indexer logits budget 在 unified memory 上 OOM、GLM-5.3 kpool 加宽 top-k buffer 超 2048、Qwen4Exp QSA 主 KV 支持 fp8 → **sparse attention eval 须标硬件 profile（integrated vs discrete）+ index buffer 有效宽度 + KV dtype 三条维度。**

## 1) Agent工程化（high-signal）

- [fix(pregel): hydrate DeltaChannels with config-resolved checkpointer to prevent data loss in update_state（LangGraph PR #8828, created **2026-09-06**）](https://github.com/langchain-ai/langgraph/pull/8828)
  - 摘要: Fixes #8653。LangGraph Platform 等场景通过 `config` 动态注入 checkpointer（`CONFIG_KEY_CHECKPOINTER`）时，`update_state` / `get_state` 在 `_prepare_state_snapshot` 未把 config 解析出的 saver 传入 `channels_from_checkpoint()` → `DeltaChannel`（如 `messages`）历史被静默覆盖。Fix：四个 state reader + `bulk_update_state`/`abulk_update_state` 统一传递 config-resolved checkpointer；附 E2E regression。
  - 为什么重要(对我): **skill 落地 + 评测**——HITL pause/resume、time-travel debugging 是 agent eval 的 state integrity gate；config-injected checkpointer 是 Platform 默认路径，此 bug = production 与 local eval 行为分叉。
  - 明日动作(<=20min): 写 **是/否**：你是否在 eval 测 `update_state` 后 `messages` channel 完整性（是/否/未知）。

- [fix(checkpoint): map shared embeddings across duplicate indexed texts in InMemoryStore（LangGraph PR #8827, created **2026-09-06**）](https://github.com/langchain-ai/langgraph/pull/8827)
  - 摘要: Fixes #8822。`InMemoryStore.batch()` 对重复 indexed text 去重后只生成一份 embedding，但 `_insertinmem_store` 按 1:1 写入 → 共享文本的后续 index target 丢失向量。Fix：按 `zip(embeddings, to_embed.values())` 将同一 embedding 写入所有共享 target；附 sync/async regression。
  - 为什么重要(对我): **RAG + skill 落地**——long-term memory store 批量写入重复 chunk 是常见路径；eval 若只测 unique text 会 miss 向量丢失。
  - 明日动作(<=20min): 写 **是/否**：你的 memory store eval 是否测 duplicate indexed text batch（是/否/未知）。

- [fix(core): re-validate f-string PromptTemplate at format time（LangChain PR #40237, created **2026-09-06**）](https://github.com/langchain-ai/langchain/pull/40237)
  - 摘要: Closes CVE-2025-65106 bypass（GHSA-6qv9-48xg-fc7f）。`PromptTemplate` 构造时 `pre_init_validation` 阻止 `{u.password}` 等属性访问，但 pickle/copy/子类直设 `template` 可绕过 → `format()` 时泄露对象内部。Fix：在 `format()` 路径重新校验 f-string 变量名。
  - 为什么重要(对我): **skill 落地 + 安全门禁**——agent prompt pipeline 若经序列化/adapter 重建 PromptTemplate，构造时校验不足；eval 须加 format-time 属性泄露负例。
  - 明日动作(<=20min): 写 **是/否**：你的 agent prompt 是否经 pickle/copy/非标准构造路径（是/否/未知）。

- [fix(langchain): enforce interrupt_on policy on cross-tool edits in HumanInTheLoopMiddleware（LangChain PR #40231, created **2026-09-06**）](https://github.com/langchain-ai/langchain/pull/40231)
  - 摘要: Fixes #40166。HITL 中间件在 human 将 tool call 从 `tool_a` 改为 `tool_b` 时，只检查原 tool 的 `interrupt_on` 策略，目标 tool 的审批 gate 被绕过。Fix：edit 改 tool name 时查目标 tool 的 `interrupt_on`，触发第二次 interrupt。
  - 为什么重要(对我): **skill 落地 + 评测**——HITL eval 须覆盖 cross-tool edit 负例；与 0905 `allow_list` shell gate 形成「人审 + 命令白名单 + 跨 tool 审批」三层。
  - 明日动作(<=20min): 写 **是/否**：你的 HITL eval 是否测 human edit tool name（是/否/未知）。

- [fix(langchain): isolate file-search fallback with a subprocess timeout（LangChain PR #40232, created **2026-09-06**）](https://github.com/langchain-ai/langchain/pull/40232)
  - 摘要: `FilesystemFileSearchMiddleware` 的 `grep_search` 在 ripgrep 不可用时走 Python `re` fallback，模型可控 regex 可在 agent 进程内 catastrophic backtracking 无限阻塞（`([0-9a-f]+)+g` 对 sha 行：len 40 仍 >10s 无界）。Fix：fallback 移入 worker subprocess + 30s 硬超时，对齐 ripgrep 路径。
  - 为什么重要(对我): **skill 落地 + token 优化**——filesystem skill 的 regex 是 agent liveness gate；eval 须测 nested-quantifier 负例 + timeout 行为。
  - 明日动作(<=20min): 写 **是/否**：你的 filesystem skill eval 是否测 catastrophic regex timeout（是/否/未知）。

- [fix(core): make default_parser tolerate a judge response with no reasoning line（LlamaIndex PR #22968, created **2026-09-06**）](https://github.com/run-llama/llama_index/pull/22968)
  - 摘要: `CorrectnessEvaluator` 默认 `default_parser` 按首换行拆 `(score, reasoning)`；judge 只返回 `"4.5"` 时 `ValueError: not enough values to unpack` 中止整个 eval run；leading blank line 时 `float("")` 静默得 `None`。Fix：用 `str.partition("\n")` 解析，缺 reasoning 得 `""` 而非 raise。
  - 为什么重要(对我): **评测**——LLM-as-judge eval 的 parser 容错是 batch eval 的 liveness gate；须测 score-only / leading-blank 两条边界。
  - 明日动作(<=20min): 写 **是/否**：你的 judge eval 是否测 score-only response（是/否/未知）。

- [fix(core): honor the query MMR threshold in SimpleVectorStore（LlamaIndex PR #22967, created **2026-09-06**）](https://github.com/run-llama/llama_index/pull/22967)
  - 摘要: `SimpleVectorStore.query()` 忽略 `VectorStoreQuery.mmr_threshold`，通过 query object 配置的多样性阈值静默失效。Fix：无 keyword 时转发 query 字段；保留 keyword 优先级（含显式 `None`/`0.0`）；9-case 矩阵 3/9 在 main 失败。
  - 为什么重要(对我): **评测 + RAG**——MMR threshold 是 retrieval diversity eval 的独立维度；只测 keyword 路径会 false pass。
  - 明日动作(<=20min): 写 **是/否**：你是否通过 `VectorStoreQuery.mmr_threshold` 配置多样性（是/否/未知）。

- [fix(core): respect source document filters in SimpleVectorStore（LlamaIndex PR #22966, created **2026-09-06**）](https://github.com/run-llama/llama_index/pull/22966)
  - 摘要: `VectorIndexRetriever` 文档承诺 `doc_ids` 为源文档限制并转发到 `VectorStoreQuery`，但 `SimpleVectorStore` 忽略 → 跨文档 chunk 泄漏。Fix：经 `text_id_to_ref_doc_id` 过滤；空列表/未知 ID 无结果；learner 空候选集提前返回。17-case 14/17 在 main 失败。
  - 为什么重要(对我): **评测 + RAG agent**——multi-doc RAG eval 的 doc scope 断言是 gold 集基础；忽略 `doc_ids` = eval 与 API 文档脱节。
  - 明日动作(<=20min): 写 **是/否**：你的 RAG eval 是否断言 `doc_ids` 过滤（是/否/未知）。

- [Perf][Tool Parser] Track DeepSeek streaming delimiters incrementally（vLLM PR #55565, created **2026-09-06**）](https://github.com/vllm-project/vllm/pull/55565)
  - 摘要: DeepSeek V3/V3.1 streaming tool parser 每 delta 扫描完整累积 token 序列做 delimiter 计数 → 无 tool call 的响应工作量 O(T²)。Fix：外层 marker 出现前只检查 `delta_token_ids`；出现后增量更新 inner delimiter count；V3/V3.1 共享 state class。21 passed + differential output validation。
  - 为什么重要(对我): **token 优化 + agent serving**——streaming tool parse 是 agent latency 关键路径；eval 须区分 no-tool 长回复的 CPU 开销。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serving DeepSeek V3/V3.1 streaming tool call（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Bugfix][Sparse MLA] Lower the default indexer logits budget on integrated (unified-memory) GPUs（vLLM PR #55572, created **2026-09-06**）](https://github.com/vllm-project/vllm/pull/55572)
  - 摘要: Fixes #55569。integrated GPU（`cudaDeviceProp.integrated == 1`，如 GB10/DGX Spark）长 prefill 时 sparse MLA indexer logits 默认 budget 在 unified memory 上 OOM。Fix：检测 integrated GPU 并降低默认 indexer logits budget。
  - 为什么重要(对我): **Talk ≥3 turns + sparse attention indexer**——与 0905 DSv4 Hadamard rotation / top-k stash 同族；indexer 内存预算须按硬件 profile（integrated vs discrete）分 eval 维度。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 integrated GPU serving sparse MLA indexer（是/否/未知）。

- [Bugfix][GLM-5.3] Fit the kpool-widened sparse top-k buffer to the 2048-wide SM120 index without a config edit（vLLM PR #55563, created **2026-09-06**）](https://github.com/vllm-project/vllm/pull/55563)
  - 摘要: GLM-5.3-Flash 出厂 `index_topk=2048, index_kpool=4`；kpool indexer 将 top-k buffer 加宽 pool tail 并 pad 到 128 倍数 → 有效宽度 2176，SM120 sparse backend 硬限 2048 → 启动 reject 或 kernel 收到错误宽度。Fix：init 时自动 fit 为 `index_topk = 2048 - tail`（2045），写回 config 使 indexer 与 backend 一致。
  - 为什么重要(对我): **Talk ≥3 turns + sparse top-k/indexer**——kpool 加宽是 sparse attention 的 index buffer 语义；eval 须标 effective width 而非 raw config。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serving GLM-5.3 sparse MLA on SM120（是/否/未知）。

- [Model] Qwen4Exp: fp8_e4m3 main KV cache on the QSA path（vLLM PR #55557, created **2026-09-06**）](https://github.com/vllm-project/vllm/pull/55557)
  - 摘要: Qwen3.8-Flash-Next 的 QSA（sparse attention）层主 K/V cache 支持 `--kv-cache-dtype fp8`（`fp8_e4m3`）；indexer side cache（raw-key ring、compressed keys）与 GDN state 保持 bf16。`ops/qsa.py` 新增 `IS_FP8` kernel path 加载 fp8 pages 并应用 per-tensor K/V scales；附 bf16 reference 对比 test。
  - 为什么重要(对我): **Talk ≥3 turns + sparse attention KV layout**——QSA 路径的 main KV 与 indexer side cache dtype 分离是 hybrid sparse model 内存模型核心。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 Qwen3.8-Flash-Next QSA + fp8 KV cache（是/否/未知）。

- [Bugfix] Propagate quantized-KV scales to KV-sharing layers（vLLM PR #55559, created **2026-09-06**）](https://github.com/vllm-project/vllm/pull/55559)
  - 摘要: Gemma 3n/4 E-series 的 KV-sharing layers（`num_kv_shared_layers`）读取 target layer 写的 cache，但 quantized-KV checkpoint 只为 target layer 有 `k_scale`/`v_scale` → sharing layers 默认 1.0 解量化错误。Fix：bind shared cache 时将 target scales 复制到 sharing layer。Per-layer attention output error 从 layer 15+ 的 0.40-0.91 降至 0.19-0.26。
  - 为什么重要(对我): **Talk ≥3 turns + attention KV dequant**——KV-sharing 是 attention 内存共享机制；scale 传播是 quantized KV eval 的前置条件。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serving Gemma KV-sharing + quantized KV（是/否/未知）。

- [Feature] Add reasoning_eos_policy to close think on mid-reasoning EOS（vLLM PR #55562, created **2026-09-06**）](https://github.com/vllm-project/vllm/pull/55562)
  - 摘要: 新增 `reasoning_eos_policy`：reasoning model 在 think block 内遇到 mid-reasoning EOS 时，强制 `reasoning_end_str` 关闭 think 再继续 answer；Qwen3 `<tool_call>` 在 think 内开始视为离开 think，tool-call token 不被改写。29 unit tests passed。
  - 为什么重要(对我): **Talk ≥3 turns + reasoning attention path**——reasoning model 的 EOS 语义与 think/tool-call 边界是 multi-turn agent eval 的 token 路由核心。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serving reasoning model + mid-reasoning EOS（是/否/未知）。

- [Bugfix][Tool Parser] HY-V4 streaming: fix name boundary, drop nameless calls（vLLM PR #55550, created **2026-09-06**）](https://github.com/vllm-project/vllm/pull/55550)
  - 摘要: `hy_v4` streaming parser 在 `</tool_call><tool_call>name<arg_key>` 单 delta 时，`arg_idx` 优先于更近的 `end_idx` → tool name 切片跨 call 边界；空 function name 也被 emit。Fix：`name_end = min(arg_idx, end_idx)` + 丢弃 nameless call。首个 repo 内 test module。
  - 为什么重要(对我): **Talk ≥3 turns + agent tool-call parsing**——streaming batched delta 的 delimiter 优先级是 tool parse 正确性边界；与 0905 GLM XML multi-call 同构。
  - 明日动作(<=20min): 写 **是/否**：你是否 serving HY-V4 streaming tool call（是/否/未知）。

## 3) 热门项目/可试个人项目

- [vercel-labs/agent-browser — Browser automation CLI for AI agents（created **2026-01-11**, trending **2026-09-05**）](https://github.com/vercel-labs/agent-browser)
  - 摘要: Rust CLI，让 AI agent 驱动 Chrome 完成 open/click/fill/screenshot，无需 Node.js 或 Playwright 运行时依赖；Apache 2.0，~42k stars（2026-09-06 GitHub API）。与 Playwright/Puppeteer 类项目差异在架构：agent 侧零 JS runtime，适合轻量 self-hosted agent 部署。
  - 为什么重要(对我): **skill 落地低成本试验**——browser skill 是 agent eval 的通用基础设施；Rust CLI 可作为 Cursor plugin / agent skill 的 subprocess 后端，复现成本低于完整 Playwright stack。
  - 明日动作(<=20min): `agent-browser --help` 或读 README 前 50 行，写 **1 条** 它与你现有 browser skill 的差异（≤20 词）。
  - 估计复现成本: **低**（clone + 读 README + 写 1 条接点，~15min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Introducing GPT-6 Astra for developers（Simon Willison Blog, **2026-09-05**）](https://simonwillison.net/2026/Sep/5/introducing-gpt-6-astra-for-developers/)
  - 机会信号: OpenAI 将 GPT-6 Astra 定位为 Fable 竞品（$10/$50 per M tokens），Coding Agent Index 成本效率领先 Claude Fable 5；ExploitBench 100%、ARC-AGI 3 在 custom harness 下 99.9%——coding agent 与 security eval 能力成为 API 定价锚点，独立开发者可用公开 benchmark 做模型选型与产品差异化。
  - 风险信号: 自报 benchmark 需区分 default harness vs custom Provider Adapter harness（ARC-AGI 62.7% vs 99.9%）；Astra 安全能力极强也意味 agent 滥用门槛降低；与 0904/0905 rogue agent wiki 事件形成「能力↑ + 治理缺口」张力。
  - 我可执行的一步(<=30min): 读 Simon 文 + [0903 GPT-6 Astra 定价帖](https://simonwillison.net/2026/Sep/3/gpt6-astra/)，写 **3 条** 你的 agent eval playbook 能否检测「benchmark 作弊式 external write」（各 ≤15 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[115. Distinct Subsequences](https://leetcode.com/problems/distinct-subsequences/)（**Day 67, Sep 2026 Daily Challenge**，承接 0905 的 2951「Smallest Stable Index II」，今日 **Hard**「String + DP，统计 t 在 s 中的 distinct subsequence 数」）：关键洞察——DP 状态 `dp[j]` 表示 t 前 j 字符的匹配数，遇 `s[i]==t[j]` 时 `dp[j]+=dp[j-1]`；与 agent eval「路径计数/状态聚合」同构。明日动作：手写 O(n·m) DP；写 **≤15 词**：distinct subsequence 对你 eval 多路径聚合意味着什么。

- **IELTS（≤1 条）**：[IELTS Writing Practice Tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/writing)：0905 已轮换 Speaking，今日换 **Writing Task 2**（选 1 题，限时 40min 写 outline + 开头段）。明日动作：完成 1 次 Task 2 outline；记录 **≥2 个** 论证结构/连接词失误；写 **≤15 词** 你最弱的 Writing 维度（Task Response / Coherence / Vocabulary / Grammar）。

## 6) 噪音过滤记录

- **docs(docs):创建学习计划文档（LangGraph #8830）**：非英文/非工程信号 → 过滤。
- **fix(openai): login_chatgpt_device（LangChain #40242）**：auth fix，与当前 focus 弱相关 → 过滤。
- **fix(huggingface): translation pipeline（LangChain #40238）**：非 agent 路径 → 过滤。
- **fix(core): mime_type for base64 image（LangChain #40239）**：边界 fix，信号弱 → 过滤。
- **test(core): deprecated .text() accessor（LangChain #40243/#40230）**：测试覆盖，非行为变更 → 过滤。
- **fix: add_documents TypeError（LangChain #40229）**：通用 fix，与 focus 弱相关 → 过滤。
- **fix(core): CohereRerankRelevancyMetric API key（LlamaIndex #22969）**：错误消息 fix → 过滤。
- **fix(core): SimpleVectorStore filesystem round trips（LlamaIndex #22965）**：持久化 fix，优先级低于同日 eval 相关三连修 → 过滤。
- **fix(redis-chat-store): message order（LlamaIndex #22963）**：chat store fix，非 eval 核心 → 过滤。
- **fix(autogen-ext): claude-3-haiku context window（AutoGen #8199）**：model table 数据修正 → 过滤。
- **docs: security extension points（AutoGen #8198）**：文档有价值但非代码信号；已在 0904 MCP security 邻域覆盖 → 过滤。
- **CI / infra / model-table PRs（vLLM #55570/#55567/#55566/#55564/#55553/#55551/#55549 等）**：validation、content format fallback、vision perf、weight compare、NIXL、embedding、multimodal cache → 过滤。
- **0905 已收录项**：Hadamard rotation #55459、PLE stride #55467、DeepSeek reasoning_content #40220、GLM XML parser #55460 → semantic duplicate，不重复。
- **Hermes Agent 240k stars 报道**：GitHub API 未找到对应主 repo（仅 awesome/derivative repos）→ 无法验证，不收录。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-06** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **902**（`0905.md` §7: 893 + 本日 §1: 9）
- Theory: **407**（`0905.md` §7: 401 + 本日 §2: 6）
- Career/Business: **137**（`0905.md` §7: 136 + 本日 §4: 1）
- LC: **119**（`0905.md` §7: 118 + 本日 §5: 1）
- IELTS: **119**（`0905.md` §7: 118 + 本日 §5: 1）
