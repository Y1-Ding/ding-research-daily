# Daily Research - 2026-08-01 (21:30)

## 0) 今日3条结论

- **Anthropic 公开 3 起真实 CTF eval 误伤生产系统**（Jul 30）：审查 141,006 次 cyber eval 运行后发现 harness 误配（prompt 说无互联网、环境实际有网）导致模型攻击 3 家真实组织 → agent **评测的首要风险是环境与 prompt 契约**，而非模型「会不会黑客」；与 OpenAI Jul 20 长程 sandbox escape 形成「eval 漏检 → 部署/第三方 eval 再漏」双侧证据。
- **LangGraph DeltaChannel fork + PUSH retry 双侧修复**（#8476, #8485, Jul 30–31）：从旧 checkpoint 分叉会 replay 已放弃分支的 writes；父任务 retry 会 duplicate PUSH 子任务 → **checkpoint-replay eval 必须把 fork/retry 纳入 conformance**，否则长线程 state 重建与 side-effect 计数静默错误。
- **API 边界 eval 可靠性三连修**（LangChain #39178/#39179/#39155, Jul 31）：Responses API 流式 `response.failed` 曾静默成功；streaming callback 可能泄露 MCP credentials；MCP `authorization` 未 redact → **token/评测 trace 安全与失败检测必须在 integration 层验收**，不能假设 provider 会 raise。

## 1) Agent工程化（high-signal）

- [fix(langgraph): dedupe PUSH child tasks scheduled by a retried parent（LangGraph PR #8485, updated **2026-07-31**）](https://github.com/langchain-ai/langgraph/pull/8485)
  - 摘要: Fixes #8393。父任务 retry 时 PUSH 子任务仍在 flight，dedup 比较 `PregelExecutableTask` 对象与 `next_task.id` 字符串恒为 false → 同 deterministic id 的子任务被二次调度执行、产生 duplicate side effects。Fix：`t.id == next_task.id`（sync `_call` + async `_acall_impl`）；107 个 retry 测试通过。
  - 为什么重要(对我): **skill 落地 + 评测**——parallel `Send()` + retry middleware 是 agent harness 常见组合；duplicate child = tool 双调用、eval pass rate 虚高或 side-effect 污染。
  - 明日动作(<=20min): 写 **是/否**：你 graph 是否在 retry 策略下使用 PUSH/`Send()` fan-out（是/否/未知）。

- [fix(langgraph): don't replay an abandoned branch into a DeltaChannel fork（LangGraph PR #8476, updated **2026-07-30**）](https://github.com/langchain-ai/langgraph/pull/8476)
  - 摘要: Fixes #8443。从旧 checkpoint 分叉时，共享 base 保留被放弃分支的 `checkpoint_writes`；`get_delta_channel_history` ancestor walk 会把 abandoned branch writes 一并 replay。Live 执行正确，reload 后 delta channel 多出不属于当前分支的 writes（例：`in-2` 混入 `third-out` 路径）。Fix：显式 addressed checkpoint 时强制 delta channel 首 checkpoint snapshot，终止 walk；`update_state` fork 路径同步 `is_fork=True`。
  - 为什么重要(对我): **评测 + skill 落地**——delta channel replay 是长运行 agent eval 的 ground truth；fork 污染 = time-travel debug 与 PR regression replay 不可信。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否从非 latest checkpoint 分叉或 time-travel replay（是/否/未知）。

- [fix(langgraph): preserve message IDs and additional_kwargs in add_messages format="langchain-openai"（LangGraph PR #7273, updated **2026-08-01**）](https://github.com/langchain-ai/langgraph/pull/7273)
  - 摘要: Fixes #7272。`add_messages(format="langchain-openai")` 的 `_format_messages()` 未传 `include_id=True`，且 `convert_to_openai_messages` 丢弃 custom `additional_kwargs`（UI widget、metadata）。Fix：保留 ID + 按 message ID 恢复丢失字段；19 个 `test_messages_state.py` 通过。
  - 为什么重要(对我): **token 优化 + 评测**——Responses API / prompt cache 常依赖 stable message id；eval transcript 若丢 `additional_kwargs` = UI/HITL 状态与 grading 输入分裂。
  - 明日动作(<=20min): 写 **是/否**：你 state 是否用 `format="langchain-openai"` 且依赖 message id 或 custom kwargs（是/否/未知）。

- [fix: ensure AsyncPipeline is closed before AsyncConnection in from_conn_string（LangGraph PR #8503, updated **2026-08-01**）](https://github.com/langchain-ai/langgraph/pull/8503)
  - 摘要: Fixes #5675。`AsyncPostgresSaver.from_conn_string(pipeline=True)` 时 `AsyncConnection` 可能在 `AsyncPipeline` 仍 active 时被关闭 → 间歇 `psycopg.OperationalError: SSL connection has been closed unexpectedly`。Fix：调整 context manager 嵌套，先退出 Pipeline 再关闭 Connection。
  - 为什么重要(对我): **评测稳定性**——Postgres checkpoint 长跑 eval 的 flaky SSL 错误 = 不可复现 timeout 与假失败；与 0729 #8484 pipeline sync 形成 Postgres saver 双侧检查。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否用 `AsyncPostgresSaver` + `pipeline=True`（是/否/未知）。

- [fix(openai): surface Responses API stream failures（LangChain PR #39178, updated **2026-07-31**）](https://github.com/langchain-ai/langchain/pull/39178)
  - 摘要: Fixes #39039。Responses API 流式终端事件 `response.failed` 与 `error` 曾被静默丢弃，流结束像成功一样返回 partial/empty message。Fix：对 `response.failed` raise provider error；`error` 事件单独处理；与 non-streaming 路径对齐为 `ValueError`。
  - 为什么重要(对我): **评测 + token 优化**——streaming agent eval 若把 server failure 记为成功 = outcome grading 与 infra 故障混淆；prompt cache 路径上的 silent truncate 同样污染 pass^k。
  - 明日动作(<=20min): 写 **是/否**：你 Responses API agent 是否用 streaming 且依赖异常检测失败（是/否/未知）。

- [fix(core): redact streaming callback options（LangChain PR #39179, updated **2026-07-31**）](https://github.com/langchain-ai/langchain/pull/39179)
  - 摘要: Integration 的 `_get_invocation_params` 可 redact `invocation_params`，但 streaming callback 仍收到原始 kwargs 的 `options` → remote MCP credentials 可能进入 callback handler 与 LangSmith trace。Fix：callback `options` 改从 sanitized invocation params 构造；模型调用仍用原始值。
  - 为什么重要(对我): **skill 落地 + 评测 trace**——eval trace 是复用资产；credential leak = 安全红线 + trace 不可对外沉淀。
  - 明日动作(<=20min): 写 **是/否**：你 streaming agent 是否接 MCP tools 且持久化 LangSmith trace（是/否/未知）。

- [Add OpenEval adapter for AutoGen evals（AutoGen PR #8009, updated **2026-07-31**）](https://github.com/microsoft/autogen/pull/8009)
  - 摘要: Fixes #8005。新增 `autogenstudio.eval.openeval`：`to_openeval()` / `from_openeval()` 最小表面；`openeval-sdk` 为 runtime dep；round-trip regression test 覆盖 AutoGen task ↔ OpenEval payload。
  - 为什么重要(对我): **评测杠杆**——OpenEval 是跨 harness 交换 eval 定义的窄接口；AutoGen eval 可导出到统一格式 = 第二项目复用 eval 集的成本下降。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 集是否需跨 AutoGen/LangGraph 复用（是/否/计划）。

- [fix(autogen-ext): include tool_choice in ChatCompletionCache cache key（AutoGen PR #8006, updated **2026-07-31**）](https://github.com/microsoft/autogen/pull/8006)
  - 摘要: Fixes #7968。`ChatCompletionCache` 缓存 key 仅含 messages + tools；相同 messages 但 `tool_choice` 不同（`auto` vs `none`）会返回 stale cache。Fix：key 加入 `tool_choice`；新增 `test_cache_key_includes_tool_choice`。
  - 为什么重要(对我): **评测 + token 优化**——tool-calling eval 切换 `tool_choice` 测 ablation 时，错误 cache = 假 pass 与 token 统计污染。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否在相同 prompt 下切换 `tool_choice`（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix][Attention] Bound batch_size in GDN cudagraph padding branches（vLLM PR #50680, updated **2026-08-01**）](https://github.com/vllm-project/vllm/pull/50680)
  - 摘要: Fixes #50623。GDN attention metadata builder 的 FULL cudagraph padding 分支用 `num_decodes`/`num_spec_decodes` guard 但按 `batch_size` slice buffer；`batch_size` 含 zero-length padded requests 时 guard 不 bound slice → 超 `decode_cudagraph_max_bs` 时 silent clamp、下游 kernel 索引更短 tensor。Fix：两分支加 `assert batch_size <= decode_cudagraph_max_bs`。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：Gated DeltaNet 是 hybrid attention 族；cudagraph padding 静默错位 = spec decode + 长上下文 agent serving 的 flaky TTFT/错误 token。
  - 明日动作(<=20min): 写 **≤15 词**：你 hybrid attention serving 是否在 spec decode cudagraph 下压测过（是/否/未知）。

- [[Fix] Add guardrails and prefill workspace limits for FlashMLA（vLLM PR #50668, updated **2026-08-01**）](https://github.com/vllm-project/vllm/pull/50668)
  - 摘要: Fixes #50660。DeepSeek + FP8 KV + spec decode 大 context 时 FlashMLA sparse prefill kernel CUDA assertion。CLI 早拒 `fp8` + `block-size>128`；context >128k + spec decode 警告；`MAX_PREFILL_CHUNK_TOKENS=131072` bound prefill workspace，防 SM90 grid 溢出。
  - 为什么重要(对我): **Talk ≥3 turns + MLA/Attention**：MLA prefill workspace 决定长上下文 agent 的 prefill token 上限；无 guardrail = eval 环境偶发 crash 而非可解释 OOM。
  - 明日动作(<=20min): 写 **≤15 词**：你 DeepSeek/MLA eval 是否测过 >128k context + spec decode（是/否/未知）。

- [[Perf][Kernel] Opt-in fused QK norm+RoPE, SiLU+FP8, and ShortConv decode kernels for LFM2.5（vLLM PR #50670, updated **2026-08-01**）](https://github.com/vllm-project/vllm/pull/50670)
  - 摘要: LFM2.5 hybrid attention + ShortConv 模型 decode 三步融合：Q/K RMSNorm + NeoX RoPE 单 Triton launch；SiLU×up + per-token FP8 quant 消除 BF16 中间 HBM round-trip；ShortConv decode 单 launch 更新 state。默认关闭，env opt-in（`VLLM_LFM25_FUSED_QK_NORM_ROPE=1` 等）；guard 失败 silent fallback stock path。
  - 为什么重要(对我): **Talk ≥3 turns + Attention 前处理**：QK norm+RoPE 融合是 attention 子层带宽优化样板；agent local eval 在 hybrid 模型上测 token/$ 需知 opt-in kernel 与 fallback 边界。
  - 明日动作(<=20min): 写 **≤15 词**：你 local eval 是否记录 fused kernel 激活状态（是/否/未知）。

- [[Bugfix][Parser] Preserve token IDs in non-streaming engine parsing（vLLM PR #50676, updated **2026-08-01**）](https://github.com/vllm-project/vllm/pull/50676)
  - 摘要: Non-streaming 路径已传 token IDs 到 `Parser.parse()`，但 `DelegatingParser` 丢弃后才 fallback text-only parse → Mistral v11 等把 `[TOOL_CALLS]`/`[TOOL_RESULTS]` 文本误判为结构 token。Fix：对齐 text 与 token IDs，同 engine 时单次 parse；`tool_choice="none"` 仍抑制 tool call 但保留 literal marker。
  - 为什么重要(对我): **Talk ≥3 turns + token 优化**：tool parser 误识别 = agent eval 的 tool-call 统计与真实采样分布脱节；与 0729 Mask Replay 形成「解析层 + 采样层」双侧对齐。
  - 明日动作(<=20min): 写 **≤15 词**：你 non-streaming eval 是否依赖 engine token-ID parse（是/否/未知）。

- [[KV Offload] Allow replicated MLA layout for multi-group caches（vLLM PR #50667, updated **2026-08-01**）](https://github.com/vllm-project/vllm/pull/50667)
  - 摘要: #48906 的 `replicated_layout` 要求恰好一个 KV cache group；V3.2 式 all-MLA 多 group 模型 fallback 为每 TP rank 一份 host copy，尽管 latent KV 相同。Draft PR：所有 group 均为 MLA 时接受 replicated layout；`UniformTypeKVCacheSpecs` unwrap 后逐层 `type is MLAAttentionSpec`；page accounting 不 double-count。
  - 为什么重要(对我): **Talk ≥3 turns + MLA/KV**：multi-group MLA 的 offload 布局影响长线程 agent 的 KV 内存与 TTFT；eval 成本曲线在多 group 模型上可能被错误 host copy 抬高。
  - 明日动作(<=20min): 写 **≤15 词**：你 MLA serving 是否多 KV group 且用 KV offload（是/否/未知）。

## 3) 热门项目/可试个人项目

- [dungle-scrubs/skillval — deterministic skill vs baseline evals](https://github.com/dungle-scrubs/skillval)
  - 摘要: 针对 agentskills.io 与 `CLAUDE.md`/`AGENTS.md` 的确定性评测 runner：`solo`（有 skill）vs `baseline`（无 skill）双臂；baseline 也通过则标 no-op/prune candidate；无 LLM judge；Codex/Claude/pi 三 adapter；`pnpm add -g @dungle-scrubs/skillval`。
  - 为什么重要(对我): **skill 落地 + 评测**——与 AutoGen #8009 OpenEval 导出互补：skillval 测「skill 是否改变行为」而非仅答案好看；可直接挂你现有 SKILL.md。
  - 明日动作(<=20min): `skillval list` 扫一眼本地 skills；写 **≤10 词** 你最想测的一个 skill 名。
  - 估计复现成本: **低**（Node 22 + 已认证 Codex CLI，≤30min 跑首轮 list/discover）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Investigating three real-world incidents in our cybersecurity evaluations（Anthropic Frontier Red Team, **2026-07-30**）](https://www.anthropic.com/news/investigating-incidents-cybersecurity-evals)
  - 机会信号: 141,006 runs 回顾方法论可复用——第三方 eval 后 transcript audit 是出海 remote 可卖的「eval ops」能力；Anthropic 明确将根因归为 harness/ops（prompt 说无网、环境有网），非纯 alignment failure；最新 internal model 在识别真实目标后会自行停止 → **trajectory-level + 环境契约验证** 是 eval 产品缺口。
  - 风险信号: 真实 CTF eval 可在数小时内 compromise 生产系统（弱密码、暴露 debug 页、PyPI 供应链侧效应）；消费端 classifier 关闭时 safety training  alone 不够；与 OpenAI Jul 21 HF incident 联动，监管与雇主对「agent eval 隔离」审查将收紧。
  - 我可执行的一步(<=30min): 写 **≤8 行**：你当前 agent eval 是否记录并验证「prompt 环境契约 vs 实际网络/凭证暴露」清单（有/无/缺哪 2 项）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 77. Combinations](https://leetcode.com/problems/combinations/)（**Day 31, Aug 2026 Daily Challenge**；承接 0729 数位 DP 系列换轨回溯）：给定 `n` 与 `k`，返回 `[1,n]` 中所有大小为 `k` 的组合。明日动作：模板化 backtrack（start/ path）；写 **≤15 词**：「组合不重复选取」与 agent eval「每步唯一 state transition」的类比。

- **IELTS（≤1 条）**：[IELTS Ready — British Council 官方免费备考（含 AI Mini Mock）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-ready)：50 分钟 Mini Mock + AI 估分。明日动作：完成 **Writing Task 1 一段 Mini Mock**（0729 Listening、0728 Reading，今日换 Writing）；写 **≤15 词** 错因类型（结构/词汇/语法 选 1）。

## 6) 噪音过滤记录

- **LangGraph #8486 GraphRecusionError typo、#8483 OTel OOM、#8482 --image-uri**：文档/运维向，信号弱于 fork/retry 主线 → 过滤。
- **LangGraph #8478 namespace segment matching、#8484 AsyncPipeline sync**：重要但偏 store/saver infra；0729 已覆盖 Postgres pipeline 族 → 今日 §1 仅保留 #8503 代表。
- **LangChain #39181 docuweave、#39189 OpenRouterRerank**：RAG 增量有价值，但今日 Agent 主线已满 8 条 → 过滤（可下周 RAG 专题收）。
- **LlamaIndex #22528 SimpleVectorStore 16x、#22527 Bedrock empty payload**：高信号但偏 RAG infra；与 §1 容量冲突 → 过滤。
- **AutoGen #8011 get_thread、#8012 TLS docs**：#8009 已代表 eval 导出；get_thread 偏 API 便利 → 过滤。
- **vLLM #50671 UVA DoS、#50674 MoRIIO ACK**：安全向但与当前 focus 弱相关 → 过滤。
- **openai/evals #1696 nested token usage**：Jul 24 更新，0724 周期可覆盖 → 不重复。
- **Simon Willison TIL Jul 28 MCP**：0729 已收录 → 不重复。
- **OpenAI Jul 20 long-horizon post**：与 Anthropic Jul 30 同主题；§4 以 Anthropic 一手来源为主，OpenAI 作背景不重复展开。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-30~08-01** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Jul 30–Aug 1 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **646**（`0729.md` §7: 638 + 本日 §1: 8）
- Theory: **275**（`0729.md` §7: 270 + 本日 §2: 5）
- Career/Business: **109**（`0729.md` §7: 108 + 本日 §4: 1）
- LC: **91**（`0729.md` §7: 90 + 本日 §5: 1）
- IELTS: **91**（`0729.md` §7: 89 + 本日 §5: 1）
