# Daily Research - 2026-05-30 (21:30)

> 采集口径：优先 **X_WHITELIST / YOUTUBE_WHITELIST / GITHUB_WHITELIST / BLOG_WHITELIST / CAREER_BUSINESS_WHITELIST**；评分 `relevance(40)+actionability(25)+credibility(20)+freshness(15)`，正文主段 **≥70**（职业池 **≥65** 且具可执行信号）。与 `0529.md` 及更早文件做 **URL 去重** + **语义去重**。本日 **`[OUTSIDE_WHITELIST]` 0 条** → 主链 **0%（≤20%）**。  
> **自动化提示（给编排器）**：Cron 触发时区若仍为 UTC，请将等价目标对齐 **Asia/Shanghai 每日 21:30**；失败 **重试 1 次**；连续 **2** 个日历日失败再发告警（本仓库文件不承载告警状态机）。

## 0) 今日3条结论

- **LangGraph 子 agent 观测命名收敛**：`#37721`（`BaseTool.subagent_name`）已 **关闭**；落地路径改为 **`lc_agent_name` 检测**（[#7928](https://github.com/langchain-ai/langgraph/pull/7928)，**Merged 2026-05-29**）——工具内 `create_agent(name=...)` 的子图在 lifecycle/`run.subgraphs` 上显示真实 agent 名而非 `tools`。
- **远程 v3 流式栈同日闭环**：`langgraph-sdk` **0.4.0**（[#7923](https://github.com/langchain-ai/langgraph/pull/7923)）+ **RemoteGraph v3**（[#7927](https://github.com/langchain-ai/langgraph/pull/7927)）+ **Decoder/interleave**（[#7935](https://github.com/langchain-ai/langgraph/pull/7935)，**Merged 2026-05-29**）使本地/托管图在 **v3 投影与到达序 interleave** 上可对齐。
- **评测与推理基建各有一条「契约级」更新**：OpenAI 发布 **第三方评测 playbook**（harness/有效性检查，**2026-05-29**）；vLLM 发布 **v0.22.0**（**2026-05-29**），MRv2 向默认推进、**多层 KV offload** 与 **batch-invariant FP8（+28.9% E2E）** 影响 agent 长跑成本模型。

## 1) Agent工程化（high-signal）

- [feat(langgraph): name tool-dispatched subagents via lc_agent_name（`langgraph` PR #7928，**Merged 2026-05-29**）](https://github.com/langchain-ai/langgraph/pull/7928)
  - 摘要: 当工具体 invoke 带 `create_agent(name=...)` 的内层 agent 时，supervisor 的 `run.subgraphs`/`run.lifecycle` 不再一律叫 `tools`；通过 **子 task 的 `lc_agent_name` 与父 namespace 不同** 判定 subagent，将 `graph_name` 设为内层名，并用父 `tools` task 的 `tool_call_id` 填 `cause`。**不再需要** `BaseTool.subagent_name`；伴随关闭 [#37721](https://github.com/langchain-ai/langchain/pull/37721)。
  - 为什么重要(对我): 子 agent **eval/追踪/告警** 可直接按 **agent 名** 聚合，而非误归到 `tools` 节点；与 DeepAgents 去 `SubagentTransformer` 的方向一致。
  - 明日动作(<=20min): 在你方 **1 个** tool-dispatch 子图场景，列出期望的 **lifecycle `graph_name`** 与是否已设 **`create_agent` 的 name**（各 ≤15 字符）。

- [feat(sdk-py): extract stream decoders and add interleave_projections（`langgraph` PR #7935，**Merged 2026-05-29**）](https://github.com/langchain-ai/langgraph/pull/7935)
  - 摘要: 五类投影重构为传输无关的 **`Decoder.feed()`**；新增 `AsyncThreadStream`/`SyncThreadStream.interleave_projections(channels)`，按 **到达序** 产出 `(channel, item)`；修复 sync 路径 **交错 tool call 事件丢失**；末次提交扩展 **updates/checkpoints/tasks** 通道并拒绝保留 channel 名。
  - 为什么重要(对我): 多通道 streaming UI 的 **合并顺序** 成为可单测契约；为 [#7938](https://github.com/langchain-ai/langgraph/pull/7938) 将 `RemoteGraph.interleave` 接到 SDK 铺路（draft）。
  - 明日动作(<=20min): 若你用 LangGraph SDK 消费流，标出是否需 **按到达序 interleave**；若是，记下你用的 **channel 列表**（≥2 个）。

- [feat(langgraph): add v3 streaming support to RemoteGraph（`langgraph` PR #7927，**Merged 2026-05-29**）](https://github.com/langchain-ai/langgraph/pull/7927)
  - 摘要: `RemoteGraph` 支持 `stream_events(version="v3")`/`astream_events(version="v3")`，薄适配层包装 SDK 的 `AsyncThreadStream`/`SyncThreadStream`，与本地 `CompiledStateGraph` v3 表面对齐；**不支持** 的 kwargs（`control`、`transformers` 等）显式 `NotImplementedError`；sync `interleave()` 暂 deferred（由 #7935/#7938 接续）。
  - 为什么重要(对我): **本地图 + 托管 LangGraph** 可共用同一套 v3 消费代码，降低「仅生产环境可复现」的观测分裂。
  - 明日动作(<=20min): 确认你方是否调用 **`RemoteGraph.stream_events`**；若否，写 **1 句** 仍用 v2 `.stream()` 的原因（≤25 词）。

- [release(sdk-py): 0.4.0（`langgraph` PR #7923，**Merged 2026-05-28**）](https://github.com/langchain-ai/langgraph/pull/7923)
  - 摘要: `langgraph-sdk` **0.3.15 → 0.4.0**；自 0.3.15 以来落地 **thread 中心化流式入口** `client.threads.stream(...)`（async+sync）、SSE/WebSocket 传输与 **重连**、messages/tool_calls/subgraphs 等投影及 scoped subgraph handles（#7818–#7833 系列）。
  - 为什么重要(对我): 客户端应从「裸 SSE 解析」迁到 **thread stream + 投影 API**，与 #7935 decoder 模型一致。
  - 明日动作(<=20min): 查你项目 `langgraph-sdk` 版本；若 `<0.4.0`，记 **1 条** 升级时需改的 import/调用点（文件路径即可）。

- [vLLM v0.22.0 release（`vllm-project/vllm`，**Published 2026-05-29**）](https://github.com/vllm-project/vllm/releases/tag/v0.22.0)
  - 摘要: **459 commits**；亮点包括 **Model Runner V2** 对 Qwen3 dense **默认走 MRv2 oracle**（#39337）、有 KV connector 时 **自动回退 MRv1**（#42955）；**多层 KV cache offload** 框架（#40020）+ 文件系统二级层（#41735）+ Mooncake 磁盘 offload（#42689）；**batch-invariant Cutlass FP8** 宣称 **+28.9% E2E**（#40408）；实验性 **Rust frontend**（#40848/#43283）与 **DP Supervisor**（#40841）。
  - 为什么重要(对我): 长上下文 agent 的 **KV 层级与 MR 路径** 直接影响 **$/token 与 tail latency**；升级前需对照你是否依赖 **connector/disaggregated** 路径（触发 MRv1 fallback）。
  - 明日动作(<=20min): 若你跑 vLLM，打开 release notes **≤5 分钟**，写 **1 条** 与你栈相关的变更（MRv2 / KV offload / batch-invariant 三选一）。

- [Strengthening societal resilience with Rosalind Biodefense（OpenAI Blog，**2026-05-29**）](https://openai.com/index/strengthening-societal-resilience-with-rosalind-biodefense/)
  - 摘要: 推出 **Rosalind Biodefense** 计划（赞助可信开发者用 **GPT‑Rosalind** 构建流行病监测/筛查/对策等防御应用）并扩大 **政府与盟友** 在公共卫生/生物防御任务上的 **受信访问**；强调 July 2025 起对生物学 **High Capability** 模型的 **Preparedness** 分层防护与外部红队。
  - 为什么重要(对我): **垂直 domain agent**（生物防御）的 **trusted access + 评估** 模式可借鉴到你方 **高敏工具/RAG** 的 **分 tier 发布** 设计。
  - 明日动作(<=20min): 用中文写 **≤35 词**：为你当前 agent 标 **1 个** 最接近的「高能力域」及是否已有 **独立 harness+eval**（有/无）。

## 2) 原理深化（Attention/Transformer/Agent）

- [A shared playbook for trustworthy third party evaluations（OpenAI Blog，**2026-05-29**）](https://openai.com/index/trustworthy-third-party-evaluations-foundations/)
  - 摘要: 前沿模型评测结果不再只取决于模型，而取决于 **harness**（工具、状态、重试、预算）；文内区分 **能力 elicitation / 防护鲁棒性 / 对照比较** 三类 claim 及对应 harness；要求报告披露 **reward hacking、refusal、contamination、broken problems、sandbagging** 等有效性检查，并举例 **METR 奖励黑客** 如何把「13 小时 horizon」修正为 **~6 小时**。
  - 为什么重要(对我): **三回合追问链**：(1) 你的 agent eval 测的是 **模型** 还是 **模型+harness**？(2) 若换 **Codex CLI / compaction / 预算**，分数是否仍可比？(3) 你是否在报告里写清 **under-elicitation** 与 **lower-bound**？
  - 明日动作(<=20min): 为你 **1 个** 现有 benchmark 填表：**claim 类型** + **harness 三要素**（工具/预算/有效性检查各 ≤10 词）。

- [vLLM v0.22.0 — batch-invariant inference（release notes，**2026-05-29**）](https://github.com/vllm-project/vllm/releases/tag/v0.22.0)
  - 摘要: **Batch-invariant** 路径新增 Cutlass FP8（#40408，文称 **28.9% E2E**）、SM80 compile-mode（#42456）、NVFP4 Cutlass linear（#39912）；与 **MRv2**、**多层 KV offload** 同版发布，说明推理栈正把 **数值路径不变性** 与 **内存层级** 一并优化。
  - 为什么重要(对我): **三回合追问链**：(1) batch-invariant 对你 **A/B eval** 意味着什么（batch size 是否仍影响 logits）？(2) FP8 路径是否与你的 **量化模型** 一致？(3) 若 agent 依赖 **确定性重放**，是否需在回归集里加 **变 batch** 用例？
  - 明日动作(<=20min): 查你方 vLLM 是否启用 **batch-invariant**；写 **是/否/未知** + **1 个** 若启用时的监控指标名（自定亦可）。

## 3) 热门项目/可试个人项目

- [langgraph-sdk 0.4.0 — `client.threads.stream` 试用（GitHub WHITELIST / LangGraph）](https://github.com/langchain-ai/langgraph/releases)
  - 摘要: 配合已合并的 [#7923](https://github.com/langchain-ai/langgraph/pull/7923)，可用 **thread 中心化** API 消费 v3 流（messages/tool_calls/subgraphs 投影），无需手写 SSE 解析；与本地 [#7935](https://github.com/langchain-ai/langgraph/pull/7935) decoder 模型一致。
  - 为什么重要(对我): 用 **最小脚本** 验证你方 **托管 LangGraph** 流的 **channel 契约**，再决定是否升级生产客户端。
  - 明日动作(<=20min): 在 dev 环境 `pip install -U langgraph-sdk`（或 monorepo editable），对 **1 个** test thread 跑 `threads.stream` 并打印 **2 个** channel 样例事件类型。
  - 估计复现成本: **低**（需已有 LangGraph Server/Cloud 端点与 API key）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [A shared playbook for trustworthy third party evaluations（OpenAI Blog，**2026-05-29**）](https://openai.com/index/trustworthy-third-party-evaluations-foundations/)
  - 机会信号: OpenAI 明确推动 **第三方评测标准化**（harness 披露、有效性检查、reasoning traces），并建议 capability eval 至少以 **Codex** 为 **common floor**——**独立评测/红队/合规咨询** 在 **出海模型上架** 场景需求上升。
  - 风险信号: 若只做 **裸 benchmark 刷分** 而无 harness 文档，客户与监管 **可信度折扣**；**maximum elicitation** 与 **controlled comparison** 混用会 **误导采购**。
  - 我可执行的一步(<=30min): 写 **≤50 词**「你可对外提供的评测 deliverable 清单」（含 harness 说明 + 有效性检查 2 项），并标 **1 个** 目标客户角色（模型方/应用方/监管方 三选一）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：阅读 LeetCode Explore [`Queue & Stack`](https://leetcode.com/explore/learn/card/queue-stack/) 中 **Implement Queue using Stacks**：把「**两个栈模拟队列**」映射为 agent **「延迟输出工具结果」** 的双缓冲。明日动作：用中文写 **≤30 词**：为你 **1 个** streaming 管道标 **push 侧** 与 **pop 侧** 各是什么（各 ≤10 词）。

- **IELTS（≤1 条）**：浏览 [IELTS 官方 — Test format](https://ielts.org/take-a-test/test-format) 中 **Listening** 四部分结构。明日动作：用中文写 **≤35 词**：标 **Section 3**（学术对话）与你 **技术播客精听** 的 **1 个** 可共用练习法（自定）。

## 6) 噪音过滤记录

- **`https://github.com/langchain-ai/langgraph/pull/7935`**（Open 状态）、**`https://github.com/langchain-ai/langgraph/pull/7920`**、**`https://github.com/langchain-ai/langchain/pull/37721`**（Open）、**`https://github.com/run-llama/llama_index/pull/21780`**、**`https://github.com/vllm-project/vllm/pull/43961`**：与 `0529.md` §1 **URL/语义去重**（本日 §1 仅收录 **#7935/#7928/#7927 已合并** 与 **#37721 关闭** 等 **新状态**）。
- **`https://openai.com/index/openai-frontier-governance-framework/`**、**`https://www.anthropic.com/news/claude-opus-4-8`**、**`https://simonwillison.net/2026/May/29/datasette/`**、**`https://www.anthropic.com/news/series-h`**：与 `0528.md`–`0529.md` **URL 去重**。
- **`https://github.com/microsoft/autogen/releases`**：最近 release **2025-09-30**，无 **2026-05-29/30** 可核验信号 → 丢弃。
- **`https://github.com/openai/evals/commits/main`**：最近 commit **2026-04-14**，无当日高信号 → 丢弃。
- **X_WHITELIST / YOUTUBE_WHITELIST 当日帖文**：本环境 **无法稳定抓取原帖 URL/正文** → 不收录。
- **DeepMind Blog 列表页**：无 **2026-05-30** 可核验单篇链接与摘要 → 丢弃（避免目录页当证据）。

## 7) 本周累计计数

- Agent: **197**（`0529.md` §7: 191 + 本日 §1: 6）
- Theory: **77**（`0529.md` §7: 75 + 本日 §2: 2）
- Career/Business: **48**（`0529.md` §7: 47 + 本日 §4: 1）
- LC: **32**（`0529.md` §7: 31 + 本日 §5: 1）
- IELTS: **32**（`0529.md` §7: 31 + 本日 §5: 1）
