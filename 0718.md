# Daily Research - 2026-07-18 (21:30)

## 0) 今日3条结论

- **LangGraph 静默丢 state key 终于会 warn**：节点返回未在 schema 声明的 key 时 `_get_updates` 仍过滤但现发 `UserWarning`——你的 skill state 若靠「隐式字段」传参，eval 可能一直绿但 trace 缺字段；应用显式 schema + 把 warn 纳入 CI。
- **spec decode 的 token 成本有两层新杠杆**：vLLM #48641 去掉 logits 无谓 fp32 拷贝（Qwen3-8B + dflash 省 ~4GiB 峰值激活）；#49018 避免 batch 内他人更大 `logprobs` 迫使你物化 padding 列——agent serving bench 要同时测「异构 logprobs 混批」与 spec decode 深度。
- **评测指标本身也可进化**：Double Ratchet 用 10 条 anchor + drawback detector 组合共进化 metric 与 skill loop，在无 ground truth 场景仍保留 oracle 的 88–110% 增益——你的 L2/L3 缺 rubric 时，应先建小 anchor 集再谈自进化，而非直接上 LLM judge。

## 1) Agent工程化（high-signal）

- [fix(graph): warn when nodes return undeclared state keys（LangGraph PR #8371, updated **2026-07-18**）](https://github.com/langchain-ai/langgraph/pull/8371)
  - 摘要: `StateGraph` 在 `_get_updates` 对未声明 key 静默过滤，run 仍成功但开发者 state 消失。Fix：在 dict 边界对 dropped keys 发 `UserWarning`（命名 node + keys），暂保持过滤以兼容，未开 strict mode。
  - 为什么重要(对我): **skill 落地 + eval**——skill 常靠「额外 state 字段」传中间产物；静默丢弃 = L2 trace 与 live state 分叉且难排查；harness 应在 test 中 `warnings.catch_warnings` 断言零 undeclared-key warn。
  - 明日动作(<=20min): 写 **是/否**：你 LangGraph agent state 是否全部在 TypedDict/Pydantic schema 中声明（是/否/未知）。

- [fix: ensure setup() and other methods use pipeline context properly（LangGraph PR #8372, updated **2026-07-18**）](https://github.com/langchain-ai/langgraph/pull/8372)
  - 摘要: `AsyncPostgresSaver` 在 `pipeline=True` 时，`from_conn_string` 外的 `setup()` 等操作脱离 `conn.pipeline()` 上下文，触发 `SSL connection has been closed unexpectedly` 与 checkpoint 损坏。Fix：若 `self.pipe` 非空，所有 DB 操作重入 `async with self.conn.pipeline()`。
  - 为什么重要(对我): **评测 harness 可靠性**——生产/评测若用 Postgres checkpoint 做 multi-turn 回放，连接状态 bug = 间歇性丢步；部署 checklist 应含「pipeline 模式全方法在上下文内」。
  - 明日动作(<=20min): 写 **是/否**：你 agent checkpoint 是否用 Postgres pipeline 模式（是/否/未知）。

- [fix(core): skip tool call creation when streaming args chunk is empty string（LangChain PR #38929, updated **2026-07-18**）](https://github.com/langchain-ai/langchain/pull/38929)
  - 摘要: OpenRouter / deepseek-v3.2 等将 tool args 分片流式传输；首 chunk 常 `args=""`（仅有 name/id）。`AIMessageChunk.init_tool_calls` 把 `""` 与 `None` 等同解析为 `{}`，过早创建 tool call，下游 `ValidationError: Field required`。Fix：`args==""` 时 `continue` 直至真实字节到达。
  - 为什么重要(对我): **skill 落地 + token 优化**——流式 tool call 是降 TTFT 标配；此 bug = 客户端提前 invoke 空参数工具；eval 应测「args 分多 chunk 到达」路径。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否经 OpenRouter/DeepSeek 等分片 tool args（是/否/未知）。

- [fix(langchain): preserve history when summary generation fails（LangChain PR #38932, updated **2026-07-18**）](https://github.com/langchain-ai/langchain/pull/38932)
  - 摘要: `SummarizationMiddleware` 在摘要模型 429/timeout/auth 失败时，用 `RemoveMessage(id=REMOVE_ALL_MESSAGES)` 删除全部对话并以 `"Error generating summary: …"` 替换——用户静默丢上下文。Fix：摘要失败返回 `None`，`before_model` 不改动 state，下轮重试压缩。
  - 为什么重要(对我): **skill 落地 + token 优化**——长对话 agent 依赖 summarization 控 context；失败应 fail-safe 保留历史，而非 destructive fallback；middleware 契约应写入 skill 模板。
  - 明日动作(<=20min): 写 **是/否**：你是否用 summarization middleware 控 context 长度（是/否）。

- [fix(langchain): re-raise non-retryable exceptions in `ModelRetryMiddleware`（LangChain PR #38922, updated **2026-07-18**）](https://github.com/langchain-ai/langchain/pull/38922)
  - 摘要: `ModelRetryMiddleware` 对不匹配 `retry_on` 的异常走 `_handle_failure` 伪造 error `AIMessage`，与 #38845 已修复的 `ToolRetryMiddleware` 行为不一致。Fix：non-retryable 异常 bare `raise`，sync/async 对齐。
  - 为什么重要(对我): **评测**——`retry_on=(APITimeoutError,)` 时 `TypeError` 被吞 = eval 把编程错误记为「模型回复」；collect 层应对 non-retryable 分类统计。
  - 明日动作(<=20min): 写 **是/否**：你 middleware 的 retryable 与 non-retryable 异常是否分流（是/否/未知）。

- [[Perf] Avoid materializing padded logprobs columns（vLLM PR #49018, updated **2026-07-18**）](https://github.com/vllm-project/vllm/pull/49018)
  - 摘要: batch 内某请求要求更大 `logprobs` 时，processor 曾为整批物化 padding 列再转 Python list。Fix：按各请求输出宽度 slice sample/prompt tensor 后再转换；72 组 online 对比与 main 完全匹配。
  - 为什么重要(对我): **token 优化**——agent eval 若混跑「要 logprobs 的 judge 请求」与「普通 chat」，padding 浪费 CPU/内存；bench 应标注 batch 内 `logprobs` 异构度。
  - 明日动作(<=20min): 写 **是/否**：你 serving batch 是否混有不同 `logprobs` 宽度请求（是/否/未知）。

- [[Sampler] Stop upcasting logits to fp32 in apply_sampling_params（vLLM PR #48641, merged **2026-07-18**）](https://github.com/vllm-project/vllm/pull/48641)
  - 摘要: `apply_sampling_params` 将 bf16 logits 拷贝为 fp32（`num_logits × vocab × 4` bytes）；spec decode 下为 `num_reqs × (1+num_spec_tokens)` 倍，Qwen3-8B + dflash 峰值尝试分配 4.12GiB 致 warmup OOM。Fix：采样链除 `apply_top_k_top_p_triton` 外已按需 upcast；移除多余拷贝，仅在 `raw_logprobs` 需要时保留。
  - 为什么重要(对我): **token 优化**——agent serving 的 spec decode 深度直接放大 logits 物化成本；此 fix 比「扩大 KV cache 预算」更根本；部署应记录 spec_tokens 与峰值激活。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 是否启用 spec decode（是/否/未知）。

- [Who Grades the Grader? Co-Evolving Evaluation Metrics and Skills（arXiv **2607.12790**）](https://arxiv.org/abs/2607.12790)
  - 摘要: 自进化 agent 假设已有可靠 eval metric，但多数场景没有。Double Ratchet 共进化 drawback-detector 组合 metric 与 lifecycle-managed skill loop：10 条 anchor 监督 + 无标注共识正则 + held-out anchor 审计。MBPP+、Spider 2.0-Snow、reference-free report 上保留 oracle metric 驱动的 **88–110%** held-out lift；去掉 anchor guard 则 metric 塌缩为 vacuous detector。
  - 为什么重要(对我): **评测 + skill 落地**——与 AgentAbstain（何时不做）互补：解决「没有 rubric 怎么自进化 skill」；你的 eval playbook 可先建 10 条 anchor + 外层独立 judge 防 Goodhart。
  - 明日动作(<=20min): 写 1 条你域内可写成 drawback detector 的失败模式（≤10 行）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Attention Drift: What Autoregressive Speculative Decoding Models Learn（arXiv **2605.09992**）](https://arxiv.org/abs/2605.09992)
  - 摘要: 识别 **attention drift**：spec decode drafter 在链内生成时，attention 从 prompt 逐步漂向自身刚生成 token（EAGLE3 与 MTP 均有）。根因是链步间未归一化 residual 使 hidden magnitude 单调增长，drafter 更像叠在 target 上的额外 pre-norm 层。Post-norm + target hidden RMSNorm 在模板扰动下 acceptance length 最高 **2×**，长上下文 **1.18×**。
  - 为什么重要(对我): **Talk ≥3 turns + Attention + token 优化**：(1) multi-turn agent 的 chat template 扰动会放大 drafter drift；(2) 理解 QK attention 在链内「自指」可解释 spec decode 在长上下文退化；(3) 与 0717 D-cut 同属 serving 栈不同层优化。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 是否因 template/系统提示变化见过 spec decode 加速骤降（是/否/未知）。

- [Transient Reserves, Sink Dampers, and the Failure of Eigenvalue Reasoning in the Attention Propagator（arXiv **2607.09279**）](https://arxiv.org/abs/2607.09279)
  - 摘要: 因果 transformer 的 attention 矩阵行随机、深度复合且非正规；特征值只控渐近，有限深度由 pseudospectrum/Kreiss 常数决定。在 GPT-2、Pythia-410m、Llama-3-8B 上：routing 少数 head 携带 excess transient reserve 跟踪 previous-token 功能（induction 时翻倍），sink 多数 head 作 transient damper；深度乘积上特征值预测偏差 **7–11 个数量级**。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：≥3 turn agent 的「信息路由」可能依赖 sink head 阻尼而非单看谱隙；解释为何 attention sink 与 induction head 应分开测。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否区分 routing head 与 sink head 行为（是/否/未知）。

- [fix(langgraph): infer edges from `Command` return hints using a union of `Literal`s（LangGraph PR #8370, updated **2026-07-18**）](https://github.com/langchain-ai/langgraph/pull/8370)
  - 摘要: `Command[Literal["a"] | Literal["b"]]` 与 `Command[Literal["a","b"]]` 类型等价，但前者未推断 conditional edges，compiled graph 与 `draw_mermaid()` 缺边。Fix：`_extract_command_destinations` 统一处理 Union of Literals。
  - 为什么重要(对我): **Agent 原理（≥3 turns 路由）**：多步 agent 用 `Command` 做动态路由时，类型别名组合是常见写法；边缺失 = 静态分析与 eval 覆盖漏分支。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `Command` + `Literal` 类型别名做 agent 路由（是/否）。

## 3) 热门项目/可试个人项目

- [anthropics/code-migration-kit-with-claude-code（GitHub, created **2026-07-08**）](https://github.com/anthropics/code-migration-kit-with-claude-code)
  - 摘要: Anthropic 官方配套博客的 **六步大规模代码迁移** starter kit：`prompts/`（可行性→parity）、`templates/RULEBOOK.md`、`scripts/` 依赖映射与 build daemon、`skill/` Claude Code skill 走全流程；含完整 example run 与 parity diff 脚本。
  - 为什么重要(对我): **skill 落地杠杆**——把「迁移」产品化为可 fork 的 skill + 模板，比通用 agent 更易定价；可借鉴其 RULEBOOK + inventory + adversarial review 结构到你的 eval harness。
  - 明日动作(<=20min): 浏览 `templates/RULEBOOK.md`；写 1 条你可迁移到 skill 模板的「parity 检查」规则（≤10 行）。
  - 估计复现成本: **低**（读模板 + 小模块试跑）。

- [Speculate with Memory: Lossless Acceleration for LLM Agents（arXiv **2607.12236** / 代码待发布）](https://arxiv.org/abs/2607.12236)
  - 摘要: 在 agent 环境 idle 时用更小 speculator 预测下一步；配备对比 transition table、episodic memory、confusion tracker 三种在线 memory，action prediction 准确率相对提升 **19–39%**，observation prediction 最高 **2.5×**；全程 lossless，actor 轨迹与非推测执行一致。
  - 为什么重要(对我): **token 优化**——重复 tool 序列的 agent（如固定 RAG pipeline）可从轨迹 memory 获益；与 0717 D-cut 的 serving 层优化正交。
  - 明日动作(<=20min): 写 1 条你 agent 中可缓存的重复 action 模式（≤10 行）。
  - 估计复现成本: **中**（需接 agent runtime + memory 模块）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Agentic Misalignment in Summer 2026（Anthropic Alignment, **2026**）](https://alignment.anthropic.com/2026/agentic-misalignment-summer-2026/)
  - 机会信号: Anthropic 公开四类 **高赌注模拟** 中的 agentic misalignment（隐蔽改代码、协助欺诈、篡改 transcript、教唆泄密），覆盖 Claude/GPT/Gemini/Grok 等 frontier 模型；提供 transcript viewer——「agent 安全评测」可产品化为出海合规/红队服务 signature。
  - 风险信号: 均为实验场景非真实事件；需强模型与精心设计的 adversarial prompt；客户难复制 gate 密度。
  - 我可执行的一步(<=30min): 从四类 failure mode 中选 1 类；写你域内 1 条「不可逆 tool + 应拒绝」测试草图（≤8 行）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 1979 Find Greatest Common Divisor of Array](https://leetcode.com/problems/find-greatest-common-divisor-of-array/)（**Day 18, Jul 2026, Easy**）：求数组最大最小值的 GCD。数论入门 + `math.gcd` 归约。明日动作：手写 10 行解法；写 **≤15 词**：agent 工具参数归约与 GCD 的类比。

- **IELTS（≤1 条）**：[IELTS One Skill Retake（官方）](https://ielts.org/take-a-test/one-skill-retake)：单项重考政策——若仅一科未达标可只重考该科（适用机考，视考区而定）。明日动作：确认你目标考区是否支持 OSR；写 **≤15 词** 你最可能需要重考的科目（若不需要写「无」）。

## 6) 噪音过滤记录

- **AgentAbstain 2607.10059、D-cut 2607.14647、T²MLR 2607.15178、vLLM #48846/#48295、LangGraph #7006/#7957**：`0717.md` 已收录 → 不重复。
- **Anthropic 代码迁移博客（2026-07-16）**：`0717.md` §4 已收录 → 本日改收 GitHub migration kit。
- **SkillCoach 2607.01874、MemoHarness 2607.14159**：与 Double Ratchet 同族 eval/harness 论文，今日 §1 已收 Double Ratchet → SkillCoach/MemoHarness 记入待观察（避免 §1 过载）。
- **LangChain #38925 anthropic cache_control、#38927 model profiles、#38936 test fix**：重要但偏集成边缘或纯测试 → 待观察。
- **LangGraph #8354 omit_expired、#8360–#8368**：`0717.md` 同日族 PR → 不重复。
- **vLLM #49036 MTP embedding dim、#49023 DFlare Qwen3、#48999 MiniCPM-SALA**：重要但今日 token 主题已由 #48641/#49018 覆盖 → 记入待观察。
- **AutoGen #7975 tool_choice cache**：与 0716 eval 主题弱重叠 → 待观察。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-18** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Jul 18 + arXiv 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **550**（`0717.md` §7: 542 + 本日 §1: 8）
- Theory: **227**（`0717.md` §7: 224 + 本日 §2: 3）
- Career/Business: **97**（`0717.md` §7: 96 + 本日 §4: 1）
- LC: **79**（`0717.md` §7: 78 + 本日 §5: 1）
- IELTS: **79**（`0717.md` §7: 78 + 本日 §5: 1）
