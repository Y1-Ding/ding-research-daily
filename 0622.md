# Daily Research - 2026-06-22 (21:30)

## 0) 今日3条结论

- **Hybrid 模型 prefix cache 粒度问题有 RFC 级解法**：**2026-06-22** 更新 [#45702](https://github.com/vllm-project/vllm/issues/45702)——引入 `hash_block_size` 将 **prefix 匹配粒度** 与 **Mamba state block size（如 1024）** 解耦；支持 **partial cache hit + copy-on-write**，使 agent 的 chat turn / tool-call 边界不再被迫对齐 1024-token 粗块。
- **LangChain `create_agent` 同日暴露两类 middleware 图路由/消息完整性 bug**：**2026-06-22** 开 [#38351](https://github.com/langchain-ai/langchain/issues/38351)（`wrap_model_call` 注入 synthetic ToolMessage 后 router 返回 `"model"` 但 path_map 未注册 → `KeyError('model')`）+ [#38352](https://github.com/langchain-ai/langchain/issues/38352)（`SummarizationMiddleware` 用 **set intersection** 而非 **full coverage** 判断 cutoff → orphan ToolMessage 导致 provider 400）。
- **LangGraph 流式可靠性两条独立故障线仍在活跃**：**2026-06-22** [#8112](https://github.com/langchain-ai/langgraph/issues/8112) 作者补充 **seq≥10 时 lexicographic ID 比较会反转**；同日 [#7845](https://github.com/langchain-ai/langgraph/issues/7845) 作者确认 **升级至最新版后 tool-call 协议文本仍会泄漏到用户可见 content**。

## 1) Agent工程化（high-signal）

- [[RFC] Fine-Grained Prefix Cache Hits for Hybrid Models（#45702，updated **2026-06-22**）](https://github.com/vllm-project/vllm/issues/45702)
  - 摘要: Hybrid full-attention + Mamba 模型中，attention 自然 block size（16/32）须对齐 Mamba state block（如 1024），导致 prefix cache 有效粒度变粗、agentic trace 命中率下降。RFC 提出 `hash_block_size` 细粒度 hash 链 + `hit_length` 显式表达可复用 token 数；partial hit 时对 KV/Mamba state 做 **copy-on-write**，避免 in-place 污染。
  - 为什么重要(对我): **长上下文 agent + hybrid 架构** 的 prefix cache 是 **TTFT/成本杠杆**——tool-call 边界不对齐 1024 时，当前实现会 **整段重算**。
  - 明日动作(<=20min): 写 **≤15 词**：你的 agent prompt 平均 turn 长度（<256 / 256-1024 / >1024 / 未知）。

- [create_agent: model_to_tools router 返回 "model" 但 path_map 未注册 → KeyError（#38351，opened **2026-06-22**）](https://github.com/langchain-ai/langchain/issues/38351)
  - 摘要: `factory.py` 中 `model_to_tools_destinations = ["tools", exit_node]` 仅在 `response_format` 或 `loop_exit_node != "model"` 时追加 `loop_entry_node`；但 `_make_model_to_tools_edge` 在 **middleware 一步注入 synthetic ToolMessage**（HITL/cache/replay 模式）时仍会返回 `model_destination` → LangGraph `self.ends[r]` 抛 `KeyError('model')`。同日有 PR [#38355](https://github.com/langchain-ai/langchain/pull/38355)/[#38356](https://github.com/langchain-ai/langchain/pull/38356) 提议最小修复。
  - 为什么重要(对我): **agent middleware 的 router return-set 必须与 conditional_edges path_map 严格对齐**——这是 graph-native agent 的 **静态可验证性** 问题。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `wrap_model_call` 注入 synthetic ToolMessage（是/否/未知）。

- [SummarizationMiddleware 保留 orphan ToolMessage → provider 400（#38352，opened **2026-06-22**）](https://github.com/langchain-ai/langchain/issues/38352)
  - 摘要: `_find_safe_cutoff_point` 在 cutoff 落在 `ToolMessage` 时用 **set intersection** 找配对 `AIMessage`；当同一 run 中部分 `tool_call_id` 的 `AIMessage` 已被 summarize 掉、另一部分仍存活时，**短路匹配** 会留下 orphan `ToolMessage`。下一 turn 调用时 OpenAI 返回 `400: messages with role 'tool' must be a response to...`，Anthropic 返回 `tool_use_id has no matching tool_result`。
  - 为什么重要(对我): **长会话 agent 的 summarization** 是生产标配——orphan 消息是 **静默 400** 类 bug，eval 与线上均难排查。
  - 明日动作(<=20min): 写 **≤15 词**：你的 agent 是否启用 **message summarization middleware**（是/否/计划）。

- [langgraph-runtime-inmem 可恢复流 SSE ID 同毫秒重复（#8112，updated **2026-06-22**）](https://github.com/langchain-ai/langgraph/issues/8112)
  - 摘要: `_generate_ms_seq_id()` 硬编码 `f"{ms}-0"`；token 流高频时同毫秒多事件共享 ID。客户端以 `Last-Event-ID` 重连时，`message.id > message_id` 的 **字符串比较** 会跳过同 ID 的后续帧 → **丢 token**。作者 **2026-06-22** 补充：若仅加 per-ms counter 而不改比较逻辑，seq≥10 时 lexicographic 比较反转（`"…132-2" > "…132-10"` 为 True）会 **重放重复帧**。
  - 为什么重要(对我): **resumable streaming agent UI** 的 exactly-once 语义依赖 ID 设计——毫秒级 token 流是常态。
  - 明日动作(<=20min): 写 **是/否**：你的 agent 前端是否用 `stream_resumable: true`（是/否/未知）。

- [Streaming agents 将 malformed tool-call 协议文本泄漏为用户可见 content（#7845，updated **2026-06-22**）](https://github.com/langchain-ai/langgraph/issues/7845)
  - 摘要: 模型在 tool-call 边界偶发 CJK stray token 时，provider parser 无法识别结构化 tool call，整段 `to=functions.SomeTool {...}` 落入 `AIMessageChunk.content` 并经 `on_chat_model_stream` 推给用户；工具未执行、对话分叉。作者 **2026-06-22** 确认升级至最新 `langgraph + langchain-core + langchain-openai` 后 **仍复现**；相关 PR [#8015](https://github.com/langchain-ai/langgraph/pull/8015) 待 merge。
  - 为什么重要(对我): **生产 agent 的 UX 与安全**——内部 function name/args 泄漏；同时是 **tool 未触发** 的 silent failure。
  - 明日动作(<=20min): 写 **≤15 词**：你的 streaming UI 是否过滤 `to=functions.` 形态文本（是/否/未知）。

- [Checkpoint serialization 丢失 deque maxlen（#8157，opened **2026-06-21**，PR [#8160](https://github.com/langchain-ai/langgraph/pull/8160)）](https://github.com/langchain-ai/langgraph/issues/8157)
  - 摘要: `JsonPlusSerializer` 将 `deque` 与 `set` 同分支编码，仅传 `tuple(obj)`；round-trip 后 `maxlen=None`，bounded deque 变 unbounded，**滑动窗口/内存上限失效**。Reporter 已备 backward-compatible fix + regression test。
  - 为什么重要(对我): agent state 常用 `deque(maxlen=N)` 做 **recent tool results / message window**——checkpoint 后 silently 改变 eviction 行为。
  - 明日动作(<=20min): 写 **是/否**：你的 graph state 是否含 `deque(maxlen=...)`（是/否/未知）。

- [Responses API streaming + nested Pydantic schema 生成无效 strict JSON schema（#38223，updated **2026-06-22**）](https://github.com/langchain-ai/langchain/issues/38223)
  - 摘要: `use_responses_api=True` + `with_structured_output(nested_model)` 在非流式走 `responses.parse`（OK），流式路径手动构建 `text.format` 并 **硬编码 `strict=True`**，且 `_recursive_set_additional_properties_false` **不递归 `$defs`** → OpenAI 400 `additionalProperties is required...`。`astream_events()` 内任何 `.ainvoke()` structured output 均走流式路径。
  - 为什么重要(对我): **LangGraph 节点内 structured output + event streaming** 是 agent eval 常见组合——同 schema 流/非流行为不一致是 **eval 陷阱**。
  - 明日动作(<=20min): 写 **是/否**：你是否在 `astream_events` 内调用 `with_structured_output`（是/否/未知）。

- [[Bug] workflow_as_mcp 共享单 Workflow 实例跨 MCP client（llama_index #22071，opened **2026-06-22**）](https://github.com/run-llama/llama_index/issues/22071)
  - 摘要: `workflow_as_mcp(workflow)` 闭包捕获同一 `Workflow` 实例；各 MCP client 的 `workflow.run()` 共享 `self.*` 可变状态（counter/cache/tenant id）。`AGUIWorkflowRouter` 用 `workflow_factory` 逐请求隔离，但 MCP helper 无等价选项。PR [#22074](https://github.com/run-llama/llama_index/pull/22074) 提议 factory 支持。
  - 为什么重要(对我): **多租户 agent MCP server** 的 state isolation 是安全前提——单实例共享会导致 **跨 client 数据泄漏**。
  - 明日动作(<=20min): 写 **≤15 词**：你是否将 Workflow 暴露为 MCP tool（是/否/计划）。

## 2) 原理深化（Attention/Transformer/Agent）

- [hash_block_size 与 block_size 解耦的 prefix cache 语义（#45702，**2026-06-22**）](https://github.com/vllm-project/vllm/issues/45702)
  - 摘要: `BlockHashListWithBlockSize` 从大 block 的 fine hash 链 **派生** full-block key；partial alias 用同一 boundary hash；`hit_length` 弥补 `len(blocks)*block_size` 对 partial hit 的高估。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) hash 链 fingerprint 的是 **prefix 还是 block 内容**？(2) partial hit 的 COW 对 Mamba recurrent state vs KV slice 有何不同？(3) agent turn 边界作为 cache key 时，**最优 hash_block_size** 如何选？
  - 明日动作(<=20min): 画 **1 条** hash 链：`H2→H4→H6(partial H8)`，标 COW 点（各 ≤3 框）。

- [Agent 图 conditional edge：router return-set ⊆ path_map（#38351，**2026-06-22**）](https://github.com/langchain-ai/langchain/issues/38351)
  - 摘要: `create_agent` 的 post-model router 与 `add_conditional_edges` path_map **独立构造**，framework 不 enforce subset 关系；middleware 可改变 router 输出而不改变 destinations 列表。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) agent factory 应在 **编译期还是运行期** 验证路由完备性？(2) synthetic ToolMessage 一步满足全部 tool_calls 时，为何应回 **model 而非 tools**？(3) 与 LangGraph `Command(goto=...)` 跳转的 **交互契约** 是什么？
  - 明日动作(<=20min): 写 **≤20 词**：你 agent 图的 post-model 边有几个 destination（2/3/更多/未知）。

- [Resumable SSE ID：毫秒-序列 vs 字符串序（#8112，**2026-06-22**）](https://github.com/langchain-ai/langgraph/issues/8112)
  - 摘要: Redis Stream 风格 `ms-seq` ID 需 **单调唯一**；纯字符串 `>` 比较在 seq 变两位数后 **字典序≠数值序**；作者建议 zero-pad 或 `(int(ms), int(seq))` tuple 比较。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) event ID 应表达 **wall clock 还是 logical sequence**？(2) token 流同毫秒密度下 seq 上界如何估计？(3) reconnect 语义应是 **at-least-once 还是 exactly-once**？
  - 明日动作(<=20min): 写 **≤15 词**：你更接受 replay 丢帧还是重复帧（丢/重复/都要避免）。

- [Checkpoint serde 对 bounded collection 语义的保真（#8157，**2026-06-21**）](https://github.com/langchain-ai/langgraph/issues/8157)
  - 摘要: `deque`/`set` 共用 encode 分支丢失 `maxlen`；silent failure 改变 **eviction policy** 而非抛错。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) agent state 的 **capacity bound** 应在 serde 层显式编码还是约定？(2) round-trip 后 behavior change 与 **crash** 哪个更危险？(3) 与 #7714 checkpoint bloat 的 **联合优化** 空间？
  - 明日动作(<=20min): 写 **≤10 词**：你 state 里 bounded 结构类型（deque/list/其他）。

## 3) 热门项目/可试个人项目

- [llama_index workflow_as_mcp factory 隔离（#22071 + PR #22074，**2026-06-22**）](https://github.com/run-llama/llama_index/pull/22074)
  - 摘要: 将 `workflow_as_mcp` 扩展为接受 `workflow_factory: Callable[[], Workflow]`，每 MCP 请求构造新实例；修复多 client 共享 `self.history`/`self.call_count` 的 tenant 泄漏。
  - 为什么重要(对我): **个人 MCP agent 原型** 可在本地用两 client 并发调用验证 isolation。
  - 明日动作(<=20min): 读 PR diff 中 factory 签名；写 **≤10 词** 你是否需要 per-request Workflow（是/否）。
  - 估计复现成本: **低**（`llama-index-tools-mcp` + 文中 CountingWorkflow 示例）。

- [sqlite-utils 4.0rc1：migrations + nested transactions（Simon Willison Blog，**2026-06-21**）](https://simonwillison.net/2026/Jun/21/sqlite-utils-40rc1/)
  - 摘要: `sqlite-utils` v4 RC 内置 `Migrations`（源自 sqlite-migrate）与 `db.atomic()` savepoint 嵌套事务；agent 本地 **observability/eval 数据** 可用 schema migration 管理而不手写 DDL。
  - 为什么重要(对我): **agent eval 结果持久化** 常需轻量 DB——migration 让 schema 演进可版本化。
  - 明日动作(<=20min): `uvx --with sqlite-utils==4.0rc1 sqlite-utils --help`；写 **≤10 词** 你是否用 SQLite 存 eval（是/否）。
  - 估计复现成本: **低**（`pip install sqlite-utils==4.0rc1` + 官方 migrations 示例）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Temporary Cloudflare Accounts for AI agents：`npx wrangler deploy --temporary`（Simon Willison Blog，**2026-06-21**）](https://simonwillison.net/2026/Jun/21/temporary-cloudflare-accounts/)
  - 机会信号: 无需 Cloudflare 账号即可 `wrangler deploy --temporary`，部署 **60 分钟 ephemeral Workers**；Simon 用 Codex 生成 HTTP redirect follower 并验证可用。**AI solo company** 可零摩擦 demo agent tool/API 给潜在客户，claim 后可转为持久项目。
  - 风险信号: 60 分钟过期需及时 claim；临时 URL 不适合生产 SLA；依赖 Cloudflare 生态绑定。
  - 我可执行的一步(<=30min): 读 Simon 文中 temporary deploy 流程；写 **≤20 词** 你是否有可包装为 Worker 的 agent tool（是/否/候选）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 622. Design Circular Queue](https://leetcode.com/problems/design-circular-queue/)：固定容量环形队列 ↔ LangGraph #8157 的 `deque(maxlen)` 语义——实现后写 **≤15 词**：`maxlen` 满时 append 行为（丢弃头/拒绝/未知）。

- **IELTS（≤1 条）**：[British Council — IELTS Academic Writing Task 2 sample](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-practice-tests/academic-test/writing)：官方 Essay 样题。明日动作：写 **Introduction 1 句 + 1 个 body topic sentence**（≤20 min）；写 **≤15 词** 自我评分弱项（论证/衔接/语法 选 1）。

## 6) 噪音过滤记录

- **vLLM KV offload race/lifecycle/stalled transfer/min_tokens/CuTeDSL warmup**：`0621.md` **URL/语义去重** → 无 **2026-06-22** 新 merge/review 信号 → 不重复展开。
- **vLLM #46366 cohere2_moe KeyError `layers.0.mlp.down_proj.weight`（v0.22.1+）**：单模型 regression（CohereLabs/North-Mini-Code），通用 agent 信号弱 → 丢弃。
- **vLLM #46367 GLM-5.2-FP8 weird outputs on 8xB200**：硬件/模型专项，当日无 root-cause → 丢弃。
- **vLLM #46364 long audio resample int32 overflow**：multimodal audio edge-case，agent 主线弱 → 丢弃。
- **LangGraph #7714 checkpoint bloat、#8094 usage_metadata 丢失、#7985 MCP ToolNode TypeError**：无 **2026-06-22** 新进展 → 丢弃。
- **LangChain #38339 Groq performance tier**：`0621.md` 已收录 → 丢弃。
- **Anthropic / OpenAI / DeepMind / Lil'Log**：无 **2026-06-22** agent 工程直接新 post → 丢弃。
- **Simon MCP auth gateway（2026-06-19）**：`0620.md` 已收录 → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取原帖 → 不收录。

## 7) 本周累计计数

- Agent: **354**（`0621.md` §7: 346 + 本日 §1: 8）
- Theory: **150**（`0621.md` §7: 146 + 本日 §2: 4）
- Career/Business: **72**（`0621.md` §7: 71 + 本日 §4: 1）
- LC: **54**（`0621.md` §7: 53 + 本日 §5: 1）
- IELTS: **54**（`0621.md` §7: 53 + 本日 §5: 1）
