# Daily Research - 2026-08-15 (21:30)

## 0) 今日3条结论

- **ATR prompt-injection eval 进入 openai/evals 主线**（#1657, updated Aug 14）：16 条多语言样本（paraphrase / language-switch / indirect doc / tool-context injection）+ modelgraded safety，源自 Apache-2.0 的 Agent Threat Rules（330 rules，SKILL.md 上 96.9% recall）→ **你的 L3 eval 可直接 fork 这套 probe 格式，把 skill 落地场景映射到 ATR rule id**。
- **MCP skill 工具链「schema 层」集中补齐**（LlamaIndex #22550 / #22692 / #22688, updated Aug 15）：`JsonSchemaToPydantic` 无需 ClientSession 即可把 MCP tool schema 转 Pydantic；`FunctionTool` 修复 NumPy/paren-less Google docstring 解析与 `default_factory` 每次调用冻结 → **skill 评测须覆盖「schema 生成」与「default_factory 可变默认值」两类 silent drift**。
- **vLLM attention 路径三条并行修复**（#52440 / #52436 / #52419, updated Aug 15）：CUDA Graph replay 对 attention metadata 地址 fail-closed；DSpark adaptive verification + structured output 在 `draft_budget==0` 时 crash；EAGLE partial-hash-hit 路径 cache 注册被 block-align 截断 → **agent serving golden 集须分 path 覆盖：cudagraph replay / spec-decode+grammar / prefix-cache hit**。

## 1) Agent工程化（high-signal）

- [Add atr_prompt_injection eval (modelgraded safety, 16 multilingual samples)（openai/evals PR #1657, updated **2026-08-14**）](https://github.com/openai/evals/pull/1657)
  - 摘要: 新增 `atr_prompt_injection` eval：从开源 [Agent Threat Rules (ATR)](https://github.com/Agent-Threat-Rule/agent-threat-rules) 抽取 16 条 prompt/indirect injection 样本，覆盖 paraphrase、德/中/日/西语切换、fictional/academic framing、文档注入、tool-context 注入；modelgraded 判定模型是否拒绝。ATR 330-rule corpus 在 SKILL.md 对抗样本上测得 96.9% recall，已被 Cisco AI Defense skill-scanner 等采用。
  - 为什么重要(对我): **评测 + skill 落地**——skill/MCP agent 的 L3 eval 需要标准化 injection probe；ATR 是可直接复用的开源 rule corpus + eval 格式。
  - 明日动作(<=20min): 写 **是/否**：你现有 eval 是否覆盖 indirect tool-context injection（是/否/未知）。

- [feat(tools-mcp): expose JSON Schema → Pydantic conversion without a client（LlamaIndex PR #22550, updated **2026-08-15**）](https://github.com/run-llama/llama_index/pull/22550)
  - 摘要: Closes #22510。提取 `JsonSchemaToPydantic` 独立类，无需构造 `McpToolSpec` + `ClientSession` 即可将 `mcp.types.Tool.inputSchema` 转为 Pydantic model；`McpToolSpec` 内部委托给该实例。解决「已有 raw tool schema、只需类型转换」场景的 accidental coupling。
  - 为什么重要(对我): **skill 落地**——MCP skill 开发/测试常只需 schema→Pydantic，不必拉起完整 MCP client；降低 skill 单测与 eval fixture 搭建成本。
  - 明日动作(<=20min): 写 **1 条**：你现有 MCP tool 测试中，哪些步骤可用 `JsonSchemaToPydantic` 替代完整 client（≤20 词）。

- [fix(core): support numpy, paren-less google params and multiline descriptions in FunctionTool（LlamaIndex PR #22692, updated **2026-08-15**）](https://github.com/run-llama/llama_index/pull/22692)
  - 摘要: Fixes #22687。`FunctionTool.extract_param_docs` 原仅识别带括号的 Google 风格（`query (str):`），NumPy 风格与无括号 Google 参数丢失 description；多行描述被截断为 `"One of:"` 等残缺短语。Fix 系统解析 Google（含 `param: desc`）、NumPy `Parameters` 段，并收集缩进续行。
  - 为什么重要(对我): **skill 落地 + 评测**——tool schema 中 param description 是 agent 选参依据；silent 丢失 = eval 对比 tool call 时 model 看到的 schema 与开发者意图不一致。
  - 明日动作(<=20min): 写 **是/否**：你 FunctionTool/MCP tool 的 docstring 是否用 NumPy 或无括号 Google 风格（是/否/未知）。

- [fix(core): FunctionTool evaluates Field(default_factory=...) once per tool instead of per call（LlamaIndex PR #22688, updated **2026-08-15**）](https://github.com/run-llama/llama_index/pull/22688)
  - 摘要: `FunctionTool.__init__` 用 `call_default_factory=True` 在构造时一次性物化所有 `Field(default_factory=...)`，后续每次 `call()` 复用同一 frozen dict。后果：`uuid4()` 类 factory 三次调用返回同一 job_id；mutable default（如 `[]`）跨调用共享。Fix 改为每次调用时执行 factory。
  - 为什么重要(对我): **skill 落地 + 评测**——带动态默认参数（request id、timestamp、nonce）的 tool 在 eval 中会 silent 复用同一值，trace 去重与并发测试全部失真。
  - 明日动作(<=20min): 写 **是/否**：你 tool 定义是否用 `Field(default_factory=...)`（是/否/未知）。

- [fix(deepseek): map `prompt_cache_hit_tokens` to `cache_read`（LangChain PR #39668, updated **2026-08-15**）](https://github.com/langchain-ai/langchain/pull/39668)
  - 摘要: Closes #39637。DeepSeek 在 `usage` 顶层返回 `prompt_cache_hit_tokens` / `prompt_cache_miss_tokens`，无 `prompt_tokens_details`；`ChatDeepSeek` 委托 OpenAI base class 映射时 cache read 被丢弃。DeepSeek cache hit 计费约为 miss 的 1/10，丢失字段 = cost 报表系统性低估 savings。
  - 为什么重要(对我): **token 优化 + 评测**——多 provider agent 的 L2 cost 门禁须覆盖 DeepSeek cache 字段；与 0813 Anthropic cache 修复形成「全 provider cache 计量」拼图。
  - 明日动作(<=20min): 写 **是/否**：你 cost 报表是否读 DeepSeek `prompt_cache_hit_tokens`（是/否/未知）。

- [fix: feat(autogen-core): add gen_ai.agent.action_ref to trace_tool_span（AutoGen PR #8057, updated **2026-08-15**）](https://github.com/microsoft/autogen/pull/8057)
  - 摘要: Fixes #7850。在 `trace_tool_span()` 增加 `gen_ai.agent.action_ref`——基于 action class + actor + JCS-normalized consequential input 的确定性 SHA-256 handle，用于跨 producer 审计关联。现有 `gen_ai.tool.name` / `call.id` 仅 producer-scoped，消费者无法独立重算 correlation key。
  - 为什么重要(对我): **评测 + skill 落地**——multi-agent eval 跨 runtime 对比 trace 时，需要 recomputable action identity；对齐 OTel GenAI semantic conventions 方向。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval trace 是否用 recomputable action id（是/否/未知）。

- [fix(checkpoint): include state path in serialization error（LangGraph PR #8624, updated **2026-08-15**）](https://github.com/langchain-ai/langgraph/pull/8624)
  - 摘要: Fixes #8606。`JsonPlusSerializer.dumps_typed` 编码失败时，原错误仅报 offending type，不报在 graph state 中的路径（如 `$."outer".level1[1]."level2"."bad"`）。失败仅在 checkpoint 时 surfaced，距产生坏值的 node 已远。Fix 增加 `_find_unsupported_path` 递归 walk（Pydantic v2 fields、dataclass、dict、sequence）。
  - 为什么重要(对我): **skill 落地 + 评测**——复杂 nested state agent 的 eval debug 常卡在 checkpoint 序列化；无 path = 手动 walk state 找 culprit，eval 迭代成本高。
  - 明日动作(<=20min): 写 **是/否**：你 graph state 是否含 nested Pydantic/dataclass（是/否/未知）。

- [fix(checkpoint-postgres): avoid shared async lock for pooled async savers（LangGraph PR #7269, updated **2026-08-14**）](https://github.com/langchain-ai/langgraph/pull/7269)
  - 摘要: Fixes #7259。`AsyncConnectionPool` 模式下 async Postgres checkpointer 不再用 instance-level shared lock；单 connection 仍保留 lock。Benchmark：pool-aware 1310 ops/sec vs legacy serialized 484 ops/sec，**2.71×** 吞吐提升。220 tests pass on pgvector Postgres 15/16。
  - 为什么重要(对我): **Agent 工程化**——高并发 agent eval / serving 若用 pooled Postgres checkpoint，legacy lock 是隐性瓶颈；吞吐翻倍直接影响 eval 周转。
  - 明日动作(<=20min): 写 **是/否**：你是否用 LangGraph `AsyncConnectionPool` + Postgres checkpointer（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[MRV2][CUDA Graph] Guard attention metadata addresses before replay（vLLM PR #52440, updated **2026-08-15**）](https://github.com/vllm-project/vllm/pull/52440)
  - 摘要: CUDA Graph replay 捕获 attention metadata 的 tensor `data_ptr()`；本 PR 在 DEBUG 模式对 nested attention metadata 做 snapshot，replay 前校验地址不变，缺失或变更则 fail-closed。覆盖 model runner、autoregressive prefill/decode/fused、DFlash、Multi-Module MTP 路径；PIECEWISE replay 与非 DEBUG 不变。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：cudagraph 复用 attention metadata 指针；地址漂移 = silent wrong attention output，agent tool loop 逐步劣化难归因。
  - 明日动作(<=20min): 写 **≤15 词**：你是否启用 vLLM CUDA Graph replay（是/否/未知）。

- [[Bugfix][Spec Decode][Structured Output] DSpark: fix grammar bitmask when draft budget is zero（vLLM PR #52436, updated **2026-08-15**）](https://github.com/vllm-project/vllm/pull/52436)
  - 摘要: DSpark adaptive verification（#47808）+ structured outputs 时，当 controller 选 `draft_budget=0`（drafter confidence 低、验证成本不值得时**常规选择**），`assert num_masks == len(mapping)` crash。Fix 处理 zero-draft 步的 grammar bitmask mapping。
  - 为什么重要(对我): **Talk ≥3 turns + Spec Decode/Attention**：adaptive verification 每步 draft 数动态变化；与 grammar-constrained decode 的 attention mask 须对齐，zero-draft 非边缘 case。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 DSpark + structured output（是/否/未知）。

- [[Bugfix][Spec Decode] Keep EAGLE cache registration on partial-hash-hit path（vLLM PR #52419, updated **2026-08-15**）](https://github.com/vllm-project/vllm/pull/52419)
  - 摘要: #50062 后 EAGLE branch 用 `floor(n/scheduler_block_size)*scheduler_block_size + manager.block_size` 重算 bound，在 fine-grained partial hash hit 路径把 registration 截断到 block 对齐边界，tail tokens 不再注册。Fix 恢复 raw token count bound。
  - 为什么重要(对我): **Talk ≥3 turns + KV Cache/Attention**：prefix cache hit 后 EAGLE draft 的 KV 注册不完整 = spec decode attention 读错 cache，多轮 agent 首 token 后质量断崖。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 EAGLE spec decode + prefix caching（是/否/未知）。

- [[Model] Support ModelOpt FP8_PB_WO in Kimi-K3 attention（vLLM PR #52406, updated **2026-08-15**）](https://github.com/vllm-project/vllm/pull/52406)
  - 摘要: Kimi-K3 MLA/KDA attention 支持 ModelOpt `FP8_PB_WO` 权重：fused projection 输出 pad 到 128-row FP8 block 粒度后 trim；MLA weight absorption 对标准 2D block-scale layout 直接 dequantize。覆盖 homogeneous/mixed ModelOpt algorithms + TP=8 shapes。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/MLA**：Kimi-K3 类长 context agent serving 走 MLA；FP8 block padding 与 absorption 路径错误 = attention 数值 silent drift。
  - 明日动作(<=20min): 写 **≤15 词**：你 serve 的模型是否用 MLA + FP8 ModelOpt（是/否/未知）。

- [How Claude's text watermarking works（Anthropic, **2026-08-14**）](https://www.anthropic.com/news/claude-text-watermark)
  - 摘要: 未来 Claude 文本输出将含 SynthID-Text watermark（Google DeepMind Nature 2024）：在「多候选词等价」的 low-stakes 选择点，用水印 key + 前文决定随机源（替代默认 RNG），读者不可感知、不增 token、不影响质量。EU AI Act Code of Practice 要求标记 AI 生成内容；全球 launch（暂无法按区域 scope）。代码等精确输出场景 watermark 稀疏；将提供 detection API。
  - 为什么重要(对我): **Talk ≥3 turns + 生成原理**：watermark 作用于 softmax 后采样随机性源——与 attention→logits→sampling 链路直接相关；agent eval 若比较「逐 token 一致性」须知晓 watermark 改变 low-entropy 选择。
  - 明日动作(<=20min): 写 **≤15 词**：你的 eval 是否假设同 prompt 可复现相同 token 序列（是/否）。

## 3) 热门项目/可试个人项目

- [docs: add an agent discovery example using MCP（LlamaIndex PR #22681, updated **2026-08-15**）](https://github.com/run-llama/llama_index/pull/22681)
  - 摘要: 新增 `docs/examples/tools/agent_discovery.ipynb`：agent 从任务出发，运行时查询公开 registry 发现第三方 agent（非预置 tool list）；对返回 candidate 按 trust score / verified / transaction count / flags 做 vetting 后推荐。无 hard-code，用 `BasicMCPClient` + `aget_tools_from_mcp_url`。
  - 为什么重要(对我): **Agent 工程化 + skill 落地**——MCP agent discovery 是 skill 生态下一层（发现→信任→调用）；可直接 fork 为「skill registry eval」原型。
  - 明日动作(<=20min): clone notebook，写 **1 条** 你可替换的 registry URL 或 trust 规则（≤20 词）。
  - 估计复现成本: **低**（跑 notebook + 换 registry，~30min；生产 trust gate 集成 **中**）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [How Claude's text watermarking works — EU AI Act 合规与全球 launch（Anthropic, **2026-08-14**）](https://www.anthropic.com/news/claude-text-watermark)
  - 机会信号: Anthropic 全球 rollout SynthID-Text watermark + 将提供 detection API → **「AI 内容合规检测 / watermark 验证」** 可做成 B2B 服务；与 C2PA 文件 metadata 并列，形成 text+media 双轨合规工具链。190+ 签署方 Code of Practice 意味着同类需求将扩散到所有 major provider。
  - 风险信号: watermark 可被重写去除；小样本检测置信度低；全球 launch 因无法区域 scope——商业化边界与法律责任仍模糊。
  - 我可执行的一步(<=30min): 读 Anthropic 文中「limitations」段，写 **3 条** 可做成 eval/SOP 的「watermark 检测置信度边界」（各 ≤20 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[3354. Make Array Elements Equal to Zero](https://leetcode.com/problems/make-array-elements-equal-to-zero/)（**Day 45, Aug 2026 Daily Challenge**；承接 0813 的 3352 Hard，今日 **Easy**「从 `nums[i]==0` 出发选方向，遇正数减 1 并反向，求能使全数组归零的起始方案数」）：关键洞察——前缀和 `l` 与后缀和 `s-l` 比较：`l*2==s` 则双向均可（+2），`|l*2-s|==1` 则朝较大侧（+1）。明日动作：手写 O(n) 单遍前缀和版；写 **≤15 词**：「双向游走+翻转」与 agent trace「遇障碍反向重规划」的类比。

- **IELTS（≤1 条）**：[IELTS Listening Practice Tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/listening)：0813 已轮换 Writing，今日换 **Listening**（on-computer 六类题型各练 1 组，含 audio + transcript）。明日动作：完成 1 组 Listening，记录 **≥3 个** 拼写/数字陷阱；写 **≤15 词** 你最弱的 Listening 题型。

## 6) 噪音过滤记录

- **LangGraph #8623 uuid6 thread-safety**：infra fix，与 skill/eval focus 弱相关 → 过滤。
- **LangGraph #8619 CLI uv export**：build 优化 → 过滤。
- **LangGraph #8617 Store TypedDict**：类型宽化，信号弱于 #8624 path error → 过滤。
- **LangGraph #8622 interrupt loop warn**：有用但信号弱于 #8624 checkpoint debug → 过滤（记入 Agent 备选）。
- **LangChain #39670 model-profiles refresh**：自动化数据 → 过滤。
- **LangChain #39666 callback dispatch**：tracing fix，信号中等但 #39668 cache 更贴 token focus → 过滤。
- **LangChain #39669 Fireworks rerank**：集成向，非当前 focus → 过滤。
- **LlamaIndex #22694 CohereRerankRelevancyMetric**：eval 相关但属默认 model 过期 fix，信号弱于 ATR eval → 过滤。
- **LlamaIndex #22691 anthropic default model**：配置 fix → 过滤。
- **AutoGen #8058 Enum structured output**：边缘 provider fix，信号弱于 #8057 trace → 过滤。
- **AutoGen #8059 HttpTool _from_config**：有用但单点 bug，信号弱于 MCP schema 三连修 → 过滤。
- **vLLM #52445/#52441 Kimi/Gemma4**：model-specific，非 Attention 通用 → 过滤。
- **Simon Willison Aug 14 "Don't classify. Hallucinate!"**：RAG tagging 技巧，与当前 focus 弱相关 → 过滤。
- **Simon Willison Aug 8 auto mode default**：Aug 14 已生效，但原帖 Aug 8、`0811.md` 周期可能已覆盖 → 不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-08-14/15** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + Anthropic Aug 14 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **734**（`0813.md` §7: 726 + 本日 §1: 8）
- Theory: **330**（`0813.md` §7: 325 + 本日 §2: 5）
- Career/Business: **120**（`0813.md` §7: 119 + 本日 §4: 1）
- LC: **102**（`0813.md` §7: 101 + 本日 §5: 1）
- IELTS: **102**（`0813.md` §7: 101 + 本日 §5: 1）
