# Daily Research - 2026-06-09 (21:30)

## 0) 今日3条结论

- **长时程 agent 的三件套正在成型**：OpenAI 官方把 **Skills（可挂载 SOP）+ 托管 Shell（真执行环境）+ Server-side Compaction（自动压上下文）** 作为一组原语发布；对你意味着 **eval/planning 应测「跨 20+ 步」连续性**，而非只看单轮 tool call 成功率。
- **并行工具调用的安全边界比「工具名碰撞」更隐蔽**：LangGraph **#7989** 显示单个 tool 可 **伪造 sibling `tool_call_id` 的 `ToolMessage`** 且 **不抛异常**——多工具并行时需在框架层做 **call-id 绑定校验**，否则状态图会被「串台」污染。
- **Agent 感知层出现「屏幕即传感器」路线**：Simon Willison 评 WWDC 2026 称 Apple 新 Siri 用 **Vision LLM 读屏** 绕过 per-app 集成；这与 **agent-native infrastructure**（传感器/执行器分解）方向一致，但 **prompt injection + 屏幕读数** 的组合风险仍需观望实测。

## 1) Agent工程化（high-signal）

- [Shell + Skills + Compaction: Tips for long-running agents that do real work（OpenAI Developers Blog）](https://developers.openai.com/blog/skills-shell-tips)
  - 摘要: OpenAI 发布 **长时程 agent 三原语**：**(1) Skills**（对齐 Agent Skills 开放标准，目录 + `SKILL.md`，模型按 `name/description` 路由加载）；(2) **升级版 Shell tool**（托管容器，可装依赖、跑脚本、写 `/mnt/data` 产物）；(3) **Server-side compaction**（Responses API 内自动压缩上下文，或显式 `/responses/compact`）。官方建议 **技能描述写清「何时用/何时不用」+ 负例**、**容器复用 + `previous_response_id` 续跑**、**网络 allowlist 双层（org + request）+ `domain_secrets` 防凭证泄露**。
  - 为什么重要(对我): 你的 **agent 工程栈** 若仍靠 **巨型 system prompt**，在 **>30 步任务** 上会 brittle；Skills 把 **稳定流程外置为版本化 bundle**，Compaction 把 **checkpoint 从手工删消息** 变成 **平台能力**——直接对标你的 **planning/RAG/eval 长链路**。
  - 明日动作(<=20min): 为你 **1 个** 长任务 agent，用中文写 **≤30 词**：哪些步骤应进 **Skill**（可复用 SOP）vs 留在 **system prompt**（一次性约束）；标 **是否已有 compaction 策略**（是/否/未知）。

- [Siri AI at WWDC 2026（Simon Willison Blog，**2026-06-08**）](https://simonwillison.net/2026/Jun/8/wwdc/)
  - 摘要: Simon 对 Apple WWDC 2026 **持「见到再信」态度**，但认为新 Siri 在技术上 **更可行**：**(1)** 授权 **定制 Gemini 衍生模型** 跑在 **Private Cloud Compute**；(2) 用 **Vision LLM 从用户屏幕提取信息**，**无需每个 app 单独集成 Apple Intelligence**；(3) 发布 **Core AI / `coreai-torch`**，把 **PyTorch `ExportedProgram`** 映射到 Apple 硬件。Simon 引用 Apple Security Research：**最重负载（含 agentic tool-use）的 PCC 扩展到 Google Cloud + NVIDIA GPU**，仍声称保持隐私架构。
  - 为什么重要(对我): 这是 **「agent 读屏 = 通用传感器」** 的产品化信号——对你做 **RAG/工具编排** 的启示是：**UI 状态可能变成非结构化观测**，parser/HITL 要预留 **视觉通道**；同时 **Lethal Trifecta**（私有数据 + 不可信内容 + 外传）在 OS 级 agent 上会更尖锐。
  - 明日动作(<=20min): 画 **≤4 框** 数据流：`Screen → Vision LLM → Tool/Action → External`，标 **1 个** 你可控的 **exfiltration 切断点**（≤15 词）。

- [langchain-core==1.4.2：废弃 problematic `dict()` 方法（GitHub Release，**Published 2026-06-08**）](https://github.com/langchain-ai/langchain/releases/tag/langchain-core%3D%3D1.4.2)
  - 摘要: **`langchain-core 1.4.2`** 合并 **#31685**：`Serializable.dict()` 与 Python 内置 **`dict` 类型注解冲突**；新增 **`asdict()`**（dataclasses 风格）并逐步 **deprecate `dict()`**。PR 于 **2026-06-08** merge，影响 `prompts` / `output_parsers` / `llms` / `chat_models` 等基类序列化路径。
  - 为什么重要(对我): agent 栈里大量 **middleware、checkpoint、trace** 会 **`.dict()` 序列化消息**；升级后若 CI 仍调用旧 API，会在 **类型检查 + 运行时 deprecation** 双杀——应在 **eval 管线与自定义 middleware** 中统一搜 **`\.dict\(\)`** 迁移到 **`asdict()`**。
  - 明日动作(<=20min): 在你 **1 个** agent 仓库跑 `rg '\.dict\(\)' --glob '*.py' | head -5`，记 **命中文件数**（数字即可）；若有命中，标 **是否 LangChain 对象**（是/否/未查）。

- [Fix host memory leak from undrained `new_block_ids`（vLLM PR #44490，**Reporter 确认修复 2026-06-08**）](https://github.com/vllm-project/vllm/pull/44490)
  - 摘要: **V1 调度器** 在 **无 Mamba 层** 的 attention 模型上，**`new_block_ids` 只追加不 drain** → **host RSS 线性增长**（报告：12h **+163.8 MiB** 无平台期）；修复为 **每 scheduler step 无条件 `take_new_block_ids()`**。Reporter **@ashgold** 于 **2026-06-08** 回复：**内存泄漏已解决**；但 **CPU 从 1.3 降到 1 导致延迟上升** 为 **另一独立问题**。
  - 为什么重要(对我): 自托管 **vLLM agent 推理** 若跑 **高 QPS + prefix caching + max_tokens=1**（如路由/分类），会在 **数小时级** 踩 **宿主内存泄漏**——与模型精度无关，是 **调度器不变量** 问题；升级前应用 **VmRSS 监控** 对照该 PR。
  - 明日动作(<=20min): 若你用 vLLM V1，查部署 **是否 enable prefix caching + 短 max_tokens**（是/否）；写 **≤20 词** 你的 **RSS 监控** 现状（有/无/仅 GPU）。

- [ToolNode accepts ToolMessage results bound to sibling `tool_call_id` values（langgraph #7989，**Updated 2026-06-06**）](https://github.com/langchain-ai/langgraph/issues/7989)
  - 摘要: 并行 tool call 时，**`bad_tool` 可在返回列表中夹带 `tool_call_id="call_good"` 的伪造 `ToolMessage`**，`good_tool` 再返回真结果 → 下游出现 **两条 `call_good`**，**无异常**。作者已开 **#7997** 草案（仅校验 **top-level `ToolMessage`**）；**`Command(update={"messages":[...]})` 路径仍可走私 sibling 消息**（社区 repro 已贴，待 follow-up issue）。
  - 为什么重要(对我): 这是 **多 agent / 多插件工具聚合** 的 **权限串台**：恶意或 buggy tool 可 **提前污染 sibling 观测**，导致 **planning 基于假 tool 输出**——比 **#7988 工具名覆盖** 更难察觉（无 crash、ID 看起来合法）。
  - 明日动作(<=20min): 为你 **1 个** 并行 `ToolNode`，写 **≤25 词**：当前是否校验 **返回 `ToolMessage.tool_call_id` == 当前 invoke id**（是/否/未查）；若否，标 **最高风险工具**（1 个名字）。

- [langgraph-api 0.9.0: validator rejects `stream_mode` "tools"/"lifecycle"（#7986，**Opened 2026-06-02**）](https://github.com/langchain-ai/langgraph/issues/7986)
  - 摘要: 自托管 **`langgraph-api==0.9.0`** 的 **bundled `openapi.json`** 中 **`RunCreateStateful.stream_mode` enum 缺少 `"tools"` / `"lifecycle"`**，但 **`schema.StreamMode` 类型与 runtime 均支持** → 请求校验失败，**`useStream` 的 `toolProgress` 在 self-hosted 上断裂**。复现者定位 validator 读的是 **wheel 内 `site-packages/openapi.json`**，非源码树。
  - 为什么重要(对我): **Agent 前端可观测性**（tool 进度条 / lifecycle 事件）在 **LangGraph Cloud vs 自建** 间会出现 **「类型能编、网关拒收」** 的 **契约漂移**——部署自建 API 时需把 **OpenAPI enum 与 SDK `StreamMode`** 纳入 **冒烟测试**。
  - 明日动作(<=20min): 若你自建 LangGraph API，查当前版本 **是否 0.9.0**（是/否/未知）；尝试 `stream_mode: ["tools"]` **是否 400**（测/未测），结果 **1 词**。

- [Testing Agent Skills Systematically with Evals（OpenAI Developers Blog）](https://developers.openai.com/blog/eval-skills)
  - 摘要: OpenAI 建议用 **`codex exec --json`** 抓取 **JSONL 事件流**（含 `command_execution`），对 skill 路由与执行做 **确定性断言**；可用 **`--output-schema`** 约束最终 JSON 评分。强调 **`SKILL.md` frontmatter 的 `name`/`description` 是路由主信号**——模糊描述会导致 **skill 不触发或误触发**。
  - 为什么重要(对我): 把 **agent eval** 从「看最终答案」推进到 **「可重放事件日志 + schema 评分」**——与你 **training/eval** 轨道直接对齐；可抄 **最小 harness**：跑 agent → 记 JSONL → 3 条 deterministic checks。
  - 明日动作(<=20min): 为你 **1 个** skill/SOP，写 **≤20 词** `description` 草稿，含 **1 条「何时不要用」** 负例（自拟）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Vision LLM 作为跨应用 Agent 传感器（Simon Willison / WWDC 2026，**2026-06-08**）](https://simonwillison.net/2026/Jun/8/wwdc/)
  - 摘要: 传统 **App Intents** 需每应用暴露结构化 API；**读屏 + Vision LLM** 把 **像素/布局** 当作 **统一观测空间**——类似 **多模态 Transformer** 把 **图像 token 与文本 token 对齐到同一决策头**，但 **观测噪声与对抗内容**（屏幕上的注入文本）成为新攻击面。
  - 为什么重要(对我): **三回合追问**：(1) 你的 agent **观测是结构化 API 还是原始 UI**？(2) **Attention 落在屏幕何处** 可否被第三方内容劫持？(3) **HITL** 是否在 **「读屏结论」→「执行动作」** 之间？
  - 明日动作(<=20min): 用中文写 **≤35 词**：对比 **「API 工具」vs「视觉读屏」** 各 **1 个** 失败模式（自定）。

- [并行 Tool Call 的「消息绑定」不变量（langgraph #7989）](https://github.com/langchain-ai/langgraph/issues/7989)
  - 摘要: 单轮 **AIMessage** 含多个 `tool_calls` 时，每个 tool 的 **返回应只终止自己的 `tool_call_id`**；否则 **状态更新算子** 在 **fan-out/fan-in** 图上产生 **非确定性合并**——与 **Transformer 多头 attention 各头应只写各自 KV 槽** 同类：**共享缓冲区无隔离 → 串扰**。
  - 为什么重要(对我): 设计 **multi-tool agent** 时，**ToolMessage 不是普通文本**，而是 **带索引的结构化侧信道**；eval 应加 **「并行 call 串台」** 对抗用例（一个 tool 故意返回 sibling id）。
  - 明日动作(<=20min): 写 **≤4 条** 伪代码断言：`assert all(tm.tool_call_id == current_id for tm in tool_returns)`（中文注释每行 ≤8 字）。

- [Compaction 作为「上下文记忆」的工程近似（OpenAI Responses API）](https://developers.openai.com/blog/skills-shell-tips)
  - 摘要: **Server-side compaction** 在上下文超阈值时 **自动压缩历史**，避免 agent **手工删消息**；这与 **「1M context ≠ memory」** 论点互补——**长窗口** 解决 **单次可见性**，**compaction** 解决 **跨步保留任务相关摘要**（仍非结构化长期记忆）。
  - 为什么重要(对我): 你的 **talk ≥3 turns** 场景里，应区分 **(a) 窗口内全量历史**、**(b) 压缩摘要**、**(c) 外部 store** 三层；否则会把 **compaction 摘要** 误当 **ground truth** 做 planning。
  - 明日动作(<=20min): 画 **三层栈**（窗口 / 压缩 / Store），各写 **1 个** 你 agent 里对应字段或模块名（≤12 词/层）。

## 3) 热门项目/可试个人项目

- [用 `codex exec --json` + `--output-schema` 做 Skill 回归（OpenAI Developers Blog）](https://developers.openai.com/blog/eval-skills)
  - 摘要: 官方示例：**跑 skill → JSONL 记 `command_execution` → deterministic checks**；可选 **第二遍 grader** 用 schema 约束评分。适合把 **个人 SOP**（如「发布前检查清单」）变成 **可 CI 的 skill**。
  - 为什么重要(对我): **低成本个人项目**：无需训练模型，只需 **目录 + SKILL.md + 3 条断言** 即可得到 **可复现 agent 回归**。
  - 明日动作(<=20min): 选 **1 个** 你每周重复 ≥2 次的任务，写 **skill 名 + 1 条可观测成功标准**（≤25 词）。
  - 估计复现成本: **低**（CLI + 本地目录；无 GPU）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Why Your AI Agent Stack Will Break at 10 Agents（Indie Hackers，**CAREER_BUSINESS_WHITELIST**）](https://www.indiehackers.com/post/why-your-ai-agent-stack-will-break-at-10-agents-and-what-to-do-about-it-314969caed)
  - 机会信号: 从业者共识：**更多 agent ≠ 更多能力**；demo **95–98%** 可靠性到生产常降至 **80–87%**，延迟 **1–3s → 10–40s**。成功团队靠 **显式状态管理、day-1 可观测、最小权限、关键节点 HITL、复用优于冗余**——对 **AI 一人公司** 即 **先建治理再扩 agent 数量**。
  - 风险信号: 文发布于 **2026-04-11**，引用 Gartner/UC Berkeley 等 **需自行核验**；**「10 个」阈值** 是经验框架非定律。
  - 我可执行的一步(<=30min): 列出你当前 **≤10 个** 「agent/自动化」（名字即可），标 **哪一个缺 observability**（1 个）；写 **≤20 词** 明天要加的 **1 条 metric/log**。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 208. Implement Trie (Prefix Tree)](https://leetcode.com/problems/implement-trie-prefix-tree/)：为 **工具路由/registry** 练 **前缀树插入/搜索/startsWith**——与 **#7988 工具名碰撞**、**#7989 call-id 绑定** 对照：Trie 管 **命名空间路由**，ID 校验管 **并行边界**。明日动作：实现 `insert/search/startsWith` 后，用中文写 **≤30 词**：**`startsWith("pay_")` 能否发现 `pay_refund` 与 `pay_delete` 共存风险**（是/否 + 理由）。

- **IELTS（≤1 条）**：[IELTS 官方 — IELTS on computer (Writing on Paper)](https://ielts.org/take-a-test/test-types/ielts-on-computer-writing-on-paper)：**2026 年中起** 多数市场 **机考为主**；部分市场可选 **Writing on Paper**（题目在屏幕、答案手写）。**One Skill Retake** 须与原始考试 **同 delivery mode**（选手写则重考也须手写 Writing）。明日动作：查目标考区是否提供 **Writing on Paper**（官网 1 次），用中文写 **≤35 词**：对你 **Writing 备考** 意味着练 **打字还是手写计时**（二选一 + 1 理由）。

## 6) 噪音过滤记录

- **`#44875`（streaming parser engine）**、**`#37960`（reasoning_content）**、**`#7988`（duplicate tool names）**、**`#44879`（fp8 KV MTP）**、**`datasette-agent-edit`**：与 `0608.md` **URL/语义去重**。
- **`#7990/#8022`（图级 context）**：`0608.md` 已覆盖；**#8022** 于 **2026-06-08** 因 **external contributor 未 assign** 自动关闭 → 本日不重复叙事，仅作流程注记。
- **Anthropic Opus 4.8 / Dynamic workflows GA**：发布于 **5 月下旬–6 月初**，近一周日报已多轮跟进 → 丢弃。
- **Karpathy Sequoia Ascent 2026 演讲**：优质但 **非 6/8–6/9 新内容** → 丢弃。
- **X_WHITELIST / YOUTUBE_WHITELIST / Bilibili**：本环境无法稳定抓取原帖 → 不收录。
- **Indie Hackers「5 workflows making money」等**：收入数字难独立核验 → 改用 **可执行治理框架** 文。
- **veerakarthick235/AI-Leetcode-Solver-Agent**：非 GITHUB_WHITELIST，且涉及 **自动刷题** 合规风险 → 丢弃。

## 7) 本周累计计数

- Agent: **256**（`0608.md` §7: 249 + 本日 §1: 7）
- Theory: **101**（`0608.md` §7: 98 + 本日 §2: 3）
- Career/Business: **59**（`0608.md` §7: 58 + 本日 §4: 1）
- LC: **41**（`0608.md` §7: 40 + 本日 §5: 1）
- IELTS: **41**（`0608.md` §7: 40 + 本日 §5: 1）
