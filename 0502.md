# Daily Research - 2026-05-02 (21:30)

> 采集口径：优先 **BLOG_WHITELIST / GITHUB_WHITELIST / YOUTUBE_WHITELIST / CAREER_BUSINESS_WHITELIST**；评分 relevance(40)+actionability(25)+credibility(20)+freshness(15)，正文主段 ≥70（职业池 ≥65 且具可执行信号）。**非白名单主链条目 1 条**（§3 `deepagents` 仓库，`[OUTSIDE_WHITELIST]`，约占主链 **<20%**）。

## 0) 今日3条结论

- **Harness 正在「模型族化」**：LangChain 在 **2026-04-29** 的 [Tuning Deep Agents](https://www.langchain.com/blog/tuning-deep-agents-different-models) 中把 **Deep Agents** 从「一套通用 prompt/tools」升级为 **按 OpenAI / Anthropic / Google 的 harness profile** 自动切换（含工具名对齐如 `apply_patch` / `shell_command`），并在 **tau2-bench 子集** 上给出 **+10pt 量级** 的对比表——说明 **eval 差异主要来自 harness，而不是只换模型名**。
- **编排层仍在修「流式状态」边角**：**LangGraph `1.2.0a5`（2026-05-01）** 修复 **`_messages_delta_reducer` 对 dict/str 写入的归一化**（见 [release notes](https://github.com/langchain-ai/langgraph/releases/tag/1.2.0a5)），这类 bug 会表现为 **checkpoint / 消息归并** 偶发错乱，适合作为你排查「图状态不一致」时的优先怀疑点。
- **合规截止线在逼近**：LangChain **2026-04-27** 的 [EU AI Act 交叉解读](https://www.langchain.com/blog/langsmith-langchain-oss-eu-ai-act) 明确写出 **欧盟《人工智能法》高风险条款主要合规截止日为 2026-08-02**；若你做 **面向欧盟客户的 B2B agent**，需要把 **trace / 在线 eval / HITL** 从「工程最佳实践」提升为 **可举证控制措施**。

## 1) Agent工程化（high-signal）

- [langgraph==1.2.0a5](https://github.com/langchain-ai/langgraph/releases/tag/1.2.0a5)（`langchain-ai/langgraph`，published **2026-05-01**）
  - 摘要: 相对 1.2.0a4：修复 **`_messages_delta_reducer` 在写入为 `dict`/`str` 时的强制归一化**（#7680）；并含 alpha 线版本对齐说明。
  - 为什么重要(对我): 消息归约器影响 **增量写入 + checkpoint 回放**；异常写入会导致 **工具消息顺序/合并** 与预期不一致，排查难度高。
  - 明日动作(<=20min): 若你用自定义 `messages` reducer 或 `StreamChannel` 写消息，检查是否存在 **把 `ToolMessage`/`dict` 混写入同一条目** 的路径，并加一条单测覆盖。

- [langchain==1.3.0a1](https://github.com/langchain-ai/langchain/releases/tag/langchain%3D%3D1.3.0a1)（`langchain-ai/langchain`，published **2026-05-01**）
  - 摘要: 初版 alpha：**`create_agent` 接入 `stream_events(version='v3')`**；**core 层 `stream_events` v3 协议**；**HITL middleware 增加 `respond` 决策**（#37095）；release body 同时汇总了大量近期 agent/middleware 变更链接。
  - 为什么重要(对我): **流式协议 v3** 与 **人机回路的显式 `respond`** 直接关系 **可观测性、调试与线上审批** 三类生产问题。
  - 明日动作(<=20min): 在 staging 把 `stream_events` 切到 **v3** 跑一次最小用例，记录 **事件类型序列** 与你当前 UI/日志解析是否兼容。

- [langchain-openrouter==0.2.3](https://github.com/langchain-ai/langchain/releases/tag/langchain-openrouter%3D%3D0.2.3)（`langchain-ai/langchain`，published **2026-05-01**）
  - 摘要: 修复流式场景下 **`reasoning_details` 片段合并**（#36401）。
  - 为什么重要(对我): 推理链/思维链若被 **切碎**，下游 **eval、轨迹可视化、缓存键** 会全部失真。
  - 明日动作(<=20min): 若你用 OpenRouter + reasoning 模型，抓一条 trace，确认 **`reasoning_details` 是否在最终 message 上完整可解析**。

- [Tuning Deep Agents to Work Well with Different Models](https://www.langchain.com/blog/tuning-deep-agents-different-models)（LangChain Blog，**2026-04-29**）
  - 摘要: 推出 **按模型族的 harness profile**；给出 **tau2-bench 子集** 上 **GPT 5.3 Codex / Opus 4.7** 的「基座 harness vs profile」表格（文内数字：**33%→53%**、**43%→53%**）；并解释 Codex 侧主要改 **工具形态（`apply_patch`、`shell_command` 别名）** 与 **并行批量读/搜** 的 prompt 片段。
  - 为什么重要(对我): 你用 **Deep Agents / 自建 harness** 时，「换模型」应同步 **版本化 profile**，否则 eval 不可比。
  - 明日动作(<=20min): 为你线上在用的 **2 个模型 id** 各写 3 行 **profile 差异清单**（工具名、禁止并行与否、是否强调先读文件）。

- [Profiles（Deep Agents 官方文档）](https://docs.langchain.com/oss/python/deepagents/profiles)（LangChain Docs）
  - 摘要: 说明 **`deepagents>=0.5.4`** 起 **HarnessProfile / ProviderProfile** 的字段面：**prompt 前后缀、工具排除、middleware 排除、子 agent**；强调 **harness profile 与 provider profile 分工**；以及 **YAML 插件分发**（entry points）。
  - 为什么重要(对我): 把「提示词热改」变成 **可审计的配置工件**，便于 **A/B 与回滚**。
  - 明日动作(<=20min): 读 **Merge semantics** 表格，写下你对 **`excluded_middleware` 取并集** 的一条风险（例如误删安全中间件）及对应治理动作。

- [How LangSmith and LangChain OSS Help You Meet EU AI Act Requirements](https://www.langchain.com/blog/langsmith-langchain-oss-eu-ai-act)（LangChain Blog，**2026-04-27**）
  - 摘要: 以 **2026-08-02** 截止为锚点，交叉解读 **Art. 9/10/12/13/14/15/72** 与 **全链路 trace、线上 evaluator、LangGraph interrupt、标注队列、留存策略**；并给出 **LangSmith EU（`eu.smith.langchain.com`）** 数据驻留选项与 trace 留存档位说明。
  - 为什么重要(对我): **出海 B2B** 时，客户尽调常问「日志、人在回路、事故监测」——此文是可引用的 **能力映射表**。
  - 明日动作(<=20min): 打开文尾 **Article crosswalk 表**，只抄与你产品相关的 **2 条 Article**，各写一句你已具备/缺口。

## 2) 原理深化（Attention/Transformer/Agent）

- [Gemma Scope 2: helping the AI safety community deepen understanding of complex language model behavior](https://deepmind.google/blog/gemma-scope-2-helping-the-ai-safety-community-deepen-understanding-of-complex-language-model-behavior/)（Google DeepMind Blog，**2025-12-19**）
  - 摘要: 发布面向 **Gemma 3 全尺寸** 的可解释性工具包 **Gemma Scope 2**（SAE / transcoder 等），并给出 **Neuronpedia 交互演示**、**Hugging Face 权重**、技术报告 PDF；文中明确动机包括 **jailbreak、幻觉、谄媚** 等行为审计，以及 **链式思维忠实度** 分析工具。
  - 为什么重要(对我): 做 **工具调用 agent** 时，「可见输出」与「内部表征」不一致是 **安全与 eval** 的共同敌人；开源 interpretability 栈是 **可复现诊断** 的基础。
  - 明日动作(<=20min): 读 **What's new** 小节，记下 **chat-tuned** 版本与 **refusal** 分析工具的一句话定位，评估你是否需要 **离线审计管线**。

- [Measuring progress toward AGI: A cognitive framework](https://deepmind.google/blog/measuring-progress-toward-agi-a-cognitive-framework/)（Google DeepMind Blog，**2026-03-17**）
  - 摘要: 提出 **10 项认知能力**（含 **Attention**、**Executive functions**、**Metacognition** 等）与 **三阶段评测协议**（模型任务集、人类基线、相对人类分布映射）；并启动 **Kaggle hackathon** 以补齐 **learning / metacognition / attention / executive / social** 等 **eval gap**（文内给出奖金与时间安排）。
  - 为什么重要(对我): 把你的 **agent eval** 从「单一榜单分」对齐到 **可分解认知维度**，更利于 **找短板**（例如 planning vs tool reliability）。
  - 明日动作(<=20min): 选一个你正在优化的 agent 任务，把它映射到文中 **10 维里的 2 维**，各写 **1 条可量化代理指标**。

- [LLM 0.32a0 is a major backwards-compatible refactor](https://simonwillison.net/2026/Apr/29/llm/)（Simon Willison Blog，**2026-04-29**）
  - 摘要: **LLM** 库 alpha：**输入升级为 message 序列**；**输出为分类型 streaming parts**（文本/工具调用/推理等）；提供 **`response.to_dict()` / `Response.from_dict()`** 序列化；并讨论 **CLI** 对 reasoning token 的展示与 **`-R/--no-reasoning`**。
  - 为什么重要(对我): 这是「**对话 turn + 工具 + 推理**」成为默认接口后，**最小应用层抽象应该如何长得像 OpenAI messages** 的参考实现。
  - 明日动作(<=20min): 若你自研 agent 客户端，对照文中 `stream_events()` 分支，列出你当前解析器 **尚未覆盖的事件类型**。

## 3) 热门项目/可试个人项目

- [deepagents](https://github.com/langchain-ai/deepagents) `[OUTSIDE_WHITELIST]`
  - 摘要: LangChain **Deep Agents** 上游仓库；与 [Tuning Deep Agents](https://www.langchain.com/blog/tuning-deep-agents-different-models) / [Profiles 文档](https://docs.langchain.com/oss/python/deepagents/profiles) 配套，可 fork 后 **自定义 harness profile** 并做 **tau2-bench 子集回归**。
  - 为什么重要(对我): 用 **同一任务集** 验证「profile 切换」相对「只换 endpoint」是否真提升，而不是体感。
  - 明日动作(<=20min): 在本地 `uv add deepagents`（见博文命令），跑通 `create_deep_agent` **默认 profile** 的最小脚本，打印 **最终 tool 列表名** 是否与你的 Codex/Claude 习惯一致。
  - 估计复现成本: **中**（依赖模型 key + 评测集可选）

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Anthropic and NEC collaborate to build Japan’s largest AI engineering workforce](https://www.anthropic.com/news/anthropic-nec)（Anthropic Newsroom，**2026-04-24**）
  - 机会信号: **NEC 作为 Anthropic 首个日本本土全球伙伴**、面向约 **3 万** NEC 员工推广 **Claude / Claude Code / Cowork**，并共建 **金融/制造/地方政府** 等行业方案——说明 **日系大企业采购** 与 **本地化交付** 仍是可验证的落地路径（非仅模型发布）。
  - 风险信号: 新闻不等于 **对你个人的 headcount**；跨国交付仍受 **数据出境、行业监管、语言/流程** 约束。
  - 我可执行的一步(<=30min): 用「**Claude + 制造业/金融 RAG**」为关键词，整理 **3 条** 你可复用的 **垂直场景假设**（客户角色、数据形态、合规门槛各一行）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**: 打开 [LeetCode 周赛日历](https://leetcode.com/contest/)，预约下一场 **Virtual**；结合今日 **LangGraph reducer** 主题，练一题时在注释里写清 **状态合并不变量**（类比消息归约）。

- **IELTS（≤1 条）**: 精读 [IELTS One Skill Retake](https://ielts.org/take-a-test/booking-your-test/one-skill-retake) 官方页（是否覆盖你的考试类型/地区以页面为准），用 **30 秒** 记下你目标考区是否可选 **OSR** 与前提条件。

## 6) 噪音过滤记录

- **OpenAI / Anthropic 首页**：截至采集时 **无新的 2026-05-02 当日帖**；**未重复** 展开 `0501.md` 已写的 Advanced Account Security / ParseBench 等（**语义去重**）。
- **a16z / Indie Hackers（CAREER_BUSINESS_WHITELIST）**：本次 **未抓到可用的一手长文链接**（站点返回/导航不稳定），**不硬写**。
- **YouTube 白名单频道**：未核验到 **带精确视频 URL** 的条目，遵守「无链接不收」。
- **X 白名单账号**：无稳定可引用的 **单帖 URL**，已丢弃。
- **vLLM / llama_index / autogen / evals**：**无早于本周、且未被前文覆盖** 的新 release 可增信（`v0.14.21`、`v0.20.0` 等已在往期或较旧），故不占主条。

## 7) 本周累计计数

- Agent: **42**（`0501.md` §7: 36 + 本日 §1: 6）
- Theory: **16**（`0501.md` §7: 13 + 本日 §2: 3）
- Career/Business: **9**（`0501.md` §7: 8 + 本日 §4: 1）
- LC: **5**（`0501.md` §7: 4 + 本日 §5: 1）
- IELTS: **5**（`0501.md` §7: 4 + 本日 §5: 1）

---

**非白名单占比**: 主链显式 `[OUTSIDE_WHITELIST]` **1 / 16 ≈ 6.3%**（仅 §3 `deepagents`），**≤20%**。
