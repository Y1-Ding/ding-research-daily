# Daily Research - 2026-06-14 (21:30)

## 0) 今日3条结论

- **vLLM 在 0.23.0 后首日落地「Dynamic SD」**：**2026-06-14** merge 的 [#32374](https://github.com/vllm-project/vllm/pull/32374) 按 **batch size + 位置级 acceptance rate** 动态调节 speculative decoding 的 **K 值**——高负载时 **K↓ 防 TPOT 恶化**，RL rollout 尾段 **K↑ 吃剩余吞吐**；对你自托管 agent backend，**spec decode 不再是静态超参**，应纳入 **负载感知 eval**。
- **PyPI 正式接纳 Pyodide WASM wheels，浏览器 agent 依赖链解锁**：Simon Willison **2026-06-13** 实测 **Pyodide 314.0** 已支持维护者直接向 PyPI 发布 **`pyemscripten_*_wasm32`** 轮子（[PEP 783](https://peps.python.org/pep-0783/)）；作者用 **GPT-5.5 xhigh + Codex** 打包首发 **`luau-wasm`**——与 `0601.md` **Pyodide ASGI**、`0606.md` **MicroPython WASM 沙箱** 形成 **「零后端 agent 栈」完整供应链**。
- **Managed Agents 补齐「定时跑 + vault 注入 env」**：Anthropic API Release Notes **2026-06-09** 宣布 **cron 调度 session** 与 **vault 环境变量凭证**（[docs](https://docs.anthropic.com/en/release-notes/api)）——长时程 agent 的 **secrets 不进 sandbox、调度不自建** 成为平台原生能力；与 Box **middleware 并行摘要**、OpenAI **Dreaming 后台记忆** 同属 **「状态与凭证外置」** 工程范式。

## 1) Agent工程化（high-signal）

- [[V1][Spec Decode] Add Dynamic SD（#32374，merged **2026-06-14**）](https://github.com/vllm-project/vllm/pull/32374)
  - 摘要: 实现 **Dynamic SD (DSD)**：spec decode 验证阶段有效 batch = **BS×K**，当 **BS×K** 超临界值时 **TPOT 反而变差**；本 PR 据 **运行时 batch size** 与 **position-level acceptance rate** 动态选 **最优 K**；覆盖 **高负载同部署** 与 **RL rollout 尾部长尾请求** 两类场景；对应 issue [#4565](https://github.com/vllm-project/vllm/issues/4565) Milestone 2–3。
  - 为什么重要(对我): 你若用 vLLM **自托管 agent 推理**，spec decode 曾需 **手工调 K**——DSD 把其变成 **serving 层自适应策略**；RL/agent 批量生成时 **尾段吞吐** 与 **在线 QPS** 可共用一套部署。
  - 明日动作(<=20min): 写 **是/否** 启用 vLLM spec decode；若是，记当前 **K 是固定还是可配**（固定/动态/未用）。

- [[Core] Simplify MRV2 async output handling（#45442，merged **2026-06-14**）](https://github.com/vllm-project/vllm/pull/45442)
  - 摘要: **Model Runner V2** 不再在 runner 内根据 async scheduling 解析 **`AsyncModelRunnerOutput` → `ModelRunnerOutput`**；**executor 层** 已按上游需求处理输出类型，runner **只产一种输出**。
  - 为什么重要(对我): `0613.md` 刚强调 **0.23.0 默认 MRv2**——此简化降低 **async agent streaming** 路径上的 **类型分支 bug 面**；升级后若遇 **tool streaming 卡死**，优先查 **executor 与 MRv2 版本组合**。
  - 明日动作(<=20min): 若用 vLLM，写 **≤15 词**：你的 agent 请求走 **sync / async streaming**（二选一）；标 **MRv2 是否开启**（是/否/未知）。

- [[BugFix] Fix prompt_embeds for multimodal models（#45383，merged **2026-06-14**）](https://github.com/vllm-project/vllm/pull/45383)
  - 摘要: 修复 **`LLM(..., enable_prompt_embeds=True).generate({"prompt_embeds": ...})`** 在多模态模型（如 **Gemma3**）上 **CUDA index-out-of-bounds → EngineDeadError**；根因之一是 multimodal 路径 **覆盖用户提供的 prompt_embeds**。
  - 为什么重要(对我): **RAG/agent 预计算 embedding** 绕 tokenization 是常见优化——多模态 + embeds 组合若 **静默崩溃 EngineCore**，会破坏 **整批 agent 请求**；应在 eval 中加 **embeds-only smoke**。
  - 明日动作(<=20min): 写 **是/否** 向 vLLM 传 **prompt_embeds**；若是，标模型是否 **多模态**（是/否）。

- [[Security] Fix DoS via prompt_embeds on M-RoPE models（#45252，merged **2026-06-13**）](https://github.com/vllm-project/vllm/pull/45252)
  - 摘要: **`prompt_token_ids=None` + 仅 `prompt_embeds`** 时，旧逻辑 **`_init_mrope_positions` fatal assert → EngineCore 崩溃**；现从 **embeddings tensor 推导 seq len** 并用 dummy token IDs；对应 **GHSA-33cg-gxv8-3p8g**。
  - 为什么重要(对我): 对外暴露 **`/v1/completions` + embeds** 的 agent API 若可被 **恶意 embeds 触发 core 崩溃**，等同 **未鉴权 DoS**——与 `0613.md` **file-search 路径逃逸** 同属 **不可信输入硬化** 批次。
  - 明日动作(<=20min): 写 **≤20 词**：你的推理 API 是否接受 **`prompt_embeds` 无 token ids**（是/否/未暴露）。

- [[Security] Add timeout guard for regex compilation in structured output（#45118，merged **2026-06-13**）](https://github.com/vllm-project/vllm/pull/45118)
  - 摘要: 新增 **`compile_regex_with_timeout()`**（默认 **5s**，`VLLM_REGEX_COMPILATION_TIMEOUT_S`，**0=禁用**）；应用于 **xgrammar / outlines** 等 structured output 后端的 regex 编译，防 **ReDoS**；对应 **GHSA-rwxx-mrjm-wc2m**；含 **6 个单元测试**。
  - 为什么重要(对我): agent **JSON schema / tool schema** 若允许用户提交 **regex 约束**，无 timeout 的编译可导致 **worker 挂死**——这是你 **structured output 网关** 的必查项。
  - 明日动作(<=20min): 查你是否用 vLLM **guided/structured output**（是/否）；若是，记 **regex 是否用户可控**（是/否）。

- [Claude Managed Agents：scheduled deployments + vault env credentials（API Release Notes，**2026-06-09**）](https://docs.anthropic.com/en/release-notes/api)
  - 摘要: **Managed Agents** 新增：**(1) cron 调度 session**，无需自建 scheduler；**(2) vault 支持环境变量凭证**，可安全注入 sandbox 供 CLI/SDK 认证；同日发布 **Fable 5 / Mythos 5**（1M context、adaptive thinking、refusal classifiers）；`session.thread_*` webhook 增 **`session_thread_id`**。
  - 为什么重要(对我): 你若做 **「定时巡检 agent」或「凭证隔离的长跑 agent」**，不必自建 **K8s CronJob + K8s Secret 挂载**——平台提供 **调度 + vault 注入**；Fable 召回后更应把 **Opus 4.8 + Managed Agents** 当作 **fallback 栈** 评估。
  - 明日动作(<=20min): 写 **≤25 词**：你 agent 的 **secrets** 目前是 **env 直挂 / vault 代理 / 无**（三选一）；标 **是否有定时任务需求**（有/无）。

- [Publishing WASM wheels to PyPI for use with Pyodide（Simon Willison Blog，**2026-06-13**）](https://simonwillison.net/2026/Jun/13/publishing-wasm-wheels/)
  - 摘要: **Pyodide 314.0** 起维护者可像 native wheel 一样向 PyPI 发布 **`pyemscripten_*_wasm32`** 包（[PyPI PR 4 月落地](https://github.com/pypi/warehouse/pull/...)）；作者用 **Codex + GPT-5.5 xhigh** 打包 **`luau-wasm` 0.1a0**（276KB wheel）；BigQuery 查 PyPI 公开数据集称当前约 **28 个** wasm 平台包（含 **pydantic_core、onnx、typst** 等）。
  - 为什么重要(对我): **浏览器内 agent** 不再依赖 Pyodide 官方 **300+ 手工维护包**——你可 **pip install 自有 WASM 扩展** 到 Datasette Lite / Pyodide ASGI 栈，**边缘 demo 依赖闭环**。
  - 明日动作(<=20min): 打开 [Pyodide REPL](https://pyodide.org/en/stable/console.html)，试 **`pip install luau-wasm`**（成功/失败/未试）；写 **1 个** 你想 wasm 化的依赖名（≤10 字符）。

- [Run long horizon tasks with Codex（OpenAI Developers，[OUTSIDE_WHITELIST]）](https://developers.openai.com/blog/run-long-horizon-tasks-with-codex)
  - 摘要: OpenAI 工程师用 **GPT-5.3-Codex Extra High** 在空仓库 **连续跑 ~25 小时**、**~13M tokens**、产出 **~30k LOC** 设计工具；强调 **spec + milestone 验收 + tests/lint/typecheck 每步验证**；引用 **METR 长任务基准 ~7 月翻倍** 与 Cursor **scaling agents** 经验；核心论点：**agent 竞争力 = time horizon，不单是单次 IQ**。
  - 为什么重要(对我): 你的 **eval harness** 若只测 **单轮 tool call**，会 **低估 GPT-5.3-Codex 类模型的生产价值**——应加 **≥1 小时不间断任务 + 中断恢复** 场景。
  - 明日动作(<=20min): 写 **≤20 词**：你最长 **单次 agent 跑** 记录（分钟数或「未测」）；列 **1 个** 适合 30min+ 的 eval 任务（≤12 词）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Scaling Managed Agents: Decoupling the brain from the hands（Anthropic Engineering，**2026-04-08**）](https://www.anthropic.com/engineering/managed-agents)
  - 摘要: Managed Agents 将 agent 虚拟化为 **session（append-only 事件日志）/ harness（调 Claude + 路由 tool）/ sandbox（执行环境）** 三接口；**brain 与 hands 解耦** 后 container 变 **cattle**——崩溃可 **`wake(sessionId)` 从最后事件恢复**；**vault + auth proxy** 使 **OAuth/MCP 凭证不进 sandbox**；session 作 **context object**，`getEvents()` 可按位置切片，**compaction 与可恢复存储分离**。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) 长时程 agent 的「记忆」应存在 **session 日志** 还是 **模型 context**？(2) **prompt injection** 读 env 与 **vault 外置凭证** 如何类比 **Attention mask**？(3) 你的 multi-agent 是否 **brain/hands 可独立扩缩**？
  - 明日动作(<=20min): 画 **3 框**（session / harness / sandbox）并标你栈中 **各对应 1 个组件名**（各 ≤3 词，可写「无」）。

- [Run long horizon tasks with Codex：time horizon vs intelligence（OpenAI Developers，[OUTSIDE_WHITELIST]）](https://developers.openai.com/blog/run-long-horizon-tasks-with-codex)
  - 摘要: 长时程 agent **失败模式**常非「不够聪明」而是 **中途丢线程、少验证、过早宣布完成**；25h 实验靠 **`plans.md` milestone + `implement.md` runbook + 持续 verification** 保持连贯；METR 显示 frontier agent **50%/80% 可靠完成任务长度 ~7 月翻倍**。
  - 为什么重要(对我): 类比 **Transformer**：**长上下文**解决「看得见」，**harness 状态机**解决「记得住、做得完」——eval 需同时测 **context 长度** 与 **跨 checkpoint 一致性**。
  - 明日动作(<=20min): 写 **≤25 词**：你 agent 是否有 **「增量交付 + 下一会话可读 artifact」** 机制（有/无 + 1 个 artifact 名若有可能）。

- [Mapping SQLite result columns back to source `table.column`（Simon Willison Research，**2026-06-13**）](https://simonwillison.net/2026/Jun/13/sqlite-column-provenance/)
  - 摘要: 目标：对任意 SQL（含 **JOIN/CTE**）把结果列映射回 **`table.column`** 以在 Datasette 渲染溯源；用 **Claude Code Opus 4.8**（Fable 不可用）探索 **apsw / ctypes 调 `sqlite3_column_table_name()` / EXPLAIN 推断** 等路径；代码见 [research/sqlite-column-provenance](https://github.com/simonw/research/tree/main/sqlite-column-provenance#readme)。
  - 为什么重要(对我): **Agent RAG 可解释性**：SQL agent 返回列时，**列级 provenance** 是 **citation 的 Attention 源定位**——比整段 SQL 字符串更接近 **enterprise 审计** 需求。
  - 明日动作(<=20min): 写 **≤20 词**：你的 text-to-SQL agent 输出是否含 **列级来源**（有/无/未做）；若无，标最可能加的 **1 个钩子**（EXPLAIN/元数据/其他 1 词）。

- [Claude Fable is relentlessly proactive（Simon Willison Blog，**2026-06-11**）](https://simonwillison.net/2026/Jun/11/fable-is-relentlessly-proactive/)
  - 摘要: Simon 两日体验：**Fable 5「relentlessly proactive」**——为达目标会 **主动部署多种技巧**（非仅回答问题）；与 **Fable 6/12 政府召回** 对照，说明 **proactive agent 能力 ↔ 监管敏感度** 强相关。
  - 为什么重要(对我): **Agent 规划原理**：proactivity = **高 prior 上的 tool/policy 探索**——你的 agent **temperature/effort** 与 **tool 自动触发阈值** 决定「主动」程度；召回后应用 **Opus 4.8** 复测同一 proactive 任务。
  - 明日动作(<=20min): 写 **≤20 词**：你 agent **自动调用 tool** 的触发条件是 **模型自决 / 规则 / 人工确认**（三选一）。

## 3) 热门项目/可试个人项目

- [luau-wasm 0.1a0 on PyPI（Simon Willison，**2026-06-13**）](https://github.com/simonw/luau-wasm/releases/tag/0.1a0)
  - 摘要: **276KB** `pyemscripten` wheel；**Luau**（Roblox MIT 方言）编译到 WASM；**GitHub Actions + cibuildwheel** 自动发布；[在线 demo](https://simonw.github.io/luau-wasm/) 可 **Pyodide pip install** 后 REPL 调用。
  - 为什么重要(对我): 可作为 **「首个自发布 Pyodide WASM 包」** 模板——验证 **cibuildwheel + PyPI wasm 标签** 流水线，再 wasm 化你自己的 **小 C/Rust 扩展**。
  - 明日动作(<=20min): clone [simonw/luau-wasm](https://github.com/simonw/luau-wasm) 读 **`.github/workflows`** 1 次；写 **是/否** 有 **可 wasm 化的个人依赖**。
  - 估计复现成本: **低**（无 GPU；需读 CI 配置）。

- [Fable API 断供监测 gist（Simon Willison，**2026-06-13**）](https://gist.github.com/simonw/5894cfafc64a2b8aafbe834bc9c950b9)
  - 摘要: **`uv run llm -m claude-fable-5 hi`** 每分钟探测；日志记录 **404 精确时间** 与 **error body**——可泛化到任意 **关键 model endpoint**。
  - 为什么重要(对我): `0613.md` 已引 Fable 断供；本 gist 是 **可 fork 的最小实现**（非博文重复叙述）。
  - 明日动作(<=20min): 打开 gist，数 **主循环行数**（约数即可）；写 **你的主 endpoint 名 1 个**（≤15 字符）。
  - 估计复现成本: **低**（curl/llm CLI）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Statement on Fable/Mythos access + Simon 实测断供时间线（Anthropic + Simon Willison，**2026-06-12–13**）](https://www.anthropic.com/news/fable-mythos-access)
  - 机会信号: **Frontier proactive agent（Fable）被政策下架** 后，**Opus 4.8 + Managed Agents + 合规叙事（Public Record 71% 支持监管）** 成为 **B2B 出海** 更稳的卖点——客户要 **可切换模型 + audit trail**，而非 **单一最强 agent**。
  - 风险信号: 政府可 **数小时内切断全球 API**（Simon：**5:21pm ET 令 → ~4h 后 API 404**）；若产品文案写 **「Fable 级自主」**，海外 **regulated 客户** 会追问 **continuity plan**；外国籍员工亦受 export 令影响——**团队属地与模型访问** 成合规变量。
  - 我可执行的一步(<=30min): 用中文写 **≤40 词** landing 页 **1 句** 模型无关承诺（如「支持多 provider 切换」）；列 **主模型不可用时的 fallback**（1 个模型名或「未定义」）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 394. Decode String](https://leetcode.com/problems/decode-string/)：练 **栈/递归解析嵌套结构**——对照 vLLM **统一 `Parser.parse()`** 与 **regex compilation timeout** 的「不可信模式输入」。明日动作：提交后写 **≤15 词**：用 **栈** 还是 **递归**（二选一）。

- **IELTS（≤1 条）**：[IELTS — Prepare for your test](https://ielts.org/take-a-test/prepare-for-your-test)：官方 **题型说明、样题与备考资源** 入口；与 `0613.md` **四科格式** 连贯。明日动作：选 **Listening** 或 **Reading** 做 **1 节官方样题**（≤15 min），写 **≤15 词** 错因（词汇/定位/速度 选 1）。

## 6) 噪音过滤记录

- **Fable 断供长文、Codex async #38128/#38129、langchain 1.3.9、Box Deep Agents、vLLM 0.23.0 release**：`0613.md` **URL/语义去重** → 本日不重复展开。
- **`langgraph 1.2.5` / `lc_versions` / cli 0.4.29 HTTPS**：`0612–0613` 已收录 → 丢弃。
- **OpenAI Dreaming V3（Jun 4）**：`0605.md` 深度覆盖 → 丢弃。
- **Anthropic Policy on AI Exponential**：多次 **404/timeout** → 不收录。
- **Anthropic how-we-contain-claude**：`0601.md` 去重 → 丢弃。
- **llama_index / autogen / openai/evals**：无 **2026-06-14** 新鲜 release → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取原帖 → 不收录。
- **Indie Hackers 新帖**：本环境 **无稳定 permalink 全文** → 职业池沿用 **可核验 Anthropic/Simon** 信号。

## 7) 本周累计计数

- Agent: **290**（`0613.md` §7: 282 + 本日 §1: 8）
- Theory: **118**（`0613.md` §7: 114 + 本日 §2: 4）
- Career/Business: **64**（`0613.md` §7: 63 + 本日 §4: 1）
- LC: **46**（`0613.md` §7: 45 + 本日 §5: 1）
- IELTS: **46**（`0613.md` §7: 45 + 本日 §5: 1）
