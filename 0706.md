# Daily Research - 2026-07-06 (21:30)

## 0) 今日3条结论

- **LangGraph `@task` cache key 对同 `tobytes()` 不同 dtype 碰撞（#8009 / PR #8285，updated 2026-07-06）**：numpy/torch/PIL 对象共享字节但语义不同会被冻成同一 cache key → cached skill step 静默返回错结果；collect→test 若用 tensor 入参需加 dtype/shape 显式 key 或禁用 cache。
- **LangChain streaming 在首 SSE chunk `args=""` 即物化空 tool_call（#38682，opened 2026-07-06）**：`AIMessageChunk.init_tool_calls` 把空字符串与 `None` 等同为 `{}` → 分片 tool args 的 provider 上 agent 提前触发 `ValidationError`；L2 streaming trajectory eval 与 prod 分片语义分叉。
- **ASAG 用 attention entropy + confidence 在 action transition point 早停（arXiv 2606.15070）**：Qwen3-8B 上 accuracy +3.2% 且 token -40% → token 优化除「砍 context / lazy schema」外，可把 **attention 分布熵** 作 L1 serving+eval 门禁信号。

## 1) Agent工程化（high-signal）

- [fix(langgraph): preserve dtype/metadata in default cache key for tobytes() objects（LangGraph PR #8285 updated **2026-07-06**）](https://github.com/langchain-ai/langgraph/pull/8285)
  - 摘要: `default_cache_key` 对暴露 `.tobytes()` 的对象只冻 `(typename, tobytes(), shape)`，丢弃 numpy `dtype`/strides 与 PIL `mode`/`palette`；两输入字节相同但解释不同会碰撞 → `@task` + `CachePolicy` 返回错误缓存、body 不重跑。Fix：tobytes 分支补 dtype/shape/strides/PIL 元数据；`_freeze` 递归 tuple 消除 positional/keyword 路径不对称。Fixes [#8009](https://github.com/langchain-ai/langgraph/issues/8009)。PR 因 maintainer assignment 门禁自动关闭，issue 仍 open。
  - 为什么重要(对我): **skill 落地 + eval**——skill pipeline 若用 numpy embedding / image tensor 作 task 入参，cache 碰撞使 parallel eval run 出现不可复现假阳性。
  - 明日动作(<=20min): 写 **是/否**：你 LangGraph `@task` 是否缓存含 numpy/tensor 入参（是/否/未知）。

- [fix(langchain): filter non-serializable state in Send for `ShellToolMiddleware`（LangChain PR #34500 updated **2026-07-06**）](https://github.com/langchain-ai/langchain/pull/34500)
  - 摘要: `ShellToolMiddleware` + `InMemorySaver` 组合报 `TypeError: Type is not msgpack serializable: Send`；根因是 `model_to_tools()` 把含 `subprocess.Popen`/`threading.Thread`/`queue.Queue` 的 `shell_session_resources` 整包塞进 `Send.arg`，绕过 `UntrackedValue` 注解。Fix：在 `factory.py` 新增 `_NON_SERIALIZABLE_STATE_FIELDS` + `_filter_serializable_state()`，Send 前过滤。28 个 shell 单测全过。Fixes [#34490](https://github.com/langchain-ai/langchain/issues/34490)。PR 因 assignment 门禁关闭，issue 仍 open。
  - 为什么重要(对我): **skill 落地**——shell/code-exec skill 是 collect→test 常见路径；checkpoint 序列化失败使 HITL 长会话 eval 不可恢复。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否在 shell middleware 上启 checkpointer（是/否/仅 prod）。

- [Bug: streaming tool call fires prematurely when first SSE chunk has empty args（LangChain #38682 opened **2026-07-06**）](https://github.com/langchain-ai/langchain/issues/38682)
  - 摘要: OpenRouter / deepseek-v3.2 等 provider 将 tool args 分多 SSE chunk 发送；首 chunk `args=""` 时 `AIMessageChunk` 已在 `tool_calls` 填入 `{'name':..., 'args': {}, ...}`，实际参数未到达。根因：`libs/core/langchain_core/messages/ai.py` 的 `init_tool_calls` 对 `args=None` 与 `args=""` 均产出 `{}`。下游 agent 用空 args 调 tool → `ValidationError`。
  - 为什么重要(对我): **eval + skill**——streaming L2 step eval 若按 chunk 计数 tool invocation，会与「args 完整后才算一次 call」的 prod 语义不一致。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否在 streaming 模式校验 tool args 完整性（是/否/仅 end-state）。

- [Agent re-attempts original tool call after HumanInTheLoopMiddleware edit decision（LangChain #33787 updated **2026-07-06**）](https://github.com/langchain-ai/langchain/issues/33787)
  - 摘要: HITL `edit` 决策成功执行**编辑后** tool call，但下一步 agent 对比原始 `HumanMessage` 与编辑结果，判定原始请求仍未满足 → 再生成 **未编辑** 的原始 tool call。用户视角：`edit` 语义应是「替换」而非「追加」，当前行为形成 edit→reject 双操作循环。
  - 为什么重要(对我): **skill 落地 + eval**——HITL skill 的 edit 路径是 L3 guardrail 核心；trajectory eval 若只测 approve/reject 会漏掉 edit 后意图未清除的假阴性。
  - 明日动作(<=20min): 写 **是/否**：你 HITL eval 是否覆盖 `edit` 后不再重试原 call（是/否/未测）。

- [fix(core): handle malformed Anthropic content blocks（LangChain PR #38687 updated **2026-07-06**）](https://github.com/langchain-ai/langchain/pull/38687)
  - 摘要: 读取 `BaseMessage.content_blocks` 时，缺字段的 Anthropic document/image source 或 citation 不再 `KeyError`；畸形 block 走 fallback 或转 `non_standard_annotation`。Fixes [#38667](https://github.com/langchain-ai/langchain/issues/38667)。PR 待 maintainer review。
  - 为什么重要(对我): **eval**——collect 阶段若 provider 返回残缺 content block，eval harness 在 message 规范化环节崩溃 vs prod 静默降级，造成环境分叉。
  - 明日动作(<=20min): 写 **是/否**：你 trajectory log 是否保留 raw provider block（是/否/仅 parsed）。

- [feat(OpenAIResponse): Add reasoning delta handling（LlamaIndex PR #21812 updated **2026-07-06**）](https://github.com/run-llama/llama_index/pull/21812)
  - 摘要: OpenAI Responses API 经 `ResponseReasoningTextDeltaEvent` / `ResponseReasoningSummaryTextDeltaEvent` 流式 reasoning，此前 `OpenAIResponses` 静默丢弃。现新增独立 `thinking_delta` channel（`ThinkingBlock`），与 answer `delta` 严格分离；`additional_kwargs` 按事件写入防 kwargs 复用导致 reasoning 渗入后续 answer chunk。sync/async 路径均覆盖。
  - 为什么重要(对我): **eval + token 优化**——reasoning token 与 answer token 分流是 L2 per-channel grading 与 token 预算门禁的前提；混流会使 thinking cost 不可归因。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否单独计 reasoning token（是/否/仅总 token）。

- [Fix relevance/choice-number desync in default_parse_choice_select_answer_fn（LlamaIndex PR #22252 updated **2026-07-06**）](https://github.com/run-llama/llama_index/pull/22252)
  - 摘要: `default_parse_choice_select_answer_fn` 对每行先 append `answer_num` 再 parse relevance；若 relevance 无数字（如 `Relevance: high`）则 `continue` 跳过 relevance 但 number 已写入 → `answer_nums` 比 `answer_relevances` 长，zip 后 doc↔score 错位。
  - 为什么重要(对我): **eval**——RAG node-selector / reranker eval 若用该 parser，L2 relevance grading 会出现系统性错位假阴性。
  - 明日动作(<=20min): 写 **是/否**：你 RAG eval 是否用 LlamaIndex choice-select parser（是/否/自定义）。

- [State of Agent Engineering 2026 — 89% observability vs 52% evals（LangChain Survey，**2026-06-12**）](https://www.langchain.com/state-of-agent-engineering)
  - 摘要: 1,340 人调研：57.3% 已有 agent 在生产；**89%** 实现 observability（生产团队 94%），仅 **52.4%** 跑 offline eval、37.3% online eval；29.5% 完全不 eval。质量仍是第一大生产障碍（32%）。Karpathy 在 Sequoia Ascent 2026 亦强调同一 gap：「能看见 agent 做了什么」≠「能判断做得对不对」。
  - 为什么重要(对我): **eval 杠杆**——你 map_prompt_test 的 collect→test 应优先补 offline eval 门禁，而非只扩 tracing；这是可写进 resume 的 industry baseline。
  - 明日动作(<=20min): 写 **≤15 词**：你当前 offline eval 样本量（0 / <30 / ≥30）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Stop When Further Reasoning Won't Help: Attention-State Adaptive Generation / ASAG（arXiv **2606.15070**）](https://arxiv.org/abs/2606.15070)
  - 摘要: 训练无关的 ASAG 在 **action transition points (ATP)** 联合探测 model confidence 与 **attention entropy**，判断 reasoning 态是否稳定以早停或轻量干预「thinking trap」。DeepSeek-R1-Distill / Qwen3 九 benchmark：Qwen3-8B accuracy +3.2%，token 生成量约 **-40%**；优于仅依赖 output confidence 的早停（易在难题过早退出、易题过晚退出）。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) attention entropy 能否作你 agent **≥3 turn** 中间的 L1 停步信号？(2) ATP 探测点 vs 固定 max_tokens 哪个更适合 serving eval 一致性？(3) 与 Tool Attention 的 protocol 层 gating 是互补（tool 侧砍 schema、reasoning 侧砍 CoT）还是冲突？
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 是否设 reasoning token 上限（有/无/仅模型默认）。

- [[Bug]: Qwen3.6 hybrid + prefix caching + MTP3 → tool-call leakage（vLLM #47194 updated **2026-07-05**）](https://github.com/vllm-project/vllm/issues/47194)
  - 摘要: Qwen3.6 hybrid（full-attention + Mamba/GDN）在 **prefix caching + MTP3 speculative decoding** 下 cache hit 仍高，但 tool call 10x 仅 2/10 pass、needle recall 10K/30K 0/10、multi-turn tool 0/5；`<tool_call>` XML 泄漏为纯文本、parser 不产出有效 `tool_calls`。无 MTP 路径正确。疑为 hybrid KV/state 在 speculative rollback 下不安全复用。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) prefix cache 命中 ≠ attention 状态正确——eval 能否分层报告 cache-hit 子集？(2) hybrid 架构的 FA/Mamba 状态复用对 L2 tool-call grading 意味着什么？(3) 与 ASAG 用 attention 态做决策时，cache 污染是否扭曲 entropy 信号？
  - 明日动作(<=20min): 写 **是/否**：你 serving 是否开 prefix cache + speculative decoding（是/否/仅其一）。

- [From Reasoning Traces to Reusable Modules（arXiv **2606.18089**）](https://arxiv.org/html/2606.18089v1)
  - 摘要: 将 reasoning trace 建模为层次 latent selection：problem descriptor 诱导离散选择变量，选出可复用 **atomic skills** 与 **routing mechanisms**。SFT 轨迹提供模块原料但 skills/routing 统计纠缠；RL 在 reward 下重采样轨迹，高效分解出带局部兼容接口的原子模块并组合泛化到未见 composition。给出 SFT+RL 数据配对协议与 local-witness 组合理论。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) 你 skill 栈是「整段 SFT 轨迹」还是可拆 **atomic skill + router**？(2) RL enrich 能否映射到你 agent eval 的「新组合泛化」split？(3) routing mechanism 与 Tool Attention 的 ISO gating 是否同构（选模块 vs 选 tool）？
  - 明日动作(<=20min): 写 **≤15 词**：你 skill 是否分 atomic + router 两层（是/否/仅单 skill）。

## 3) 热门项目/可试个人项目

- [Eval proposal: prompt-defense-eval（openai/evals #1663 updated **2026-07-06**）](https://github.com/openai/evals/issues/1663)
  - 摘要: 33 samples（29 adversarial + 4 benign），覆盖 OWASP LLM02 七类危险输出（XSS/SQLi/shell/path traversal/credential leak 等）；22 条 regex 规则、**sub-5ms** 确定性打分、无需 LLM-judge，`temperature=0` 可复现。对抗样本是「合理开发者请求」而非明显 jailbreak。
  - 为什么重要(对我): **eval 杠杆**——可作 agent 输出安全 L2 门禁模板；regex 确定性评分适合 CI collect→test，补你现有 LLM-judge eval 的盲区。
  - 明日动作(<=20min): fork issue 样例；写 **≤15 词** 你 agent eval 是否有 deterministic safety gate（有/无）。
  - 估计复现成本: **低**（纯 Python regex，无 GPU；需接你 agent 输出流做 post-check）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [sqlite-utils 4.0rc3 — Claude Fable 5 + GPT-5.5 双模型清 backlog（Simon Willison Blog **2026-07-06**）](https://simonwillison.net/2026/Jul/6/sqlite-utils/)
  - 机会信号: **一人公司 multi-agent 分工**——周末未发 4.0 stable 因 Fable 5 + GPT-5.5 持续清 issue/PR；compound foreign keys 与 case-insensitive column names 两处「横切 breaking change」适合多模型并行攻坚。对比 7/5 的 $149 单会话，今日信号是 **按任务类型选模型** 而非单会话堆叠。
  - 风险信号: 横切 refactor（column name 语义）易引入 silent data bug；无 subagent 成本追踪时长会话仍不可预测。
  - 我可执行的一步(<=30min): 列你当前 agent 任务的 **模型分工表**（规划/实现/review 各用何模型）；写 **≤30 词** 是否追踪 token 成本（有/无）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 123 Best Time to Buy and Sell Stock III](https://leetcode.com/problems/best-time-to-buy-and-sell-stock-iii/)：至多两笔交易，四变量 DP `hold1/sell1/hold2/sell2` 单遍 O(n) O(1)。明日动作：手写四变量版（≤20min）；写 **≤15 词**：与 agent「两阶段决策、不可重叠」的类比。

- **IELTS（≤1 条）**：[IELTS Writing Task 2 — Discuss both views（British Council 样题）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-practice-tests/writing)：用「AI agent 是否应完全自主决策」脱敏写 250 词 discuss 结构。明日动作：列 2 个 for + 2 个 against + 1 句立场（≤20min）；写 **≤15 词** 弱项（grammar/cohesion/vocab 选 1）。

## 6) 噪音过滤记录

- **LangGraph #8217/#8218 / PR #8284 / #8282**：`0705.md` 已收录 → 不重复。
- **LangChain PR #38671 / LlamaIndex #22249 / #21302 / AutoGen #7916**：`0705.md` 已收录 → 不重复。
- **Tool Attention（2604.21816）/ NF-CoT（2606.06447）/ vLLM #39848**：`0705.md` 已收录 → 不重复。
- **ACE PR #42645**：`0531.md` / `0601.md` 已收录 → 不重复。
- **Agentic DFS（2606.00183）**：`0701.md` 已收录 → 不重复。
- **LangChain #38351 KeyError('model')**：`0622.md` 已收录；今日仅 activity bump → 不重复展开。
- **AutoGen #7800 Guardrails**：`0626.md` 已收录 → 不重复。
- **LlamaIndex #21813 Google thinking delta**：与 #21812 同主题；本日 §1 已收 #21812 → #21813 不重复。
- **vLLM #47739 Mistral tool_call_id / #43597 XPU perf**：serving 格式/infra，与今日 focus 弱于 #47194 → 丢弃。
- **LangGraph #8286 jsonschema-rs build**：环境/依赖，非 agent 工程 → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-06** 原帖 URL → 不收录。
- **@gefei55 / @ianneo_ai 职业帖**：第三方聚合无法验证当日原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv + Simon Willison + LangChain Survey 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **466**（`0705.md` §7: 458 + 本日 §1: 8）
- Theory: **191**（`0705.md` §7: 188 + 本日 §2: 3）
- Career/Business: **85**（`0705.md` §7: 84 + 本日 §4: 1）
- LC: **67**（`0705.md` §7: 66 + 本日 §5: 1）
- IELTS: **67**（`0705.md` §7: 66 + 本日 §5: 1）
