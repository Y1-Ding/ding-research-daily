# Daily Research - 2026-06-10 (21:30)

## 0) 今日3条结论

- **上下文压缩进入「可编程触发」阶段**：LangChain **`SummarizationMiddleware`** 在 **1.3.5** 落地 **AND/OR `TriggerClause`**（如 `{"tokens": 4000, "messages": 10}` 双阈值同满足才压缩），**1.3.6** 同日热修 **私有字段兼容**——长时程 agent 的 **compaction 策略** 应从「固定 token 上限」升级为 **可测的触发谓词**。
- **Frontier 模型发布附带「不可见降级」新范式**：Simon Willison（**2026-06-10**）引用 Anthropic **Fable 5 system card**：对 **预训练/分布式训练/加速器设计** 等请求，模型可通过 **prompt 修改、steering vectors、PEFT** **静默降效** 且 **不 fallback**——对你做 **agent eval** 意味着需区分 **「模型能力不足」vs「策略性 sabotage」**。
- **工具链一周三连补**：**`langchain-openai 1.3.0`** 支持 Responses API **`apply_patch`** 内置工具；**`langchain-perplexity 1.4.0`** 补齐 **`bind_tools` + Agent API round-trip**；配合 **Claude Fable 5** 长链路编码实测（Simon **2026-06-09** 单日 **~$110** token 用量）——**harness + 内置 patch 工具 + 更强模型** 正同时到位。

## 1) Agent工程化（high-signal）

- [langchain==1.3.5：`SummarizationMiddleware` AND-capable trigger conditions（GitHub Release，**Published 2026-06-10**）](https://github.com/langchain-ai/langchain/releases/tag/langchain%3D%3D1.3.5)
  - 摘要: **`langchain 1.3.5`** 合并 **#34576**：`trigger` 支持 **`TriggerClause` 字典 AND**（单 clause 内 `tokens`/`messages`/`fraction` 全满足才 summarize）、**列表 OR**、以及与 JS 版对齐的 **tuple 兼容**；典型场景：**大 tool 返回短暂冲高 token 但消息数少** 时不误压缩；**多轮短消息** 但 token 未达阈值时亦保留近期上下文。
  - 为什么重要(对我): 你的 **planning/RAG 长对话 agent** 若用 **SummarizationMiddleware**，此前 **单阈值** 易在 **「一次大检索」** 或 **「多轮小步」** 两种形态下 **过早丢上下文**；AND 触发让你把 **compaction 写成可 eval 的策略**（与 OpenAI server-side compaction 形成对照实验）。
  - 明日动作(<=20min): 为你 **1 个** 长对话 agent，写 **1 条** `trigger` 草稿（含 **tokens + messages** 双条件，各给数字）；标 **当前是否已用 SummarizationMiddleware**（是/否）。

- [langchain==1.3.6：preserve summarization trigger compatibility（GitHub Release / #38000，**Published 2026-06-10**）](https://github.com/langchain-ai/langchain/releases/tag/langchain%3D%3D1.3.6)
  - 摘要: **#38000** 在 **#34576** 次日热修：内部以 **`_trigger_clauses`** 为 canonical，**`_trigger_conditions`** 保留为 **tuple 形兼容投影**，避免复合 dict（如 `{"tokens": 1000, "messages": 5}`）被 **错误展平为 OR 式 tuple 条件**；**1.3.6** 仅含该兼容修复（**#38001**）。
  - 为什么重要(对我): 若你 **monkey-patch 或测试** 依赖 middleware **私有 `_trigger_conditions`**，升级 **1.3.5→1.3.6** 行为会变；**eval 应用例锁定版本** 并测 **compound trigger 是否在预期步数触发**。
  - 明日动作(<=20min): 查你项目 **`langchain` 版本**（`pip show langchain | grep Version`）；若 **≥1.3.5**，写 **≤15 词** 是否依赖 **私有 trigger 字段**（是/否/未查）。

- [langchain-openai==1.3.0：支持 `apply_patch` built-in tool（GitHub Release / #37157，**Published 2026-06-09**）](https://github.com/langchain-ai/langchain/releases/tag/langchain-openai%3D%3D1.3.0)
  - 摘要: **`langchain-openai 1.3.0`** 合并 **#37157**：`bind_tools([{"type": "apply_patch"}])` 可用；在 Responses API 路径上 **保留 `apply_patch_call` / `apply_patch_call_output`** 于 **非流式、流式 chunk、round-trip 输入** 转换；单测覆盖 **core passthrough + 双向序列化**。
  - 为什么重要(对我): **文件编辑型 coding agent** 若走 **LangChain + OpenAI Responses**，此前 **patch 回合** 可能在 **AIMessage.content** 转换中 **丢结构化 patch 项** → **多步 refactor eval** 断链；现可与 **Codex/Agents SDK 的 apply_patch harness** 对齐做 **跨栈对比**。
  - 明日动作(<=20min): 若你用 **ChatOpenAI + Responses API**，列 **1 个** 需 **apply_patch** 的任务（≤12 词）；标 **当前是否 bind 该内置工具**（是/否/未用 OpenAI）。

- [langchain-perplexity==1.4.0：`bind_tools` + Responses-API tool round-trip（GitHub Release / #37934，**Published 2026-06-09**）](https://github.com/langchain-ai/langchain/releases/tag/langchain-perplexity%3D%3D1.4.0)
  - 摘要: 在 **1.3.2** 修复出站 `ToolMessage` 序列化后，**1.4.0** 实现 **`ChatPerplexity.bind_tools`**：`AIMessage.tool_calls` → **`function_call`**、`ToolMessage` → **`function_call_output`**；流式在 **`response.output_item.done`** 发 **`tool_call_chunk`**；**`openai/gpt-5.5` + `use_responses_api=True`** 下 **client-side function tool 往返** 已 live 验证；**`sonar` 族仍 `has_tool_calling=False`**（API 400）。
  - 为什么重要(对我): **搜索增强 agent**（Perplexity Agent API）终于进入 **`langchain-tests` 标准 tool 套件**（35 passed）；你可把 **「检索 + 本地函数」** 合成 **单 harness eval**，而不仅是 **纯问答 RAG**。
  - 明日动作(<=20min): 若用过 **ChatPerplexity**，记 **当前版本**；写 **≤20 词** **1 个** 需 **bind_tools 回归** 的用例（如「搜完再调本地计算器」）。

- [Initial impressions of Claude Fable 5（Simon Willison Blog，**2026-06-09**）](https://simonwillison.net/2026/Jun/9/claude-fable-5/)
  - 摘要: Simon 实测 **~5.5h**：Fable 5 **慢、贵、知识面广**（无搜索下列举其开源项目 **带日期** 优于 Opus 4.8）；**1M context / 128k max output / 截止 2026-01**；定价 **$10/$50 per M tokens**（约为 Opus 4.8 **2×**）；订阅用户 **至 6/22 含在套餐内** 后改按量计费。编码侧：用 **claude.ai 容器** 把 **micropython-wasm → cpython-wasm wheel**；在 **Datasette Agent** 实现 **human-in-the-loop `ask_user()`**，并驱动 **LLM 0.32a3** 新增 **`PauseChain`、保证 `tool_call_id`、从 pending tool calls 恢复链** 等 API。
  - 为什么重要(对我): **长时程 agent eval** 应对标 **Fable 级任务**（跨库 clone、多轮 patch、暂停-恢复工具链）；**PauseChain 语义**（暂停时不向模型塞 placeholder）是你 **HITL planning** 的直接参考实现。
  - 明日动作(<=20min): 读 [LLM #1482 PauseChain 讨论](https://github.com/simonw/llm/issues/1482) 摘要（1 次）；用中文写 **≤25 词**：你的 agent **暂停工具链** 时是否 **避免空 ToolMessage**（是/否/未实现）。

- [langgraph==1.2.4：`_on_started` backward-compatible fix（GitHub Release，**Published 2026-06-02**）](https://github.com/langchain-ai/langgraph/releases/tag/1.2.4)
  - 摘要: **`langgraph 1.2.4`**：**#7987** 保持 **`_on_started` 与旧版 override（predating `cause`）向后兼容**；另含 **sdk-py factory-graph 集成测试**（**#7978**）。
  - 为什么重要(对我): 自建 **LangGraph 流式/生命周期 hook** 若 override **`_on_started`**，小版本升级曾可能 **破坏自定义 telemetry 或 checkpoint 预热**；升级 **1.2.3→1.2.4** 应跑 **1 条 stream smoke**。
  - 明日动作(<=20min): 若你 override 过 graph stream callback，查 **`langgraph` 版本**；记 **是否 <1.2.4**（是/否/未用 LangGraph）。

- [How to Build a Custom Agent Harness（LangChain Blog，**2026-06-03**）](https://blog.langchain.dev/how-to-build-a-custom-agent-harness/)
  - 摘要: LangChain 官方定义 **agent = model + harness**；**`create_agent`** 为最小 harness，**middleware** 在 **model/tool 前后、启停** 挂钩；表格映射 **SummarizationMiddleware、ShellToolMiddleware、HumanInTheLoopMiddleware、ModelFallbackMiddleware** 等 **能力与预置 middleware**；强调 **task-harness fit** 而非堆模型。
  - 为什么重要(对我): 与今日 **SummarizationMiddleware 触发器升级**、**apply_patch** 工具链更新 **同一叙事**：你应把 **eval 矩阵** 从「换模型」扩到 **「换 middleware 栈」**。
  - 明日动作(<=20min): 列出你当前 harness 已用的 **≤3 个** middleware 名（无则写「仅 create_agent 默认」）；标 **缺哪一类**（压缩/重试/HITL 选 1）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Claude Fable 5 静默 safeguard：steering vectors / PEFT（Simon Willison Blog，**2026-06-10**）](https://simonwillison.net/2026/Jun/10/)
  - 摘要: Simon 引用 **Fable 5 & Mythos 5 system card（319 页）**：对 **frontier LLM 开发类请求**（预训练管线、分布式训练、ML 加速器设计等），safeguard **对用户不可见**、**不 fallback 其他模型**，而通过 **prompt 修改、steering vectors、PEFT** 限制效度；估计影响 **~0.03% 流量 / <0.1% 组织**。与网络安全/生化的 **可见拒绝** 不同。
  - 为什么重要(对我): **Agent 原理层**：模型输出不再是 **单一「能力上界」**，而是 **策略函数 + 隐藏干预** 的混合体——**eval 若只测 pass@1**，无法发现 **系统性降质**；**三回合追问**：(1) 你的 **训练/infra 类 prompt** 是否可能被静默干预？(2) **A/B 同 prompt 跨版本** 方差是否异常？(3) 是否需 **Mythos/无 safeguard 通道** 做 **对照基线**？
  - 明日动作(<=20min): 写 **≤35 词**：若 agent **静默降效**，你现有 eval **能否检出**（能/不能 + 1 理由）。

- [Summarization 触发的 AND/OR 语义（langchain #34576）](https://github.com/langchain-ai/langchain/pull/34576)
  - 摘要: **单 clause 内 AND**（各阈值同满足）、**clause 间 OR**（如 `[{"tokens": 4000, "messages": 10}, ("messages", 50)]`）；类比 **Agent 记忆**：**「窗口满」≠「该忘」**——需 **多信号合取** 才触发 **有损压缩**（类似 **attention 仅在 multiple heads 一致时写 KV 摘要** 的工程近似）。
  - 为什么重要(对我): 设计 **长对话 agent** 时，**compaction 是 attention 窗口外的第二层记忆**；误触发 = **人为引入 hallucination 条件**；应用 **合成对话** eval：**大 tool 输出 + 少轮数** 不应 summarize。
  - 明日动作(<=20min): 写 **2 条** 伪对话场景（各 ≤15 词），标 **应/不应** 触发 summarize（自定阈值）。

- [Fable 5「模型体量」与知识深度（Simon Willison Blog，**2026-06-09**）](https://simonwillison.net/2026/Jun/9/claude-fable-5/)
  - 摘要: Simon 提出 **「big model smell」**：同 prompt 下 Fable **记忆更多项目细节与日期**（无 search），视为 **参数量/训练数据深度的 tea-leaves**；他仍偏好 **tool 查事实**，但承认 **更深先验** 可能加速 **库 API 选择与编码模式**。
  - 为什么重要(对我): **Transformer agent** 在 **工具不足时** 会回落到 **权重记忆**；**talk ≥3 turns** 场景应测：**关闭 search/RAG 后性能跌幅**——跌幅大则说明 **harness 欠工具化**，非模型真强。
  - 明日动作(<=20min): 为你 **1 个** coding agent 任务，写 **≤20 词**：哪些子步骤 **必须 tool**、哪些可 **靠权重**（各举 1 例）。

## 3) 热门项目/可试个人项目

- [Fable 5 生成的 `cpython_wasm` wheel（Simon Willison Blog，**2026-06-09**）](https://simonwillison.net/2026/Jun/9/claude-fable-5/)
  - 摘要: 在 claude.ai 容器中，Fable 将 **micropython-wasm** 研究延伸为 **Brett Cannon cpython-wasi-build** 方案，产出 **~13.9MB `cpython_wasm-0.1.0` wheel**；可用 `uv run --with <wheel-url> cpython-wasm -c '...'` 跑 **完整 CPython 沙箱**（作者称 zip-stdlib 路径仍待 `_PYTHONHOME` 微调）。
  - 为什么重要(对我): 对比你已关注的 **micropython-wasm**，这是 **「MicroPython → 完整 Python」** 的 **低成本 PoC 路径**，适合 **agent code interpreter** 能力阶梯实验。
  - 明日动作(<=20min): 复制文内 **`uv run --with ... cpython-wasm -c`** 命令跑 **1 行 print**；记录 **成功/失败**（1 词）。
  - 估计复现成本: **低**（`uv` + 下载 wheel；无 GPU）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Announcing Claude Fable 5 on Snowflake Cortex AI（Snowflake Blog，**2026-06-09**，**[OUTSIDE_WHITELIST]**）](https://www.snowflake.com/en/blog/claude-fable-5-snowflake-cortex-ai/)
  - 机会信号: **同日上线** Snowflake **Cortex Agents / CoWork / CoCo / AI_COMPLETE SQL**；文引 Anthropic：**Fable 5 safety classifiers 平均 <5% session 触发，触发时 graceful fallback 而非硬拒**——**企业 agent 采购** 看 **「可治理的长时程模型 + 数据 perimeter」**，不是裸 API。
  - 风险信号: Snowflake 为 **launch partner 宣传**，非独立 benchmark；**「数周工作压缩为分钟」** 为营销表述；**出海 agent 产品** 若无法提供 **同类 fallback/审计叙事**，难进 **Fortune 500 数据栈**。
  - 我可执行的一步(<=30min): 用中文写 **≤40 词**：你的 agent 产品 **最接近 CoCo（编码）还是 CoWork（知识工）**；列 **1 个** 你可写的 **governance 卖点**（≤12 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 981. Time Based Key-Value Store](https://leetcode.com/problems/time-based-key-value-store/)：练 **timestamp 单调下的 get/set**——对照 **SummarizationMiddleware 触发器** 与 **agent checkpoint「按时间失效」** 设计。明日动作：实现 `set/get` 后，用中文写 **≤25 词**：**`get(key, t)` 用二分还是线性** 及前提（1 句）。

- **IELTS（≤1 条）**：[IELTS 官方 — Test taker portal](https://ielts.org/take-a-test/your-ielts-results)：成绩与 **One Skill Retake** 状态在 **机考后 1–5 天**（纸笔 **13 天**）可查；**Retake 须与原始考试同 delivery mode**（与 `0609.md` Writing on Paper 政策连贯）。明日动作：若你有 **目标考期**，查 portal **1 次**，写 **≤20 词** 你下一科优先 **Retake 哪一项**（L/R/W/S 选一 + 1 理由）。

## 6) 噪音过滤记录

- **OpenAI Skills/Shell/Compaction、eval-skills、langgraph #7989/#7986、WWDC Vision LLM、vLLM #44490、langchain-core `dict()` 废弃**：与 `0609.md` **URL/语义去重**。
- **`micropython-wasm` 0.1a2 长文**：`0606.md` 已深度覆盖；本日仅通过 **Fable→cpython_wasm 升级线** 引用。
- **Uber $1,500/工具/月 AI cap**：`0603.md` 已收录 → 丢弃。
- **Anthropic newsroom `claude-fable-5` 直链**：本环境 **404**；改用 **Simon Willison Blog（BLOG_WHITELIST）+ Snowflake 同日公告（[OUTSIDE_WHITELIST]）** 交叉核验。
- **LangChain Rubrics 博客**：slug 多次 **404**，无法核验正文 → 丢弃。
- **X_WHITELIST / YOUTUBE_WHITELIST / Bilibili**：本环境 **无法稳定抓取原帖** → 不收录。
- **VentureBeat / Instagram 二手转述**：非一手可核验链接 → 丢弃。

## 7) 本周累计计数

- Agent: **263**（`0609.md` §7: 256 + 本日 §1: 7）
- Theory: **104**（`0609.md` §7: 101 + 本日 §2: 3）
- Career/Business: **60**（`0609.md` §7: 59 + 本日 §4: 1）
- LC: **42**（`0609.md` §7: 41 + 本日 §5: 1）
- IELTS: **42**（`0609.md` §7: 41 + 本日 §5: 1）
