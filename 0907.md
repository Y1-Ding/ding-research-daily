# Daily Research - 2026-09-07 (21:30)

## 0) 今日3条结论

- **LangChain `ContextEditingMiddleware` 异步路径会阻塞 event loop**（#40264/#40262, created **2026-09-07**）：model-based token counting 在 `awrap_model_call` 同步执行 → 拖慢同 runtime 所有 coroutine → **token 优化 middleware 的 eval 须测 async 并发场景下的 event-loop stall，不能只测 sync path。**
- **vLLM GLM-5.3-Flash 同日三连 perf PR 重构 attention 路由**（#55736/#55737/#55738, created **2026-09-07**）：NoPE layout 可切 dense masked-MHA prefill、KDA 走 FlashKDA、decode 消除 KDA 冗余 copy → **sparse attention eval 须按 prefill/decode 分 profile，不能只用单一 kernel 路径断言 latency。**
- **DeepSeek thinking-mode 多轮 tool call 须回传 `reasoning_content`**（LangChain #40254, created **2026-09-07**）：`_get_request_payload` 静默丢弃历史 assistant reasoning → 400 → **multi-turn agent eval gold 须断言 reasoning 字段在 follow-up request 中完整保留。**

## 1) Agent工程化（high-signal）

- [fix(langchain): offload `ContextEditingMiddleware` token counting off the event loop（LangChain PR #40264, created **2026-09-07**）](https://github.com/langchain-ai/langchain/pull/40264)
  - 摘要: Fixes #40261。`ContextEditingMiddleware.awrap_model_call` 用 model-based token counter（`get_num_tokens_from_messages`）时同步阻塞 asyncio event loop；Fix 通过 `run_in_executor` 将 deep copy + token counting 移入线程池，与 `ShellToolMiddleware` 同模式；附 fake model 阻塞回归测试。
  - 为什么重要(对我): **token 优化 + skill 落地**——context editing 是长 horizon agent 的 token budget gate；async middleware 的 event-loop stall 是 production liveness 盲区。
  - 明日动作(<=20min): 写 **是/否**：你的 context trim eval 是否测 async 并发下 event loop 是否被 token counter 阻塞（是/否/未知）。

- [fix(langchain): offload context edits in async middleware（LangChain PR #40262, created **2026-09-07**）](https://github.com/langchain-ai/langchain/pull/40262)
  - 摘要: 同 Fixes #40261。将整个 context-editing pass（初始 count + 重复 `clear_at_least` recount）offload 到 executor，保护 custom synchronous token counter；回归测试验证 token count 在 event-loop 外线程执行而 async model handler 仍在 loop 上。
  - 为什么重要(对我): **token 优化**——与 #40264 互补：覆盖 repeated recount 路径；eval 须测 `clear_at_least` 循环中的累计 blocking 时间。
  - 明日动作(<=20min): 写 **是/否**：你的 agent 是否在 async 路径使用 `ContextEditingMiddleware` + model token counter（是/否/未知）。

- [fix(deepseek): preserve reasoning_content in request payload（LangChain PR #40254, created **2026-09-07**）](https://github.com/langchain-ai/langchain/pull/40254)
  - 摘要: Fixes #40219。`ChatDeepSeek` 多轮 tool-calling 时 `_get_request_payload` 静默丢弃 prior assistant messages 的 `reasoning_content`，违反 DeepSeek thinking-mode 协议 → 400；Fix 在 payload 构建时经 `_convert_input` 重新 attach reasoning 字段；附 4 条回归。
  - 为什么重要(对我): **skill 落地 + 评测**——reasoning model 的 multi-turn tool call 是 agent protocol compliance gate；与 0906 reasoning_eos_policy 形成「request 侧 + serving 侧」双 gate。
  - 明日动作(<=20min): 写 **是/否**：你的 DeepSeek thinking-mode eval 是否断言 follow-up request 含 `reasoning_content`（是/否/未知）。

- [fix(langchain): avoid blocking during async shell cleanup（LangChain PR #40257, created **2026-09-07**）](https://github.com/langchain-ai/langchain/pull/40257)
  - 摘要: Fixes #40256。`ShellToolMiddleware.aafter_agent()` 的 shutdown commands + process cleanup 同步阻塞 event loop；Fix 经 `run_in_executor()` 委托 cleanup，与 `abefore_agent()` 对齐；附 deterministic 回归（cleanup 阻塞时 loop 仍可调度其他 work）。
  - 为什么重要(对我): **skill 落地**——shell skill 的 agent lifecycle cleanup 是 async liveness gate；eval 须测 agent 结束后的并发 responsiveness。
  - 明日动作(<=20min): 写 **是/否**：你的 shell skill eval 是否测 async cleanup 期间 event loop 响应（是/否/未知）。

- [fix(langchain): inherit shell environments by execution policy（LangChain PR #40253, created **2026-09-07**）](https://github.com/langchain-ai/langchain/pull/40253)
  - 摘要: Fixes #40252。`ShellToolMiddleware` 在 `env` 省略时 host/Codex session 应继承 parent process environment，Docker 保持隔离，显式 `env` 仍替换；environment 准备移至 execution policy；60 targeted unit tests passed。
  - 为什么重要(对我): **skill 落地**——shell skill 的环境继承是 reproducibility gate；local eval 与 Platform 部署若 env 不一致会导致 false pass/fail。
  - 明日动作(<=20min): 写 **是/否**：你的 shell skill eval 是否区分 host/Codex/Docker 三种 execution policy 的 env 行为（是/否/未知）。

- [feat: add llama-index-tools-opticparse integration（LlamaIndex PR #22974, created **2026-09-07**）](https://github.com/run-llama/llama_index/pull/22974)
  - 摘要: 新增 `llama-index-tools-opticparse`（PyPI v1.0.0）：(1) `extract`——零 CSS 多模态 vision scraping，anti-bot  resilient；(2) `inspect_threat`——<1.6s 启发式 crypto drainer/phishing 检测。面向 LlamaIndex agent/RAG/multi-agent workflow。
  - 为什么重要(对我): **skill 落地**——agent tool integration 模板：vision scraping + security inspection 双 tool 是可复用的 skill 接点模式。
  - 明日动作(<=20min): 读 PR README 前 30 行，写 **1 条** `extract` vs 传统 CSS selector scraping 对你 RAG agent 意味着什么（≤20 词）。

- [fix: MessageFilterAgent preserves strict chronological order（AutoGen PR #8204, created **2026-09-07**）](https://github.com/microsoft/autogen/pull/8204)
  - 摘要: Fixes #7971。`MessageFilterAgent._apply_filter` 原按 `per_source` 声明顺序分组 → message timeline 反转；Fix 保持原始 `messages` 列表时序，保证 downstream agent 的 causal ordering；附 `test_message_filter_agent_chronological_order`。
  - 为什么重要(对我): **评测**——multi-agent eval 的 message ordering 是 state integrity gate；filter 导致的时序反转会让 trace replay 与 gold 断言脱节。
  - 明日动作(<=20min): 写 **是/否**：你的 multi-agent eval 是否断言 filter 后 message 时序不变（是/否/未知）。

- [fix: TextCanvas.apply_patch validates context lines（AutoGen PR #8203, created **2026-09-07**）](https://github.com/microsoft/autogen/pull/8203)
  - 摘要: Fixes #8193。`TextCanvas.apply_patch` 原允许 stale patch 静默覆盖新 edits；Fix 从 hunk 构造 expected source lines 并与 working copy 精确匹配，不匹配则 `ValueError`；附 regression test。
  - 为什么重要(对我): **skill 落地 + 评测**——coding agent 的 patch apply 是 tool-call eval 核心；context mismatch rejection 是 negative gold 集必备用例。
  - 明日动作(<=20min): 写 **是/否**：你的 coding agent eval 是否测 stale patch rejection（是/否/未知）。

- [What's new in Claude Fable 5.1 — mid-conversation effort + cache reads at 0.025×（Anthropic, released **2026-09-01**）](https://platform.claude.com/docs/en/models/fable-5-1/whats-new-fable-5-1)
  - 摘要: Claude Fable 5.1 支持 mid-conversation effort 切换且不 invalidate prompt cache（beta header `mid-conversation-output-config-2026-07-01`）；cache reads 定价降至 base input 的 0.025×（Fable 5 为 0.1×）；agent 在 hard step 提 effort、routine step 降 effort 而不丢 cache prefix。
  - 为什么重要(对我): **token 优化**——长 horizon agent 的 cost-per-successful-task 可直接受益于 per-step effort + cache read 定价；eval 须分 effort level 测 cost 而非只看 total tokens。
  - 明日动作(<=20min): 写 **≤15 词**：你是否在 agent eval 中分 effort level 记录 cost（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Perf][GLM-5.3-Flash] Dense/masked-MHA sparse prefill for the NoPE (256, 0, 256) layout（vLLM PR #55738, created **2026-09-07**）](https://github.com/vllm-project/vllm/pull/55738)
  - 摘要: GLM-5.3-Flash MLA layout（qk_nope 256, qk_rope 0, v 256）在 main 上无 FlashAttention prefill backend → 全部 prefill 走 per-token top-k MQA kernel（KV-gather bound，16k chunk 5.9 ms/layer）；Fix 注册 masked-MHA prefill path + skip NoPE K concat，8×2048 从 ~1.6 ms 降至 ~0.2 ms 量级。
  - 为什么重要(对我): **Talk ≥3 turns + sparse/dense attention routing**——同一 model 的 prefill 可能在 sparse top-k MQA 与 dense MHA 间切换；eval 须标 effective backend 而非 config label。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serving GLM-5.3-Flash prefill（是/否/未知）。

- [Perf][GLM-5.3-Flash] Use FlashKDA for KDA chunked prefill（vLLM PR #55737, created **2026-09-07**）](https://github.com/vllm-project/vllm/pull/55737)
  - 摘要: GLM-5.3-Flash KDA chunked prefill 从 ~15-kernel Triton `chunk_kda_with_fused_gate` 切到 FlashKDA（`vllm._flashkda_C`，Kimi-K3 已用）；GB300 上 8×2048: 183→49 us，4×8192: 555→146 us，1×16384: 1371→787 us；in-kernel q/k l2norm + raw beta logits，drop-in 等价。
  - 为什么重要(对我): **Talk ≥3 turns + KDA/recurrent attention**——KDA 是 GLM-5.3 的 hybrid attention 组件；FlashKDA vs Triton 是 prefill kernel 选择维度。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 profiling KDA prefill separately from MLA（是/否/未知）。

- [Perf][GLM-5.3-Flash] Decode hot-path cleanups（vLLM PR #55736, created **2026-09-07**）](https://github.com/vllm-project/vllm/pull/55736)
  - 摘要: 三处独立 decode 优化：(1) KDA recurrent kernel 读 strided q/k/v/beta 消除 4 copy kernels/layer/step；(2) NoPE MQA query 无需 concat；(3) 消除 duplicate router GEMM。Profiling 于 FP8 TP4 4×GB300 `FLASHINFER_MLA_SPARSE`。
  - 为什么重要(对我): **Talk ≥3 turns + decode attention hot path**——decode 的 copy/concat 开销在 batch 64 时 ~0.4 ms/step 量级；sparse MLA serving eval 须分 prefill/decode profile。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 decode profiling 与 prefill 分开（是/否/未知）。

- [Add SAGE_ATTN backend backed by SageAttention PR #402（vLLM PR #55732, created **2026-09-07**）](https://github.com/vllm-project/vllm/pull/55732)
  - 摘要: 集成 SageAttention 为显式 `--attention-backend SAGE_ATTN` 路径，operator backend 来自 [SageAttention PR #402](https://github.com/thu-ml/SageAttention/pull/402)（seqlens + paged KV decode）；已在 vLLM stack 上验证 accuracy 与 performance。
  - 为什么重要(对我): **Talk ≥3 turns + quantized attention**——SageAttention 是 attention 近似/量化 backend 新选项；eval 须标 backend + accuracy delta。
  - 明日动作(<=20min): 写 **≤15 词**：你是否对比过 SAGE_ATTN vs FLASH_ATTN accuracy（是/否/未知）。

- [Core][Performance] Overlap producer async KV loads（vLLM PR #55724, created **2026-09-07**）](https://github.com/vllm-project/vllm/pull/55724)
  - 摘要: Pure KV producer 使用 async connector 时，admission loop 在 running requests 耗尽 token budget 后立即退出 → eligible remote load 延迟到下一步、无法与当前 model execution overlap；Fix 允许 pure producer 在 local budget 耗尽后仍 admit `load_kv_async=True` 的 WAITING requests（0 scheduled tokens），consumers/`kv_both`/无 connector 保持原行为。
  - 为什么重要(对我): **Talk ≥3 turns + KV cache / attention memory**——async KV load overlap 是 disaggregated serving 的 scheduler 语义；eval 须测 producer-consumer 场景的 TTFT 与 overlap ratio。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 disaggregated KV producer/consumer serving（是/否/未知）。

- [Bugfix][XPU] Fix KDA RecoverSSM device-pointer overflow（vLLM PR #55730, created **2026-09-07**）](https://github.com/vllm-project/vllm/pull/55730)
  - 摘要: `KDARecoverSSMCommitContext` 用 signed `torch.int64` 存 device pointers；XPU Level Zero USM 地址落在 64-bit 上半空间（如 `0xffffd556b6400000`）→ signed int64 overflow → metadata 构建失败；Fix 改用 unsigned pointer storage。
  - 为什么重要(对我): **Talk ≥3 turns + KDA/SSM state recovery**——KDA recurrent state 的 pointer metadata 是 cross-device attention 实现细节；eval 须标 hardware backend（CUDA vs XPU）。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 XPU serving KDA models（是/否/未知）。

## 3) 热门项目/可试个人项目

- [s1dashu/ip-as-logo-skill — Compact Agent Skill for IP mascot generation（created **2026-08-18**, ~5k stars **2026-09-07** GitHub API）](https://github.com/s1dashu/ip-as-logo-skill)
  - 摘要: 遵循 open Agent Skills format 的 compact skill：生成极简可爱 IP mascot（4–7 大形状、三色语义、dominant lower-corner composition）；不绑定特定 agent 产品；附 [ipaslogo.com](https://ipaslogo.com) 可搜索 logo 库。MIT，~5018 stars。
  - 为什么重要(对我): **skill 落地低成本试验**——compact skill spec + 明确 visual constraint 是可复用模板；比通用 prompt 更接近 Cursor/Harness plugin skill 结构。
  - 明日动作(<=20min): clone repo，读 `SKILL.md`（或等价文件）前 40 行，写 **1 条** 其 constraint 设计对你 agent skill 的启发（≤20 词）。
  - 估计复现成本: **低**（clone + 读 skill spec + 写 1 条接点，~15min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Formalizing Fermat's Last Theorem（Anthropic Research, **2026-09-04**）](https://www.anthropic.com/research/formalizing-fermats-last-theorem)
  - 机会信号: Claude 多 agent harness（Prove2Me + Claude Code）11 天自主形式化 FLT——13M 行 Lean、29,500 中间定理、~6B output tokens；Prove2Me DAG 调度 + 自然语言 theorem 索引使 consumer subscription 级别也可协作形式化（Vinogradov Three Primes 3 天完成）。**长 horizon agent + 可验证产出** 是 AI solo company 的高杠杆 signature 路径。
  - 风险信号: 6B tokens 成本极高、Lean 编译资源密集；形式化产出难直接 monetize；与 0906 GPT-6 Astra benchmark 争议形成「能力展示 vs 可审计验证」张力。
  - 我可执行的一步(<=30min): 读 Anthropic 文 + [Prove2Me arXiv:2608.28433](https://doi.org/10.48550/arXiv.2608.28433) abstract，写 **3 条** Prove2Me DAG 调度对你 agent eval trace 管理的启发（各 ≤15 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[940. Distinct Subsequences II](https://leetcode.com/problems/distinct-subsequences-ii/)（**2026-09-07 Daily Challenge**，承接 0906 的 115「Distinct Subsequences I」，今日 **Hard**「String + DP + mod，统计 s 的全部 distinct non-empty subsequence 数」）：关键洞察——按字符末位聚合 `dp[c]` 避免重复计数（`total = sum(dp) + 1`）；与 agent eval「状态空间计数 + dedup」同构。明日动作：手写 O(n·|Σ|) DP；写 **≤15 词**：distinct subsequence II 对你 eval 状态 dedup 意味着什么。

- **IELTS（≤1 条）**：[IELTS Reading Practice Tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/reading)：0906 已轮换 Writing，今日换 **Reading**（选 Academic 或 General 1 套，限时 20min 做 Section 1）。明日动作：完成 1 次 Section 1；记录 **≥2 个** T/F/NG 或 matching 失误类型；写 **≤15 词** 你最弱的 Reading 维度（Skimming / Scanning / Inference / Vocabulary）。

## 6) 噪音过滤记录

- **fix(langchain): cap retry delay after applying jitter（#40260）**：通用 retry fix，与 focus 弱相关 → 过滤。
- **chore(model-profiles): refresh model profile data（#40258）**：数据刷新，非行为变更 → 过滤。
- **fix(deepseek): route strict mode to beta endpoint（#40249, Sep 6）**：endpoint routing，0906 邻域已覆盖 DeepSeek 路径 → 过滤。
- **fix(core): drop FlagEmbeddingReranker from __all__（LlamaIndex #22977）**：import fix → 过滤。
- **fix(lancedb): pass node by keyword（LlamaIndex #22976）**：multimodal fix，信号弱 → 过滤。
- **fix(runtime): use running loop for futures（AutoGen #8202）**：runtime fix，非 eval 核心 → 过滤。
- **feat: Cross-Chain Intent Tool ERC-7683（AutoGen #8201）**：crypto tool，与 focus 无关 → 过滤。
- **CI / docs / benchmark infra PRs（vLLM #55740/#55735/#55734/#55731/#55728/#55727/#55723/#55721/#55719 等）**：benchmark bounds、validation migration、encoder cache、NPU CI、multimodal caps → 过滤。
- **0906 已收录项**：LangGraph #8828 DeltaChannel、LlamaIndex SimpleVectorStore 三连修、vLLM sparse MLA/KV-sharing/reasoning_eos 等 → semantic duplicate，不重复。
- **Simon Willison GPT-6 Astra 系列（Sep 3–5）**：0906 §4 已收录 → 不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-07** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **911**（`0906.md` §7: 902 + 本日 §1: 9）
- Theory: **413**（`0906.md` §7: 407 + 本日 §2: 6）
- Career/Business: **138**（`0906.md` §7: 137 + 本日 §4: 1）
- LC: **120**（`0906.md` §7: 119 + 本日 §5: 1）
- IELTS: **120**（`0906.md` §7: 119 + 本日 §5: 1）
