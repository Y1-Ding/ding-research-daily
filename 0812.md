# Daily Research - 2026-08-12 (21:30)

## 0) 今日3条结论

- **LangGraph compile 瓶颈在 subgraph 源码解析**（#8569, updated Aug 12）：`inspect.getsource` + `ast.parse` 占 compile 时间 **80%**；改 bytecode `dis` walk 后 detection **1132ms→4ms**，总 build **4.9x** → **skill 落地若 node 多，升级后须重测 CI compile 与 hot-reload 延迟**。
- **Kimi-K3 ReplaySSM 用 checkpoint+per-token record 替代全量 recurrent state**（vLLM #51855, updated Aug 12）：同 46.48 GiB cache budget 下有效容量 **+10.97%** → **agent spec decode 的 hidden token 成本不只在 attention，还在 KDA state materialization**。
- **Claude 子 agent 协调 60 个 worker、31M output tokens 推进黎曼ζ零点下界 41.6%→67.2%**（Anthropic, Aug 10）：长时多 agent + 数值验证 + Lean 形式化 → **你的 L3 eval 应测「子 agent 互审/重证」能否降低单 agent 策略性错误，而非只测单线程 task success**。

## 1) Agent工程化（high-signal）

- [fix(langgraph): detect subgraphs from bytecode instead of source（LangGraph PR #8569, updated **2026-08-12**）](https://github.com/langchain-ai/langgraph/pull/8569)
  - 摘要: Fixes #8559。`find_subgraph_pregel` 在 build 时对每个 node 做 `inspect.getsource` + `ast.parse`（占 compile **80%**）；改为 `dis` walk `func.__code__` + closure/globals 解析 attribute path。Benchmark（713 nodes）：总 build **1412ms→286ms（4.9x）**，detection **1132ms→4ms（~266x）**；并支持 REPL/notebook/`exec` 定义的无源码函数。
  - 为什么重要(对我): **skill 落地**——多 node agent graph 的 compile 是 dev loop 与 CI 隐性成本；bytecode 路径还修复 notebook 场景 subgraph 检测 silent fail。
  - 明日动作(<=20min): 写 **是/否**：你 graph 是否 >50 nodes 或常在 notebook 定义 node fn（是/否/未知）。

- [fix(core): clear usage metadata callback on with-block exceptions（LangChain PR #38993, updated **2026-08-12**）](https://github.com/langchain-ai/langchain/pull/38993)
  - 摘要: Fixes #38989。`get_usage_metadata_callback()` 的 `ContextVar` 仅在 `yield` 后清理；`with` 块抛异常时 callback 残留，后续 `llm.invoke()` 仍计入错误 session 的 token 计数。
  - 为什么重要(对我): **评测 + token 优化**——eval harness 常在异常路径后继续跑 case；silent token 污染 = cost/latency 报表不可信。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否在 `with get_usage_metadata_callback()` 内可能抛异常（是/否/未知）。

- [fix(core): raise ValueError when explicit tool_outputs length mismatches tool_calls（LangChain PR #39142, merged **2026-08-12**）](https://github.com/langchain-ai/langchain/pull/39142)
  - 摘要: Fixes #39138。`tool_example_to_messages` 在显式提供 `tool_outputs` 时，`zip(..., strict=False)` 静默丢弃多余 output 或留下无配对 `ToolMessage`；Fix 在 zip 前做长度校验并抛描述性 `ValueError`。`tool_outputs=None` 自动占位行为不变。
  - 为什么重要(对我): **评测**——few-shot tool example 是 agent eval 数据构造基础；静默 malformed history = 训练/评测集 silent corruption。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `tool_example_to_messages` 构造 eval few-shot（是/否/未知）。

- [fix(core): preserve non-str/non-dict items in `DictPromptTemplate` list values（LangChain PR #39588, merged **2026-08-12**）](https://github.com/langchain-ai/langchain/pull/39588)
  - 摘要: Fixes #39152。`DictPromptTemplate.format()` 对 list 值只保留 `str`/`dict`，`int`/`float`/`bool`/`None`/嵌套 list 被静默丢弃；Fix 加 `else: formatted_v.append(x)` pass-through。
  - 为什么重要(对我): **skill 落地**——tool-use content block 常含 `top_k_scores: [1,2,3]`、`flags: [True]`；丢失 = agent 收到残缺 tool result，eval 对比时 state drift。
  - 明日动作(<=20min): 写 **是/否**：你 prompt 是否用 `DictPromptTemplate` 传 tool result 列表（是/否/未知）。

- [fix(anthropic): correct model profile data for Fable 5, Sonnet 5, Opus 4.1（LangChain PR #39604, merged **2026-08-12**）](https://github.com/langchain-ai/langchain/pull/39604)
  - 摘要: `claude-fable-5`/`claude-sonnet-5` 错误报告 `structured_output: false`；Fable 5 缺 reasoning effort 元数据；退役 `claude-opus-4-1` 稀疏条目移除。对照 Anthropic structured outputs 兼容表与 2026-08-05 退役公告修正。
  - 为什么重要(对我): **评测**——`model.profile` 驱动 eval 路由（structured output / reasoning effort）；错误 profile = eval 跳过本应支持的 capability 或选错 effort。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否读 `model.profile` 决定 structured output 路径（是/否/未知）。

- [feat: migrate llama-index-tools-mcp to mcp 2.x（LlamaIndex PR #22557, merged **2026-08-11**, updated **2026-08-12**）](https://github.com/run-llama/llama_index/pull/22557)
  - 摘要: Fixes #22515。`mcp>=1.24,<2` → `mcp>=2,<3`；适配 SDK 2.x：`FastMCP` 移除、camelCase→snake_case、HTTP/SSE 层切 `httpx2`、`ProgressFnT` 路径变更。版本 **0.4.8→0.5.0**（breaking：须 mcp 2.x）。
  - 为什么重要(对我): **skill 落地**——MCP 是 agent tool 接入标准路径；2.x breaking 若不 pin 版本，CI/prod skill server 会 silent 断裂。
  - 明日动作(<=20min): 查你项目 `llama-index-tools-mcp` 与 `mcp` 版本；写 **是/否/未知**：是否已锁 `mcp<2`。

- [fix(postgres): bind metadata filter keys instead of interpolating into SQL（LlamaIndex PR #22478, updated **2026-08-12**）](https://github.com/run-llama/llama_index/pull/22478)
  - 摘要: Fixes #22475。`PGVectorStore._build_filter_clause` 将 `filter_.key` 直接拼入 SQL；key 含单引号可篡改查询。一旦 key 来自 agent tool（模型选 metadata 字段），即 SQL injection。Fix 用 UUID 参数名 bind key。
  - 为什么重要(对我): **skill 落地 + 评测**——RAG agent 让模型选 filter field 是常见 pattern；未参数化 = L3 红队探针必炸点。
  - 明日动作(<=20min): 写 **是/否**：你 PG vector filter key 是否可能来自用户/模型输入（是/否/未知）。

- [fix(cli): make analytics telemetry non-blocking（LangGraph PR #8604, updated **2026-08-12**）](https://github.com/langchain-ai/langgraph/pull/8604)
  - 摘要: Fixes #8074。CLI analytics worker 非 daemon + `urlopen()` 无 timeout → telemetry endpoint 卡住时进程永不退出。Fix：3s timeout + daemon thread + swallow failures；保留 `LANGGRAPH_CLI_NO_ANALYTICS=1` opt-out。
  - 为什么重要(对我): **skill 落地**——LangGraph CLI 是本地 dev/CI deploy 入口；hang = eval pipeline 间歇性卡死，难归因。
  - 明日动作(<=20min): 写 **是/否**：你 CI 是否跑 `langgraph` CLI 且可能网络受限（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[K3] support replayssm for K3（vLLM PR #51855, updated **2026-08-12**）](https://github.com/vllm-project/vllm/pull/51855)
  - 摘要: Kimi-K3 KDA path 新增 ReplaySSM speculative decoding（Model Runner V2，ROCm/NVIDIA）。无 ReplaySSM 时每个 speculative position 需 materialize 完整 recurrent state；ReplaySSM 保留一个 checkpoint + per-token record，verify 后重建 accepted position state。TP8 Kimi-K3 同 46.48 GiB cache budget 有效容量 **+10.97%**；支持 `mamba_cache_mode=align` prefix caching 与 DSpark。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/KDA**：spec decode 的瓶颈从 attention 扩展到 SSM/KDA state；agent 每步 tool call 的 draft verify 成本与 cache 容量直接相关。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serve Kimi-K3 + spec decode（是/否/未知）。

- [[ROCm][Perf] Kimi-K3 Remove prefill pipeline stall in chunk KDA（vLLM PR #51862, updated **2026-08-12**）](https://github.com/vllm-project/vllm/pull/51862)
  - 摘要: ROCm Kimi-K3 每步 prefill/mixed 在 `prepare_chunk_indices` stall：`tolist()` D2H + host Python loop 阻塞 device queue。`chunk_indices`/`chunk_offsets` 已在 metadata builder 从 `query_start_loc_cpu` 构建但未传入 kernel。Fix：kernel 可选接受预构建 metadata；metadata builder 改 device 侧构建，消除重复 D2H。
  - 为什么重要(对我): **Talk ≥3 turns + KDA/Attention**：KDA chunk attention 的 host-device sync 是 agent prefill TTFT 隐形杀手；与 0811 #51725 adaptive scheduled tokens 互补（scheduler vs kernel path）。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent serving 是否测过 prefill stall（是/否/未知）。

- [[Bugfix] Fix hybrid attention cache miss due to eagle drop（vLLM PR #51295, updated **2026-08-12**）](https://github.com/vllm-project/vllm/pull/51295)
  - 摘要: Kimi K3 hybrid sparse attention + Eagle prefix cache：`prompt_length=24577` 时 Mamba checkpoint 与 full attention cache 对齐点 collapse，导致 Turn 2 本应 hit 23040 却 full miss（24576 vs 24577 差 1 token）。Fix：Eagle drop 时前移 mamba/SWA cache state 以对齐 extra full-attention block。
  - 为什么重要(对我): **Talk ≥3 turns + KV/Attention**：多轮 agent 对话 +1 token 即 full cache miss = TTFT/latency 断崖；eval 须覆盖「边界长度」case 而非只测 round 1。
  - 明日动作(<=20min): 写 **≤15 词**：你 multi-turn eval 是否测 prompt_length±1 边界（是/否/未知）。

- [[Kernel] Add native B12X linear, MoE, and causal attention backends（vLLM PR #51696, updated **2026-08-12**）](https://github.com/vllm-project/vllm/pull/51696)
  - 摘要: Opt-in `vllm[b12x]` 为 SM120/SM121 添加 B12X linear/MoE/**causal paged MHA/MQA/GQA** backend（BF16/FP16/FP8 KV；prefill/decode/mixed；CUDA graph）。纯 CuTeDSL、无额外 build step；与 FlashInfer-embedded B12X 路径不重复。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：Blackwell 消费级 GPU 上 agent serving 的 attention kernel 选型新分支；decode 小 batch 路径影响 tool loop 逐步延迟。
  - 明日动作(<=20min): 写 **≤15 词**：你部署硬件是否含 SM120/121（是/否/未知）。

- [Learning more about Claude's mathematical capabilities（Anthropic Research, **2026-08-10**）](https://www.anthropic.com/research/riemann-zeta)
  - 摘要: 未发布研究版 Claude 在尝试黎曼猜想时，将ζ函数满足 RH 的零点比例下界从 **41.6%→67.2%**。方法：Claude Code 两 session、**31M output tokens**、协调 **~60 子 agent**（2 个核心数学 agent + 13 贡献 + 30 失败尝试 + 13 验证者 + 2 写论文）；2400 shell commands、数千数值检查、54 篇 arXiv 去重、Lean 形式化验证。人类数学家 Alpöge/Furman 验证。
  - 为什么重要(对我): **Talk ≥3 turns + Agent 原理**——长时多 agent 编排（分工/互审/重证）是可测量方法论；对应你 eval 的「子 agent 验证链」与「鼓励性 prompt 对策略坚持的影响」。
  - 明日动作(<=20min): 读 methodology 段，写 **2 条** 可映射到你 L3 eval 的「子 agent 角色分工」模板（各 ≤20 词）。

## 3) 热门项目/可试个人项目

- [Transcripts of Claude sub-agents E2 and E2-pairs, typeset and annotated（Anthropic CDN PDF, **2026-08-03/04**）](https://www-cdn.anthropic.com/8a0d1add3c637b858a9a181e98c40e9548c3f44f.pdf)
  - 摘要: 完整排版注解的自主子 agent「E2」3.5 小时 transcript（54 model messages，无网络调用，含 thinking text）。由 orchestrating Claude（非人类）brief 做 Pontryagin-space「negative index」路线；路线失败后转向证明 ≥½ 零点在 critical line（后续推到 ⅔，见 companion transcript）。附 [Riemann 论文](https://www.anthropic.com/research/riemann-zeta) 与 [Appendix 方法论](https://www.anthropic.com/research/riemann-zeta)。
  - 为什么重要(对我): **Agent 工程化可读样本**——子 agent brief/review/ pivot 策略的可复盘 trace；比论文更适合设计你自己的 multi-agent eval scenario。
  - 明日动作(<=20min): 浏览 PDF 前 5 页 editorial + 1 个 REPORT 段，写 **1 条**「orchestrator→subagent brief 结构」观察。
  - 估计复现成本: **低**（只读 PDF + 笔记，~20min；复现数学证明 **高**）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Learning more about Claude's mathematical capabilities — 子 agent 研究流水线公开（Anthropic, **2026-08-10**）](https://www.anthropic.com/research/riemann-zeta)
  - 机会信号: Anthropic 公开完整研究叙事（子 agent 分工、Lean 形式化、人类数学家验证链）+ 子 agent transcript PDF → **「AI 辅助研究编排 + 验证门禁」是可产品化服务**；一人公司可用「长时 agent run 审计 + 人类-in-the-loop 验证报告」切入科研机构/企业 R&D。
  - 风险信号: 成果是「意外 byproduct」而非直接攻克 RH；31M tokens 成本极高，不宜外推为日常研发 ROI；数学验证链难泛化到工程 agent。
  - 我可执行的一步(<=30min): 从 [transcript PDF](https://www-cdn.anthropic.com/8a0d1add3c637b858a9a181e98c40e9548c3f44f.pdf) 提取 **3 条** 可写成服务 SOP 的「验证门禁」（各 ≤20 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[3351. Sum of Good Subsequences](https://leetcode.com/problems/sum-of-good-subsequences/)（**Day 42, Aug 2026 Daily Challenge**；承接 0811 的 3350 Medium，今日 **Hard**「好子序列=相邻差恰为 1，求所有好子序列元素和 mod 1e9+7」）：DP 维护以 `x` 结尾的子序列 **count `g[x]`** 与 **sum `f[x]`**，扫描时从 `x±1` 扩展。明日动作：手写 hashmap 版 O(n)；写 **≤15 词**：「差为 1 扩展」与 agent trace「合法相邻 action 链 DP」的类比。

- **IELTS（≤1 条）**：[IELTS Reading Practice Tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/reading)：0811 已轮换 Listening，今日换 **Reading**（Academic 或 General 任选，60 分钟 3 sections）。明日动作：完成 1 套 Reading，记录 **≥3 个** 易错题型（T/F/NG、heading match、同义替换）；写 **≤15 词** 你最弱的 Reading 题型。

## 6) 噪音过滤记录

- **LangGraph #8600–#8605**：与 `0811.md` 已收录的 #8591–#8594（SQLite rollback / CLI telemetry）语义重复 → 不重复。
- **LangChain #39609/#39605 model-profiles refresh**：自动化数据刷新，无 actionable insight → 过滤。
- **LangChain #39602 postponed annotations**：与 `0811.md` #39583 同 fix 不同 PR 号 → 语义重复。
- **AutoGen #8039 cache copy**：边缘 cache 变异 fix，与当前 focus 弱相关 → 过滤。
- **vLLM #51899 shared cache key binding**：安全向，与 Attention focus 弱相关 → 过滤。
- **vLLM #51823 BGE-M3 pooling**：embedding 任务，非 agent serving 主路径 → 过滤。
- **LlamaIndex #21897 Gemini token counting**：与 0810 token 主题部分重叠，信号弱于 MCP 2.x → 过滤。
- **Simon Willison Aug 12**：仅观鸟记录，无 AI 技术帖 → 不收录。
- **Anthropic Agentic Misalignment**：`0811.md` 已收录 → 不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-08-12** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Aug 12 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **718**（`0811.md` §7: 710 + 本日 §1: 8）
- Theory: **320**（`0811.md` §7: 315 + 本日 §2: 5）
- Career/Business: **118**（`0811.md` §7: 117 + 本日 §4: 1）
- LC: **100**（`0811.md` §7: 99 + 本日 §5: 1）
- IELTS: **100**（`0811.md` §7: 99 + 本日 §5: 1）
