# Daily Research - 2026-07-10 (21:30)

## 0) 今日3条结论

- **LangGraph 1.2.9（#8316 merged 2026-07-10）打包 #8315**：非 fresh `update_state` 在 `DeltaChannel` 上未推进 `counters_since_delta_snapshot`，Postgres checkpointer replay 会错位 → eval/生产若用 delta channel + `update_state` 热修 state，应升级到 1.2.9 并加 replay 回归。
- **LlamaIndex #22299 暴露 skill/state 注入位点 bug**：`state_prompt` 在首步 LLM 后 `formatted_input_with_state=True` 永不复位，tool 改 `ctx.store["state"]` 后后续步仍见 raw user message → skill 落地必须把「state 刷新」写成可测 invariant，而非假设 prompt 每步重渲染。
- **LlamaIndex #22294 跨 7 框架的 judge 注入族**：`find("{")`→`rfind("}")` 可被 model-under-test 输出劫持 eval verdict → collect→test 的 LLM-as-judge 链路需把 JSON 抽取当作安全边界，与 token 优化（少 retry loop）同属 eval 隐性税。

## 1) Agent工程化（high-signal）

- [fix: updateState metadata/counters for delta channel（LangGraph PR #8315 → release #8316 **1.2.9** merged **2026-07-09/10**）](https://github.com/langchain-ai/langgraph/pull/8315)
  - 摘要: 非 fresh `update_state` 在 `DeltaChannel` 未推进 `counters_since_delta_snapshot`；Postgres checkpointer 依赖该 counter 做 replay。#8315 fix + #8316 发布 1.2.9 打包；11 个 targeted test + Postgres integration 3 cases pass。
  - 为什么重要(对我): **skill 落地 + eval**——delta channel 是 LangGraph 长时 agent state 的增量快照机制；counter 错位会导致 replay 后 state 与 trace 不一致，parallel eval 产生假阳性/假阴性。
  - 明日动作(<=20min): 写 **是/否**：你 LangGraph agent 是否用 `update_state` 热修 thread state（是/否/仅 fresh run）。

- [fix(core): refresh state_prompt on the next LLM step after a tool mutates state（LlamaIndex PR #22299 updated **2026-07-10**）](https://github.com/run-llama/llama_index/pull/22299)
  - 摘要: `FunctionAgent`/`AgentWorkflow` 用 `initial_state` + `state_prompt` 时，首步正确格式化，但 tool 改 `ctx.store["state"]` 后后续 LLM 步仍见 raw user message。根因：`formatted_input_with_state` 首步后置 True 后 tool-loop 不复位。Fix：在 `aggregate_tool_results()` 返回下一 `AgentInput` 前复位 flag（single + multi-agent path）。
  - 为什么重要(对我): **skill 落地**——state_prompt 是 skill 规则注入 state 的 harness 位点；与 0709 Nemotron harness playbook「规则放哪」直接同构——tool 改 state 后 prompt 必须重渲染。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 是否在 tool 后刷新 state prompt（有/无/未知）。

- [fix(core): stop asyncio.gather from leaking orphaned tool tasks in apredict_and_call（LlamaIndex PR #22298 updated **2026-07-10**）](https://github.com/run-llama/llama_index/pull/22298)
  - 摘要: `apredict_and_call()` 并行 tool 时，hallucinated tool name 在 lookup 阶段抛 `KeyError`；`gather(return_exceptions=False)` 不 cancel 兄弟 task → 孤儿 task 泄漏 socket/FD。Fix：`return_exceptions=True`，unknown name 转为 errored `ToolOutput`。
  - 为什么重要(对我): **eval + token**——parallel tool eval 中一个坏 call 可导致资源泄漏 + 隐性重试成本；eval 应断言 `asyncio.all_tasks()` 无新增 pending。
  - 明日动作(<=20min): 写 **是/否**：你 parallel tool eval 是否测 hallucinated tool name path（是/否/仅 happy path）。

- [Fix verdict-injection in _marshal_llm_to_json（LlamaIndex PR #22294 updated **2026-07-10**）](https://github.com/run-llama/llama_index/pull/22294)
  - 摘要: `output_parsers/utils.py` 用 `find("{")`→`rfind("}")` 抽 JSON，model-under-test 输出可劫持 judge verdict（prompt-injection-into-judge，HIGH）。Fix：`_find_all_balanced_json()` 取最后完整 JSON object。同族漏洞已在 deepeval/ragas/guardrails/promptfoo/instructor/CrewAI 开 PR。
  - 为什么重要(对我): **eval 安全**——LLM-as-judge 是 map_prompt_test L2/L3 常见路径；JSON 抽取 bug = eval 可被 adversarial trace 污染，分数不可信。
  - 明日动作(<=20min): 写 **≤15 词**：你 judge 输出解析用 first-to-last brace 吗（是/否/未知）。

- [Fix streaming tool call premature fire on empty args（LangChain PR #38777 closed **2026-07-10**）](https://github.com/langchain-ai/langchain/pull/38777)
  - 摘要: Fixes #38682。streaming 路径在 tool args 仍为空时过早 fire tool call 事件 → 下游 handler 收到残缺 call、可能触发无效执行或重复 retry。Fix 延迟 fire 直至 args 非空或流结束。
  - 为什么重要(对我): **skill 落地 + token**——streaming eval trace 若记录 phantom tool call，会 inflate tool-call 计数与 token 重试 loop；与 0709 vLLM spec-decode grammar 前置过滤同属「premature emission」族。
  - 明日动作(<=20min): 写 **是/否**：你 streaming eval 是否过滤 empty-args tool call（是/否/未测）。

- [Your coding agent bill doubled. Here's how to fix it.（LangChain Blog, **2026-07-02**）](https://www.langchain.com/blog/fix-your-coding-agent-bill)
  - 摘要: 2026 H1「tokenmaxxing」后账单失控根因是**工具碎片化**（Claude Code/Cursor/Copilot 各记各的）。LangSmith 统一 trace Claude Code、Codex、Cursor、Copilot Chat、Pi、OpenCode；四阶段循环：See spend → Standardize → Optimize（Engine 标 redundant tool call）→ Govern（LLM Gateway spend cap + OSS routing）。
  - 为什么重要(对我): **token 优化杠杆**——agent 工程化不只减 prompt，更要跨工具归一化 session 成本；Engine 标 redundant context retrieval 可直接映射 skill 合并/缓存策略。
  - 明日动作(<=20min): 写 **≤15 词**：你最近 1 次 agent session 有无 redundant tool call（有/无/未查 trace）。

- [Research: Using DSPy to evaluate and improve Datasette Agent's SQL system prompts（Simon Willison Blog, **2026-07-02**）](https://simonwillison.net/2026/Jul/2/dspy-datasette-agent-prompts/)
  - 摘要: 用 DSPy harness 对 Datasette Agent 生产 SQL system prompt 做 gold-standard eval + 优化。关键发现：schema 只列表名 +「别调 describe_table」建议 → 模型猜列名（page_count, o.order_id）引发 error-retry loop；应在 schema listing 含列名或软化建议。
  - 为什么重要(对我): **eval + skill 落地**——prompt 里「省 token 的省略」可能因 retry loop 反增总 token；与 0709 harness tuning「规则放 tool return 有效」形成对照实验。
  - 明日动作(<=20min): 写 **≤15 词**：你 skill prompt 是否省略 schema 细节（是/否）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix][Attention] Preserve post-load tensors across weight reloads（vLLM PR #48251 updated **2026-07-10**）](https://github.com/vllm-project/vllm/pull/48251)
  - 摘要: `process_weights_after_loading()` 派生 tensor 在 layerwise reload 后被替换为新 allocation：标准 MLA 的 `W_UV`/`W_UK_T` 使 CUDA graph 持 obsolete 地址；FlashInfer sink 从 BF16→FP32 `.to()` 脱离 reloadable parameter。Fix：`replace_parameter(prefer_copy=True)` 原地刷新 + sink buffer in-place update；Level-2 sleep/reload 场景回归。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) stale attention sink 是否扭曲 multi-turn KV 轨迹？(2) 与 0709 P/D MTP lookahead slot 污染是否同族「reload 后 state 不一致」？(3) eval 是否分层报告 sleep/reload 后分数 swing？
  - 明日动作(<=20min): 写 **是/否**：你 serving 是否用 weight reload / sleep-wake（是/否/仅 cold start）。

- [[Kernel][ROCm][Perf] FlyDSL decode-attention kernel for 4-bit TurboQuant KV cache（vLLM PR #47896 updated **2026-07-10**）](https://github.com/vllm-project/vllm/pull/47896)
  - 摘要: AMD 上游 FlyDSL TurboQuant 4-bit KV decode kernel（gfx950/MI355X opt-in `VLLM_ROCM_TQ_FLYDSL_DECODE=1`）。支持 HEAD_SIZE=128、GQA {8,16,6}、可选 in-kernel Hadamard。GSM8K：MiniMax-M2.5 88.17→87.95/88.40；Qwen3-32B 95.45→95.15/95.07（±0.4pp）。
  - 为什么重要(对我): **Talk ≥3 turns + token**：(1) 4-bit KV 是否改变 ≥3 turn tool-call 的 attention 质量边界？(2) agentic serving 长 context 的 KV 税可否量化到 bit-width 层？(3) eval 是否报告 quant KV on/off？
  - 明日动作(<=20min): 写 **是/否**：你 agent serving 是否开 KV quant（是/否/未知）。

- [The Key to Going Linear: Analysis-Driven Transformer Linearization（arXiv **2607.07706**, July 2026）](https://arxiv.org/abs/2607.07706)
  - 摘要: 冻结 backbone 严格 regime 下比较 linear attention 替换：一阶近似证明 softmax 依赖 key-dependent rank-1 正交投影 → delta-style（Gated Delta Networks）优于纯 gated accumulation（GLA）。结构干预：sink tokens、short conv、fixed-budget cache routing 缩小 gap；LLaMA/Qwen 至 32B，MMLU 超 prior post-hoc baselines。
  - 为什么重要(对我): **Attention 机制**：delta update 的 rank-1 几何校正解释「为何 linearized attention 在 long-context agent 仍可用」；与 0709 Fingerprint 谱分析、0708 Legible selective detector 形成 attention 可解释性三角。
  - 明日动作(<=20min): 写 **≤15 词**：你 long-context eval 是否测 linear vs softmax attention（有/无）。

## 3) 热门项目/可试个人项目

- [DSPy + Datasette Agent prompt optimization（Simon Willison research thread）](https://simonwillison.net/2026/Jul/2/dspy-datasette-agent-prompts/)
  - 摘要: 用 DSPy 对生产 SQL prompt 做 harness eval：live in-process Datasette + gold dataset + custom metrics。发现 schema 省略导致 retry loop；可用 GPT-4.1 mini/nano 低成本筛 prompt 变体。
  - 为什么重要(对我): **eval 杠杆**——窄域 agent（SQL/RAG）可用 DSPy 替代从零建 eval infra；直接映射 map_prompt_test collect→test 的 cheap iteration。
  - 明日动作(<=20min): `pip install dspy datasette-agent`；读 research 页；写 **≤15 词** 你最该 eval 的 1 条 skill 规则。
  - 估计复现成本: **低**（CPU + 小模型 API；无需 GPU）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Your coding agent bill doubled. Here's how to fix it.（LangChain Blog **2026-07-02**）](https://www.langchain.com/blog/fix-your-coding-agent-bill)
  - 机会信号: **一人公司 signature 切口**——跨工具统一 trace + Engine 自动标 waste = 可对外讲的「agent 成本工程」方法论；harness/skill 优化比追 frontier model 更具可复制性与客户付费点（LangSmith GA 面向非开发者建 agent）。
  - 风险信号: 早期团队可能只需单工具 native dashboard；过度 governance（Gateway cap）可能阻断探索性 agent session；「6x bill」叙事需用自家 trace 验证而非行业 anecdote。
  - 我可执行的一步(<=30min): 选 1 个你常用 coding agent，手动估算最近 7 天 session 数 × 估 token；写 **3 条** 可合并的 redundant call 模式（各 ≤10 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 3534 Path Existence Queries in a Graph II](https://leetcode.com/problems/path-existence-queries-in-a-graph-ii/)：在 3532（0709）基础上求**最短路径**而非仅连通性；按值排序 + binary jumping/sparse table，O(n log n) 预处理。明日动作：先复习 3532 grouping；再写 shortest-path 版（≤25min）；写 **≤15 词**：与 agent 多 hop tool 链最短路径的类比。

- **IELTS（≤1 条）**：[IELTS Preparation Tips 2026: Score Band 7+（EEC Global）](https://eecglobal.com/blog/ielts-preparation-tips-band-7-2026)：Speaking Band 7 关键——Part 1 答 2–4 句、Part 2 用 1min 准备写关键词、Part 3 用 discourse markers 延展（"What I mean is..."）；每日录音自检 hesitation/fillers。明日动作：选 1 个 Part 3 题录音 2min（≤20min）；写 **≤15 词** 最弱 criterion（Fluency/LR/GRA/Pronunciation 选 1）。

## 6) 噪音过滤记录

- **LangGraph #8312（root messages inbox bounds）**：`0709.md` 已收录，仍 open → 不重复；待 merge 后再收。
- **AutoGen #7936 SelectorGroupChat livelock**：`0709.md` 已收录 → 不重复。
- **LlamaIndex #22288 AG-UI frontend tool messages**：`0709.md` 已收录 → 不重复。
- **PACE（2607.02032）/ Harness-Bench（2605.27922）/ Nemotron harness playbook**：`0709.md` 已收录 → 不重复。
- **Fingerprint（2607.06621）/ Legible-by-Construction（2607.04319）**：`0708.md`/`0709.md` 已收录 → 不重复。
- **vLLM #47885 grammar-aware spec decode / #46694 MTP KV**：`0709.md` 已收录 → 不重复。
- **LangGraph #8317/#8318 deps bump**：chore only → 丢弃。
- **LangChain #38775/#38773 PIIMatch export / #38776 o-series tokens**：infra/public API，弱于今日 focus → 丢弃。
- **LangChain #38737/#38763 regex injection security**：security fix，非 agent 语义主线 → 丢弃。
- **AutoGen #7942 file:// protocol / #7941 docstring**：security/docs → 丢弃。
- **vLLM #38476 MLA sparse / #42562 router GEMM / #48164 CI audit**：kernel/infra，弱于 attention reload + TQ decode → 不重复展开。
- **openai/evals #1691 community health**：非 eval 方法论 → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-10** 原帖 URL → 不收录。
- **@gefei55 / @ianneo_ai 职业帖**：无法验证当日原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv + LangChain/Simon Willison Blog 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **497**（`0709.md` §7: 490 + 本日 §1: 7）
- Theory: **203**（`0709.md` §7: 200 + 本日 §2: 3）
- Career/Business: **89**（`0709.md` §7: 88 + 本日 §4: 1）
- LC: **71**（`0709.md` §7: 70 + 本日 §5: 1）
- IELTS: **71**（`0709.md` §7: 70 + 本日 §5: 1）
