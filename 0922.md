# Daily Research - 2026-09-22 (21:30)

## 0) 今日3条结论

- **Gemini 并行 `tool_call` 无 `index` 会在 v3 stream 组装时静默丢调用**（LangChain PR **#40759**, created **2026-09-22**）：每 call 独立 chunk 时 positional key 碰撞 → 只保留最后一个 → **L2 eval 须覆盖「多 tool 同轮 + 无 index」并断言 `tool_calls` 数量**。
- **`ToolStrategy` 结构化输出缺 `max_retries` 会无限重试同一无效 tool**（#**40754**）+ **`ToolRetryMiddleware` 耗尽后不再写 “Please try again.”**（#**40742**）→ **token 预算与 effectful tool 的 retry gold 须分轨**（避免 transcript 胀满 + 重复执行）。
- **DeepSeek-V4.1 decoder SWA bounded replay 落地**（vLLM **#58132**）+ **DiffKV 多 token split-KV**（#**58141**）→ **长上下文 agent 的 prefill/verify 路径与 SWA 层 eval 须按模型族分桶**，不可沿用全序列 Attention 假设。

## 1) Agent工程化（high-signal）

- [fix(core): key un-indexed `tool_call` blocks by `id` in v3 streams（LangChain PR #40759, created **2026-09-22**）](https://github.com/langchain-ai/langchain/pull/40759)
  - 摘要: 无 `index` 的完整 client `tool_call` 现按 `id` 分桶；`tool_call_chunk` 仍走 positional（`id` 分片会碎裂）。修复 Gemini 等「每 call 一 chunk、均在 position 0」时 **并行调用被覆盖**（含 `thought_signature` 静默占位）。
  - 为什么重要(对我): **skill 落地 + 评测**——多 tool 同轮是 L2 标配，streaming 组装是 **契约层**。
  - 明日动作(<=20min): 写 **是/否**：eval 是否断言并行 `tool_calls` 数量（是/否/未知）。

- [feat(anthropic,openai): mid-conversation tool changes on `SystemMessage`（LangChain PR #40758, created **2026-09-22**）](https://github.com/langchain-ai/langchain/pull/40758)
  - 摘要: `tool_addition`/`tool_removal`（Anthropic）与 `additional_tools`（OpenAI Responses）经 `SystemMessage` + `non_standard` 块上送；Chat Completions 对 OpenAI tool change **显式报错**。
  - 为什么重要(对我): **skill 落地**——会话中途改 tool 集是 **HITL/版本化 skill** 的产品形态，须写 routing gold。
  - 明日动作(<=20min): 写 **1 条**：你的 agent 是否允许 mid-conversation 增删 tool（是/否/未知）。

- [feat(langchain): add max_retries cap to ToolStrategy structured output（LangChain PR #40754, created **2026-09-22**）](https://github.com/langchain-ai/langchain/pull/40754)
  - 摘要: `ToolStrategy` 新增 opt-in `max_retries`；按尾部 error `ToolMessage` 计数，超限抛验证错误而非无限循环（fixes #40753）。
  - 为什么重要(对我): **评测 + token**——结构化输出 stuck loop 是 **context 爆炸** 典型根因。
  - 明日动作(<=20min): 写 **是/否**：`ToolStrategy` 是否设 `max_retries`（是/否/未知）。

- [fix(anthropic): support client toolsets（LangChain PR #40756, created **2026-09-22**）](https://github.com/langchain-ai/langchain/pull/40756)
  - 摘要: 支持 `browser_toolset_20260801`；`toolset_name` 在 raw/v1/stream/result 往返保留，结果关联限定 **最近 assistant turn**。
  - 为什么重要(对我): **skill 落地**——browser toolset 与 API agent 的 **多轮 identity** 是合规 eval 新维度。
  - 明日动作(<=20min): 写 **是/否**：是否用 Anthropic client toolset（是/否/未知）。

- [fix(langchain): stop telling the model to retry an exhausted tool call（LangChain PR #40742, created **2026-09-22**）](https://github.com/langchain-ai/langchain/pull/40742)
  - 摘要: `ToolRetryMiddleware` 耗尽后 `ToolMessage` 不再以 “Please try again.” 结尾，只报告工具名/次数/异常（fixes #40688）；retry 次数本身不变。
  - 为什么重要(对我): **token + 副作用 eval**——避免模型在 **未知是否落地** 后再发起同 tool。
  - 明日动作(<=20min): 写 **是/否**：是否启用默认 `ToolRetryMiddleware`（是/否/未知）。

- [fix agents retry failed return-direct tools（LangChain PR #40739, created **2026-09-22**）](https://github.com/langchain-ai/langchain/pull/40739)
  - 摘要: `return_direct=True` 工具 **失败** 时路由回 model 而非直接结束；成功仍 direct return（fixes #36411）。
  - 为什么重要(对我): **评测**——return-direct 失败路径常被 gold 遗漏。
  - 明日动作(<=20min): 写 **1 条** gold：return-direct 工具抛错后是否应再规划（≤12 词）。

- [fix(typesafe): hide `model_route` from agent input schema（LangChain PR #40733, created **2026-09-22**）](https://github.com/langchain-ai/langchain/pull/40733)
  - 摘要: `ModelRouterMiddleware` 的 `model_route` 标 `OmitFromInput`，避免 caller 误当作可控输入（fixes #40729）。
  - 为什么重要(对我): **skill/schema eval**——内部 state 泄漏进 input schema 会污染 **tool-calling 边界测试**。
  - 明日动作(<=20min): 写 **是/否**：agent input schema 是否含 router 字段（是/否/未知）。

- [fix(core): stop counting base64 payloads as characters in count_tokens_approximately（LangChain PR #40732, created **2026-09-22**）](https://github.com/langchain-ai/langchain/pull/40732)
  - 摘要: `input_image` 按固定 image 惩罚计；未知块经 `_repr_without_payloads()` 剥离 base64/`data:` URL（fixes #40731）；`trim_messages` 默认计数不再 **10万级误杀历史**。
  - 为什么重要(对我): **token 优化**——多模态 agent 的 **trim 门禁** 须与真实计费对齐。
  - 明日动作(<=20min): 写 **是/否**：是否用 `trim_messages` + 近似计数（是/否/未知）。

- [fix(anthropic): reject sampling params deprecated for Opus 4.7+, Opus 5 and Sonnet 5（LangChain PR #40743, created **2026-09-22**）](https://github.com/langchain-ai/langchain/pull/40743)
  - 摘要: 将 Fable 已有本地校验扩展到 Opus 4.7+/Opus 5/Sonnet 5；非默认 `temperature`/`top_p` 与任意 `top_k` **构造/invoke 即 ValueError**（fixes #40721）。
  - 为什么重要(对我): **评测 fail-fast**——避免 HTTP 400 后才暴露配置错误。
  - 明日动作(<=20min): 写 **是/否**：eval harness 是否仍传 `top_k`（是/否/未知）。

- [fix(checkpoint): preserve explicit empty fields list in index config（LangGraph PR #9039, created **2026-09-22**）](https://github.com/langchain-ai/langgraph/pull/9039)
  - 摘要: `fields=[]` 不再被 `or ["$"]` 变成全文档嵌入；仅 `None`/缺失才默认 `["$"]`（fixes #9033；Memory + Postgres store）。
  - 为什么重要(对我): **RAG store + skill 记忆**——「禁用默认向量、按 put  opt-in」是 **检索 eval 静默污染** 温床。
  - 明日动作(<=20min): 写 **是/否**：store `index_config.fields` 是否显式 `[]`（是/否/未知）。

- [fix(core): pass ChatMessage input to memory blocks（LlamaIndex PR #23180, created **2026-09-22**）](https://github.com/run-llama/llama_index/pull/23180)
  - 摘要: `Memory.get/aget` 传入的 `ChatMessage` 现进入 block 上下文；此前仅 string 生效，空 short-term 时 **VectorMemory 无召回**。
  - 为什么重要(对我): **多轮 agent eval**——当前 user turn 作 retrieval query 是 L2 基础假设。
  - 明日动作(<=20min): 写 **是/否**：Memory API 是否传 `ChatMessage`（是/否/未知）。

- [fix(core): separate query and text embedding cache entries（LlamaIndex PR #23181, created **2026-09-22**）](https://github.com/run-llama/llama_index/pull/23181)
  - 摘要: `BaseEmbedding` 将 query/text 缓存分集合；同文本双角色不再 **错用对方向量**（不对称模型如 Voyage）。
  - 为什么重要(对我): **RAG eval**——检索 gold 对 query/doc embedding 分工敏感。
  - 明日动作(<=20min): 写 **是/否**：是否不对称 embedding（是/否/未知）。

- [fix(core): replace SQL chat history atomically（LlamaIndex PR #23183, created **2026-09-22**）](https://github.com/run-llama/llama_index/pull/23183)
  - 摘要: `SQLAlchemyChatStore.set_messages` 删除与插入 **同一事务**；中途序列化/插入失败不再留下空历史。
  - 为什么重要(对我): **HITL + 记忆 eval**——部分写入会破坏 **resume gold**。
  - 明日动作(<=20min): 写 **是/否**：是否 SQL chat store（是/否/未知）。

- [fix(autogen-ext): preserve canvas tools when clearing memory（AutoGen PR #8269, created **2026-09-22**）](https://github.com/microsoft/autogen/pull/8269)
  - 摘要: `TextCanvasMemory.clear()` 原地清文件/修订历史，已注册 canvas 工具仍指向 **同一实例**；避免「写成功但未进下一轮 context」。
  - 为什么重要(对我): **skill 落地**——可复用 canvas 是 **多轮 artifact agent** 常见模式。
  - 明日动作(<=20min): 写 **是/否**：是否 clear 后复用 canvas memory（是/否/未知）。

- [[Bugfix][Frontend] Aggregate completion cached tokens（vLLM PR #58116, created **2026-09-22**）](https://github.com/vllm-project/vllm/pull/58116)
  - 摘要: batched completion 的 `cached_tokens` 现跨 prompt 汇总；streaming 按 index 累加，修复 **只报首/末 prompt** 的用量。
  - 为什么重要(对我): **token 优化 eval**——prefix cache 指标是 **成本门禁**，批处理不可低估。
  - 明日动作(<=20min): 写 **是/否**：eval 是否读 completion `cached_tokens`（是/否/未知）。

- [[Bugfix] [Rust Frontend][Parser] Recover DeepSeek DSML invokes without a calls opener（vLLM PR #58057, created **2026-09-22**）](https://github.com/vllm-project/vllm/pull/58057)
  - 摘要: Rust frontend 识别缺 `calls` 开标记的完整 DSML `invoke`，与 Python 对齐 `tool_calls`（fixes #48931）。
  - 为什么重要(对我): **评测 harness**——Rust/Python 双前端须 **同 gold**。
  - 明日动作(<=20min): 写 **是/否**：serving 是否 Rust frontend（是/否/未知）。

- [[Bugfix][Tool Parser] phi4_mini_json: report no tool call for an empty payload（vLLM PR #58072, created **2026-09-22**）](https://github.com/vllm-project/vllm/pull/58072)
  - 摘要: 空 `functools[]` 或非 JSON payload 不再 `tools_called=True` 且吞掉正文；恢复 `tools_called == bool(tool_calls)` 不变量。
  - 为什么重要(对我): **tool eval**——假阳性 tool call 会 **清空模型文本输出**。
  - 明日动作(<=20min): 写 **是/否**：是否 phi4 + auto tool choice（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Perf][Attention] Enable bounded multi-token DiffKV split-KV（vLLM PR #58141, created **2026-09-22**）](https://github.com/vllm-project/vllm/pull/58141)
  - 摘要: DiffKV 在「每 CTA 单 query、行数在 scratch 内」时走 split-KV；长上下文 speculative verify 不再强制 2D attention；含 padded graph capture 测例。
  - 为什么重要(对我): **Talk ≥3 turns + KV/Attention**——spec verify 是 agent **多轮草稿接受率** 的算力瓶颈。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 DiffKV + spec decode（是/否/未知）。

- [[Model] Decoder-side SWA bounded replay for DeepSeek-V4.1（vLLM PR #58132, created **2026-09-22**）](https://github.com/vllm-project/vllm/pull/58132)
  - 摘要: KV-source 层之后 SWA 层 prefill 仅 replay **末 128 token**；piecewise graph 下 eager break，FULL decode 图不变；gsm8k A/B 与 `--no-swa-bounded-replay` 接近（#56752 简化重提）。
  - 为什么重要(对我): **Talk ≥3 turns + SWA**——decoder 侧近似改变 **长 history 多轮一致性** eval 边界。
  - 明日动作(<=20min): 写 **是/否**：是否 DSv4.1 + prefix hit（是/否/未知）。

- [[Attention] Support scalar FP8 KV cache in Triton DiffKV（vLLM PR #58128, created **2026-09-22**）](https://github.com/vllm-project/vllm/pull/58128)
  - 摘要: Triton DiffKV 读 packed FP8 KV 并分 K/V descale；MiMo decoder 转发 cache policy，保留 full-attention 与显式 sliding window。
  - 为什么重要(对我): **Talk ≥3 turns + FP8 KV**——与 0921 FlashInfer dequant scratch 同属 **容量/精度** 维度。
  - 明日动作(<=20min): 写 **≤15 词**：是否 FP8 KV + DiffKV（是/否/未知）。

- [[Bugfix][Attention] Fixed-width sparse indexer prefill logits（vLLM PR #58068, created **2026-09-22**）](https://github.com/vllm-project/vllm/pull/58068)
  - 摘要: 稀疏 MLA indexer prefill 的 DeepGEMM logits 按 `max_model_len/compress_ratio` **固定列宽**分配，避免长 prefill 每 chunk 新尺寸导致 UMA 设备内存暴涨（fixes #55569；默认 integrated GPU 开启）。
  - 为什么重要(对我): **Talk ≥3 turns + sparse Attention**——长 prompt agent 的 **prefill 内存** 是可用性上限。
  - 明日动作(<=20min): 写 **是/否**：是否 GLM/DeepSeek sparse indexer（是/否/未知）。

- [[Bugfix] Pin num_splits for the FA4 hd256 kernel in sparse MLA（vLLM PR #58075, created **2026-09-22**）](https://github.com/vllm-project/vllm/pull/58075)
  - 摘要: sparse MLA 路径为 FA4 hd256 kernel 固定 `num_splits`，避免错误切分影响数值/性能。
  - 为什么重要(对我): **Talk ≥3 turns + MLA**——draft/target 混部时 **Attention 后端参数** 须进模型 recipe。
  - 明日动作(<=20min): 写 **≤15 词**：是否 sparse MLA + FA4（是/否/未知）。

- [[Bugfix][XPU] Forward num_splits to the XPU flash attention kernel（vLLM PR #58131, created **2026-09-22**）](https://github.com/vllm-project/vllm/pull/58131)
  - 摘要: XPU flash attention 现接收 `num_splits`，与 CUDA 路径调度语义对齐。
  - 为什么重要(对我): **Talk ≥3 turns + 跨硬件 Attention**——异构 eval 须分平台断言延迟/数值。
  - 明日动作(<=20min): 写 **是/否**：是否在 XPU 跑 flash attn（是/否/未知）。

- [[Core][KV Events] Emit skipped ancestry for partial cache events（vLLM PR #58121, created **2026-09-22**）](https://github.com/vllm-project/vllm/pull/58121)
  - 摘要: `cache_partial_block` 在物理块大于 `hash_block_size` 时填充 `skipped_*` 元数据，消费者可重建被省略的细粒度 hash 链（stack #44488）。
  - 为什么重要(对我): **Talk ≥3 turns + prefix cache**——KV 事件流是 **多轮命中审计** 的数据面。
  - 明日动作(<=20min): 写 **是/否**：是否消费 KV cache events（是/否/未知）。

## 3) 热门项目/可试个人项目

- [[Benchmark] Record model_id in bench latency/throughput --output-json（vLLM PR #58112, created **2026-09-22**）](https://github.com/vllm-project/vllm/pull/58112)
  - 摘要: `vllm bench` 的 `--output-json` 写入 `model_id`，便于多模型/多版本压测结果 **可追溯合并**。
  - 为什么重要(对我): **评测杠杆**——可把 bench JSON 纳入 **agent serving 回归流水线**（对齐 0920 model recipes）。
  - 明日动作(<=20min): 给现有 bench 输出 **加一列** `model_id` 对照（或确认已含）。
  - 估计复现成本: **低**（拉 main + 跑一次 bench --output-json，~20min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [`thinking_mismatch_allowed` in `input_transformations`（Anthropic Release notes **2026-09-14**）](https://docs.anthropic.com/en/release-notes/api)
  - 机会信号: `thinking-binding-controls-2026-08-01` beta 在 **未强制 prefix 校验** 的请求上记录 `thinking_mismatch_allowed` → Enterprise 可卖 **「生产流量 thinking 绑定审计」** 再引导客户开 enforcement。
  - 风险信号: 与 **多轮 eval 可复现** 冲突——历史被编辑时 thinking 块行为因账号/模型而异。
  - 我可执行的一步(<=30min): 写 **3 行**：你是否存 `input_transformations` / 是否计划 enforcement / 对 eval 影响（每格 ≤8 词）。

- [Managed Agents `auto` permission + `ant beta:sessions connect`（Anthropic Release notes **2026-09-10**）](https://docs.anthropic.com/en/release-notes/api)
  - 机会信号: 服务端逐 call 评估 run/deny/pause + CLI **附着会话审批** → 出海可对标「托管 agent vs 自研 harness」的 **合规运维** 叙事。
  - 风险信号: `auto` 策略与自有 HITL gold **语义不一致** 时难做 A/B。
  - 我可执行的一步(<=30min): 写 **1 句**：你更熟 **自研 middleware HITL** 还是 **托管 auto policy**（≤20 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[3500. Find X Value of Array II](https://leetcode.com/problems/find-x-value-of-array-ii/)（**2026-09-22 Daily Challenge**，Hard，LeetCode GraphQL `activeDailyCodingChallengeQuestion`）：在 I 基础上加强约束；与 **分段聚合 + 更强不变量** 同构。明日动作：一题或写 **≤15 词**「Hard 日题」对你周计划意味着什么。

- **IELTS（≤1 条）**：[IELTS Reading practice tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/reading)：0921 为 Listening，今日 **Reading 一组（~20min 计时）**。明日动作：写 **2 个** 失误类型（定位/同义替换）；**≤15 词** 最弱 Reading 题型。

## 6) 噪音过滤记录

- **LangChain #40747/#40750 model profiles & OpenWiki**：维护/文档 → 过滤。
- **LangChain #40735–#40737/#40740 release & fireworks tests**：发布/CI → 过滤。
- **LlamaIndex #23173–#23178 GeoMind Resource**：重复 spam PR → 过滤（只保留有实质修复的 #23172+）。
- **LlamaIndex #23184/#23185**：与 #23183 同族 SQL store；#23185 空 batch 边界已单列，#23184 serialize init 弱信号 → 过滤 #23184。
- **vLLM #58148 Grafana $__rate_interval**：仪表盘 → 过滤。
- **vLLM #58081 dependabot 191 updates**：批量依赖 → 过滤。
- **vLLM #58110–#58111 encoder-decoder translation**：非今日 agent focus → 过滤。
- **vLLM #58041 KV pause RL 长文**：高信号但偏 RL 运维 niche → 未进主报告（可自行跟进 Prime-RL）。
- **Anthropic Sep 14 compaction / Sep 18 Chrome compliance**：0919–0921 已引用 → 今日 §4 换 **thinking binding + Managed Agents**。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-22** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + Anthropic Docs 当日 ≥8 条主信号，无需 skill 补充。
- **openai/evals**：2026-09-22 无新 PR → 不收录。
- **Simon Willison / Lil'Log**：**2026-09-22** 无新帖（`/2026/Sep/22/` 404）→ 不收录。

## 7) 本周累计计数

- Agent: **1078**（`0921.md` §7: 1062 + 本日 §1: 16）
- Theory: **496**（`0921.md` §7: 489 + 本日 §2: 7）
- Career/Business: **158**（`0921.md` §7: 156 + 本日 §4: 2）
- LC: **135**（`0921.md` §7: 134 + 本日 §5: 1）
- IELTS: **135**（`0921.md` §7: 134 + 本日 §5: 1）
