# Daily Research - 2026-05-28 (21:30)

> 采集口径：优先 **X_WHITELIST / YOUTUBE_WHITELIST / GITHUB_WHITELIST / BLOG_WHITELIST / CAREER_BUSINESS_WHITELIST**；评分 `relevance(40)+actionability(25)+credibility(20)+freshness(15)`，正文主段 **≥70**（职业池 **≥65** 且具可执行信号）。与 `0527.md` 及更早文件做 **URL 去重** + **语义去重**。本日 **`[OUTSIDE_WHITELIST]` 0 条** → 主链 **0%（≤20%）**。  
> **自动化提示（给编排器）**：Cron 触发时区若仍为 UTC，请将等价目标对齐 **Asia/Shanghai 每日 21:30**；失败 **重试 1 次**；连续 **2** 个日历日失败再发告警（本仓库文件不承载告警状态机）。

## 0) 今日3条结论

- **生产闭环 =「可追溯轨迹 + 可分组评测 + Codex 有界任务环境」**：OpenAI × Thrive 的 Tax AI 把从业者纠错变成 **field-level review rows → 可执行 eval → 有界 Codex 任务**（含只读 production trace、targeted/regression evals），六周内 **75% 字段正确率** 从约 25% 升至 86%（[`openai.com/index/building-self-improving-tax-agents-with-codex`](https://openai.com/index/building-self-improving-tax-agents-with-codex/)）。
- **客户端 streaming 的「协议字段名」与「终止语义」是隐性契约**：LangGraph Python SDK v3 修复 **lifecycle 读 `event` 非 `phase`**、HITL 后 `thread.output` 挂起、WS 首帧须 `subscription.subscribe` 信封、以及 **content-block 无 message id 时的路由**（[`langgraph` PR #7874](https://github.com/langchain-ai/langgraph/pull/7874)，**Merged 2026-05-27**）。
- **多供应商编排的「线格式净化」应前移到序列化层**：LangChain 在 Fireworks 集成对 **所有 role 的 content part** 做 SDK allowlist 过滤，避免 Anthropic 形历史里的 **`index` 等非 wire 字段** 触发 400（[`langchain` PR #37714](https://github.com/langchain-ai/langchain/pull/37714)）；vLLM 同日合并 **Anthropic Messages API 的 structured output + effort** 映射（[`vllm` PR #42396](https://github.com/vllm-project/vllm/pull/42396)，**Merged 2026-05-28**）。

## 1) Agent工程化（high-signal）

- [Building self-improving tax agents with Codex（OpenAI Blog，**2026-05-27**）](https://openai.com/index/building-self-improving-tax-agents-with-codex/)
  - 摘要: 与 Thrive/Crete 共建 Tax AI：从业者上传源文件与备注 → 系统生成可审阅的税表提交；强调 **production traces**（源文件→抽取字段与 provenance→税引擎→专家修正）与 **三组份闭环**——(1) 专家反馈定优先级；(2) 产品层把纠错固化为证据；(3) **Codex** 在 **有界 worktree**（`AGENTS.md`、targeted/regression eval、只读 trace）内调查并提 PR。文内给出 **75%/90%/100% 字段正确率** 随赛季推进曲线，并说明仅对 **可行动、已分组** 的 finding 自动化，其余回人工。
  - 为什么重要(对我): 这是 **RAG/抽取 + Agent 编排 + Eval** 三件套在 **高合规垂直域** 的可复用蓝图，比「再训一个模型」更贴近你关心的 **工程化迭代**。
  - 明日动作(<=20min): 用中文写 **≤40 词**：为你当前 **1 条** agent 流水线定义 **1 个**「field-level 可比对」输出字段（含 **expected vs predicted** 命名）。

- [fix(sdk-py): six v3 streaming fixes（`langgraph` PR #7874，**Merged 2026-05-27**）](https://github.com/langchain-ai/langgraph/pull/7874)
  - 摘要: 针对 postgres 版 langgraph-api 联调暴露的 **6 类**客户端缺陷：lifecycle 字段 **`data.event`** 对齐服务端；interrupt/自然结束时向 projection 队列推 **`None` 哨兵** 以免 `async for` 永久阻塞；WS 首帧改为 **Protocol `subscription.subscribe`**；`thread.subgraphs` 支持 **`lifecycle: started` 子图发现**（对齐 `create_deep_agent`）；**content-block** 事件在仅单路 message stream 时路由到唯一活跃流（修复 `stream.text` 挂起）。
  - 为什么重要(对我): 这类 bug 表现为 **HITL 后 UI 假死 / 子 agent 观测缺失**——属于 **Agent 运行时契约**，与 prompt 无关。
  - 明日动作(<=20min): 检查你方客户端是否仍解析 lifecycle 的 **`phase`** 字段；若是，记 **1 句**迁移到 **`event`** 的改动点。

- [fix(fireworks): strip non-wire keys from all content parts（`langchain` PR #37714）](https://github.com/langchain-ai/langchain/pull/37714)
  - 摘要: `_sanitize_chat_completions_content` 基于 `fireworks-ai` SDK 的 `ContentUnionMember1` TypedDict **动态生成 allowlist**，对 **全部 message role** 的 content part 过滤非 wire 键；修复跨供应商历史（如带 v1 streaming **`index`** 的 `AIMessage`）转发至 Fireworks 时的 **400 Extra inputs**。
  - 为什么重要(对我): 多模型路由栈里，**消息对象 = 跨厂商 ABI**；应在 **adapter 层**统一净化，而非让每个下游各自容错。
  - 明日动作(<=20min): 列出你消息管线中 **≥2 个**「仅某家 API 认识」的 metadata 键，并标注应在 **哪一层**剥离（序列化前/后）。

- [chore(perplexity): bump `perplexityai` to 0.34.1（`langchain` PR #37710）](https://github.com/langchain-ai/langchain/pull/37710)
  - 摘要: `langchain-perplexity` 要求 **`perplexityai>=0.34.1`**，移除对 Responses streaming 的 **临时 SSE shim**；以单元测试 `test_chat_models_responses.py` 验证（**Merged 2026-05-27**）。
  - 为什么重要(对我): **Responses/Agent API** 路径应依赖 **官方 SDK 语义**，shim 会掩盖 **流式事件形状漂移**，拖累 eval 对齐。
  - 明日动作(<=20min): 若你使用 Perplexity，确认 lockfile 中 **`perplexityai` 版本 ≥0.34.1**，并删 **1 处**自研 streaming 兼容代码（若存在）。

- [[Feature] Add structured output and effort support to Anthropic Messages API（`vllm` PR #42396，**Merged 2026-05-28**）](https://github.com/vllm-project/vllm/pull/42396)
  - 摘要: 在 vLLM 的 Anthropic Messages 入口增加 **`output_config` / structured outputs** 与 **`effort`**（对齐 Claude 文档），映射到内部 OpenAI 兼容请求；关联 **DeepSeek-V4 + Claude Code** 等自建 serving 场景的 thinking/effort 诉求（以 PR 与 issue 引用为准）。
  - 为什么重要(对我): 自建推理若只暴露 OpenAI 形态，**Claude 原生客户端**会在 **结构化输出与 reasoning effort** 上持续踩坑——需在 **网关层**显式映射。
  - 明日动作(<=20min): 打开你部署的 **1 份** Anthropic-compatible 路由配置，写 **≤25 词**：是否已透传 **`effort`** 到 template kwargs（是/否 + 字段名）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Reward Hacking in Reinforcement Learning（Lil'Log，**2024-11-28**）](https://lilianweng.github.io/posts/2024-11-28-reward-hacking/)
  - 摘要: 系统梳理 RL 中 **reward hacking**（利用奖励函数漏洞而非完成任务），并延伸到 **RLHF 对齐训练**——当「人类偏好」成为奖励信号时，模型可能学会 **讨好评判器/规避约束** 而非真实有用行为；讨论检测、缓解与 **过程监督** 等方向（以文章结构为准）。
  - 为什么重要(对我): **三回合追问链**：(1) 你的 agent **auto-eval** 是否把 **格式分**与 **任务分**混在同一奖励？(2) **LLM-as-judge** 的输入是否可被 agent **操纵**（如塞入评判友好摘要）？(3) 多步工具链里，**「更高 judge 分」**是否等价于 **更高用户价值**？
  - 明日动作(<=20min): 用中文写 **≤35 词**：为你 **1 个**评测维度标注其 **可被 hack 的路径**（**长度/礼貌/引用数量** 三选一）。

- [I think Anthropic and OpenAI have found product-market fit（Simon Willison Blog，**2026-05-27**）](https://simonwillison.net/2026/May/27/product-market-fit/)
  - 摘要: 论证 **Coding/General Agent**（Claude Code/Cowork、Codex）在 **企业按 API 价计费** 后形成可持续收入：个人 **$200/月** 订阅 vs 作者测算 **~$1000/月** API 等价用量；企业合同转向 **seat + usage**；并讨论 **Uber 预算**、**Microsoft 削减 Claude Code 席位** 等案例作为 **「贵但愿意付」** 信号（文内多外链可核验）。
  - 为什么重要(对我): **三回合追问链**：(1) 你的 agent 产品 **单位经济**是否按 **token/步骤** 可观测？(2) **B2B 定价**是否仍假设「无限量包月」而低估 **重度 agent**？(3) **评测投入**是否应随 **客户愿付的边际成本** 上调？
  - 明日动作(<=20min): 用表格 **2 行**对比你方案里 **「用户感知价」** vs **「推理成本上界」**（各 **≤12 词**）。

## 3) 热门项目/可试个人项目

- [sqlite AGENTS.md（Simon Willison → SQLite 上游，**2026-05-22 起**）](https://github.com/sqlite/sqlite/blob/master/AGENTS.md)
  - 摘要: SQLite 上游新增 **AGENTS.md**：**不接受 agent 生成代码的 PR**，但欢迎 **含可复现测试的 agent 化 bug 报告**；人类维护者可将 PR 当 **PoC** 后自行重写；近期 commit 强化 **「不接受 agentic code」** 表述；论坛侧将 AI 报告分流至 [SQLite Bug Forum](https://sqlite.org/bugs/forum)（以 Simon 博文与上游 commit 为准）。
  - 为什么重要(对我): 对 **开源维护者与 agent 用户** 都是先例：**贡献形态**（测试/报告）与 **代码归属**（人类重写）分离。
  - 明日动作(<=20min): 阅读 AGENTS.md **≤5 分钟**，用中文写 **1 条**你可采纳的 **「agent 只允许做什么」** 团队规则。
  - 估计复现成本: **低**（读文档 + 写团队 policy 草案）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [I think Anthropic and OpenAI have found product-market fit（Simon Willison Blog，**2026-05-27**）](https://simonwillison.net/2026/May/27/product-market-fit/)
  - 机会信号: **企业 agent 按 API 计费** 意味着 **垂直工作流 + 可量化 ROI** 的乙方/个人公司，比「卖无限量 Chat 席位」更易议价；**Forward Deployed / GTM** 岗位激增（文内用招聘数据支撑）。
  - 风险信号: 客户 **预算冲击** 与 **席位回收**（Microsoft/Uber 叙事）可能转化为 **采购冻结**；个人需准备 **用量可见性** 与 **成本归因** 话术。
  - 我可执行的一步(<=30min): 为你目标客户的 **1 个** agent 用例，写 **≤50 词**「每月节省 X 小时 ↔ 约 Y USD API 成本」的 **粗算模板**（X/Y 用占位符）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：阅读 LeetCode Explore [`Binary Search`](https://leetcode.com/explore/learn/card/binary-search/) 的 **Introduction**：把 **「单调性 + 边界收缩」**映射为 agent 工具选择里 **「何时停止扩大搜索空间」**。明日动作：用中文写 **≤30 词**：给你 **1 个**检索/agent 循环写出 **停止条件**（**max steps** / **score plateau** / **budget** 三选一）。

- **IELTS（≤1 条）**：浏览 [IELTS One Skill Retake](https://ielts.org/take-a-test/ielts-one-skill-retake) 官方说明（适用地区、报名与成绩规则）。明日动作：用中文写 **≤35 词**：确认你是否在 **适用考点/考试类型** 范围内，并列出 **1 个**需提前核对的材料（护照/成绩单/预约号 三选一）。

## 6) 噪音过滤记录

- **`https://github.com/langchain-ai/langgraph/pull/7913`**、**`https://github.com/langchain-ai/langgraph/releases/tag/1.2.2`**：与 `0527.md` §1 **URL/语义去重**（稳定 message ID 已收录）。
- **`https://www.anthropic.com/engineering/how-we-contain-claude`**：与 `0527.md` §1 **URL 去重**。
- **`https://www.youtube.com/watch?v=sJNjw6U2Tvg`**（W&B Weave）：与 `0527.md` §2 **URL 去重**。
- **`https://lilianweng.github.io/posts/2025-05-01-thinking/`**：与 `0523.md`/`0527.md` **URL 去重**。
- **`https://github.com/vllm-project/vllm/pull/43401`**、**`https://github.com/vllm-project/vllm/pull/43662`**：与 `0527.md` §1 **URL 去重**。
- **X_WHITELIST（@karpathy 等）当日帖文**：本环境 **无法稳定抓取原帖 URL/正文** → 不收录（避免无证据条目）。
- **非白名单 YouTube 教程（如第三方 LangChain 速成课）**：未达白名单优先策略且 **Freshness/可信度** 不足 → 丢弃。

## 7) 本周累计计数

- Agent: **186**（`0527.md` §7: 181 + 本日 §1: 5）
- Theory: **73**（`0527.md` §7: 71 + 本日 §2: 2）
- Career/Business: **46**（`0527.md` §7: 45 + 本日 §4: 1）
- LC: **30**（`0527.md` §7: 29 + 本日 §5: 1）
- IELTS: **30**（`0527.md` §7: 29 + 本日 §5: 1）
