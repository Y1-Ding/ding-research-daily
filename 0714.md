# Daily Research - 2026-07-14 (21:30)

## 0) 今日3条结论

- **LangGraph #8335 在 write 侧引入 copy-on-write，堵住 caller 可变 state 与 channel 存储的别名泄漏**：`LastValue.update()` 直接 `self.value = values[-1]` 使首次 `.invoke()` 后 channel 与 caller 共享同一 list 对象；eval harness 复用同一 state dict 跑多轮时 trace 非确定性——与 0713 #38819（bind_tools mutation）同属「框架层写穿 caller 状态」族，harness 应测 invoke 前后 caller 对象 identity 不变。
- **vLLM #48596 修复「请求结束时最后一个 KV block 永不 offload」导致 prefix cache 静默 miss**：`schedule()` 时 finishing token 尚未处理，末 block 仍为 partial 被跳过；`request_finished` 原无 store kickoff → 同 prefix 下一请求 miss 末 block、多算 prefill；multi-turn agent serving 的 token 优化应在「含 EOS 的完整 prefix」场景测 hit rate，而非仅测中间 turn。
- **AgentLTL（arXiv 2607.02599）用 FO-LTL 对 agent trace 做确定性、无 judge 的过程合规评分**：同一 spec 既可 harness（在线 gate tool call prefix）又可 finetune（dense reward）；block-and-warn 在 7 模型中 5 个提升 compliance——skill eval 可补「过程规则层」而非仅 final-answer / LLM judge。

## 1) Agent工程化（high-signal）

- [fix: synchronize pipeline after setup migrations to prevent SSL errors (closes #5675)（LangGraph PR #8336 opened **2026-07-14**）](https://github.com/langchain-ai/langgraph/pull/8336)
  - 摘要: `AsyncPostgresSaver` 在 `pipeline=True` 时，`setup()` 执行 migration 后 pipeline 仍 defer 发送命令；若 connection 在 reuse 前未 sync，后续操作触发 `psycopg.OperationalError: SSL connection has been closed unexpectedly`。Fix：在 `setup()` 末尾显式 `await self.pipe.sync()` 排空 pipeline。
  - 为什么重要(对我): **skill 落地 + eval**——与 0713 #8332（跨 yield 持有 pipeline）、#8328（autocommit 冲突）同属 Postgres checkpointer 生产路径三连修；eval harness 若在 `setup()` 后立即跑长时 agent 应显式测 SSL 稳定性。
  - 明日动作(<=20min): 写 **是/否**：你 Postgres checkpointer 是否在 `setup()` 后立即跑含 LLM 调用的 eval（是/否）。

- [fix(langgraph): prevent state channel aliasing via copy-on-write at update（LangGraph PR #8335 opened **2026-07-14**）](https://github.com/langchain-ai/langgraph/pull/8335)
  - 摘要: 在 state update 写入侧建立 copy-on-write 边界，防止 caller 拥有的可变输入直接成为 channel 存储（closes #8314 write 侧）。注：`result['items']` 在 node/router 读取后原地修改仍会反映——读侧别名仍为 open issue。
  - 为什么重要(对我): **skill 落地 + eval**——#8314 证明 plain node `state["items"].append(...)` 即可泄漏；多轮 eval 复用同一 input dict 时，A/B 配置污染；harness 应 assert `id(caller_state[k]) != id(result[k])` 于首次 invoke 后。
  - 明日动作(<=20min): 写 **是/否**：你 LangGraph eval 是否复用同一 state dict 对象跑多轮（是/否）。

- [Fix multiple critical bugs across Anthropic, Perplexity, and Agent Middlewares（LangChain PR #38844 merged **2026-07-14**）](https://github.com/langchain-ai/langchain/pull/38844)
  - 摘要: 合并修复三处：(1) `ChatAnthropic.bind_tools` 原地修改 caller `tool_choice` dict；(2) `ChatPerplexity` Responses 路由原地修改 caller `extra_body`；(3) `ModelRetryMiddleware` / `ModelFallbackMiddleware` 吞掉 `GraphInterrupt`，human-in-the-loop 中断失效。
  - 为什么重要(对我): **skill 落地 + eval**——#38844 把 0713 #38819 与 middleware 层合并；GraphInterrupt 被吞 = eval 中 approval gate 静默跳过；middleware stack 应单测「interrupt 是否穿透到 harness」。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否用 ModelRetry/Fallback middleware 且依赖 GraphInterrupt（是/否/未知）。

- [feat(langchain): `ToolErrorMiddleware`（LangChain PR #38781 merged **2026-07-14**）](https://github.com/langchain-ai/langchain/pull/38781)
  - 摘要: 新增 `ToolErrorMiddleware`：通过 `on_error(exc, request) -> str | None` 将指定异常转为 `ToolMessage` 而非 crash graph；`None` 则 propagate。支持在 `create_agent(..., middleware=[ToolErrorMiddleware(on_error)])` 中按 tool/异常类型定制错误回传。
  - 为什么重要(对我): **skill 落地 + eval**——tool failure 是 agent eval 核心维度；可脚本化「某 tool 抛 MyError → 期望 ToolMessage 含 X」；与 OpenAI eval-skills 的 `command_execution` deterministic check 同族。
  - 明日动作(<=20min): 列 **1 条** 你 agent 中最常见的 tool 异常类型及期望 agent 行为（≤15 词）。

- [[Bugfix][KV Offloading] Offload last block at request finish and prevent reuse race（vLLM PR #48596 opened **2026-07-14**）](https://github.com/vllm-project/vllm/pull/48596)
  - 摘要: 修复请求 finish 时最后一个 KV block 永不 offload：`schedule()` 时 EOS 未处理，末 block 为 partial 被 skip；`request_finished` 新增 `update_offload_keys` + `_build_store_jobs_for_req` 延迟入队。并修复 store job 未 submit 前 block 被新请求重分配导致 silent corruption 的 race。
  - 为什么重要(对我): **token 优化**——multi-turn agent 末 token（EOS/tool result）也是 prefix 一部分；offload miss = 下轮同 prefix 重算末 block；collect→test 应在 ≥3 turn 场景测 prefix reuse 是否覆盖完整对话。
  - 明日动作(<=20min): 写 **是/否**：你 agent serving 是否用 KV offload / 分布式 prefix cache（是/否/未知）。

- [[Frontend] Add cache_salt support to the Anthropic Messages API（vLLM PR #48566 opened **2026-07-14**）](https://github.com/vllm-project/vllm/pull/48566)
  - 摘要: `cache_salt`（多租户 prefix-cache 隔离）已在 `/v1/chat/completions` 等支持，但 Anthropic Messages API (`/v1/messages`) 缺失——请求恒达引擎时 `cache_salt=None`，不同用户共享 prefix-cache blocks。Fix：在 `AnthropicMessagesRequest` 加 vLLM 扩展字段并转发至内部 `ChatCompletionRequest`。
  - 为什么重要(对我): **token 优化 + skill 落地**——多用户 agent 平台若走 Anthropic 方言 API，无 cache_salt = 跨用户 prefix 污染或错误 hit；self-host vLLM 的 eval 环境应显式设 per-tenant salt。
  - 明日动作(<=20min): 写 **是/否**：你 vLLM 部署是否走 Anthropic Messages API 且需多租户 cache 隔离（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Kernel] TRITON_MLA SWA（vLLM PR #48605 opened **2026-07-14**）](https://github.com/vllm-project/vllm/pull/48605)
  - 摘要: 为 TritonMLA 启用 sliding-window attention：windowed decode、超长 prompt 的 windowed new-token prefill、带 cached context 的 chunked-prefill SWA、per-layer 不同 window size、以及 `SlidingWindowMLASpec` 仅保留 window 长度 KV。新增 `triton_swa_scatter_combined` 融合 kernel 替代 4 次 index_put。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：SWA 是 hybrid/linear backbone 的常见组件；(1) agent ≥3 turn 时 window 外 token 不可 attend，eval 应报告 effective context 而非 max_len；(2) 与 0713 #48489（hybrid KV eviction ordering）互补——SWA 层 KV 生命周期更短；(3) serving 选型：FlashAttn 为 chunked-context SWA 的 supported prefill backend。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent backbone 是否用 sliding-window / MLA（是/否/未知）。

- [[DeepSeek-V4] Handle 3-D packed hidden states in attention forward（vLLM PR #48598 opened **2026-07-14**）](https://github.com/vllm-project/vllm/pull/48598)
  - 摘要: HC/MTP 传入 shape `[num_tokens, num_slots, hidden_dim]` 的 packed hidden states，slot 0 为 attention target token，其余为 auxiliary multi-token predictions。Fix：3-D 时取 `hidden_states[:, 0, :]` 作为 attention GEMM 与 attention op 输入；2-D 路径不变。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：MTP/multi-token prediction 与 agent 每步多候选 action 有结构类比；(1) attention 仅看 target slot，auxiliary slot 不参与 KV 写入；(2) eval 若用 MTP 模型，trace 中需区分 target vs draft token；(3) 与 0713 linear attention 论文的「跨层路由」问题不同轴——这是 serving 层 slot 选择。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否区分 MTP/auxiliary slot 与 target token（是/否/未知）。

- [Metacognition in LLMs: Foundations, Progress, and Opportunities（arXiv **2607.11881**, submitted **2026-07-13**）](https://arxiv.org/abs/2607.11881)
  - 摘要: Yale 团队首篇 LLM 元认知综述：元认知环 = monitoring（不确定性、任务进度判断）+ control（规划、策略选择、资源重分配）。覆盖测量/评测 benchmark、prompt 激发、agentic 系统应用；开源论文列表 [yale-nlp/LLM-Metacognition](https://github.com/yale-nlp/LLM-Metacognition)。
  - 为什么重要(对我): **Talk ≥3 turns + Agent 原理**：≥3 turn agent 需 monitoring 何时换策略/请求 human；(1) 你的 eval 是否测「agent 自知失败/不确定」而非仅 final success？(2) metacognitive control 与 tool retry/fallback 如何分工？(3) 综述指出 LLM 置信度校准仍弱——judge-free eval（如 AgentLTL）与 verbalized uncertainty 应分层。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent eval 是否含 uncertainty/monitoring 维度（有/无）。

## 3) 热门项目/可试个人项目

- [miguelgarcia/langgraph-chat-agent-eval（PyPI **0.1.1**）](https://github.com/miguelgarcia/langgraph-chat-agent-eval)
  - 摘要: pytest 驱动的 LangGraph chat agent 端到端 eval：真实 LLM 调用 + deterministic tool mock + LLM-as-judge 逐步评分；支持 multi-turn scenario、arg-level tool 断言、JSON 结果与 `eval-view` 终端报告。
  - 为什么重要(对我): **eval 杠杆**——可直接接入 map_prompt_test：mock tool 保确定性，judge 评过程；比从零写 harness 快；与 0713 PACE（proxy triage）互补——此为 full-graph 行为测试。
  - 明日动作(<=20min): `pip install langgraph-chat-agent-eval`；读 README Quickstart；写 **≤15 词** 你最该测的 1 个 multi-turn scenario。
  - 估计复现成本: **低**（pip + 现有 model API + pytest）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Evals for AI Agents: How Product Builders Get the Most Out of Every New Model（Anthropic Webinar, **2026-07-14** 10:00 PT）](https://www.anthropic.com/webinars/evals-for-ai-agents-how-product-builders-get-the-most-out-of-every-new-model)
  - 机会信号: Anthropic Applied AI 今日直播——强调 multi-step agent（tool + retrieve + 多步产出）不能用 single-turn eval 判断新模型是否值得切换；从 production failures 建首 eval set、快速决策 model adoption → 出海 signature 可打在「垂域 agent eval 方法论 + 可复用 harness」。
  - 风险信号: 无 production failure 采集则无法建高质量 eval set；直播偏 product builder 视角，封闭垂域需脱敏改造 failure case；录音 48h 内发放——今日可先注册/参会记笔记。
  - 我可执行的一步(<=30min): 注册或参会；列 **3 条** 你最近 agent 真实 near-prod failure（各 ≤12 词），标哪条可脱敏成 eval case。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 207 Course Schedule](https://leetcode.com/problems/course-schedule/)：判断 prerequisite 图是否有环（能否修完所有课）；Kahn BFS 处理节点数 < numCourses 即存在环；与 0713 #210（返回拓扑序）同族。明日动作：手画 Example 2 的环；写 Kahn 核心 20 行（≤25min）；写 **≤15 词**：与 agent tool 依赖图「是否存在死锁/环」的类比。

- **IELTS（≤1 条）**：[Exploring the potential of conversational AI in speaking assessment（IELTS.org, Apr 2026）](https://ielts.org/news-and-insights/exploring-the-potential-of-conversational-ai-in-speaking-assessment)：GPT-4o 驱动的 SDS 模拟 IELTS Speaking Part 3，能 elicit turn-taking/topic development 且区分高低 proficiency，但缺乏 active listening 信号（yeah/right）、非言语反馈，交互偏「标准化面试」非真实对话。明日动作：选 1 个 technology Part 3 题自录 2min；自检是否含 opinion + reason + example + 对追问的扩展（≤20min）；写 **≤15 词** 你最弱项（Fluency/LR/GRA/Pronunciation 选 1）。

## 6) 噪音过滤记录

- **LangGraph #8332 pipeline 跨 yield、#8334 cron end_time**：`0713.md` 已收录 → 不重复。
- **LangChain #38819 bind_tools mutation**：`0713.md` 已收录；#38844 为合并修复 → 主报告收录 #38844，不单列 #38819。
- **vLLM #48485 Queue-Informed LRU、#48489 hybrid KV eviction**：`0713.md` 已收录 → 不重复。
- **arXiv 2607.07953 linear attention、PACE 2607.02032、OpenAI eval-skills**：`0713.md` 已收录 → 不重复。
- **arXiv 2607.07706 / SDM 2607.07386 / HOLA 2607.02303**：`0710–0712.md` 已收录 → 不重复。
- **LangGraph #8333 serde PurePath、#8330 typo、#8331 NamedBarrierValue**：chore/docs → 丢弃。
- **vLLM #48610 CI torch-nightly、#48609 revert dense MHA、#48607–#48604 ROCm/MiniMax**：infra/kernel → 丢弃。
- **LlamaIndex #22357 docs、#22355 vector store compat、#22356 empty doc fix**：弱于今日 state/KV focus → 丢弃。
- **AutoGen #7953 Correctover MCP proposal**：外部 vendor 营销向 proposal，无 mergeable patch → 丢弃。
- **arXiv 2607.11849 AdvancedMathBench、2607.11736 MET moral reasoning**：与当前 Agent/Attention focus 映射弱 → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-14** 原帖 URL → 不收录。
- **@gefei55 / @ianneo_ai 职业帖**：无法验证当日原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv + Anthropic 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **521**（`0713.md` §7: 515 + 本日 §1: 6）
- Theory: **215**（`0713.md` §7: 212 + 本日 §2: 3）
- Career/Business: **93**（`0713.md` §7: 92 + 本日 §4: 1）
- LC: **75**（`0713.md` §7: 74 + 本日 §5: 1）
- IELTS: **75**（`0713.md` §7: 74 + 本日 §5: 1）
