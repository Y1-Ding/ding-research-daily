# Daily Research - 2026-08-08 (21:30)

## 0) 今日3条结论

- **DeltaChannel 仍是本周 eval 最大地雷族，且三后端各踩不同坑**（LangGraph #8544/#8556/#8557, Aug 7–8）：parallel super-step 按 `task_id` 而非 `task_path` replay → `get_state` 与 `invoke` 不一致；Postgres 在 target 未加载时把 walk cursor 误置 `None` → 深线程 `messages` hydrate 为 `[]`；SQLite 用 `checkpoint_id <=` 范围过滤而非 parent 指针 → 自定义 id/时钟偏移丢祖先 writes → **trajectory eval 必须覆盖 parallel writes、深分页 checkpoint、非 uuid6 id 三条路径**。
- **多 provider fallback 会「毒化」持久化 message history**（LangChain #36601, merged Aug 7）：Bedrock 超时后 OpenAI fallback 返回 `function_call` block 写入 checkpoint，下轮 Bedrock 恢复即因不识别该 block 永久失败 → **skill 落地须在 middleware 边界把 fallback 输出 normalize 为 provider-agnostic `tool_use`**。
- **Middleware tracing 可单点拖慢 eval 500–1000ms**（LangChain #38910 + LangGraph #8523, Aug 7）：`AgentMiddleware` wrap/node hooks 对完整 message history 做 trace；`trace_policy=TracePolicy(process_inputs=omit_payload)` 可 scrub → **长跑 eval 的 wall-clock 与 LangSmith $ 须把 tracing payload 纳入 ablation**。

## 1) Agent工程化（high-signal）

- [fix(langgraph): detect subgraphs from bytecode instead of source（LangGraph PR #8569, updated **2026-08-08**）](https://github.com/langchain-ai/langgraph/pull/8569)
  - 摘要: Fixes #8559。`find_subgraph_pregel` 原用 `inspect.getsource` + `ast.parse` 占 `StateGraph.compile()` 约 80% 时间；改为 `dis` 遍历 `func.__code__` + closure/globals 解析属性路径。benchmark（713 nodes）：build 1412ms→286ms（4.9x），detection 1132ms→4ms（283x）；且可检测 REPL/notebook/`exec` 定义的无源码函数。
  - 为什么重要(对我): **skill 落地**——大型 multi-skill agent graph 的 compile 延迟直接影响 dev loop 与 CI；bytecode 路径还修复 notebook 开发时 subgraph 静默未检测。
  - 明日动作(<=20min): 写 **是/否**：你 agent graph 节点数是否 >50 且 compile 成为瓶颈（是/否/未知）。

- [fix: order delta channel replay by task path（LangGraph PR #8544, updated **2026-08-07~08**）](https://github.com/langchain-ai/langgraph/pull/8544)
  - 摘要: Fixes #8382。各 saver 按 `(task_id, idx)` 排序 replay writes，但 live 执行按 `task_path_str(task.path[:3])` 应用 → 同 super-step 多 task 写同一 `DeltaChannel` 时 replay 顺序任意，`get_state` 与 `invoke` 返回值不一致，且错误 replay 被持久化为后续 base。Fix：按 `(task_path, task_id, idx)` 排序，与 Send channel 的 `SELECT_PENDING_SENDS_SQL` 一致。
  - 为什么重要(对我): **评测**——parallel tool call / fan-out agent 的 trajectory grading 若用 `get_state`，顺序错 = 假 pass/fail。
  - 明日动作(<=20min): 写 **是/否**：你 graph 是否有同 super-step 多 task 写同一 channel（是/否/未知）。

- [fix(checkpoint-postgres): derive the delta walk cursor once the target loads（LangGraph PR #8556, updated **2026-08-07~08**）](https://github.com/langchain-ai/langgraph/pull/8556)
  - 摘要: Fixes #8448。Postgres `get_delta_channel_history` stage-1 分页（1024 rows）时，target checkpoint 不在首页则 `parent_of` 尚无 target 行 → walk cursor 误初始化为 `None`（与 root 同表示）且不再重试 → 深线程 `messages` channel hydrate 为 `[]`，无异常无警告。Fix：仅当 `target_id in parent_of` 时才初始化 walk。
  - 为什么重要(对我): **评测 + skill 落地**——`get_state_history` 对旧 checkpoint 做 ablation 时静默空 state = eval 假 pass。
  - 明日动作(<=20min): 写 **是/否**：你是否对 >1024 checkpoint 的线程做 `get_state`（是/否/未知）。

- [fix(checkpoint-sqlite): walk delta ancestors by parent pointer（LangGraph PR #8557, updated **2026-08-07~08**）](https://github.com/langchain-ai/langgraph/pull/8557)
  - 摘要: Fixes #8550。SQLite 用 `checkpoint_id <= target` 范围过滤走祖先，隐含要求 child id > parent id（uuid6 碰巧满足）；自定义 id 或时钟偏移时 parent 被排除 → seed 与 writes 丢失。Fix：recursive CTE 沿 `parent_checkpoint_id` 走，不再依赖 id 排序。
  - 为什么重要(对我): **评测**——本地 dev 用 sqlite checkpointer 时，非 uuid6 id 或跨进程写线程 = trajectory 缺步。
  - 明日动作(<=20min): 写 **是/否**：你是否用 sqlite checkpointer + 自定义 checkpoint id（是/否/未知）。

- [fix(langchain): normalize fallback tool-call blocks in `ModelFallbackMiddleware`（LangChain PR #36601, merged **2026-08-07**）](https://github.com/langchain-ai/langchain/pull/36601)
  - 摘要: Fixes langchain-ai/langchain-aws#1206。`ChatBedrockConverse` 主模型 + `AzureChatOpenAI` fallback 场景：Bedrock 超时后 fallback 返回 OpenAI `function_call` block 写入 checkpoint → 下轮 Bedrock 恢复因不识别该 block 永久失败（thread poisoned）。Fix：在 middleware 边界将 fallback 的 `function_call` normalize 为 LangChain 标准 `tool_use`（含 `id`/`callId`/`call_id` alias、string/dict arguments）。
  - 为什么重要(对我): **skill 落地 + 评测**——multi-provider durable agent 的 checkpoint 必须是 provider-agnostic；否则 eval 跨 provider 切换 = 不可复现 poison。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `ModelFallbackMiddleware` 跨 provider fallback（是/否/未知）。

- [feat(langchain): expose toggle to trace inputs on `AgentMiddleware`（LangChain PR #38910, updated **2026-08-07**）](https://github.com/langchain-ai/langchain/pull/38910)
  - 摘要: 依赖 LangGraph #8523 `TracePolicy`。`AgentMiddleware` 对长 message history trace 输入，deepagents eval 观测每 middleware 增加 500–1000ms（wrap hooks ~70%，node hooks ~20%）。新增 `trace_policy: TracePolicy | None`，可用 `TracePolicy(process_inputs=omit_payload)` scrub trace 输入；默认行为不变。
  - 为什么重要(对我): **token 优化 + 评测**——与 0805–0806 middleware messages 泄漏族互补：tracing payload 膨胀同样拖慢 eval 并虚增 LangSmith 成本。
  - 明日动作(<=20min): 写 **是/否**：你 middleware stack 是否 >2 层且 eval wall-clock 可疑（是/否/未知）。

- [fix: preserve chronological message order in MessageFilterAgent._apply_filter（AutoGen PR #7981, updated **2026-08-07~08**）](https://github.com/microsoft/autogen/pull/7981)
  - 摘要: Fixes #7971。`MessageFilterAgent._apply_filter` 按 `per_source` config 顺序 append，而非消息原始时间序 → 文档示例 A→B→A→B 图中 B 先看到 A 的新消息再看到自己的旧回复。Fix：`result.sort(key=lambda m: messages.index(m))`。
  - 为什么重要(对我): **评测**——multi-agent eval 的 message filter 若乱序，grading 输入与真实执行不一致。
  - 明日动作(<=20min): 写 **是/否**：你是否用 AutoGen `MessageFilterAgent`（是/否/未知）。

- [fix(mcp): prevent tool override name collisions（AutoGen PR #8026, updated **2026-08-07~08**）](https://github.com/microsoft/autogen/pull/8026)
  - 摘要: `McpWorkbench.tool_overrides` 仅校验 override 间冲突，未校验最终暴露名与 MCP server 实际 `list_tools` 结果 → 可将 `fetch` rename 为已存在的 `search`，真实 `search` 调用被路由到 `fetch`；stale override 亦可 shadow 现存 tool。Fix：对实际 tool list 校验 + 重建 reverse routing。
  - 为什么重要(对我): **skill 落地**——MCP tool override 是 agent skill 集成的常见模式；silent 路由错误 = eval 假 pass（调用了错误 tool 却返回 200）。
  - 明日动作(<=20min): 写 **是/否**：你是否用 MCP tool_overrides rename tools（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Core][Spec Decode] Add lookahead-aware prefix cache hashing for EAGLE-style draft models（vLLM PR #50897, updated **2026-08-07~08**）](https://github.com/vllm-project/vllm/pull/50897)
  - 摘要: Implements #50438。EAGLE-style draft KV 在 block 边界依赖 target prefix + 后继 finalized token；旧 cache key 仅证明 block prefix → 保守丢弃最后 matched unit。新 key 含 successor token 及其 input identity → 可复用最后 draft-attention block 而不无条件 drop；对 hybrid full-attention + Mamba/GDN 模型（aligned cache unit 可达数百 token）收益显著。覆盖 EAGLE/EAGLE3/MTP/DFlash/DSpark。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/KV**：spec decode + prefix cache 的边界条件直接决定 agent 长 session 的 $/token；hybrid 模型上 last-block drop 是隐藏成本。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serve EAGLE/MTP spec decode 模型（是/否/未知）。

- [[BugFix] Preserve divergent FA hits with external Mamba state（vLLM PR #51468, updated **2026-08-07~08**）](https://github.com/vllm-project/vllm/pull/51468)
  - 摘要: hybrid (full-attention + Mamba) + KV connector 场景：`truncate_computed_blocks()` assert 每 group 持有 `num_computed_tokens // block_size` blocks，但 Mamba group 存 whole recurrent state 非 per-token KV → external hit 可在 local 不覆盖的边界供 state，assert 变 engine-fatal。Fix：仅对 `MambaSpec` clamp `num_blocks`，attention group 保持 loud assert。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/KV**：hybrid SSM+Attention 长上下文 agent 的 prefix cache + connector 是下一代 serving 栈；误 assert = 生产 prefill worker 崩溃。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 hybrid FA+Mamba + KV connector（是/否/未知）。

- [[Kernel] ReplaySSM: cache SSM inputs for faster Mamba2 speculative decode（vLLM PR #49847, updated **2026-08-07~08**）](https://github.com/vllm-project/vllm/pull/49847)
  - 摘要: Mamba2 spec decode 原生路径为每 draft token 写完整 `(nheads, head_dim, dstate)` state snapshot → batch 256 时 DRAM traffic 随 T 线性增长可低于 plain decode。ReplaySSM 用 checkpoint state + 小 circular buffer 缓存 per-step SSM inputs（`x`, `dt`, `B`）→ rollback 变 cursor move、verification 并行、traffic 对 T 平坦（bs256 Nemotron-3-Super-120B：T=8 baseline 9.73GB vs ReplaySSM non-flush 1.23GB）。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/SSM**：spec decode 是 agent 低延迟推理关键路径；SSM state snapshot 成本是 hybrid 模型 spec decode 的结构性瓶颈。
  - 明日动作(<=20min): 写 **≤15 词**：你 profiling 是否关注 spec decode 的 SSM state traffic（是/否/未知）。

- [The Tokenpocalypse Is Here（Simon Willison Blog / 404 Media, **2026-08-07**）](https://simonwillison.net/2026/Aug/7/pdfs-are-terrible/)
  - 摘要: Accenture 内部数据（泄露会议录音，404 Media 6/24 报道）：token 消耗主力是非工程师（如 PDF→图片→markdown 转换）；agentic AI strategy lead 确认。Simon 延伸：PDF 是糟糕的信息媒介，企业 agent 部署的 token 成本治理须下沉到非工程用户行为层。
  - 为什么重要(对我): **Talk ≥3 turns + token 优化**：agent eval 的 $/task 不仅看模型与 prompt，还看输入 pipeline（PDF 解析链可 10x token）；对你意味着 eval harness 须记录 input preprocessing token 明细。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否拆分 preprocessing vs inference tokens（是/否/未知）。

- [Now we have a timeline of the OpenAI accidental attack against Hugging Face（Simon Willison Blog, **2026-08-07**）](https://simonwillison.net/2026/Aug/7/openai-timeline/)
  - 摘要: OpenAI 在 Black Hat 最后一场 presentation 公布 Hugging Face Incident 完整时间线（视频已发布）。Simon 据此重构事件链：CTF 评估环境误配互联网访问 → 模型攻击真实第三方。继 Anthropic（PyPI 投毒）、Meta（Muse Spark 1.1）后第三家；Irregular 同时托管多家误配环境。
  - 为什么重要(对我): **Talk ≥3 turns + Agent eval 范式**：第三方 cyber eval 的 sandbox 配置须纳入验收 checklist；「CTF = 隔离」不可假设。
  - 明日动作(<=20min): 写 **≤15 词**：你外包 eval 是否验过 network egress（是/否/未知）。

## 3) 热门项目/可试个人项目

- [Add RPI v0.1 Recursive Perspective Integration evaluation（openai/evals PR #1700, updated **2026-08-07~08**）](https://github.com/openai/evals/pull/1700)
  - 摘要: 新增 model-graded benchmark RPI v0.1，评估推理策略质量（多视角分析、认识论校准、二阶后果、比例性、决策收敛）而非仅事实正确性；含复杂任务与简单 control task 以测「推理深度是否匹配问题需求」；检测 false equivalence、speculative recursion、analysis paralysis、sycophancy 等失败模式。
  - 为什么重要(对我): **评测杠杆**——补充 factual eval 的 reasoning-process 维度；对 agent L2–L3 eval 的「过程质量 rubric」有参考价值。
  - 明日动作(<=20min): clone `openai/evals`，读 RPI dataset 中 1 条 sample，写 **≤15 词** 哪条失败模式与你 eval 最相关。
  - 估计复现成本: **中**（需 OpenAI API key + 理解 oaieval registry，首跑 ~30min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [The Tokenpocalypse Is Here: Companies Are Scrambling To Stop Spending So Much on AI（404 Media, **2026-06-24**，Simon Willison 转引 **2026-08-07**）](https://www.404media.co/the-tokenpocalypse-is-here-companies-are-scrambling-to-stop-spending-so-much-on-ai/)
  - 机会信号: 大企业（Accenture 等）正急寻 token 成本治理与 agentic AI 策略人才；非工程师行为是主要消耗源 → **agent eval + token optimization** 技能在企业采购决策中有直接 ROI 叙事。
  - 风险信号: 成本削减周期可能压缩 AI 预算；「token 治理」岗位偏咨询/平台而非 research。
  - 我可执行的一步(<=30min): 写 **3 条** 你可量化的 token 节省案例（如 middleware trace scrub、PDF 直解析 vs 图片链），各 ≤20 词，不涉客户名。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[3347. Maximum Frequency of an Element After Performing Operations II](https://leetcode.com/problems/maximum-frequency-of-an-element-after-performing-operations-ii/)（**Day 38, Aug 2026 Daily Challenge**；承接 0806 数位链，今日切区间覆盖 + 差分数组）：给定 `nums`, `k`, `numOperations`，每次选未选过的 index 加 `[-k,k]` 整数，求最大频率。明日动作：模板化 `TreeMap` 差分 + `min(s, cnt[x]+numOperations)`；写 **≤15 词**：「区间覆盖计数」与 agent eval「多路径汇聚同一状态」的类比。

- **IELTS（≤1 条）**：[IELTS Reading 官方样题（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-academic-reading-test)：0806 已轮换 Listening，今日换 **Academic Reading Passage 1**（20 分钟完成 1 篇 + 核对答案）。明日动作：记录 **≥3 个**错题类型（T/F/NG、匹配题、词汇替换）；写 **≤15 词** 你最弱的阅读题型。

## 6) 噪音过滤记录

- **LangGraph #8574/#8573/#8571/#8566/#8562**：SSL pipeline、OTel OOM、sqlite setup、history lock — infra/chore，非当前 focus → 过滤。
- **LangGraph #8548/#8538/#8535**：0806 已收录 DeltaChannel fork/hydrate/seed 族 → 不重复（本日 #8544/#8556/#8557 为新变体）。
- **LangGraph #8526 plain-value seed**：0805–0806 已覆盖，Aug 7 merge → 不重复。
- **LangChain #39325/#39324 h2 bump、#39348 SECURITY.md、#39344 model profile refresh**：chore → 过滤。
- **LangChain #39310 function_call status、#39339 tool metadata**：重要但偏 provider 序列化细节，今日 §1 已满 8 条 → 过滤。
- **AutoGen #8029 rstrip empty assistant、#8025 tool truncation**：0805–0806 已收录或偏边缘 → 过滤。
- **vLLM #50969/#51018 Ray/DP port fixes、#51255 Dots3**：infra/新模型，与 Attention focus 弱相关 → 过滤。
- **Simon Willison Aug 7 Raccoon Heist Codex**：agent 视觉自检失败有趣但非当前 focus → 过滤。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-08-07~08** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + Simon Willison Blog Aug 7 高信号 ≥8 条，无需 skill 补充。
- **0807.md 缺失**：本 run 仅产出 `0808.md`；若昨日 automation 失败，需人工确认 cron 日志。

## 7) 本周累计计数

- Agent: **686**（`0806.md` §7: 678 + 本日 §1: 8）
- Theory: **300**（`0806.md` §7: 295 + 本日 §2: 5）
- Career/Business: **114**（`0806.md` §7: 113 + 本日 §4: 1）
- LC: **96**（`0806.md` §7: 95 + 本日 §5: 1）
- IELTS: **96**（`0806.md` §7: 95 + 本日 §5: 1）
