# Daily Research - 2026-08-02 (21:30)

## 0) 今日3条结论

- **Anthropic cached prompt token 被 LlamaIndex 系统性漏计**（#22548, Aug 2）：`input_tokens` 不含 cache read/creation → TokenCountingHandler 低估 prompt 用量、高估 cache 节省 → **token 优化 eval 必须在 callback 层单独加 `cache_read_input_tokens`**，不能只看 `input_tokens`。
- **LangGraph SSL + pipeline 双侧修复收敛**（#8503 Aug 1 调嵌套顺序，#8507 Aug 2 对 SSL 连接直接禁用 pipeline）：根因是 LLM 调用间隙 pipeline 跨空闲保持 → Supabase 等 SSL 连接被服务端关闭 → **长线程 agent eval 用 AsyncPostgresSaver 需确认 SSL 场景是否 `pipeline=False`**。
- **smevals 把 eval 拆成 run/grade 两阶段**（Simon Willison + Prime Radiant, Jul 31）：YAML 目录定义 task/config/grader/checker，支持 `uvx smevals run` → `grade` → `serve` 静态报告 → **skill 落地评测可复用「跑完再改 grader 重评」模式**，与 AutoGen OpenEval 导出互补。

## 1) Agent工程化（high-signal）

- [fix(callbacks): count Anthropic cached prompt tokens in TokenCountingHandler（LlamaIndex PR #22548, updated **2026-08-02**）](https://github.com/run-llama/llama_index/pull/22548)
  - 摘要: Fixes token 统计 bug。Anthropic `input_tokens` 为净额（不含 cache）；`cache_read_input_tokens` / `cache_creation_input_tokens` 单独报告。`get_tokens_from_response` 只读 `input_tokens` → 暖 cache 下 prompt token 被系统性低估。Fix：callback 层合并 cache 字段计入 prompt tokens。
  - 为什么重要(对我): **token 优化 + 评测**——prompt cache ablation eval 若漏计 cached tokens，$/task 与 cache hit rate 全错；LangSmith/LlamaIndex trace 不可作成本 ground truth。
  - 明日动作(<=20min): 写 **是/否**：你 Anthropic agent eval 是否单独记录 `cache_read_input_tokens`（是/否/未知）。

- [fix(vertex): surface usage_metadata token counts in Vertex Gemini chat responses（LlamaIndex PR #22547, updated **2026-08-02**）](https://github.com/run-llama/llama_index/pull/22547)
  - 摘要: Fixes token 暴露缺口。Vertex Gemini `chat()`/`achat()` 的 `usage_metadata`（`prompt_token_count`、`candidates_token_count`、`cached_content_token_count` 等）被静默丢弃，仅保留 `tool_calls` 于 `additional_kwargs`。Google GenAI 集成已有提取；本 PR 为 Vertex 对齐。
  - 为什么重要(对我): **token 优化 + 评测**——多 provider eval 需统一 token 字段；Vertex 路径缺失 = 跨模型成本对比偏倚。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否用 Vertex Gemini 且依赖 response 内 token 计数（是/否/未知）。

- [fix: Disable AsyncPipeline for SSL connections to prevent premature SSL closure（LangGraph PR #8507, updated **2026-08-02**）](https://github.com/langchain-ai/langgraph/pull/8507)
  - 摘要: Fixes #5675（与 #8503 不同路径）。`AsyncPostgresSaver` + `pipeline=True` + 外部 SSL PostgreSQL（如 Supabase）时，psycopg `AsyncPipeline` 在 LLM 调用间隙保持 pipeline 打开 → 服务端因空闲关闭 SSL → `psycopg.OperationalError: SSL connection has been closed unexpectedly`。Fix：SSL 连接直接禁用 `AsyncPipeline`。
  - 为什么重要(对我): **skill 落地 + 评测稳定性**——长线程 agent eval 用 Postgres checkpoint 时，SSL flaky = 不可复现 timeout；与 0801 #8503 嵌套顺序修复形成双侧检查清单。
  - 明日动作(<=20min): 写 **是/否**：你 checkpoint DB 是否 SSL 外部服务且 `pipeline=True`（是/否/未知）。

- [fix: Pin opentelemetry-exporter-prometheus to safe version in templates（LangGraph PR #8506, updated **2026-08-02**）](https://github.com/langchain-ai/langgraph/pull/8506)
  - 摘要: Fixes #8409。`langgraph new` 脚手架未 pin `opentelemetry-exporter-prometheus`，高并发 workflow 下可能解析到 `>=0.58b0,<0.59` 漏洞版本 → OOM crash。Fix：模板下载后程序化添加安全版本约束。
  - 为什么重要(对我): **skill 落地**——新 agent 项目脚手架是 skill 分发入口；默认依赖 OOM = 生产 eval 环境不稳定、trace 采集中断。
  - 明日动作(<=20min): 写 **是/否**：你 LangGraph 项目是否启用 OTel Prometheus exporter（是/否/未知）。

- [fix(agentchat): SelectorGroupChat fallback must not return excluded previous speaker（AutoGen PR #7936, updated **2026-08-01**）](https://github.com/microsoft/autogen/pull/7936)
  - 摘要: Fixes livelock。`allow_repeated_speaker=False` 时 `_select_speaker` 从 `participants` 排除上一 speaker；模型 `max_selector_attempts` 失败后 fallback 仍返回 `_previous_speaker` → 违反契约，两 agent 团队陷入 **livelock**。Fix：fallback 从剩余 participants 选取，而非强制返回被排除者。
  - 为什么重要(对我): **评测 + skill 落地**——multi-agent eval 的 speaker 轮换是确定性约束；livelock = eval hang 而非 fail，pass^k 统计污染。
  - 明日动作(<=20min): 写 **是/否**：你 multi-agent eval 是否设 `allow_repeated_speaker=False`（是/否/未知）。

- [fix(core): copy structured_output_kwargs to avoid mutating caller dict（LangChain PR #39191, updated **2026-08-01**）](https://github.com/langchain-ai/langchain/pull/39191)
  - 摘要: Fixes #39169。`StructuredPrompt` 合并 `strict=True` 等 extra kwargs 时直接 mutate 调用方传入的 `structured_output_kwargs` dict → 同一 dict 复用于多个 prompt 时产生意外副作用。Fix：merge 前先 copy；含 regression test。
  - 为什么重要(对我): **skill 落地 + 评测**——structured output eval 常复用同一 kwargs dict 做 ablation；静默 mutation = A/B 配置污染。
  - 明日动作(<=20min): 写 **是/否**：你 structured output eval 是否复用同一 kwargs dict（是/否/未知）。

- [fix: Path Traversal in cli.py（LangGraph PR #8309, updated **2026-08-02**）](https://github.com/langchain-ai/langgraph/pull/8309)
  - 摘要: Corridor 生成修复。LangGraph CLI `env` 字段未防 path traversal → `../../.env` 或项目外绝对路径可读取/转发外部文件。Fix：`Path.is_relative_to()` 校验 env 路径必须在 project root 内，否则 `click.UsageError`。
  - 为什么重要(对我): **skill 落地安全**——agent 脚手架若允许 env 逃逸，CI/CD 与本地 eval 环境可被诱导加载外部凭证。
  - 明日动作(<=20min): 写 **是/否**：你 LangGraph 项目是否通过 CLI `--env` 加载自定义 env 文件（是/否/未知）。

- [smevals — a small eval suite for evaluating models, prompts, and harnesses（Simon Willison Blog, **2026-07-31**）](https://simonwillison.net/2026/Jul/31/smevals/)
  - 摘要: Simon Willison + Prime Radiant 发布 `smevals`：YAML 目录定义 eval/task/config/grader/checker；`uvx smevals run` 执行 → `grade` 独立评分 → `serve`/`build` 浏览静态 HTML 报告。核心设计：**run 与 grade 分离**，改 grader 可重评已有 run；config 可测不同 system prompt / model / harness。
  - 为什么重要(对我): **评测杠杆**——与 0801 skillval（skill vs baseline）和 AutoGen #8009 OpenEval 导出形成三角：smevals 管「小 eval 套件 + harness 对比」，可直接让 coding agent `uvx smevals docs` 自建 eval。
  - 明日动作(<=20min): `uvx smevals docs` 扫一眼 README；写 **≤10 词** 你最想测的一个 eval 问题。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix] Refresh stale M-RoPE state when streaming re-feeds grow a RUNNING request's prompt（vLLM PR #50741, updated **2026-08-02**）](https://github.com/vllm-project/vllm/pull/50741)
  - 摘要: Fixes #50731。Qwen3-Omni realtime audio（`/v1/realtime` + `async_chunk: true`）streaming re-feed 增长 RUNNING request prompt 时，M-RoPE state 未刷新 → `RuntimeError: expanded size (74) must match existing size (0)`。机制：`NewRequestData.from_request` 按引用传 `prompt_token_ids`；uniproc executor 下 streaming re-feed 原地增长 token 列表但 M-RoPE position state 仍基于旧长度。
  - 为什么重要(对我): **Talk ≥3 turns + RoPE/Attention**：M-RoPE 是多模态位置编码；streaming agent 的 prompt 增长若不同步 RoPE state = attention 层 position 错位，eval crash 而非可解释 OOM。
  - 明日动作(<=20min): 写 **≤15 词**：你 streaming agent 是否在 RUNNING 状态 re-feed 增长 prompt（是/否/未知）。

- [[Spec Decode][Perf] Optimize DSpark Markov head with addmm（vLLM PR #50737, updated **2026-08-02**）](https://github.com/vllm-project/vllm/pull/50737)
  - 摘要: DSpark spec decode：LM-head 一次产出所有 draft position base logits，再逐步做 dense Markov projection。原路径每 step 独立 projection + elementwise add。Fix：sample layout 改 step-major，每 step 用 `base_logits.addmm_(markov_embed, weight.t())` 单 GEMM 写入，消除独立 add kernel 与额外 allocation。
  - 为什么重要(对我): **Talk ≥3 turns + Spec Decode/Attention**：Markov head 是 draft model 的 attention 后投影；融合 GEMM 降低 spec decode 每 token 带宽 = agent serving TTFT/$ 直接受益。
  - 明日动作(<=20min): 写 **≤15 词**：你 local eval 是否用 DSpark spec decode（是/否/未知）。

- [[Bugfix][Mamba] Fix overlapping state copy race（vLLM PR #50729, updated **2026-08-02**）](https://github.com/vllm-project/vllm/pull/50729)
  - 摘要: Fixes spec decode 下 Mamba state copy race。#40172 引入 fused Mamba state copy，#42406 加 aligned-prefix 支持但保留 memcpy 式并行 load/store；spec decode 在同 block 内 shift convolution state 时 source/destination 重叠 → wave scheduling 可 corrupt values → V1 worker failure。Fix：重叠区域改用 safe copy 路径。
  - 为什么重要(对我): **Talk ≥3 turns + Hybrid Attention**：Mamba/SSM 是 attention 替代路径；spec decode + hybrid model 的 state copy race = agent serving flaky crash，与 0801 GDN cudagraph padding 同属「静默错误」族。
  - 明日动作(<=20min): 写 **≤15 词**：你 hybrid model eval 是否在 spec decode 下压测过（是/否/未知）。

- [[Bugfix][Core] Fix invalid block handling for hybrid KV cache groups（vLLM PR #50742, updated **2026-08-02**）](https://github.com/vllm-project/vllm/pull/50742)
  - 摘要: Fixes #50687。Invalid-block recovery 假设每 request 仅一个 KV cache group：`get_block_ids()` 返回单 list。Hybrid memory allocator 下返回 per-group list → `ValueError: too many values to unpack`。Fix：遍历所有 group 做 recovery，不再 single-group 假设。
  - 为什么重要(对我): **Talk ≥3 turns + KV/Attention**：multi-group hybrid KV（如 V3.2 all-MLA）是长上下文 agent 基础设施；recovery 路径 crash = eval 环境不可恢复而非 graceful degrade。
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 是否 multi KV cache group hybrid 模型（是/否/未知）。

- [[Feature] Uneven decode context parallelism for heterogeneous TP ranks（vLLM PR #50736, updated **2026-08-02**）](https://github.com/vllm-project/vllm/pull/50736)
  - 摘要: Stacked on #50735/#50733。Decode context parallelism（DCP）现按 rank 均分 KV token；与 `--rank-tp-ratio` 不均等 TP shard 配对错误——最大 weight shard 的 rank 反而 KV token 最少。Fix：配置 rank ratio vector 时按比例分配 DCP token 轴；even-split 路径不变。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/KV 分片**：DCP 决定 decode 阶段每 rank 的 KV attention 计算量；不均等 GPU 上错误分片 = agent eval 成本曲线与 latency 不可复现。
  - 明日动作(<=20min): 写 **≤15 词**：你 local serving 是否 heterogeneous GPU + context parallelism（是/否/未知）。

## 3) 热门项目/可试个人项目

- [prime-radiant-inc/smevals — YAML-based eval framework with run/grade separation](https://github.com/prime-radiant-inc/smevals)
  - 摘要: Python CLI（`uvx smevals`）；Eval = YAML 目录 + task/config/grader/checker 脚本；`run` 与 `grade` 分离支持改 grader 重评；`serve` 实时 Web UI（port 7001）+ `build` 静态 HTML 报告；README 设计为 agent-readable（`smevals docs` 输出完整文档）。
  - 为什么重要(对我): **评测杠杆**——可直接挂你现有 agent harness 做 config ablation；与 0801 skillval（skill A/B）和 smevals blog（Jul 31）形成「工具 + 文档 + 源码」完整链路。
  - 明日动作(<=20min): `uvx smevals docs | head -50` 扫结构；写 **≤10 词** 第一个想建的 eval task 名。
  - 估计复现成本: **低**（Python 3.10+ + uvx，≤30min 跑通 docs + 示例 eval）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Introducing Claude Opus 5 — Mid-conversation tool changes without invalidating prompt cache（Anthropic, **2026-07-24**）](https://www.anthropic.com/news/claude-opus-5)
  - 机会信号: Claude Platform beta 功能——对话中途切换可用 tools **不 invalidate prompt cache** → API 产品层正式承认「tool surface 动态变更 + cache 复用」是高频场景；Opus 5 同价（$5/$25 per M tokens）但 Frontier-Bench v0.1 SOTA → 出海 remote eval/API 集成可卖「multi-tool agent + cache-aware routing」咨询。
  - 风险信号: Opus 5 cyber classifiers 仍会 fallback 到 Opus 4.8；Automatic fallbacks 可能掩盖 safety refusal 为 silent model downgrade → 客户 eval 需记录实际 routing model，不能假设请求 model = 执行 model。
  - 我可执行的一步(<=30min): 写 **≤8 行**：你 agent 是否在 runtime 切换 tool set？若是有无记录 cache hit/miss 与 model fallback 事件（有/无/缺哪 2 项）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 46. Permutations](https://leetcode.com/problems/permutations/)（**Day 32, Aug 2026 Daily Challenge**；承接 0801 Combinations 换轨全排列）：给定不含重复数字的数组，返回所有可能的全排列。明日动作：模板化 backtrack（used/path）；写 **≤15 词**：「排列顺序敏感」与 agent eval「tool call 序列等价类」的类比。

- **IELTS（≤1 条）**：[IELTS Ready — British Council 官方免费备考（含 AI Mini Mock）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-ready)：50 分钟 Mini Mock + AI 估分。明日动作：完成 **Speaking Part 1 一段 Mini Mock**（0801 Writing、0729 Listening、0728 Reading，今日换 Speaking）；写 **≤15 词** 流利度/词汇/发音 选 1 个弱项。

## 6) 噪音过滤记录

- **LangGraph #6719 deps upgrade、LangChain #39166 model-profiles refresh**：纯 chore，无 agent 行为变更 → 过滤。
- **LangChain #39181 docuweave、#39189 OpenRouterRerank**：RAG 向，今日 Agent 主线已满 8 条 → 过滤（可下周 RAG 专题收）。
- **LangGraph #8503 AsyncPipeline 嵌套顺序**（0801 已收录）：#8507 为 SSL 场景不同 fix 路径，今日 §1 保留 #8507 代表。
- **LangChain #39178 Responses API stream failures**（0801 已收录）→ 不重复。
- **vLLM #50744 empty gRPC stop strings、#50745 GB10 doc、#50743 RISC-V FP16**：偏 frontend/doc/CPU，与当前 Attention focus 弱相关 → 过滤。
- **AutoGen #8017 MCP dep cap、#8016 Blender scaffold**：#7936 已代表 agentchat eval 修复 → 过滤。
- **Anthropic Jul 30 cyber eval incidents**（0801 §4 已收录）→ 不重复。
- **OpenAI Evals platform deprecation**（Jun 3 公告）：重要但非 Aug 1–2 新信号；trace grading 迁移可下周专节收 → 过滤。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-08-01~08-02** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + Blog Jul 31–Aug 2 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **654**（`0801.md` §7: 646 + 本日 §1: 8）
- Theory: **280**（`0801.md` §7: 275 + 本日 §2: 5）
- Career/Business: **110**（`0801.md` §7: 109 + 本日 §4: 1）
- LC: **92**（`0801.md` §7: 91 + 本日 §5: 1）
- IELTS: **92**（`0801.md` §7: 91 + 本日 §5: 1）
