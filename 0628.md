# Daily Research - 2026-06-28 (21:30)

## 0) 今日3条结论

- **AutoGen `GuardrailProvider` 协议进入 ready-for-review（#7881，2026-06-28 更新）**：`ALLOW`/`DENY`/`MODIFY` 三态 + per-tool guardrail chain，在 `run_json` 执行前拦截 tool call——与 openai/evals healthcare governance eval 的 ALLOW/DENY/ESCALATE 决策线直接对齐，是 **skill 落地 + eval** 的工程化护栏接口。
- **`merge_dicts` metadata 翻倍 bug 进入第五日仍未 merge**：issue [#38366](https://github.com/langchain-ai/langchain/issues/38366) 仍 **open**；**2026-06-28** 新 PR [#38512](https://github.com/langchain-ai/langchain/pull/38512)/[#38511](https://github.com/langchain-ai/langchain/pull/38511)/[#38510](https://github.com/langchain-ai/langchain/pull/38510) 均 **auto-close 未 merge**（external + missing-issue-link）——streaming chunk 聚合 `response_metadata` 污染问题依旧。
- **Agent KV cache 成为 attention 优化新主战场**：IntentKV（cross-turn intent + layout-preserving eviction）与 InfoKV（entropy + attention Forward Influence）分别从 **多轮 agent serving** 与 **长推理 decoding** 两侧压缩 KV；vLLM Oscar-2 2-bit KV quant（#46774）则从 **推理内存** 侧切入——三条线共同指向「agent trace 的 token/KV 成本」。

## 1) Agent工程化（high-signal）

- [tools: add GuardrailProvider protocol for tool call interception（#7881 ready for review **2026-06-28**）](https://github.com/microsoft/autogen/pull/7881)
  - 摘要: 实现 `GuardrailProvider` Protocol（#7405）：`Decision` enum（ALLOW/DENY/MODIFY）+ `GuardrailResult`；`BaseTool.run_json()` 在 tool 执行前按 chain 评估 guardrail，DENY 立即返回、MODIFY 替换 args 后继续。**2026-06-28** 追加 commit 修复 denial semantics + malformed MODIFY fail-closed，并标记 ready for review。14 个新测试覆盖 chain/short-circuit/per-instance 隔离。
  - 为什么重要(对我): **skill 落地 + eval**——tool call 拦截是 production agent 的 F/C 规则引擎落点；ALLOW/DENY/MODIFY 可直接映射你的 governance eval 三元决策与 L2 grading。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 是否在 tool 执行前有拦截层（有/无/未知）。

- [Add healthcare_ai_governance eval（openai/evals #1682 opened **2026-06-27**，active）](https://github.com/openai/evals/pull/1682)
  - 摘要: 20 个 hand-written 场景，测试 governed healthcare agent 对 PHI/权限/临床安全做出 `ALLOW`/`DENY`/`ESCALATE` 决策；exact match grading。覆盖越权、PHI 暴露、高风险临床动作无人工 sign-off、身份撤销后继续行动等 failure mode。
  - 为什么重要(对我): **agent eval 新维度**——generic benchmark 通过的 agent 在 governance 场景可能直接造成伤害；三元决策可复用到 F/C 规则 eval 框架。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent eval 是否测 governance 决策（测/不测/未知）。

- [Beyond Function Calling: ToolBench-X（arXiv 2606.25819，2026-06-24）](https://arxiv.org/abs/2606.25819) / [Foreverskyou/ToolBench-X](https://github.com/Foreverskyou/ToolBench-X)
  - 摘要: 在干净 tool 环境上注入 5 类可恢复 hazard（Specification Drift、Invocation Error、Execution Failure、Output Drift、Cross-source Conflict）；每例保证至少一条 recovery path（retry/fallback/verify/cross-check）。实验显示：可靠 tool 下表现好的 agent 在 recoverable hazard 下大幅失败；失败主因是 hazard 诊断与 recovery 策略不足，而非 tool 调用量或 inference budget。代码仓库 **2026-06-23** 创建，README 标注 full release 筹备中。
  - 为什么重要(对我): **eval 维度扩展**——你的 collect→test→eval 若只测 clean tool env，上线后 tool 漂移/执行失败会直接击穿；ToolBench-X 的 5 hazard 类型可映射为 L2 failure mode 清单。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否注入 tool 故障（是/否/未知）。

- [fix(core): restore prompt helper fallback to supplied LLM metadata（LlamaIndex #22177 opened **2026-06-28**）](https://github.com/run-llama/llama_index/pull/22177)
  - 摘要: `Settings.prompt_helper` / `Settings.chat_prompt_helper` property 在 LLM 未设置时会抛 `ValueError`，导致显式传入 `llm` 的 synthesizer 无法 fallback 到 `PromptHelper.from_llm_metadata(llm.metadata)`。Fix 改为读 `Settings._prompt_helper` / `Settings._chat_prompt_helper` 内部字段，并加 regression test。
  - 为什么重要(对我): **skill 落地 + token 优化**——prompt helper 决定 chunk size / context window 切分；回归会导致 RAG agent 静默用错 context 预算，eval 结果不可比。
  - 明日动作(<=20min): 写 **是/否**：你是否显式传 `llm` 给 `get_response_synthesizer`（是/否/未知）。

- [Fix AgentWorkflow agent-level structured output（LlamaIndex #22163 opened **2026-06-27**，active **2026-06-28**）](https://github.com/run-llama/llama_index/pull/22163)
  - 摘要: `AgentWorkflow` 运行 agent 时原先丢失 agent 自身的 `structured_output_fn`/`output_cls`；Fix 保留 agent-level 配置，workflow-level 仍优先。Fixes [#22159](https://github.com/run-llama/llama_index/issues/22159)。
  - 为什么重要(对我): **skill 落地 + eval**——multi-agent workflow 中 per-agent structured output 是 L2 eval 的 grading 锚点，被吞掉等于 eval harness 失效。
  - 明日动作(<=20min): 写 **是/否**：你是否在 AgentWorkflow 内用 per-agent `structured_output_fn`（是/否/未知）。

- [[WIP][Feature] Oscar-2: 2-bit KV cache quantisation（vLLM #46774 updated **2026-06-28**）](https://github.com/vllm-project/vllm/pull/46774)
  - 摘要: 新 2-bit KV cache 量化后端（data-free）；Qwen3-32B 测试：内存降 **5.15×**（相对 FP16），GSM8K 精度仅降 **0.07%**。Closes [#46221](https://github.com/vllm-project/vllm/issues/46221)。作者计划补充 Qwen3-4B-Thinking/8B、GLM-4.7-FP8 及与 nvfp4/turboquant 对比。
  - 为什么重要(对我): **token 优化**——长 trace agent 的 KV 内存是 serving 瓶颈；2-bit data-free quant 若保精度，可直接降低 agent 并发成本。
  - 明日动作(<=20min): 写 **是/否**：你是否监控 agent serving 的 KV 内存占用（是/否/未知）。

- [[KV Offloading] Add basic offloading metrics（vLLM #45958 updated **2026-06-28**）](https://github.com/vllm-project/vllm/pull/45958)
  - 摘要: 新增 `vllm:kv_offload_lookup_delay_seconds`、`vllm:kv_offload_allocation_failure`、`vllm:kv_offload_cpu_allocation_size` 三个 metric；附 scheduler 与 CPU manager 单测。
  - 为什么重要(对我): **token 优化 + eval 对齐**——KV offload 是长 context agent 的标配；无 metric 则无法把 offload delay/allocation failure 纳入 L3 eval 门禁。
  - 明日动作(<=20min): 写 **是/否**：你是否用 vLLM KV offloading（是/否/未知）。

- [[KV-Offloading] Fix tensors_per_block stride（vLLM #46888 merged **2026-06-28**）](https://github.com/vllm-project/vllm/pull/46888)
  - 摘要: Offloading connector 在 `Qwen3.6-35B-A3B` 与 `NVIDIA-Nemotron-3-Super-120B-FP8` 上因 `tensors_per_block` stride 不一致 assert 失败；Fix 统一 stride 处理。Follow-up to #44577。
  - 为什么重要(对我): **skill 落地**——MoE + KV offload 是 production agent 常见配置；stride bug 导致特定模型族直接无法 serve。
  - 明日动作(<=20min): 写 **是/否**：你是否用 Qwen3.6/Nemotron 类 MoE 做 agent backend（是/否/未知）。

- [[Model Runner V2][Spec Decode] Handle tuple hidden states from MTP draft models（vLLM #46786 merged **2026-06-28**）](https://github.com/vllm-project/vllm/pull/46786)
  - 摘要: 部分 MTP draft model 声明单 tensor 但返回 `(logits_hidden, feedback_hidden)` tuple 用于 final-norm 正确性；旧代码未处理导致 spec decode 失败。Fixes [#46798](https://github.com/vllm-project/vllm/issues/46798)。
  - 为什么重要(对我): **token 优化（spec decode）**——spec decode 是 agent TTFT/TPS 优化的关键杠杆；MTP tuple hidden states 是 reasoning model 的新契约。
  - 明日动作(<=20min): 写 **是/否**：你是否用 MTP/EAGLE spec decode（是/否/未知）。

- [[Render][Speculator] Add return_loss_mask to render endpoint（vLLM #46846 merged **2026-06-28**）](https://github.com/vllm-project/vllm/pull/46846)
  - 摘要: `POST /v1/chat/completions/render` 新增 `return_loss_mask`，在单次 `apply_chat_template` 中返回 per-token assistant/trainable mask；供 [speculators](https://github.com/vllm-project/speculators/issues/652) 等训练框架复用 vLLM tokenization 而非本地重复实现。
  - 为什么重要(对我): **eval + training 对齐**——agent RL/SFT 的 loss mask 与 inference tokenization 不一致会导致 eval 与训练分布漂移。
  - 明日动作(<=20min): 写 **是/否**：你的 agent 训练是否复用 inference tokenizer（是/否/未知）。

- [fix(core): don't evict on in-place InMemoryCache update（LangChain #38512 opened & auto-closed **2026-06-28**）](https://github.com/langchain-ai/langchain/pull/38512)
  - 摘要: `InMemoryCache.update()` 在 cache 满时对已有 key 的 refresh 也会 evict 最旧条目，导致无关 prompt 被踢出、有效 maxsize 缩水。Fix：仅在新 key 插入时 evict。Closes [#36750](https://github.com/langchain-ai/langchain/issues/36750)。**auto-close 未 merge**。
  - 为什么重要(对我): **token 优化**——LLM cache 是 agent 重复 query 的成本杠杆；错误 eviction 导致 cache hit rate 下降、eval 成本膨胀。
  - 明日动作(<=20min): 写 **是/否**：你是否用 LangChain `InMemoryCache`（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [IntentKV: Cross-Turn Intent-Aware KV Cache Pruning for Agent Inference（arXiv 2606.09916，2026-06-06）](https://arxiv.org/abs/2606.09916)
  - 摘要: 多轮 agent 将短 query 展开为长 tool-call/search/reasoning 轨迹，KV cache 成为 serving 瓶颈（非参数计算）。IntentKV 保持 base LLM frozen，维护 session-level QueryMemory 聚合跨轮 intent，用 memory-attention 评分 live history tokens；eviction 采用 slot-map redirection（dropped positions → sentinel dead slot），保留 surviving K/V rows/RoPE phases/slot identities，与 radix prefix cache 兼容。8k KV budget 下：Qwen3-8B peak tokens 降 23.9%、Qwen2.5-14B 降 30.7%；最长 100 BCP queries 上 worst-case peak 从 92.3k → 20.5k（↓77.8%），raw KV reads 从 411M → 31M（↓92.6%）。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) prompt-local KV pruning（SnapKV/H2O）为何在多轮 agent 会 stale？(2) layout-preserving eviction vs compaction 对 vLLM/SGLang prefix cache 意味着什么？(3) cross-turn QueryMemory 是否应纳入 L2 eval（过早 prune 导致 tool-call 证据丢失）？
  - 明日动作(<=20min): 写 **≤10 词**：你 agent 是否做 KV cache pruning（是/否/未知）。

- [InfoKV: Information-Aware KV Cache Compression for Long Reasoning（arXiv 2606.26875，2026-06-25）](https://arxiv.org/abs/2606.26875)
  - 摘要: 现有 KV 压缩主要依赖 attention weight 估计 token 重要性，但 attention 是 backward-looking 短程信号。提出 Forward Influence 度量压缩 token 对未来 context 的影响：高 entropy token 对 distant future 影响更强。InfoKV 将 token-level predictive uncertainty + layer-wise representation evolution 的 entropy score 与 attention score 融合。Llama-3.1/3.2、DeepSeek-R1 长推理 benchmark 上 consistently 优于纯 attention-based 方法。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) attention score 选 KV 为何在长推理会丢 distant-future 信息？(2) entropy 信号与 attention 的 Forward Influence 差异对 agent multi-step reasoning 意味着什么？(3) 你的 L2 eval 是否区分 prefill vs decoding 阶段的 KV 压缩影响？
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否区分 prefill/decoding KV 行为（区分/不区分/未知）。

- [[Bugfix][DSv3.2] Skip indexer weights for index-cache-skipped layers（vLLM #46600 merged **2026-06-28**）](https://github.com/vllm-project/vllm/pull/46600)
  - 摘要: DeepSeek-V3.2 在 `index_topk_freq>1` 时仅部分 layer 构建 sparse indexer（其余 `_skip_topk`），但 checkpoint 仍含所有 layer 的 `indexer.*` weights，导致 `load_weights` 对 skipped layer 赋值失败。Fix：仅对实际构建 indexer 的 layer prefix 加载 indexer weights。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) sparse attention indexer 与 full attention 的 per-layer 分工是什么？(2) `index_topk_freq` 跳过 layer 对 agent 长 context 的 attention pattern 意味着什么？(3) 加载 indexer weights 失败是否是 eval 与 production 配置不一致的隐性断点？
  - 明日动作(<=20min): 写 **≤10 词**：你是否用 DeepSeek sparse indexer（是/否/未知）。

## 3) 热门项目/可试个人项目

- [Zhudongsheng75/ToolMaze](https://github.com/Zhudongsheng75/ToolMaze)
  - 摘要: 配套 arXiv [2606.05806](https://arxiv.org/abs/2606.05806) 的可执行 eval 框架：在 sandboxed tool runtime 中拦截 tool call 注入 P0–P4 perturbation（transient/persistent），按 C1–C4 复杂度评分 recovery behaviour。trace logger 记录每轮 tool call、perturbation status、token count。
  - 为什么重要(对我): **eval harness 参考**——比 ToolBench-X 更早可跑；perturbation injection + complexity-aware judge 可直接借鉴到你的 F/C 规则 eval。
  - 明日动作(<=20min): clone 仓库；写 **≤15 词** 你想注入的 1 种 tool 故障类型。
  - 估计复现成本: **低**（Python + sandbox，无 GPU 要求）。

- [Foreverskyou/ToolBench-X](https://github.com/Foreverskyou/ToolBench-X)
  - 摘要: ToolBench-X benchmark 官方仓库（**2026-06-23** 创建）；README 标注 codebase 与 benchmark files 正在整理，full release soon。配套论文 [2606.25819](https://arxiv.org/abs/2606.25819)。
  - 为什么重要(对我): **eval 先行占位**——5 hazard 类型可提前设计 eval case，等 release 后直接对接。
  - 明日动作(<=20min): star 仓库 + 写 **≤15 词** 你最想测的 1 种 hazard（Specification Drift/Execution Failure/…）。
  - 估计复现成本: **低**（当前仅 README，等 release 后升为 **中**）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Ian Xiaohei Illustrations EN — Codex Skill 英文版 fork（@ianneo_ai）](https://github.com/tojileon/ian-xiaohei-illustrations-en)
  - 机会信号: **skill 即产品 + 出海**——`@ianneo_ai` 原版 skill 的英文 fork（84 stars，MIT），将「认知锚点 → 物理场景 → 16:9 手绘 PNG」workflow 适配英文长文/Notion/博客；证明一人公司内容生产 skill 可跨语言复用。
  - 风险信号: 视觉 IP 绑定「小黑」风格；英文 fork 与原版维护分叉；Codex Skill 分发仍依赖 GitHub/X 而非 marketplace。
  - 我可执行的一步(<=30min): 读英文 README 的 skill 结构；写 **≤20 词** 你能否把 agent eval 报告自动配图 skill 化（能/不能/需改造）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 116 Populating Next Right Pointers in Each Node](https://leetcode.com/problems/populating-next-right-pointers-in-each-node/)：完美二叉树每层节点 `next` 指向右邻；O(1) 空间解法利用已建立的 `next` 指针逐层遍历（`left.next=right; right.next=next.left`）。明日动作：手写 O(1) 空间版（≤20min）；写 **≤15 词**：逐层 `next` 链接对你 multi-agent workflow 状态传递意味着什么。

- **IELTS（≤1 条）**：[IELTS Writing Task 2 — Agree/Disagree 结构模板（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-practice-tests/writing/academic/task-2/agree-disagree-essay)：4 段结构（paraphrase + position → body ×2 with examples → conclusion restate）；position 段 alone 区分 band 5 vs 7。明日动作：用 1 道 agree/disagree 样题写 position 段（≤20min，目标 50–60 词）；写 **≤15 词** 弱项（position/论证/例子 选 1）。

## 6) 噪音过滤记录

- **LangChain #38510/#38512/#38511 auto-close**：#38510 JSON parser 泛化、#38512 InMemoryCache、#38511 Chroma relevance 均 **2026-06-28 auto-close 未 merge**（external + missing-issue-link）；#38512 作为 InMemoryCache 追踪条目收录，#38510 与 `0627.md` #38503 同线（#36603 serialization）合并追踪不重复收录，#38511 RAG 信号弱于 agent eval 主线 → 丢弃。
- **LangGraph #8208 auto-close**：Postgres pipeline autocommit fix 思路正确但 **2026-06-28 auto-close 未 merge**；与 `0626.md` #8198 checkpoint 线重复 → 不单独收录。
- **LangChain merge_dicts #38366/#38502**：第五日追踪已在 §0 结论；#38502 与 `0627.md` 重复 → 不重复收录 PR 细节。
- **LlamaIndex #22164/#22165**：`0627.md` 已收录；#22164 **2026-06-28** 仍 closed 未 merge → 不重复。
- **vLLM ROCm/GLM5 硬件专项**（#46930/#46886/#44313 等）：硬件/CI 专项，通用 agent 信号弱 → 丢弃。
- **vLLM #44297 spec decode bitmask**：`0627.md` 已收录，**2026-06-27** 后无新 update → 不重复。
- **MiniMax MSA / SelfCompact**：`0627.md` 已收录，无新进展 → 不重复。
- **Simon Willison / Anthropic / OpenAI blog**：**2026-06-28** 无新 post 匹配当前 focus → 不收录。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-06-28** 原帖 → 不收录。
- **@gefei55 职业内容**：第三方聚合页无法验证 **2026-06-28** 原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub 当日高信号 ≥8 条，无需 skill 补充。
- **openai/evals #1683**：化学方程式 label 修正，与 agent eval 主线弱相关 → 丢弃。

## 7) 本周累计计数

- Agent: **410**（`0627.md` §7: 399 + 本日 §1: 11）
- Theory: **173**（`0627.md` §7: 170 + 本日 §2: 3）
- Career/Business: **78**（`0627.md` §7: 77 + 本日 §4: 1）
- LC: **60**（`0627.md` §7: 59 + 本日 §5: 1）
- IELTS: **60**（`0627.md` §7: 59 + 本日 §5: 1）
