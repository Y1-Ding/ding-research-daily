# Daily Research - 2026-06-23 (21:30)

## 0) 今日3条结论

- **LangGraph inmem 删 thread 会泄漏 channel blobs**：**2026-06-23** [#8054](https://github.com/langchain-ai/langgraph/issues/8054) 维护者复现——`delete_thread` 清 `checkpointer.storage` 但 **blobs 残留**（`storage=False, blobs=5`），长驻 `langgraph dev` 会 **无界内存增长**；官方 `checkpointer.delete_thread()` 可正确清零。
- **vLLM 0.23 `qwen3_coder` tool-call 输出格式与 0.22 不兼容**：**2026-06-23** [#46493](https://github.com/vllm-project/vllm/issues/46493)——同模型 `qwen3.6-35b-a3b-awq` + `--tool-call-parser qwen3_coder`，0.22 输出 **Hermes 风格 JSON**，0.23 变为 **XML `<tool_call>`**；agent 客户端 parser 升级前会 **静默断链**。
- **SummarizationMiddleware orphan ToolMessage 已有上游修复**：**2026-06-23** 合并 [#38382](https://github.com/langchain-ai/langchain/pull/38382)（`0622.md` #38352 的 fix）——`_find_safe_cutoff_point` 改用 `unmatched_tool_ids` **全量匹配** 后才 cutoff，避免 mixed-orphan 场景留下 orphan `ToolMessage` → provider 400。

## 1) Agent工程化（high-signal）

- [`langgraph dev` 删 thread 泄漏 channel blobs → 无界内存（#8054，updated **2026-06-23**）](https://github.com/langchain-ai/langgraph/issues/8054)
  - 摘要: inmem runtime 的 thread 删除路径只清 `checkpointer.storage`，**不清理该 thread 的 blob entries**。维护者 **2026-06-23** 在 `langgraph-runtime-inmem==0.30.0` 复现：删后 `storage=False` 但 `blobs=5` 仍残留；对比 `checkpointer.delete_thread()` 可 `blobs=0`。长驻 dev server 反复建删 thread 会累积泄漏。
  - 为什么重要(对我): **本地 agent 开发/压测** 若频繁删 thread 而不重启，内存会 **静默爬升**——与 checkpoint bloat (#7714) 是不同泄漏路径。
  - 明日动作(<=20min): 写 **是/否**：你是否在 `langgraph dev` 中长驻运行且会删 thread（是/否/未知）。

- [SHACKLE 预执行 guard 节点防 agent 无限循环（#8167 closes #6731，**2026-06-23**）](https://github.com/langchain-ai/langgraph/pull/8167)
  - 摘要: 新增 `langgraph.shackle.ShackleGuard`——在 tool 执行前检查最后 `AIMessage.tool_calls`，按 **budget 剩余 / 重复调用检测（loop of death）/ 错误信号放大（401/403/500/timeout）/ wall-clock timeout** 路由到 `allow`→tools、`deny`→`__end__`、`hitl`→human_approval。纯 Python <200 行，零新依赖。同时覆盖 #4397 HITL approval flow。
  - 为什么重要(对我): **recursion limit 是事后熔断**——pre-execution circuit breaker 可在 **重复 tool call 模式** 早期截断，降低 token 浪费与 eval 超时。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 是否已有 **repeat-call 检测**（是/否/计划）。

- [fix(agents): SummarizationMiddleware orphan ToolMessage（#38382 merged **2026-06-23**）](https://github.com/langchain-ai/langchain/pull/38382)
  - 摘要: 修复 #38352——原逻辑用 set intersection 短路匹配，mixed-orphan 时保留无配对 `ToolMessage`；新逻辑维护 `unmatched_tool_ids`，**所有 ToolMessage 均匹配到 AIMessage** 才返回 cutoff，否则 fallback 跳过整块 orphan。
  - 为什么重要(对我): **长会话 agent summarization** 的 production bug 已有上游 fix——升级前需自查是否 pin 了旧版 middleware。
  - 明日动作(<=20min): 写 **版本号或否**：你是否启用 `SummarizationMiddleware` 及 langchain 版本（是+版本/否/未知）。

- [count_tokens_approximately 对 audio/video/file 按 base64 字符计数高估（#38229，opened **2026-06-23**）](https://github.com/langchain-ai/langchain/issues/38229)
  - 摘要: `langchain_core.messages.utils.count_tokens_approximately` 对 `image` 块给固定 penalty，但 `audio`/`video`/`file`/`text-plain` data 块 fall through 到 `repr(block)` 并 **char-count base64 payload**——10KB base64 音频可计 ~10K tokens，而 image 仅 ~85 tokens。影响 **summarization trigger / budget middleware** 的 token 估算。
  - 为什么重要(对我): **multimodal agent 的 token budget** 若依赖 approximate count，会 **过早触发 summarize 或拒绝请求**。
  - 明日动作(<=20min): 写 **是/否**：你的 agent 是否用 `count_tokens_approximately` 做 budget 决策（是/否/未知）。

- [merge_dicts 流式聚合时 model_name/finish_reason 字符串翻倍（#38366，opened **2026-06-23**）](https://github.com/langchain-ai/langchain/issues/38366)
  - 摘要: `langchain_core.utils._merge.merge_dicts` 对 streaming chunk 的 `response_metadata` 合并时，**相同字符串值** 的 `model_name`/`finish_reason` 等字段走 concatenate 分支 → `"stopstop"`、`"model/model"`。影响 **eval 日志、usage 归因、finish_reason 分支逻辑**。
  - 为什么重要(对我): **agent eval pipeline** 若从 aggregated chunk 读 metadata，会得到 **污染字段**——与 #38223 streaming schema 问题是不同故障线。
  - 明日动作(<=20min): 写 **是/否**：你的 eval 是否从 `chunk + chunk` 聚合 metadata（是/否/未知）。

- [vLLM 0.22 vs 0.23 `--tool-call-parser qwen3_coder` 输出格式断裂（#46493，opened **2026-06-23**）](https://github.com/vllm-project/vllm/issues/46493)
  - 摘要: 模型 `qwen3.6-35b-a3b-awq`，命令含 `--tool-call-parser qwen3_coder --enable-auto-tool-choice --reasoning-parser qwen3`。0.22 输出 Hermes 风格 JSON `{"category":1,"intent_ids":[1]}`；0.23 输出 XML `<tool_call><function=...>`。Reporter 确认 **同 parser 名、不同版本行为**。
  - 为什么重要(对我): **自托管 agent inference 升级** 时 tool-call parser 是 **静默 breaking change**——客户端/OpenAI-compatible 层需版本 pin 或双 parser 兼容。
  - 明日动作(<=20min): 写 **版本号**：你 vLLM 版本及 qwen tool parser（如 0.22/0.23/未知）。

- [[Bugfix] Responses API assistant EasyInputMessageParam 字符串 content（#44361 merged **2026-06-23**）](https://github.com/vllm-project/vllm/pull/44361)
  - 摘要: `{"role":"assistant","type":"message","content":"15. QED."}` 被错误 coerce 为 `ResponseOutputMessage`，Pydantic 逐字符解析 → BadRequest。修复：不 coerce `EasyInputMessageParam`，在 `construct_chat_messages_with_tool_call` 中与 `ResponseOutputMessage` 同等处理。e2e 验证 Qwen3-8B + gpt-oss-20b。
  - 为什么重要(对我): **multi-turn agent 走 vLLM Responses API** 时，assistant 历史消息格式是 **常见输入**——此 bug 会阻断整条 trace replay。
  - 明日动作(<=20min): 写 **是/否**：你是否用 vLLM `/v1/responses` 做 agent 多轮（是/否/未知）。

- [fix(google-genai): MALFORMED_FUNCTION_CALL 不再 raise（llama_index #22098，opened **2026-06-23**）](https://github.com/run-llama/llama_index/issues/22098)
  - 摘要: Gemini 偶发 `MALFORMED_FUNCTION_CALL`/`UNEXPECTED_TOOL_CALL` finish reason（上游 transient server 行为）。原 `_error_if_finished_early` 对非 STOP 一律 `RuntimeError` 崩溃 agent loop；patch 对这两类 **log warning 并正常返回**，与 langchain-google-genai 策略对齐。关联 googleapis/python-genai#1818。
  - 为什么重要(对我): **Gemini agent** 的 tool-call 容错应在 framework 层 **降级而非 crash**——否则一次 malformed 即终止整 run。
  - 明日动作(<=20min): 写 **是/否**：你是否用 Gemini 做 tool-calling agent（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Role Confusion：LLM 按文本风格而非 role tag 分类特权/非特权文本（Simon Blog + 论文，**2026-06-22**）](https://role-confusion.github.io)
  - 摘要: Charles Ye 等提出 **role confusion**——模型区分 system/user/think/assistant/tool 时，**写作风格比 role tag 权重更高**。攻击者模仿 `think` 块风格可将 user 输入误判为特权推理；**destyling**（轻微改写风格）使攻击成功率从 61% 降至 10%。结论：无 genuine role perception 则 injection defense 是 perpetual whack-a-mole。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) agent system prompt 的 **格式一致性** 是否反而扩大 attack surface？(2) tool output 若 mimics assistant style，模型会如何归类？(3) 你的 guardrail 应检测 **style mimicry** 还是 **tag boundary**？
  - 明日动作(<=20min): 读 [role-confusion.github.io](https://role-confusion.github.io) §3；写 **≤15 词**：你 agent 是否隔离 tool output 格式（是/否/未知）。

- [Pre-execution circuit breaker vs recursion limit 事后熔断（#8167/#6731，**2026-06-23**）](https://github.com/langchain-ai/langgraph/issues/6731)
  - 摘要: Agent 在 tool 重复调用时可达 recursion limit 才终止；SHACKLE 在 **tool 执行前** 检测 repeat pattern + budget，路由到 deny/HITL。无限循环根因常是 **模型+环境反馈环** 而非 graph 结构 bug。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) repeat-call 检测应看 **tool name+args hash** 还是仅 name？(2) budget 用 token 还是 wall-clock？(3) HITL 与 deny 的 **eval 可复现性** 如何平衡？
  - 明日动作(<=20min): 写 **≤15 词**：你定义的 "loop of death" 判据（重复次数/相同args/其他）。

- [Tool-call parser 版本间输出格式语义漂移（#46493，**2026-06-23**）](https://github.com/vllm-project/vllm/issues/46493)
  - 摘要: 同 `--tool-call-parser qwen3_coder`，0.22→Hermes JSON、0.23→XML。Parser 名不变但 **token 序列契约** 变了——说明 inference 层 "parser" 是 **启发式后处理** 而非稳定 ABI。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) tool-call 应定义为 **结构化 token 还是后处理文本**？(2) 升级 inference 时如何 **契约测试** parser 输出？(3) XML vs JSON parser 对 **streaming partial parse** 有何不同？
  - 明日动作(<=20min): 写 **≤10 词**：你是否有 parser 输出 snapshot test（是/否）。

- [Checkpoint storage vs channel blobs 生命周期不一致（#8054，**2026-06-23**）](https://github.com/langchain-ai/langgraph/issues/8054)
  - 摘要: Thread 删除只清 storage 索引，blobs（channel 大对象） orphaned。说明 inmem runtime 的 **资源所有权模型** 未统一——storage 与 blobs 是两套 GC。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) agent state 的 **logical delete** 是否应 cascade 到所有附属存储？(2) dev vs prod runtime 的 GC 语义差异对 **eval 环境忠实度** 有何影响？(3) 与 #8157 deque maxlen serde 丢失的 **silent state drift** 是否同类？
  - 明日动作(<=20min): 写 **≤10 词**：你 dev 环境是否定期重启 runtime（是/否/未知）。

## 3) 热门项目/可试个人项目

- [llama_index workflow_as_mcp `workflow_factory` 逐请求隔离（#22087，opened **2026-06-23**）](https://github.com/run-llama/llama_index/issues/22087)
  - 摘要: 修复 #22071——`workflow_as_mcp` 新增可选 `workflow_factory: Callable[[], Workflow]`，每 MCP 请求构造新实例；保留 `workflow=...` 单实例兼容路径。回归测试证明两 client 不共享 `self.*` 可变状态。
  - 为什么重要(对我): **多租户 MCP agent server** 本地验证 isolation 的最低成本路径。
  - 明日动作(<=20min): 读 PR diff 中 factory 签名；用两 client 并发调用 CountingWorkflow 验证 counter 不串。
  - 估计复现成本: **低**（`llama-index-tools-mcp` + 文中示例）。

- [Fix MCP tool schema `additionalProperties` 映射（llama_index #22099，opened **2026-06-23**）](https://github.com/run-llama/llama_index/issues/22099)
  - 摘要: MCP tool JSON Schema 转 Pydantic 时显式映射 `additionalProperties: false/true/object/omitted` → `model_config` extra fields 行为；覆盖 default/boolean/typed/nested 用例，确保 round-trip schema 语义一致。
  - 为什么重要(对我): **MCP tool schema 严格性** 直接影响 agent 能否传额外参数——与 LangChain #38223 `additionalProperties` 问题是同源不同层。
  - 明日动作(<=20min): 找一个你常用 MCP tool 的 schema；写 **≤10 词** 其 `additionalProperties` 值（false/true/未知）。
  - 估计复现成本: **低**（读 issue + 本地 schema 对照）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [LangGraph docs：agent 完成声明的可审计 final-state receipts（#7844，updated **2026-06-23**）](https://github.com/langchain-ai/langgraph/issues/7844)
  - 机会信号: 提议在 LangGraph 文档增加 **auditable completion receipt** 模式——记录 agent 声称完成的事项、支撑证据、责任人、human approval 门槛。对 **regulated industries（金融/航空）** 与 **AI solo company 卖合规 agent** 是直接产品差异化信号；Anthropic **2026-06-12** TCS/DXC 合作亦指向 regulated 部署。
  - 风险信号: 仅为 feature request，无官方采纳；receipt 层增加工程复杂度；可能与现有 observability（LangSmith）重叠。
  - 我可执行的一步(<=30min): 读 [SACP receipt layer](https://github.com/aDragon0707/sacp)；写 **≤20 词** 你 agent 最终输出是否含可验证 evidence（是/否/计划）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 641. Design Circular Deque](https://leetcode.com/problems/design-circular-deque/)：双端环形队列 ↔ LangGraph #8054 的 **delete 未 cascade GC**——实现后写 **≤15 词**：`delete` 只清索引不释放对象时会发生什么（泄漏/悬空/未知）。

- **IELTS（≤1 条）**：[British Council — IELTS Academic Writing Task 1 sample](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-practice-tests/academic-test/writing)：官方图表描述样题。明日动作：写 **Overview 1 句 + 1 个 data comparison 句**（≤20 min）；写 **≤15 词** 弱项（数据选取/比较结构/语法 选 1）。

## 6) 噪音过滤记录

- **vLLM #45702 hybrid prefix cache RFC**：`0622.md` 已收录；**2026-06-23** 无新 merge/实质性 review comment → 语义去重丢弃。
- **LangChain #38351 create_agent KeyError('model')、#38352 orphan ToolMessage（open）**：`0622.md` 已展开；#38382 fix 已单独收录 → 原 open issue 不重复。
- **LangGraph #8112 SSE duplicate ID、#7845 tool-call leak、#8157 deque maxlen**：`0622.md` 已收录；**2026-06-23** 无新进展 → 丢弃。
- **LangGraph #8165 exit-mode UUID task_id**：**2026-06-22** 开，当日无新 comment → 丢弃（待 merge 再收录）。
- **vLLM #46379 swap_blocks_batch default stream、#42405 DMA expandable_segments**：KV offload 主线，`0621.md`/`0620.md` 已覆盖同类主题 → 丢弃。
- **vLLM NemotronLayerNorm / MoE refactor / ROCm DSV4**：硬件/模型专项，agent 通用信号弱 → 丢弃。
- **Simon sqlite-utils 4.0rc1、Temporary Cloudflare Accounts**：`0622.md` 已收录 → 丢弃。
- **Simon Moebius browser port（2026-06-22）**：技术探索向，当日 agent 主线信号弱于 #22087/#22099 → 移至噪音（可在技术探索池自行跟进）。
- **Anthropic / OpenAI / DeepMind / Lil'Log**：无 **2026-06-23** agent 工程直接新 post → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取原帖 → 不收录。

## 7) 本周累计计数

- Agent: **362**（`0622.md` §7: 354 + 本日 §1: 8）
- Theory: **154**（`0622.md` §7: 150 + 本日 §2: 4）
- Career/Business: **73**（`0622.md` §7: 72 + 本日 §4: 1）
- LC: **55**（`0622.md` §7: 54 + 本日 §5: 1）
- IELTS: **55**（`0622.md` §7: 54 + 本日 §5: 1）
