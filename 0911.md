# Daily Research - 2026-09-11 (21:30)

## 0) 今日3条结论

- **LangGraph `interrupt(response_schema=...)` 把 HITL resume 从 free-form JSON 升级为 typed form + Pydantic 校验**（#8886, created **2026-09-10**）：invalid resume 在 node commit 前 raise → **skill 落地须把 interrupt/resume 当作 schema-gated eval 维度，而非只测 happy-path resume。**
- **parallel tool call ID 碰撞成批出现**（LangChain #40401 + LlamaIndex #22368, updated **2026-09-11**）：Gemini 并行同 tool 多 call 时 positional key / tool-name-as-id 导致 earlier call 被 drop → **agent eval 须测 parallel same-tool calls 的 result correlation gold，不能假设 id 唯一。**
- **vLLM YaRN 二次 scaling bug 暴露 RoPE 参数链与 Transformers 漂移**（#56446, merged **2026-09-11**）：vendor alias 下 `max_model_len` 被 factor 乘两次，cos/sin cache 越界 → **long-context attention eval 须 pin rope_type + max_len derivation，不能只看 config 数字。**

## 1) Agent工程化（high-signal）

- [feat(langgraph): add response_schema to interrupt()（LangGraph PR #8886, created **2026-09-10**）](https://github.com/langchain-ai/langgraph/pull/8886)
  - 摘要: 新增 keyword-only `response_schema` 至 `interrupt()`，存于 `Interrupt.response_schema`（JSON Schema）；支持 dict / Pydantic / TypedDict / dataclass；Pydantic 路径 resume 值 validate 后才 return，invalid 在 node commit 前 raise；`stream_mode="debug"` payload 增 `response_schema` 字段；SDK-py TypedDict 同步。
  - 为什么重要(对我): **skill 落地 + 评测**——HITL interrupt/resume 的 typed form gate 是 agent platform eval 的 schema-validation 维度；须测 invalid resume reject 与 coercion path。
  - 明日动作(<=20min): 写 **是/否**：你的 agent eval 是否测 interrupt resume schema validation（是/否/未知）。

- [fix(langgraph): hydrate message lists in RemoteGraph invoke/ainvoke outputs（LangGraph PR #8884, created **2026-09-10**）](https://github.com/langchain-ai/langgraph/pull/8884)
  - 摘要: Fixes #8885 / langchain#40368。`RemoteGraph.invoke/ainvoke` 原返回 raw `list[dict]` message，local `Pregel` 经 `add_messages` reducer 返回 `BaseMessage`；deepagents 等用 `isinstance(msg, AIMessage)` 选 final response 时 silent empty。Fix 新增 `_hydrate_output_messages` 在 invoke/ainvoke 返回前 hydrate。
  - 为什么重要(对我): **skill 落地 + 评测**——local vs remote graph output contract parity 是 agent platform eval 的 deployment-mode gold；须双模式断言同一 consumer 行为。
  - 明日动作(<=20min): 写 **是/否**：你的 agent eval 是否区分 local vs remote graph message type（是/否/未知）。

- [fix(core): prevent parallel tool calls from colliding in v3 message streams（LangChain PR #40401, created **2026-09-11**）](https://github.com/langchain-ai/langchain/pull/40401)
  - 摘要: Fixes #40392。Gemini 等模型并行 tool call 以 separate chunks 流式返回，每 chunk 内 block position 均为 0 → `_iter_protocol_blocks` positional key 碰撞，`_accumulate` 丢弃 earlier calls。Fix 优先用 `tool_call.id` 作 key。
  - 为什么重要(对我): **评测**——parallel tool call stream aggregation 是 agent eval 的 delivery-shape gold；须测 same-chunk-position 多 call 不丢结果。
  - 明日动作(<=20min): 写 **是/否**：你的 tool-call eval 是否含 parallel same-position stream case（是/否/未知）。

- [fix(core): apply strict to pre-wrapped {type:function} dicts in convert_to_openai_tool（LangChain PR #40397, created **2026-09-11**）](https://github.com/langchain-ai/langchain/pull/40397)
  - 摘要: Fixes #40332。caller 传 `{"type":"function","function":{...}}` + `strict=True` 时 early return 静默丢弃 strict；bare vs wrapped 产出不同 schema（`function.strict` / `additionalProperties` 缺失）。Fix 对 wrapped form 走 `convert_to_openai_function(strict=strict)`。
  - 为什么重要(对我): **skill 落地 + token 优化**——tool schema strictness 的 input-shape parity 是 agent platform eval 的 schema-gate gold；须测 wrapped vs bare 同输出。
  - 明日动作(<=20min): 写 **是/否**：你的 tool schema eval 是否测 pre-wrapped dict + strict=True（是/否/未知）。

- [fix: gemini tool request ids（LlamaIndex PR #22368, updated **2026-09-11**）](https://github.com/run-llama/llama_index/pull/22368)
  - 摘要: `GoogleGenAI.get_tool_calls_from_response()` 原用 tool name 作 `ToolSelection.tool_id`，丢弃 Gemini 返回的 per-call unique ID；parallel 同 tool 多 call 时 ID 碰撞，result 无法 correlate。Fix 保留 Gemini per-call ID。
  - 为什么重要(对我): **评测 + skill 落地**——与 LangChain #40401 同根：parallel same-tool call correlation 是 voice/agent workflow eval 的 ID-uniqueness gold。
  - 明日动作(<=20min): 写 **是/否**：你的 parallel tool-call eval 是否测 same-tool-name 多 call（是/否/未知）。

- [fix(core): restore token streaming for the default Refine program（LlamaIndex PR #23022, created **2026-09-11**）](https://github.com/run-llama/llama_index/pull/23022)
  - 摘要: 自 0.14.22 (#21374) 起 `Refine/CompactAndRefine` + `streaming=True` 不再 token stream，而是一次 yield 整段 answer；`CondensePlusContextChatEngine.astream_chat()` 硬编码 `CompactAndRefine` 无 workaround。根因：`_get_attribute_from_object_generator` 对 plain token stream 也 drain 为单值。
  - 为什么重要(对我): **token 优化 + 评测**——RAG response synthesizer streaming parity 是 latency/UX eval 的 delivery-mode gold；须测 Refine path token-by-token 而非 aggregate chunk。
  - 明日动作(<=20min): 写 **是/否**：你的 RAG agent eval 是否测 Refine streaming token granularity（是/否/未知）。

- [fix(core): return empty results when similarity_top_k=0 in embedding utils（LlamaIndex PR #23021, created **2026-09-11**）](https://github.com/run-llama/llama_index/pull/23021)
  - 摘要: Fixes #22508。`similarity_top_k=0` 被 truthy check 当 `None`（unlimited），返回全部 embeddings 而非空列表。Fix 三函数 early exit + `is not None` 判断 + unit tests。
  - 为什么重要(对我): **评测 + RAG skill**——explicit zero-top-k 是 agent retrieval eval 的 boundary gold（disable retrieval path）；须测 `top_k=0` 返回空而非全量。
  - 明日动作(<=20min): 写 **是/否**：你的 RAG eval 是否测 `similarity_top_k=0` boundary（是/否/未知）。

- [fix(agentchat): retry MagenticOne ledger parsing when next_speaker is missing（AutoGen PR #7913, updated **2026-09-11**）](https://github.com/microsoft/autogen/pull/7913)
  - 摘要: MagenticOne progress-ledger JSON 缺 `next_speaker` 时，`required_keys` loop 设 `key_error=True` 意图 retry，但 loop 后仍无条件访问 `progress_ledger["next_speaker"]["answer"]` → KeyError 而非 retry。
  - 为什么重要(对我): **评测 + multi-agent**——orchestrator ledger parse retry path 是 multi-agent eval 的 malformed-JSON gold；须测 missing-field retry 而非 crash。
  - 明日动作(<=20min): 写 **是/否**：你的 multi-agent eval 是否测 orchestrator ledger missing-field retry（是/否/未知）。

- [[Bugfix][MRV2] Match fast-prefill padding to active LoRA batches（vLLM PR #56456, created **2026-09-11**）](https://github.com/vllm-project/vllm/pull/56456)
  - 摘要: fast-prefill + LoRA batch 中 main dispatch 用实际 active LoRA count，但 `FastPrefillHelper.prepare()` 固定 `num_active_loras=0` → 两 dispatch 选不同 graph mode/padding，logits metadata shape 与 input batch 不一致。
  - 为什么重要(对我): **评测 + skill 落地**——LoRA-aware serving eval 须标 adapter count 维度；fast-prefill path 须与 main dispatch padding 一致 gold。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 LoRA + fast-prefill serving（是/否/未知）。

- [Datasette 1.0a39 security releases — frontier model audit workflow（Simon Willison Blog, **2026-09-11**）](https://simonwillison.net/2026/Sep/11/)
  - 摘要: Simon + Alex 用 Claude Fable 5.1、GPT-5.6、GPT-6 Astra 对 Datasette 做 extensive security audit，近一周协作 fix；workflow 拆分——一人写 failing test、另一人 implement fix，确保两人 + 多 model agent 四眼审查；计划纳入全部开发流程。
  - 为什么重要(对我): **skill 落地 + 评测**——「test-first + dual-human + multi-model agent audit」是可复用 agent eval/skill 验收模板；直接挂 internal skill release gate。
  - 明日动作(<=20min): 写 **1 条** test-first dual-human audit 对你 skill eval pipeline 的接点（≤20 词）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix] Align vLLM YaRN with Transformers and stop re-scaling max_model_len（vLLM PR #56446, merged **2026-09-11**）](https://github.com/vllm-project/vllm/pull/56446)
  - 摘要: `_get_and_verify_max_len` 仅对 exact string `"yarn"` reset，vendor alias（`telechat3-yarn`、`deepseek_yarn`）导致已 scaled `max_position_embeddings` 再乘 `factor` → cos/sin cache 越界（例：TeleChat3 131K→32K，Sarvam 5.2M→131K）。Fix realign 至 `transformers.modeling_rope_utils._compute_yarn_parameters`。
  - 为什么重要(对我): **Talk ≥3 turns + RoPE/Attention**——YaRN factor chain 是 long-context attention eval 的 rope-derivation gold；须 pin upstream rule 而非 vendor alias 特殊 case。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 YaRN / long-context model serving（是/否/未知）。

- [[Bugfix][KV Offload] Prevent cache reuse across KV cache layouts（vLLM PR #55907, created **2026-09-08**, updated **2026-09-11**）](https://github.com/vllm-project/vllm/pull/55907)
  - 摘要: Fixes #55904。persistent KV offload namespace 不含 `kv_cache_layout`；不同 layout 共享 `root_dir` 时同一 prefix 映射同一 file 但 bytes 不可互换，size check 仍 pass → silent wrong output。Fix 将 `kv_cache_layout` 加入 `FileMapper.fields`。
  - 为什么重要(对我): **Talk ≥3 turns + KV/attention cache**——KV cache layout identity 是 disaggregated serving eval 的 cache-key gold；须测 cross-layout reuse reject 而非只测 hit rate。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 KV offload + multi-layout serving（是/否/未知）。

- [[V1][Core] Add public build_full_block_hash_chain helper for offline prefix-cache analysis（vLLM PR #48838, updated **2026-09-11**）](https://github.com/vllm-project/vllm/pull/48838)
  - 摘要: Motivation #47993 offline prefix-cache analyzer。社区 review 指出 out-of-tree tool 须 replicate V1 scheduler exact hash chain 否则 semantic drift。Fix 新增 public stable helper `build_full_block_hash_chain` 供 external analyzer 调用，避免 reimplement 私有 internals。
  - 为什么重要(对我): **Talk ≥3 turns + prefix cache / attention KV**——prefix cache hash chain 是 attention KV reuse eval 的 correctness oracle；external tool 须 bind 官方 helper 而非 copy hash logic。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 prefix-cache offline analysis（是/否/未知）。

## 3) 热门项目/可试个人项目

- [MasRama/agenteval — CI for AI agents with YAML golden suites（created **2026-07-22**, 0 stars **2026-09-11** GitHub API）](https://github.com/MasRama/agenteval)
  - 摘要: YAML golden suites + 5 reliability metrics + baseline compare + CI regression gate；deterministic-first scoring + optional LLM judge；LCS trajectory evidence；Streamlit dashboard；GitHub Actions composite action；支持 CrewAI/AutoGen/OpenAI Agents SDK/LangGraph adapters。
  - 为什么重要(对我): **skill 落地 + 评测**——「YAML golden + baseline regression + trajectory evidence」直接对齐 internal agent eval 脚手架；Failure Memory / adversarial variants 设计可参考。
  - 明日动作(<=20min): 读 README §「GitHub Actions」，写 **1 条** composite action vs inline eval 对你 CI gate 的启发（≤20 词）。
  - 估计复现成本: **低**（clone + 跑 smoke suite + 写 1 条接点，~20min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Measuring tactical intelligence targeting and conventional weapons capabilities of AI models（Anthropic Research, **2026-09-10**）](https://www.anthropic.com/research/intelligence-targeting-conventional-weapons-capabilities)
  - 机会信号: Frontier Red Team 发布 identity correlation / geolocation / GNC drone guidance 等 **simulated eval suites**；Mythos Preview 在 account linkage 接近 theoretical max，geolocation median error 37km 逼近 superhuman；强调 **eval-driven capability measurement + on-platform classifiers** → agent eval 方法论在 national-security 场景标准化，可脱敏借鉴「find-fix-eval + anti-cheat monitor」设计。
  - 风险信号: synthetic data 非 fully realistic；unrestricted search 可能 artificial ceiling；open-weights（Kimi K3）仍 show concerning capability；eval 与 real-world generalization gap。
  - 我可执行的一步(<=30min): 读 blog §「Models as targeters」中 anti-cheat monitor 设计，写 **2 条** eval harness anti-cheat 对你 agent eval 的接点（各 ≤15 词）。

- [Datasette security audit with frontier models — dual-human + test-first workflow（Simon Willison Blog, **2026-09-11**）](https://simonwillison.net/2026/Sep/11/)
  - 机会信号: 独立开发者用 frontier models（Fable 5.1 / GPT-5.6 / Astra）做 security audit 并 productize 为 release gate → **AI solo 可复用的「multi-model audit as a service」signature 切口**；dual-human split（test author vs fix author）降低 agent hallucination risk。
  - 风险信号: frontier model audit cost 高（多 model × 近一周）；subtle bug 仍可能漏；公开 mix public/private tables 场景 niche。
  - 我可执行的一步(<=30min): 草拟 **≤100 词**「agent skill release = test-first audit gate」主张 v0.1（不写客户名）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[2165. Unique 3-Digit Even Numbers](https://leetcode.com/problems/unique-3-digit-even-numbers/)（**2026-09-11 Daily Challenge**，verified via LeetCode GraphQL `activeDailyCodingChallengeQuestion`，Easy）：关键洞察——从 digits 枚举 3-digit even number 并 dedup（set/brute force）；与 agent eval「枚举 action space + uniqueness gold」同构。明日动作：手写 brute-force + set 版；写 **≤15 词**：action-space enumeration 对你 tool-call eval 意味着什么。

- **IELTS（≤1 条）**：[IELTS Reading Practice Tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/reading)：0910 已轮换 Listening，今日换 **Reading**（选 1 套 3 passages，限时 60min）。明日动作：完成 1 套 Reading；记录 **≥2 个**失误类型（T/F/NG 混淆 / 定位慢 / 同义替换 miss）；写 **≤15 词**你最弱的 Reading 维度。

## 6) 噪音过滤记录

- **fix(openrouter): pass retry_config=None when max_retries=0（#40398）**：provider retry config → 弱于 parallel tool call theme → 过滤。
- **fix(text-splitters): HTMLHeaderTextSplitter non-heading tags（#40400/#40402）**：text splitter edge → 过滤。
- **chore(model-profiles): refresh model profile data（#40399）**：deps/data → 过滤。
- **fix(openai): route gpt-6-astra to responses API（#40394）**：routing config → 过滤。
- **docs: add OrcaReplay to observability integrations（LlamaIndex #23024）**：docs → 过滤。
- **feat: add AntibrowWebReader（#23023）**：新 reader vendor → 过滤。
- **Revise README title（#23020）**：docs → 过滤。
- **0909–0910 已收录项**：LangGraph #8872–#8875/#8882、LangChain #40356/#40363、AutoGen #8215、vLLM #56257–#56265、openai/evals #1831、Anthropic alignment assessment Sep 9、mini-swe-harness 等 → semantic duplicate，不重复。
- **GPT-6 Astra / Claude Fable 5.1 发布帖（Sep 1–5）**：模型发布营销，无新 actionable engineering signal → 过滤。
- **OpenAI ChatGPT for Financial Services / Data agent（Sep 10）**：enterprise product launch → 过滤。
- **Anthropic Fermat FLT formalization（Sep 4）**：math agent 成就，弱于当前 Attention/eval focus → 过滤。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-11** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + blog ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **953**（`0910.md` §7: 943 + 本日 §1: 10）
- Theory: **433**（`0910.md` §7: 430 + 本日 §2: 3）
- Career/Business: **143**（`0910.md` §7: 141 + 本日 §4: 2）
- LC: **124**（`0910.md` §7: 123 + 本日 §5: 1）
- IELTS: **124**（`0910.md` §7: 123 + 本日 §5: 1）
