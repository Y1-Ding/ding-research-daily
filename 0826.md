# Daily Research - 2026-08-26 (21:30)

## 0) 今日3条结论

- **OpenAI 发布 skill 系统化 eval 指南**（[eval-skills](https://developers.openai.com/blog/eval-skills)，last-modified **2026-08-26**）：skill 迭代须走「prompt → trace(JSONL) → 确定性检查 → rubric 打分」闭环；`codex exec --json` 抓 `command_execution` 事件 + `--output-schema` 做风格 rubric。**Agent skill eval 不能靠 vibe，须像 E2E test 一样可回归。**
- **LangGraph MCP 暴露面默认 temporary run**（#8725, updated **2026-08-26**）：MCP tool call → `runs.wait(thread_id=None)` + `on_completion=delete` → `checkpointer=None` + thread 删除 → `interrupt()`/HITL 不可达、无 post-hoc trace。**MCP skill 落地 eval 须区分 Platform MCP 路径 vs 自建 durable thread。**
- **LangChain OpenAI 兼容端点丢弃 nested token details**（#39926/#39931, updated **2026-08-26**）：`_create_usage_metadata` 过滤非标准 `prompt_tokens_details`/`completion_tokens_details` 键 → DashScope/vLLM/DeepSeek 的 `text_tokens`、`accepted_prediction_tokens` 等丢失。**token 优化 + cost eval 须断言 provider-specific fields 是否保留。**

## 1) Agent工程化（high-signal）

- [Testing Agent Skills Systematically with Evals（OpenAI Blog, last-modified **2026-08-26**）](https://developers.openai.com/blog/eval-skills)
  - 摘要: OpenAI 开发者博客发布 skill eval 完整 playbook：先写可测量的 definition of done（outcome/process/style/efficiency 四类），用 `$skill-creator` 建 skill，手动触发暴露 hidden assumptions，再用 10–20 条 CSV prompt set 做回归；核心 harness 用 `codex exec --json --full-auto` 捕获 JSONL trace，对 `command_execution` 做确定性检查（如是否跑 `npm install`、是否创建 `package.json`），最后用 `--output-schema` + rubric schema 做 qualitative 风格评分；扩展方向含 command thrashing 计数、token budget 追踪、build smoke test。
  - 为什么重要(对我): **skill 落地 + 评测**——直接可复用到 Cursor skill / Harness skill 的 eval harness 设计；与你 map_prompt_test 的 collect→test→eval 链路同构。
  - 明日动作(<=20min): 写 **1 条** 你现有 skill 的「process goal」确定性检查（如「是否 invoke skill / 是否跑某命令」）。

- [fix(openai): preserve provider-specific token usage details in UsageMetadata（LangChain PR #39931, updated **2026-08-26**）](https://github.com/langchain-ai/langchain/pull/39931)
  - 摘要: Fixes #39926。`langchain-openai` 的 `_create_usage_metadata` 此前丢弃 OpenAI-compatible 端点返回的非标准 nested token 字段（如 `text_tokens`、`accepted_prediction_tokens`）；Fix 动态保留 `prompt_tokens_details`/`completion_tokens_details` 中所有 provider-specific 键到 `UsageMetadata.input_token_details`/`output_token_details`。
  - 为什么重要(对我): **token 优化 + 评测**——自托管 vLLM/DeepSeek agent cost dashboard 若依赖 LangChain usage_metadata，当前 main 会系统性 under-report 细粒度 token 分布。
  - 明日动作(<=20min): 写 **是/否**：你是否用 OpenAI-compatible 端点 + LangChain usage_metadata 做 cost eval（是/否/未知）。

- [fix(openai): correct reasoning_effort_levels for gpt-5 and gpt-5.1 profiles（LangChain PR #39936, updated **2026-08-26**）](https://github.com/langchain-ai/langchain/pull/39936)
  - 摘要: Fixes #39935。profile augmentations 给 gpt-5/gpt-5.1 系列广告了 `xhigh` reasoning effort，但 OpenAI 文档仅支持 minimal/low/medium/high；Fix 对齐官方 supported levels，避免 agent 配置无效 effort 后在 runtime 失败。
  - 为什么重要(对我): **token 优化 + 评测**——reasoning model agent eval 须在 config 层断言 effort level 合法性，否则 latency/cost 归因到错误参数。
  - 明日动作(<=20min): 写 **是/否**：你是否配置 `reasoning_effort`（是/否/未知）。

- [MCP-exposed assistant tool calls run as temporary runs so follow-up are impossible（LangGraph Issue #8725, updated **2026-08-26**）](https://github.com/langchain-ai/langgraph/issues/8725)
  - 摘要: LangGraph Platform MCP handler 用 `client.runs.wait(thread_id=None, ...)` 且不传 `on_completion="keep"` → run 标记 temporary → `checkpointer=None` 执行 → 完成后 thread 删除。后果：① `interrupt()` HITL 无法 park/resume；② 无 post-hoc trace/thread handle；③ 长 run 阻塞 MCP 连接。Reporter 给出 one-line fix：`on_completion="keep"`（cron scheduler 已有先例）。
  - 为什么重要(对我): **skill 落地 + 评测**——MCP-exposed agent skill eval 须单独测 durable vs temporary 路径；HITL gold 集在 MCP 面当前不可达。
  - 明日动作(<=20min): 写 **是/否**：你是否通过 LangGraph Platform MCP 暴露 agent（是/否/未知）。

- [checkpoint-sqlite: failed store batches can partially commit changes（LangGraph Issue #8590, updated **2026-08-26**）](https://github.com/langchain-ai/langgraph/issues/8590)
  - 摘要: `SqliteStore.batch()`/`AsyncSqliteStore.abatch()` 的 `_cursor()` 在 `finally` 无条件 `COMMIT` → 后续 op 抛异常时 earlier mutation（如 `GetOp(refresh_ttl=True)` 的 TTL refresh）仍被持久化。独立 repro 在 `langgraph-checkpoint-sqlite==3.1.1` 验证；PR #8592/#8605 仍 open/unmerged。
  - 为什么重要(对我): **skill 落地 + durable agent eval**——store batch 非原子 → agent state 可能 half-applied；L3 eval 须加「batch 中途 fail → 外部可见状态不变」探针。
  - 明日动作(<=20min): 写 **是/否**：你是否用 SqliteStore batch + TTL refresh（是/否/未知）。

- [Conformance detector counts an override-that-raises as an implementation（LangGraph Issue #8722, updated **2026-08-26**）](https://github.com/langchain-ai/langgraph/issues/8722)
  - 摘要: LangGraph conformance suite 的 detector 将「override 方法但 raise NotImplementedError」计为有效 implementation → `SqliteSaver` 在 store 相关 conformance 项得 NONE (0/3) 而非 NOT_APPLICABLE。含 minimal reproducible example。
  - 为什么重要(对我): **评测**——若你用 conformance suite 做 saver backend 选型 eval，当前 scorer 可能误报/漏报 backend 能力。
  - 明日动作(<=20min): 写 **是/否**：你是否跑 LangGraph conformance tests（是/否/未知）。

- [fix(core): avoid retrying failed function tools（LlamaIndex PR #22841, updated **2026-08-26**）](https://github.com/run-llama/llama_index/pull/22841)
  - 摘要: `call_tool`/`acall_tool` 将 positional call 的任何 exception 视为「应 retry with kwargs」信号 → 已执行 irreversible side effect 的 tool 可能被调用两次。Fix 在 invocation 前从 callable signature 判定 calling convention，仅对 keyword-only/positional-only 分别传参。
  - 为什么重要(对我): **skill 落地 + 评测**——function tool agent eval 须覆盖「side-effect tool + business exception」路径，否则 eval pass 但 production double-execute。
  - 明日动作(<=20min): 写 **是/否**：你 function tools 是否有 irreversible side effect（是/否/未知）。

- [fix(tools/mcp): correct streamable-http tuple unpack for mcp 2.0（LlamaIndex PR #22843, updated **2026-08-26**）](https://github.com/run-llama/llama_index/pull/22843)
  - 摘要: Fixes #22655。`mcp` 2.0.0 的 `streamable_http_client()` 从 3-tuple 改为 2-tuple `(read, write)`；`BasicMCPClient._run_session()` 仍 unpack `(read, write, _)` → 每条 streamable-HTTP 连接 `ValueError`。Fix 移除 trailing `_` + 回归测试。
  - 为什么重要(对我): **skill 落地**——MCP 2.0 skill agent 在 LlamaIndex 上当前 main 全断；升级 mcp 包后须回归 streamable-HTTP 路径。
  - 明日动作(<=20min): 写 **是/否**：你是否用 LlamaIndex BasicMCPClient + streamable HTTP（是/否/未知）。

- [fix(autogen-ext): include tool_choice in ChatCompletionCache hash key（AutoGen PR #8096, updated **2026-08-25**）](https://github.com/microsoft/autogen/pull/8096)
  - 摘要: Fixes #7968。`ChatCompletionCache` 的 SHA-256 key 含 messages/tools/json_output/extra_create_args 但**不含 `tool_choice`** → 仅 `tool_choice` 不同（`auto` vs `none`）的两次 call hash 相同 → 第二次 silent cache hit 返回第一次结果。
  - 为什么重要(对我): **评测 + token 优化**——agent eval 若交替测 tool-on/tool-off 路径且启用 completion cache，结果会被污染。
  - 明日动作(<=20min): 写 **是/否**：你是否用 AutoGen ChatCompletionCache（是/否/未知）。

- [feat(cli): support single-layer user builds（LangGraph PR #8724, updated **2026-08-26**）](https://github.com/langchain-ai/langgraph/pull/8724)
  - 摘要: Draft PR。`langgraph build --single-user-layer` 将 source copy + dep install + API restore + cleanup 合并为单个 bind-mounted RUN → K8s BuildKit benchmark 中 C3+gVisor median solve time 54.93s→44.76s（~10s）；压缩镜像大小基本不变。刻意 trade warm-cache 复用换 cold builder 速度。
  - 为什么重要(对我): **skill 落地**——LangGraph agent 部署 eval 若测 CI build latency，layer 策略会系统性影响 benchmark。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `langgraph build` 部署 agent（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[AttentionBackend][HPC-ops] update hpc rope norm to support stride kv cache（vLLM PR #53705, merged **2026-08-26**）](https://github.com/vllm-project/vllm/pull/53705)
  - 摘要: Fix hpc rope norm 不支持 stride KV cache 的 bug → 在非 contiguous KV layout 下 RoPE+norm 路径 silent wrong/crash。Merged Aug 26 after CI pass。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**——RoPE 施加在 KV cache 的 stride/layout 上；agent serving eval 须标注 KV cache layout（contiguous vs strided）+ backend 路径。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serve 带 stride KV cache 的模型（是/否/未知）。

- [fix(simple_kv_offload): align CPU offload pool size across PP/spec-decode workers（vLLM PR #52921, updated **2026-08-26**）](https://github.com/vllm-project/vllm/pull/52921)
  - 摘要: `SimpleCPUOffloadConnector` 在 PP（uneven layer splits）或 spec-decode（draft KV on last PP stage）下，scheduler 与 worker 对 `num_cpu_blocks` 估算不一致 → scheduler 分配更大 `cpu_block_id` → OOB DMA（MI355X TP8×PP2 上 `hipMemcpyBatchAsync` SIGSEGV）。Fix 端到端对齐 offload pool sizing。
  - 为什么重要(对我): **Talk ≥3 turns + KV/Attention**——spec decode 的 draft/target KV 分 stage 存放；PP+spec eval 须覆盖 CPU offload 路径的 block ID 一致性。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 PP + spec decode + CPU KV offload（是/否/未知）。

- [[Bugfix] Thread kv_transfer_params into engine for /inference/v1/generate（vLLM PR #42644, updated **2026-08-26**）](https://github.com/vllm-project/vllm/pull/42644)
  - 摘要: `ServingTokens.serve_tokens` 解析 request body 中的 connector transfer params 但未转发到 `engine_client.generate(...)` → connectors 从 `sampling_params.extra_args` 读值时拿到空 → P/D disagg 下 prefill 响应 `kv_transfer_params=None` → decode worker 本地 re-prefill 而非 pull KV cache。
  - 为什么重要(对我): **Talk ≥3 turns + KV transfer**——disaggregated agent serving eval 若测 TTFT/latency，silent param drop 会把 KV reuse 失败误归因到模型。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 vLLM P/D disaggregated serving（是/否/未知）。

- [Not All Attention Is Equal: A Quantitative Survey of the EEI Trade-off（arXiv:2608.15459, literature cutoff **2026-05-31**）](https://arxiv.org/abs/2608.15459)
  - 摘要: 2026 综述：从 Bahdanau alignment → Transformer → Vision adaptation → 21 种 efficient attention 方法，用 Efficiency–Expressiveness–Interpretability (EEI) 三维框架量化评分；覆盖 FlashAttention、GQA、Mamba、NSA/DashAttention (2026 preprint) 等；归纳 5 个 open problems（unified efficiency benchmarks、learned routing、length generalization、scalable mechanistic interpretability 等）。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**——EEI trade-off 是选择 agent serving backend（full attn vs linear vs hybrid）的理论锚点；可与 vLLM PR 中 KV/offload 实践对照。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent serving 当前用的 attention 类型（full/linear/hybrid/未知）。

## 3) 热门项目/可试个人项目

- [pbshgthm/arc-skill（GitHub, created **2026-08-19**）](https://github.com/pbshgthm/arc-skill)
  - 摘要: 129 行 SKILL.md + 4343 行 CLI tool，让 agent 玩 ARC-AGI-3（25 个未知规则游戏）。Claude Code + Opus 5 在 7,645 actions 完成 25/25 游戏、RHAE 100.00（ARC scorecard 验证）；核心约束：每次 action 前须 graded prediction（7,627 条预测中 443 miss）。
  - 为什么重要(对我): **skill 落地 + 评测**——「action 前 prediction + 事后 grading」是可复用的 agent eval 模式；可直接 A/B 你的 skill vs arc-skill 在同一 benchmark 子集。
  - 明日动作(<=20min): 写 **1 条** 你 agent eval 能否加「action 前 prediction gate」（≤15 词）。
  - 估计复现成本: **低**（clone + 选 1 个 ARC-AGI-3 game ID 试跑，~30min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [A quote from Paul Dix（Simon Willison Blog, **2026-08-26**）](https://simonwillison.net/2026/Aug/26/paul-dix/)
  - 机会信号: InfluxDB 作者 Paul Dix 指出 AI 写 100 万行代码后持续 refine，配合 verification oracle 可产出运行在数百万开发者机器上的可靠软件——**「verification system + proper direction」是 AI 交付复杂软件的新杠杆**。对个人 AI 公司/出海产品：build verification harness 比堆 feature 更差异化。
  - 风险信号: 有 oracle 的场景（如语言迁移、有 ground-truth 对照）不代表无 oracle 的 agent 产品也能同速交付；verification 本身的设计与维护成本被低估。
  - 我可执行的一步(<=30min): 写 **1 条** 你当前 agent 工作中哪块有天然 oracle 可建 verification loop（≤20 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[2904. Shortest and Lexicographically Smallest Beautiful String](https://leetcode.com/problems/shortest-and-lexicographically-smallest-beautiful-string/)（**Day 56, Aug 2026 Daily Challenge**, verified via LeetCode GraphQL **2026-08-26**；承接 0825 的 3718 Easy「缺失 k 倍数」，今日 **Medium**「最短且字典序最小的 beautiful 子串 = 恰好 k 个 '1' 的最小窗口 + 同长字典序比较」）：关键洞察——滑动窗口维护 ones 计数，同长时比较 substring 字典序；与 agent eval「最小覆盖窗口 + tie-break 规则」同构。明日动作：手写解法；写 **≤15 词**：「窗口 + tie-break」对你 eval rubric 平局处理意味着什么。

- **IELTS（≤1 条）**：[IELTS Listening Practice Tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/listening)：0825 已轮换 Speaking，今日换 **Listening**（选 Section 1–2 各练 1 段，~20min）。明日动作：完成 1 次 Listening 练习，记录 **≥2 个** 拼写/数字失误；写 **≤15 词** 你最弱的 Listening 环节。

## 6) 噪音过滤记录

- **fix(core): support '+' bullet in MarkdownListOutputParser（LangChain #39932）**：边缘 output parser 路径，与当前 skill/eval/token focus 弱相关 → 过滤。
- **LangGraph #8715 StreamTransformer error drop**：stream infra bug，非 eval focus → 过滤。
- **LangGraph #8726 OpenTelemetry OOM pin**：部署 infra，PR 已 auto-closed（external）→ 记入备选。
- **LlamaIndex #22842 StructuredLLMRerank async**：RAG rerank 优化，非当前 focus → 过滤。
- **LlamaIndex #22843 vs #22659**：0813 已收录 #22659 同主题 PR → 本日 #22843 为 mcp 2.0 新 breakage（不同 root cause），已收录 §1。
- **vLLM #53704 FlatLogprobs**：0825 已收录同 PR（merged **2026-08-26**）→ semantic duplicate，不重复。
- **vLLM #53122 DFlash quant mapping**：spec decode infra，与当前 focus 弱相关 → 过滤。
- **AutoGen #8100 FileSurfer base_path**：组件序列化 fix，非 eval focus → 过滤。
- **openai/evals**：无 **2026-08-26** repo 更新 → 不收录（OpenAI Blog eval-skills 已覆盖 eval 主题）。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-08-25~26** 原帖 URL → 不收录。
- **Anthropic multiagent-systems（Aug 13）**：高价值但非今日更新 → 不重复（0824 前后已覆盖同类主题）。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + OpenAI Blog + Simon Willison ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **787**（`0825.md` §7: 777 + 本日 §1: 10）
- Theory: **355**（`0825.md` §7: 351 + 本日 §2: 4）
- Career/Business: **126**（`0825.md` §7: 125 + 本日 §4: 1）
- LC: **108**（`0825.md` §7: 107 + 本日 §5: 1）
- IELTS: **108**（`0825.md` §7: 107 + 本日 §5: 1）
