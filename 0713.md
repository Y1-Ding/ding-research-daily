# Daily Research - 2026-07-13 (21:30)

## 0) 今日3条结论

- **LangGraph #8332 揭示 checkpointer context manager 会在 yield 后仍持有 pipeline**：`from_conn_string` 的 pipeline 跨用户 async 代码（如 LLM 调用）保持打开 → 阻塞其他查询或 SSL 超时；skill/eval harness 若用 `async with AsyncPostgresSaver.from_conn_string()` 包裹长时 agent run，checkpoint 会在「看似正常」时随机断链。
- **vLLM #48485 提出 Queue-Informed LRU：prefix cache 驱逐应看 waiting queue 短期需求**：纯 LRU 可能 evict 队首请求即将复用的 prefix → 刚 admit 又重算 KV、TTFT 飙升；token 优化若依赖 APC，应把「调度器 waiting queue 前 N 个请求的 cached prefix」纳入驱逐优先级，而非只看 last-access 时间。
- **arXiv 2607.07953 给出 linear attention 选型地图**：350M/15B token 对照下 KDA+Muon 最低 val loss、纯 GDN+AdamW 最高吞吐；跨层路由 CLVR（转发 write value）有效、CLER（转发 write error）无效 → multi-turn agent 选 backbone 时「loss vs 吞吐 vs 跨层记忆」需分开测，不能只看 context window 数字。

## 1) Agent工程化（high-signal）

- [fix: avoid holding pipeline across async yield in AsyncPostgresSaver (closes #5675)（LangGraph PR #8332 opened **2026-07-13**）](https://github.com/langchain-ai/langgraph/pull/8332)
  - 摘要: `from_conn_string` 创建 `AsyncPipeline` 后 yield checkpointer，但 pipeline 在用户代码执行期间仍保持打开；用户代码（如 LLM 调用）运行时 pipeline 阻塞其他查询或超时 → SSL connection closed。Fix：从 `from_conn_string` 移除跨 yield 的 `async with conn.pipeline()`，改在 checkpoint 操作内部按需创建 pipeline。
  - 为什么重要(对我): **skill 落地 + eval**——与 0712 #8328（autocommit 冲突）同属 Postgres checkpointer 生产路径；长时 parallel eval 用 context manager 包裹 agent 时，pipeline 跨 yield = trace/checkpoint 随机断裂；harness 应显式测「context manager 内嵌 LLM 调用」场景。
  - 明日动作(<=20min): 写 **是/否**：你 LangGraph eval 是否用 `from_conn_string` context manager 包裹含 LLM 调用的代码（是/否）。

- [[RFC]: Waiting-Queue-Informed LRU for Prefix Cache Eviction（vLLM issue #48485 + PR #48488 opened **2026-07-13**）](https://github.com/vllm-project/vllm/issues/48485)
  - 摘要: APC 当前按纯 LRU 驱逐 free cached KV blocks， unaware of waiting queue 需求。内存压力下可能 evict 队首请求即将复用的 prefix → admit 后重算、cache hit 下降、TTFT 上升。提案 Queue-Informed LRU：分配需 evict cached block 时，inspect waiting queue 前 `max_num_running_reqs` 个请求，read-only 查找其 local contiguous cached prefix，优先 evict 不在 retained set 的 blocks；retained block 不 pin，容量不足时仍按原 LRU 回退。Lazy trigger：仅当普通 LRU 需 evict cached data 时才查 queue。
  - 为什么重要(对我): **token 优化**——长 skill prompt prefix caching 是核心杠杆；与 0712 #48402（block_size 静默失效）互补：前者是「caching 开着但驱逐策略盲」；collect→test 的 token A/B 应在高并发 waiting queue 场景下测 hit rate，而非仅单请求重复。
  - 明日动作(<=20min): 写 **是/否**：你 agent serving 是否在并发 waiting queue 下测过 prefix cache hit rate（是/否/未测）。

- [fix(anthropic): prevent bind_tools from mutating caller-provided tool_choice（LangChain PR #38819 opened **2026-07-13**）](https://github.com/langchain-ai/langchain/pull/38819)
  - 摘要: Fixes #38779。`ChatAnthropic.bind_tools()` 在 `parallel_tool_calls=False` 时原地修改 caller 的 `tool_choice` dict，添加 `"disable_parallel_tool_use": True`；因存的是引用而非 copy。Fix：`tool_choice.copy()` + 回归测试验证 caller dict 不被修改。
  - 为什么重要(对我): **skill 落地**——eval harness 常复用同一 `tool_choice` dict 跑多轮对比；原地 mutation 使 A/B 配置污染、trace 出现「第二轮莫名 disable parallel tools」；与 0712 #38809（schema 幽灵字段）同属「绑定层副作用 ≠ 声明语义」。
  - 明日动作(<=20min): 写 **是/否**：你 agent harness 是否复用同一 tool_choice/config dict 跑多轮 eval（是/否）。

- [fix(node-parser): TokenTextSplitter can emit chunks exceeding chunk_size（LlamaIndex PR #22339 opened **2026-07-12**）](https://github.com/run-llama/llama_index/pull/22339)
  - 摘要: `_merge()` 用 additive token count (`cur_len`) 决定何时关闭 chunk，但 emit 的是 `"".join(cur_chunk).strip()`；tiktoken 下 leading space 常与后词融合为 1 token（`" foo"`），strip 后 bare word 可能 2 token → 实际 emit chunk 可超 `chunk_size` 上限。
  - 为什么重要(对我): **token 优化 + RAG**——chunk 边界直接影响 embedding 成本与 retrieval 精度；若 eval 假设「chunk ≤ chunk_size tokens」而实际超标，token budget 与 recall 指标会系统性偏差；collect 阶段应快照真实 chunk token 分布。
  - 明日动作(<=20min): 写 **是/否**：你 RAG pipeline 是否验证过实际 chunk token 数 ≤ 配置上限（是/否/未测）。

- [Pace: A Proxy for Agentic Capability Evaluation（arXiv **2607.02032**, July 2026）](https://arxiv.org/abs/2607.02032)
  - 摘要: PACE 从 19 个非 agentic benchmark 中选 ~100 个 atomic instances，用 bootstrap 回归映射到 4 个 agentic target（GAIA、SWE-Bench Verified/Multimodal、SWT-Bench）。14 models 上 LOOCV MAE <4%、Spearman >0.81、pairwise ranking ~85%，成本 <1% 全量 agent eval。开源 [neulab/pace](https://github.com/neulab/pace) + [neulab/pace-bench](https://huggingface.co/datasets/neulab/pace-bench)。
  - 为什么重要(对我): **eval 杠杆**——map_prompt_test 可在 collect 阶段用 proxy 筛 model/routing/tool-policy 候选，全量 agent eval 仅对 top-K；但 proxy 不能替代 tool failure、long-horizon、环境差异测试——应分层：PACE triage → 全量 gold 集门禁。
  - 明日动作(<=20min): 读 PACE README 前 30 行；写 **≤15 词**：你现有 eval 是否有 cheap triage 层（有/无）。

- [Testing Agent Skills Systematically with Evals（OpenAI Developers Blog）](https://developers.openai.com/blog/eval-skills)
  - 摘要: Codex skill eval 模式：定义 success（outcome/process/style/efficiency）→ 小 prompt set（10–20 条，含 should_trigger=false 负例）→ `codex exec --json` 捕获 JSONL trace → 对 `command_execution` 事件做 deterministic checks → `--output-schema` rubric 评 style。扩展：command count 测 thrashing、`usage.input_tokens` 测 prompt bloat、`git status` 测 repo 洁净度。
  - 为什么重要(对我): **skill 落地 + eval**——直接可映射你的 skill harness：trigger 可靠性（name/description）、过程检查（tool 调用序列）、token budget 回归；与 0712 AgentLens 互补：AgentLens 评 trajectory 可读性，此文评 skill 行为可脚本化。
  - 明日动作(<=20min): 写 **≤15 词**：你 skill eval 是否区分 trigger 测试 vs 执行测试（有/无）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Linear Attention Architectures: Mechanisms, Trade-offs, and Cross-Layer Routing（arXiv **2607.07953**, July 2026）](https://arxiv.org/abs/2607.07953)
  - 摘要: 统一记号对比 softmax attention 与 DeltaNet/GDN/KDA/GDN-2 四种 linear recurrent memory；350M/15B token 实验：KDA+Muon 最低 val loss，纯 GDN+AdamW 最高吞吐，hybrid stack 降 loss 但牺牲吞吐。提出跨层路由 CLER（转发下层 delta write error）无效；CLVR（转发 write value 到 aligned hidden stream）对 DeltaNet/GDN 小幅降 loss，保持 linear-time。实现开源 [tommasocerruti/linear-attention-architectures](https://github.com/tommasocerruti/linear-attention-architectures)。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) agent 长对话若用 linear backbone，KDA vs GDN 的 decay/erase/write 粒度如何影响 multi-turn recall？(2) CLVR 跨层传 write value 对 ≥3 turn tool trace 的「层间证据传递」意味着什么？(3) 你的 long-context eval 是否分 loss 与吞吐两轴报告？
  - 明日动作(<=20min): 写 **≤15 词**：你 agent context 是否用 linear/GDN/KDA backbone（是/否/未知）。

- [[Bug]: Deferred block-free path loses per-group eviction ordering for hybrid KV cache configs（vLLM issue #48489 opened **2026-07-13**）](https://github.com/vllm-project/vllm/issues/48489)
  - 摘要: hybrid KV cache（attention + Mamba 等）配置下，deferred block-free 路径丢失 per-group eviction ordering；不同 KV group 的 block 驱逐顺序错乱，可导致 multi-turn serving 中 attention 与 recurrent state 的 KV 块生命周期不一致。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：hybrid 模型 serving 层 KV 管理 ≠ 纯 attention；(1) 你的 ≥3 turn eval 是否在 hybrid backbone 上跑过？(2) 与 0712 #48404（sparse indexer block table 静默截断）是否同族「KV 元数据≠实际布局」？(3) eval 是否报告 hybrid KV 配置？
  - 明日动作(<=20min): 写 **是/否**：你 agent serving 是否用 hybrid KV cache 模型（是/否/未知）。

- [fix: don't mutate caller's chat_history in ChatSummaryMemoryBuffer.get（LlamaIndex PR #22345 opened **2026-07-13**）](https://github.com/run-llama/llama_index/pull/22345)
  - 摘要: `ChatSummaryMemoryBuffer.get()` 是 read-style 调用，但通过 `get_all()` 返回 store list 引用并传入 `_split_messages_summary_or_full_text`，原地修改 caller 传入的 `chat_history` list；导致 multi-turn memory 在「只读 get」时 silently 改变状态。
  - 为什么重要(对我): **Talk ≥3 turns + Agent memory**：≥3 turn 对话中 memory.get() 副作用 = eval replay 非确定性；与 0712 LangChain bind_tools mutation 同族「框架读操作写穿 caller 状态」；memory harness 应测 get() 前后 caller list 不变。
  - 明日动作(<=20min): 写 **是/否**：你 agent memory 的 get/read 路径是否可能 mutate caller state（是/否/未知）。

## 3) 热门项目/可试个人项目

- [neulab/pace（GitHub, PACE 官方实现）](https://github.com/neulab/pace)
  - 摘要: PACE proxy eval 框架：从 19 个非 agentic benchmark 自动选 ~100 instances，bootstrap 回归预测 GAIA/SWE-Bench 等 agentic 分数；配套 [neulab/pace-bench](https://huggingface.co/datasets/neulab/pace-bench) HuggingFace 数据集（每 target ~100 条带 weight 的 source instances）。
  - 为什么重要(对我): **eval 杠杆**——可直接接入 map_prompt_test：collect 用 pace-bench 做 model 初筛，test 仅对 top 候选跑全量 gold；复用 CMU 选好的 atomic capability 子集，避免自己从零建 proxy。
  - 明日动作(<=20min): `pip install` 或 clone；读 README；写 **≤15 词** 你最该映射的 1 个 agentic target（GAIA/SWE/其他）。
  - 估计复现成本: **低**（pip + 现有 model API；无训练）。

- [tommasocerruti/linear-attention-architectures（GitHub, arXiv 2607.07953 配套）](https://github.com/tommasocerruti/linear-attention-architectures)
  - 摘要: Megatron 可训练实现：KDA、GDN-2、CLVR/CLER 跨层路由变体；350M–3B DeltaNet 实验复现；统一 recurrent-memory 记号便于对比 decay/erase/write 机制。
  - 为什么重要(对我): **原理→实验接点**——若需验证 linear attention 对 multi-turn recall 的影响，可直接 fork 改 eval hook；比从零写 GDN 省数周。
  - 明日动作(<=20min): 浏览 repo README + 列 1 条你想测的 multi-turn 指标（≤12 词）。
  - 估计复现成本: **高**（需 GPU 集群 + Megatron 环境；350M 最小也需多卡）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Evals for AI Agents: How Product Builders Get the Most Out of Every New Model（Anthropic Webinar, **2026-07-14**）](https://www.anthropic.com/webinars/evals-for-ai-agents-how-product-builders-get-the-most-out-of-every-new-model)
  - 机会信号: Anthropic Applied AI 面向 product builder 的 eval 专场——强调 multi-step agent（tool call + retrieve + 逐步产出）不能用 single-turn eval 判断新模型是否值得切换；从 production failures 建首 eval set、快速决策 model adoption → 出海/一人公司 signature 可打在「垂域 agent eval 方法论 + 可复用 harness」，而非追 frontier 训练。
  - 风险信号: 无 production failure 采集则无法建高质量 eval set；webinar 偏 product 视角，法律/车控等封闭垂域需脱敏改造 failure case。
  - 我可执行的一步(<=30min): 列 **3 条** 你最近 agent eval 中真实 production/near-prod failure（各 ≤12 词），标哪条可脱敏成 eval case。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 210 Course Schedule II](https://leetcode.com/problems/course-schedule-ii/)：有向图拓扑排序返回合法课程顺序；Kahn BFS（in-degree 队列）或 DFS 三色标记+后序反转；与 0712 #802（安全状态）同属 DAG 族。明日动作：手画 Example 1 的 prerequisite 边方向；写 Kahn 核心 25 行（≤25min）；写 **≤15 词**：与 agent tool 依赖图「合法执行顺序」的类比。

- **IELTS（≤1 条）**：[Differences between IELTS 6 and 7 for productive skills（IELTS.org）](https://ielts.org/news-and-insights/differences-between-ielts-6-and-7-for-productive-skills)：Band 7 要求 extended turns、nuanced opinions、handle follow-up questions；Part 3 测 analyse/discuss/speculate 抽象议题。明日动作：选 1 个 technology Part 3 题（如 AI agents impact on work）录音 2min，自检是否含 opinion + reason + example + speculation（≤20min）；写 **≤15 词** 你最弱 criterion（Fluency/LR/GRA/Pronunciation 选 1）。

## 6) 噪音过滤记录

- **LangGraph #8326 range/PurePath、#8328 autocommit pipeline**：`0712.md` 已收录 → 不重复。
- **LangChain #38809 tool schema kwargs**：`0712.md` 已收录 → 不重复。
- **vLLM #48402 prefix caching inert、#48404 sparse indexer、#48407 short sequence**：`0712.md` 已收录 → 不重复。
- **AgentLens arXiv 2607.06624、HOLA 2607.02303、SDM 2607.07386**：`0712.md` 已收录 → 不重复。
- **Anthropic Claude Code expertise research**：`0712.md` 已收录 → 不重复。
- **LangGraph #8330 typo fix、#8331 NamedBarrierValue annotations**：chore/docs → 丢弃。
- **LangChain #38821 model-profiles refresh、#38822 attestations**：infra chore → 丢弃。
- **LlamaIndex #22343–#22346 reader/embedding fixes**：infra bug，弱于今日 token/memory focus → 丢弃。
- **AutoGen #7951 Agent RCE security wave**：高信号但偏 security advisory 综述，无你当前框架的直接 patch → 记入噪音（可在 agent sandbox 设计时回溯）。
- **vLLM #48483–#48490 cudagraph/MoE/mypy**：kernel/infra → 不展开。
- **Simon Willison DSPy eval（Jul 2）**：早于本周重点且非当日 → 不重复收录。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-13** 原帖 URL → 不收录。
- **@gefei55 / @ianneo_ai 职业帖**：无法验证当日原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv + OpenAI/Anthropic 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **515**（`0712.md` §7: 509 + 本日 §1: 6）
- Theory: **212**（`0712.md` §7: 209 + 本日 §2: 3）
- Career/Business: **92**（`0712.md` §7: 91 + 本日 §4: 1）
- LC: **74**（`0712.md` §7: 73 + 本日 §5: 1）
- IELTS: **74**（`0712.md` §7: 73 + 本日 §5: 1）
