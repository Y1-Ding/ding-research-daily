# Daily Research - 2026-07-20 (21:30)

## 0) 今日3条结论

- **sync eval harness 有 silent pass bug**：LangGraph `SyncRunsClient.wait(raise_error=True)` 对失败 run 仍返回 `__error__` envelope 而不抛异常（#8383/#8385）——并行 eval worker 若混用 sync/async SDK，会把「运行失败」记成「正常返回」；harness 应统一 client 并断言无 `__error__` key。
- **skill eval 需要「按 skill 生成任务」框架**：Tessl 2606.17819 在 500 个真实 skill 上合成 ~1000 任务，测 instruction-following + goal-completion；Opus 4.8（88.0）与 GLM 5.1（85.0）领先，而 Kimi K2.6 等仅 57–60——你的 L2/L3 不能只测「有没有 skill」，要测「模型是否 follow skill 指令」。
- **RAG rerank 可静默丢文档**：LlamaIndex `default_parse_choice_select_answer_fn` 在 relevance 解析失败时 `answer_nums` 与 `answer_relevances` 错位（#22404）——LLM judge 输出 `Relevance: high` 时后续 doc 分数全串线；collect 层应对 choice-select 输出加 schema 校验。

## 1) Agent工程化（high-signal）

- [fix(sdk-py): honor raise_error in sync wait; case-insensitive reserved headers（LangGraph PR #8385, updated **2026-07-20**）](https://github.com/langchain-ai/langgraph/pull/8385)
  - 摘要: (1) `SyncRunsClient.wait(..., raise_error=True)` 仅把 `raise_error` 放进请求 body 并直接返回含 `__error__` 的响应，async 客户端则 client-side raise；(2) reserved `x-api-key` 检查仍大小写敏感，`X-API-Key` 可与自动 API key 共存。Fix：mirror async 的 `__error__` raise 逻辑；header 名 case-fold 后比对。
  - 为什么重要(对我): **评测 + skill 落地**——eval harness 用 sync wait 跑 LangGraph Cloud 时，失败 run 被当成功 JSON 入库 = 假阳性；与 0719 #8379 同属 SDK 集成陷阱，CI 应 parametrized 测 sync/async 双路径。
  - 明日动作(<=20min): 写 **是/否**：你 LangGraph eval 用 sync 还是 async `wait`（sync/async/混用）。

- [SyncRunsClient.wait ignores raise_error for failed runs（LangGraph Issue #8383, updated **2026-07-20**）](https://github.com/langchain-ai/langgraph/issues/8383)
  - 摘要: 复现：`wait(raise_error=True)` 收到 `{"__error__": {"error": "RuntimeError", "message": "boom"}}` 仍 print 返回而非 raise。服务端 `/runs/wait` schema 无 `raise_error` 字段——该选项本应纯 client-side，sync 实现却误发给 server。
  - 为什么重要(对我): **评测可靠性**——multi-turn skill eval 依赖 wait 判定终态；此 bug 让「agent 崩溃」与「agent 完成」在 collect 层不可分。
  - 明日动作(<=20min): grep 你代码库 `raise_error` + `SyncRunsClient`；写 **是/否**：是否存在（是/否）。

- [fix: ensure pipeline context is properly managed in AsyncPostgresSaver（LangGraph PR #8386, updated **2026-07-20**）](https://github.com/langchain-ai/langgraph/pull/8386)
  - 摘要: `pipeline=True` 时 `from_conn_string` 退出 pipeline 上下文后 `setup()` 等操作触发 `SSL connection has been closed unexpectedly`。Fix：`setup()` 在 pipeline 上下文内执行；退出前显式 `pipe.sync()` 刷缓冲。
  - 为什么重要(对我): **skill 落地 + eval**——与 0718 #8372、0719 #8381 构成完整 pipeline 生命周期修复链；multi-turn checkpoint 回放若在 setup 阶段间歇失败，应查此路径。
  - 明日动作(<=20min): 写 **是/否**：你 Postgres checkpoint 是否在 `from_conn_string` 外单独调 `setup()`（是/否/未知）。

- [feat(langchain): add `DeduplicateToolCallsMiddleware` to collapse parallel tool calls（LangChain PR #38969, updated **2026-07-19**）](https://github.com/langchain-ai/langchain/pull/38969)
  - 摘要: LLM 常在单响应中生成重复 parallel tool call（同名同参），导致冗余 side effect 与额外 token/延迟。新增 `DeduplicateToolCallsMiddleware` 在 model response 层折叠重复 tool call，确保每个唯一调用只执行一次。
  - 为什么重要(对我): **skill 落地 + token 优化**——skill agent 并行调同一检索/写文件工具时，dedup 是零成本降本杠杆；可并入 skill 模板 middleware 栈，eval 应测「模型重复 emit 同 tool」场景。
  - 明日动作(<=20min): 写 1 条你域内「模型重复调用同一 tool」的真实样例（≤10 行）。

- [perf(core): lazily import transformers in get_tokenizer（LangChain PR #38970, updated **2026-07-19**）](https://github.com/langchain-ai/langchain/pull/38970)
  - 摘要: `BaseChatModel` 模块级 import `transformers` 导致任何 `langchain_core` 导入都拖入巨型依赖。Fix：将 import 移入 `get_tokenizer()` 内部，仅在实际计 token 时加载。
  - 为什么重要(对我): **token 优化 + eval harness 启动**——CI 并行跑 10+ eval worker 时，冷启动 import 成本可累积；skill CLI/短脚本受益明显。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval worker 冷启动是否 >5s（是/否/未知）。

- [Keep answer_nums and answer_relevances aligned in choice-select parsing（LlamaIndex PR #22404, updated **2026-07-20**）](https://github.com/run-llama/llama_index/pull/22404)
  - 摘要: `default_parse_choice_select_answer_fn` 先 append `answer_nums` 再 parse relevance；当 LLM 输出 `Relevance: high` 等不可解析值时 `continue` 跳过 relevance 但 nums 已追加 → 两列表错位，`LLMRerank._postprocess_nodes` 的 `zip` 静默错配分数并丢最后一个 node。
  - 为什么重要(对我): **评测 + RAG**——choice-select rerank 是 L2 检索 eval 常见路径；无报错但排名错误 = eval 最难排查的 silent failure；应对 LLM reranker 输出加 JSON/schema 或 post-parse 长度断言。
  - 明日动作(<=20min): 写 **是/否**：你 RAG pipeline 是否用 choice-select rerank（是/否/未知）。

- [fix: honor max_tokens in VllmServer OpenAI-like mode（LlamaIndex PR #22405, updated **2026-07-20**）](https://github.com/run-llama/llama_index/pull/22405)
  - 摘要: `VllmServer(..., openai_like=True)` 构建 payload 时忽略运行时 `max_tokens` override，仍用构造时 `max_new_tokens`。Fix：OpenAI-compatible 模式优先 honor `max_tokens`，保留 `max_new_tokens` 兼容。
  - 为什么重要(对我): **token 优化 + eval**——judge 请求常需短 `max_tokens` 而主 agent 需长输出；忽略 override = bench 成本膨胀且与 prod 行为不一致。
  - 明日动作(<=20min): 写 **是/否**：你是否通过 LlamaIndex `VllmServer` 做 agent serving（是/否）。

- [A Framework for Evaluating Agentic Skills at Scale（arXiv **2606.17819**）](https://arxiv.org/abs/2606.17819)
  - 摘要: 给定任意 skill，框架自动合成 realistic task（不泄露 skill 细节）+ instruction-following / goal-completion rubric；在 500 个真实开源 skill 上生成 ~1000 任务，评 19 种 agent–model 配置。发现：模型间 instruction-following 差距巨大（Opus 4.8: 88.0 vs Kimi K2.6: ~57–60）；skill 显著改变行为，是小模型追平大模型的杠杆。数据集：[tesslio/task-evals-for-skills](https://huggingface.co/datasets/tesslio/task-evals-for-skills)。
  - 为什么重要(对我): **skill 落地 + 评测**——与 0719 SkillCoach（过程四维）互补：本框架解决「新 skill 怎么生成 eval task」；你的 playbook 可先 fork 其 task 合成 + rubric 模板，再叠 SkillCoach 过程分。
  - 明日动作(<=20min): 浏览 HF 数据集 README；写 1 条你可映射的「skill → auto task」规则（≤10 行）。

## 2) 原理深化（Attention/Transformer/Agent）

- [DSpark: Confidence-Scheduled Speculative Decoding with Semi-Autoregressive Generation（arXiv **2607.05147**, submitted **2026-07-07**）](https://arxiv.org/abs/2607.05147)
  - 摘要: 并行 drafter 生成长 draft 但 acceptance 快速衰减；DSpark 用 semi-autoregressive 架构（parallel backbone + lightweight sequential module）建模 block 内 token 依赖，并以 confidence-scheduled verification 按 prefix survival 概率动态裁剪验证长度。在 DeepSeek-V4 serving 生产流量下，匹配吞吐时 per-user 生成加速 **60–85%**，缓解高并发下 verification waste。
  - 为什么重要(对我): **Talk ≥3 turns + Attention + token 优化**：(1) multi-turn agent 的 spec decode 瓶颈在 verification 而非 draft；(2) confidence scheduling 可按 turn 风险分配 verify 预算；(3) 与 0718 Attention Drift 同属 serving 栈，eval bench 应记录「并发度 × spec depth × acceptance length」。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent serving 是否启用 speculative decoding（是/否/未知）。

- [[ROCm][MLA] Enable AITER sparse MLA decode + MQA-only prefill for GLM DSA（vLLM PR #49188, updated **2026-07-20**）](https://github.com/vllm-project/vllm/pull/49188)
  - 摘要: GLM DSA（DeepSeek Sparse Attention）在 ROCm AITER sparse MLA 路径上缺 decode/prefill metadata（`num_decodes`/`prefill` 等）且错误尝试 dense-MHA prefill → `AttributeError`/`NotImplementedError`。Fix：metadata 补全 + `supports_mha_prefill=False` 时 prefill 走 top-k MQA 路径。在 MI350 TP=8 上 gsm8k 5-shot：GLM-5.1/5.2 NVFP4 非-MTP 与 MTP 均 ~0.95 exact_match。
  - 为什么重要(对我): **Attention + token 优化**：sparse MLA 是长上下文 agent 的 attention 算子层；部署栈应记录「GPU 代际 → sparse backend → prefill/decode 分裂」回退链，MTP 与 sparse attention 需联合回归。
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 是否用 sparse MLA 类模型（是/否/未知）。

- [[Bugfix] Fix MiniMax M3 index_topk kernel for non-power-of-2 num_idx_heads（vLLM PR #49199, updated **2026-07-20**）](https://github.com/vllm-project/vllm/pull/49199)
  - 摘要: MiniMax M3 `index_topk` Triton kernel 在 `num_idx_heads` 非 2 的幂时编译/执行失败：误用 `tl.math.next_power_of_2`（应为 `triton.next_power_of_2`），且 `q_mask` 未同时 bound `h_offsets`/`q_offsets` 致 OOB。Fix 覆盖 CUDA 与 ROCm 路径；回归测试 `num_idx_heads=6`。
  - 为什么重要(对我): **Attention 机制**：indexer head 数是 sparse attention 路由的核心超参，非幂次配置是常见部署选择；attention kernel correctness bug = 静默错误 top-k 索引 → 长上下文 agent 召回退化。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否覆盖非标准 head 数配置（是/否/未知）。

## 3) 热门项目/可试个人项目

- [aws-samples/sample-agent-skill-eval（GitHub, created **2026-03-15**）](https://github.com/aws-samples/sample-agent-skill-eval)
  - 摘要: agentskills.io 规范的 skill 审计框架：四维评分——**Safety**（静态分析 secrets/injection）、**Quality**（with-skill vs without-skill）、**Reliability**（trigger precision 相关/无关 query）、**Cost Efficiency**（Pareto 分类质量-成本）；含 regression 对比版本 baseline 与 lifecycle 追踪。
  - 为什么重要(对我): **skill 落地 + 评测杠杆**——比 0719 `agent-skills-eval` 多 safety/trigger/cost 维度；可取其 Pareto 分类进 eval playbook 的「skill 是否值得加载」门禁。
  - 明日动作(<=20min): 读 README 的 `functional` + `trigger` 子命令；对 1 个现有 skill 跑 `audit`；记录 safety 分。
  - 估计复现成本: **低**（单 skill audit，≤30min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Kimi K3, and what we can still learn from the pelican benchmark（Simon Willison Blog, **2026-07-16**）](https://simonwillison.net/2026/Jul/16/kimi-k3/)
  - 机会信号: Moonshot Kimi K3（2.8T 参数，$3/$15 per M tokens）在 Artificial Analysis 长程知识工作 Elo 1547；Simon 实测单条 pelican prompt 消耗 **13,241 reasoning tokens**（总成本 25¢）——说明「agentic 能力」评测应看 **$/task + tool reliability**，而非 pelican 类创意基准；harness/评测/编排是出海 remote 的稀缺 signature。
  - 风险信号: K3 目前仅 max reasoning effort、隐藏 ~85 token system prompt；pelican 与 agent tool-calling 能力相关性已断裂（Simon 明确声明）。
  - 我可执行的一步(<=30min): 写 **≤8 行** 你工作中 1 个任务的「$/successful-run」粗算（含 reasoning + tool 轮次），替代 pelican 式创意测试。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 735 Asteroid Collision](https://leetcode.com/problems/asteroid-collision/)（**Day 20, Jul 2026, Medium**）：栈模拟相向碰撞，小者爆炸、等大双爆。明日动作：手写栈解法 15 行；写 **≤15 词**：agent middleware 栈式冲突消解与 asteroid collision 的类比。

- **IELTS（≤1 条）**：[IELTS Ready — British Council 官方免费备考（含 AI Mini Mock）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-ready)：50 分钟 Mini Mock + AI 估分。明日动作：完成 **Writing Task 1 或 Task 2 一节 Mini Mock**；写 **≤15 词** 错因类型（结构/词汇/语法 选 1）。

## 6) 噪音过滤记录

- **LangGraph #8381 pipeline sync、#8380 Counter/OrderedDict、#8362 trace_inputs、#8379 x-api-key**：`0719.md` 已收录 → 不重复（#8385/#8386 为同日延伸）。
- **LangChain #38964 tracer 并发、vLLM #47861 MTP cache、AutoGen #7969 tool_choice cache、SkillCoach 2607.01874、AHR 2607.13425、Legible 2607.08946**：`0717–0719.md` 已收录 → 不重复。
- **darkrishabh/agent-skills-eval**：`0719.md` §3 已收录 → 今日 §3 改用 aws-samples 四维审计。
- **Speculate with Memory 2607.12236**：`0718.md` 已收录 → 不重复。
- **Anthropic Demystifying evals（2026-01-09）**：高价值但非当日新鲜信号 → 记入待观察（与 2606.17819 方法论重叠）。
- **vLLM #49185 DiffKV MLA、#49194 NCCL MoE、#49195 HTTP 500**：重要但偏 infra/模型特定，今日 §2 已收 #49188/#49199 → 待观察。
- **openai/evals #1694 iss_intervention**：有趣但样本/文档不完整，且偏 niche metric → 待观察（今日 §1 已满 8 条）。
- **LangChain #38968/#38966 mustache falsy**：模板边缘 case → 待观察。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-20** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Jul 20 + arXiv 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **566**（`0719.md` §7: 558 + 本日 §1: 8）
- Theory: **233**（`0719.md` §7: 230 + 本日 §2: 3）
- Career/Business: **99**（`0719.md` §7: 98 + 本日 §4: 1）
- LC: **81**（`0719.md` §7: 80 + 本日 §5: 1）
- IELTS: **81**（`0719.md` §7: 80 + 本日 §5: 1）
