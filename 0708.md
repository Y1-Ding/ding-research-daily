# Daily Research - 2026-07-08 (21:30)

## 0) 今日3条结论

- **LangGraph #8299（updated 2026-07-08）在 DeltaChannel 写失败时停止 checkpoint 持久化**：`finally` 路径不再在 failed write 后仍落盘 → 与 0707 #8290 fresh-thread snapshot 同属 state 完整性族；eval 应断言「写失败 = 无新 checkpoint」而非仅测 end-state。
- **vLLM #47728 merged 2026-07-08：async scheduling 下 KV block 按 settled basis 释放**：`num_computed_tokens - num_in_flight_tokens` 防止 attention window 内 block 被提前回收 → multi-turn tool-call grading 在 async/PP 路径可能读到 null KV；serving eval 需分层报告 scheduling 模式。
- **Legible-by-Construction（arXiv 2607.04319, July 2026）首次端到端可读 Attention+FFN**：value 经 sigmoid 约束为 fuzzy membership detector，44–62% 通道成为 crisp selective feature → 与 SSRP「attention latch」形成对照：可读 detector vs 不可见 latch，multi-turn eval 可测哪类 head 在 goal pivot 时仍 fire。

## 1) Agent工程化（high-signal）

- [fix: stop checkpoints after failed delta writes（LangGraph PR #8299 updated **2026-07-08**）](https://github.com/langchain-ai/langgraph/pull/8299)
  - 摘要: Fixes #8234。sync/async Pregel loop 在 DeltaChannel `put_writes` future 失败时，原 `finally` 路径仍允许 checkpoint 持久化。Fix：先传播 write failure，再决定是否 persist；`durability="sync"` 下 failed delta write 不再产生 checkpoint。7 个 regression test 通过（含 async write ordering）。
  - 为什么重要(对我): **skill 落地 + eval**——0707 #8290 修 fresh-thread 首写丢失；本 PR 修「写失败仍 checkpoint」的 silent corruption，collect→test 与 prod 在 error path 上应对齐。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否在 tool/state 写失败路径断言 checkpoint 不变（是/否/仅 happy path）。

- [fix: avoid AsyncPipeline with SSL connections to prevent OperationalError（LangGraph PR #8301 updated **2026-07-08**）](https://github.com/langchain-ai/langgraph/pull/8301)
  - 摘要: Fixes #5675（延续 0707 #8295 线程）。`AsyncPostgresSaver` + `pipeline=True` + SSL（Supabase 等云 PG 常见）时，psycopg `AsyncPipeline` 不支持 SSL → `OperationalError: SSL connection has been closed unexpectedly`。Fix：检测 conn string 是否启用 SSL（非 `sslmode=disable`），若 SSL 且 `pipeline=True` 则 warn 并回退 non-pipeline 模式。
  - 为什么重要(对我): **skill 落地**——0707 #8295 修 pipeline 生命周期；本 PR 是 SSL 场景的 complementary path，parallel eval 用云 PG checkpointer 时两条 fix 都需知晓。
  - 明日动作(<=20min): 写 **是/否**：你 Postgres checkpointer 是否走 SSL + pipeline（是/否/仅 local）。

- [fix: allow custom tool_choice for ToolStrategy compatibility（LangChain PR #38711 updated **2026-07-08**）](https://github.com/langchain-ai/langchain/pull/38711)
  - 摘要: Fixes #38705。`create_agent` + `ToolStrategy` 原硬编码 `tool_choice="any"`，与 dashscope 等仅支持 `"none"`/`"auto"` 的 API 不兼容。Fix：`create_agent` 新增 `tool_choice: str | None = None`，传入 `ModelRequest`；未指定时仍默认 `"any"`，向后兼容。
  - 为什么重要(对我): **skill 落地 + token**——tool_choice 影响每轮是否强制 tool call，进而影响 token 预算与 eval 可复现性；多 provider skill 栈需显式配置而非隐式 `"any"`。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否显式设 `tool_choice`（是/否/未知）。

- [fix(langchain): clear stale structured_response on retry（LangChain PR #38704 updated **2026-07-07**）](https://github.com/langchain-ai/langchain/pull/38704)
  - 摘要: Fixes #36957。`create_agent` + checkpointer + `response_format` 时，同 thread 重试可能返回上一轮 parsed `structured_response`（如上一轮 `Hi`、本轮应 `Bye` 却停在 `Hi`）。根因：checkpointed state + routing 以 key 存在即 success。Fix：`_build_commands` 在 `response_format` 活跃且无 fresh parsed response 时将 stale 值清为 `None`；四条 model node 路径（sync/async × middleware）均已接线；routing 改为 `state.get("structured_response") is not None`。
  - 为什么重要(对我): **eval**——structured output skill 的 L2 eval 若用同 thread 多轮，stale parse 会产生假阳性；与 0707 streaming tool call 完整性同属「state 污染」门禁族。
  - 明日动作(<=20min): 写 **是/否**：你 structured output eval 是否测同 thread 重试（是/否/仅新 thread）。

- [fix(google-genai): don't send default params in gemini genconfig（LlamaIndex PR #22270 merged **2026-07-08**）](https://github.com/run-llama/llama_index/pull/22270)
  - 摘要: Gemini 3-series 在 genconfig 中收到 `temperature` 等默认参数可能抛错。Fix：移除默认参数注入；未设 temp 时 API 默认 1.0，向后兼容。版本 bump `llama-index-llms-google-genai` 0.9.6。
  - 为什么重要(对我): **token 优化**——无效/被拒参数导致 retry 循环是隐性 token 税；skill 落地应对齐「仅发送显式覆盖项」原则。
  - 明日动作(<=20min): 写 **是/否**：你 LLM wrapper 是否 strip 默认 genconfig 参数（是/否/未知）。

- [examples: recall memory before RAG + memory-aware retriever pattern（LlamaIndex PR #22274 / #22276 updated **2026-07-08**）](https://github.com/run-llama/llama_index/pull/22274)
  - 摘要: 同日落地 memory skill 模式：(1) #22274 notebook——先 recall Memory context 再 document retrieval，两者合并进最终 prompt；(2) #22276——retriever 在 delegate 给 `VectorIndexRetriever` 前用 recalled Memory  enrich query。与 #22271–#22273 文档批（external persistent memory adapter、memory vs vector store 澄清）形成完整 skill 落地链。
  - 为什么重要(对我): **skill 落地**——typed memory slot（L3–L5）与 RAG 的编排顺序是 collect→test 可测接口；可直接映射 AgenticSTS 的 bounded-memory contract。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 是否区分 memory recall vs RAG retrieve（有/无/混用）。

- [fix(grpc): eliminate request_id collision in host pending responses（AutoGen PR #7637 updated **2026-07-08**）](https://github.com/microsoft/autogen/pull/7637)
  - 摘要: Fixes #7016。多 `GrpcWorkerAgentRuntime` 向同一 target runtime 发 RPC 时，各 runtime 的 `request_id` 均从 `"1"` 递增 → host `_pending_responses[target][request_id]` 碰撞 → `KeyError`。Fix：host 转发前用 `uuid4()` 替换 request_id 作 pending key，响应返回时恢复 original id。含 cross-runtime relay→inner agent 拓扑 regression test。
  - 为什么重要(对我): **skill 落地 + eval**——multi-agent skill 跨 runtime 编排时，request_id 碰撞是 rare-but-catastrophic；distributed eval 需覆盖 relay 拓扑。
  - 明日动作(<=20min): 写 **是/否**：你 multi-agent 是否跨 gRPC runtime（是/否/仅 in-process）。

- [Think Before You Grid-Search: Floor-First Triage for LLM Serving（arXiv **2607.05876**, July 2026）](https://arxiv.org/abs/2607.05876)
  - 摘要: **Floor First** 方法论 + zero-dependency floor calculator + **agent skill（SKILL.md）**：decode step 建模为五维资源向量（HBM/FLOPs/network bytes/messages/KV capacity），先算 analytical floor 再 reconcile benchmark，residual 超阈值才开 profiler。论文 calibration 由 coding agent 在 production cluster 上按 skill 协议执行。Composable——新 attention variant 仅声明一个 module。
  - 为什么重要(对我): **token 优化 + skill 落地**——与 0705–0707 vLLM serving 议题衔接；agent 优化 loop 的「先 floor 再 benchmark」可作 eval harness 的 cost gate 模板。
  - 明日动作(<=20min): 读 abstract §1；写 **≤15 词** 你 serving 优化是否先算 token/KV floor（有/无）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Legible-by-Construction: Attention and End-to-End Transformers（arXiv **2607.04319**, July 2026）](https://arxiv.org/abs/2607.04319)
  - 摘要: 将 FFN legible 设计（fuzzy set intersection/difference + self-forgetting quantifier）扩展到 Attention：value 经 sigmoid 约束为 $v=\sigma(W_v x)\in[0,1]$ fuzzy membership detector；Boolean variant 做 within-token intersection/negation。关键设计：**约束 detect（bounded value），free readout（output projection 不绑 vocabulary）**。125M 参数五设计：44–62% value channels 成为 crisp selective detector，legibility 随 depth 上升（最深层 70–95%）；与 legible FFN 耦合的首个端到端 legible LM 在 LAMBADA/BLiMP 达 parity。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) crisp detector 在 deep layer 是否对应「早期 constraint latch」的可读版本？(2) 与 SSRP attention latch 是互补（可读 vs 不可见）还是同一机制？(3) eval 能否用 selective-head ablation 测 mid-conversation pivot？
  - 明日动作(<=20min): 写 **≤15 词**：你 multi-turn eval 是否测 head-level 行为（有/无/仅 E2E）。

- [[Bugfix][V1] Free out-of-window blocks on the processed-token basis under async scheduling（vLLM PR #47728 merged **2026-07-08**）](https://github.com/vllm-project/vllm/pull/47728)
  - 摘要: async scheduling（现默认）或 PP 下 scheduler 领先 GPU，`remove_skipped_blocks` 用 optimistic `num_computed_tokens` 作 free boundary → (1) #47282：in-flight step attention window 内 block 被提前回收，KV load 覆盖；(2) spec rejection 后 boundary 回退致 attention 读 null KV。Fix：跟踪 `num_in_flight_tokens`，在 **settled basis** `num_computed_tokens - num_in_flight_tokens` 上释放 block。Invariant：block 仅当对所有 unprocessed step 的 attention window 外且无 rollback 需求时才归还 pool。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) null KV 是否扭曲 ≥3 turn tool-call 轨迹 grading？(2) 与 0706 #47194 hybrid+prefix cache 污染是否同族？(3) eval 是否分层报告 async vs sync scheduling？
  - 明日动作(<=20min): 写 **是/否**：你 serving 是否开 async scheduling（是/否/仅 sync）。

- [AgenticSTS: A Bounded-Memory Testbed for Long-Horizon LLM Agents（arXiv **2607.02255**, submitted **2026-07-02**）](https://arxiv.org/abs/2607.02255)
  - 摘要: 将 long-horizon agent memory 形式化为 **bounded contract**：每步决策由 typed retrieval 组装 fresh user message（L1 协议 / L2 schema / L3 rules / L4 episodic / L5 skills），无 raw cross-decision transcript。在 Slay the Spire 2（A0 难度，公开 benchmark 五配置 0 win，human 16%）上 298 trajectories：no-scaffold 3/10 win，启用 L5 skills 6/10（Fisher p≈0.37，directional）。释放代码 + 数据集 + frozen memory snapshots。
  - 为什么重要(对我): **Talk ≥3 turns + eval**：(1) typed slot ablation 是否比「截断 history」更可测？(2) L5 skill layer 增益是否映射你 skill 落地 ROI？(3) 与 LlamaIndex #22274 memory-before-RAG 是否同构？
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否做 memory layer ablation（有/无/仅 E2E）。

## 3) 热门项目/可试个人项目

- [AgenticSTS — Bounded-Memory Long-Horizon Agent Testbed（GitHub AlayaLab/AgenticSTS）](https://github.com/AlayaLab/AgenticSTS)
  - 摘要: arXiv 2607.02255 配套开源：Slay the Spire 2 上实现 L1–L5 typed memory contract，298 completed trajectories + condition tags + frozen snapshots + analysis scripts。HuggingFace 数据集 [ShandaAI/AgenticSTS-trajectories](https://huggingface.co/datasets/ShandaAI/AgenticSTS-trajectories)。
  - 为什么重要(对我): **eval 杠杆**——可直接复用 slot ablation 方法论到 map_prompt_test；比从零建 long-horizon bench 成本低。
  - 明日动作(<=20min): clone repo；读 README 中 L1–L5 slot 定义；写 **≤15 词** 哪条 slot 最像你的 skill 层。
  - 估计复现成本: **中**（需游戏环境 + API key；无需自训模型）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Understand to participate（Simon Willison Blog → Geoffrey Litt @ AIE **2026-07-02**）](https://simonwillison.net/2026/Jul/2/understand-to-participate/)
  - 机会信号: **责任半径升级**——Geoffrey Litt 框架：与 coding agent 协作时需「理解到足以参与」的深度，否则 cognitive debt 累积、无法继续 steer。与 0707 Simon subagent 分工（judgment 留主 loop）形成互补：一人公司不是「全 delegate」，而是「理解边界内的 delegate」。
  - 风险信号: AIE 300+ talks 信息过载；「理解」标准主观，易变成过度精读拖慢交付。
  - 我可执行的一步(<=30min): 写 **3 条**：你当前 agent 项目里「必须自己理解才能验收」的模块（各 ≤10 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 236 Lowest Common Ancestor of a Binary Tree](https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-tree/)：后序遍历或 parent map，$O(n)$ 找两节点最近公共祖先。明日动作：手写递归版（≤20min）；写 **≤15 词**：与 agent 多分支轨迹「汇合点」的类比。

- **IELTS（≤1 条）**：[IELTS Reading 2026: Band 7+ Question-Type Playbook（XMocks, June/July window）](https://blog.xmocks.ai/ielts-reading-2026-the-band-7-question-type-playbook-for-the-june-and-july-test-windows/)：July 窗口建议——弱项 question type 隔离 drill（T/F/NG + Matching Headings 优先）+ 20min/passage pacing。明日动作：标 2 个弱项 type 各做 5 题 untimed（≤20min）；写 **≤15 词** 弱项（T/F/NG/headings/inference 选 1）。

## 6) 噪音过滤记录

- **LangGraph #8290/#8295/#8293/#8294 / LangChain #38694–#38698 / LlamaIndex #22256/#22258**：`0707.md` 已收录 → 不重复。
- **SSRP（2604.24512）/ Sparse Failure Alerting（2606.05414）/ vLLM #45963**：`0707.md` 已收录 → 不重复。
- **Tool Attention（2604.21816）/ Agentic DFS（2606.00183）**：`0703.md`/`0701.md` 已收录 → 不重复。
- **DSPy Datasette Agent / llm-coding-agent / subagent delegation**：`0707.md`/`0705.md` 已收录 → 不重复。
- **Better Models: Worse Tools（Armin Jul 4）**：`0705.md` 已收录 → 不重复。
- **LangChain #35412 empty HumanMessage whitespace**：edge case fix，与今日 focus 弱 → 丢弃。
- **LangChain #38710 o-series token counting / #38712 model-profiles refresh**：infra，非 agent 语义 → 丢弃。
- **AutoGen #7926 get_running_loop**：API hygiene，信号弱 → 丢弃。
- **openai/evals #1690 CompletionTokensDetail TypeError**：bugfix，非 eval 方法论 → 丢弃。
- **LlamaIndex #22266 VoyageEmbedding retry / #22269 bedrock bearer**：embedding infra，弱于 memory skill 批 → 不重复展开。
- **vLLM #45560 Harmony strict tool call**：open draft，无 merge → 观望，本日不单列。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-08** 原帖 URL → 不收录。
- **@gefei55 / @ianneo_ai 职业帖**：第三方聚合无法验证当日原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv + Simon Willison 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **483**（`0707.md` §7: 475 + 本日 §1: 8）
- Theory: **197**（`0707.md` §7: 194 + 本日 §2: 3）
- Career/Business: **87**（`0707.md` §7: 86 + 本日 §4: 1）
- LC: **69**（`0707.md` §7: 68 + 本日 §5: 1）
- IELTS: **69**（`0707.md` §7: 68 + 本日 §5: 1）
