# Daily Research - 2026-06-07 (21:30)

> 采集口径：优先 **X_WHITELIST / YOUTUBE_WHITELIST / GITHUB_WHITELIST / BLOG_WHITELIST / CAREER_BUSINESS_WHITELIST**；评分 `relevance(40)+actionability(25)+credibility(20)+freshness(15)`，正文主段 **≥70**（职业池 **≥65** 且具可执行信号）。与 `0606.md` 及更早文件做 **URL 去重** + **语义去重**。本日 **`[OUTSIDE_WHITELIST]` 0 条** → 主链 **0%（≤20%）**。  
> **自动化提示（给编排器）**：Cron 触发时区若仍为 UTC，请将等价目标对齐 **Asia/Shanghai 每日 21:30**；失败 **重试 1 次**；连续 **2** 个日历日失败再发告警（本仓库文件不承载告警状态机）。

## 0) 今日3条结论

- **LangGraph SDK 与 `websockets 16` 的依赖上限冲突，会阻塞 v3 WebSocket 流式客户端升级**：**2026-06-07** [Issue #8021](https://github.com/langchain-ai/langgraph/issues/8021) 报告 `langgraph-sdk` 约束 **`websockets>=14,<16`**，而用户环境已装 **`websockets==16.0`**（**2026-01-10** 发布）；社区在 issue 内给出最小修复：**放宽至 `<17` 并锁 `websockets==16.0`**，本地 **`491 passed`**。对你：若走 **RemoteGraph / sdk-py v3 websocket stream**，升级前需 **pin websockets<16** 或 **跟踪官方 PR**。
- **vLLM 原生 KV offload + MTP 推测解码在长上下文下会 `cuMemcpyBatchAsync` 段错误**：**2026-06-07** [PR #44784](https://github.com/vllm-project/vllm/pull/44784) 根因是 **`OffloadingConnectorScheduler` 误将 EAGLE/MTP draft-attention 组纳入 load/store**，draft 组 **无稳定 block hash** → **`num_gpu_blocks` 膨胀至 ~105k** → **GPU block 越界**；修复：**`is_eagle_group` 跳过调度** + **DMA 前 bounds check**。对你：**agent 长会话 + speculative decoding + KV tiering** 三件套同开时，应把 **draft 组与 full-attn KV 缓存** 视为 **不同物理资源池**。
- **LangGraph 节点缓存键碰撞（#8009）修复 PR 再度因流程关闭，漏洞仍 Open**：**#8019**（**2026-06-06** 关闭，**draft + missing-issue-link**，**未合并**）提案在 **`default_cache_key` 冻结 byte-like 输入时纳入 dtype/shape 等语义元数据**；**#8009** 至 **2026-06-07** 仍 **Open**。与 `0606.md` 的 **#8012/#7845 修复 PR 关闭** 并读：**外部贡献修复线** 与 **生产 harness** 需并行维护。

## 1) Agent工程化（high-signal）

- [langgraph sdk-py to support websockets 16（Issue #8021，**Opened 2026-06-07**）](https://github.com/langchain-ai/langgraph/issues/8021)
  - 摘要: **`pyproject.toml` 限制 `websockets<16`**；报告者在 **Python 3.14.5 + langgraph_sdk 0.3.15 + websockets 16.0** 环境触发不兼容。评论者 **@fengjikui** 提议改为 **`websockets>=14,<17`** 并刷新 **`uv.lock`**，**`make test` 491 passed**；因外部依赖变更需 maintainer 指派后开 PR。
  - 为什么重要(对我): **sdk-py 0.4.x** 已合入 **websocket stream transports**（见 `0606` 前序 release 笔记）；依赖上限不跟进会 **卡住** 你在 **自托管 LangGraph Server** 上的 **低延迟双向流** 升级。
  - 明日动作(<=20min): 记录你当前 **`websockets` 版本**（`pip show websockets` 1 行）；若 **≥16** 且用 **langgraph-sdk**，写 **≤15 词** 临时 pin 策略（是/否/不适用）。

- [[Fix] Prevent cuMemcpyBatchAsync segfault with MTP and KV offloading（vLLM PR #44784，**Opened 2026-06-07**）](https://github.com/vllm-project/vllm/pull/44784)
  - 摘要: **`--kv-offloading-backend native` + MTP speculative decoding + 长上下文** → **`swap_blocks_batch` CUDA segfault**；根因 **draft-attention 组被纳入 offload 调度**；修复 **跳过 eagle/MTP 组** 并在 **`gpu_worker.py` 加越界 Python 异常**（Closes **#44780**）。
  - 为什么重要(对我): **多工具 agent** 常同时需要 **推测解码降延迟** 与 **KV 分层降成本**；该 bug 属于 **「异构 attention 组共享块表」** 类集成错误，CI 难捕、线上硬崩。
  - 明日动作(<=20min): 若你用 **vLLM v1 KV connector**，查配置是否 **同时启用 MTP/EAGLE 与 native offload**（是/否/未知）；若是，标 **≤15 词** 降级方案（关其一/等 #44784）。

- [default_cache_key collides distinct inputs（Issue #8009，**2026-06-07** 仍 Open；PR #8019 关闭未合并）](https://github.com/langchain-ai/langgraph/issues/8009)
  - 摘要: **`numpy` 数组 / `PIL.Image` 等 **相同 `tobytes()`、不同 dtype/shape/palette** 的 keyword 入参 → **缓存键碰撞** → **错误命中缓存结果**。**#8019** 提案 **freeze 时纳入语义元数据** + **回归测试**，**2026-06-06** 因 **`missing-issue-link` 自动关闭**。
  - 为什么重要(对我): **视觉/RAG agent** 常把 **图像张量** 作为 tool 入参；缓存键若只看 **字节流**，eval 与生产会出现 **偶发、难复现的「正确输入→错误输出」**。
  - 明日动作(<=20min): 为你 **1 个** 带缓存的 LangGraph 节点，列 **是否传入 ndarray/PIL/bytes-like**（是/否）；若是，写 **≤20 词** 临时规避（关缓存/加 salt key/自研 key）。

- [Discussion: deterministic compiled subflows for predictable tool sequences（Issue #7855，**2026-06-07** 活跃）](https://github.com/langchain-ai/langgraph/issues/7855)
  - 摘要: 提案将 **「计划已知后的工具链」** 从 **逐步 LLM 调用** 拆为 **可重放、确定性的 compiled subflow**，以降低 **token 成本** 与 **非确定性漂移**；**2026-06-07** 讨论延伸至 **进入子流前的路由/状态新鲜度**（与 **WisePick 能力评分层** 等外部方案对话）。
  - 为什么重要(对我): 这是 **Agent 工程** 里 **plan-once / execute-many** 的显式化——适合 **固定 ETL、审批链、合规检查序列**；也提示 **纯图内 deterministic 边** 不够，还需 **入口守卫**。
  - 明日动作(<=20min): 为你当前 **1 个** agent 流程，标 **≥3 步连续工具调用中无需再推理的段落**（有/无）；若有，写 **≤20 词** 是否可抽成 **无 LLM 子图**。

- [langchain-perplexity==1.3.2 发布（GitHub Release，**Published 2026-06-05**）](https://github.com/langchain-ai/langchain/releases/tag/langchain-perplexity%3D%3D1.3.2)
  - 摘要: **#37911** 修复 **`ToolMessage` 与 `AIMessage.tool_calls` 序列化**；面向 **Perplexity 作 search/RAG 工具链** 的 **多轮 tool 消息** 往返。
  - 为什么重要(对我): **搜索增强 agent** 若用 **langchain-perplexity**，序列化缺口会导致 **checkpoint / 远程 API** 丢 **tool_calls 结构** → **下一轮模型看不到工具边界**。
  - 明日动作(<=20min): 若你用 **ChatPerplexity + tools**，记录 **`langchain-perplexity` 版本**；若 **<1.3.2**，列 **1 个** 需回归的 **tool round-trip** 用例（≤15 词）。

- [Security: OWASP Agent Memory Guard（microsoft/autogen Issue #7671，**2026-06-07** 更新）](https://github.com/microsoft/autogen/issues/7671)
  - 摘要: **[OWASP Agent Memory Guard](https://github.com/OWASP/www-project-agent-memory-guard)**（`pip install agent-memory-guard`）提供 **memory write/read 扫描**、**语义异常检测**、**block/sanitize/alert**；**v0.3.0** 新增 **`amg scan` CLI、`amg serve` REST、DistilBERT 可选检测、SARIF GitHub Action**；作者给出 **AutoGen `install_guard(agent)` 一行 hook**（[issue #35](https://github.com/OWASP/www-project-agent-memory-guard/issues/35)）。
  - 为什么重要(对我): **多 agent 共享 memory** 时 **memory poisoning** 是 **RAG/agent 栈** 的 **横向传播面**；与 `0606.md` **沙箱/lockdown** 互补——即使 **执行隔离**，**污染记忆** 仍可在 **后续回合** 改变行为。
  - 明日动作(<=20min): 为你 **1 个** 多 agent 系统，写 **≤25 词**：memory 是否 **跨 agent 共享**（是/否）及 **是否校验写入**（有/无）。

- [Building self-improving tax agents with Codex（OpenAI Blog，**2026-05-27**）](https://openai.com/index/building-self-improving-tax-agents-with-codex/)
  - 摘要: **Thrive/Crete** 与 OpenAI 共建 **Tax AI**：**从业者纠错 → 结构化 trace → 分组 finding → 边界化 eval → Codex 调查/改码/回归**；**6 周内 75% 字段正确率从 ~25% → 86%**；强调 **三支柱**：贴近从业者、生产留证、Codex 驱动改进环（含 **scoped worktree + read-only trace** 目录结构示例）。
  - 为什么重要(对我): 这是 **可复制的 agent eval 产品化模板**——比「再加 prompt」更可落地的是 **把 correction 变成 FIND-xxx 任务包**；对你做 **垂直 agent** 可直接借鉴 **field-level diff → eval yaml**。
  - 明日动作(<=20min): 为你 **1 个** agent，写 **≤30 词**：生产纠错是否 **结构化落盘**（是/否/部分）及 **缺哪一环**（trace/grouping/eval）。

## 2) 原理深化（Attention/Transformer/Agent）

- [确定性子流 vs 逐步 LLM 推理的边界（Issue #7855，**2026-06-07**）](https://github.com/langchain-ai/langgraph/issues/7855)
  - 摘要: 问题陈述：**可预测工具序列** 若每步都经 **LLM**，带来 **不必要调用、成本、漂移**；社区回应区分 **执行布局（ChainWeaver 类）** 与 **进入路径前的能力/状态评分（WisePick 类）**——后者回答 **「凭何相信此刻仍应走该子流」**。
  - 为什么重要(对我): **三回合追问**：(1) 你的 **plan** 是 **一次性** 还是 **每步重规划**？(2) **子流入口** 有无 **置信度/弃权**？(3) **状态过期** 时谁 **失效子流**？
  - 明日动作(<=20min): 画 **≤4 节点**：`intent` → `route score` → `compiled subflow` / `LLM agent` → `abort?`（每节点 ≤8 词）。

- [Deterministic checkpoint resume contract across crash boundaries（Issue #6818，**2026-06-06** 讨论更新）](https://github.com/langchain-ai/langgraph/issues/6818)
  - 摘要: 需求：**崩溃/重启后 resume** 应与 **不间断运行** 在 **终端状态与步骤结果** 上 **确定性一致**；**2026-06-06** 评论追问契约是 **全局 outcome parity** 还是 **「每步仍源自同一授权 checkpoint 上下文」** 的更强保证。
  - 为什么重要(对我): **Agent 耐久性** 不仅是 **能恢复**，而是 **恢复后语义等价**——否则 **HITL / 计费 / 审计** 会对不上。
  - 明日动作(<=20min): 为你 **1 个** checkpointed agent，写 **≤25 词**：kill 进程后 **resume** 是否测过 **逐步对齐**（是/否/未知）。

- [MTP draft-attention 组与 full-attention KV offload 的资源语义（vLLM #44784，**2026-06-07**）](https://github.com/vllm-project/vllm/pull/44784)
  - 摘要: **EAGLE/MTP draft 组** **无稳定 block hash**、**gpu_block_size 极小**；误入 **offload 调度** 会把 **逻辑块数** 算爆。**修复本质**：**attention 组类型** 决定 **是否参与 KV 块迁移**——类比 **多头里不同 head 角色不可混用同一 KV 槽位表**。
  - 为什么重要(对我): **推测解码** 引入 **辅助 attention 计算图**；与 **主模型 KV** 混排是 **架构级错误**，不是调参问题。
  - 明日动作(<=20min): 用中文写 **≤30 词**：解释 **draft 组为何不能有与 full 相同的 block hash 假设**（自定比喻）。

## 3) 热门项目/可试个人项目

- [OWASP Agent Memory Guard v0.3.0（PyPI / GitHub，经 AutoGen #7671，**2026-06-07**）](https://pypi.org/project/agent-memory-guard/)
  - 摘要: **`pip install agent-memory-guard`**；**`amg scan`** 扫 memory 文件、**`amg serve`** 独立服务、可选 **DistilBERT**、**SARIF CI**；AutoGen 侧 **`install_guard(agent)`** 保护 **GroupChat 内部 dispatch**。
  - 为什么重要(对我): **低门槛** 验证 **memory poisoning 检测** 是否值得进你的 **agent 安全基线**。
  - 明日动作(<=20min): `pip install agent-memory-guard && amg scan --help` 截图或粘贴 **help 首屏**（1 次）。
  - 估计复现成本: **低**（无 GPU；ML 检测可选）。

- [docs: deterministic fast-path routing cookbook — SynaptoRoute（LangGraph PR #8018，**Closed 2026-06-06**，教程仍可参考）](https://github.com/langchain-ai/langgraph/pull/8018)
  - 摘要: 提案 notebook **`deterministic-routing-fallback.ipynb`**：**高置信本地路由绕过 LLM**、**弃权 fail-open 回退**、**置信度写入 `AIMessage.response_metadata` 供 LangSmith**；因 **missing-issue-link** 关闭，但 **三条保证** 与 **docs #4321** 反馈对齐。
  - 为什么重要(对我): 个人项目可 **1 小时内** 复刻 **「路由器=优化层而非 agent 替代」** 的最小对照实验。
  - 明日动作(<=20min): Fork 思路写 **≤20 词**：你的 **1 个** 高置信 intent 是什么（自定）。
  - 估计复现成本: **低–中**（需 LangGraph + 本地 router 库；PR 未合并需自写 notebook）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [The practical way I improved signup → paid by 5.8x（Indie Hackers / Bazzly，**CAREER_BUSINESS_WHITELIST**）](https://www.indiehackers.com/post/the-practical-way-i-improved-signup-paid-by-5-8x-R0apPw58xNNCOtra7lDd)
  - 机会信号: 作者将 **signup→paid 从 2.5% 提到 14.6%**，核心杠杆是 **activation** 而非流量：**删掉价值前可选步骤**、**5 分钟内强制小赢**、**邮件拉回中断 setup**；与 **AI 预填 onboarding**（同作者 **$0→$1k MRR** 帖）一致——**solo AI 产品** 应先 **修漏斗再买量**。
  - 风险信号: 帖末推广 **Bazzly（Reddit 获客）** → **渠道建议需甄别**；**5.8x** 为 **单产品叙事**，未必可迁移至 **B2B agent**。
  - 我可执行的一步(<=30min): 为你 **1 个** side project，用中文写 **≤40 词**：用户 **注册后 5 分钟内** 能否获得 **可陈述的一次小赢**（是/否 + 缺什么）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：LeetCode Explore [`Graph`](https://leetcode.com/explore/learn/card/graph/) 中 **Course Schedule**（拓扑排序判环）：**有向依赖链** 能否线性执行 ↔ **#7855** 的 **compiled tool sequence** 需 **无环 + 顺序确定**。明日动作：用中文写 **≤30 词**：为你 **1 个** 工具链列 **是否存在循环依赖**（有/无/未知）。

- **IELTS（≤1 条）**：浏览 [IELTS 官方 — Test format](https://ielts.org/take-a-test/test-format) 中 **Listening 四部分结构**（日常对话→学术独白）。明日动作：用中文写 **≤35 词**：把 **「先抓场景再抓细节」** 映射为你 **下周 1 次** 英文 **技术播客精听**（自定来源）。

## 6) 噪音过滤记录

- **`micropython-wasm`**、**`#8012`**、**`#7845`**、**`#44733`**、**`#7487`**、**`FallbackLLM #21894`**：与 `0606.md` **URL/语义去重**（本日仅 **跟进 #8009/#8019 状态** 与 **#8021 新 issue**）。
- **`https://openai.com/index/chatgpt-memory-dreaming/`**：`0605.md`–`0606.md` 已收录 → 本日不重复。
- **`#8018`/`#8019`/`#8016`/`#8017`**：**关闭未合并** → 记为 **参考/阻塞信号**，不记为已修复。
- **X_WHITELIST / YOUTUBE_WHITELIST / Bilibili**：无法稳定抓取原帖 → 不收录。
- **DeepMind Blog**：抓取超时 → 无 **`0607`** 可核验新文 → 丢弃。
- **`openai/evals`**：无 **`0607`** 新合并/发布 → 丢弃。
- **Indie Hackers Build Board 标题帖**：无独立 permalink 可核验全文 → 改用 **可直达** 的 **signup→paid 5.8x** 长文。

## 7) 本周累计计数

- Agent: **242**（`0606.md` §7: 235 + 本日 §1: 7）
- Theory: **95**（`0606.md` §7: 92 + 本日 §2: 3）
- Career/Business: **57**（`0606.md` §7: 56 + 本日 §4: 1）
- LC: **39**（`0606.md` §7: 38 + 本日 §5: 1）
- IELTS: **39**（`0606.md` §7: 38 + 本日 §5: 1）
