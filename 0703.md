# Daily Research - 2026-07-03 (21:30)

## 0) 今日3条结论

- **LangGraph `FuturesDict.on_done` 存在 O(T²) 锁竞争（#8240 / PR #8270，updated 2026-07-03）**：400 路并行 tool call 超步每轮 ~80k 次 `future.exception()` → 多 agent / `Send` map 的 skill eval 出现「同图不同延迟」噪声，L2 latency gate 假阳性。
- **vLLM `_postprocess_messages` 对 scalar tool arguments 未归一化（#47555，opened 2026-07-03）**：`arguments` JSON 解析为字符串/数字时 GLM 等 template 调 `.items()` 直接 500 → agent serving 层 tool-call eval 与 prod 500 分叉，需 L3 门禁覆盖 malformed arguments。
- **FlowTracer 用 attention-induced DAG + answer-targeted flow 做 token 级 RL credit（arXiv 2606.10646）**：把 attention 边容量经 Doob-h 重加权后做 flow throughput → 为 ≥3 turn agent eval 的「关键推理 token vs filler」分层打分提供可复现图论框架，优于均匀 GRPO credit。

## 1) Agent工程化（high-signal）

- [fix: add pipeline sync in setup() to prevent SSL connection closure（LangGraph PR #8273 updated **2026-07-03**）](https://github.com/langchain-ai/langgraph/pull/8273)
  - 摘要: `AsyncPostgresSaver` 在 `pipeline=True` 模式下，`setup()` 连续发送多条语句未 `sync()`，连接中断（网络/Supabase idle timeout）后 pipeline 进入不一致状态，报 `psycopg.OperationalError: SSL connection has been closed unexpectedly`。Fix：每条 cursor 执行后显式 `await self.pipe.sync()`。Closes #5675。
  - 为什么重要(对我): **skill 落地**——Postgres checkpoint 是长会话 agent 生产标配；`setup()` 间歇失败导致 checkpoint 不可用，collect→test→eval 的 state 持久化与 prod 分叉。
  - 明日动作(<=20min): 写 **是/否/未知**：你 Postgres saver 是否用 `pipeline=True`（是/否/未知）。

- [fix(checkpoint): actionable error message for unsupported serialization types（LangGraph PR #8272 updated **2026-07-03**）](https://github.com/langchain-ai/langgraph/pull/8272)
  - 摘要: `JsonPlusSerializer` 遇到不支持类型时 `TypeError` 无指引。PR 重写 error message：列出原生支持类型 + 两条路径（转换类型 / 实现 `SerializerProtocol`）；docstring 增「Custom serialization」示例；100 项 `test_jsonplus.py` 通过。Closes #2557。
  - 为什么重要(对我): **skill 落地 + eval**——skill state 常含自定义 Pydantic/dataclass；opaque serialization 错误使 L3 debug 耗时，eval harness 难定位「哪类 state 字段不可序列化」。
  - 明日动作(<=20min): 写 **≤15 词**：你 checkpoint state 是否含自定义类型（有/无/仅 dict）。

- [perf: FuturesDict.on_done re-scans all completed futures on every callback（LangGraph #8240）/ PR #8270 updated **2026-07-03**）](https://github.com/langchain-ai/langgraph/issues/8240)
  - 摘要: `FuturesDict.on_done` 每完成一个 task 就对整个 `self.done` 集合重扫 `should_stop`，T 路并行超步产生 O(T²) 次 `future.exception()` 锁竞争；400 task fan-out 每 `invoke` ~80k 次调用。Fix：仅对刚完成的 `{fut}` 调用 `should_stop`（PR #8270 已提，当前 closed 待 maintainer assign）。
  - 为什么重要(对我): **skill 落地 + token 优化**——parallel tool call / multi-agent `Send` 是 skill stack 常态；超步调度开销直接吃 eval 吞吐与 serving latency budget。
  - 明日动作(<=20min): 写 **≤15 词**：你 graph 单超步最大并行 task 数（≤10 / 10–100 / >100）。

- [anthropic: add advisor_ to built-in tool prefixes（LangChain PR #38642 updated **2026-07-03**）](https://github.com/langchain-ai/langchain/pull/38642)
  - 摘要: Anthropic 新增 `advisor_*` server tool（如 `advisor_20260301`）；未列入 `_ANTHROPIC_BUILTIN_TOOL_PREFIXES` 时走 `convert_to_anthropic_tool` 假设 OpenAI 形 `parameters` 键 → `KeyError: 'parameters'`（同 #31240 `web_search` 失败模式）。Fix：前缀列表加 `advisor_` + 回归测试。
  - 为什么重要(对我): **skill 落地**——advisor 是 Anthropic 内置 sub-agent skill；集成层漏注册使 model 可见但 API 层崩溃，per-skill eval 出现「tool 列表有、调用 500」假阴性。
  - 明日动作(<=20min): 写 **是/否**：你是否用 Anthropic advisor / built-in server tools（是/否/未知）。

- [fix(protocols/ag-ui): deep-copy initial_state so workflows don't share state（LlamaIndex PR #22076 merged **2026-07-03**）](https://github.com/run-llama/llama_index/pull/22076)
  - 摘要: `get_default_workflow_factory` 内层闭包共享 operator 传入的 `initial_state` dict 引用；`AGUIChatWorkflow.__init__` 按引用存储，factory 产出的所有 workflow 别名同一 dict。任一 workflow 变异 `initial_state` 泄漏到其他实例。Fix：`deepcopy(initial_state)`。Closes #22069。
  - 为什么重要(对我): **skill 落地 + eval**——AG-UI 多会话 factory 是 skill 动态挂载常见模式；state 别名使 parallel eval run 互相污染，L2 trajectory match 假阳性/假阴性。
  - 明日动作(<=20min): 写 **是/否**：你 workflow factory 是否 deep-copy 初始 state（是/否/未知）。

- [[Bugfix] Coerce scalar tool call arguments and reject malformed JSON in _postprocess_messages（vLLM PR #47555 updated **2026-07-03**）](https://github.com/vllm-project/vllm/pull/47555)
  - 摘要: `tool_calls[].function.arguments` JSON 解析为 scalar（如 `"\"escaped string\""`）时原样存储，GLM 等 chat template 调 `arguments.items()` → `'str' object has no attribute 'items'` → 500。Fix：scalar/null 强制 `{}`（对齐 #43862）；malformed JSON 抛 `VLLMValidationError` → 400。Fixes #46328。
  - 为什么重要(对我): **eval + serving**——tool-call L2 exact-match grading 依赖 arguments dict；serving 层 500 与 eval mock 行为分叉，且 scalar arguments 是真实模型偶发输出。
  - 明日动作(<=20min): 写 **是/否**：你 eval harness 是否校验 tool arguments 为 dict（是/否/仅 end-state）。

- [[Core] Optimize sliding-window prefix-cache miss scan（vLLM PR #47556 updated **2026-07-03**）](https://github.com/vllm-project/vllm/pull/47556)
  - 摘要: `SlidingWindowManager.find_longest_cache_hit()` 原从右向左逐 block 扫描；低 prefix-cache 命中率时，滑动窗口内一次 miss 可排除同窗口后续候选 tail。新实现按候选 tail 跳跃扫描，保留 partial hit fallback。
  - 为什么重要(对我): **token 优化**——长 trace agent 的 sliding-window 模型（Gemma 等）prefix cache 命中直接影响 TTFT；serving 优化直接降低 multi-turn eval 成本。
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 是否用 sliding-window + prefix cache（是/否/未知）。

- [Shell + Skills + Compaction: Tips for long-running agents（OpenAI Developers Blog）](https://developers.openai.com/blog/skills-shell-tips)
  - 摘要: OpenAI 总结长运行 agent 三件套：Skills（可挂载 procedure/guardrail）、hosted shell（受控联网执行）、server-side compaction（自动压缩防 context 溢出）。Glean 案例：Salesforce skill eval 准确率 73%→85%，TTFT -18.1%；负例 + template 嵌入是关键 tactic。
  - 为什么重要(对我): **skill 落地 + eval**——与 Cursor/Harness skill 栈同构；Glean 的 eval 提升数字是可引用的 skill A/B 方法论锚点。
  - 明日动作(<=20min): 写 **≤15 词**：你长会话 agent 是否有自动 compaction（有/无/仅 truncate）。

## 2) 原理深化（Attention/Transformer/Agent）

- [How Does Reasoning Flow? Tracing Attention-Induced Information Flow for Targeted RL in LLMs（arXiv **2606.10646**）](https://arxiv.org/abs/2606.10646)
  - 摘要: FlowTracer：将 token 序列建模为 attention-induced DAG，边容量来自聚合 attention weight；经 Doob-h-like 重加权保留仅流向 answer region 的影响并满足局部 flow conservation；super-source→super-sink 注入 unit flow 得 token throughput，用于 RLVR 的 token-level reward shaping。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) throughput 高的 token 是否对应你 gold 集里的「关键推理步」？(2) 能否替代均匀 GRPO credit 做 L2 per-token grading？(3) synthesized flow backbone 与 HydraHead head 专化是否可联合做 serving+eval 预算分配？
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否区分 reasoning token vs filler（是/否/仅 outcome）。

- [Enhancing Multi-Agent Communication through Attention Steering with Context Relevance / Agent-Radar（arXiv **2605.30136**）](https://arxiv.org/abs/2605.30136)
  - 摘要: Agent-Radar：training-free 多 agent 上下文管理；联合 semantic relevance + spatial decay（agent 图 hop 距离）+ temporal decay 选句子级 context，经 Selective Prompt Anchoring 引导 attention，不压缩/剪枝完整 transcript。五 benchmark 最高 +7.64 分；AutoGen/GPTSwarm 插件最高 +12.87 分。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) attention steering vs summarization 哪个更适合你 ≥3 turn service agent？(2) spatial decay 能否映射到你 F/C 规则里的 agent 拓扑？(3) 7.64 分提升对 token 预算（不剪枝全 transcript）的 trade-off 值不值？
  - 明日动作(<=20min): 写 **≤15 词**：你 multi-agent 是否做 context 压缩（压缩/steering/无）。

- [BOSCH: Black-Box Binary Optimization for Short-Context Attention-Head Selection in LLMs（ACL 2026 Long）](https://aclanthology.org/2026.acl-long.1877/)
  - 摘要: BOSCH：training-free 黑盒优化，将 SWA hybridization 分解为 layer-importance probe → 自适应 per-layer SWA ratio → grouped head-level 优化。1.7B–30B 四模型上 consistently 优于 layer-level heuristic 和 6 种 static head ranking；高 SWA ratio 增益更大；选定 head 随 ratio 大幅 turnover，说明需 per-ratio 重选而非固定 ranking。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) head-level vs layer-level attention 优化哪个更适合 agent 长 trace serving？(2) turnover 现象对你 HydraHead/DFS 两 head 专化假说意味着什么？(3) training-free probe 能否嵌入 eval 前的 calibration 步？
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 优化粒度是 layer 还是 head（layer/head/未知）。

## 3) 热门项目/可试个人项目

- [shot-scraper 1.10 — `shot-scraper video` 让 agent 录制功能演示视频（Simon Willison Blog，**2026-06-30**）](https://simonwillison.net/2026/Jun/30/shot-scraper-video/)
  - 摘要: 新命令接受 `storyboard.yml`（server 启动 + scenes 点击/填表/等待），Playwright screencast 录 webm/mp4。作者用 Codex Desktop + `--help` 输出自举完成 demo YAML；`--help` 即 embedded SKILL 模式。
  - 为什么重要(对我): **eval 可复用杠杆**——agent 改 UI 后自动录 demo 可作为 L3 人工审查 artifact，比纯 log 更适合 collect→test→eval 的 visual regression。
  - 明日动作(<=20min): `uvx shot-scraper video --help` 读 help；写 **≤15 词** 你 eval 是否要求 visual demo（是/否/仅 screenshot）。
  - 估计复现成本: **低**（`uvx shot-scraper` + 静态 HTML demo；无 GPU）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [More details on Fable 5's cyber safeguards and our jailbreak framework（Anthropic Blog，**2026-07-02**）](https://www.anthropic.com/news/fable-safeguards-jailbreak-framework)
  - 机会信号: **行业级 jailbreak 严重度框架（CJS-0 到 CJS-4）**——Amazon/Microsoft/Google 等 Glasswing 伙伴共建；四轴评分（capability gain / breadth / weaponization ease / discoverability）→ 出海 agent 产品可引用统一 severity taxonomy 做 governance eval 与合规对话。
  - 风险信号: Fable 5 classifier safety margin 扩大导致 benign coding 误拦；订阅计划 Fable 用量受限（7 月 7 日前 50% weekly cap，之后需 usage credits）→ 前沿模型成本不可预测。
  - 我可执行的一步(<=30min): 读 CJS 框架四轴定义；写 **≤30 词** 你 agent governance eval 是否有 severity 分级（有/无/仅 pass-fail）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 121 Best Time to Buy and Sell Stock](https://leetcode.com/problems/best-time-to-buy-and-sell-stock/)：单次遍历维护 `min_price` 与 `max_profit`：`profit = max(profit, price - min_price)`。明日动作：手写 O(n) 一遍版（≤20min）；写 **≤15 词**：min-so-far 与 agent 在线决策的类比。

- **IELTS（≤1 条）**：[IELTS Speaking — Describe a time you solved a problem（British Council 样题）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-practice-tests/speaking)：Part 2 用「debug agent eval 假阴性」脱敏练 2 分钟（situation/task/action/result）。明日动作：录音 2 分钟自评结构（≤20min）；写 **≤15 词** 弱项（fluency/词汇/结构 选 1）。

## 6) 噪音过滤记录

- **LangGraph #5672（cancel 丢 streamed state）/ #8268（Postgres 连接）/ #7065（AAR）**：`0702.md` 已收录 → 不重复。
- **LangChain #38611（@tool description）/ AutoGen #7867 / #7405**：`0702.md` 已收录 → 不重复。
- **HydraHead（2606.20097）/ Program Synthesis Attention（2606.19317）/ ICRL（2605.05755）**：`0702.md` 已收录 → 不重复。
- **ACTS（2606.03965）/ eval-skills（OpenAI Developers）**：`0626.md`/`0609.md` 已收录 → 不重复。
- **LangChain #38637（HuggingFaceEndpointEmbeddings bill_to）**：embedding 集成 bug，与当前 focus（skill/eval/token）弱相关 → 丢弃。
- **AutoGen #7676（docs TextMessage import）**：文档 fix，无工程信号 → 丢弃。
- **vLLM #47187（Transformers backend perf）/ #47332（Gemma4 FA4 mask）**：serving infra，与 agent skill 落地间接 → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-03** 原帖 → 不收录。
- **@gefei55 / @ianneo_ai 职业内容**：第三方聚合无法验证原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv + Anthropic 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **450**（`0702.md` §7: 442 + 本日 §1: 8）
- Theory: **185**（`0702.md` §7: 182 + 本日 §2: 3）
- Career/Business: **83**（`0702.md` §7: 82 + 本日 §4: 1）
- LC: **65**（`0702.md` §7: 64 + 本日 §5: 1）
- IELTS: **65**（`0702.md` §7: 64 + 本日 §5: 1）
