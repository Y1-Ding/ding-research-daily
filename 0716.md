# Daily Research - 2026-07-16 (21:30)

## 0) 今日3条结论

- **AgentCompass 把 eval 拆成 Benchmark × Harness × Environment 三层**：20+ benchmark 共用异步 runtime + trajectory 分析，可直接映射你的 map_prompt_test——collect 层（harness）与 test 层（benchmark+env）应可插拔组合，而非每换 bench 重写执行逻辑。
- **vLLM 同日落地两条 token 优化链路**：spec decode 可独立设 `speculative_config.kv_cache_dtype`（target FP8 / drafter BF16）；packed MLA KV offload 用 `as_strided` 替代 `set_()` 避免假 overflow——multi-tier agent serving 需分组件测 KV dtype 与 offload 边界。
- **Transforming Rank 用谱秩解释 Pre-Norm 为何优于 Post-Norm**：skip/norm/FFN 4× 宽度扩张共同决定 depth 上 Jacobian rank 存活——长上下文 agent backbone 选型时，norm 放置与 residual scale 是比「层数」更底层的稳定性旋钮。

## 1) Agent工程化（high-signal）

- [AgentCompass: A Unified Evaluation Infrastructure for Agent Capabilities（arXiv **2607.13705**, submitted **2026-07-16**）](https://arxiv.org/abs/2607.13705)
  - 摘要: 开源 eval 基础设施，解耦 **Benchmark / Harness / Environment** 三层；异步 fault-tolerant runtime + trajectory 级分析（tool call、env feedback、reward-hacking 检测）。原生集成 20+ benchmark（τ³-bench、GAIA、SWE-bench-Pro、SkillsBench、SciCode 等），代码在 https://github.com/open-compass/AgentCompass。
  - 为什么重要(对我): **评测 + skill 落地**——与 0715 Long-Horizon-Terminal-Bench 的 dense grader 互补：前者是「测什么」，AgentCompass 是「怎么可复用测」；你的 L2/L3 eval 可借鉴 harness 独立层，让同一 collect trace 跑多 bench 而不 fork pipeline。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否把 harness（agent loop/tool 接口）与 benchmark 定义分开（是/否/仅概念上）。

- [fix(ag-ui): raise ValueError instead of fabricating tool_call_id（LlamaIndex PR #22103 merged **2026-07-16**）](https://github.com/run-llama/llama_index/pull/22103)
  - 摘要: `llama_index_message_to_ag_ui_message` 在 `tool_call_id` 缺失时旧逻辑静默 `uuid4()` 伪造 ID → AG-UI 协议下 ToolMessage 无法与先前 ToolCall 关联。Fix：缺 ID 直接 `ValueError`，把 contract violation 暴露而非埋 silent bug。
  - 为什么重要(对我): **skill 落地 + eval**——multi-turn agent trace 中 tool call/result 配对是 L2 基础断言；伪造 ID = eval 假阳性「有配对」；harness 应在 import 层 catch 此类协议错误。
  - 明日动作(<=20min): 写 **是/否**：你 agent trace 是否 assert 每个 tool result 的 call_id 有对应 call（是/否/未知）。

- [fix(core): raise TypeError on type-incompatible AddableDict merge instead of silent data loss（LangChain PR #38863 opened **2026-07-15**, updated **2026-07-16**）](https://github.com/langchain-ai/langchain/pull/38863)
  - 摘要: `AddableDict.__add__` 在 `chunk[key]+other[key]` 抛 `TypeError` 时旧逻辑静默用 right-hand 覆盖 left-hand（如 `int` 被 `str` 替换）。Fix：重抛带 key/type 信息的 `TypeError`。`AddableDict` 用于 LCEL `.astream_events` chunk 聚合。
  - 为什么重要(对我): **评测 + skill 落地**——streaming agent eval 若聚合 state/metrics，静默类型丢失 = 过程分算错且无告警；collect 阶段应对 stream chunk merge 做 type-consistency 断言。
  - 明日动作(<=20min): 写 **是/否**：你 LCEL/streaming pipeline 是否聚合异构 chunk（是/否/未知）。

- [fix(openai): route reasoning tool calls to Responses API（LangChain PR #38860 opened **2026-07-15**）](https://github.com/langchain-ai/langchain/pull/38860)
  - 摘要: GPT-5.6 function-tool 请求、以及 GPT-5.4/5.5 在 reasoning enabled 时的 function-tool，自动路由到 Responses API（Chat Completions 不支持 reasoning+tools 组合）。`use_responses_api=False` 可强制回 Chat Completions（需 `reasoning_effort="none"`）。
  - 为什么重要(对我): **skill 落地**——reasoning model + tool 是常见 skill 栈；endpoint 路由错误 = 线上 tool 全挂；skill 模板应记录 model→API 路由矩阵并在 smoke test 覆盖。
  - 明日动作(<=20min): 写 **是/否**：你 stack 是否用 GPT-5.x + function tools + reasoning（是/否）。

- [[Spec Decode] Add kv_cache_dtype to speculative_config to control separately from target（vLLM PR #48787 merged **2026-07-16**）](https://github.com/vllm-project/vllm/pull/48787)
  - 摘要: 全局 `--kv-cache-dtype fp8` 会同时作用于 target 与 spec drafter；drafter 若无 FP8+non-causal attention backend 则启动失败。新增 `speculative_config.kv_cache_dtype`：target 保持 FP8，drafter 可设 `bfloat16`——B300 上 GLM-5.2+speculator GSM8k 95%。
  - 为什么重要(对我): **token 优化**——spec decode 是 agent serving 降 latency 主路径；target/drafter KV dtype 解耦 = 按组件调优而非全局一刀切；与 0715 #48123 lookup_scope 同属 KV 策略分层。
  - 明日动作(<=20min): 写 **是/否**：你是否用 speculative decoding 部署 agent LLM（是/否/未知）。

- [[Bugfix] Fix offloading set_ overflow for packed non-uniform KV caches（vLLM PR #48530 merged **2026-07-16**）](https://github.com/vllm-project/vllm/pull/48530)
  - 摘要: MLA 等 packed KV cache 中各层 `page_size < block_stride`，`tensor.set_()` 的 bounds check 误报 overflow（实际内存合法但跨层边界）。Fix：用 `torch.as_strided()` 创建 view，shape/stride/offset 与预期 `set_()` 一致。GLM-5.2-FP8 + KV offloading 生产验证通过。
  - 为什么重要(对我): **token 优化**——长上下文 agent KV offload 是成本核心；packed layout 崩溃 = 线上 agent 不可用；eval 应记录 KV layout（uniform vs packed）作为 serving 维度。
  - 明日动作(<=20min): 写 **是/否**：你 vLLM 部署是否用 packed/non-uniform KV cache（是/否/未知）。

- [fix: keep message queue accounting consistent when runtime is stopped with in-flight messages（AutoGen PR #7963 opened **2026-07-16**）](https://github.com/microsoft/autogen/pull/7963)
  - 摘要: `SingleThreadedAgentRuntime.stop()` 替换 `_message_queue` 时，in-flight handler 完成后对**新空队列**调 `task_done()` → `ValueError: task_done() called too many times`（#7007，Ctrl+C 中断常见）。Fix：handler 捕获原队列引用；RPC 若 runtime 已停则 resolve `CancelledError` 而非挂起 future。
  - 为什么重要(对我): **评测 harness 可靠性**——eval batch 跑 multi-agent 时常中断/超时；queue 会计错误 = trace 截断 + 假失败；harness teardown 应 assert 无 orphan future。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否用 AutoGen runtime 且会 mid-run stop（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Transforming Rank: How Architecture Navigates the Spectral Pathologies of Depth（arXiv **2607.14018**, submitted **2026-07-16**）](https://arxiv.org/abs/2607.14018)
  - 摘要: MIT CSAIL 从**谱秩（effective rank）**角度解析 Transformer FFN block：skip connection 绕过 rank-reducing branch 保梯度秩，但引入 ensemble-like 行为；norm 放置（Pre vs Post）控制 branch-to-skip ratio，解释 Post-Norm rank collapse vs Pre-Norm plateau；FFN 4× 宽度扩张 + 双矩阵结构在 expanded space 做 activation 以保 branch Jacobian full rank（Marchenko–Pastur 阈值）。初始化 input–output Jacobian rank 可预测 CIFAR-10 可训练性。代码：https://github.com/everettk/transforming-rank-paper
  - 为什么重要(对我): **Talk ≥3 turns + Attention/Transformer**：(1) 深度 agent backbone 稳定性不只看 attention，FFN block 的 rank 路径同样关键；(2) Pre-Norm 流行有谱论解释，非经验偏好；(3) 与 token 优化（KV/offload）正交——理解 depth rank 帮你判断「加层」vs「换 sparse attention」哪条杠杆更大。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 模型选型是否考虑过 depth vs width 对长上下文稳定性的 tradeoff（有/无）。

- [COBS: Cumulant Order Block Sparse Attention（arXiv **2607.09052**）](https://arxiv.org/abs/2607.09052)
  - 摘要: 分析 DeepSeek NSA 的 block selection 瓶颈：选 block 需近似 **attention mass**（块内 attention score 之和），一阶 selector（mean-pool）丢失曲率信息。COBS 用 cumulant expansion 二阶统计（compressed key covariance per block）改进选择；32k RULER 上 NSA 0.30→0.82（dense 0.90），KV read traffic 仅 NSA 的 1.21×、dense 的 1/15。
  - 为什么重要(对我): **Talk ≥3 turns + Attention + token 优化**：(1) sparse attention 质量瓶颈在 **block selection 近似误差**，非 kernel 实现；(2) agent 长上下文 serving 若走 block-sparse，eval 应测 selection recall 而非仅 E2E latency；(3) 二阶统计 ≈ 用 curvature 补一阶 mean 的信息缺口——与 STAPO normalized entropy 同属「校准不确定性信号」族。
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 是否评估过 sparse/block attention 对 agent 工具调用准确率的影响（是/否）。

- [STAPO: Selective Trajectory-Aware Policy Optimization for LLM Agent Training（ACL 2026 Long, arXiv **2607.04963**）](https://arxiv.org/abs/2607.04963)
  - 摘要: 长程 RL agent 训练中的 **trajectory neglect**（中间步丢失任务目标/历史）：Shannon entropy 混淆 state complexity 与 agent confidence。提出 **normalized entropy** 定位 outlier steps，STAPO 两阶段——(1) Outlier Localization；(2) Selective Optimization（trajectory-aware reward + trajectory-independent penalty 防 reward hacking）。ALFWorld/WebShop/Search-QA SOTA。
  - 为什么重要(对我): **Talk ≥3 turns + Agent 原理**：(1) ≥3 turn agent 的失败常在中间步而非首尾；(2) eval 可用 step-level entropy deviation 标「疑似 neglect」而不等 final fail；(3) 与 0715 dense partial-credit 互补——训练侧定位 neglect，评测侧量 partial progress。
  - 明日动作(<=20min): 写 **≤15 词**：你 L2 eval 是否记录逐步 entropy/confidence 信号（有/无）。

## 3) 热门项目/可试个人项目

- [AgentCompass（open-compass/AgentCompass）](https://github.com/open-compass/AgentCompass)
  - 摘要: 上海 AI Lab 开源 agent eval 基础设施；`benchmark × harness × environment` 解耦，内置 τ³-bench/GAIA/SWE-bench-Pro/SkillsBench 等 20+ bench + trajectory 分析与 reward-hacking 检测器。
  - 为什么重要(对我): **评测杠杆**——可直接 fork 跑 1 个你已有 gold case 的 bench，对比你自建 harness vs 官方 harness 的 pass rate 差异，沉淀「harness 差异表」。
  - 明日动作(<=20min): clone repo；跑 README 最小 example；写 **≤15 词** 你最该接入的 1 个 bench 名。
  - 估计复现成本: **中**（需配 model API + 部分 bench 环境）。

- [[V1][Core] Add public build_full_block_hash_chain helper for offline prefix-cache analysis（vLLM PR #48838 opened **2026-07-16**）](https://github.com/vllm-project/vllm/pull/48838)
  - 摘要: 提取 V1 scheduler 的 block hash chain 逻辑为公开 helper `build_full_block_hash_chain`，供离线 prefix-cache 分析（无需 GPU）；解决 out-of-tree 工具与引擎 hash 语义漂移问题；含 `NONE_HASH` adopt-don't-clobber 与并发安全。
  - 为什么重要(对我): **token 优化杠杆**——agent 多轮对话 prefix-cache hit rate 直接影响成本；10 分钟可用 helper 验证「哪些 prompt 前缀可复用」。
  - 明日动作(<=20min): 读 PR description 的 contract 段；写 **≤15 词** 你 agent 最长可复用 prefix 是什么。
  - 估计复现成本: **低**（纯 CPU + pytest 可验证）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [GPT-Red: Unlocking Self-Improvement for Robustness（OpenAI Blog, **2026-07-15**）](https://openai.com/index/unlocking-self-improvement-gpt-red)
  - 机会信号: OpenAI 用 self-play RL 训练内部 red-team 模型 GPT-Red，攻击成功率 84% vs 人类 13%；融入 GPT-5.3 以来各版训练 → GPT-5.6 Sol 直接 prompt injection 失败率降至 0.05%。**安全 eval flywheel** 可产品化——垂域 agent 的「automated adversarial eval」是出海 signature（合规/企业采购刚需），比通用 chat 更易收费。
  - 风险信号: GPT-Red 不公开；multi-turn conversational attack 仍弱；过度 robust 可能误伤 capability（OpenAI 称 frontier/over-refusal 未变，但需自证）。
  - 我可执行的一步(<=30min): 选 1 条你 agent 的 prompt-injection 攻击面（如 RAG 文档/工具返回）；写 3 条 adversarial case + 期望防御行为（≤10 行）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 3867 Sum of GCD of Formed Pairs](https://leetcode.com/problems/sum-of-gcd-of-formed-pairs/)（**Day 16, Jul 2026**）：枚举或优化求所有数对 GCD 之和；数论 + 枚举/筛法。明日动作：先手写 brute-force 20 行验证小数据，再查 Euler 公式优化（≤25min）；写 **≤15 词**：agent 组合枚举任务与 GCD 聚合的类比。

- **IELTS（≤1 条）**：[IDP India: IELTS transitions to computer-based from September 2026（EducationTimes, **2026-07-16**）](https://www.educationtimes.com/article/newsroom/99742279/idp-education-announces-transition-to-ielts-computer-based-format-from-september-2026)：印度 2026-09 起全面机考；纸笔最后三场 **7/25、8/6、8/22**；可选 Writing on Paper（L/R 机考 + Writing 手写）。明日动作：若目标印度，确认是否赶末班车纸笔或练机写；写 **≤15 词** 你更适机写还是手写（选 1）。

## 6) 噪音过滤记录

- **Long-Horizon-Terminal-Bench 2607.08964、ResonatorLM 2607.05583、Lil'Log Harness Engineering**：`0715.md` 已收录 → 不重复。
- **AutoGen #7960 governance sample、LlamaIndex #22364 MCP draft-07**：`0715.md` 已收录 → 不重复。
- **OpenAI managing AI investments Jul 14**：`0715.md` 已收录 → 不重复。
- **LangChain #38876 model profile refresh**：数据 chore → 丢弃。
- **vLLM #48802 docs typo、#48784 ROCm matmul precision、#48741 RMSNorm**：infra/docs → 丢弃。
- **vLLM #48846 MiniMax M2 whitespace**：与 0711 #48348 tool parser 族相关但当日新 PR、未 merge → 记入待观察（今日 §1 已满）。
- **LlamaIndex #22377 redis decode_responses、#22367 pgvector numeric**：storage 层，弱于今日 eval/KV focus → 丢弃。
- **AutoGen #7944–7946 docs**：文档 → 丢弃。
- **arXiv 2607.04319 Legible-by-Construction**：`0708–0714.md` 已收录 → 不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-16** 原帖 URL → 不收录。
- **@gefei55 / @ianneo_ai 职业帖**：无法验证当日原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv + OpenAI 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **534**（`0715.md` §7: 527 + 本日 §1: 7）
- Theory: **221**（`0715.md` §7: 218 + 本日 §2: 3）
- Career/Business: **95**（`0715.md` §7: 94 + 本日 §4: 1）
- LC: **77**（`0715.md` §7: 76 + 本日 §5: 1）
- IELTS: **77**（`0715.md` §7: 76 + 本日 §5: 1）
