# Daily Research - 2026-08-04 (21:30)

## 0) 今日3条结论

- **LangGraph 嵌套 subgraph 的 DeltaChannel 会「静默空读 + 磁盘丢历史」**（#8520, Aug 4）：`get_state`/`update_state` 在子图 namespace 用 `self.checkpointer=None` 而非 caller-resolved saver → DeltaChannel 全空；`bulk_update_state` 还会在 snapshot 时把空 channel 写入磁盘 → **checkpoint-replay eval 必须把 nested subgraph + DeltaChannel 纳入 conformance**，不能只看根图。
- **ReAct parser Thought/Answer 不对称 → max_iterations 假失败**（LlamaIndex #22564/#22568, Aug 3–4）：`extract_tool_use` 已允许无 `Thought:`，但 `extract_final_response` 仍强制 `Thought:` → `Answer:` 路径抛 `ValueError` 触发 retry 直至耗尽 → **agent eval 的 parser conformance 需单独测「无 Thought 最终答案」**。
- **Sparse MLA masked-MHA workspace 固定 64MiB < 32K prefill 所需 128MiB**（vLLM #50906, Aug 4）：mask 按 `num_prefills × max_query_len × max_key_len` 增长；异步 CUDA 错误可在 HTTP 200 后才爆 → **长上下文 Attention eval 需按 batch shape 验 workspace 上限，不能假设默认 buffer 够用**。

## 1) Agent工程化（high-signal）

- [fix(langgraph): hydrate delta channels with the caller-resolved checkpointer（LangGraph PR #8520, updated **2026-08-04**）](https://github.com/langchain-ai/langgraph/pull/8520)
  - 摘要: Fixes #8470。子图无自有 checkpointer 时，`_prepare_state_snapshot` 用 `self.checkpointer`（常为 `None`）而非 config 已解析的 saver 调用 `channels_from_checkpoint` → `DeltaChannel` 静默为空。写路径 `bulk_update_state` 同理：空 hydrate 后 `create_checkpoint` 把错误 `_DeltaSnapshot` 写入磁盘，**累积历史丢失**。Fix：四 reader + bulk update 传 caller-resolved saver；7 新测全过。
  - 为什么重要(对我): **评测 + skill 落地**——嵌套 LangGraph agent（planner/executor 子图）的 `get_state_history` 是 trajectory eval ground truth；空读 = pass^k 基于假 state，写路径 = 生产数据腐蚀。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否对 nested subgraph namespace 做 `get_state`/`get_state_history` 断言（是/否/未知）。

- [fix: Prevent pipeline with connection pool in AsyncPostgresSaver.from_conn_string（LangGraph PR #8519, updated **2026-08-03**）](https://github.com/langchain-ai/langgraph/pull/8519)
  - 摘要: Fixes SSL 间歇断连另一路径。`from_conn_string(pipeline=True)` 创建 `AsyncConnectionPool` 后又包 `conn.pipeline()`——`AsyncPipeline` 要求单 `AsyncConnection` 非 pool；`__init__` 本已拒绝该组合，但 `from_conn_string` 仍可触发 → LLM 间隙 idle SSL 被服务端关闭。Fix：pool 路径禁用 pipeline。
  - 为什么重要(对我): **skill 落地 + 评测稳定性**——与 0802 #8507（SSL 直连）形成 Postgres checkpoint 双侧清单；长跑 eval flaky = 不可复现 timeout。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `AsyncPostgresSaver.from_conn_string` + `pipeline=True`（是/否/未知）。

- [fix: warn when node returns keys not declared in state schema（LangGraph PR #8511, updated **2026-08-03**）](https://github.com/langchain-ai/langgraph/pull/8511)
  - 摘要: Fixes #8320。节点返回 state schema 未声明的 key 时静默丢弃、无 warning → 开发者加字段忘改 schema 时极难 debug。Fix：`attach_node` 内 `_get_updates` 过滤前 warn 未声明 key。
  - 为什么重要(对我): **skill 落地**——skill 迭代常给节点加新 state 字段；静默丢弃 = eval 以为写入成功、grading 读不到。
  - 明日动作(<=20min): 写 **是/否**：你最近 30 天是否给 LangGraph 节点加了 schema 外返回字段（是/否/未知）。

- [fix(langgraph): honor per-node cache_policy=None with set_node_defaults（LangGraph PR #7996, updated **2026-08-03**）](https://github.com/langchain-ai/langgraph/pull/7996)
  - 摘要: `set_node_defaults(cache_policy=...)` 无法让单节点 `add_node(cache_policy=None)` 真正 opt-out；`None` 与 `MISSING` 未区分 → 该节点仍继承默认 cache。Fix：`cache_policy_opt_out` flag + regression tests（默认/覆盖/显式禁用三路）。
  - 为什么重要(对我): **token 优化**——per-node cache ablation eval 若 opt-out 无效，$/task 与 cache hit 对比全错。
  - 明日动作(<=20min): 写 **是/否**：你是否对特定节点设 `cache_policy=None` 做 cache ablation（是/否/未知）。

- [fix(langgraph): preserve message IDs and additional_kwargs in add_messages format="langchain-openai"（LangGraph PR #7273, updated **2026-08-03**）](https://github.com/langchain-ai/langgraph/pull/7273)
  - 摘要: Fixes #7272。`add_messages(format="langchain-openai")` round-trip 丢 message ID 与 custom `additional_kwargs`（UI widget、metadata）。Fix：`include_id=True` + 按 ID 恢复 kwargs；19 tests pass。
  - 为什么重要(对我): **token 优化 + 评测**——prompt cache / Responses API 依赖 stable id；eval transcript 分裂 = grading 输入与 HITL 状态不一致。
  - 明日动作(<=20min): 写 **是/否**：你 state 是否 `format="langchain-openai"` 且依赖 message id 或 custom kwargs（是/否/未知）。

- [fix(core): accept a ReAct final answer without a "Thought:" prefix（LlamaIndex PR #22564, updated **2026-08-04**）](https://github.com/run-llama/llama_index/pull/22564)
  - 摘要: `extract_tool_use` 在 #19190 已允许无 `Thought:`，但 `extract_final_response` 仍强制 → 模型输出 `Answer:` 无 `Thought:` 时 `ValueError` → `ReActAgent.take_step` retry 直至 `max_iterations`。Fix：双 regex（有/无 Thought）对齐两半 parser；#22568 为 `extract_final_response` 配套 fix。
  - 为什么重要(对我): **评测**——parser hang 污染 pass^k（timeout 而非 fail）；与 tool-call 序列 eval 的「格式等价类」直接相关。
  - 明日动作(<=20min): 写 **是/否**：你 ReAct eval 是否测过「仅 Answer: 无 Thought:」输出（是/否/未知）。

- [fix: preserve parallel tool results before return direct（LlamaIndex PR #22562, updated **2026-08-03**）](https://github.com/run-llama/llama_index/pull/22562)
  - 摘要: `FunctionAgent.handle_tool_call_results()` 遇首个 `return_direct=True` 即停止迭代 → 并行 tool call 中后续已完成结果被省略 → scratchpad 缺 `tool` response，下一轮 LLM 见 assistant tool_call 无匹配 result。Fix：先记录全部 parallel result，再选首个 eligible direct result。
  - 为什么重要(对我): **skill 落地 + 评测**——parallel tools 是 agent skill 常见模式；协议不完整 = eval 假失败或 silent 状态损坏。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否在 parallel tool call 下用 `return_direct`（是/否/未知）。

- [fix(ext): include tool_choice in ChatCompletionCache cache key（AutoGen PR #7975, updated **2026-08-03**）](https://github.com/microsoft/autogen/pull/7975)
  - 摘要: Fixes #7968。`ChatCompletionCache` key 忽略 `tool_choice` → `auto`/`required`/`none` 请求碰撞同一缓存 → 错响应。Fix：normalize `tool_choice` 纳入 key payload。
  - 为什么重要(对我): **token 优化 + 评测**——tool_choice ablation eval 若共享 cache = 结论反向；与 Anthropic cache token 漏计（0802 #22548）同属「缓存键不完整」族。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否在同一 messages 上切换 `tool_choice`（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix][Attention] Guard sparse MLA masked MHA workspace（vLLM PR #50906, updated **2026-08-04**）](https://github.com/vllm-project/vllm/pull/50906)
  - 摘要: Sparse MLA masked-MHA 的 bit-packed top-k mask 大小约 `num_prefills × round_up(max_query_len,tile_m) × ceil(max_key_len/32)`。固定 workspace 64MiB，单条 32K prefill 需 128MiB；heterogeneous batch 更大。reshape  oversized mask → CUDA error，且可能 HTTP 200 后异步失败。Fix：按需求 guard/分配 workspace。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/KV**：masked MHA 是 sparse attention 路径；workspace 低估 = agent 长 prompt eval 随机 worker crash，与 0802 hybrid KV recovery 同属 serving 静默错误族。
  - 明日动作(<=20min): 写 **≤15 词**：你 local eval 是否压过 32K+ sparse MLA batch（是/否/未知）。

- [[Bugfix][Attention] Size FlashInfer sparse MLA workspace for decode-context-parallel（vLLM PR #50791, updated **2026-08-03**）](https://github.com/vllm-project/vllm/pull/50791)
  - 摘要: Fixes #50781。`--decode-context-parallel-size > 1` 时 FlashInfer sparse MLA（B200/SM100）用静态 394MiB workspace，无 DCP/head/batch 感知 → `trtllm_batch_decode_with_kv_cache_mla` workspace overflow，单条 moderate prompt 可打垮全部 DCP worker。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/KV 分片**：DCP 改变每 rank decode attention 计算与 buffer 需求；eval 成本/latency 曲线在 DCP>1 时不可复现。
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 是否 `decode-context-parallel-size > 1`（是/否/未知）。

- [[ROCm][MLA] Fix fp8 KV cache decode on the AITER MLA backend（vLLM PR #51011, updated **2026-08-04**）](https://github.com/vllm-project/vllm/pull/51011)
  - 摘要: Kimi-K3 TP8（12 MLA heads/rank）+ `--kv-cache-dtype fp8` 在 ROCm AITER MLA 三路失败：head 整除 16 时 Gluon bf16-query kernel 断言；非整除 head asm 路径；第三路 silent 错误最危险。Fix：fp8 decode 路由 asm kernel + persistent schedule（qlen>4）。
  - 为什么重要(对我): **Talk ≥3 turns + MLA/Attention**：MLA 压缩 KV 是长上下文 agent 基础设施；fp8 KV ablation eval 在 ROCm 上可能 silent wrong 而非 clean fail。
  - 明日动作(<=20min): 写 **≤15 词**：你是否在 ROCm 上跑 fp8 MLA decode eval（是/否/未知）。

- [Attention Mechanisms Explained — Under the Hood（Hammad Abbasi, interactive guide）](https://hammadabbasi.com/under-the-hood/attention)
  - 摘要: 交互式 10 节：Q/K/V 投影、attention score heatmap、softmax temperature、multi-head、causal mask、cross-attention、**Flash Attention** IO 对比（4096 seq：32MB vs 3MB）、induction head、MHA/MQA/GQA KV-cache 计算器（128K 下 MHA 64GB vs GQA 16GB）。
  - 为什么重要(对我): **Talk ≥3 turns**：可把「attention sink / lost-in-middle」与 0802 M-RoPE streaming、本日 sparse MLA workspace 三条 serving bug 串成「position + buffer + mask 三维」自查清单。
  - 明日动作(<=20min): 玩一节 **Flash Attention** slider（seq len 4096），写 **≤15 词**：HBM vs SRAM 带宽差对你 agent 意味着什么。

- [fix(core): preserve scalar values in dict prompt lists（LangChain PR #39235, updated **2026-08-03**）](https://github.com/langchain-ai/langchain/pull/39235)
  - 摘要: `DictPromptTemplate` 格式化 list/tuple 时静默丢弃 int/bool/None 与嵌套 list → tool-use payload、score、ID 在进模型前损坏。Fix：递归 formatter 保留 scalar pass-through；补 mixed content block regression。
  - 为什么重要(对我): **Talk ≥3 turns + Agent 输入编码**：structured tool message 是 attention 前的「token 化输入」；静默丢字段 = eval 以为发了 score/id，模型实际未见。
  - 明日动作(<=20min): 写 **是/否**：你 prompt 是否用 `DictPromptTemplate` 嵌套 list 传 tool 结构化字段（是/否/未知）。

## 3) 热门项目/可试个人项目

- [langchain-ai/langchain-skills — Agent Skills for LangChain/LangGraph/Deep Agents](https://github.com/langchain-ai/langchain-skills)
  - 摘要: 21 skills（`langgraph-fundamentals`、`eval-engineering`、`langchain-middleware` 等）；`npx skills add langchain-ai/langchain-skills --skill '*' --yes` 安装；[官方博客](https://www.langchain.com/blog/langchain-skills) 报告 Claude Code 在 LangChain 生态任务 pass rate **29%→95%**（LangSmith eval）。
  - 为什么重要(对我): **skill 落地杠杆**——与 0802 smevals（eval 套件）、0801 skillval（skill A/B）互补：这是 **authoring/distribution** 侧官方 skill 库 + 可量化 uplift 数据。
  - 明日动作(<=20min): `npx skills add langchain-ai/langchain-skills --skill eval-engineering --yes` 扫 `SKILL.md` 前 30 行；写 **≤10 词** 你最想复制的 eval 模式。
  - 估计复现成本: **低**（npx + markdown skills，≤20min 安装单个 skill）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [How to get hired at an AI startup in 2026: current job openings and ways（AI+ Founders Community Substack, **2026-08-03**）](https://aiplusfounderscommunity.substack.com/p/ai-startups-hiring-2026)
  - 机会信号: 方法论强调 **stream 选题**（如「agent memory」而非泛「AI」）+ 带具体问题冷启动（4 句 teardown/fix）而非海投；本周列 Sierra **Software Engineer, Agent**（多城）、ElevenLabs **remote-first 跨 30 国**、Cribl **Cribl AI**、Harvey **Legal Engineer**、Replit BD 明示重视 vibe-coding——与 Agent eval/skill signature 可对接。
  - 风险信号: 作者坦承 20 封冷启仅 2 回复；Junior 岗位稀薄；「Legal engineer / agent strategist」头衔新但 hiring bar 未标准化，易陷入泛投焦虑。
  - 我可执行的一步(<=30min): 选 **1 个 stream**（agent eval / skill harness）+ **1 家公司**，写 4 句 cold email 草稿（具体产品问题 + 你做过的小 fix/mock），不附简历。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[39. Combination Sum](https://leetcode.com/problems/combination-sum/)（**Day 34, Aug 2026 Daily Challenge**；承接 0803 Letter Combinations、0802 Permutations 的 backtracking 链）：给定无重复候选数与 target，返回所有和为 target 的组合（同一数可无限次使用）。明日动作：模板化 `dfs(start, remain)` + 同 index 复用；写 **≤15 词**：「组合可复用」与 agent eval「tool 可重复调用」的边界类比。

- **IELTS（≤1 条）**：[IELTS Ready — British Council 官方免费备考（含 AI Mini Mock）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-ready)：50 分钟 Mini Mock + AI 估分。明日动作：完成 **Reading 一篇 Mini Mock**（0802 Speaking、0801 Writing、0729 Listening 已轮换，今日换 Reading）；写 **≤15 词** 主旨题/细节题 选 1 个弱项。

## 6) 噪音过滤记录

- **LangGraph #8521 README typo、#8512 docs link、#8510 gateway doc 示例**：无行为变更 → 过滤。
- **LangGraph #8518 uuid6 thread lock**（#8409）：重要但偏 infra id 生成，今日 §1 已满 8 条 → 过滤（可并入 checkpoint 稳定性专题）。
- **LangChain #39239–39244 deps bump**：纯 chore → 过滤。
- **LlamaIndex #22565–22571 deps/docs/FunASR**：非当前 focus → 过滤；#22567 Refine early-exit 偏 RAG → 过滤。
- **AutoGen #8020/#8021 MCP 2.x 迁移、#8009 OpenEval**（与 #7975 同族 eval 基础设施，今日 §1 已代表 cache key）：#8009 可下周专节 → 过滤。
- **vLLM #51014–51022 doc/ROCm/audio**：与 Attention focus 弱相关 → 过滤。
- **Karpathy Sequoia Jul 31 agentic engineering**：高信号但非 Aug 3–4 新帖 → 不重复（0801 已引 eval gap 主题）。
- **futureagi/agentry LangGraph eval 教程**：非 whitelist、非 Aug 3–4 一手源 → 过滤。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-08-03~08-04** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Aug 3–4 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **662**（`0802.md` §7: 654 + 本日 §1: 8）
- Theory: **285**（`0802.md` §7: 280 + 本日 §2: 5）
- Career/Business: **111**（`0802.md` §7: 110 + 本日 §4: 1）
- LC: **93**（`0802.md` §7: 92 + 本日 §5: 1）
- IELTS: **93**（`0802.md` §7: 92 + 本日 §5: 1）
