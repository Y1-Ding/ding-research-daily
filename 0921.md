# Daily Research - 2026-09-21 (21:30)

## 0) 今日3条结论

- **Tool-call 流式 finalize 会把 `index` 泄漏进 client `tool_call`**（LangChain PR **#40697**, created **2026-09-21**）：`block-delta` 路径经 `_merge_block_delta_into_store` 写入 `index` → 与下一块合并时 `TypeError` → **L2 streaming eval 须断言 finalized block 无 `index`（server_tool_call 除外）**。
- **Auto Mode 必须在「最终 tool_call」上分类**（#**40695** + **#40696**, created **2026-09-21**）：HITL 改参后若 Auto Mode 在外层先分类会 **错路由/重复计费** → **middleware 声明 `requires_final_request` / `may_modify_request` 并写 ordering gold**。
- **FlashInfer FP8-KV prefill 曾为整块 table 分配 dequant scratch**（vLLM **#57918**, created **2026-09-21**）：65 token / 16-page 场景 256 列 table 曾分配 **257** 临时页 → 现按 host seq bound 至 **6** → **长 prefix + FP8 KV 的 token/显存预算须按 seq 而非 table capacity 建模**。

## 1) Agent工程化（high-signal）

- [feat(langchain): add tool-call request ordering capabilities（LangChain PR #40695, created **2026-09-21**）](https://github.com/langchain-ai/langchain/pull/40695)
  - 摘要: `wrap_tool_call` 新增 opt-in 能力位：`may_modify_request`（如 HITL 改 tool/args）与 `requires_final_request`（如 Auto Mode）；`create_agent()` 在「需最终请求」外包「会改请求」时 **启动即拒绝**。
  - 为什么重要(对我): **skill 落地 + 评测**——middleware 顺序从「约定」变成 **可静态检查** 的契约。
  - 明日动作(<=20min): 写 **1 条** gold：HITL 在 Auto Mode 前/后各一种，期望路由是否一致（≤15 词）。

- [fix(typesafe): classify final tool call requests（LangChain PR #40696, created **2026-09-21**）](https://github.com/langchain-ai/langchain/pull/40696)
  - 摘要: Auto Mode 改为在 **最终** `ToolCallRequest` 上分类；依赖 #40695，禁止在声明 `requires_final_request` 的 middleware 之后再放会改 request 的 wrapper（`ToolRetryMiddleware` 仍可在外）。
  - 为什么重要(对我): **评测 + token**——避免「分类一次、执行另一次」的 **双重 TypeSafe 调用**。
  - 明日动作(<=20min): 写 **是/否**：你的 agent 是否 HITL + Auto Mode 叠放（是/否/未知）。

- [fix(core): drop `index` when the stream sweep finalizes a client tool_call（LangChain PR #40697, created **2026-09-21**）](https://github.com/langchain-ai/langchain/pull/40697)
  - 摘要: `_sweep_chunk_store` 对 client `tool_call` 现与 `_compat_bridge` 一致丢弃 `index`；`block-delta` 路径曾泄漏 `index` 导致 `merge_lists` `TypeError`（4 新测，core **2286 passed**）。
  - 为什么重要(对我): **streaming tool eval**——与 0919 vLLM reasoning/structured 分维测同族：**chunk 边界即契约**。
  - 明日动作(<=20min): 写 **是/否**：eval 是否用 OpenAI-style `block-delta` tool stream（是/否/未知）。

- [fix(core): decode escaped JSON Pointer tokens in schema references（LangChain PR #40699, created **2026-09-21**）](https://github.com/langchain-ai/langchain/pull/40699)
  - 摘要: `convert_to_openai_tool()` 对 `$ref` 内 `~1`/`~0` 按 RFC 6901 解码，`location/city` 等定义不再 `KeyError` 或指错 key。
  - 为什么重要(对我): **tool schema + eval**——带斜杠的 JSON Schema 是 **负例/正例 gold** 常见漏点。
  - 明日动作(<=20min): 写 **1 个** 含 `~1` 的 `$ref` 是否进 tool 转换测试（是/否）。

- [fix(typesafe): preserve model fallback routing（LangChain PR #40701, created **2026-09-21**）](https://github.com/langchain-ai/langchain/pull/40701)
  - 摘要: `ModelRouterMiddleware` + `ModelFallbackMiddleware` 时 fallback 请求不再被 router 覆盖；按 LangGraph node attempt 跟踪原请求，retry 仍走 routed model。
  - 为什么重要(对我): **skill 落地**——多模型 agent 的 **fallback eval** 须与 retry 分轨断言。
  - 明日动作(<=20min): 写 **是/否**：是否 Router+Fallback 叠放（是/否/未知）。

- [fix(typesafe): reject empty `questions` in `TypeSafeClassifier`（LangChain PR #40707, created **2026-09-21**）](https://github.com/langchain-ai/langchain/pull/40707)
  - 摘要: 动态构造空 `questions` 映射时现 **本地 ValueError**，不再打到 HTTP API（fixes #40706）。
  - 为什么重要(对我): **评测 fail-fast**——与 0919 router 无 user turn 命名错误同类。
  - 明日动作(<=20min): 写 **1 条** negative gold：空 questions 期望异常类型（≤8 词）。

- [fix(checkpoint): extract array paths in multi-field store selectors（LangGraph PR #9026, created **2026-09-21**）](https://github.com/langchain-ai/langgraph/pull/9026)
  - 摘要: `{items[*].text,title}` 等多字段 selector 曾 **静默省略数组值**；现复用 path traversal，覆盖 wildcard/负索引/`InMemoryStore.put()` embedding 路径。
  - 为什么重要(对我): **RAG store + skill 记忆**——store 索引错误会污染 **检索 eval** 而非 crash。
  - 明日动作(<=20min): 写 **是/否**：store selector 是否含 `[*]` 数组路径（是/否/未知）。

- [fix(checkpoint): bind encrypted data to row identity（LangGraph PR #9027, created **2026-09-21**）](https://github.com/langchain-ai/langgraph/pull/9027)
  - 摘要: 加密 checkpoint 现以 AEAD AAD 绑定 thread/checkpoint/channel 身份，防 **跨行密文替换**（fixes #9004；PG/SQLite 全量测通过）。
  - 为什么重要(对我): **HITL + 多租户 eval**——加密 checkpoint 的 **完整性** 应进安全 gold。
  - 明日动作(<=20min): 写 **是/否**：是否启用 encrypted serializer（是/否/未知）。

- [fix(agent): handle chat_history without user messages in BaseWorkflowAgent.init_run（LlamaIndex PR #23154, created **2026-09-21**）](https://github.com/run-llama/llama_index/pull/23154)
  - 摘要: 仅 assistant 欢迎语等无 `user` 的 `chat_history` 曾 `user_hist[-1]` `IndexError`；现回退到最新非 system 消息（fixes #23153）。
  - 为什么重要(对我): **多轮 agent eval**——resume/bot-greeting 是 **L2 入口** 常见场景。
  - 明日动作(<=20min): 写 **1 条** gold：无 user 仅有 assistant 开场是否应可 init（是/否）。

- [fix(core): keep repacked sub-chunks in order in the Refine loop（LlamaIndex PR #23158, created **2026-09-21**）](https://github.com/run-llama/llama_index/pull/23158)
  - 摘要: `Refine` 对 oversized chunk repack 后用 `extendleft` 导致 **子块逆序 refine**；`repacked[::-1]` 修复 sync/async 两路径（fixes #23155；#23156/#23157 同修复，本日只收一条）。
  - 为什么重要(对我): **RAG + token**——逆序 refine 浪费 token 且扭曲 **顺序敏感文档** 的答案 gold。
  - 明日动作(<=20min): 写 **是/否**：是否用 `Refine`/`COMPACT` repack（是/否/未知）。

- [fix(core): preserve primitive validation in JSON Schema unions（AutoGen PR #8267, created **2026-09-21**）](https://github.com/microsoft/autogen/pull/8267)
  - 摘要: `schema_to_pydantic_model()` 对 `anyOf`/`oneOf` 中 integer/string 分支曾丢 `minimum`/pattern 等；现保留 primitive 校验（16 新测）。
  - 为什么重要(对我): **tool 参数 eval**——与 0920 #8264 array items 互补；union 是 **边界 gold** 温床。
  - 明日动作(<=20min): 写 **是/否**：工具字段是否 `int|null` 带 `minimum`（是/否/未知）。

- [[Frontend] Add streaming parity tests and docs for derender（vLLM PR #57922, created **2026-09-21**）](https://github.com/vllm-project/vllm/pull/57922)
  - 摘要: RFC #47161 PR3：对 `stream=true` chat completions 按 chunk 边界喂 `/derender`，断言 content/reasoning/tool_calls/finish_reason 与引擎一致；含 spec-decode 形 `stream_interval=4` 与尾 chunk 仅 finish_reason。
  - 为什么重要(对我): **评测 harness**——derender 是 **token-id→语义** 对齐层，应纳入 streaming L2。
  - 明日动作(<=20min): 写 **是/否**：eval 是否比对 derender 与 chat stream（是/否/未知）。

- [[Docs][KV Cache] Clarify external cache reset semantics（vLLM PR #57907, created **2026-09-21**）](https://github.com/vllm-project/vllm/pull/57907)
  - 摘要: `POST /reset_prefix_cache` 默认只清 **本地** prefix；connector 层须 `reset_external=true`；文档说明 `success` 含义与 benchmark 用 `cache_salt`（fixes #57727）。
  - 为什么重要(对我): **benchmark/eval 门禁**——误以为「reset=冷启动」会污染 **prefix 复用与 token 成本** 指标。
  - 明日动作(<=20min): 写 **1 句**：你的 eval 脚本 reset 后是否设 `reset_external`（≤12 词）。

- [[Perf][Spec Decode] Filter rejection sampler output rows in Python instead of per-row NumPy（vLLM PR #57888, created **2026-09-21**）](https://github.com/vllm-project/vllm/pull/57888)
  - 摘要: `RejectionSampler.parse_output` 曾每行 NumPy fancy index；现整批转 Python 过滤，logprobs off 时去掉 `valid_mask` 分配——**每 spec step 全 batch** 热路径。
  - 为什么重要(对我): **token 优化 + serving**——spec decode 的 CPU 开销影响 **agent TPOT** 与压测可比性。
  - 明日动作(<=20min): 写 **是/否**：线上是否开 spec decode（是/否/未知）。

- [Fix: Async EPLB silently drops batches during MoE offloading（vLLM PR #57905, created **2026-09-21**）](https://github.com/vllm-project/vllm/pull/57905)
  - 摘要: MoE offload 下 async EPLB 曾 **静默丢 batch**（fixes #57883）：解开 collective 门控并为 `event.wait()` 加 timeout 防挂死。
  - 为什么重要(对我): **评测可复现**——rank 间 batch 不一致会导致 **轨迹 eval 随机失败**。
  - 明日动作(<=20min): 写 **是/否**：是否 MoE + EPLB/offload（是/否/未知）。

- [[KV Offload][FS] Opt-in per-block CRC32C checksums（vLLM PR #57912, created **2026-09-21**）](https://github.com/vllm-project/vllm/pull/57912)
  - 摘要: FS tier 可选 `checksum_blocks`：xattr 存 CRC32C，load 失败删块并计 `checksum_failures`；无 checksum 旧块仍可读（#54363 item 1）。
  - 为什么重要(对我): **长对话 KV offload eval**——静默坏块会破坏 **prefix 命中 gold**。
  - 明日动作(<=20min): 写 **是/否**：是否 FS KV offload（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Perf][Attention] Bound FlashInfer prefill dequantization scratch（vLLM PR #57918, created **2026-09-21**）](https://github.com/vllm-project/vllm/pull/57918)
  - 摘要: BF16/FP16 Q + FP8 KV 的 FlashInfer prefill fallback 曾按 block-table **列容量** 分配 dequant 页；现按 host seq bound（含 cached prefix），单 prefill 65 token 例：256 列时 **257→6** 临时页。
  - 为什么重要(对我): **Talk ≥3 turns + KV/Attention**——prefill 显存与 **长 history** agent 容量直接相关。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 FP8 KV + 长 prefix prefill（是/否/未知）。

- [[Bugfix][Mamba] Preserve aligned prefix-cache snapshots with CoW（vLLM PR #57908, created **2026-09-21**）](https://github.com/vllm-project/vllm/pull/57908)
  - 摘要: align-mode Mamba 在 **满对齐 prefix hit** 或 producer 发布边界后仍原地写共享 state → cache hash 与真实 recurrent state 脱节；现对全对齐 state 扩展 CoW（resume 私有块 / producer 发布 cache-owned 块）。
  - 为什么重要(对我): **Talk ≥3 turns + 非 Transformer KV**——Mamba/SSM agent 的 **prefix cache 语义** 与 Transformer 不同，eval 须分模型族。
  - 明日动作(<=20min): 写 **是/否**：是否 Mamba + prefix caching（是/否/未知）。

- [[Perf][HiSparse] Avoid repeated prefix scans and residency updates（vLLM PR #57930, created **2026-09-21**）](https://github.com/vllm-project/vllm/pull/57930)
  - 摘要: HiSparse 每步从 0 重扫已 durable 的 prefix；20 个 resident group × 数百请求可占满 scheduler CPU。现从 `max(ready_prefix_pages, importing_pages)` 起扫，共享 materialization 仅首 manager 调用。
  - 为什么重要(对我): **Talk ≥3 turns + sparse/prefix Attention**——调度 CPU 瓶颈会放大 **多轮并发** 的 tail latency。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 HiSparse + 高并发（是/否/未知）。

- [[Perf][HiSparse] Allow full graphs for P/D prompt-token replay（vLLM PR #57939, created **2026-09-21**）](https://github.com/vllm-project/vllm/pull/57939)
  - 摘要: P/D 全 prompt import 后仍 replay 末 token 出 logits；该步被标 logical prefill 导致整批掉出 FULL CUDA graph；现 HiSparse+P/D 下 **单 token replay 按 decode 形** 进 graph dispatch。
  - 为什么重要(对我): **Talk ≥3 turns + decode Attention**——disagg agent 的 **末 token replay** 是 TTFT/TPOT 隐藏项。
  - 明日动作(<=20min): 写 **是/否**：是否 P/D disagg serving（是/否/未知）。

- [[ROCm] Dspark MLA module（vLLM PR #57942, created **2026-09-21**）](https://github.com/vllm-project/vllm/pull/57942)
  - 摘要: Kimi-K3 DSpark drafter 在 ROCm 改走 `kimi_k3/amd/dspark_mla`（`K3DSparkModel`），draft 用 `MLAAttention` + `concat_and_cache_mla`，拒绝 `mla_use_output_gate`；CUDA 路径不变。
  - 为什么重要(对我): **Talk ≥3 turns + MLA**——spec decode draft 与主模型 **共享 latent KV 抽象**，影响多轮草稿可见性。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 Kimi-K3 spec decode（是/否/未知）。

- [[Spec Decode] Support Kimi-K3 MLA drafts in DFlash2（vLLM PR #57902, created **2026-09-21**）](https://github.com/vllm-project/vllm/pull/57902)
  - 摘要: `lightseekorg/kimi-k3-dflash2`：`dflash_config.attention_mode: mla` 加载 MLA backbone；含 latent context KV 预计算、sliding-window 非因果 Triton MLA、全层 FlashInfer MLA 默认。
  - 为什么重要(对我): **Talk ≥3 turns + Attention 变体**——draft 块内 **非因果可见** 改变接受率与多轮一致性。
  - 明日动作(<=20min): 写 **是/否**：eval 是否测 MLA spec decode（是/否/未知）。

- [[Perf][ROCm][Kimi-K3] Fuse all-reduce with RMSNorm in latent-MoE（vLLM PR #57920, created **2026-09-21**）](https://github.com/vllm-project/vllm/pull/57920)
  - 摘要: Latent-MoE tail 的 cross-device AR + RMSNorm 可走 `rocm_aiter_fused_allreduce_rmsnorm`（`can_fuse_allreduce_rms_norm` 守卫 + 单测）。
  - 为什么重要(对我): **Talk ≥3 turns + MoE/Norm**——多轮 decode 每步 norm+comm 融合影响 **路由稳定与延迟**。
  - 明日动作(<=20min): 写 **≤15 词**：是否 ROCm Kimi-K3 latent MoE（是/否/未知）。

## 3) 热门项目/可试个人项目

- [[Docs] Add an Engram feature page explaining Engram usage in vLLM（vLLM PR #57910, created **2026-09-21**）](https://github.com/vllm-project/vllm/pull/57910)
  - 摘要: 新增 `docs/features/engram.md`：n-gram embedding lookup、DSv4.1/Qwen4Exp 启用方式、`--engram-config`（含 cpu_offload 等）说明。
  - 为什么重要(对我): **skill 落地**——可把「Engram 是否开启/ offload」写成 **serving recipe checklist**（类比 0920 model recipes skill）。
  - 明日动作(<=20min): 读该页 **1 节**，写 **1 句** 对你当前模型是否相关。
  - 估计复现成本: **低**（读文档 + 对照一条 launch flag，~20min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [On-demand conversation compaction（Anthropic API Docs + Release notes **2026-09-14**）](https://docs.anthropic.com/en/docs/build-with-claude/compaction)
  - 机会信号: Messages API `compaction` + `compact-2026-09-04` beta → **客户自管长对话 token 账单**；与 Managed Agents session-hour 计价形成 **「压缩 vs 托管」** 产品组合叙事，适合出海咨询贴「agent 成本拆解」。
  - 风险信号: 压缩时机与 **thinking block 绑定**、cache 失效策略须自测；误用 compaction 会伤 **多轮 eval 可复现**。
  - 我可执行的一步(<=30min): 写 **3 行**：你的 agent 场景 / 是否适合 on-demand compact / 主要风险（每格 ≤8 词）。

- [Compliance API — Claude in Chrome session transcripts（Anthropic Release notes **2026-09-18**）](https://docs.anthropic.com/en/release-notes/api)
  - 机会信号: Enterprise `read:compliance_user_data` 现覆盖 **Chrome 扩展会话** transcript → 出海 Enterprise 售前可讲 **「browser agent 合规审计」** 差异化（非仅 API log）。
  - 风险信号: 仅 Enterprise + 合规密钥；**browser agent** 数据驻留与地域条款仍须法务框定。
  - 我可执行的一步(<=30min): 写 **1 句**：你手头项目 closest analog 是 API agent 还是 browser surface（≤20 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[3499. Find X Value of Array I](https://leetcode.com/problems/find-x-value-of-array-i/)（**2026-09-21 Daily Challenge**，Medium，LeetCode GraphQL `activeDailyCodingChallengeQuestion`）：子数组乘积模运算；与 **按段聚合 trace metric（模/溢出边界）** 同构。明日动作：一题或写 **≤15 词**「模运算分段」对你 L2 聚合意味着什么。

- **IELTS（≤1 条）**：[IELTS Listening practice tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/listening)：0920 为 Reading，今日 **Listening 一组（~20min 计时）**。明日动作：写 **2 个** 失误类型（拼写/单复数/数字）；**≤15 词** 最弱 Listening 子技能。

## 6) 噪音过滤记录

- **LangChain #40705 model profiles refresh**：维护数据 → 过滤（0919 #40665 同族）。
- **LlamaIndex #23156/#23157**：与 #23158 同 issue #23155 → semantic duplicate，只收 #23158。
- **vLLM #57925 RDNA3 tracking DO NOT MERGE**：非可落地变更 → 过滤。
- **vLLM #57940 P/D disagg WIP**：未完成 → 过滤（#57939 已覆盖 replay/graph 信号）。
- **vLLM #57921 routed_experts docstring**：_docs-only 弱动作 → 过滤（#57922 已覆盖 eval 向 derender）。
- **vLLM #57928/#57924 Nemotron VL MM**：多模态非今日 focus → 过滤。
- **vLLM #57945 CUDA12 KV connector build**：构建修复 → 过滤。
- **Anthropic Sep 14 compaction**：未重复进 §1（非当日 PR），仅在 §4 作职业/产品信号引用。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-21** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + Anthropic Docs 当日 ≥8 条主信号，无需 skill 补充。
- **openai/evals**：2026-09-21 无新 PR → 不收录。
- **Simon Willison / Lil'Log**：未发现 **2026-09-21** 新帖可验证链接 → 不收录。

## 7) 本周累计计数

- Agent: **1062**（`0920.md` §7: 1046 + 本日 §1: 16）
- Theory: **489**（`0920.md` §7: 482 + 本日 §2: 7）
- Career/Business: **156**（`0920.md` §7: 154 + 本日 §4: 2）
- LC: **134**（`0920.md` §7: 133 + 本日 §5: 1）
- IELTS: **134**（`0920.md` §7: 133 + 本日 §5: 1）
