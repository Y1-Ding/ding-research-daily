# Daily Research - 2026-09-13 (21:30)

## 0) 今日3条结论

- **ToolStrategy 与同级普通 tool call 同批返回时，structured output 校验失败不会触发重试**（LangChain #40430, created **2026-09-13**）：`_make_tools_to_model_edge` 误把 validation-error `ToolMessage` 当成已接受 structured output → **评测须覆盖「structured-output tool + 普通 tool 同 AIMessage」的 batch 路径，不能只测单 tool。**
- **LangGraph SQLite store 未 honor `TTLConfig.omit_expired`**（#8905, created **2026-09-13**）：sweeper 未删前过期行仍被 `get`/`search` 返回，与 Postgres store 行为不一致 → **agent 长期记忆 eval 须测 TTL 窗口内 read path，不能假设过期即不可见。**
- **DFlash speculative decode 把 `sliding_window_non_causal=False` 误映射为全局 causal**（vLLM #56692, created **2026-09-13**）：full-attention 层在 draft block 内应为 bidirectional → **Attention + spec-decode eval 须按 layer type 分别 pin causality mask，不能用一个全局 flag。**

## 1) Agent工程化（high-signal）

- [fix(langchain): retry invalid structured output when a sibling tool call ran in the same batch（LangChain PR #40430, created **2026-09-13**）](https://github.com/langchain-ai/langchain/pull/40430)
  - 摘要: Fixes #40423。模型在同一 `AIMessage` 中同时返回 **invalid** structured-output tool call（`ToolStrategy`, `handle_errors=True`）与普通 tool call 时，agent loop 在第一次 model call 后即结束：validation-error `ToolMessage` 未回灌模型、无 retry、`structured_response=None`。根因：`factory.py` 中 exit condition 3 只要 trailing `ToolMessage` 带 structured-output tool 名就 `end_destination`，无法区分「已接受」与「被拒绝待重试」。Fix：仅当 `state.get("structured_response") is not None` 时才走该 exit，与 model-to-model edge 一致。
  - 为什么重要(对我): **评测 + skill 落地**——mixed-batch structured output retry 是 agent platform eval 的 ToolStrategy gold；须测 sibling ordinary tool call 不阻断 validation retry。
  - 明日动作(<=20min): 写 **是/否**：你的 agent eval 是否测「structured output 失败 + 同级普通 tool」同批（是/否/未知）。

- [fix(checkpoint-sqlite): honor TTLConfig.omit_expired in store read paths（LangGraph PR #8905, created **2026-09-13**）](https://github.com/langchain-ai/langgraph/pull/8905)
  - 摘要: Fixes #8902。`SqliteStore`/`AsyncSqliteStore` 声明 `supports_ttl=True` 但未读 `omit_expired`；`omit_expired=True` 时 sweeper 未删的过期行仍被 `get`/`search`/`list_namespaces` 返回。Fix：在 `BaseSqliteStore` 加 `_omit_expired`（对齐 Postgres），GET/search 查询追加 `(expires_at IS NULL OR expires_at > CURRENT_TIMESTAMP)`。
  - 为什么重要(对我): **skill 落地 + 评测**——checkpoint/store TTL read semantics 是 long-running agent eval 的 memory-freshness gold；SQLite vs Postgres 须行为一致。
  - 明日动作(<=20min): 写 **是/否**：你的 agent memory eval 是否测 `omit_expired` 读路径（是/否/未知）。

- [fix(vertexai-search): preserve structured result provenance（LlamaIndex PR #23038, created **2026-09-13**）](https://github.com/run-llama/llama_index/pull/23038)
  - 摘要: Fixes #21933。结构化 Vertex AI Search 结果原先丢弃源身份与 metadata。Fix 在 node metadata 保留 `struct_data` 字段及 Discovery Engine `document_id`/`document_name`；JSON text、排序、rank score 不变；canonical identity 优先于冲突 struct 字段。
  - 为什么重要(对我): **评测 + RAG agent**——retrieval provenance 是 agent eval 的 citation/trace gold；须测 structured search 结果可回溯 `document_id`。
  - 明日动作(<=20min): 写 **是/否**：你的 RAG agent eval 是否断言 retrieval `document_id`（是/否/未知）。

- [fix(core): refresh agent state prompt after tool updates（LlamaIndex PR #22249, updated **2026-09-13**）](https://github.com/run-llama/llama_index/pull/22249)
  - 摘要: Fixes #22248。`FunctionAgent`/`AgentWorkflow` 用 `initial_state` + `state_prompt` 时，tool  mutate `ctx.store["state"]` 后下一步 LLM 仍看到旧 state：因 `formatted_input_with_state` 在 tool results 处理后未重置。Fix：单/多 agent 路径在 tool results 处理后重置 flag；state prompt 在 input message 深拷贝上格式化，不污染 memory-backed `ChatMessage`。
  - 为什么重要(对我): **skill 落地 + 评测**——tool-driven state mutation visibility 是 agent eval 的 state-sync gold；须测 tool 改 state 后下一步 prompt 含新 state。
  - 明日动作(<=20min): 写 **是/否**：你的 agent eval 是否测 tool 后 state_prompt 刷新（是/否/未知）。

- [fix(core): validate streamed chat-with-tools responses at stream end（LlamaIndex PR #22740, updated **2026-09-13**）](https://github.com/run-llama/llama_index/pull/22740)
  - 摘要: Fixes #22721。`stream_chat_with_tools`/`astream_chat_with_tools` 原带 `# TODO: no validation` 字面返回 raw stream；非流式路径会 `_validate_chat_with_tools_response`（各 provider 在 `allow_parallel_tool_calls=False` 时 `force_single_tool_call`）。Fix：wrapper generator 流结束后对最终 cumulative `ChatResponse` 跑同样 validation，chunk 透传不变。
  - 为什么重要(对我): **评测 + token 路径**——streaming vs non-streaming tool-call normalization 一致性是 agent eval 的 delivery-mode gold；须测 stream 结束后的 tool call 形态与非流式一致。
  - 明日动作(<=20min): 写 **是/否**：你的 tool-call eval 是否对比 stream/end vs invoke（是/否/未知）。

- [feat(agentchat): SelectorGroupChat concurrent speakers（AutoGen PR #8206, updated **2026-09-13**）](https://github.com/microsoft/autogen/pull/8206)
  - 摘要: Fixes #5395。`selector_func`（及 `max_concurrent_speakers`>1 时的 model selection）可返回多个 speaker；manager state 持久化 `previous_speakers`；`BaseGroupChatManager` 修复 termination 条件接收完整 concurrent-turn delta（非仅最后完成的 speaker）。含 concurrent speakers regression test。
  - 为什么重要(对我): **skill 落地 + 评测**——multi-agent concurrent turn 是 group-chat agent eval 的 termination/trace gold；须测并行 speaker 的 stop condition 与消息 delta。
  - 明日动作(<=20min): 写 **是/否**：你的 multi-agent eval 是否测 concurrent speakers（是/否/未知）。

- [fix(openrouter): pass strategy="none" RetryConfig when max_retries=0（LangChain PR #40427, created **2026-09-13**）](https://github.com/langchain-ai/langchain/pull/40427)
  - 摘要: Fixes #40379。`ChatOpenRouter(max_retries=0)` 时 `_build_client` 未设 `retry_config`，SDK 将 `UNSET` 解释为默认 backoff（最长 **1 小时**重试），与 caller 意图相反。Fix：`max_retries==0` 时传 `RetryConfig(strategy="none", ...)`。
  - 为什么重要(对我): **token 优化 + 评测**——显式 zero-retry 的 latency/cost bound 是 agent eval 的 provider-config gold；须 pin `max_retries=0` 时无隐式 backoff。
  - 明日动作(<=20min): 写 **是/否**：你的 agent eval 是否 pin OpenRouter `max_retries`（是/否/未知）。

- [Generating running routes with GPT-6 Astra and ChatGPT Work（Simon Willison Blog, **2026-09-12**）](https://simonwillison.net/2026/Sep/12/astra-running-routes/)
  - 摘要: ChatGPT Work + GPT-6 Astra 用 Nominatim/Overpass 在本地算 5K/10K 跑步环线（~27min），产出可视化与 GPX/GeoJSON；线程 compaction 后无法找回当时运行的 Python 代码。Simon 称 compaction 不保留 pre-compacted 文本、不可审计是 **anti-feature**，agent 系统应保留并可检索 pre-compaction trace。
  - 为什么重要(对我): **skill 落地 + token 优化 + 评测**——context compaction 与可审计 trace 的权衡是 long-horizon agent eval 的 compaction-gold；可脱敏设计「compaction 后仍可 fetch pre-compact tool/code log」。
  - 明日动作(<=20min): 写 **1 条** compaction 对你 agent trace eval 的接点（≤20 词）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix][Spec Decode] Keep full attention non-causal in Speculators DFlash（vLLM PR #56692, created **2026-09-13**）](https://github.com/vllm-project/vllm/pull/56692)
  - 摘要: Speculators-format DFlash checkpoint 加载时，`sliding_window_non_causal=False` 被映射为全局 `dflash_config.causal=True`，使 full-attention 层在 draft block 内也变成 causal；训练侧 full attention 在 block 内为 bidirectional。Fix：对 causal SWA 用 layer-type 默认，保留 `True`/省略字段的全局 non-causal 行为；DFlash2 共享 converter。
  - 为什么重要(对我): **Talk ≥3 turns + Attention mask**——mixed-layer spec-decode causality 是 draft-model eval 的 mask-contract gold；须按层类型断言 bidirectional vs causal。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 DFlash/speculator draft serving（是/否/未知）。

- [[Bugfix][Core] Profile and validate kv_cache_memory_bytes instead of skipping the profile（vLLM PR #56698, created **2026-09-13**）](https://github.com/vllm-project/vllm/pull/56698)
  - 摘要: `--kv-cache-memory-bytes` 分支原先跳过 `memory_profiling`，未校验 pinned size 是否容纳 model weights + activation peak → 短请求正常、流量峰值时 OOM。`gpu_memory_utilization` 有 fractional slack 掩盖问题；pinned size 无 slack。Fix 对该路径做 profile/validate；并讨论 `skip_attn=True` 时 attention activation 未计入的问题。
  - 为什么重要(对我): **Talk ≥3 turns + KV/Attention memory**——pinned KV budget vs activation peak 是 serving eval 的 memory-gold；须测长 prompt/高并发下 OOM 边界。
  - 明日动作(<=20min): 写 **≤15 词**：你是否使用 `--kv-cache-memory-bytes`（是/否/未知）。

- [[Core] Implement kv compression algorithms（vLLM PR #56673, updated **2026-09-13**）](https://github.com/vllm-project/vllm/pull/56673)
  - 摘要: 实现 eviction/filtering 类 KV cache compression，目标包括 [KeyDiff](https://arxiv.org/abs/2504.15364)。新增 `tests/v1/worker/test_kv_compression_standalone.py`、`tests/v1/core/test_kv_compression_scheduler.py`；作者称现有测试覆盖无回归。
  - 为什么重要(对我): **Talk ≥3 turns + KV compression**——KeyDiff 类压缩是长上下文 Attention 的 memory/latency tradeoff；agent eval 若测长 trace 须 pin compression 算法与质量回归。
  - 明日动作(<=20min): 写 **≤15 词**：你是否长上下文 KV compression（是/否/未知）。

- [[perf] DeepSeek-V4.1-Flash sparse-attention candidate-block kernels（vLLM PR #56686, created **2026-09-13**）](https://github.com/vllm-project/vllm/pull/56686)
  - 摘要: 实验性 profiling PR：B200×8 TP 上 `vllm bench throughput`（ShareGPT），DeepSeek-V4.1-Flash 1000 req 耗时 24.6s→22.9s；主要收益来自按 **1M-token limit** 定尺寸的 sparse-attention candidate-block kernels。记录 env（torch 2.13、flashinfer 0.6.18 等）与瓶颈假设，欢迎讨论。
  - 为什么重要(对我): **Talk ≥3 turns + sparse Attention**——million-token sparse attention kernel sizing 是 throughput eval 的 backend-knob gold；须 pin token limit 与 kernel 版本再比 accuracy。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 sparse-attention 长上下文 bench（是/否/未知）。

- [fix: TextBlock.amerge no longer emits an empty block on oversized first split（LlamaIndex PR #22247, updated **2026-09-13**）](https://github.com/run-llama/llama_index/pull/22247)
  - 摘要: `TextBlock.amerge` 在首 split 已超 `chunk_size` 且 accumulator 为空时仍 flush，产生空 `TextBlock`；最终 flush 与 `BaseRecursiveContentBlock.amerge` 均有非空 guard。Fix 对齐非空检查，避免空 block 进入 `ChatMessage.blocks` 并被部分 provider 拒绝。
  - 为什么重要(对我): **Talk ≥3 turns + 多模态/长文本分块**——streaming merge 空块是 token/chunk pipeline 的 edge-case gold；长上下文 agent 须测 oversized-first-split 路径。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 `TextBlock.amerge` 长消息路径（是/否/未知）。

## 3) 热门项目/可试个人项目

- [joshuaswarren/remnic — local-first agent memory + evals + MCP（203 stars, updated **2026-09-13** GitHub API）](https://github.com/joshuaswarren/remnic)
  - 摘要: MIT；markdown+YAML frontmatter 本地记忆；hybrid BM25+vector+rerank、graph recall、per-result provenance；支持 OpenClaw/Cursor/MCP 等共享 store；含 memory-worth scoring 与 eval 文档；npm `@remnic/cli`。
  - 为什么重要(对我): **skill 落地 + 评测**——「scoped memory + provenance + eval」与 internal agent memory eval 脚手架同构；可对照你的 L2 trace/citation 需求做 gap 分析。
  - 明日动作(<=20min): 读 README「Sharp retrieval」一节，写 **1 条** provenance 字段对你 RAG eval 的启发（≤20 词）。
  - 估计复现成本: **低**（`npm i -g @remnic/cli` + 本地 markdown store smoke，~30min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Compaction hides agent code from users（Simon Willison Blog, **2026-09-12**）](https://simonwillison.net/2026/Sep/12/astra-running-routes/)
  - 机会信号: 长时程 consumer agent（Astra/Work）已暴露 **compaction vs auditability** 产品缺口 → **「compaction-safe agent observability」** 可作 B2B/agent-platform 小切口（脱敏对标 internal trace eval）。
  - 风险信号: 用户无法复现/审计 27min agent 任务中的代码与工具链；compaction 默认丢弃 pre-compact 上下文；信任与合规叙事受损。
  - 我可执行的一步(<=30min): 列 **5 条** compaction 后仍须保留的 trace 字段 checklist v0.1（tool name、args hash、pre-compact log pointer 等），写入 playbook 接点。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[835. Image Overlap](https://leetcode.com/problems/image-overlap/)（**2026-09-13 Daily Challenge**，verified via LeetCode GraphQL `activeDailyCodingChallengeQuestion`，Medium）：关键洞察——将二维图像重叠转为卷积/滑动窗口计数或 brute-force 平移枚举；与 agent eval「spatial pattern match / submatrix scoring」同构。明日动作：完成 Medium 一题；写 **≤15 词**：2D pattern match 对你 tool-output verification 意味着什么。

- **IELTS（≤1 条）**：[IELTS Listening Practice Tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/listening)：0912 已轮换 Writing，今日 **Listening**（选 1 套 Section 1–4，限时 ~30min）。明日动作：完成 1 套 Listening；记录 **≥2 个**失误类型（数字听写 / 同义替换 / 地图题）；写 **≤15 词**你最弱的 Listening 题型。

## 6) 噪音过滤记录

- **test(core): pipeline step chaining（#40429）**：unit test only → 过滤。
- **fix(core): v0 multimodal `id` known_keys（#40426）**：与 #40428 同主题，保留 #40428 → #40426 过滤。
- **chore(model-profiles) / docs OpenWiki（#40416/#40359）**：deps/docs → 过滤。
- **test(schema) text node hash（#23040）**：test only → 过滤。
- **docs/import paths / OCI README grammar（#23039/#23037）**：docs → 过滤。
- **[CI] H200 MIG / jit-monitor CI（#56695/#56693）**：infra → 过滤。
- **[Perf][Marlin] DS V4.1 MoE padding（#56697）**：MoE kernel → 弱于当前 Attention focus → 过滤。
- **0909–0912 已收录项**：LangGraph #8886、LangChain #40414/#40410、LlamaIndex #22992/#22788/#22258/#22256、AutoGen #7930、vLLM #46558/#56305/#52244/#56608/#56604、browser-use-pi、RubyGems 等 → semantic duplicate，不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-13** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + Simon Willison Blog ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **969**（`0912.md` §7: 961 + 本日 §1: 8）
- Theory: **443**（`0912.md` §7: 438 + 本日 §2: 5）
- Career/Business: **145**（`0912.md` §7: 144 + 本日 §4: 1）
- LC: **126**（`0912.md` §7: 125 + 本日 §5: 1）
- IELTS: **126**（`0912.md` §7: 125 + 本日 §5: 1）
