# Daily Research - 2026-07-11 (21:30)

## 0) 今日3条结论

- **LlamaIndex 同日两刀 token 观测漏洞（#22311 + #22315）**：Anthropic 流式 `usage` 丢弃 `cache_creation/cache_read` tokens；embedding batch 一处失败时 `asyncio.gather` 泄漏孤儿 API 调用 → skill/token 优化若只看 aggregate `input_tokens`，会系统性低估 prompt-cache 收益、高估失败路径成本。
- **vLLM 7/11 集中暴露 streaming tool-parser「单 delta 整包」破损族（#48343 + #48348）**：`stream_interval>1` 或 async scheduling 下，DeepSeek V3 截断非 `"}` 结尾的 JSON args、InternLM2 丢整包 tool call → agent eval trace 在 serving 层即已失真，需在 collect 阶段标注 parser/model 组合。
- **Harness MDP（arXiv 2607.05458）把「执行 harness」升格为可学习控制层**：冻结 LLM，离线 AW 学 structural actions（observe/retrieve/check/submit）；最终任务分与 Harness Maturity Score（HMS）可分离 → 你的 skill 落地应同时测「答案对」与「过程成熟」，而非只调 prompt。

## 1) Agent工程化（high-signal）

- [fix(anthropic): preserve cache_creation/cache_read tokens in streamed usage metadata（LlamaIndex PR #22311 opened **2026-07-11**）](https://github.com/run-llama/llama_index/pull/22311)
  - 摘要: `stream_chat`/`astream_chat` 每 chunk 的 `usage` 只含 `input_tokens`/`output_tokens`；Anthropic 在 `message_start` 报告 cache token 类、`message_delta` 不重复，delta handler 重建 usage 时丢弃 → 流式路径完全丢失 `cache_creation_input_tokens` 与 `cache_read_input_tokens`。Fix：在 `message_start` 捕获并 carry-forward 到最终 chunk。
  - 为什么重要(对我): **token 优化 + eval**——prompt caching 是长 skill prompt 的核心杠杆；streaming eval 若 usage 不含 cache 类，A/B 对比会误判 caching 无效，与 LangChain 0709/0710「账单/token 税」主线直接同构。
  - 明日动作(<=20min): 写 **是/否**：你 Anthropic agent 是否用 streaming 且读 `usage` 做成本归因（是/否/未测）。

- [fix: prevent orphaned embedding API calls in aget_text_embedding_batch（LlamaIndex PR #22315 opened **2026-07-11**）](https://github.com/run-llama/llama_index/pull/22315)
  - 摘要: `BaseEmbedding` 两层嵌套 `asyncio.gather`：batch 中一条文本失败时，兄弟 embedding 调用成为孤儿 task，无人 await/cancel → socket/FD 泄漏 + 隐性 API 计费。Fix：新增 `gather_with_bounded_lifetime()`，每条 coroutine 捕获异常、等全部 sibling 完成后再 re-raise 首个异常；同步覆盖 plain/tqdm 两路 gather（`tqdm_asyncio.gather` 不支持 `return_exceptions`）。
  - 为什么重要(对我): **token + eval**——RAG/skill collect 阶段 parallel embed 是常见路径；eval 只测 happy path 会漏掉「一条坏文档拖高全 batch 成本」；与 0710 #22298 parallel tool 孤儿 task 同族。
  - 明日动作(<=20min): 写 **是/否**：你 embedding batch 失败时是否断言无 pending asyncio task（是/否/未知）。

- [Learning to Control LLM Agent Harnesses with Offline Reinforcement Learning（arXiv **2607.05458**, July 2026）](https://arxiv.org/abs/2607.05458)
  - 摘要: 将 agent harness 形式化为有限视界 **Harness MDP**：轻量 controller 选 structural actions（observe/retrieve/call-tool/draft/check/revise/submit），LLM executor 冻结；离线 AW 训练，仅用终端 task-rubric reward。提出 **Harness Maturity Score（HMS）** 与最终任务分解耦——过程行为（如 CheckBeforeSubmit）可在 offline buffer 支撑不足时仍改善，但最终质量增益需高 return 轨迹支持。六域 + τ-bench/AgentBench 适配器验证；代码 https://github.com/Hik289/Agentic-RL-harness.git。
  - 为什么重要(对我): **skill 落地 + eval**——harness 是可学习层，不是固定 infra；直接映射 map_prompt_test：除 L2/L3 答案分外，应加 HMS 类过程指标（check-before-submit、evidence-grounding）。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否测 harness 过程分（有/无/仅 E2E）。

- [Separating signal from noise in coding evaluations（OpenAI Blog, **2026-07-08**）](https://openai.com/index/separating-signal-from-noise-coding-evaluations)
  - 摘要: 五名资深工程师 + agent investigator 审计 SWE-Bench Pro 731 任务；估计 **~30% 任务 broken**（ambiguous spec、环境/测试问题）；OpenAI **撤回**此前推荐采用 SWE-Bench Pro 的建议。方法论：给 investigator agent 仓库+环境，区分合理歧义 vs 真破损，多轮独立重复后人工裁决。
  - 为什么重要(对我): **eval 信号**——agent coding benchmark 分数通胀可能来自数据集噪声而非能力；你的 collect→test 应借鉴「agent 审计 + 人工 spot-check」筛 gold，避免 skill 优化追着坏题跑。
  - 明日动作(<=20min): 写 **≤15 词**：你 gold 集最近是否做过破损题审计（有/无/计划）。

- [[Bugfix][Tool Parser] DeepSeek V3: don't truncate final streamed tool arguments（vLLM PR #48343 opened **2026-07-11**）](https://github.com/vllm-project/vllm/pull/48343)
  - 摘要: 最终 args 与 `<｜tool▁call▁end｜>` 同 delta 到达时，parser 假设 args 以 `"}` 结尾重建 tail → number/boolean/nested object 结尾的 JSON 被截断（`{"code": 1` 而非 `123`）。Fix：用 `stream_tool_call_portion_regex` 从完整 tool-call 文本提取，emit `expected_args[len(already_streamed):]`。
  - 为什么重要(对我): **skill 落地 + eval**——streaming tool-call trace 是 L2 tool 计数与 args 校验的数据源；parser 截断 = eval 假阴性 + 无效 retry loop（token 税）。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否覆盖 streaming + `stream_interval>1`（是/否）。

- [[Bugfix][Tool Parser] InternLM2: stream tool calls that arrive whole in a single delta（vLLM PR #48348 opened **2026-07-11**）](https://github.com/vllm-project/vllm/pull/48348)
  - 摘要: 整包 tool call 单 delta 到达时：(1) name 分支 return 后 args 永不流出；(2) assistant content 与 tool call 同 delta 时 content 分支提前 return 丢整包 call。Fix：`is_complete_json` 时一次 emit name+full args（对齐 #47955 gigachat3、#48295 llama3_json 修法）。
  - 为什么重要(对我): **eval trace 完整性**——与 #48343 同属「单 delta 整包」破损类；multi-model eval harness 需按 parser 列已知破损矩阵。
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 是否开 async scheduling 或 `stream_interval>1`（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix] Fix level-2 sleep/wake crash with enable_lora=True（vLLM PR #48337 opened **2026-07-11**）](https://github.com/vllm-project/vllm/pull/48337)
  - 摘要: Level-2 sleep → `reload_weights()` 在 `enable_lora=True` 时 `KeyError`：LoRA wrap 使参数路径变为 `base_layer.weight`；且 `lora_a/b_stacked` 非 parameter、reload 后不恢复，GPU 上为未定义数据静默污染输出。Fix：`named_modules()` 展平 `base_layer`；`zero_lora_state()` 显式列表重零 stacked tensors；3× sleep/wake 回归 A100 验证输出一致。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) LoRA adapter reload 后 attention 权重与 stacked state 是否一致？(2) 与 0710 #48251 attention sink reload 是否同族「reload 后 state 不一致」？(3) RL/sleep-wake eval 是否分层报告 LoRA on/off？
  - 明日动作(<=20min): 写 **是/否**：你 serving 是否用 sleep/wake + LoRA（是/否/仅 cold start）。

- [[Bugfix] Disable FA3 AOT scheduling for spec-decode proposer（vLLM PR #48340 draft **2026-07-11**）](https://github.com/vllm-project/vllm/pull/48340)
  - 摘要: Spec-decode draft model 走 FA3 时，`get_scheduler_metadata` 硬编码 `is_varlen=true` 与 `mha_fwd` 实际 tensor shape 推导不一致 → Hopper GPU `CHECK_SHAPE` crash。Fix：speculator 传 `fast_build=True` 跳过 AOT scheduling，让 `mha_fwd` 内部分配 metadata。
  - 为什么重要(对我): **Attention 调度 + token**：spec-decode 是 agent serving 降延迟核心；FA3 AOT metadata 与 varlen 真值 divergence = attention kernel 层「元数据≠实际 KV 布局」，multi-turn 轨迹可能在中途 crash 而非软劣化。
  - 明日动作(<=20min): 写 **是/否**：你 agent serving 是否开 spec-decode + FA3（是/否/未知）。

- [Agentic Transformers Provably Learn to Search via Reinforcement Learning（arXiv **2606.00183**, May 2026）](https://arxiv.org/abs/2606.00183)
  - 摘要: 理论证明：在 stochastic k-ary tree 环境中，两 head transformer 可实现 randomized DFS——一头跟踪历史 action，一头检测失败并触发 backtrack；policy gradient + depth-wise curriculum 下该机制从稀疏 RL 反馈分阶段涌现，无需专家 demo；训练 depth-1/2 可泛化更深树；不平衡 goal 分布下 discount 导出 ranked DFS。
  - 为什么重要(对我): **Attention 机制 + Agent ≥3 turns**：attention head 功能分化（trace vs failure-detect）是 multi-turn search 的可解释 normal form；与 Harness MDP 的 structural actions、0709 PACE 的 turn 级控制形成「机制—控制—评测」三角。
  - 明日动作(<=20min): 写 **≤15 词**：你 ≥3 turn eval 是否测 backtrack 行为（有/无）。

## 3) 热门项目/可试个人项目

- [hwchase17/autoresearch-agents（GitHub, active 2026）](https://github.com/hwchase17/autoresearch-agents)
  - 摘要: Karpathy autoresearch 的 agent 版：固定 `run_eval.py` + `dataset.json`，让 coding agent 自主改 `agent.py`（prompt/tools/arch），LangSmith `evaluate()` 打分，keep/discard 写 `results.tsv`。`program.md` 即驱动自主实验的 skill 文件。
  - 为什么重要(对我): **eval 杠杆**——窄域 agent 可用「冻结 eval harness + 开放 agent 代码」做 overnight 优化；直接映射 map_prompt_test 的 collect 固定、test 自动迭代。
  - 明日动作(<=20min): clone 仓库；读 `program.md` 前 30 行；写 **≤15 词** 你最该 frozen 的 1 个 eval 文件。
  - 估计复现成本: **低**（LangSmith API + 小数据集；无需 GPU）。

- [Hik289/Agentic-RL-harness（arXiv 2607.05458 官方代码）](https://github.com/Hik289/Agentic-RL-harness)
  - 摘要: Harness MDP 离线 AW 训练实现；六控制域 + τ-bench/AgentBench 适配器；分离 task-rubric 与 HMS 过程分；ablation 对比 behavior cloning 与 Forced CHECK。
  - 为什么重要(对我): **skill + eval 复用**——可把 HMS 指标移植到你现有 gold 集，作为 L2 过程门禁的补充维度。
  - 明日动作(<=20min): 读 README；列 **2 条** HMS 事件你可映射到现有 eval（各 ≤10 词）。
  - 估计复现成本: **中**（需配环境 + offline rollout buffer；无 frontier 训练）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [ChatGPT is now a partner for your most ambitious work（OpenAI Blog, **2026-07-09**）](https://openai.com/index/chatgpt-for-your-most-ambitious-work/)
  - 机会信号: **全球一人公司基础设施成型**——ChatGPT Work 跨 app/file 长时 agent + GPT-5.6 多步推理；Pro→Plus 逐级 rollout = 付费 agent 市场从开发者扩到知识工作者；harness/eval 咨询可挂在「Work 完不成」的交付断点。
  - 风险信号: 平台内闭环（Work 在 ChatGPT 内）挤压第三方 agent 工具；长时 session 成本与隐私（跨 app 授权）是 adoption 摩擦；「hours-long agent」叙事需用自家 trace 验证而非 marketing。
  - 我可执行的一步(<=30min): 列 **3 条** 你当前项目里 Work 类 agent 做不了的断点（各 ≤12 词），标哪条可做对外 demo。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 3535 Unit Conversion II](https://leetcode.com/problems/unit-conversion-ii/)：单位换算构成有向树，从 0 号单位 BFS 预处理 `res[i]`，查询 `res[b]*inv(res[a]) mod 1e9+7；延续 3534 图系列，练「预处理 + 查询」范式。明日动作：先复习 3528/3534；再写 3535 核心 15 行（≤25min）；写 **≤15 词**：与 agent tool 链单位换算的类比。

- **IELTS（≤1 条）**：[Latest IELTS Speaking Topics 2026（EngVarta）](https://engvarta.com/latest-ielts-speaking-topics-with-sample-answers/)：Part 3 高分结构 **Point–Because–Example–Although**（主张→原因→例子→让步），比单句回答更能展示 GRA 复杂句。明日动作：选 1 个 Part 3 technology 题按 PBEA 录音 2min（≤20min）；写 **≤15 词** 你最弱 criterion（Fluency/LR/GRA/Pronunciation 选 1）。

## 6) 噪音过滤记录

- **LangGraph 1.2.9 / #8315 delta channel**：`0710.md` 已收录 → 不重复。
- **LlamaIndex #22299 state_prompt / #22298 gather leak / #22294 verdict-injection**：`0710.md` 已收录 → 不重复。
- **vLLM #48251 attention reload / #47896 TurboQuant KV**：`0710.md` 已收录 → 不重复。
- **arXiv 2607.07706 linear attention / 2607.04963 STAPO**：`0710.md` 或近三日已覆盖 → 不重复展开。
- **LangChain #38777 streaming empty-args**：`0710.md` 已收录且 PR closed（未 merge）→ 不重复。
- **LangGraph #8312 root messages inbox bounds**：`0709.md` 已收录，仍 open → 待 merge。
- **LangGraph CLI 0.4.31（2026-07-10）**：deps/prebuild 为主，弱于今日 token/eval focus → 丢弃。
- **LangChain #38797 model-profiles refresh**：infra chore → 丢弃。
- **LlamaIndex #22308/#22309 deps/docs**：chore → 丢弃。
- **vLLM #48344/#48346 KV revert、#48335 router GEMM、#48334 XPU FP8**：kernel/infra，弱于今日 parser + LoRA sleep → 不展开。
- **OpenAI GPT-5.6 细节 / Simon Willison 7/11 观鸟帖**：`0709.md` 已收 GPT-5.6；7/11 无技术帖 → 不收录。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-11** 原帖 URL → 不收录。
- **@gefei55 / @ianneo_ai 职业帖**：无法验证当日原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + OpenAI Blog + arXiv 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **503**（`0710.md` §7: 497 + 本日 §1: 6）
- Theory: **206**（`0710.md` §7: 203 + 本日 §2: 3）
- Career/Business: **90**（`0710.md` §7: 89 + 本日 §4: 1）
- LC: **72**（`0710.md` §7: 71 + 本日 §5: 1）
- IELTS: **72**（`0710.md` §7: 71 + 本日 §5: 1）
