# Daily Research - 2026-07-07 (21:30)

## 0) 今日3条结论

- **LangChain PR #38694 用 `chunk_position=="last"` 门控 `args=""` 的 streaming tool call（updated 2026-07-07）**：中间 chunk 不再物化 `args={}` 的空 tool call，直接修复 #38682 根因；L2 streaming eval 应与 prod 对齐「仅 last chunk 或 args 合并完成后才算可执行 call」。
- **SSRP 将 multi-turn agent 的「Attention Latch」形式化（arXiv 2604.24512）**：历史 intent 的 attention 权重压过中途更新 → ReAct baseline 在 3-hop synthesis 跌至 0.1%；Architect/Executive 分离 + 自合成 SOP 是 protocol 层 unlatch，与 Tool Attention 的 schema gating 互补。
- **LangGraph 1.2.8 发布 + #8290 修复 fresh-thread `DeltaChannel` 首写静默丢失（2026-07-06/07）**：`update_state` 现强制 snapshot 而非 stub checkpoint；skill state 初始化 eval 需断言单 checkpoint 形态，否则 collect→test 与 prod 历史形状分叉。

## 1) Agent工程化（high-signal）

- [fix(core): don't fire streaming tool calls on empty args before they arrive（LangChain PR #38694 updated **2026-07-07**）](https://github.com/langchain-ai/langchain/pull/38694)
  - 摘要: Fixes #38682。`AIMessageChunk.init_tool_calls` 原将 `args=""` 与 `args=None` 均解析为 `{}`，provider（OpenRouter、deepseek-v3.2 等）分片 SSE 时首 chunk 即暴露「完整」空 args tool call。Fix：仅当 `chunk_position=="last"` 才把 `args=""` 当零参；中间 chunk 的 `tool_calls` 为 `[]`。保留 partial JSON 渐进解析的既有行为；malformed args 仍进 `invalid_tool_calls`。213 单测通过。
  - 为什么重要(对我): **eval + skill**——0706 已记 #38682 症状；此 PR 是 collect→test 与 prod streaming 语义对齐的具体 patch，L2 step eval 应加 regression case。
  - 明日动作(<=20min): 写 **是/否**：你 eval harness 是否测 `args=""` 中间 chunk 不触发 tool（是/否/仅 end-state）。

- [Fix model_to_tools path map coverage for router return labels（LangChain PR #38698 updated **2026-07-07**）](https://github.com/langchain-ai/langchain/pull/38698)
  - 摘要: Fixes #38351。`model_to_tools` 条件边 path map 未覆盖 router 可返回的 `"model"` 标签 → runtime `KeyError("model")`。Fix：在 `model_to_tools_destinations` 定稿后确保 `loop_entry_node` 入 map；不变更路由逻辑。典型复现：`response_format=None`、`loop_exit_node="model"`、AI tool call 已被 injected ToolMessage 匹配时 router 仍返 `"model"` 但 map 缺项。
  - 为什么重要(对我): **skill 落地 + eval**——middleware + model_to_tools 是 LangChain v1 agent 默认路径；collect 通过但 dispatch 崩溃造成 eval/prod 分叉。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否用 `wrap_model_call` + model_to_tools（是/否/未知）。

- [Fail early on missing model-to-tools path map destination（LangChain PR #38696 updated **2026-07-07**）](https://github.com/langchain-ai/langchain/pull/38696)
  - 摘要: 与 #38698 同根因 #38351，侧重 construction-time：存在 `wrap_model_call` middleware 但 path map 缺 model 重入 destination 时，构造期抛具名 diagnostic，而非等到 LangGraph dispatch 才 `KeyError`。含 synthetic ToolMessage wrapper 形状的 regression。
  - 为什么重要(对我): **eval 杠杆**——fail-fast 使 CI collect 阶段即可捕获 routing contract 破损，避免浪费 LLM call 后才失败。
  - 明日动作(<=20min): 写 **是/否**：你 agent factory 测试是否覆盖 path map 完备性（是/否/仅 e2e）。

- [fix: manage AsyncPipeline lifecycle in AsyncPostgresSaver.from_conn_string（LangGraph PR #8295 updated **2026-07-07**）](https://github.com/langchain-ai/langgraph/pull/8295)
  - 摘要: `from_conn_string` 原用 `async with conn.pipeline()`，yield saver 后 context 退出即关闭 pipeline → stale `pipe` 引用 → `psycopg.OperationalError: SSL connection has been closed unexpectedly`。Fix：手动 enter/exit pipeline 生命周期；`Capabilities().has_pipeline()` 检测后再启用。Closes #5675（延续 0702/0703 同一 issue 线程）。
  - 为什么重要(对我): **skill 落地**——Postgres checkpointer 是长会话 HITL eval 基础；连接/pipeline 生命周期 bug 使 parallel eval run 随机断连不可复现。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `AsyncPostgresSaver.from_conn_string`（是/否/仅 sync）。

- [fix(langgraph): use get_name() for async node trace name（LangGraph PR #8294 updated **2026-07-07**）](https://github.com/langchain-ai/langgraph/pull/8294)
  - 摘要: `RunnableCallable.ainvoke` 对 `on_chain_start` 传 `name=config.get("run_name") or self.name`，无名节点（如 `functools.partial`）async 路径为 `None` → tracer 显示 `"Unnamed"`；sync `invoke` 用 `get_name()` 回退类名 `RunnableCallable`。Fix：async 亦用 `get_name()`，两路径 trace 名一致。
  - 为什么重要(对我): **eval + observability**——L2 trajectory 按 node name 聚合时，sync/async eval 路径若名不一致会产生假分叉。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否按 LangSmith run name 做 step 对齐（是/否/仅 tool）。

- [Fix return_direct tools aborting early when remaining_steps is low（LangGraph PR #8293 updated **2026-07-06**）](https://github.com/langchain-ai/langgraph/pull/8293)
  - 摘要: Fixes #8204。`create_react_agent` 的 `_are_more_steps_needed` 未考虑 `return_direct` tool 不消耗 agent loop iteration；`remaining_steps=1` 时 abort「need more steps」而非执行 return_direct tool。另有 vacuous-truth：`all([])` 在无 tool call 时为 True 可能误 abort。Fix：无 tool call 早退；return_direct 永不触发 abort；非 return_direct 仍要求 `remaining_steps>=2`。
  - 为什么重要(对我): **skill 落地 + eval**——return_direct 是「单步终结」skill 常见模式；step budget eval 若未区分会直接假阴性。
  - 明日动作(<=20min): 写 **是/否**：你 skill 栈是否有 return_direct tool（是/否/未知）。

- [fix: delta channel bug with updateState on fresh thread will force snapshot instead of stub checkpoint（LangGraph PR #8290，**langgraph 1.2.8** released **2026-07-06**）](https://github.com/langchain-ai/langgraph/pull/8290)
  - 摘要: Fixes deepagents#3774。fresh thread 上 `update_state`/`bulk_update_state` 对 `DeltaChannel` 首写静默丢失——DeltaChannel 需祖先 replay 但无 parent。#8011 用 stub checkpoint (step -1) 绕过；本 PR 改为 fresh thread 强制 snapshot 所有 DeltaChannel 进首个 checkpoint（step 0、无 parent、inline snapshot）。`get_state_history` 从双 checkpoint 变为单 checkpoint。
  - 为什么重要(对我): **skill 落地 + eval**——state 初始化是 collect→test 第一步；history 形状变化使旧 eval 断言需更新。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否在 fresh thread 测 `update_state`（是/否/仅 invoke）。

- [fix: support MCP prompt content blocks（LlamaIndex PR #22256 updated **2026-07-07**）](https://github.com/run-llama/llama_index/pull/22256)
  - 摘要: Fixes #21270。`BasicMCPClient.get_prompt()` 已支持 text/image，但对其他合法 MCP prompt content variant 抛错。现映射 `AudioContent`→`AudioBlock`、embedded text/binary resources→`TextBlock`/`ImageBlock`/`AudioBlock`/`DocumentBlock`（按 MIME）、`ResourceLink`→对应 media block。
  - 为什么重要(对我): **skill 落地**——MCP prompt 是 skill 注入路径；content block 覆盖不全使部分 MCP server 在 collect 阶段即失败。
  - 明日动作(<=20min): 写 **是/否**：你 MCP skill 是否用 `get_prompt()`（是/否/仅 tools）。

- [fix: require AG-UI tool call ids（LlamaIndex PR #22258 updated **2026-07-07**）](https://github.com/run-llama/llama_index/pull/22258)
  - 摘要: Fixes #22068。`llama_index_message_to_ag_ui_message()` 原在缺 ID 时静默 fabricate 随机 `tool_call_id`；现要求 `additional_kwargs["tool_call_id"]`，缺失则 `ValueError`。5 个 regression test 通过。
  - 为什么重要(对我): **eval**——随机 ID 使 L2 tool-message pairing eval 不可复现；与 0706 streaming tool call 议题同属「ID/args 完整性」门禁族。
  - 明日动作(<=20min): 写 **是/否**：你 AG-UI / frontend eval 是否断言 tool_call_id 来源（是/否/未测）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Beyond the Attention Stability Boundary: Agentic Self-Synthesizing Reasoning Protocols / SSRP（arXiv **2604.24512**）](https://arxiv.org/html/2604.24512v1)
  - 摘要: 形式化 **Attention Latch**：decoder-only Transformer 中历史 intent token 的 attention 权重压过中途 contradictory update（Information Over-squashing 的行为表现），即 `I(O;G₁)>I(O;G₂)`。提出 **SSRP**：Architect（高推理）自合成 SOP purge 过时 intent，Executive（高吞吐）按 SOP 执行。9K trajectories / MultiWOZ 2.2：3-hop semantic hijacking 下 Vanilla ReAct (GPT 5.4) 成功率 **0.1%**，SSRP **71.6%**；Equidistant stress test 90% 证明 latch 非单纯 positional bias。Procedural Integrity audit **98.8%**。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) 你 ≥3 turn agent 是否会出现「早期 constraint latch」？(2) Architect/Executive 分离 vs 单模型 HITL edit 哪条更像 SSRP？(3) 与 ASAG（0706）attention entropy 早停是互补信号还是重复？
  - 明日动作(<=20min): 写 **≤15 词**：你 multi-turn eval 是否测 mid-conversation goal pivot（有/无）。

- [When Evidence is Sparse: Weakly Supervised Early Failure Alerting in Dialogs and LLM-Agent Trajectories（arXiv **2606.05414**）](https://arxiv.org/abs/2606.05414)
  - 摘要: 轨迹级 success/fail 标签下做 **早失败告警**：失败证据仅占 **4.7–11.3%** turns、平均在轨迹 **59–83.6%** 处才首次出现。prefix-label 监督不匹配 multi-turn 稀疏结构。两阶段：**attention-based failure predictor** 从轨迹标签学稀疏 turn-level 证据 + **α-STOP** 单策略在 inference 选 accuracy-earliness  operating point（无需每点单独训练 trigger）。五 benchmark（含 tool use / planning）hypervolume 优于 SOTA trigger **3–42%**，每 operating point 训练成本降 **1–3 数量级**。
  - 为什么重要(对我): **Talk ≥3 turns + eval**：(1) attention 权重能否作 L1 早停而非等 terminal label？(2) α-STOP 的 operating point 能否映射你 eval 的 cost/latency 门禁？(3) 与 ASAG ATP 探测是否同族（稀疏证据 vs entropy）？
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否有 mid-trajectory abort 指标（有/无/仅 final）。

- [Disable dynamic speculative decoding when DP is enabled（vLLM PR #45963 updated **2026-07-07**）](https://github.com/vllm-project/vllm/pull/45963)
  - 摘要: `data_parallel_size>1` 时禁用 `num_speculative_tokens_per_batch_size` 动态 speculative decoding。根因：各 rank 按本地 scheduled request 数选不同 K，EAGLE/draft proposer 的 K-dependent DP collective 不同步。Warn 并回退静态 `num_speculative_tokens`。与 0706 #47194（hybrid + prefix cache + MTP → tool-call 污染）同属 **serving 层 attention/KV 状态一致性** 议题。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) DP + speculative 不同步是否扭曲 multi-turn tool-call grading？(2) 动态 K 降级对 token 预算门禁意味着什么？(3) eval 是否应分层报告 DP rank 一致性？
  - 明日动作(<=20min): 写 **是/否**：你 serving 是否开 data parallelism（是/否/仅 TP）。

## 3) 热门项目/可试个人项目

- [Research: Using DSPy to evaluate and improve Datasette Agent's SQL system prompts（Simon Willison Blog **2026-07-02**）](https://simonwillison.net/2026/Jul/2/#research-using-dspy-to-evaluate-and-improve-datasette-agents-sql-system-prompts)
  - 摘要: Claude Fable 5 异步任务：`pip install datasette-agent dspy`，用 DSPy 评估并改进 Datasette Agent 只读 SQL 的 system prompt。GPT 4.1 mini/nano 测试发现：schema 仅列表名 +「勿重复 describe_table」建议 → 模型猜列名（page_count、o.order_id）→ error-retry 循环。改进方向：prompt 内嵌列名或软化该建议。
  - 为什么重要(对我): **eval 杠杆**——DSPy 作 prompt 迭代闭环，与 map_prompt_test collect→test 同构；trace 驱动发现比纯 LLM-judge 更可行动。
  - 明日动作(<=20min): 读 DSPy eval 文档首页；写 **≤15 词** 你 prompt eval 是否用 trace 驱动（是/否）。
  - 估计复现成本: **低**（pip install + 小样本 SQL agent；无需 GPU）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [For all coding tasks use your judgement to decide an appropriate lower power model and run that in a subagent（Simon Willison Blog **2026-07-03**）](https://simonwillison.net/2026/Jul/3/#for-all-coding-tasks-use-your-judgement-to-decide-an-appropriate-lower-power-model-and-run-that-in-a-subagent)
  - 机会信号: **一人公司 token 成本杠杆**——Fable 订阅涨价前，Simon 让 Claude 将 coding 任务 delegate 到 subagent（sonnet 实现 / haiku 机械编辑），主 loop 保留 judgment/review；Fable allowance 消耗明显放缓。与 0705–0706 sqlite-utils multi-model 分工形成连续信号。
  - 风险信号: subagent 模型选择靠 agent 自行判断，无统一 cost dashboard 时长会话仍不可预测；review 留在主 loop 意味着责任半径仍在人。
  - 我可执行的一步(<=30min): 写 **模型分工表**（规划/实现/review 各用何模型）+ **≤30 词** 是否追踪 subagent token（有/无）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 124 Binary Tree Maximum Path Sum](https://leetcode.com/problems/binary-tree-maximum-path-sum/)：树形 DP，`dfs` 返回以当前节点为端点的最大链和，全局维护 `max_sum = max(max_sum, left_gain + node.val + right_gain)`。明日动作：手写后序遍历版（≤20min）；写 **≤15 词**：与 agent 工具调用链「局部最优拼接全局路径」的类比。

- **IELTS（≤1 条）**：[IELTS Reading — Matching headings（British Council 样题）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-practice-tests/reading)：练 skimming + heading 匹配；用技术文章（如 LangChain State of Agent Engineering）脱敏做 1 篇限时 15min。明日动作：完成 1 篇并标 2 个错因（≤20min）；写 **≤15 词** 弱项（speed/vocab/inference 选 1）。

## 6) 噪音过滤记录

- **LangGraph #8285 / LangChain #38682 / #34500 / #33787 / #38687 / LlamaIndex #21812 / #22252**：`0706.md` 已收录 → 不重复。
- **ASAG（2606.15070）/ From Reasoning Traces（2606.18089）/ vLLM #47194**：`0706.md` 已收录 → 不重复。
- **Tool Attention（2604.21816）/ Agent-Radar（2605.30136）**：`0703.md`/`0705.md` 已收录 → 不重复。
- **LangGraph #8292 release 1.2.8 deps-only changelog**：无额外 agent 语义 → 本日 §1 已收 #8290 实质 fix → release PR 不单列。
- **LangChain #34808 client_args / #38699 model-profiles / #38697 deepseek test**：infra/配置，与今日 focus 弱 → 丢弃。
- **vLLM #47857 LongCat / #47870 dep bump / #47868 XPU**：模型/infra churn，非 agent eval → 丢弃。
- **AutoGen #7800 Guardrails**：`0626.md` 已收录 → 不重复。
- **openai/evals #1686 SelfCorrectionEval**：`0702.md` 已收录 → 不重复。
- **Simon Jul 6 sqlite-utils / Hy3 / Jul 4 Better Models**：`0705.md`/`0706.md` 已收录或近重复 → 不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-07** 原帖 URL → 不收录。
- **@gefei55 / @ianneo_ai 职业帖**：第三方聚合无法验证当日原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv + Simon Willison 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **475**（`0706.md` §7: 466 + 本日 §1: 9）
- Theory: **194**（`0706.md` §7: 191 + 本日 §2: 3）
- Career/Business: **86**（`0706.md` §7: 85 + 本日 §4: 1）
- LC: **68**（`0706.md` §7: 67 + 本日 §5: 1）
- IELTS: **68**（`0706.md` §7: 67 + 本日 §5: 1）
