# Daily Research - 2026-08-29 (21:30)

## 0) 今日3条结论

- **InMemoryStore 的 `get`/`delete` 会 autovivify namespace**（LangGraph #8734, PR #8751 closed **2026-08-29**）：`dict.get` 读 miss 即在 `defaultdict` 中创建空 namespace → `list_namespaces()` 报告从未写入的 namespace；**durable agent eval 的 store 探针须用「重复 miss 后 namespace 计数不变」回归。**
- **`AIMessage.content_blocks` 仍在 mutate provider content**（LangChain #40001, PR #40007 closed **2026-08-29**）：Anthropic translator 读一次就 strip `index`、Google grounding 直接 attach citation 到 live dict → **agent eval 断言 message 不变时须覆盖 `content_blocks` 双读路径。**
- **MTP spec decode 下 hybrid GDN prefix cache 系统性 miss**（vLLM #52244, updated **2026-08-29**）：prompt 长度为 hash unit 整数倍时 replay 命中 **0**；根因是 GDN state 发布边界与 MTP rewind 规则 compose 错误 → **multi-turn agent token 优化 eval 须加「prompt_len % unit == 0」负例。**

## 1) Agent工程化（high-signal）

- [[Feature] Add fixed-token prefill scoring（vLLM PR #54335, created **2026-08-29**）](https://github.com/vllm-project/vllm/pull/54335)
  - 摘要: 新增 `SamplingParams.prompt_logprob_token_ids`，在 prefill 各位置对固定 candidate token 打分；结果经 `PromptTokenLogprobsTensors` → GPU runner → `RequestOutput` 返回。M1 范围：request-wise 固定 ID，不含 sparse per-position list / prefix-cache read / OpenAI protocol field。
  - 为什么重要(对我): **评测 + token 优化**——可在不生成 full completion 的情况下对 prompt 内固定 token 序列做 logprob audit（tool name、routing keyword、skill trigger phrase）；与 generative scoring (#39351) 互补。
  - 明日动作(<=20min): 写 **1 条** 你 agent eval 是否需要 prefill-position logprob（≤15 词）。

- [[Bugfix][KV Offload] Stop offloading the final sampled token's KV slot（vLLM PR #54288, updated **2026-08-29**）](https://github.com/vllm-project/vllm/pull/54288)
  - 摘要: Fixes #54193。`_build_store_jobs` 允许 offload 到 `req.num_tokens`，但最后一 token 无自有 KV（由前一 position forward 产出）；finish 落在 block boundary 时 offload tier 收到 hash 合法但 slot 越界的 block。后续 multi-turn replay 会 match hash 并 load 坏 block；spec decode 下 junk KV 可污染 draft。
  - 为什么重要(对我): **agent serving eval**——KV offload + multi-turn + spec decode 组合的 eval 须断言最后一 token slot 不被 offload。
  - 明日动作(<=20min): 写 **是/否**：你是否用 KV offload + multi-turn agent（是/否/未知）。

- [[Bugfix][V1] Restore hybrid GDN prefix-cache hits under MTP spec decoding（vLLM PR #52244, updated **2026-08-29**）](https://github.com/vllm-project/vllm/pull/52244)
  - 摘要: Qwen3.5-122B-A10B + MTP spec decode 下，prompt replay 的 cached token 远低于 ceiling；prompt 长度为 1072/2000/2144（hash unit 67 整数倍）时命中 **0**。根因：lookup cap `prompt_len-1`、MTP rewind 67、GDN state 发布在 `prompt_len//67*67` 三规则 compose 错误。
  - 为什么重要(对我): **token 优化 + 评测**——prefix cache eval 须覆盖「prompt_len % hash_unit == 0」边界；否则 multi-turn agent 成本 benchmark 系统性高估。
  - 明日动作(<=20min): 写 **是/否**：你 agent serving 是否用 MTP/EAGLE spec decode（是/否/未知）。

- [[Bugfix][V1] Keep an encoder cache entry until its last occurrence is freed（vLLM PR #54284, merged **2026-08-29**）](https://github.com/vllm-project/vllm/pull/54284)
  - 摘要: `EncoderCacheManager.cached` 按 request 计数而非 position 计数；同一 request 多次引用同一 `mm_hash` 只持一个 ref，但 `free_encoder_input` 在**第一次** occurrence 释放时就 discard → 后续 occurrence 的 encoder cache 被 premature free。
  - 为什么重要(对我): **skill 落地 + multimodal agent eval**——multi-image / repeated screenshot agent skill 的 eval 须覆盖同一 mm item 多次出现场景。
  - 明日动作(<=20min): 写 **是/否**：你 multimodal agent 是否在同一 turn 多次引用同一 image（是/否/未知）。

- [[Perf] Avoid h2d copies from non-pinned CPU tensors（vLLM PR #54299, merged **2026-08-29**）](https://github.com/vllm-project/vllm/pull/54299)
  - 摘要: 非 pinned CPU tensor 的 `non_blocking` H2D copy 可导致 GPU/CPU overlap stall（#53491 暴露）。本 PR pin CPU tensor 后再 H2D，恢复 overlap。
  - 为什么重要(对我): **token/latency eval**——agent serving benchmark 若未 pin input tensor，latency 测量会混入 host-side stall 噪声。
  - 明日动作(<=20min): 写 **是/否**：你 agent serving eval 是否 pin input buffers（是/否/未知）。

- [InMemoryStore reads create namespaces（LangGraph Issue #8734, PR #8751 closed **2026-08-29**）](https://github.com/langchain-ai/langgraph/issues/8734)
  - 摘要: `InMemoryStore` 的 `get`/`delete` 路径通过 `dict.get` 读 miss 时在 `defaultdict` 中 autovivify namespace；`list_namespaces()` 报告从未写入的 namespace。PR #8751 修复：read-through `dict.get`、delete 后移除空 namespace、search 时 guard vector lookup；含 3 个回归 test。
  - 为什么重要(对我): **skill 落地 + durable agent eval**——store namespace 是 skill routing / memory eval 的基础；miss 不应改变 store 拓扑。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `InMemoryStore` 做 agent memory eval（是/否/未知）。

- [Checkpoint numpy codec: structured dtypes corrupt silently（LangGraph Issue #8746, PR #8747 closed **2026-08-28**）](https://github.com/langchain-ai/langgraph/issues/8746)
  - 摘要: `EXT_NUMPY_ARRAY` codec 用 `dtype.str` + raw buffer 无法表达 structured/object dtype；structured dtype 静默 corrupt 为 `|V12` void、object dtype 持久化 raw `id()` 指针（reload 为 `None`）、round-trip 数组 read-only。PR #8747 改用 `dtype.descr`、encode object elements、skip datetime64 C-contiguous fast path、`writeable` flag。
  - 为什么重要(对我): **评测 + checkpoint eval**——agent state 含 numpy embedding/metadata 时，checkpoint round-trip eval 须覆盖 structured/object dtype 负例。
  - 明日动作(<=20min): 写 **是/否**：你 checkpointer 是否存 numpy array metadata（是/否/未知）。

- [fix(langgraph): copy channel containers when taking a checkpoint（LangGraph PR #8749, closed **2026-08-29**）](https://github.com/langchain-ai/langgraph/pull/8749)
  - 摘要: Fixes #8748。`Topic.checkpoint()` / `NamedBarrierValue.checkpoint()` 返回 live container；`create_checkpoint` 直接存入 `channel_values`，后续 `update()` 原地 mutate → 已 taken 的 checkpoint 被 retroactive 修改。Fix：在 `checkpoint()` 中 shallow copy（`values.copy()` / `seen.copy()`）。
  - 为什么重要(对我): **durable agent eval**——Topic/Barrier channel 的 history replay eval 须断言旧 checkpoint 不被后续 superstep 污染。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `Topic` / `NamedBarrierValue` channel（是/否/未知）。

- [core: AIMessage.content_blocks mutates message.content（LangChain Issue #40001, PR #40007 closed **2026-08-29**）](https://github.com/langchain-ai/langchain/issues/40001)
  - 摘要: `AIMessage.content_blocks` 在 Anthropic translator 中 strip 非标准 block 的 `index`、在 Google GenAI translator 中 attach grounding citation 到 live text block dict；读一次 `content_blocks` 后 `message.content` 已变。0823 已收录 Bedrock Converse 同类 bug，今日扩展到 Anthropic + Google。
  - 为什么重要(对我): **skill 落地 + 评测**——trace/eval 若双读 `content_blocks` 做 assertion，会 silent false pass/fail；须 copy-before-translate 或断言 content 不变。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否断言 `message.content` 在 `content_blocks` 读后不变（是/否/未知）。

- [fix(core): preserve upsert data when transformations fail（LlamaIndex PR #22798, updated **2026-08-29**）](https://github.com/run-llama/llama_index/pull/22798)
  - 摘要: Fixes #22639。`IngestionPipeline` 原先在 transformation 运行**前**就删除 docstore/vector-store 旧数据；transformation 失败时旧索引已删、新数据未写入。Fix：defer pending deletions 到 transformation 成功之后（sync + async）。
  - 为什么重要(对我): **RAG pipeline eval + skill 落地**——ingestion skill 的 failure-injection eval 须断言 transform 失败不丢旧索引。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `IngestionPipeline` + `UPSERTS` mode（是/否/未知）。

- [Just a rumour of a bug is enough to find a security exploit these days（Simon Willison Blog, **2026-08-28**）](https://simonwillison.net/2026/Aug/28/just-a-rumour-of-a-bug/)
  - 摘要: OCaml 维护者 Anil Madhavapeddy 报告：公开讨论 patch 后 ~10 分钟内即有自动化 probe 尝试 percent-encoded traversal；rclone 过去一个月收到 40+ security disclosure（此前 10 年仅 ~20），~75% 含需关注的 nugget。GitHub CVE 分配从 2-3 天延长到 3-4 周。
  - 为什么重要(对我): **skill 落地 + agent eval**——开源 agent skill 的 security disclosure 响应 SLA 须按「分钟级自动化 exploit」重估；eval 须含 embargo 期间 patch 讨论泄漏场景。
  - 明日动作(<=20min): 写 **1 条** 你 agent skill repo 的 security disclosure 流程（≤15 词）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Attention][DCP] Enable FlashInfer MLA for DSpark drafting（vLLM PR #54277, merged **2026-08-29**）](https://github.com/vllm-project/vllm/pull/54277)
  - 摘要: DSpark 需在 shared draft metadata 中支持 non-causal multi-token DCP drafting。本 PR：从 draft model global seq len 重算 rank-local DCP seq len；统一 EAGLE/MTP/DFlash/DSpark metadata plumbing；causal target MLA 与 non-causal DSpark draft 分 KV-cache group；advertise FlashInfer MLA for non-causal DCP drafting。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/KV**——spec decode 下 MLA + DCP 的 KV layout 与 causal/non-causal 分组是 long-context agent serving 的核心约束；eval 须标注 draft backend + DCP rank。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 MLA + spec decode + DCP（是/否/未知）。

- [[Bugfix] Guard slot mapping block table loads（vLLM PR #54296, created **2026-08-29**）](https://github.com/vllm-project/vllm/pull/54296)
  - 摘要: Fixes #53982。`ComputeSlotMappingKernel` 与 Model Runner V2 `_compute_slot_mappings_kernel()` 在 narrow cache group 的 block table 未覆盖完整 sequence 时，block index 可超 row width → OOB GPU read。Fix：`block_indices < block_table_stride` mask + `PAD_SLOT_ID`；RTX 4090 复现并验证 10 passed。
  - 为什么重要(对我): **Talk ≥3 turns + KV/paged attention**——slot mapping 是 paged attention 与 KV cache 的接合点；multi-group cache eval 须加 narrow-table 负例。
  - 明日动作(<=20min): 写 **≤15 词**：你 serving eval 是否覆盖 multi cache group（是/否/未知）。

- [[Perf][Kernel][Attention] Reorder causal-prefill launches in Triton unified attention（vLLM PR #54344, created **2026-08-29**）](https://github.com/vllm-project/vllm/pull/54344)
  - 摘要: Triton unified attention 的 causal-prefill 当前用 `(q_block, kv_head)` grid launch；later query blocks 有更长的 KV prefix、finish 更晚，产生 launch tail。本 PR opt-in 重排 launch order 缩短 tail（不改变 mapping 语义）；明确不从 `max_query_len > 1` 单独判定（spec verify 会 schedule 多 target token）。
  - 为什么重要(对我): **Talk ≥3 turns + Attention kernel**——prefill attention 的 launch schedule 影响 agent 首 token latency；benchmark 须标注是否启用 reorder opt-in。
  - 明日动作(<=20min): 写 **≤15 词**：你 prefill latency eval 是否区分 attention launch schedule（是/否/未知）。

- [[Kimi-K3][Perf] Make native CUDA AttnRes the SM100 default（vLLM PR #54261, updated **2026-08-28**）](https://github.com/vllm-project/vllm/pull/54261)
  - 摘要: 将 native CUDA Kimi-K3 AttnRes 设为 SM100 上 dense `hidden_size=7168` 的 default；扩展 register/TMEM kernel 覆盖 block count 0–8、optional prefix add、output norm、block-bank writes；B300 上 1-token/4-blocks CUDA 10.24→8.26µs（1.49x），8192-token/8-blocks 395→229µs（1.72x）；Triton 为 fallback。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**——AttnRes 是 hybrid/recurrent model 的 attention 替代路径；cross-hardware eval 须标注 AttnRes backend（CUDA native vs Triton）。
  - 明日动作(<=20min): 写 **≤15 词**：你 hybrid model eval 是否标注 AttnRes backend（是/否/未知）。

## 3) 热门项目/可试个人项目

- [feat(evalboard): render multi-variant runs, one row per (task, arm)（UiPath/coder_eval PR #145, merged **2026-08-28**）](https://github.com/UiPath/coder_eval/pull/145)
  - 摘要: Evalboard 现可渲染声明 `variants:` 的 run：每个 task 按 arm 分行展示、可 drill-down。框架层 `run.json` 已有 `variant_id` + `reports_experiment.py` 的 win rate，evalboard 是唯一缺失层。
  - 为什么重要(对我): **skill 评测**——A/B variant 可视化是 skill trigger / prompt 对比 eval 的最低可用 UI；与 0828 收录的 activation check 形成「跑→看→比」闭环。
  - 明日动作(<=20min): 对 1 个现有 skill 写 2-arm YAML variant，本地跑并打开 evalboard。
  - 估计复现成本: **低**（`pip install coder-eval` + 1 skill + 2 variant，~30min）。

- [fix(tier3): retry malformed structured judge responses（NVIDIA/SkillEvaluator PR #78, merged **2026-08-28**）](https://github.com/NVIDIA/SkillEvaluator/pull/78)
  - 摘要: Tier-3 semantic judge 对 malformed/empty/non-finite/schema-invalid 的 structured response 精确 retry 一次；4096-token structured-judge default；区分 malformed output retry vs provider/configuration error 单次失败。
  - 为什么重要(对我): **skill 评测**——L3 judge-based eval 的 flaky rate 直接决定 CI gate 可信度；retry-on-malformed 是可复用的 judge robustness 模式。
  - 明日动作(<=20min): 读 PR diff 的 retry 分支，写 **1 条** 可迁入你 playbook 的 judge retry 规则（≤20 词）。
  - 估计复现成本: **中**（Python 3.12+、1 skill 目录、judge API key，~60min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Funding better evaluations of AI's impact on wellbeing（Anthropic Blog, **2026-08-25**）](https://www.anthropic.com/news/wellbeing-research-grants)
  - 机会信号: $5M grant 资助独立研究者构建 **open-source wellbeing evaluations/benchmarks**（模型访问 + 技术支持）；申请截止 **2026-09-21** → Agent 评测背景可申报「AI 对用户 wellbeing 影响的 open eval」方向，与 agent-eval-playbook 的 L3+ 门禁方法论直接相关；出海 remote 可卖 eval framework 咨询。
  - 风险信号: grant 竞争激烈、独立研究无商业保证；wellbeing eval 定义主观、需 IRB/伦理审查；非 biology/chemistry 领域，与垂域经验需脱敏包装。
  - 我可执行的一步(<=30min): 读 [application form](https://www.anthropic.com/news/wellbeing-research-grants) 要求，写 **3 条** 你现有 agent eval 经验可映射到 wellbeing eval 的接点（各 ≤15 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[2948. Make Lexicographically Smallest Array by Swapping Elements](https://leetcode.com/problems/make-lexicographically-smallest-array-by-swapping-elements/)（**Day 59, Aug 2026 Daily Challenge**, verified via LeetCode GraphQL **2026-08-29**；承接 0828 的 3734 Hard「字典序大于 target 的最小回文排列」，今日 **Medium**「受限交换下字典序最小数组 = 连通分量 + 排序 + 贪心放置」）：关键洞察——`limit` 定义交换图连通分量，分量内排序后按原位置贪心填入最小可行值；与 agent eval「约束图上的可行解构造」同构。明日动作：手写解法；写 **≤15 词**：「交换约束 + 字典序最小」对你 eval 约束组合意味着什么。

- **IELTS（≤1 条）**：[IELTS Writing Practice Tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/writing)：0828 已轮换 Reading，今日换 **Writing**（Task 1 或 Task 2 选 1 篇，限时 ~40min）。明日动作：完成 1 次 Writing，记录 **≥2 个** 句式/衔接词失误；写 **≤15 词** 你最弱的 Writing 维度（Task Achievement / Coherence / Lexical / Grammar）。

## 6) 噪音过滤记录

- **fix(checkpoint): stop InMemoryStore reads from creating namespaces（LangGraph #8750）**：与 #8751 同 fix #8734 → semantic duplicate，只收录 #8751。
- **fix(core): avoid mutating Google grounding text blocks（LangChain #40015）**：与 #40007/#40001 同族 → 合并收录于 #40001。
- **fix(core): support '+' bullet in MarkdownListOutputParser（LangChain #40005）**：边缘 output parser，0826 已过滤同类 → 不重复。
- **chore(model-profiles): refresh model profile data（LangChain #40009）**：数据刷新，非 eval focus → 过滤。
- **fix(core): skip blank documents in SemanticSplitterNodeParser（LlamaIndex #22876）**：token 优化相关但边缘 case → 降优先级，未达主报告 8 条缺口时不单独占 Agent 槽位（已纳入 §1 第 10 条）。
- **vLLM #54254/#54254 ROCm Kimi-K3 专项**：AMD 硬件特定 → 过滤。
- **vLLM #54168 low-M MoE tail**：Kimi-K3 SM100 专项 kernel → 过滤。
- **openai/evals / microsoft/autogen**：无 **2026-08-29** repo 更新 → 不收录。
- **Anthropic MHS / Expanding scientists（Aug 27）**：0828 已收录 → semantic duplicate，不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-08-28~29** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + Simon Willison + Anthropic ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **818**（`0828.md` §7: 807 + 本日 §1: 11）
- Theory: **367**（`0828.md` §7: 363 + 本日 §2: 4）
- Career/Business: **129**（`0828.md` §7: 128 + 本日 §4: 1）
- LC: **111**（`0828.md` §7: 110 + 本日 §5: 1）
- IELTS: **111**（`0828.md` §7: 110 + 本日 §5: 1）
