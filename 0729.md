# Daily Research - 2026-07-29 (21:30)

## 0) 今日3条结论

- **LangGraph 公开 `get_delta_channel_history` + DeltaChannel 快照 cadence 重构**（#7699, **2026-07-29**）：checkpoint saver 层新增稳定 API 遍历 delta channel 写入链；快照频率从「每 N 个 superstep」改为「每 N 次 channel 更新」，Postgres 读延迟 -35%~-66%、存储 -62%~-66% → agent eval 的 checkpoint-replay 与长线程 state 重建有了可编程接口，不再依赖私有 K-channel walk。
- **LangChain model retry/fallback middleware 会吞 GraphBubbleUp 中断**（#39120, **2026-07-29**）：HITL / `interrupt()` 控制流被 middleware 当成可重试错误包装 → human-in-the-loop eval 与 prod 图执行契约断裂；非 retryable 错误应立即 raise，Graph 控制信号必须穿透 middleware 栈。
- **vLLM 实验性 Mask Replay 对齐 rollout/training 采样分布**（#49577, **2026-07-29**）：top-p 过滤后的 vocabulary support 以 CSR `SamplingMask` 返回，训练侧重算 logprob 时覆盖同一 support → GRPO/importance ratio 的 `logp_diff` 从持续负偏拉回 0 附近；这是 Attention 之外但直接影响 agent RL 训练稳定性的「采样分布」层修复。

## 1) Agent工程化（high-signal）

- [feat: public get_writes_history saver API + delta cadence rework（LangGraph PR #7699, updated **2026-07-29**）](https://github.com/langchain-ai/langgraph/pull/7699)
  - 摘要: 将私有 K-channel ancestor-walk 提升为公开 `get_delta_channel_history` / `aget_delta_channel_history`（返回 `DeltaChannelHistory` TypedDict：`writes` 必有、`seed` 可选）；移除 `DELTA_SENTINEL`；`DeltaChannel` 快照 cadence 改为每 N 次 channel 更新（默认 `snapshot_frequency=1000`）；Postgres 分页 stage-1 + per-channel UNION ALL stage-2。Benchmark：500-turn Postgres 单 channel 深读 17.7ms→6.1ms（-66%），K=8 mixed freq 214ms→139ms（-35%）；InMemory 存储 K=8 uniform 23.1MB→8.7MB（-62%）。
  - 为什么重要(对我): **skill 落地 + 评测**——delta channel 是长运行 agent 增量 state 的核心；公开 history API = eval harness 可确定性 replay 任意 channel 前缀而不重跑全图；cadence 重构直接降低 checkpoint 存储与读延迟，影响 eval 长跑成本。
  - 明日动作(<=20min): 写 **是/否**：你 eval replay 是否依赖 delta channel 历史而非仅最终 checkpoint（是/否/未知）。

- [fix(langchain): fix exception swallowing in model retry and fallback middleware（LangChain PR #39120, updated **2026-07-29**）](https://github.com/langchain-ai/langchain/pull/39120)
  - 摘要: model retry / fallback middleware 曾捕获 LangGraph 控制信号（如 graph interrupt），导致 HITL 流断裂；修复后 GraphBubbleUp 正确穿透，non-retryable 错误立即 raise 而非包装为 error message。57 个 `test_model_retry` / `test_model_fallback` 单测通过。
  - 为什么重要(对我): **评测 + skill 落地**——middleware 栈是 agent harness 默认层；interrupt 被吞 = HITL eval 假失败/假成功，与 Anthropic pass^k 可靠性指标直接冲突。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否在 retry middleware 之上跑 `interrupt()` / HITL（是/否/未知）。

- [fix: prevent OOM by adding thread safety to UUID v6 timestamp（LangGraph PR #8473, updated **2026-07-29**）](https://github.com/langchain-ai/langgraph/pull/8473)
  - 摘要: Fixes #8409。`uuid6()` 全局 `_last_v6_timestamp` 无锁，高并发（parallel `Send()` 多线程 graph run）下竞态可导致无限重试 spin → OOM。Fix：`threading.Lock` 保护 check-and-set。
  - 为什么重要(对我): **评测稳定性**——parallel fan-out eval 在 checkpoint ID 生成层 OOM = flaky CI 与不可复现 timeout；与 0728 idempotency middleware 形成「并行 agent 可靠性」双侧检查。
  - 明日动作(<=20min): 写 **是/否**：你 graph 是否用 parallel `Send()` 且多线程执行（是/否/未知）。

- [Warn when nodes return undeclared graph schema keys（LangGraph PR #8474, updated **2026-07-29**）](https://github.com/langchain-ai/langgraph/pull/8474)
  - 摘要: Fixes #8320。节点返回未在 graph schema 声明的 key 时发出 `UserWarning`（行为仍为忽略 undeclared keys）；覆盖 undeclared keys 与 `output_schema` 声明 keys 的回归测试。
  - 为什么重要(对我): **skill 落地 + 评测**——节点静默丢 key = state transition eval 的「看起来跑了但 state 缺字段」盲区；warning 是 eval harness 应捕获的 schema 契约信号。
  - 明日动作(<=20min): 写 **是/否**：你节点是否曾返回 schema 外 key 且未察觉（是/否/未知）。

- [fix(openai): omit non-msg_ ids for synthetic AI messages in Responses API input（LangChain PR #39116, updated **2026-07-29**）](https://github.com/langchain-ai/langchain/pull/39116)
  - 摘要: Fixes #39113。`store` 非 False 时 `_construct_responses_api_input` 将 content block 上任意 `id` 写入 Responses API input；`create_text_block()` 分配的 `lc_*` ID 被 OpenAI 拒绝（`Expected an ID that begins with 'msg'`）。Fix：仅保留以 `msg_` 开头的 item-level id。
  - 为什么重要(对我): **token 优化 + skill 落地**——与 0728 #39103 system block `id` 问题形成 Responses API 双侧守卫；content_blocks + cache_control 路径若 id 污染 = 整请求失败，prompt cache 策略 silent broken。
  - 明日动作(<=20min): 写 **是/否**：你 Responses API agent 是否用 `create_text_block()` 合成消息（是/否/未知）。

- [Validate AG-UI static tool names（LlamaIndex PR #21873, updated **2026-07-29**）](https://github.com/run-llama/llama_index/pull/21873)
  - 摘要: `AGUIChatWorkflow` 在构建 workflow tool map 前校验 static frontend/backend tool 名：拒绝 `frontend_tools` 内重复、`backend_tools` 内重复、跨组重复；否则同名 tool 在 dict keyed by `tool.metadata.name` 时静默 shadow。
  - 为什么重要(对我): **skill 落地**——AG-UI 是 frontend/backend tool 分离的 agent UI 模式；静默 shadow = tool-calling eval 统计指向错误 tool，与 LangChain schema 完整性问题同族。
  - 明日动作(<=20min): 写 **是/否**：你 agent workflow 是否混用 frontend/backend static tools（是/否/未知）。

- [fix(llms-ollama): keep thinking-only chunks in stream_chat/astream_chat（LlamaIndex PR #21878, updated **2026-07-29**）](https://github.com/run-llama/llama_index/pull/21878)
  - 摘要: Fixes #21232。Ollama 推理模型（DeepSeek-R1、QwQ、gpt-oss 等）streaming 时 `content=None` 但 `thinking` 有值的 chunk 被 `if content is None: continue` 丢弃；非 streaming `chat` 路径已正确处理。Fix 后 streaming 保留 thinking-only chunk。
  - 为什么重要(对我): **评测 trace 完整性**——reasoning model eval 若只采 final answer 而丢 thinking trace = transcript grading 与 outcome grading 分裂；local eval 成本杠杆（Ollama）的 trace 不可用。
  - 明日动作(<=20min): 写 **是/否**：你 Ollama 推理模型 eval 是否依赖 streaming trace（是/否/未知）。

- [fix/issue 7956 assistant cancellation hang（AutoGen PR #7991, updated **2026-07-29**）](https://github.com/microsoft/autogen/pull/7991)
  - 摘要: Fixes #7956。取消 in-flight tool call 时 `AssistantAgent.on_messages_stream` 永久挂起而非结束流。根因：`_execute_tool_calls` 中 `asyncio.gather` 遇 `CancelledError` 后 stream terminator (`None`) 未入队；Fix：`try/finally` 确保 queue 始终终止。含 regression test `test_tool_cancellation_terminates_stream`。
  - 为什么重要(对我): **评测 + HITL**——eval harness 超时/abort 是标准操作；cancel 挂死 = eval runner 僵尸进程与 CI 假 stuck；与 LangChain #39120 interrupt 穿透形成 cancellation 契约双侧检查。
  - 明日动作(<=20min): 写 **是/否**：你 eval runner 是否用 cancellation 中止 in-flight tool（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Feature] Mask Replay（vLLM PR #49577, updated **2026-07-29**）](https://github.com/vllm-project/vllm/pull/49577)
  - 摘要: 实验性 **sampling distribution replay**：`SamplingMask` 以 CSR 形式返回每个生成 token 在 top-k/top-p 过滤后保留的 vocabulary support（非 attention mask，不影响 causal attention / KV-cache）。动机：rollout 在截断 support 上归一化，训练常在全词表重算 → importance ratio / KL 系统性偏差。GRPO 实验：开启 replay 后 `importance_ratio_mean` 贴近 1、`logp_diff_mean` 贴近 0、方差与 approx KL 更低。
  - 为什么重要(对我): **Talk ≥3 turns + Agent RL 原理**：这是「采样分布」层与 Attention 并列的训练对齐问题；agent RL eval 若忽略 top-p support mismatch，pass rate 不变但 policy gradient 已 poisoned。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent RL 是否在 top-p 下比对 rollout vs training logprob（是/否/未知）。

- [[KV Connector] Fix NIXL mamba state pairing for multi-slot block tables（vLLM PR #50153, updated **2026-07-29**）](https://github.com/vllm-project/vllm/pull/50153)
  - 摘要: NIXL P/D transfer 对 SSM state block 做 positional pairing，默认假设每 request mamba group 仅一个 local state block——在 speculative decoding + mamba prefix caching 下失效：multi-slot table 含 scratch slots 与 null placeholders，导致 RDMA 脏 scratch 或 silent state corruption。Fix：scheduler 侧剥离 trailing speculative slots 并在 single-state cache mode 缩减到 running state slot；worker 侧用 min-count alignment 替代 single-local-block assertion。
  - 为什么重要(对我): **Talk ≥3 turns + KV/SSM**：Mamba/SSM 长上下文 agent 的 P/D disaggregation 在 spec decode 下 state pairing 错误 = multi-turn trace 不可复现；与 0728 NixlPush handshake、0727 OBJ cleanup 形成 KV offload 检查清单。
  - 明日动作(<=20min): 写 **≤15 词**：你 mamba serving 是否在 spec decode + PD 下测过 state 一致性（是/否/未知）。

- [[Frontend][Rust] Support mm_processor_kwargs in chat completions（vLLM PR #50164, updated **2026-07-29**）](https://github.com/vllm-project/vllm/pull/50164)
  - 摘要: Rust frontend `/v1/chat/completions` 原拒绝 `mm_processor_kwargs`；现接受并在 `vllm-chat` 中 merge 到 `PreProcessorConfig`，在 image/video preprocessor 运行前生效（如 per-request `min_pixels` / `max_pixels` override）。
  - 为什么重要(对我): **Talk ≥3 turns + Attention 前处理**：multimodal prefill token 数由 processor kwargs 决定；per-request override = agent eval 可在同 harness 测不同 resolution 下的 TTFT/成本曲线（与 0728 #50107 mm_processor_cache 互补）。
  - 明日动作(<=20min): 写 **≤15 词**：你 multimodal eval 是否 per-request 调 `min_pixels`/`max_pixels`（是/否/未知）。

- [[Multimodal] Expose mm hash algorithm selection to cli args（vLLM PR #49686, updated **2026-07-29**）](https://github.com/vllm-project/vllm/pull/49686)
  - 摘要: `VLLM_MM_HASHER_ALGORITHM` 环境变量之外新增 CLI `--mm-hasher-algorithm`；`tests/multimodal/test_cache.py` 验证通过。
  - 为什么重要(对我): **Talk ≥3 turns + mm cache**：multimodal hash 算法影响 processor cache 命中率与跨 run 一致性；CLI 暴露 = eval 环境可复现配置而不改 env。
  - 明日动作(<=20min): 写 **≤15 词**：你 mm cache eval 是否固定 hasher 算法（是/否/未知）。

- [feat: add FallbackLLM for built-in provider failover（LlamaIndex PR #21894, updated **2026-07-29**）](https://github.com/run-llama/llama_index/pull/21894)
  - 摘要: `FallbackLLM` 按优先级包装多 LLM provider；transient 错误（timeout、429、5xx、连接错误）自动 failover，permanent 错误（4xx auth、content policy）立即失败；`retry_attempts` 可配置。
  - 为什么重要(对我): **Talk ≥3 turns + Agent 可靠性原理**：multi-provider failover 的 error classification 是 pass^k 自动化 eval 的前置——429 若被记为 model failure 而非 infra transient，eval 指标污染。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval harness 是否区分 transient vs permanent LLM 错误（是/否/未知）。

## 3) 热门项目/可试个人项目

- [syntax-syndicate/karpathy-skills — Karpathy-inspired agent guidelines 多平台适配](https://github.com/syntax-syndicate/karpathy-skills)
  - 摘要: 将 Karpathy 观察的 LLM 编码失败模式（unchecked assumptions、过度工程、非手术式修改、无成功标准）打包为 Agent Skills / CLAUDE.md / `.cursor/rules` 等多 agent 适配器；canonical 源为 `skills/karpathy-guidelines/SKILL.md`，覆盖 Claude Code、Cursor、Codex、Aider 等 30+ 工具链。
  - 为什么重要(对我): **skill 落地**——最小单文件 skill 模板 vs 多 skill enforcement gates 的对比实验素材；可挂到你 map_prompt_test harness 测「原则约束」对 tool-calling pass rate 的影响。
  - 明日动作(<=20min): 读 `skills/karpathy-guidelines/SKILL.md` 前 30 行；写 **≤10 词** 哪条原则最贴近你 agent eval 痛点。
  - 估计复现成本: **低**（clone + 复制 adapters/cursor 规则，≤20min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Adding a custom MCP server to Claude and ChatGPT（Simon Willison TIL, **2026-07-28**）](https://til.simonwillison.net/llms/mcp-in-claude-and-chatgpt)
  - 机会信号: 消费端 Claude（Connectors → Add custom connector）与 ChatGPT（Developer mode + Plugins 目录「+」）均已支持自定义 MCP URL；Simon 用只读 Datasette MCP 演示跨产品 SQL 查询 agent。对你：出海 remote 的 demo 可从「只读 data MCP + eval trace」切入，而非重 infra；MCP 成为跨 Claude/ChatGPT 的最小互操作层。
  - 风险信号: ChatGPT 需开启 Developer mode（标 ELEVATED RISK）；Custom MCP 未经 OpenAI 审核；Voice mode 不可用 MCP tools；消费端 connector UX 仍碎片化（ChatGPT 激活 MCP 需搜索插件名）。
  - 我可执行的一步(<=30min): 写 **≤8 行**：你当前 agent 栈是否暴露为 MCP server（是/否/计划）；若否，最小只读 tool 候选是什么（1 个）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 3519 Count Numbers with Non-Decreasing Digits](https://leetcode.com/problems/count-numbers-with-non-decreasing-digits/)（**Day 29, Jul 2026, Hard**；承接 0728 的 3518 系列）：给定字符串 `l`、`r` 与进制 `b`，统计 `[l,r]` 中 base-b 表示下数位非递减的整数个数（模 1e9+7）。明日动作：数位 DP 模板（pos/lastDigit/tight）写 1 次；写 **≤15 词**：「tight 边界」与 agent eval 中「仅允许合法 state transition」的类比。

- **IELTS（≤1 条）**：[IELTS Ready — British Council 官方免费备考（含 AI Mini Mock）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-ready)：50 分钟 Mini Mock + AI 估分。明日动作：完成 **Listening 1 节 Mini Mock**（0728 做了 Reading，今日换 Listening）；写 **≤15 词** 错因类型（词汇/定位/推理 选 1）。

## 6) 噪音过滤记录

- **LangGraph #8471/#8469 DeltaChannels hydrate**：0728 同日 merge 族 → 不重复。
- **LangChain #39103 system block id、#39057 memoize convert_to_openai_tool**：0728 已收录 → 不重复。
- **LangGraph #8465 idempotency、#8467 set/frozenset**：0728 已收录 → 不重复。
- **AutoGen #7994 LangChain run_manager schema、#7211 $defs**：0727–0728 已收录 → 不重复。
- **vLLM #49989 FP8 MLA Marlin、#50107 mm_processor_cache**：0728 已收录 → 不重复。
- **LlamaIndex #21894 FallbackLLM**：已入 §2（可靠性原理角度）；§1 不重复。
- **LlamaIndex #22489 case-insensitive extensions、#22473 telemetry**：infra/低 actionable → 过滤。
- **LangChain #39115 LANGSMITH_API_KEY gateway fallback、#39119 check_diff path**：运维向，信号弱于今日 interrupt/delta 主线 → 过滤。
- **vLLM #50265 unfinished request recovery**：PR body 空，无 test plan → 过滤。
- **vLLM #49960 CPU torch.compile**：CPU-only 主机，与当前 focus 弱相关 → 过滤。
- **openai/evals #1696 nested token usage**：Jul 24 更新，0724 周期可覆盖；今日 Jul 29 白名单 GitHub 信号充足 → 不重复收录。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-29** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Jul 29 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **638**（`0728.md` §7: 630 + 本日 §1: 8）
- Theory: **270**（`0728.md` §7: 265 + 本日 §2: 5）
- Career/Business: **108**（`0728.md` §7: 107 + 本日 §4: 1）
- LC: **90**（`0728.md` §7: 89 + 本日 §5: 1）
- IELTS: **90**（`0728.md` §7: 89 + 本日 §5: 1）
