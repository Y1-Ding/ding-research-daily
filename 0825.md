# Daily Research - 2026-08-25 (21:30)

## 0) 今日3条结论

- **LangChain `count_tokens_approximately` 对截图消息低估 8–52×**（#39912, updated **2026-08-25**）：固定 85 token/图无论 1024² 还是 4K → `trim_messages` 系统性 under-budget → provider 侧 context length 失败。**Agent eval 须用真实尺寸估算 image token，不能信固定 fallback。**
- **LangGraph `durability="sync"` 写序未强制**（#8039, updated **2026-08-25**）：`put_writes`/`put` 持久化顺序 host-dependent → crash 后 replay vs re-execute 不确定。**L3 durable agent eval 须覆盖「写中途 crash → 恢复后 tool 是否重跑」探针。**
- **vLLM FlashInfer decode 扩至 SM90/SM100**（#50439, updated **2026-08-25**）：spec decode 在 SM90 走 dedicated XQA API，支持 non-causal draft attention + attention sinks；SM100 DCP CUDA graph 仍限 uniform single-token。**Agent serving eval 须标注 GPU arch + spec decode 路径，否则 latency/OOM 归因错层。**

## 1) Agent工程化（high-signal）

- [fix(core): count image tokens from image dimensions（LangChain PR #39912, updated **2026-08-25**）](https://github.com/langchain-ai/langchain/pull/39912)
  - 摘要: Fixes #39858。截图密集消息上 `count_tokens_approximately` 无论 1024×1024 还是 4K 均返回 85 → `trim_messages` under-budget 8–52×，调用在 provider 侧因 context length 失败。Fix 从 PNG/JPEG/WebP header 读尺寸估算 token（stdlib only，JPEG 可扩至 128KB 找 frame header），无尺寸时保留旧固定成本 fallback。
  - 为什么重要(对我): **token 优化 + 评测**——multimodal agent eval 若用 approximate counter 做 pre-flight trim，vision-heavy trace 会系统性假通过。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `trim_messages` + image 输入（是/否/未知）。

- [fix(perplexity): extract cache token details from input_tokens_details（LangChain PR #39913, updated **2026-08-25**）](https://github.com/langchain-ai/langchain/pull/39913)
  - 摘要: Fixes #39897。Perplexity Chat Completions / Responses API 在 `input_tokens_details` 报告 `cache_read_input_tokens` 与 `cache_creation_input_tokens`，但两条集成路径均未写入 `UsageMetadata.input_token_details` → 下游 cost dashboard 在 80%+ cache hit 时仍显示零 cache 活动。Fix 在 `_create_usage_metadata` 与 `_convert_responses_usage` 提取 cache 字段。
  - 为什么重要(对我): **token 优化 + 评测**——agent cost eval 若只看 aggregate input tokens，会 miss prompt caching 杠杆与回归。
  - 明日动作(<=20min): 写 **是/否**：你是否记录 `input_token_details.cache_read`（是/否/未知）。

- [fix(fireworks): propagate reasoning_content in streaming and outbound（LangChain PR #39909, updated **2026-08-25**）](https://github.com/langchain-ai/langchain/pull/39909)
  - 摘要: Fixes #34342。#34343 未覆盖两路径：① streaming inbound `_convert_chunk_to_message_chunk` 不读 delta 中 `reasoning_content`；② outbound `_convert_message_to_dict` 不回传 `additional_kwargs.reasoning_content` → 多轮 Fireworks thinking model（Kimi/Qwen/DeepSeek/GLM）streaming 与 history 均丢 reasoning trace。
  - 为什么重要(对我): **token 优化 + 评测**——reasoning model agent eval 若只测 non-streaming 首轮，async/stream 路径 cost 与 trace 系统性偏低。
  - 明日动作(<=20min): 写 **是/否**：你是否用 Fireworks thinking model + streaming（是/否/未知）。

- [fix(prebuilt): accept raw tool content blocks（LangGraph PR #8718, updated **2026-08-25**）](https://github.com/langchain-ai/langgraph/pull/8718)
  - 摘要: Fixes #7985。MCP tools 经 `BaseTool.invoke()` 返回 raw content block list 时，`ToolNode._normalize_tool_response` 抛 `TypeError`。Fix 将 raw block list 视为合法 `ToolMessage` content；加 `BaseTool.invoke()` 回归测。
  - 为什么重要(对我): **skill 落地 + 评测**——MCP skill agent eval 须覆盖「tool 返回 content blocks 非 string」路径，否则 production MCP 集成 silent fail。
  - 明日动作(<=20min): 写 **是/否**：你 MCP tools 是否返回 content blocks（是/否/未知）。

- [fix: fallback to non-pipeline mode on SSL error in AsyncPostgresSaver（LangGraph PR #8720, updated **2026-08-25**）](https://github.com/langchain-ai/langgraph/pull/8720)
  - 摘要: Closes #5675。AsyncPostgresSaver 在 AsyncPipeline 模式 + Supabase pooler 连接下 SSL connection closed unexpectedly。Fix 在 `from_conn_string` pipeline 模式捕获 `psycopg.OperationalError` 并 fallback 非 pipeline，保证 checkpointer 在 pipelining 不稳定环境可用。
  - 为什么重要(对我): **skill 落地 + durable agent**——durable LangGraph agent 部署在 Supabase/managed Postgres 时，eval 环境须与生产 pooler 配置对齐。
  - 明日动作(<=20min): 写 **是/否**：你是否用 AsyncPostgresSaver + managed pooler（是/否/未知）。

- [fix(checkpoint): align BaseCache set and aset parameter names（LangGraph PR #8721, updated **2026-08-25**）](https://github.com/langchain-ai/langgraph/pull/8721)
  - 摘要: Fixes #8717。`BaseCache` ABC 声明 `pairs` 参数，但 `InMemoryCache`/`RedisCache`/`SqliteCache` 实现用 `keys` → 按 ABC 签名调用 `set(pairs=...)` 直接 `TypeError`。Fix 统一为 `pairs`。
  - 为什么重要(对我): **skill 落地**——LangGraph cache 层 eval/集成若按 ABC 文档写，当前 main 上三实现均 reject；升级后须回归 cache 写入路径。
  - 明日动作(<=20min): 写 **是/否**：你是否用 LangGraph `BaseCache`（是/否/未知）。

- [fix(checkpoint): add nested checkpoint metadata round-trip coverage（LangGraph PR #8713, updated **2026-08-25**）](https://github.com/langchain-ai/langgraph/pull/8713)
  - 摘要: Fixes #8701。conformance suite 仅测 flat metadata scalar keys；nested dict/list/empty container 若 saver 静默 flatten/drop 仍可通过。Fix 加 `test_put_preserves_nested_metadata` 深嵌套 round-trip 覆盖。
  - 为什么重要(对我): **评测**——agent routing/tracing metadata 常为 nested JSON；跨 backend saver eval 须断言深结构不丢。
  - 明日动作(<=20min): 写 **是/否**：你 checkpoint metadata 是否含 nested dict（是/否/未知）。

- [durability="sync": put_writes/put persistence order is unenforced（LangGraph Issue #8039, updated **2026-08-25**）](https://github.com/langchain-ai/langgraph/issues/8039)
  - 摘要: `durability="sync"` 下 `put_writes`/`put` 持久化顺序未强制 → post-crash recovery 时 replay vs re-execute tool 行为 host-dependent。与 #7417（Cloud stale re-dispatch）、#7780（interrupt loop）为 distinct defects；含 minimal reproducible example。
  - 为什么重要(对我): **skill 落地 + 评测**——durable agent L3 eval 须设计「写中途 crash → 恢复」探针，不能假设 sync durability 即 exactly-once。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `durability="sync"`（是/否/未知）。

- [fix(protocols/ag-ui): use real tool_call_id instead of fabricating a uuid4（LlamaIndex PR #22082, updated **2026-08-25**）](https://github.com/run-llama/llama_index/pull/22082)
  - 摘要: Fixes #22068。`llama_index_message_to_ag_ui_message` 在 top-level `tool_call_id` 缺失时 mint 新 `uuid4()`，忽略 `additional_kwargs["tool_call_id"]` → 每次调用 fabricate 新 id，orphan `AssistantMessage.tool_calls[*].id` pairing。Fix 先读 `additional_kwargs`，真缺失时 fallback 到 message 稳定 `id`。
  - 为什么重要(对我): **评测 + trace 对齐**——AG-UI agent eval gold 对齐须 stable tool_call_id；random uuid 使 multi-turn tool trace 不可复现。
  - 明日动作(<=20min): 写 **是/否**：你是否用 LlamaIndex AG-UI protocol（是/否/未知）。

- [fix(ollama): stop deduping streamed tool calls by name+arguments（LlamaIndex PR #22465, updated **2026-08-25**）](https://github.com/run-llama/llama_index/pull/22465)
  - 摘要: Fixes #22464。`stream_chat`/`astream_chat` 用 `(name, arguments)` 去重 tool calls，但 Ollama wire 层每 call 有独立 `id`，ollama client pydantic 模型 drop `id`/`index` → 同名同参多 call 被误 dedupe。Fix 改去重策略保留 wire-level 区分。
  - 为什么重要(对我): **评测**——local Ollama agent eval 若 agent 并行调同名 tool，stream 路径会 silent 丢 call。
  - 明日动作(<=20min): 写 **是/否**：你是否用 Ollama streaming + 多 tool call（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Attention] Extend FlashInfer decode support on SM90 and SM100（vLLM PR #50439, updated **2026-08-25**）](https://github.com/vllm-project/vllm/pull/50439)
  - 摘要: Follow-up #49718。SM90 uniform/ragged spec decode 走 FlashInfer dedicated XQA API；支持 non-causal draft attention 与 attention sinks；移除过时 SM90 sliding-window guard（FlashInfer 0.6.17 已含 upstream fix）。SM100 DCP CUDA graph 限 uniform single-token decode（variable-length trigger recompile）。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**——spec decode 的 draft/target attention 路径因 GPU arch 分叉；agent serving benchmark 须标注 SM90 vs SM100 + spec on/off。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serve spec decode on A100/H100（是/否/未知）。

- [[Bugfix] Guard completion logprobs when sampled token missing from top logprobs（vLLM PR #53722, updated **2026-08-25**）](https://github.com/vllm-project/vllm/pull/53722)
  - 摘要: `_create_completion_logprobs` 用 `step_top_logprobs[token_id]` 索引，当 sampled token 不在 top-logprobs dict（如 top 全是 special tokens）→ `KeyError` → completion 500。Chat 路径 #17637 已 guard；本 PR 补齐 completion 路径。
  - 为什么重要(对我): **Talk ≥3 turns + eval**——agent eval 用 logprobs 做 confidence gate 时，special-token-heavy 解码会整条 trace 失败而非返回 partial signal。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 completion logprobs 做 eval gate（是/否/未知）。

- [[Bugfix][MRV2] Fix DiffusionGemma runtime OOM via tiled logits projection（vLLM PR #51953, updated **2026-08-25**）](https://github.com/vllm-project/vllm/pull/51953)
  - 摘要: Fixes #50699。DiffusionGemma full-batch logits 随并发增长 → 首 decode batch OOM。Fix 在 sampler 按 tile 投影 logits；profiling 经 `profile_run` hook 预留 transient。RTX 5090 A/B：8 concurrent × 512 tokens，util 0.9 从 OOM → 稳定；GSM8K 200q 5-shot 分数不变。
  - 为什么重要(对我): **Talk ≥3 turns + memory/Attention**——diffusion-style decode 的 logits 物化是 attention 后 OOM 根因；agent batch eval 须区分 tiled vs full projection。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serve diffusion/MRV2 模型（是/否/未知）。

- [[Kimi K3][Kernel] Support DS conv-state layout in fused KDA decode kernel（vLLM PR #53396, updated **2026-08-25**）](https://github.com/vllm-project/vllm/pull/53396)
  - 摘要: `ops.fused_kda_decode` 此前仅 SD conv-state layout；`VLLM_SSM_CONV_STATE_LAYOUT=DS`（P/D-disaggregated NIXL state transfer 所需）时 silent bypass fused path → 69 KDA layers 全走 multi-op Triton chain。Fix 将 conv-state inner strides 作 launch 参数，SD/DS 均走 fused kernel（in-proj GEMM → fused KDA decode → out-proj GEMM per layer）。
  - 为什么重要(对我): **Talk ≥3 turns + State/Attention 替代**——SSM state cache layout 与 Transformer KV block 同构 eval 问题：须区分 real fused path vs silent fallback chain。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serve Kimi K3 / KDA 模型（是/否/未知）。

## 3) 热门项目/可试个人项目

- [Spielewoy/autoprompt-skill（GitHub, updated **2026-08-25**）](https://github.com/Spielewoy/autoprompt-skill)
  - 摘要: Agent coding skill：宣称 Terminal-Bench 2.1 +14.61 分、agentic coding 任务失败率降 45%。`npm install -g autoprompt-skill` + `autoprompt` 安装到 Cursor/Claude Code 等 9 provider；v1.0.4 含 benchmark 表与 run controls（token budget、provider 切换）。中文文档在 `docs/translations/zh.md`。
  - 为什么重要(对我): **skill 落地**——可直接 A/B 你现有 agent prompt/skill vs autoprompt 在同一 eval 集上，量化 prompt engineering 杠杆。
  - 明日动作(<=20min): 写 **1 条** 你可拿 autoprompt 对比的最小 eval case（≤15 词）。
  - 估计复现成本: **低**（npm 安装 + 选 1 个 Terminal-Bench 子任务，~30min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Extensible Software in the age of LLMs（Jeremy Morrell, **2026-08-18**；Simon Willison 引用 **2026-08-19**）](https://jeremymorrell.dev/blog/extensible-software-in-the-age-of-llms/)
  - 机会信号: Cloudflare 工程师主张「稳定 accountable core + LLM 生成用户扩展」新 SaaS 形态；现代 sandbox（Dynamic Workers、V8 isolate、WASM）降低扩展部署成本。**AI 个人公司/垂域产品**可从「battle-tested core + 用户 spoken extensions」切入，而非一次性堆全功能。
  - 风险信号: 每个 extension point 扩大安全面与支持义务；host 仍须 audit trail、token budget、disable 失败代码——LLM 降 authoring 成本但不降 accountability。
  - 我可执行的一步(<=30min): 写 **1 条** 你当前 agent 工作中哪块最适合做成「用户可扩展点」而非硬编码功能（≤20 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[3718. Smallest Missing Multiple of K](https://leetcode.com/problems/smallest-missing-multiple-of-k/)（**Day 55, Aug 2026 Daily Challenge**；承接 0824 的 1872 Hard 前缀和博弈，今日 **Easy**「给定数组与 k，找最小缺失的 k 倍数」）：关键洞察——维护「已见 k 倍数集合」或按模 k 桶计数；与 agent eval「状态空间枚举 + 最小未覆盖探针」同构。明日动作：手写解法；写 **≤15 词**：「缺失 k 倍数」对你 gold 集「未覆盖类别探针」意味着什么。

- **IELTS（≤1 条）**：[IELTS Speaking Practice Tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/speaking)：0824 已轮换 Reading，今日换 **Speaking**（选 Part 1+2 各练 1 题，录音自评，~20min）。明日动作：完成 1 次 Speaking 练习，记录 **≥2 个** 流利度/词汇失误；写 **≤15 词** 你最弱的 Speaking 环节。

## 6) 噪音过滤记录

- **[Feature Proposal] Durable tool execution idempotency（LangGraph #8464）**：高价值 proposal 但尚无实现 → 信号已并入 §0 结论 #8039 durability 主题，不重复展开。
- **feat(prebuilt): Gate/Prove ActionBoundary（LangGraph #8636）**：合规/审计 feature request，与当前 skill/eval focus 弱相关 → 记入备选。
- **fix(tools-mcp): unpack 2-tuple streamable_http（LlamaIndex #22659）**：0813 已收录同 PR → semantic duplicate，今日仍为 open 无 merge → 不重复。
- **fix: ChatMessage multiple blocks SimpleChatEngine（LlamaIndex #21986）**：边缘 workflow 路径 → 记入备选。
- **docs: AutoGen Studio dependency isolation（AutoGen #8084）**：文档维护，非 eval focus → 过滤。
- **LangGraph #8719 find_tracked_packages return**：小 fix，与 focus 弱相关 → 过滤。
- **LangChain #39905/#39903 ChatTuringEngine**：新 adapter 集成，非当前 focus → 过滤。
- **LangChain #39906 model-profiles refresh**：数据刷新 → 过滤。
- **vLLM #53723 preemption_victim / #53541 RL weight tests**：infra/RL，与当前 focus 弱相关 → 过滤。
- **openai/evals**：无 **2026-08-25** 更新 → 不收录。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-08-24~25** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + Jeremy Morrell/Simon Willison ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **777**（`0824.md` §7: 767 + 本日 §1: 10）
- Theory: **351**（`0824.md` §7: 347 + 本日 §2: 4）
- Career/Business: **125**（`0824.md` §7: 124 + 本日 §4: 1）
- LC: **107**（`0824.md` §7: 106 + 本日 §5: 1）
- IELTS: **107**（`0824.md` §7: 106 + 本日 §5: 1）
