# Daily Research - 2026-09-02 (21:30)

## 0) 今日3条结论

- **Tool call 链路三条 silent corruption 同日暴露**（LangGraph #8788/#8790, vLLM #54964, created **2026-09-02**）：ToolMessage `tool_call_id` 错配未拒、validation error 被 filter 吞、Granite 4 双重 JSON encode 后 arguments 变 `{}` → **agent tool eval gold 须覆盖 id 错配 / validation 原文保留 / 已序列化 arguments 字符串三条负例。**
- **LangGraph state 默认值与 async 多模态两条接点修复**（#8794/#40143）：`Annotated[..., reducer]` 的 `Field(default_factory=...)` 首调被 `typ()` 静默丢弃；`DictPromptTemplate` async 路径 unreachable → **skill/state eval 须断言 reducer 默认值 + async multimodal prompt 实际走 `aformat`。**
- **Attention/serving 三条确定性缺陷**：PP 前向未等上一轮 tensor send 导致长 prompt garbage output（vLLM #54962）；FlashInfer MoE fused finalize 默认 nondeterministic（#54948）；GLM-5 indexer prefill 未跨 TP shard（#54951）→ **long-context benchmark 须标注 PP chunk size + MoE determinism flag + indexer TP shard 配置。**

## 1) Agent工程化（high-signal）

- [fix(langgraph): seed reducer channel with configured field default（LangGraph PR #8794, created **2026-09-02**）](https://github.com/langchain-ai/langgraph/pull/8794)
  - 摘要: Fixes #5225。`BinaryOperatorAggregate`（`Annotated[Type, reducer]` state field 的 channel）从 bare type `typ()` seed 初始值，忽略 schema 配置的 `Field(default_factory=...)`（如 `lambda: ["default"]`）→ 首调 graph 时非空 default 被静默丢弃。Fix：`BinaryOperatorAggregate` 接受 optional `default_factory`；`get_field_default` 从 Pydantic field 读取。
  - 为什么重要(对我): **skill 落地 + 评测**——agent state reducer 的 default 是 skill 初始上下文/工具列表的常见 pattern；silent discard = eval 首 turn 状态与声明 default 不一致且无 error。
  - 明日动作(<=20min): 写 **是/否**：你是否在 LangGraph state 用 `Field(default_factory=...)` + reducer（是/否/未知）。

- [fix(prebuilt): preserve model-level validation errors（LangGraph PR #8790, created **2026-09-01**）](https://github.com/langchain-ai/langgraph/pull/8790)
  - 摘要: Fixes #8648。ToolNode 为过滤 injected argument 的 validation error 时，把 model-level Pydantic error 一并丢掉；且 `loc == ()` 时加空 location prefix，model 收不到真实 validation message。Fix：保留 model-level errors；空 loc 不加前缀。
  - 为什么重要(对我): **评测 + skill 落地**——tool argument validation 是 agent eval 的核心断言点；error 被 filter = eval 误判「model 没收到有效 feedback」。
  - 明日动作(<=20min): 写 **是/否**：你是否在 ToolNode eval 断言 validation error message 原文（是/否/未知）。

- [fix(prebuilt): validate ToolMessage call IDs（LangGraph PR #8788, created **2026-09-01**）](https://github.com/langchain-ai/langgraph/pull/8788)
  - 摘要: Fixes #7989。ToolNode 此前接受 `tool_call_id` 属于其他 tool call 的 ToolMessage（direct return、list return、command update 三路径均未校验）。Fix：三路径均 reject 错配 id。
  - 为什么重要(对我): **skill 落地 + MCP eval**——multi-tool agent 若 tool result 错绑 id，下游 model 基于错误上下文继续推理；eval 须将此列为 hard fail 而非 silent accept。
  - 明日动作(<=20min): 写 **是/否**：你是否在 agent eval 覆盖 tool_call_id 错配负例（是/否/未知）。

- [[Bugfix][Frontend] Stop double-encoding tool call arguments（vLLM PR #54964, created **2026-09-02**）](https://github.com/vllm-project/vllm/pull/54964)
  - 摘要: Granite 4 等模型在 tool call 中把 `arguments` 已序列化为 JSON string（非 object）；hermes parser 再 serialize 一次 → client 收到双重 JSON string；`_postprocess_messages` decode 一次得 `str` 非 `dict`，#48922 guard 将其 coerce 为 `{}`，arguments **静默丢失**。Fix：检测已序列化 string 不再二次 encode。
  - 为什么重要(对我): **评测 + token 优化**——tool call serving eval 若 arguments 变 `{}`，function call eval 系统性 false negative；与 LangGraph MCP content block fix（#8785）形成上下游同构 bug class。
  - 明日动作(<=20min): 写 **是/否**：你是否 serving Granite 4 或 hermes tool parser 模型（是/否/未知）。

- [core: fix _convert_to_v1_from_anthropic mutating message.content on unknown blocks（LangChain PR #40138, created **2026-09-02**）](https://github.com/langchain-ai/langchain/pull/40138)
  - 摘要: `_convert_to_v1_from_anthropic` 在 unrecognized content block 的 `else` 分支把同一 `block` dict 引用赋给 `new_block["value"]`，随后 `.pop("index")` **原地修改原始 AIMessage.content**。Fix：deep copy block 后再 pop。
  - 为什么重要(对我): **评测 + trace 完整性**——agent trace 持久化后 replay eval 读到被污染的 content；non-standard block（thinking/tool hybrid）是 frontier model 常见形态。
  - 明日动作(<=20min): 写 **是/否**：你是否持久化 Anthropic AIMessage 做 replay eval（是/否/未知）。

- [fix(core): await aformat on DictPromptTemplate in async multimodal path（LangChain PR #40143, created **2026-09-02**）](https://github.com/langchain-ai/langchain/pull/40143)
  - 摘要: Closes #39835。`_StringImageMessagePromptTemplate.aformat()` 的 `DictPromptTemplate` 分支调用 sync `prompt.format()`，而 sibling 分支正确用 `await prompt.aformat()` → async 公开 API 无法到达 `DictPromptTemplate.aformat`。Fix：对齐为 `await prompt.aformat(**inputs)`。
  - 为什么重要(对我): **skill 落地 + 多模态 agent**——vision agent 的 async serving 路径若 unreachable，eval 在 sync 与 async 间行为不一致。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `DictPromptTemplate` 做 async 多模态 prompt（是/否/未知）。

- [fix(openai): route GPT-5.6 family to responses API（LangChain PR #40135, created **2026-09-02**）](https://github.com/langchain-ai/langchain/pull/40135)
  - 摘要: OpenAI GPT-5.6 家族（`gpt-5.6` alias、Terra、Luna）在 function tools + reasoning 场景须走 Responses API；此前仅 Sol 被覆盖，其余 variant 默认 reasoning effort 下 Chat Completions 报错。Fix：扩展 model inference + 各 variant 单元测试。
  - 为什么重要(对我): **skill 落地**——agent tool calling 的 API routing 是 skill 能否运行的前提；eval 须按 model alias 标注 API backend。
  - 明日动作(<=20min): 写 **是/否**：你是否用 ChatOpenAI 接 GPT-5.6 非 Sol variant + function tools（是/否/未知）。

- [fix: UPSERTS_AND_DELETE not deleting documents with shared hash（LlamaIndex PR #22935, created **2026-09-02**）](https://github.com/run-llama/llama_index/pull/22935)
  - 摘要: Fixes #22706。`get_all_document_hashes()` 以 hash 为 key，多 document 同 content hash 时 collision 丢 doc_id；`UPSERTS_AND_DELETE` 用 `.values()` 取 existing doc_ids → 共享 hash 的 document 删除失败。Fix：改 `get_all_ref_doc_info().keys()`。
  - 为什么重要(对我): **RAG eval + skill 落地**——ingestion pipeline eval 若 duplicate content 是合法场景，delete gold 须按 ref_doc_id 而非 hash 断言。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `DocstoreStrategy.UPSERTS_AND_DELETE`（是/否/未知）。

- [Add transport, retry and stream-event coverage to llama-index-llms-openai（LlamaIndex PR #22938, created **2026-09-02**）](https://github.com/run-llama/llama_index/pull/22938)
  - 摘要: tests-only。为 `http_client`/`async_http_client` 补覆盖：sync/async/streaming 对 local `http.server` stub 断言 custom client marker header 被收到；另覆盖 transport/retry/stream-event。无 library 代码改动。
  - 为什么重要(对我): **评测基础设施**——CONTRIBUTING 要求 mock remote 而非 live API；此 pattern 可直接复用到你的 agent eval HTTP mock fixture。
  - 明日动作(<=20min): 读 PR 中 stub server 测试，写 **≤15 词**：能否套用到你的 OpenAI-compatible eval mock。

- [docs: add MCP tool poisoning warning to McpWorkbench（AutoGen PR #8158, created **2026-09-02**）](https://github.com/microsoft/autogen/pull/8158)
  - 摘要: Related #7266/#7427。在 McpWorkbench docstring 新增 MCP tool poisoning 警告：恶意 MCP server 可在 tool description 嵌入 injected instructions。
  - 为什么重要(对我): **skill 落地 + MCP eval**——Harness Cursor plugin 与 agent skill 均走 MCP；eval 须将 tool description 视为 untrusted input 并加 poisoning 负例。
  - 明日动作(<=20min): 写 **≤15 词**：你的 MCP skill eval 是否覆盖 malicious tool description 负例。

- [docs: add agent safety and guardrails guide（AutoGen PR #8159, created **2026-09-02**）](https://github.com/microsoft/autogen/pull/8159)
  - 摘要: Fixes #6017。magentic-one guide 新增 production safety：code execution safety、MCP server safety、prompt injection prevention、resource protection 四条 guardrail checklist。
  - 为什么重要(对我): **评测 + skill 落地**——可直接对照你的 agent eval playbook 补「MCP poisoning / sandbox / injection」三条门禁 checklist。
  - 明日动作(<=20min): 对照 guide 写 **3 条** 你现有 eval 已覆盖 vs 未覆盖的 guardrail（各 ≤15 词）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix][Core] Wait for the previous PP tensor sends before the next forward pass（vLLM PR #54962, created **2026-09-02**）](https://github.com/vllm-project/vllm/pull/54962)
  - 摘要: Fixes silent output corruption from #49274。GLM-5.3（`GlmMoeDsaForCausalLM`）`--pipeline-parallel-size 4/8` + chunked prefill + prefix caching：8.2k–12k token prompt 多数 fresh prefill 输出 garbage（reasoning 1–100 token 后 digit soup）。根因：下一轮 forward 未等上一轮 PP tensor send 完成。Fix：send 完成后再启动下一 forward。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/PP**——pipeline parallelism 是 long-context serving 核心路径；silent corruption 无 error = attention eval 读到随机 garbage 仍 pass。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 PP>1 + chunked prefill serving long-context model（是/否/未知）。

- [[Perf][GLM5] Shard long-context indexer prefill rows across TP（vLLM PR #54951, created **2026-09-02**）](https://github.com/vllm-project/vllm/pull/54951)
  - 摘要: GLM-5.3-Flash sparse indexer prefill 行在 TP rank 间 cost-balanced shard；本地算 MQA logits/top-k，all-gather 仅 final int32 token indices；保留 kpool expansion/tail/mixed decode/prefill 语义。gate：long MQA prefills；排除 DCP/PCP、disabled PynCCL、batch-invariant、ROCm、FULL cudagraph。
  - 为什么重要(对我): **Talk ≥3 turns + sparse attention/indexer**——sparse MLA indexer 是 attention 子系统的 pre-attention 路由；未 shard = 单 rank 瓶颈 + eval latency 不可复现。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serving GLM-5 sparse indexer model（是/否/未知）。

- [[Kernel] Add VLLM_FLASHINFER_MOE_FUSED_FINALIZE to disable the nondeterministic MoE finalize（vLLM PR #54948, created **2026-09-02**）](https://github.com/vllm-project/vllm/pull/54948)
  - 摘要: Fixes #54945。`flashinfer.fused_moe.cutlass_fused_moe` 的 `use_fused_finalize` 默认 fused epilogue（atomics）**nondeterministic**；vLLM 从未传此参数 → temperature=0 同 prompt 不同 logits。Fix：新增 `VLLM_FLASHINFER_MOE_FUSED_FINALIZE`（default `1` 行为不变），可设为 `0` 关闭 nondeterministic finalize。
  - 为什么重要(对我): **Talk ≥3 turns + MoE/Attention epilogue**——MoE finalize 是 attention 后最后一算子；nondeterminism 使 agent eval 的 logprob-based assertion 不可复现。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 FlashInfer cutlass MoE + temperature=0 eval（是/否/未知）。

- [[Bugfix][CPU][Spec Decode] Honor use_fp64_gumbel in CPU recovered-token sampling（vLLM PR #54952, created **2026-09-02**）](https://github.com/vllm-project/vllm/pull/54952)
  - 摘要: Fixes #54843。`--use-fp64-gumbel` 承诺 spec-decode recovery 用 fp64 exponential-race noise；CPU backend shim 将 fp64 `inv_q` downcast 到 fp32 再调 C++ kernel（只读 `float*`）→ flag 在 CPU 上 silent no-op，CPU/GPU 同 random draw 在 lower-tail  regime 产生不同 recovered token。
  - 为什么重要(对我): **Talk ≥3 turns + spec decode sampling**——spec decode 是 token 优化核心路径；CPU/GPU divergence = cross-platform eval 不可比。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 CPU backend + spec decode + `--use-fp64-gumbel`（是/否/未知）。

- [Petri 2.0: New Scenarios, New Model Comparisons, and Improved Eval-Awareness Mitigations（Anthropic Alignment, **2026**）](https://alignment.anthropic.com/2026/petri-v2/)
  - 摘要: Petri automated behavioral auditing tool 2.0：新增 eval-awareness realism mitigations、70 新 seed scenarios、更多 frontier model 对比结果。Claude Opus 4.5 与 GPT-5.2 在 misuse/deception 维度显著优于前代；Gemini 3 Pro verbalized eval-awareness 上升。Auditor 用 Claude Sonnet 4.5 + GPT-5.1；judge 三方独立（Opus 4.5 / Gemini 3 Pro / GPT-5.1）。
  - 为什么重要(对我): **Talk ≥3 turns + Agent alignment eval**——Petri 是 automated agent auditing 参考架构；eval-awareness mitigation 直接映射你的 agent eval「模型是否在 eval 中表演」探针设计。
  - 明日动作(<=20min): 读 Petri 2.0 eval-awareness mitigation 一节，写 **≤15 词**：哪条 mitigation 可迁入你的 L2 eval。

## 3) 热门项目/可试个人项目

- [A quote from Rick Brewster（Simon Willison Blog, **2026-09-02**）](https://simonwillison.net/2026/Sep/2/rick-brewster/)
  - 摘要: Paint.NET 作者 Rick Brewster 谈 WINE 兼容：Claude 生成 180k 行 clean-room Direct2D rewrite（`PaintDotNet.Windows.Direct2D1.Managed.dll`），但「mostly vibe coded」、无法人工 review 全部；需 babysit COM reference counting、架构决策。对比 rest of Paint.NET 700k 行 / 20 年经验。
  - 为什么重要(对我): **低杠杆 side project + agentic engineering**——Karpathy「eval design + diff review」在 180k 行 agent 产出上的真实尺度；可作为你 agent eval playbook 的「不可全量 review 时如何设 gate」案例。
  - 明日动作(<=20min): 写 **3 条** 若 agent 产出 10k+ 行，你的 eval gate 会查什么（各 ≤15 词）。
  - 估计复现成本: **低**（读 quote + 写 3 条 gate，~15min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Improving our alignment and security efforts（Anthropic, **2026-08-31**）](https://www.anthropic.com/news/improving-alignment-security-efforts)
  - 机会信号: 7/30 三场 cyber eval incident 后，Anthropic 暂停外部 cyber eval、加固 sandbox（prompt boundary + sealed sandbox verification + real-time monitoring）；~150 产品工程师 + 研究员转向 infra hardening；offline monitoring 扩至大部分 internal frontier agentic usage；**agent eval + containment 成为 frontier lab 标配投入** → 你的 agent eval/skill security 经验可映射为「alignment infra」差异化。
  - 风险信号: 事件本身说明 eval sandbox 单层防御不足；行业可能收紧 pre-release eval 访问；叙事偏 crisis response 非产品化 playbook。
  - 我可执行的一步(<=30min): 写 **3 条** 你现有 agent eval 的 sandbox 层数 vs Anthropic 描述的 multi-layer defense（各 ≤15 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[2948. Make Lexicographically Smallest Array by Swapping Elements](https://leetcode.com/problems/make-lexicographically-smallest-array-by-swapping-elements/)（**Day 63, Sep 2026 Daily Challenge**, verified via framerc.cn daily post **2026-09-02** + LeetCode problem page；承接 0901 的 3568「网格 BFS 最短路径」，今日 **Medium**「`|nums[i]-nums[j]|<=limit` 可交换 → 值域连续段内排序填回原位 = DSU/排序分组」）：关键洞察——排序后相邻差 ≤limit 的元素同连通分量，分量内升序值填回分量索引升序位置即字典序最小；与 agent eval「等价状态类（连通分量）内取最优排列」同构。明日动作：手写分组排序解法；写 **≤15 词**：「连通分量」对你 eval 状态等价类意味着什么。

- **IELTS（≤1 条）**：[IELTS Writing Practice Tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/writing)：0901 已轮换 Listening，今日换 **Writing**（选 Task 1 图表题或 Task 2 议论文，限时 ~20min）。明日动作：完成 1 次 Writing，记录 **≥2 个** 语法/衔接失误；写 **≤15 词** 你最弱的 Writing 维度（Task Response / Coherence / Lexical / Grammar）。

## 6) 噪音过滤记录

- **chore(deps) PRs（LangGraph #8792, LangChain #40139–#40141）**：依赖 bump / typo，非工程信号 → 过滤。
- **docs PRs（AutoGen #8160/#8161/#8163, LangGraph #8789）**：文档补充有价值但 #8158/#8159 已覆盖 MCP/safety focus → 不重复展开。
- **[[ROCm][Perf] W4A16 skinny GEMM zero-points（vLLM #54965）**：ROCm quant perf，与当前 focus 弱相关 → 过滤。
- **[Bugfix] Chunk FP8 dummy weight initialization（vLLM #54958）**：dummy load OOM fix，偏 infra 验证 → 过滤。
- **[DeepSeek-V4] Batch-invariant kernels preview（vLLM #54955）**：preview DO NOT MERGE，待 split 后再跟进 → 过滤。
- **WIP: Unify MTP/STP/Prefix Caching for ReplaySSM（vLLM #54953）**：WIP 无 body → 过滤。
- **fix(cohere): empty messages bugs（LlamaIndex #22933/#22937）**：provider-specific，与当前 focus 弱相关 → 过滤。
- **Allow openai 3.x（LlamaIndex #22939）**：依赖范围 bump → 过滤。
- **0830/0901 已收录项**：ToolNode MCP content block #8785、async max_concurrency #8784、SWA tile #54718、FA device 0 #54758 → semantic duplicate，不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-02** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + Simon Willison + Anthropic ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **863**（`0901.md` §7: 852 + 本日 §1: 11）
- Theory: **383**（`0901.md` §7: 378 + 本日 §2: 5）
- Career/Business: **133**（`0901.md` §7: 132 + 本日 §4: 1）
- LC: **115**（`0901.md` §7: 114 + 本日 §5: 1）
- IELTS: **115**（`0901.md` §7: 114 + 本日 §5: 1）
