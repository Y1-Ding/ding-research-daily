# Daily Research - 2026-07-12 (21:30)

## 0) 今日3条结论

- **LangChain #38809 暴露 tool schema 的 `kwargs` 幽灵字段**：带 `**kwargs` 签名的 tool 会生成用户可见的 `kwargs` object 字段，模型可能传入无效参数 → skill 落地必须把「生成 schema = 真实可调用签名」写成 invariant，并在 collect 阶段加 schema 快照测试。
- **vLLM #48402 揭示 prefix caching 可「静默失效」**：hybrid Mamba2 模型上 `block_size > max_model_len` 时 prefix cache 永远 0 hit、无警告 → token 优化若依赖 `--enable-prefix-caching`，必须先校验 block_size 与 max_model_len 关系，否则 A/B 会误判 caching 无效。
- **AgentLens（arXiv 2607.06624）把 coding agent eval 从 pass/fail 扩到全轨迹评审**：正式验证 + LLM trajectory review + 可读解释，用于 nightly 回归与版本对比 → 你的 L2/L3 eval 应加「过程可读分」，而不只记最终答案 bit。

## 1) Agent工程化（high-signal）

- [Fix tool schema generation exposing args and kwargs fields（LangChain PR #38809 opened **2026-07-12**）](https://github.com/langchain-ai/langchain/pull/38809)
  - 摘要: Fixes #38780。`BaseTool` 从函数签名生成 JSON schema 时，Pydantic 为 `*args`/`**kwargs` 生成占位字段；带 `def _run(query: str, **kwargs: Any)` 的 tool 会暴露 `{"query": ..., "kwargs": {"type": "object"}}`，模型可把 `kwargs` 当合法入参传入，但运行时并非真实参数。Fix：过滤 variadic 占位字段。
  - 为什么重要(对我): **skill 落地**——skill tool 定义若用 `**kwargs` 透传，schema 污染会引入 phantom tool args，eval trace 出现「模型传了 kwargs 但 harness 忽略」的假交互；与 0710 verdict-injection 同属「schema/解析层 ≠ 真实语义」族。
  - 明日动作(<=20min): 写 **是/否**：你 skill tools 签名是否含 `**kwargs`（是/否）；若是，打印 1 个生成 schema 检查有无 `kwargs` 字段。

- [AgentLens: Production-Assessed Trajectory Reviews for Coding Agent Evaluation（arXiv **2607.06624**, submitted **2026-07-07**）](https://arxiv.org/abs/2607.06624)
  - 摘要: 交互式 coding agent benchmark：除 pass/fail 外，用 LLM 写 trajectory review + side-by-side 对比，每条 run 产出可读评分解释。正式验证（有客观 check 时）+ 轨迹评审结合；用于诊断模型行为、对比 agent 版本、nightly 产品回归。开源 https://github.com/agent-lens/agent-lens-bench。
  - 为什么重要(对我): **eval 信号**——与 0711 Harness MDP/HMS 互补：HMS 测过程成熟度事件，AgentLens 测「用户实际感知的整条轨迹」；map_prompt_test 可借鉴「可读 review」做 L3 人工 spot-check 的自动初筛。
  - 明日动作(<=20min): 读 README；写 **≤15 词**：你现有 eval 是否产出 trajectory 可读解释（有/无）。

- [[Bugfix] Warn when prefix caching is inert on hybrid models (block_size > max_model_len)（vLLM PR #48402 opened **2026-07-12**）](https://github.com/vllm-project/vllm/pull/48402)
  - 摘要: Fixes #48401。hybrid Mamba2（如 NemotronH）上 attention block_size 被抬到 mamba page size，可超过 `max_model_len`；block-hash prefix caching 只记录完整 block，当 `block_size > max_model_len` 时永远无法填满 block → **0 hits 且无提示**。证据：Nemotron-3-Super-120B-NVFP4，block_size=8320，`max_model_len=4096` 时 prefix_cache_hits=0；提到 8192 后 TTFT 重复请求 7.3s→0.3s。Fix：config 校验时 warning。
  - 为什么重要(对我): **token 优化**——长 skill prompt caching 是核心杠杆；若 serving 层 caching 静默失效，collect→test 的 token A/B 会得出错误结论；与 0711 Anthropic cache token 丢失同属「成本归因盲区」。
  - 明日动作(<=20min): 写 **是/否**：你 agent serving 是否开 prefix caching 且校验过 hit rate（是/否/未测）。

- [Checkpoint serialization rejects range and PurePath variants (TypeError)（LangGraph issue #8326 opened **2026-07-12**）](https://github.com/langchain-ai/langgraph/issues/8326)
  - 摘要: `JsonPlusSerializer._msgpack_default` 无法序列化 `range`、`pathlib.PurePosixPath`/`PureWindowsPath`（非 `Path` 子类，绕过现有 `isinstance(obj, pathlib.Path)` 检查）。与 #8185（Fraction/complex）同根因。MRE：`s.loads_typed(s.dumps_typed(range(0,10,2)))` → `TypeError: Type is not msgpack serializable: range`。
  - 为什么重要(对我): **skill 落地 + eval**——agent state 若含 `range`/PurePath（如文件枚举、分页迭代器），checkpoint 持久化会炸，parallel eval replay 不一致；skill harness 应列「state 可序列化类型白名单」。
  - 明日动作(<=20min): 写 **是/否**：你 agent state 是否可能含 range/PurePath（是/否/未知）。

- [fix: disable autocommit when using AsyncPipeline to prevent SSL connection errors（LangGraph PR #8328 opened **2026-07-12**）](https://github.com/langchain-ai/langgraph/pull/8328)
  - 摘要: `AsyncPostgresSaver` + `pipeline=True` 时连接以 `autocommit=True` 创建，与 psycopg pipeline 模式冲突（pipeline 需显式事务）；导致 invalid pipeline nesting → SSL connection drops。Fix：`pipeline=True` 时设 `autocommit=False`。Closes #5675。
  - 为什么重要(对我): **eval 基础设施**——长时 agent eval 用 Postgres checkpointer + pipeline 是常见生产路径；连接随机 drop = eval run 中途丢 checkpoint、trace 断裂；与 0710 delta channel counter 错位同属「持久化层静默破损」。
  - 明日动作(<=20min): 写 **是/否**：你 LangGraph eval 是否用 AsyncPostgresSaver + pipeline（是/否）。

- [Add runtime LoRA weight updates（vLLM PR #48409 opened **2026-07-12**）](https://github.com/vllm-project/vllm/pull/48409)
  - 摘要: 新增 `start_lora_weight_update` 请求，复用 `start_weight_update`/`update_weights`/`finish_weight_update` 事务；仅通过 IPC/NCCL 传输 LoRA tensor（非全模型）；推理全程 pause；接收后重建并原子替换已加载 adapter，保留 active/LRU pin；新增 RLHF 开发 endpoint + 同步/异步 engine API。
  - 为什么重要(对我): **skill 落地 + eval**——在线 LoRA 更新使「冻结 eval harness + 热更新 skill adapter」成为可能；eval 需分层报告 adapter 版本，避免 RLHF 中途换权重导致分数不可比。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否用 LoRA adapter 做 skill 特化（是/否）。

## 2) 原理深化（Attention/Transformer/Agent）

- [A Hippocampus for Linear Attention: An Exact Memory for What the Recurrent State Forgets（arXiv **2607.02303**, July 2026）](https://arxiv.org/html/2607.02303v1)
  - 摘要: 线性 attention（GDN 等）是高效「新皮层」压缩器，但固定大小 recurrent state 是**有损记忆**——多 item 关联召回、needle-in-haystack、远距离 token 复制会失败。提出 HOLA：给线性 backbone 加「海马体」——有界精确 episodic cache，优先存储 surprise（state 不应吸收的 KV 对）；读时用精确检索而非 soft averaging。框架：semiparametric test-time memory regression。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) agent 长对话 KV 税 vs 线性 state 遗忘的 trade-off？(2) ≥3 turn tool trace 中「关键证据 token」是否应走 episodic cache 而非 recurrent state？(3) 你的 long-context eval 是否测 recall-after-overwrite？
  - 明日动作(<=20min): 写 **≤15 词**：你 agent context 是否用 linear/GDN backbone（是/否/未知）。

- [Sparse Delta Memory: Scaling the State of Linear RNNs through Sparsity（arXiv **2607.07386**, July 2026）](https://arxiv.org/html/2607.07386v1)
  - 摘要: GDN update rule 可稀疏化 → **Sparse Delta Memory (SDM)**：同等算力下 state 扩大三个数量级；RULER 长上下文 recall 显著优于 GDN；学习初始 state 可存储预训练知识；8B activated 参数、>1T token 训练：SDM loss 低于 GDN，短上下文精度略优。作者指出 SDM 对「扩展序列 agent 长期记忆」有潜力。
  - 为什么重要(对我): **Attention 机制 + Agent**：稀疏 delta update 是 linear attention 扩 state 的新路径；与 0710 2607.07706「delta-style rank-1 几何校正」形成「机制—规模—agent 记忆」三角；multi-turn eval 应关注 state 容量边界而非只看 context window 数字。
  - 明日动作(<=20min): 写 **≤15 词**：你 long-context eval 是否测 recall 而非仅 perplexity（有/无）。

- [[Bugfix] Size the sparse-indexer expanded block table from the runner's block-table width（vLLM PR #48404 opened **2026-07-12**）](https://github.com/vllm-project/vllm/pull/48404)
  - 摘要: DSA sparse indexer decode 路径：`expanded_block_table_buffer` 列宽用 `cdiv(max_model_len, block_size * cp_world_size)`，但 runner 传入的 `block_table` 宽为 `cdiv(max_model_len, block_size)`（未除 CP）。TP8/DCP2/1M 时两宽差 2×（8192 vs 16384）：variable-length 路径 `RuntimeError`；uniform 路径更糟——copy loop 被 dest 宽度 bound，**静默截断** block indices。Fix：从 runner 实际 block-table 宽度分配 buffer。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：sparse attention indexer 的 block table 与 expanded buffer 不一致 = multi-turn KV 寻址在 serving 层静默丢块；(1) 你的 ≥3 turn eval 是否在 TP>1 下跑过？(2) 与 0711 FA3 AOT metadata divergence 是否同族「元数据≠实际布局」？(3) eval 是否报告 TP/CP 配置？
  - 明日动作(<=20min): 写 **是/否**：你 agent serving 是否用 sparse attention / DSA indexer（是/否/未知）。

## 3) 热门项目/可试个人项目

- [agent-lens/agent-lens-bench（GitHub, updated **2026-07-10**）](https://github.com/agent-lens/agent-lens-bench)
  - 摘要: AgentLens 官方开源 benchmark；交互式 coding agent 全轨迹评估：formal verification + LLM trajectory review + side-by-side；用于 nightly 回归与版本对比；README 含 leaderboard 与 harness 接入说明。
  - 为什么重要(对我): **eval 杠杆**——可把「可读 trajectory review」移植到现有 gold 集，作为 L3 人工评审的自动初筛；直接映射 map_prompt_test collect 固定、test 自动产出解释。
  - 明日动作(<=20min): clone 仓库；读 README 前 40 行；写 **≤15 词** 你最该映射的 1 条 eval 维度。
  - 估计复现成本: **中**（需 Java coding 环境与 judge API；无 frontier 训练）。

- [[Attention] Skip sparse indexer for short sequences（vLLM PR #48407 opened **2026-07-12**）](https://github.com/vllm-project/vllm/pull/48407)
  - 摘要: sparse indexer 在短序列（query len < topk 边界）时，top-k score range 为空或 tie/inf 结果不可靠；Fix：`fill_dense_indices_for_short_sequences` 让短行忽略 score、直接按 block_table 映射 dense indices。测试：`seq_len=2050, topk=2048` 时前 3 行 score range 为空、末行正常。
  - 为什么重要(对我): **token + attention**——短 prompt agent turn 若走 sparse indexer 可能得到错误 KV 选择；multi-turn 前几轮（短 context）与后几轮（长 context）attention 行为不一致，eval 需分层报告 turn 位置。
  - 明日动作(<=20min): 写 **是/否**：你 serving 是否开 sparse attention indexer（是/否/未知）。
  - 估计复现成本: **高**（需 DSA/DeepSeek V3.2 类模型 + CUDA；非通用 demo）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [How Claude Code is used in practice（Anthropic Research, **2026**）](https://www.anthropic.com/research/claude-code-expertise)
  - 机会信号: **领域 expertise > 纯 coding 能力**——~40 万 session 分析：expert 评级 session 验证成功率是 novice 的 2×+；novice 遇 trouble 放弃率数倍于 expert；**每句指令让 Claude 做更多工作**（direction efficiency）是 expert 标志 → 出海/一人公司 signature 可打在「垂域 agent 编排 + eval」，而非追 frontier 训练。
  - 风险信号: 无 domain command 的用户从同一工具获益极少；「人人都能 coding」叙事被数据部分反驳——competence 而非 mastery 捕获大部分收益；对外 demo 需展示 domain steering 而非裸模型能力。
  - 我可执行的一步(<=30min): 列 **3 条** 你最强垂域（法律/车控/Agent eval）里「一句指令驱动多步」的真实例子（各 ≤12 词），标哪条可脱敏成对外 case study。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 802 Find Eventual Safe States](https://leetcode.com/problems/find-eventual-safe-states/)：有向图找「所有路径终到终端节点」的安全点；经典解法 reverse graph + topological sort（或 DFS 三色标记环）。明日动作：先画 Example 1 的环 vs 安全节点；再写 reverse-graph 核心 20 行（≤25min）；写 **≤15 词**：与 agent tool 调用链「无环终止」检测的类比。

- **IELTS（≤1 条）**：[IELTS Speaking 2026: Band 7+ Three-Part Playbook（XMocks）](https://blog.xmocks.ai/ielts-speaking-2026-the-band-7-three-part-playbook-for-your-june-and-july-test-windows/)：Part 3 扩展答案框架 **Point–Because–Example–Although**（主张→原因→例子→让步），比单句回答更能展示 GRA 复杂句与灵活性。明日动作：选 1 个 technology Part 3 题按 PBEA 录音 2min（≤20min）；写 **≤15 词** 你最弱 criterion（Fluency/LR/GRA/Pronunciation 选 1）。

## 6) 噪音过滤记录

- **LlamaIndex #22311/#22315 cache token / gather leak**：`0711.md` 已收录 → 不重复。
- **vLLM #48343/#48348 streaming tool parser**：`0711.md` 已收录 → 不重复。
- **Harness MDP arXiv 2607.05458**：`0711.md` 已收录 → 不重复。
- **OpenAI SWE-Bench Pro 审计 / GPT-5.6 / ChatGPT Work**：`0709–0711.md` 已覆盖 → 不重复。
- **arXiv 2607.07706 linear attention**：`0710.md` 已收录 → 不重复。
- **LlamaIndex #22322–#22331 存储 reader 重名覆盖**：infra bug，弱于今日 skill/eval/token focus → 丢弃。
- **LangChain #38810 model-profiles refresh / #38808 gateway keys**：infra chore → 丢弃。
- **vLLM #48400 MoE NVFP4 / #48398 DeepGEMM / #48397 WSL circular import**：kernel/infra → 不展开。
- **vLLM #48396 FlashInfer long-prefill regression**：高信号但偏 serving perf benchmark，与今日 agent eval focus 弱于 #48402/#48404 → 记入噪音（可在 serving 升级时回溯）。
- **Simon Willison GPT-5.6 / Muse Spark 7/9 帖**：`0709.md` 已收 GPT-5.6 → 不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-12** 原帖 URL → 不收录。
- **@gefei55 / @ianneo_ai 职业帖**：无法验证当日原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv + Anthropic 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **509**（`0711.md` §7: 503 + 本日 §1: 6）
- Theory: **209**（`0711.md` §7: 206 + 本日 §2: 3）
- Career/Business: **91**（`0711.md` §7: 90 + 本日 §4: 1）
- LC: **73**（`0711.md` §7: 72 + 本日 §5: 1）
- IELTS: **73**（`0711.md` §7: 72 + 本日 §5: 1）
