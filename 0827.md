# Daily Research - 2026-08-27 (21:30)

## 0) 今日3条结论

- **LangGraph Platform 上 `update_state` 可永久擦除消息历史**（#8654, updated **2026-08-27**）：config-injected checkpointer 场景下 `get_state` 返回空 `messages[]`，`update_state` 按空状态 fold 并 commit → **durable agent eval 须单独测 Platform 路径的 state API，不能把 thread 历史当 ground truth。**
- **ToolRuntime 注入曾按参数名 `runtime` 匹配**（#8729, updated **2026-08-27**）：模型可控的 `runtime: str` 被误当 `ToolRuntime` 注入目标 → **function tool agent eval 须覆盖「同名非注入参数」路径，否则 side-effect tool 可能 silent 丢参。**
- **token/cache 细粒度统计修复持续推进**（LangChain #39923 merged **2026-08-27**；#39946 同日 closed）：Perplexity Responses API 的 `cache_read`/`cache_creation` 现映射到 `input_token_details`；OpenAI-compatible 端点的 provider-specific nested token 字段亦在合并 → **cost eval 须断言 cache + provider-specific token fields 是否完整保留。**

## 1) Agent工程化（high-signal）

- [fix(langgraph): update_state can permanently erase messages when the checkpointer is config-injected（LangGraph PR #8654, updated **2026-08-27**）](https://github.com/langchain-ai/langgraph/pull/8654)
  - 摘要: Fixes #8653。LangGraph Platform 编译图时不内置 checkpointer，由 run config 注入。该路径下 `get_state`/`get_state_history` 对已有完整对话的 thread 返回 `messages: []`；更严重的是 `update_state` 按错误空状态 fold 并 commit，**永久擦除**消息历史。
  - 为什么重要(对我): **skill 落地 + durable agent eval**——Platform 部署的 agent skill gold 集若用 `update_state` 做 HITL 修正，当前可能把 trace 直接抹掉；L3 eval 须加「update 前后 messages 长度不变/只增」探针。
  - 明日动作(<=20min): 写 **是/否**：你是否在 LangGraph Platform 上用 `update_state`（是/否/未知）。

- [fix(prebuilt): only inject ToolRuntime into ToolRuntime-typed parameters（LangGraph PR #8729, updated **2026-08-27**）](https://github.com/langchain-ai/langgraph/pull/8729)
  - 摘要: Fixes #8728。`_get_all_injected_args` 除类型注解外还用参数名 `runtime` 匹配 → 普通 `runtime: str` 参数被当作 `ToolRuntime` 注入目标，`_inject_tool_args` 静默改写调用约定。
  - 为什么重要(对我): **skill 落地 + 评测**——tool schema 里常见 `runtime`/`env` 等业务字段名；eval 须覆盖「同名非注入参数 + irreversible side-effect tool」组合。
  - 明日动作(<=20min): 写 **是/否**：你 function tools 是否有名为 `runtime` 的非注入参数（是/否/未知）。

- [fix(perplexity): map Responses API cache tokens into input_token_details（LangChain PR #39923, merged **2026-08-27**）](https://github.com/langchain-ai/langchain/pull/39923)
  - 摘要: Fixes #39897。`ChatPerplexity` Responses (Agent) API 路径丢弃 Perplexity 报告的 cache token 计数（例：1000 input 中 950 cache-related 全部丢失）。Fix 将 `input_tokens_details.cache_read_input_tokens`/`cache_creation_input_tokens` 映射到标准 `InputTokenDetails.cache_read`/`cache_creation`。
  - 为什么重要(对我): **token 优化 + 评测**——agent cost dashboard 若用 Perplexity Agent API + LangChain usage_metadata，cache 收益会被系统性 under-report。
  - 明日动作(<=20min): 写 **是/否**：你是否用 Perplexity Agent API + LangChain usage_metadata（是/否/未知）。

- [fix(core): make `StructuredTool` JSON-serializable（LangChain PR #39631, updated **2026-08-27**）](https://github.com/langchain-ai/langchain/pull/39631)
  - 摘要: `@tool` 装饰的 `StructuredTool` 无法 `model_dump(mode="json")`——`args_schema` 是 Pydantic model class、`func`/`coroutine` 是 callable，均无 JSON 形态。Fix 为 JSON mode 提供可序列化表示，便于 trace/log/eval artifact 落盘。
  - 为什么重要(对我): **skill 落地 + 评测**——agent eval harness 若要把 tool definition 写入 JSONL trace 或 replay 配置，当前 main 会抛 `PydanticSerializationError`。
  - 明日动作(<=20min): 写 **是/否**：你是否序列化 tool schema 到 eval artifact（是/否/未知）。

- [fix(core): preserve upsert data when transformations fail（LlamaIndex PR #22798, updated **2026-08-27**）](https://github.com/run-llama/llama_index/pull/22798)
  - 摘要: Fixes #22639。`IngestionPipeline` 在 transformation 运行**之前**就删除待 upsert 的 docstore/vector-store 数据 → transformation 失败时旧索引被删、新数据未写入。Fix 将 pending deletion 推迟到 transformation 成功之后（sync/async 均覆盖）。
  - 为什么重要(对我): **skill 落地 + RAG eval**——ingestion skill eval 须测「transform 中途 fail → 外部检索结果不变」；否则 eval pass 但 production 丢索引。
  - 明日动作(<=20min): 写 **是/否**：你是否用 LlamaIndex IngestionPipeline + transformation（是/否/未知）。

- [fix(instrumentation): avoid shared dispatcher handler defaults（LlamaIndex PR #22847, updated **2026-08-27**）](https://github.com/run-llama/llama_index/pull/22847)
  - 摘要: `Dispatcher.__init__` 对 event/span handler 使用可变 list 默认参数 → 多 dispatcher 实例可能共享同一 handler 列表（Ruff B006）。Fix 改为 `None` 默认并在构造时创建新 list。
  - 为什么重要(对我): **评测 + trace**——并行 agent eval 若共享 dispatcher，trace 事件可能交叉污染；多 worker eval 须断言 handler 隔离。
  - 明日动作(<=20min): 写 **是/否**：你是否并行跑 LlamaIndex instrumented agent eval（是/否/未知）。

- [[Detokenizer] Cache single-token decode results for logprobs（vLLM PR #54050, updated **2026-08-27**）](https://github.com/vllm-project/vllm/pull/54050)
  - 摘要: `convert_ids_list_to_tokens()` 对每个 logprob top-k token 单独 `tokenizer.decode([tid])` → Python↔Rust 往返 ~5–20µs/次；`num_logprobs=20`、batch 64 时每 step ~1300 次 decode，frontend CPU 成为瓶颈。Fix 缓存单 token decode 结果。
  - 为什么重要(对我): **token 优化 + eval**——开启 logprobs 的 agent eval 测 latency 时，detokenizer 开销可能 dominate；benchmark 须标注 logprobs 开关与 cache 是否启用。
  - 明日动作(<=20min): 写 **是/否**：你 agent serving eval 是否开 logprobs（是/否/未知）。

- [Automating repetitive work at OpenAI with Codex（OpenAI Blog, **2026-08-25**）](https://developers.openai.com/blog/automating-repetitive-work-at-openai-with-codex)
  - 摘要: OpenAI 工程师描述用 Codex + Runme notebook 跑 model eval：notebook 写 goal cell → `codex exec` 读 goal、写 plan、等人工 approve → JSONL trace 记录命令与 dead ends；Runme 通过 WebMCP 暴露 browser-side tools；`*.index.md`  companion 供后续 agent 发现历史 run。核心：**eval 是可复用 notebook artifact，不是一次性 chat。**
  - 为什么重要(对我): **skill 落地 + 评测**——与 OpenAI eval-skills（0826 已述）互补：Runme 解决「eval run 的持久化 + 发现 + approve 边界」；可映射到你的 map_prompt_test collect→test→eval 固定容器。
  - 明日动作(<=20min): 写 **1 条** 你现有 eval 是否有「plan approve 门禁」（≤15 词）。

- [fix(checkpoint): widen Store put/aput value type to Mapping[str, Any]（LangGraph PR #8617, updated **2026-08-27**）](https://github.com/langchain-ai/langgraph/pull/8617)
  - 摘要: Fixes #8616。`Store.put()`/`aput()` 要求 `dict[str, Any]`，TypedDict 不满足结构子类型 → 类型检查误报、部分 agent state 写入受阻。Fix 放宽为 `Mapping[str, Any]`。
  - 为什么重要(对我): **skill 落地**——agent state 常用 TypedDict；store eval 须覆盖 TypedDict value 写入与 round-trip。
  - 明日动作(<=20min): 写 **是/否**：你是否用 LangGraph Store + TypedDict state（是/否/未知）。

- [[Bugfix][KVConnector] Don't crash scheduler on stale KV transfer completions（vLLM PR #53746, updated **2026-08-27**）](https://github.com/vllm-project/vllm/pull/53746)
  - 摘要: Fixes #53049。`MultiConnector` 对 async saves 去重但对 async loads 做 bare union → P/D disagg 下 decode engine 双 connector（NIXL pull + LMCache MP）时，较快 load 路径完成后 request 被移除，较慢路径的 stale completion 仍触发 scheduler crash。
  - 为什么重要(对我): **agent serving eval**——多 connector KV reuse eval 须测「一 connector 先完成 → 另一 connector stale callback」路径。
  - 明日动作(<=20min): 写 **是/否**：你是否用 vLLM MultiConnector + P/D disagg（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [BF1: A Causal Dyadic Sparse-Attention Retrofit for Efficient Long-Context Transformers（arXiv:2608.20427, submitted **2026-08-19**）](https://arxiv.org/abs/2608.20427)
  - 摘要: 确定性 block-aligned dyadic sparse route：小 exact local + global first block + 对数间隔 historical blocks；每层 O(n log n) selected interactions、O(log n) 通信深度。Blackwell RTX PRO 6000 上 BF16 实现 32K 单层 prefill 10.91× speedup；Qwen3-0.6B 选 8/28 层 retrofit 后 TTFT 在 8K/16K/32K 分别降 7.7%/11.3%/15.3%，matched adaptation 下 perplexity 优于 dense continued training。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**——sparse retrofit 是「选层 + 保 correctness gate」的工程路径；agent serving eval 可对照「全 dense vs 部分 sparse layer」的 latency/quality Pareto。
  - 明日动作(<=20min): 写 **≤15 词**：你 long-context agent 是否考虑 partial sparse retrofit（是/否/未知）。

- [Sparse Token Routing in Efficient Transformers（arXiv:2608.20632, submitted **2026-08-21**）](https://arxiv.org/abs/2608.20632)
  - 摘要: SEWN 双流 Transformer：learned gate 将 token 路由到 lightweight vs full-capacity stream。routing 对 accuracy 影响极小，但 gate 的 token-importance 信号高度依赖学习方式——static lexicon prior 在 BoolQ 上 fail counterfactual faithfulness test；contextual gate 在两任务上 p<10⁻¹⁰ 分离且不改变 accuracy。SEWN-sparse hard top-k 在 PubMedQA 上 5.2–8.7× throughput vs BERT-base。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**——「routing gate 是否 faithful」是 agent eval 可测维度；与 token budget / adaptive compute eval 直接挂钩。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否测 routing/importance faithfulness（是/否/未知）。

- [[Bugfix][Scheduler] Don't pad spec decode up to `max_model_len`（vLLM PR #53962, updated **2026-08-27**）](https://github.com/vllm-project/vllm/pull/53962)
  - 摘要: waiting loop 对 single-token decode 的 uniform spec-decode padding 可使 `num_computed_tokens` 恰好到 `max_model_len`；running loop 的 cap 会减去本 step 采样 token 数 → 变负；async scheduling 下 request 被再次调度 → `num_scheduled_tokens` 负值 → `np.repeat` crash。
  - 为什么重要(对我): **Talk ≥3 turns + spec decode/Attention**——边界长度 agent eval 须覆盖「恰好 max_model_len − 1 token」的 spec decode 路径。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 vLLM spec decode + max_model_len 边界测试（是/否/未知）。

- [[Spec Decode] Validate ragged GDN decode with Qwen3.5 DSpark（vLLM PR #53929, updated **2026-08-27**）](https://github.com/vllm-project/vllm/pull/53929)
  - 摘要: 为 ragged GDN speculative-decode batch（per-request query length 在 GPU 分配）增加分层测试：device `query_start_loc` 优先于 CPU placeholder；packed varlen causal-conv 输出与 cache state 对比 independent per-request 执行；保留 `Qwen3DSparkModel` 架构用于 E2E 小 checkpoint 测试。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/KV**——ragged batch 是 agent serving 常态；eval 须标注 query length layout（device vs CPU）与 fused kernel 路径。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serve ragged/varlen agent batch（是/否/未知）。

## 3) 热门项目/可试个人项目

- [M2LabOrg/agent-skills-eval-playbook（GitHub, updated **2026-08-26**）](https://github.com/M2LabOrg/agent-skills-eval-playbook)
  - 摘要: stdlib-only Python eval harness（无第三方 eval 框架）：教写 lean/discoverable Agent Skills + 量化 benchmark。`.evals/run_evals.py` 测 trigger precision/recall、paired with-skill/baseline uplift（raw + normalized gain）、pass^k reliability、`--critique` 模式审 SKILL.md；`.evals/sandbox.sh` 用 git worktree 隔离 agent run。示例 skill：wind-turbine Flask 代码审查。
  - 为什么重要(对我): **skill 落地 + 评测**——trigger precision/recall + paired uplift 可直接迁入 agent-eval-playbook；stdlib-only 降低复用门槛。
  - 明日动作(<=20min): clone 后读 `docs/04`，写 **2 条** 可迁入你 playbook 的 metric 定义（各 ≤15 词）。
  - 估计复现成本: **低**（clone + `python .evals/run_evals.py`，需 Claude Code CLI，~45min）。

- [mgechev/skillgrade v0.3.0（GitHub, release **2026-08-26**）](https://github.com/mgechev/skillgrade)
  - 摘要: OpenAI eval-skills 博文引用的社区 skill eval CLI（Minko Gechev）。读 SKILL.md → 生成 `eval.yaml`（AI-assisted task/grader 定义）→ 执行完整 eval suite；v0.3.0（Aug 26）新增 suite 进度显示、agent 可选 model（#34/#35）。
  - 为什么重要(对我): **skill 评测**——与 `codex exec --json` 官方 pipeline 互补的「一键 skill → eval.yaml → grade」路径；可 A/B 你的 skill vs skillgrade 默认 grader。
  - 明日动作(<=20min): 对 1 个现有 skill 跑 `skillgrade`（或读 README 写 1 条 grader 映射），记录 with vs without 差分。
  - 估计复现成本: **中**（npm install + 1 skill + API key，~60min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Automating repetitive work at OpenAI with Codex（OpenAI Blog, **2026-08-25**）](https://developers.openai.com/blog/automating-repetitive-work-at-openai-with-codex)
  - 机会信号: OpenAI 内部把「重复 operational eval」产品化为 **Runme notebook + Codex exec + WebMCP + Google Drive 共享**——一人公司/出海 remote 可卖「eval runbook 模板 + approve 门禁 + index 可发现」而非单次 prompt 咨询；与 signature「Agent 评测方法论」对齐。
  - 风险信号: Runme/WebMCP 生态仍早期；notebook artifact 不等于可 CI 回归的 deterministic eval；Google Drive 依赖可能不适合企业客户。
  - 我可执行的一步(<=30min): 写 **3 条** 你当前 eval 流程缺 Runme 式「plan approve + dead-end 记录」的哪几块（各 ≤15 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[3720. Lexicographically Smallest Permutation Greater Than Target](https://leetcode.com/problems/lexicographically-smallest-permutation-greater-than-target/)（**Day 57, Aug 2026 Daily Challenge**, verified via LeetCode GraphQL **2026-08-27**；承接 0826 的 2904 Medium「beautiful 子串窗口」，今日 **Medium**「找字典序大于 target 的最小排列 = 后缀扫描 + 前缀交换/重排」）：关键洞察——从右找第一个可增大位，右侧取最小合法后缀；与 agent eval「tie-break 下最小可行解」同构。明日动作：手写解法；写 **≤15 词**：「最小大于 target」对你 eval rubric 次优解处理意味着什么。

- **IELTS（≤1 条）**：[IELTS Writing Practice Tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/writing)：0826 已轮换 Listening，今日换 **Writing**（Task 1 图表或 Task 2 议论文选 1 题，限时手写/打字，~40min）。明日动作：完成 1 篇 Writing，自评 **≥2 个** 结构/衔接失误；写 **≤15 词** 你最弱的 Writing 环节。

## 6) 噪音过滤记录

- **fix : Preserve provider specific token（LangChain #39946）**：与 0826 已收录 #39931 同 fix（#39926）→ semantic duplicate，不重复。
- **fix(core): support + bullet in MarkdownListOutputParser（#39950–#39958）**：边缘 output parser，多 PR 重复 → 过滤。
- **docs(langchain): document early framework evolution（#39955）**：文档，非 eval focus → 过滤。
- **chore(model-profiles): refresh model profile data（#39954）**：数据刷新 → 过滤。
- **fix(core): fall back plain-text read nltk stopwords（LlamaIndex #22850）**：边缘 ingestion 路径，非当前 focus → 过滤。
- **fix(bedrock): forward Cohere embedding options（LlamaIndex #22848）**：embedding 集成，非 focus → 过滤。
- **fix(grpc): support signal shutdown on Windows（AutoGen #8082）**：平台兼容，非 eval focus → 过滤。
- **vLLM #54051/#51237 XingChen4 model**：新模型支持 WIP，非 attention/eval focus → 过滤。
- **vLLM #54009 ROCM Eagle3 MiniMax**：AMD 专项 infra → 过滤。
- **Anthropic Skills API GA（Aug 19）**：高价值但非今日更新；release notes 已公开 → 不重复（待纳入 skill 落地专题时再收）。
- **Simon Willison Blog**：**2026-08-27** 无新帖 → 不收录。
- **openai/evals**：无 **2026-08-27** repo 更新 → 不收录。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-08-26~27** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + OpenAI Blog ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **797**（`0826.md` §7: 787 + 本日 §1: 10）
- Theory: **359**（`0826.md` §7: 355 + 本日 §2: 4）
- Career/Business: **127**（`0826.md` §7: 126 + 本日 §4: 1）
- LC: **109**（`0826.md` §7: 108 + 本日 §5: 1）
- IELTS: **109**（`0826.md` §7: 108 + 本日 §5: 1）
