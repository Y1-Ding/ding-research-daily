# Daily Research - 2026-09-26 (21:30)

## 0) 今日3条结论

- **LangGraph SQLite checkpoint 的 metadata 过滤对 Unicode 与写入格式不一致**（#**9083**, created **2026-09-26**）→ **skill 持久化/回放**：`José`/`café` 在 `InMemorySaver` 能搜到、SQLite 为 0 条；带非 ASCII 标签的 **thread 检索与 eval 采样** 会系统性漏样。
- **Llama3 JSON tool parser 流式路径会把「以 `{` 开头但不是 tool call」的整段回复静默丢弃**（vLLM #**58829** / #**58826**, created **2026-09-26**）→ **streaming agent eval**：非流式当 content、流式 `content=None`，L2 **空回复假阴性**；须单列 JSON-answer gold。
- **前缀缓存准入 + FlashMLA decode plan 复用**（#**58837** / #**58818**）→ **Talk ≥3 turns + token 成本**：KV 紧张时按 cached-prefix 长度重排等待队列；DSv4.1 每 decode 步 planner launch **21→4**，多轮 agent **TPOT 与 prefix 策略** 绑在一起测。

## 1) Agent工程化（high-signal）

- [fix(checkpoint-sqlite): match Unicode metadata filters（LangGraph PR #9083, created **2026-09-26**）](https://github.com/langchain-ai/langgraph/pull/9083)
  - 摘要: SQLite saver 写入 metadata 时保留 Unicode，过滤条件却转成 ASCII escape，嵌套 object/list 过滤 **零匹配**；同步/异步回归覆盖 `José`、`café`（fixes #9074）。
  - 为什么重要(对我): **checkpoint 检索 = eval 分层抽样**——非 ASCII 用户/工具元数据会让离线集 **代表性偏差**。
  - 明日动作(<=20min): 写 **是/否**：是否用 SQLite checkpoint + metadata 过滤搜 thread（是/否/未知）。

- [fix(anthropic): use explicit UTF-8 encoding in FilesystemClaudeTextEditorMiddleware file I/O（LangChain PR #40847, created **2026-09-26**）](https://github.com/langchain-ai/langchain/pull/40847)
  - 摘要: `read_text`/`write_text` 曾跟系统 locale；Windows cp936 / `LC_ALL=C` 下 UTF-8 文件 **UnicodeDecodeError** 或写出非 UTF-8（fixes #40675）。
  - 为什么重要(对我): **Claude Code 类 skill 的文件工具**——评测环境 locale 不同会制造 **不可复现** 的 tool 失败。
  - 明日动作(<=20min): 写 **是/否**：是否跑过非 UTF-8 locale 的 agent CI（是/否/未知）。

- [fix(core): max_iterations=N permits N iterations, not N-1（LlamaIndex PR #23259, created **2026-09-26**）](https://github.com/run-llama/llama_index/pull/23259)
  - 摘要: `num_iterations >= max_iterations` 在递增 **之后** 比较，导致 `max_iterations=1` 首次成功回答仍抛 `WorkflowRuntimeError`；与文档「N 次迭代」语义不一致。
  - 为什么重要(对我): **agent 步数门禁**——eval 里写的 `max_iterations` 与运行时 **差 1** 会系统性误判超时。
  - 明日动作(<=20min): 写 **1 个整数**：你当前 workflow 的 `max_iterations` 默认值。

- [fix(postprocessor): preserve retrieved scores during prev/next expansion（LlamaIndex PR #23260, created **2026-09-26**）](https://github.com/run-llama/llama_index/pull/23260)
  - 摘要: `PrevNextNodePostprocessor` 扩邻接窗口时用无分副本覆盖已检索节点，重叠窗口下 **score 消失**（含合法的 0.0）；仅新增邻居、不改排序（fixes #23199）。
  - 为什么重要(对我): **RAG agent 的 rerank/threshold eval**——分数被抹掉会让 **cutoff 与 L2 cite** 失真。
  - 明日动作(<=20min): 写 **是/否**：eval 是否依赖 prev/next 扩窗后的 score（是/否/未知）。

- [fix(core): fail fast in SimpleObjectNodeMapping.persist for non-allowlisted objects（LlamaIndex PR #23257, created **2026-09-26**）](https://github.com/run-llama/llama_index/pull/23257)
  - 摘要: `persist()` 可 pickle 任意对象，但 `from_persist_dir()` 用受限 unpickler，自定义类 **写完即不可读**；现 persist 前按 allowlist **立即失败**（fixes #23256）。
  - 为什么重要(对我): **ObjectIndex / skill 资产序列化**——静默坏文件比启动时 fail-fast 更难 debug。
  - 明日动作(<=20min): 写 **是/否**：是否 `ObjectIndex.from_objects` 自定义类（是/否/未知）。

- [fix(core): exclude only the matched directory in SimpleDirectoryReader（LlamaIndex PR #23254, created **2026-09-26**）](https://github.com/run-llama/llama_index/pull/23254)
  - 摘要: `exclude=["drafts"]` 用字符串 `startswith` 误伤 `drafts_final/`、`drafts2/` 等兄弟目录文件，ingest **静默缺文档**。
  - 为什么重要(对我): **RAG gold 覆盖度**——路径前缀 bug 会让 eval **以为模型漏召回**。
  - 明日动作(<=20min): 写 **是/否**：`SimpleDirectoryReader` 是否用目录名 exclude（是/否/未知）。

- [fix(agentchat): preserve TextMentionTermination sources on serialization（AutoGen PR #8288, created **2026-09-26**）](https://github.com/microsoft/autogen/pull/8288)
  - 摘要: `TextMentionTermination.sources` 经 `dump_component`/`load_component` 丢失，复合终止条件嵌套时 **source 过滤失效**（fixes #8282）。
  - 为什么重要(对我): **multi-agent 停止条件 = skill 合约**——序列化回归会让「提到 X 就停」在部署后变 **永远不停/误停**。
  - 明日动作(<=20min): 写 **是/否**：是否序列化 AutoGen termination（是/否/未知）。

- [Bugfix] Llama3 JSON tool parser: flush buffered '{...}' as content when it isn't a tool call（vLLM PR #58829, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58829)
  - 摘要: 流式仅看首字符 `{` 且无 `"name"` 时整段缓冲 **永不输出**；非流式当 content。对象解析完成且无 tool 字段时 **flush 为 content**（fixes #58824）。
  - 为什么重要(对我): **tool parser = L1 门禁**——JSON 答案形态须在 streaming eval 单列用例。
  - 明日动作(<=20min): 写 **是/否**：是否 Llama3 JSON parser + streaming（是/否/未知）。

- [Bugfix][GPT-OSS] Parse restarted Harmony headers instead of leaking them into tool names（vLLM PR #58831, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58831)
  - 摘要: 杂散 `<|channel|>commentary to=assistant` 后真实 header 被拼进 recipient，产生 `assistant<|channel|>analysis` **假 tool 名**；改为 reasoning 项（fixes #58825，对齐 llama.cpp #21286）。
  - 为什么重要(对我): **Harmony / Responses API agent**——header 解析错误直接污染 **tool 轨迹 eval**。
  - 明日动作(<=20min): 写 **是/否**：是否 gpt-oss + Harmony 路径（是/否/未知）。

- [Bugfix][Frontend] keep anthropic tool-result images in tool messages（vLLM PR #58823, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58823)
  - 摘要: Anthropic 转换器把 `tool_result` 内图片拆成插入的 `user` 消息，并行 tool 时序列变 `tool→user(image)→tool`，**连续性破坏**（#58647 item 6）。
  - 为什么重要(对我): **vision-tool agent eval**——多 tool 并行时 renderer 拒收或丢后续 result。
  - 明日动作(<=20min): 写 **是/否**：是否 tool 结果含 image（是/否/未知）。

- [Bugfix] DeepSeek V4.1 Flash failed to stream back to the client（vLLM PR #58822, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58822)
  - 摘要: 统一 `deepseek_v41` parser 流式路径 **静默丢弃 tool 参数 delta**；回归覆盖增量 argument streaming（fixes #58640）。
  - 为什么重要(对我): **流式 tool eval**——参数分片丢失会让 **partial JSON** 门禁全挂。
  - 明日动作(<=20min): 写 **是/否**：是否 DeepSeek V4.1 Flash serving（是/否/未知）。

- [Bugfix][Spec Decode] Avoid dynamic query lengths for draft decode graphs（vLLM PR #58821, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58821)
  - 摘要: 动态 spec decode 对 draft manager（`decode_query_len=1`）误用 target 的 query 长度展开，初始化 **ZeroDivisionError**（fixes #58692）。
  - 为什么重要(对我): **spec + multi-turn**——draft/target 图长度契约不同，eval 栈启服即失败。
  - 明日动作(<=20min): 写 **是/否**：是否 dynamic spec decode（是/否/未知）。

- [Bugfix][Frontend] Preserve caller parameters in offline pooling（vLLM PR #58842, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58842)
  - 摘要: `LLM.encode()` 把内部 `PoolingParams.task` 写进调用方对象，复用同一 params 跑 `embed` 再跑 `token_embed` **冲突抛错**；现 clone 后再赋值。
  - 为什么重要(对我): **RAG/embed agent 批跑**——共享 params 是常见脚本模式，静默 mutation 难查。
  - 明日动作(<=20min): 写 **是/否**：是否复用 `PoolingParams` 实例（是/否/未知）。

- [Security] Harden message sanitization（vLLM PR #58832, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58832)
  - 摘要: 消息清洗加固，过滤 **大写内存地址** 等模式，降低通过对话内容注入的解析/日志风险。
  - 为什么重要(对我): **对外 agent 边界**——sanitization 变更应进 **安全回归集**（与功能 eval 分开）。
  - 明日动作(<=20min): 写 **≤12 词**：你是否记录 vLLM sanitization 版本（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Perf][Scheduler] Add risk-aware prefix-cache admission（vLLM PR #58837, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58837)
  - 摘要: KV 紧张时，按等待窗口内 **cached-prefix 长度** 排序再准入（`RiskPerfScheduler` + `cache_aware_admission_window`）；默认 0 关闭，与 #48485 的 eviction 策略互补。
  - 为什么重要(对我): **prefix cache = 多轮 Attention 复用**——准入策略改 **谁吃到 cache hit**，agent SLA 非仅命中率数字。
  - 明日动作(<=20min): 写 **是/否**：是否 `enable_prefix_caching` 生产（是/否/未知）。

- [vLLM IR][RoPE] Port base RotaryEmbedding to a vLLM IR op (CUDA + ROCm/AITER)（vLLM PR #58838, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58838)
  - 摘要: 基础 `RotaryEmbedding` 迁入 vLLM IR，CUDA 与 ROCm/AITER 共用同一 op 路径，利于 **编译融合与跨后端对齐**。
  - 为什么重要(对我): **RoPE = 位置编码核心**——IR 化是后续 **长上下文与 kernel 融合** 的前置。
  - 明日动作(<=20min): 写 **≤10 词**：你关注的 RoPE 变体（如 YaRN/MLA）（一词）。

- [Bugfix][Metrics][MFU] Bill linear-attention layers of hybrid models separately（vLLM PR #58836, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58836)
  - 摘要: Qwen3-Next/3.5 的 Gated DeltaNet 层曾被计为 full attention，MFU/MBU **超物理上限**；现按 `layer_types` 分开计费并补线性层（fixes #58597）。
  - 为什么重要(对我): **hybrid 模型 profiler**——错误计费会误导 **Attention vs 线性层** 优化优先级。
  - 明日动作(<=20min): 写 **是/否**：是否用 vLLM MFU 看 hybrid（是/否/未知）。

- [Perf][DSv4.1] Share the FlashMLA decode plan across KV cache groups（vLLM PR #58818, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58818)
  - 摘要: 同一 metadata 构建 pass 内多 KV group 的 decode `topk_length` 一致，可共享 FlashMLA plan holder；每步 planner launch **21→4**。
  - 为什么重要(对我): **稀疏 MLA decode**——plan 构建是 **逐步延迟** 的隐藏项，多 group 场景放大。
  - 明日动作(<=20min): 写 **是/否**：是否 DSv4.1 + FlashMLA（是/否/未知）。

- [Qwen4Exp] Checkpoint-mapped PLE: read a decode step's cold pages with readahead（vLLM PR #58835, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58835)
  - 摘要: `checkpoint_mapped` 下 PLE 表几乎不在 page cache；decode 行集用 `MADV_WILLNEED` + `MADV_POPULATE_READ` **并行预读**，避免 GPU fault 串行 ~150µs/页（栈在 #58439）。
  - 为什么重要(对我): **权重映射 + 逐步 decode**——Attention 算力未变，**内存 IO** 可成 agent TPOT 瓶颈。
  - 明日动作(<=20min): 写 **是/否**：是否 checkpoint-mapped 权重（是/否/未知）。

- [Bugfix][Core] Fix negative indexing of KV cache block hash views（vLLM PR #58840, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58840)
  - 摘要: `BlockHashListWithBlockSize` 尾部不完整 block 时，`view[-1]` 映射到错误底层 hash；负索引应按 **逻辑 view 长度** 规范化（影响 prefix/Mooncake 等）。
  - 为什么重要(对我): **prefix cache 哈希链**——边界索引错会改 **cache hit 判定** 而非仅测试细节。
  - 明日动作(<=20min): 写 **是/否**：是否依赖 block hash view 负索引（是/否/未知）。

- [Bugfix][Spec Decode] Fix GLM-5.3 image-token mapping during MTP initialization（vLLM PR #58833, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58833)
  - 摘要: `Glm5NextConfig` 用 `image_token_id` 而非 `image_token_index`，MTP draft 初始化 **AttributeError**；补 GLM 类名映射。
  - 为什么重要(对我): **多模态 + spec decode**——draft/target token 空间对齐是 **Attention/KV 正确性** 前置。
  - 明日动作(<=20min): 写 **是/否**：是否 GLM-5.3 MTP（是/否/未知）。

- [Test][ROCm] Add AITER MLA backend contract tests（vLLM PR #58843, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58843)
  - 摘要: 为 ROCm AITER MLA 后端增加 **契约测试**，锚定 metadata/plan 与 CUDA 路径的预期行为。
  - 为什么重要(对我): **跨后端 Attention**——同一 eval 在 ROCm/CUDA **数值/延迟** 需单测护栏。
  - 明日动作(<=20min): 写 **是/否**：是否 ROCm 部署（是/否/未知）。

## 3) 热门项目/可试个人项目

- [Model] Add Laya decision model (convaiinnovations/laya)（vLLM PR #58812, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58812)
  - 摘要: **421M** ModernBERT + 2-layer decision head，**非自回归**：给定 state + 题型一次前向输出 **校准概率答案**，无文本生成/解析链路。
  - 为什么重要(对我): **routing/slot-filling agent** 可对照「小决策模型 vs LLM tool」的 **latency/幻觉** tradeoff。
  - 明日动作(<=20min): 读 README 记 **输入字段 schema**（3 行）。
  - 估计复现成本: **中**（vLLM pooling 路径 + 权重，~2–4h）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Claude Opus 5.5, GPT-6 Sol, GPT-6 Luna, and a new price war（Simon Willison Blog, **2026-09-22**）](https://simonwillison.net/2026/sep/22/opus-and-sol-and-luna/)
  - 机会信号: **cached input** 价阶拉开（如 GPT-6 Luna cached **$0.01/M** vs input **$0.10/M**）→ 出海 agent 报价可卖 **「长对话 + prefix/KV 复用」** 而非裸 token；Datasette Agent 已切 Luna 验证 **成本/速度**。
  - 风险信号: 模型名与价目 **周级变动**；客户合同若写死型号易 **毛利被价格战吃掉**。
  - 我可执行的一步(<=30min): 写 **3 行** 脱敏报价模板：基础价 / cached 价 / agent 步数上限（每行 ≤12 词）。

- [Perf][Scheduler] Add risk-aware prefix-cache admission（vLLM PR #58837, created **2026-09-26**）](https://github.com/vllm-project/vllm/pull/58837)
  - 机会信号: 可开关的 **cache-aware 排队** 是交付文档里的 **「多租户 agent 公平性」** 话术素材（长会话优先吃 prefix）。
  - 风险信号: 默认关闭；错误 window 可能 **饿死短请求**；需与产品 SLO 对齐再写进方案。
  - 我可执行的一步(<=30min): 写 **≤5 词** 你愿承诺的 SLO 指标名（如 p95 TPOT / cache hit%）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[1801. Evaluate the Bracket Pairs of a String](https://leetcode.com/problems/evaluate-the-bracket-pairs-of-a-string/)（**2026-09-26 Daily Challenge**，Medium，LeetCode GraphQL `activeDailyCodingChallengeQuestion`）：括号占位替换 + 哈希查表；与 **模板/槽位填充** 同构。明日动作：一题或写 **≤15 词**「Medium 日题」对你周计划意味着什么。

- **IELTS（≤1 条）**：[IELTS Writing practice tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/writing)：0925 为 Reading，今日 **Writing Task 2 一篇（~25min 计时）**。明日动作：写 **2 个** 失误类型（任务回应/衔接）；**≤15 词** 最弱 Writing 维度。

## 6) 噪音过滤记录

- **LangChain #40848/#40849**：typo 修复 → 过滤。
- **LlamaIndex #23258/#23255**：`aextract_table_summaries` cast / hidden 路径 → niche，未进主报告。
- **vLLM #58826**：与 #58829 同 issue #58824 的窄 fix → **语义去重**，主报告保留 #58829。
- **vLLM #58844/#58811/#58810/#58817**：CI/docs → 过滤。
- **vLLM #58828/#58827/#58820/#58819/#58815/#58814/#58813**：MoE/模型加载 niche → 未进 §2（可按栈跟进）。
- **vLLM #58834**：GLM sequence-parallel layout → 未单独收录（与 GLM 族同日，密度控制）。
- **vLLM #58830/#58839**：安全 gate / plugin route → 过滤或 niche。
- **OpenAI Blog / Anthropic / DeepMind / Lil'Log**：未发现 **2026-09-26** 可验证新文 → 不收录。
- **Simon Willison**：**2026-09-26** 无新帖；§4 引用 **2026-09-22** 价战文（职业池 freshness 降权仍 ≥65）。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-26** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill → 未调用；白名单 GitHub 当日 **≥8** 条主信号，无需 skill 补充。
- **openai/evals**：**2026-09-26** 无新 PR → 不收录。

## 7) 本周累计计数

- Agent: **1134**（`0925.md` §7: 1120 + 本日 §1: 14）
- Theory: **524**（`0925.md` §7: 517 + 本日 §2: 8）
- Career/Business: **166**（`0925.md` §7: 164 + 本日 §4: 2）
- LC: **139**（`0925.md` §7: 138 + 本日 §5: 1）
- IELTS: **139**（`0925.md` §7: 138 + 本日 §5: 1）
