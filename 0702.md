# Daily Research - 2026-07-02 (21:30)

## 0) 今日3条结论

- **LangGraph 取消流式 run 会丢失未 checkpoint 的中间状态（#5672，updated 2026-07-02）**：用户在 cancel 前看到的数秒流式输出不会持久化；重 sync 后 UI 回滚到上一个 completed checkpoint → skill 长会话 eval 的「用户可见轨迹」与 backend 权威 state 分叉，L2 trajectory match 出现假阴性。
- **AutoGen `ToolCallResult` 将结构化 tool 输出静默 `str()` 化（#7867，updated 2026-07-02）**：dict/Pydantic 返回值变成 Python repr 字符串流入 message history → 多 agent 链路中 downstream agent 无法直接消费结构化数据，L2 structured eval 被迫回退 regex parse。
- **HydraHead 按 head 功能异质性混合 FA/LA（arXiv 2606.20097）**：interpretability 驱动的一次性 head 选择——检索关键 head 保留 Full Attention、其余换 Linear Attention + head-wise scale-normalized fusion → 为长 trace serving 的 token 优化提供「按 head 分工」架构假说，与 Agentic DFS 两 head 专化形成呼应。

## 1) Agent工程化（high-signal）

- [Run Cancellation Causes Loss of Streamed State Not Yet Persisted as a Checkpoint（LangGraph #5672 updated **2026-07-02**）](https://github.com/langchain-ai/langgraph/issues/5672)
  - 摘要: LangGraph Platform/API 上，streaming run 被 cancel 时，自上一个 persisted checkpoint 以来的 in-progress streamed state（含数秒 token 输出）不会写入 backend。用户发送新消息后 front-end sync 权威 state，UI 回滚到 cancel 前最后 completed checkpoint，与用户实际看到的内容不一致。期望行为：cancel 时至少持久化 partial/aborted state。
  - 为什么重要(对我): **skill 落地 + eval**——长会话 agent 常见 cancel-and-resume；若 streamed state 不持久化，collect→test→eval 的 trajectory 与 prod 用户感知分叉，且 silent data loss 难被 L3 门禁捕获。
  - 明日动作(<=20min): 写 **是/否/未知**：你 agent 是否支持 cancel 后 resume（是/否/未知）。

- [Feature: Cryptographic action receipts (AAR) for provable agent execution（LangGraph #7065 updated **2026-07-02**）](https://github.com/langchain-ai/langgraph/issues/7065)
  - 摘要: 提议为 LangGraph 每个 node 执行生成 Ed25519 签名的 Agent Action Receipt (AAR)：记录 agent/action、input/output 的 SHA-256 hash、timestamp，离线可验证、不含 PII。Receipts 可通过 Session Continuity Certificates (SCC) 链接跨 session provenance。面向金融/医疗/法律等需 tamper-proof audit 的合规场景。
  - 为什么重要(对我): **skill 落地 + eval**——与 #7303 trust-gated checkpoint、healthcare_ai_governance eval 同构；cryptographic receipt 是 L3 人工审查 + 自动化 governance eval 的可验证 primitive。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent trace 是否含不可篡改签名（有/无/仅 log）。

- [fix: keep connection alive in AsyncPostgresSaver.from_conn_string（LangGraph #8268 merged **2026-07-02**）](https://github.com/langchain-ai/langgraph/issues/8268)
  - 摘要: `AsyncPostgresSaver.from_conn_string` 原用 async context manager，退出时关闭 DB 连接，后续调用报 `SSL connection has been closed unexpectedly`。Fix：改为 regular async method 返回 saver 并保持连接打开；用户需在创建后 `await saver.setup()`。Closes #5675。
  - 为什么重要(对我): **skill 落地**——Postgres checkpoint 是生产 LangGraph 长会话标配；连接生命周期 bug 导致 intermittent checkpoint 失败，与 #8234 durability 问题叠加时 state 可靠性更难保证。
  - 明日动作(<=20min): 写 **是/否/未知**：你是否用 Postgres checkpoint saver（是/否/未知）。

- [Support dynamic tool addition/removal after agent creation and in middleware（LangChain #33808 updated **2026-07-02**）](https://github.com/langchain-ai/langchain/issues/33808)
  - 摘要: Feature request：`create_agent()` 后无法在 middleware 中动态增删 tool，middleware 只能 filter 预注册 tool。痛点场景：(1) MCP server 按用户认证动态暴露 tool；(2) Agent UI 按运行时条件增删 tool。当前无 first-class runtime tool discovery 机制。
  - 为什么重要(对我): **skill 落地**——skill 常以动态 tool 形态注入（MCP、plugin）；静态注册迫使所有 skill 预加载，token 开销大且 per-skill eval 难隔离激活条件。
  - 明日动作(<=20min): 写 **≤15 词**：你 tool 是静态注册还是运行时发现（静态/动态/混合）。

- [fix(core): honor `description` and docstring for `@tool(infer_schema=False)`（LangChain #38611 merged **2026-07-02**）](https://github.com/langchain-ai/langchain/pull/38611)
  - 摘要: `@tool(infer_schema=False)` 无 `args_schema` 时，用户提供的 `description` 被硬编码为 `"<name> tool"` 静默丢弃；docstring 虽 required 也未用作 description。Fix：优先 explicit `description`，否则 fallback 到 stripped docstring；两者皆无仍抛 `ValueError`。Closes #38138。
  - 为什么重要(对我): **skill 落地**——skill tool 常 `infer_schema=False` 做轻量 string tool；description 丢失使 model tool selection 退化，per-skill eval 出现「选对 tool 名但 schema 误导」假阳性。
  - 明日动作(<=20min): 写 **是/否**：你 `@tool` 是否用 `infer_schema=False`（是/否/未知）。

- [Proposal: GuardrailProvider protocol for tool call interception（AutoGen #7405 updated **2026-07-02**）](https://github.com/microsoft/autogen/issues/7405)
  - 摘要: 提议 `GuardrailProvider` protocol 在 tool 执行前拦截：policy-based approval、audit logging、argument sanitization。插入 `BaseTool.run_json()` 和 `Workbench.call_tool()` 路径，向后兼容。统一 #6017 guardrails epic、#5891 approval func、#5921 AIAM 十个 enterprise gap 中的拦截层需求。
  - 为什么重要(对我): **skill 落地 + eval**——与 LangGraph #7303 trust-gated node、F/C 规则引擎同构；标准化 guardrail hook 是 per-skill eval 隔离与 composable skill stack 的基础设施。
  - 明日动作(<=20min): 写 **≤15 词**：你 tool call 是否有 pre-execution guardrail（有/无/仅 prompt）。

- [bug: ToolCallResult auto-coerces structured tool outputs to str(), losing type information（AutoGen #7867 updated **2026-07-02**）](https://github.com/microsoft/autogen/issues/7867)
  - 摘要: tool 返回 dict/list/Pydantic 时，`ToolCallResult` 静默 `str()` 化为 Python repr 字符串（如 `"{'status': 'success', ...}"`）流入 message history。Downstream agent 和 message processor 无法直接消费结构化数据，必须 regex/parse 恢复。
  - 为什么重要(对我): **eval**——multi-agent skill 链路中 structured tool output 是 L2 exact-match grading 基础；str coercion 使 eval 依赖脆弱 parse，且与 #7405 guardrail 的 argument inspection 目标冲突。
  - 明日动作(<=20min): 写 **是/否**：你 multi-agent 是否传递 structured tool output（是/否/未知）。

- [Add SelfCorrectionEval: self-correction benchmark for reasoning models（openai/evals PR #1686 opened **2026-07-01**）](https://github.com/openai/evals/pull/1686)
  - 摘要: 新增 `self_correction` eval suite：3 种 critique 策略（standard、assumptions、disprove），12 项 metrics 含 `correction_efficiency`；40 samples 跨 5 类（reasoning traps、multi-step arithmetic、logic、coding、factual traps）。文件：`evals/elsuite/self_correction.py`、`registry/evals/self_correction.yaml`、`registry/data/self_correction.jsonl`。
  - 为什么重要(对我): **eval**——self-correction 是 agent ≥3 turn 对话的核心能力；可直接挂 map_prompt_test collect→test→eval 做 reasoning model 纠错效率 baseline。
  - 明日动作(<=20min): clone PR branch；跑 1 sample 看 `correction_efficiency` metric 定义（≤20min）。

## 2) 原理深化（Attention/Transformer/Agent）

- [HydraHead: From Head-Level Functional Heterogeneity to Specialized Attention Hybridization（arXiv 2606.20097，submitted **2026-06-18**）](https://arxiv.org/abs/2606.20097)
  - 摘要: 层内 attention head 呈 block-wise functional heterogeneity。HydraHead 用 interpretability 一次性选出检索关键 head 保留 Full Attention (FA)，其余换 Linear Attention (LA)；head 维度混合 FA/LA 并用 head-wise scale-normalized fusion 缓解机制差异。三阶段迁移：参数继承+layer-wise alignment → global distillation → long-context fine-tuning。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) head-level FA/LA 分工 vs layer-wise hybrid 哪个更适合 agent 长 trace？(2) 一次性 calibration head 选择能否用于 serving token 预算分配？(3) 与 2606.00183 两 head DFS 专化是否同一设计原则？
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 是否 layer-wise 还是 head-wise 优化 attention（layer/head/未知）。

- [Explaining Attention with Program Synthesis（arXiv 2606.19317，submitted **2026-06-17**）](https://arxiv.org/abs/2606.19317)
  - 摘要: 对每个 attention head 提取 training examples 的 attention maps，prompt LM 生成 candidate Python programs 复现 maps，re-rank 得 best-fit program。BERT-Base/GPT-2/TinyLlama-1.1B/Llama-3B 上大量 head 可被高精度 program 近似；因果验证：替换 head 为 program，~25% head 可替换且 perplexity 仅增 16%，下游 QA 影响小。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) program-replaced head 能否用于 agent trace 的 attention 可解释性？(2) 25% 可替换 head 对 token 优化/pruning 的边界？(3) synthesized program 与真实 head 功能 diverge 时 eval 如何检测？
  - 明日动作(<=20min): 写 **≤10 词**：你 eval 是否检查 attention pattern（是/否/未知）。

- [Transformers Provably Implement In-Context Reinforcement Learning with Policy Improvement（arXiv 2605.05755）](https://arxiv.org/abs/2605.05755)
  - 摘要: 证明单头 linear self-attention block 可通过显式参数构造实现 batch SARSA semi-gradient 和 actor-critic update；输出更新后的 critic/actor 参数以闭合 in-context policy-improvement loop。Teacher-mimicking 训练 + gradient-flow 分析：在 training MDP 分布足够 rich 时，局部指数收敛到最优参数流形——ICRL 文献首个 convergence guarantee。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) in-context RL update 与 ReAct multi-turn agent 的对应？(2) convergence 条件对 agent eval curriculum 设计有何启示？(3) 与 2606.00183 DFS head 专化是否共享 RL 训练动力学？
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 是否利用 in-context learning 做策略改进（是/否/未知）。

## 3) 热门项目/可试个人项目

- [microsoft/agent-governance-toolkit — OpenTelemetry export across SDKs（commit **2026-07-01**）](https://github.com/microsoft/agent-governance-toolkit/commit/b3c899675e8f76a263f3f1f22a7a29137d84fe03)
  - 摘要: `feat(sdks): host-side telemetry and OpenTelemetry export across Python, Rust, Node, and .NET (#3190)`。与 LangGraph #7303 `langgraph-trust` 适配器、#7065 AAR receipts 形成 governance observability 栈：policy enforcement + trust gate + distributed trace。
  - 为什么重要(对我): **eval 可复用杠杆**——OTel export 可将 per-node governance decision 纳入 collect→test→eval trace，比纯 log 更适合 L2 automated grading。
  - 明日动作(<=20min): 读 commit diff 看 Python SDK OTel span 命名约定；写 **≤15 词** 你 eval trace 是否接 OTel（是/否/仅 log）。
  - 估计复现成本: **中**（需搭 OTel collector + agent-governance-toolkit Python SDK；无 GPU）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Feature: Agent trust verification via MoltBridge for cross-org multi-agent scenarios（AutoGen #7525 updated **2026-07-02**）](https://github.com/microsoft/autogen/issues/7525)
  - 机会信号: **跨组织 agent 信任基础设施**——MoltBridge 提供 Ed25519 身份、attestation-based 信任图、skill-scoped trust（code-review 信任 ≠ financial 信任）、可传递 graph traversal。已列 A2A registry，live API 28 endpoints。与 #7524 OPA authorization 互补：allowed vs trusted。
  - 风险信号: 第三方 trust graph 引入 vendor lock-in；attestation 数据质量依赖参与方诚实；跨境合规（数据 residency）未明确。
  - 我可执行的一步(<=30min): 读 [MoltBridge API docs](https://github.com/SageMindAI/moltbridge)；写 **≤30 词** 你 agent 跨团队委托时如何验证 trust（有/无机制）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 120 Triangle](https://leetcode.com/problems/triangle/)：从顶到底最小路径和；底向上 O(n) 空间单行 DP：`f[j] = min(f[j], f[j+1]) + triangle[i][j]`。明日动作：手写 bottom-up O(n) 空间版（≤20min）；写 **≤15 词**：三角 DP 与 agent 树搜索 backtrack 有何类比。

- **IELTS（≤1 条）**：[IELTS Speaking — Describe a skill you learned recently（British Council 样题）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-practice-tests/speaking)：Part 2 用「最近学 agent eval 方法论」脱敏练 2 分钟（what/when/how/why useful）。明日动作：录音 2 分钟自评 fluency（≤20min）；写 **≤15 词** 弱项（fluency/词汇/结构 选 1）。

## 6) 噪音过滤记录

- **LangGraph #7417（长 tool 静默重放）**：`0701.md` 已收录；**2026-07-02** 无新 comment → 不重复。
- **LangGraph #8234（sync durability）**：`0701.md` 已收录；**2026-07-02** 无 update → 不重复。
- **LangGraph #7303（trust-gated checkpoints）**：`0701.md` 已收录；**2026-07-02** 仅 metadata touch → 不重复。
- **LangGraph #4973（v1 roadmap）**：`0701.md` 已收录；**2026-07-02** 无实质新 comment → 不重复。
- **LlamaIndex #22213（FactExtractionMemoryBlock condense）**：`0701.md` 已收录 → 不重复。
- **LangChain #38484/#37972/#38561**：`0701.md` 已收录 → 不重复。
- **SelfCompact（arXiv 2606.23525）/ Agentic DFS（2606.00183）/ SWARR（2606.11634）**：`0701.md` 已收录 → 不重复。
- **LlamaIndex #22232（PandasQueryEngine RCE）**：安全漏洞但与当前 focus（skill/eval/token）弱相关 → 丢弃。
- **LangChain #38610（ChatOpenRouter port exhaustion）**：特定 integration edge case → 丢弃。
- **vLLM #46652（ITL token delta）**：benchmark infra fix，与 agent skill 落地间接 → 丢弃。
- **LlamaIndex #21213（Header-Aware Chunking）**：proposal 阶段无 merge，信号不足 → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-02** 原帖 → 不收录。
- **@gefei55 / @ianneo_ai 职业内容**：第三方聚合无法验证原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **442**（`0701.md` §7: 434 + 本日 §1: 8）
- Theory: **182**（`0701.md` §7: 179 + 本日 §2: 3）
- Career/Business: **82**（`0701.md` §7: 81 + 本日 §4: 1）
- LC: **64**（`0701.md` §7: 63 + 本日 §5: 1）
- IELTS: **64**（`0701.md` §7: 63 + 本日 §5: 1）
