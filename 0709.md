# Daily Research - 2026-07-09 (21:30)

## 0) 今日3条结论

- **LangGraph #8312（updated 2026-07-09）为 root messages inbox 加 bound**：async/sync stream 的 root-scope message buffer 现用 `max_queue_size` 封顶，溢出抛 `RuntimeError` 而非无限增长 → 长时 streaming eval 的内存/token 隐性税可被显式断言；与 0708 #8299 checkpoint 完整性同属「state 边界」门禁族。
- **PACE（arXiv 2607.02032, July 2026）用 <1% 成本的 proxy subset 预测 agentic benchmark 分数**：14 模型 × 4 agentic × 19 non-agentic，LOOCV MAE <4%、Spearman >0.80、pairwise ranking ~85% → collect→test 可在 full agent eval 前做 triage gate，但不可替代 tool-failure / long-horizon 门禁。
- **Fingerprint（arXiv 2607.06621, July 2026）证明 positional scheme 设定 attention head 的默认 spectral algebra**：RoPE 最强 previous-token head 呈 rotational spectrum，learned-absolute/ALiBi 呈 non-rotational；谱特征是 function 后的 fingerprint 而非 precursor → multi-turn eval 测 head ablation 时需分层报告 positional scheme。

## 1) Agent工程化（high-signal）

- [fix: root messages inbox bounds（LangGraph PR #8312 updated **2026-07-09**）](https://github.com/langchain-ai/langgraph/pull/8312)
  - 摘要: Corridor security finding。async root messages inbox 原无界增长；fix 用现有 stream `max_queue_size` 约束 buffer，溢出时抛清晰 `RuntimeError`；sync root inbox 同步行为。含 async/sync targeted regression test。
  - 为什么重要(对我): **skill 落地 + token**——长时 agent streaming（tool-call 事件流、eval trace 采集）若 inbox 无界，parallel eval 会 OOM 或拖慢；bound 后可写门禁「overflow = fail fast」。
  - 明日动作(<=20min): 写 **是/否**：你 streaming eval 是否设 max_queue / 断言 overflow（是/否/未知）。

- [fix(agentchat): SelectorGroupChat fallback must not return excluded previous speaker（AutoGen PR #7936 updated **2026-07-09**）](https://github.com/microsoft/autogen/pull/7936)
  - 摘要: Fixes #7471。`allow_repeated_speaker=False` 时 model 选 speaker 失败后的 fallback 仍返回 `_previous_speaker`，但该 speaker 已从 candidates 排除 → 两 agent 团队 **livelock**（同一 agent 反复被选中、无进展）。Fix：fallback 仅在 `allow_repeated_speaker=True` 时启用 previous speaker。
  - 为什么重要(对我): **skill 落地 + eval**——multi-agent skill 的 L2 eval 若测 speaker rotation，fallback path 会产生假阴性/死锁；与 0708 AutoGen #7637 request_id 碰撞同属 distributed orchestration 边界。
  - 明日动作(<=20min): 写 **是/否**：你 multi-agent eval 是否覆盖 selector fallback path（是/否/仅 happy path）。

- [fix: persist AG-UI frontend tool messages（LlamaIndex PR #22288 updated **2026-07-09**）](https://github.com/run-llama/llama_index/pull/22288)
  - 摘要: Fixes #22066。AG-UI aggregation 时 frontend tool-call 结果未持久化为 tool-role message → chat history 丢 frontend tool 轨迹。Fix：backend tool messages 在前、frontend tool messages 在后，保留 frontend stop boundary；mixed backend/frontend tool_call_id 配对 regression test。
  - 为什么重要(对我): **skill 落地**——frontend tool（browser/UI）与 backend tool 混用时，eval 若只读 backend history 会漏步；与 0708 memory-before-RAG 同属「typed slot 完整性」。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 是否混用 frontend + backend tools（有/无/仅 backend）。

- [PACE: A Proxy for Agentic Capability Evaluation（arXiv **2607.02032**, submitted **2026-07-02**）](https://arxiv.org/abs/2607.02032)
  - 摘要: 从 19 个 non-agentic benchmark 自动选取 compact subset，用 noise-aware regression 映射到 4 个 agentic target（GAIA、SWE-Bench Verified/Multimodal、SWT-Bench）。14 模型实验：LOOCV MAE <4%、Spearman >0.80、pairwise ranking ~85%，成本 <1% full agent eval。开源 [neulab/pace](https://github.com/neulab/pace)。
  - 为什么重要(对我): **eval 杠杆**——map_prompt_test 可在 full collect 前用 proxy gate 筛 model/routing/tool-policy；但论文明确不覆盖 reliability、tool failure、long-horizon 行为 → 仅 triage 非替代。
  - 明日动作(<=20min): 读 abstract §1；写 **≤15 词** 你 eval 是否有 cheap pre-screen gate（有/无）。

- [Improving Agents is a Data Mining Problem（LangChain Blog, **2026-07-07**）](https://www.langchain.com/blog/improving-agents-is-a-data-mining-problem)
  - 摘要: @swyx AIEWF talk 沉淀：Continual Learning = Observability；trace 是 agent experience 的投影。改进循环 = mine traces → curate evals → hill-climb（harness engineering / SFT / memory）。开源 trace judge 在窄任务上比 frontier 便宜且更准；Terminal Bench 2.0 harness 调优 +13.7%。
  - 为什么重要(对我): **eval + skill 落地**——与 PACE proxy 互补：PACE 做 model triage，trace mining 做 failure pattern → eval case 沉淀；可直接映射 collect→test→eval 飞轮。
  - 明日动作(<=20min): 写 **≤15 词**：你最近 1 次 eval 失败是否来自 trace 而非 E2E score（是/否/未读 trace）。

- [Tuning the harness, not the model: a Nemotron 3 Ultra playbook（LangChain Blog, **2026-07-08**）](https://www.langchain.com/blog/tuning-the-harness-not-the-model-a-nemotron-3-ultra-playbook)
  - 摘要: 固定 Nemotron 3 Ultra 权重，仅调 harness（prompt blocks、middleware context injection、tool description 位置）。Deep Agents suite 0.80→0.86（best），near Opus 4.8 0.87，成本约 1/10。关键发现：同一规则放 system prompt 无效、放 tool return 有效；conversation 维度 flat → harness ceiling，需 post-training。
  - 为什么重要(对我): **skill 落地 + eval**——skill 指令注入位置（prompt vs tool result vs in-band message）是可测变量；eval 应分层报告 harness-tunable vs model-ceiling 维度。
  - 明日动作(<=20min): 写 **1 条**：你 skill 规则最该出现在哪（system/tool-return/in-band message 选 1）。

- [feat(spec_decode): Grammar-aware draft token sampling for structured outputs（vLLM PR #47885 updated **2026-07-09**）](https://github.com/vllm-project/vllm/pull/47885)
  - 摘要: speculative decoding + structured output 时 draft model 常提议 grammar-invalid token → 浪费 speculative round。Fix：`ShadowGrammarManager` 维护 per-request `xgrammar.GrammarMatcher`；argmax invalid 时 fallback top-K=32；与 #14702 post-hoc validation 互补，proposer 端前置过滤。
  - 为什么重要(对我): **token 优化**——structured output skill（JSON schema、tool args）在 spec decode 路径的 token 税可被量化；eval 应分层报告 spec-decode on/off × structured output。
  - 明日动作(<=20min): 写 **是/否**：你 serving 是否开 spec decode + structured output（是/否/仅其一）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Fingerprint, Not Blueprint: How Positional Schemes Set the Default Spectral Algebra of Attention（arXiv **2607.06621**, July 2026）](https://arxiv.org/abs/2607.06621)
  - 摘要: 将 attention pre-softmax score 建模为双线性形式 $M=W_q^\top W_k$ 的复谱分析。7 模型 3 positional scheme：RoPE 最强 previous-token head 呈 rotational spectrum（top-quintile $D_{\text{head}}$），learned-absolute/ALiBi 呈 non-rotational；Pythia checkpoint 追踪显示 head 从 Ginibre null 出生、谱签名随 circuit formation 锁定（fingerprint 非 precursor）；消融 $\mathrm{Im}(M_t)$ 可破坏 induction head。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) RoPE rotational head 是否在 ≥3 turn goal pivot 时仍 fire？(2) 与 0708 Legible selective detector 是互补（谱 vs 可读通道）还是同机制？(3) eval 是否按 positional scheme 分层 ablation？
  - 明日动作(<=20min): 写 **≤15 词**：你 multi-turn eval 是否记录 positional encoding 类型（有/无/仅 E2E）。

- [[P/D][Bugfix] Fix PD async KV load lookahead handling for MTP spec decode（vLLM PR #46694 merged **2026-07-09**）](https://github.com/vllm-project/vllm/pull/46694)
  - 摘要: P/D disaggregation + MTP speculative decoding 路径下，async KV load 时 lookahead slot 分配未禁用 → local/remote block 不匹配、speculative slot 污染。Fix：MTP 等 draft-model spec decode 在 remote KV receive 期间 defer lookahead allocation（此前仅 EAGLE 禁用）。
  - 为什么重要(对我): **Talk ≥3 turns + token**：(1) 污染 slot 是否扭曲 multi-turn tool-call KV 轨迹？(2) 与 0708 #47728 settled-basis block free 是否同族 KV integrity？(3) eval 是否分层报告 P/D + spec decode 组合？
  - 明日动作(<=20min): 写 **是/否**：你 serving 是否用 P/D disaggregation（是/否/仅 single-node）。

- [Harness-Bench: Measuring Harness Effects across Models in Realistic Agent Workflows（arXiv **2605.27922**）](https://arxiv.org/html/2605.27922)
  - 摘要: 106 sandboxed offline tasks × 多 harness 配置 × 多 model backend，5194 trajectories。completion/process quality/efficiency/failure 在 model-harness pairing 间差异显著；recurring execution-alignment failure（reasoning 与 tool feedback/workspace state 脱钩）。主张 agent capability 应在 model-harness configuration 层面报告。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) 同 model 换 harness 分数 swing 是否 > 你 eval 的 noise floor？(2) execution-alignment failure 能否用 trajectory eval 检出？(3) 与 0709 harness tuning playbook 的「middleware 注入位置」是否同构？
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否固定 harness 再比 model（是/否/混比）。

## 3) 热门项目/可试个人项目

- [neulab/pace — Proxy Agentic Capability Evaluation](https://github.com/neulab/pace)
  - 摘要: PACE 论文配套开源。Part 1 `scripts/pacebench/` 用已有 `results/standardized_results/` 跑 selection + regression（无需 API key）；Part 2 `evaluations/` 可重跑 19 non-agentic benchmark 生成分数。支持 GAIA/SWE-Bench/SWT-Bench 四 target 的 proxy 预测。
  - 为什么重要(对我): **eval 杠杆**——可直接在 map_prompt_test 加 cheap pre-screen gate；比从零建 agent eval 基础设施成本低。
  - 明日动作(<=20min): clone repo；读 README Part 1；写 **≤15 词** 哪个 target 最像你的 agent 任务。
  - 估计复现成本: **低**（Part 1 仅 CPU；Part 2 需 API key）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Tuning the harness, not the model: a Nemotron 3 Ultra playbook（LangChain Blog **2026-07-08**）](https://www.langchain.com/blog/tuning-the-harness-not-the-model-a-nemotron-3-ultra-playbook)
  - 机会信号: **高杠杆可复制资产**——harness profile（prompt blocks + middleware 注入策略）是可迁移的 engineering asset，不绑定单一 model；open model + tuned harness 达 near-frontier 质量、成本 1/10 → 一人公司可用 eval 驱动的 harness 迭代建立 signature，而非追 model arms race。
  - 风险信号: harness tuning 有 ceiling（conversation/long-horizon 维度 flat）；过度 benchmark-conforming 的 rule 会被丢弃（文中明确淘汰 single-eval phrase trick）。
  - 我可执行的一步(<=30min): 写 **3 条** harness 改进候选（各 ≤10 词），标「core improvement」vs「model-specific profile」。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 3532 Path Existence Queries in a Graph I](https://leetcode.com/problems/path-existence-queries-in-a-graph-i/)：sorted array 上相邻差 > maxDiff 则断连，O(n) 分组后 O(1) 回答连通查询。明日动作：手写 grouping 版（≤20min）；写 **≤15 词**：与 agent state 组件分组的类比。

- **IELTS（≤1 条）**：[IELTS Writing Task 2 Band 7 Essay Guide 2026（English AIdol）](https://www.englishaidol.com/en/blog/ielts-writing-task-2-band-7-essay-guide-2026)：四段式（intro + 2 body + conclusion）、270–300 词、40min；Band 7 关键在 specificity（具体例子）而非泛泛而谈。明日动作：选 1 个 opinion 题写 intro + 1 body（untimed, ≤25min）；写 **≤15 词** 最弱 criterion（TR/CC/LR/GRA 选 1）。

## 6) 噪音过滤记录

- **LangGraph #8299/#8301/#8302 族**：`0708.md` 已收录 #8299/#8301；本日 #8299/#8301 **closed without merge**（issue #8302 仍 open）→ 不重复展开，但 state 完整性 fix 仍未落地需观望。
- **LangGraph #8313（cancel v3 streams）**：closed without merge，#8302 仍 open → 不单列，待 merge 后再收。
- **LlamaIndex #22274–#22277 memory 批**：`0708.md` 已收录 → 不重复。
- **Legible-by-Construction（2607.04319）/ AgenticSTS（2607.02255）/ vLLM #47728**：`0708.md` 已收录 → 不重复。
- **Keyless Attention（2606.21848）**：`0630.md`/`0701.md` 已收录 → 不重复。
- **Harbor x LangChain unified eval stack**：`0630.md` 前后时段无收录但为 6 月内容 → 本日优先收更新项（PACE、harness playbook）；Harbor 不单列避免 stale。
- **LangChain #38738 CRLF markdown / #38736 PIIMatch export / #38735 fireworks nested dict**：infra/edge，弱于今日 focus → 丢弃或一句带过。
- **LangGraph #8305 pub-sub mode / #8309 path traversal / #8310 secrets**：`8305` closed、`8309`/`8310` security，非 agent 语义主线 → 丢弃。
- **vLLM #48100 Olmo migration / #48096 remove Persimmon**：model backend cleanup，弱信号 → 丢弃。
- **AutoGen #7934/#7935 TokenLab docs**：docs only → 丢弃。
- **openai/evals #1691 community health**：非 eval 方法论 → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-09** 原帖 URL → 不收录。
- **@gefei55 / @ianneo_ai 职业帖**：无法验证当日原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv + LangChain Blog 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **490**（`0708.md` §7: 483 + 本日 §1: 7）
- Theory: **200**（`0708.md` §7: 197 + 本日 §2: 3）
- Career/Business: **88**（`0708.md` §7: 87 + 本日 §4: 1）
- LC: **70**（`0708.md` §7: 69 + 本日 §5: 1）
- IELTS: **70**（`0708.md` §7: 69 + 本日 §5: 1）
