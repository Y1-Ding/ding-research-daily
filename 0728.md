# Daily Research - 2026-07-28 (21:30)

## 0) 今日3条结论

- **LangGraph 新增 durable tool idempotency middleware**（#8465 / #8464, **2026-07-28**）：长运行 graph 在 worker 重启或超时重试时会重复执行 tool 并产生副作用。`DurableClaimStore`（memory/SQLite）在 tool 执行前 pre-claim 稳定 request key，重试时返回缓存结果 → agent eval 的 retry 语义必须从「再跑一次」升级为「exactly-once side effect + at-least-once delivery」。
- **Pydantic 未完成 `args_schema` 会静默暴露零参数 tool**（LangChain #39099 / PR #39102, **2026-07-28**）：forward ref 未 resolve 时模型看到空 schema 但 `invoke()` 仍校验完整字段 → tool-calling eval 的 pass rate 在「模型未传参」与「invoke 校验失败」之间分裂；`model_rebuild()` 前置是 skill/tool 封装的最低门禁。
- **FP8 MLA + Marlin kernel 在 chunk-prefill 路径 dtype 不匹配**（vLLM #49989, **2026-07-28**）：Ampere 等硬件用 Marlin 做 weight-only FP8 量化时，MLA 的 `kv_c_normed` 仍以 fp8 输入 Marlin kernel → chunk-prefill crash；与 nvFP4 已有 bypass 逻辑对齐后 Mistral-Small-4 FP8 长 prompt serving 可跑通 → MLA serving 栈的 dtype 守卫是 Attention 热路径隐形依赖。

## 1) Agent工程化（high-signal）

- [feat(pregel): add durable tool call idempotency middleware（LangGraph PR #8465, updated **2026-07-28**）](https://github.com/langchain-ai/langgraph/pull/8465)
  - 摘要: Fixes #8464。新增 `DurableClaimStore` 接口（In-Memory + SQLite 实现）与 idempotency keying：tool 执行前在 durable storage pre-claim 稳定 request key，重试时返回缓存结果而非重复 side effect。含 key 确定性、atomic claim、retry 行为单测。
  - 为什么重要(对我): **skill 落地 + 评测**——长运行 agent（checkpoint + retry）的 tool 副作用（写库、发 API、改文件）在 eval 重跑与 prod 故障恢复时会被 duplicate；无 idempotency 层 = flaky eval + 不可复现事故。
  - 明日动作(<=20min): 写 **是/否**：你 agent eval 的 tool retry 是否假设「重试 = 安全可重复执行」（是/否/未知）。

- [fix(checkpoint): restore tuple members when deserializing set/frozenset（LangGraph PR #8467, updated **2026-07-28**）](https://github.com/langchain-ai/langgraph/pull/8467)
  - 摘要: Fixes #8388。`JsonPlusSerializer` 反序列化 `set`/`frozenset` 时，msgpack 将 tuple 成员解码为 list（不可 hash）→ 重建 set 抛 `TypeError` 被 ext hook 吞掉 → `{(1,2),(3,4)}` 静默变为 `None`。Fix：重建 set 前将 list 成员还原为 tuple。含 plain/nested/mixed tuple 回归测试。
  - 为什么重要(对我): **skill 落地 + 评测**——graph state 中 set-of-tuples（如 visited edges、tool call 签名集）round-trip 失败 = checkpoint 恢复后 state 静默 corruption，multi-turn eval replay 与 prod trace 不可对齐。
  - 明日动作(<=20min): 写 **是/否**：你 graph state 是否含 `set`/`frozenset` 且成员为 tuple（是/否/未知）。

- [fix(core): rebuild incomplete args_schema instead of silently exposing no tool arguments（LangChain PR #39102, updated **2026-07-28**）](https://github.com/langchain-ai/langchain/pull/39102)
  - 摘要: Fixes #39099。`args_schema` 为 pydantic v2 模型且 `__pydantic_complete__ == False`（forward ref 未 resolve）时，tool 向模型暴露零参数，但 `invoke()` 仍按完整 schema 校验。Fix：`get_all_basemodel_annotations()` 在 introspection 前调 `model_rebuild()`； genuinely unresolvable ref 抛 contextual `NameError`。2167 core 单测通过。
  - 为什么重要(对我): **skill 落地 + 评测**——@tool 的 Pydantic 子类若 forward ref 定义在后，模型永远看不到必填参数 → tool-calling eval 的「未调用」与「调用失败」统计分裂。
  - 明日动作(<=20min): 写 **是/否**：你 skill tools 是否有 Pydantic `args_schema` 引用后定义类型（是/否/未知）。

- [fix(anthropic): strip id from system text blocks before sending（LangChain PR #39103, updated **2026-07-28**）](https://github.com/langchain-ai/langchain/pull/39103)
  - 摘要: Fixes #39100。`create_text_block()` 为 system block mint `id`（`lc_<uuid4>`），`_format_messages` 原样转发 → Anthropic API 报 `system.0.id: Extra inputs are not permitted`。Fix：对 system text block 用与 regular content 相同的 allowlist（`type`/`text`/`cache_control`/`citations`），保留 `cache_control` 与 `citations`、丢弃 `id`。148 单测通过。
  - 为什么重要(对我): **token 优化 + skill 落地**——system prompt 的 `cache_control` 是长 context agent 降本核心；`id` 字段导致整请求失败 = prompt cache 策略在 content_blocks 路径 silent broken。
  - 明日动作(<=20min): 写 **是/否**：你 system prompt 是否用 `create_text_block()` + `cache_control`（是/否/未知）。

- [fix(postgres): bind metadata filter keys instead of interpolating into SQL（LlamaIndex PR #22478, updated **2026-07-28**）](https://github.com/run-llama/llama_index/pull/22478)
  - 摘要: Fixes #22475。`PGVectorStore._build_filter_clause` 将 `filter_.key` 直接拼入 SQL 字符串 → key 含单引号可篡改查询。当 key 来自 untrusted 源（如 agent tool 让模型选 metadata 字段）时构成 SQL injection。Fix：七个 operator 分支均将 key 绑为 SQL 参数（`metadata_->>:mkey_*`），与 #22471 value-binding 对齐。15 个 DB-free 回归测试覆盖全部 operator。
  - 为什么重要(对我): **skill 落地 + RAG 评测**——agent RAG tool 若暴露 metadata filter 字段选择，未绑定 key = eval 与 prod 均可被 prompt injection 操纵检索结果。
  - 明日动作(<=20min): 写 **是/否**：你 RAG agent tool 是否允许模型指定 metadata filter key（是/否/未知）。

- [feat(ingestion): add async stream rate-limit and backoff interceptor（LlamaIndex PR #22477, updated **2026-07-28**）](https://github.com/run-llama/llama_index/pull/22477)
  - 摘要: 为 vector ingestion 新增 opt-in `AsyncRateLimitInterceptor`：捕获 HTTP 429 与 transient 网络断开，指数退避 + jitter 后重试，wired 进 `pipeline.py` async batch 路径。含 backoff delay、retry cap、成功 fallback 单测。
  - 为什么重要(对我): **评测数据 pipeline**——collect 阶段 429 若直接 drop batch，gold 集索引不完整 → downstream eval recall 系统性偏低且难复现。
  - 明日动作(<=20min): 写 **是/否**：你 ingestion pipeline 是否在 429 时 drop batch 而非 retry（是/否/未知）。

- [feat(enterprise): add structured token cost calculator middleware（AutoGen PR #8004, updated **2026-07-28**）](https://github.com/microsoft/autogen/pull/8004)
  - 摘要: 新增 opt-in middleware，为 model call 计算结构化 token 计数与估算成本；支持可插拔 pricing 与 reporting callback。含最小单测。
  - 为什么重要(对我): **token 优化 + 评测**——per-step cost attribution 是 agent eval 门禁；middleware 模式 = 可复用到你自己的 LangGraph/LC harness 而不绑死 AutoGen runtime。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval harness 的 token cost 是 middleware 还是 post-hoc 解析（middleware/post-hoc/未知）。

- [fix(core): memoize `convert_to_openai_tool` per tool instance（LangChain PR #39057, updated **2026-07-27**）](https://github.com/langchain-ai/langchain/pull/39057)
  - 摘要: `convert_to_openai_tool` 在 agent 生命周期内被 `bind_tools` 与 token-counting middleware 反复调用，每次 uncached 执行 `dereference_refs`/`_rm_titles`。本 PR 按 `BaseTool` 实例 memoize 转换结果；`name`/`description`/`args_schema`/`metadata` 变更时自动 invalidate。所有现有 caller 无 API 变更即受益。
  - 为什么重要(对我): **token 优化**——高频 tool schema 转换在 multi-tool agent 的每步 preflight 上是隐形 CPU 开销；memoize = eval 长跑吞吐微优化 + schema 一致性保障。
  - 明日动作(<=20min): 写 **是/否**：你 agent 每步是否重复 `bind_tools`/`convert_to_openai_tool`（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [bugfix: fp8 MLA with Marlin kernels（vLLM PR #49989, updated **2026-07-28**）](https://github.com/vllm-project/vllm/pull/49989)
  - 摘要: Ampere 等硬件用 Marlin kernel 做 weight-only FP8 量化时，MLA chunk-prefill 因 Marlin 不接受 fp8 input 而 crash。已有 nvFP4 bypass（`kv_b_proj_w_dtype == uint8` 时保持 input dtype），本 PR 扩展逻辑覆盖 Marlin case。验证：`VLLM_TEST_FORCE_FP8_MARLIN=1` 下 Mistral-Small-4-119B FP8 长 prompt serving + chat completion 成功。
  - 为什么重要(对我): **Talk ≥3 turns + Attention/MLA**：MLA 的 `kv_b_proj` 路径在 FP8+Marlin 组合下 dtype 守卫是 prefill 热路径隐形依赖；与 0727 #49982 Transformers backend MLA pad 形成「MLA serving 栈」检查清单。
  - 明日动作(<=20min): 写 **≤15 词**：你 MLA serving 是否测过 FP8+Marlin 长 prefill（是/否/未知）。

- [Support mm_processor_cache in the Transformers multimodal backend（vLLM PR #50107, updated **2026-07-28**）](https://github.com/vllm-project/vllm/pull/50107)
  - 摘要: Transformers multimodal backend 移除 `mm_token_type_ids` 与 cache-bypassing `apply()` override，使 `mm_processor_cache` 生效并支持 pre-embedded image embeddings。`_get_prompt_updates` 从 processor 的 `return_text_replacement_offsets` 定位 placeholder，无 offsets 时 fallback token-run。验证：LLaVA-OneVision/Gemma3 raw 无回归，pre-embedded embeds 从 fail→pass，tiled SmolVLM/Idefics3 从 crash→正常，audio（AudioFlamingo3/GLM-ASR/Granite/VibeVoice）生成通过。
  - 为什么重要(对我): **Talk ≥3 turns + Attention 前处理**：multimodal prefill 的 image patchify 在 CPU 侧重复执行 = GPU 空转；processor cache = agent eval 吞吐与 TTFT 的隐形杠杆（与 0726 #49608 renderer offload 互补）。
  - 明日动作(<=20min): 写 **≤15 词**：你 multimodal eval 是否启用 mm_processor_cache（是/否/未知）。

- [[PD][NixlPush] Skip extra `add_remote_agent` step in D->P handshake（vLLM PR #49345, updated **2026-07-28**）](https://github.com/vllm-project/vllm/pull/49345)
  - 摘要: Address S4 in #48633。D→P NixlPush handshake 中 D 执行「full handshake」会交换 KV topology/address 并注册 local/remote descs，但 D 仅通过 nixl notification 传 block_ids 给 P 写 KV，该步骤非必需。本 PR 跳过多余 `add_remote_agent` 步骤，减少 D→P 握手延迟。
  - 为什么重要(对我): **Talk ≥3 turns + KV/Attention**：P/D disaggregation 的 handshake 开销直接影响 multi-turn agent 首 token 延迟；与 0726 #49877 fetch round 状态机、0727 #49947 OBJ cleanup 形成 KV offload 三连检查。
  - 明日动作(<=20min): 写 **≤15 词**：你 PD disaggregation 是否 profiling 过 handshake 耗时（是/否/未知）。

- [Demystifying evals for AI agents（Anthropic Engineering Blog, **2026-01-09**）](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents)
  - 摘要: Anthropic 工程团队 agent eval 方法论：区分 **transcript**（完整执行轨迹）vs **outcome**（环境终态，如 DB 是否真有预订）；三类 grader（code/model/human）组合；**pass@k**（k 次至少 1 次成功，适合 human-supervised）vs **pass^k**（k 次全部成功，适合高可靠自动化）；建议从 20–50 个真实 failure case 起步而非等完美 benchmark。
  - 为什么重要(对我): **Talk ≥3 turns + Agent 原理**：pass@k vs pass^k 的选择直接决定 eval 指标能否映射 prod 可靠性要求；transcript-only grading = agent eval 最常见的「看起来对了其实没做到」盲区。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent eval 主指标是 pass@k 还是 pass^k（pass@k/pass^k/未定义）。

- [fix(core): preserve $defs array/primitive types in schema_to_pydantic_model（AutoGen PR #7211, updated **2026-07-28**）](https://github.com/microsoft/autogen/pull/7211)
  - 摘要: Fixes #7203。`schema_to_pydantic_model` 在 `$defs` 含非 object 类型（array/primitive）时错误创建空 BaseModel → `{"type":"array","items":{"type":"string"}}` 被变成 `{"type":"object","properties":{}}`。Fix：`_type_cache` 分离存储非 object defs + `_schema_to_python_type()` 路由。含 array/primitive 回归测试。
  - 为什么重要(对我): **Talk ≥3 turns + Tool schema**：JSON Schema→Pydantic 转换是 tool-calling harness 的基础层；$defs 中 array 类型被静默 object 化 = 模型看到错误 tool schema，与 LangChain #39102 forward ref 问题形成「schema 完整性」双侧检查。
  - 明日动作(<=20min): 写 **≤15 词**：你 tool schema 是否用 JSON Schema `$defs` 引用 array 类型（是/否/未知）。

## 3) 热门项目/可试个人项目

- [run-llama/llama_index — Don't drop oversized leaf nodes in CodeSplitter（PR #22372, updated **2026-07-28**）](https://github.com/run-llama/llama_index/pull/22372)
  - 摘要: `CodeSplitter._chunk_node` 对 oversized leaf（长 string literal、comment、无 AST children 的 token）原返回 `[]` 静默丢弃文本。Fix：将 oversized leaf 作为独立 chunk 输出（原子 token 允许超限而非消失）。含 literal 存活断言测试。
  - 为什么重要(对我): RAG code indexing 的 silent drop = agent code search eval 的 recall 系统性偏低；低成本验证：对一个含超长 literal 的文件跑 CodeSplitter 看 chunk 是否保留。
  - 明日动作(<=20min): 读 PR diff 中 `_chunk_node` leaf 分支；写 **≤10 词** 你 code RAG 是否用 CodeSplitter。
  - 估计复现成本: **低**（读 diff + 本地 1 个 literal 测试，≤20min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [An opinionated guide to which AI to use to do stuff（Simon Willison Blog, **2026-07-27**）](https://simonwillison.net/2026/Jul/27/)
  - 机会信号: Ethan Mollick 指南从一年前「ChatGPT/Claude/Gemini chat + Deep Research」演进到 **agentic systems**（「AI 可连续做数小时人类工作」）；ChatGPT Work / Claude Cowork / Codex / Code 命名混乱但核心信号是 **给 AI 一台电脑**；移动端 ChatGPT「Work」模式 Code Interpreter 容器可访问互联网——harness 选择比模型选择更影响 agent 能力边界。对你：出海 remote 的 signature 应是 **eval harness + agent orchestration**，而非单模型 benchmark 分数。
  - 风险信号: 指南偏 consumer/product 视角，非工程 eval 手册；Gemini 被 Ethan 移出推荐（无成熟 Codex/Cowork 对标）不代表技术栈不可用；命名混乱增加团队沟通成本。
  - 我可执行的一步(<=30min): 写 **≤8 行**：你当前 agent 栈的 harness 层（LangGraph/LC/AutoGen/自研）与 Mollick 描述的「给 AI 电脑」模式是否对齐（对齐/部分/未对齐）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 3518 Smallest Palindromic Rearrangement II](https://leetcode.com/problems/smallest-palindromic-rearrangement-ii/)（**Day 28, Jul 2026, Hard**；承接 0727 的 3517 Easy 系列）：给定回文串 s 与 k，返回第 k 个字典序最小回文重排（不足 k 个则返回空串）。明日动作：左半贪心 + 组合数跳块模板写 1 次；写 **≤15 词**：「组合数跳块」与 agent eval 中 pass@k 跳过大块 failure space 的类比。

- **IELTS（≤1 条）**：[IELTS Ready — British Council 官方免费备考（含 AI Mini Mock）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-ready)：50 分钟 Mini Mock + AI 估分。明日动作：完成 **Reading 1 节 Mini Mock**；写 **≤15 词** 错因类型（词汇/定位/推理 选 1）。

## 6) 噪音过滤记录

- **LangChain #39104 `InMemoryRecordManager` timestamp**：与 0727 #39088 同 fix → 不重复。
- **LlamaIndex #22074 workflow_as_mcp factory、#22472 Elasticsearch OR filter**：0727 已收录 → 不重复。
- **vLLM #49982 MLA Transformers backend、#44527 sparse-MLA FillFunctor、#49947 OBJ cleanup、#49799 xLAM parser**：0727 已收录 → 不重复。
- **AutoGen #7856 None chunk guard**：streaming 防御性修复，信号弱于今日 token/idempotency 主线 → 过滤。
- **vLLM #50140 FlashInfer ReplaySSM**：WIP 无 test plan/结果 → 过滤。
- **LangChain #39105/#39092 model profiles refresh**：内部数据刷新 → 过滤。
- **LlamaIndex #22479 deps bump、#22406 ipex-llm 移除**：infra/低相关 → 过滤。
- **Simon Willison Jul 27 Kimi K3 权重发布**：0720 已收录 Kimi K3 评测视角 → 不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-28** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Jul 28 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **630**（`0727.md` §7: 622 + 本日 §1: 8）
- Theory: **265**（`0727.md` §7: 260 + 本日 §2: 5）
- Career/Business: **107**（`0727.md` §7: 106 + 本日 §4: 1）
- LC: **89**（`0727.md` §7: 88 + 本日 §5: 1）
- IELTS: **89**（`0727.md` §7: 88 + 本日 §5: 1）
