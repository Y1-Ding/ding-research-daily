# Daily Research - 2026-05-05 (21:30)

> 采集口径：优先 **X_WHITELIST / YOUTUBE_WHITELIST / GITHUB_WHITELIST / BLOG_WHITELIST / CAREER_BUSINESS_WHITELIST**；评分 `relevance(40)+actionability(25)+credibility(20)+freshness(15)`，正文主段 **≥70**（职业池 **≥65** 且具可执行信号）。本日 **未使用** `[OUTSIDE_WHITELIST]` 主链条目（**0%**，**≤20%**）。与 `0504.md` 已写条目做 **URL 去重**（如 `langchain-classic==1.0.5`、`langchain-anthropic==1.4.3`、`prebuilt==1.1.0a2` 等）与 **语义去重**（不重复展开同一叙事）。

## 0) 今日3条结论

- **LangGraph 1.2 alpha 继续把「可观测/可恢复」往 saver API 推**：`1.2.0a7` 明确包含 **`get_writes_history` 公共 API + delta cadence rework**（见 [release](https://github.com/langchain-ai/langgraph/releases/tag/1.2.0a7)），对你意味着：长线程 agent 的 **写入历史审计** 与 **checkpoint 写入节奏** 更可工程化调参。
- **实时语音 agent 的瓶颈往往在媒体面而非模型面**：OpenAI 工程文解释其 **WebRTC relay + transceiver** 架构如何用 **ICE ufrag** 做首包路由、把 **有状态协议终止** 收敛到 transceiver，并把 **全球 relay ingress + geo-steered signaling** 作为延迟与抖动主杠杆（见 [post](https://openai.com/index/delivering-low-latency-voice-ai-at-scale/)）。
- **「agent harness」正在产品化成可版本化配置**：LangChain 官方文把 **Deep Agents** 的 **per-model harness profiles**（提示词/工具名/中间件等）与 **tau2-bench 子集**上的量化提升绑定叙述（见 [blog](https://blog.langchain.dev/tuning-deep-agents-different-models/)），对你意味着：**同一业务 agent** 应把 **profile + eval suite** 当作一等公民资产，而不是散落在 prompt 文件里。

## 1) Agent工程化（high-signal）

- [langgraph==1.2.0a7](https://github.com/langchain-ai/langgraph/releases/tag/1.2.0a7)（`langchain-ai/langgraph`，published **2026-05-04**）
  - 摘要: `feat: public get_writes_history saver API + delta cadence rework (#7699)` 等（见 release body）。
  - 为什么重要(对我): 你要做 **多步工具图 / 可恢复执行** 时，**writes 历史** 与 **delta 写入节奏** 直接影响 **排障成本** 与 **checkpoint I/O**。
  - 明日动作(<=20min): 在你现有 checkpointer 上找 **1 条**「写入爆炸」路径（消息列表增长最快的状态键），对照 `get_writes_history`/delta 相关描述，记下 **该路径是否值得切到 DeltaChannel / 调 snapshot_frequency**。

- [Tuning Deep Agents to Work Well with Different Models](https://blog.langchain.dev/tuning-deep-agents-different-models/)（LangChain Blog，**2026-04-29**）
  - 摘要: 引入 **harness profiles**：按 OpenAI/Anthropic/Google 的 prompting 习惯调整 **system prompt、工具命名/实现、middleware**；文中给出 **tau2-bench 子集**上 **GPT 5.3 Codex 33%→53%**、**Claude Opus 4.7 43%→53%** 等对比表，并指向 [deepagents 仓库](https://github.com/langchain-ai/deepagents) 与 [Profiles docs](https://docs.langchain.com/oss/python/deepagents/profiles)。
  - 为什么重要(对我): 把「模型差异」从 **口头经验** 变成 **可注册/可版本化** 的 harness 层，直接服务 **训练/评测/上线一致性**。
  - 明日动作(<=20min): 给你最常用的 **2 个模型** 各列 **3 条**「必须不同的工具协议假设」（例如并行读文件、patch 工具名、反思节奏），映射到 profile 字段（`system_prompt_suffix` / `excluded_tools` / middleware）。

- [The runtime behind production deep agents](https://blog.langchain.dev/runtime-behind-production-deep-agents/)（LangChain Blog，**2026-04-20**）
  - 摘要: 面向 **生产级 deep agents** 的运行时叙事（规划/子代理/长任务可靠性等工程主题；具体细节以正文为准）。
  - 为什么重要(对我): 当你把 agent 从 demo 推到 **可运维** 时，这类「运行时分解」比堆模型更决定上限。
  - 明日动作(<=20min): 用一页纸把你的 agent **拆成 5 个运行时模块**（路由、记忆、工具沙箱、观测、恢复），标出 **最缺观测数据** 的 1 个模块。

- [vLLM v0.20.1](https://github.com/vllm-project/vllm/releases/tag/v0.20.1)（`vllm-project/vllm`，published **2026-05-04**）
  - 摘要: patch release：强调 **DeepSeek V4 稳定/性能** 与多项 bugfix（见 release notes，如 **non-streaming tool calls 类型转换** 等）。
  - 为什么重要(对我): 你在 **自托管推理 + tool calling** 上，patch 类修复往往对应 **线上偶发坏输出**，而不是显式 crash。
  - 明日动作(<=20min): 若你跑 DSV4/相关模型，记录当前 **streaming vs non-streaming** 工具调用路径；对照 release 里 tool-call fixes 判断是否要做 **一条回归用例**。

- [How OpenAI delivers low-latency voice AI at scale](https://openai.com/index/delivering-low-latency-voice-ai-at-scale/)（OpenAI Engineering，**2026-05-04**）
  - 摘要: 解释 **WebRTC** 在 ChatGPT voice / Realtime API 场景下的工程约束；提出 **relay（轻量转发）+ transceiver（有状态终止）** 拆分、用 **ICE ufrag** 做 **首包路由**、配合 **Global Relay** 与 **geo-steered signaling** 降低 RTT/jitter。
  - 为什么重要(对我): 语音/实时 agent 的 **turn-taking、barge-in** 体验，本质是 **媒体与控制面** 的系统问题；此文给出可对照的 **架构检查清单**。
  - 明日动作(<=20min): 画一张 **客户端→边缘→推理** 的 6 框图，标出你系统里 **谁拥有 ICE/DTLS/SRTP 状态**；对照文章判断你是否把状态错误地散到多个服务。

- [langchain==1.3.0a1（alpha）](https://github.com/langchain-ai/langchain/releases/tag/langchain%3D%3D1.3.0a1)（`langchain-ai/langchain`，published **2026-05-01**）
  - 摘要: 初始 alpha：`feat(langchain): wire stream_events(version='v3') into create_agent` 等（见 release body）。
  - 为什么重要(对我): **LangGraph v3 streaming** 与 **`create_agent`** 的接线如果跟上，你的 **UI/观测** 可以更少手写 event 拼装逻辑。
  - 明日动作(<=20min): 仅在 **隔离分支** 尝试把一条最小 `create_agent` smoke 切到 `stream_events(version="v3")`，记录 **typed projections** 与你现有 consumer 的字段差集（先不合并主分支）。

## 2) 原理深化（Attention/Transformer/Agent）

- [How people ask Claude for personal guidance](https://www.anthropic.com/research/claude-personal-guidance)（Anthropic Research，**2026-04-30**）
  - 摘要: 基于隐私保护流程对 **约 100 万** claude.ai 对话抽样，估计约 **6%** 属于「个人生活指导」；讨论 **sycophancy（过度迎合）** 在不同主题上的差异，并描述如何用 **分类器 + 合成数据 + stress-test（含 prefilling）** 推动 **Opus 4.7 / Mythos Preview** 改进。
  - 为什么重要(对我): 把 **对话型 agent 的偏好/安全失败模式** 还原成 **可度量指标 + 训练/评测闭环**，与你在业务里做的 **human-in-the-loop eval** 同构。
  - 明日动作(<=20min): 给你自己的「顾问型」agent 写 **5 条** sycophancy 探针用例（单方面的关系/职业叙述 + 追问施压），定义 **期望的拒绝/澄清** 行为。

- [The Anatomy of an Agent Harness](https://blog.langchain.dev/the-anatomy-of-an-agent-harness/)（LangChain Blog，**2026-03-10**）
  - 摘要: 给出可操作定义：**Agent = Model + Harness**；把 harness 拆成 **system prompt、工具/MCP、沙箱与执行环境、编排（子代理/路由）、middleware/hooks** 等，并解释 **filesystem / bash / sandbox / memory+search / context rot 治理 / 长程自主** 各自解决什么「模型原生做不到」的问题。
  - 为什么重要(对我): 把「agent 原理」从 **注意力层** 拉回 **控制论闭环**：你要优化的是 **状态、动作、观测、约束** 的工程分解，而不是只调 temperature。
  - 明日动作(<=20min): 给你当前一个生产 agent，逐条对照文中 harness 组件表，标 **1 个**「你以为在模型里、其实在 harness」的决策点，并写 **1 条**把它挪到 harness 的改法。

## 3) 热门项目/可试个人项目

- [deepagents（LangChain Deep Agents）](https://github.com/langchain-ai/deepagents)
  - 摘要: 官方 deep agent harness 实现入口；与 [模型 profiles 文章](https://blog.langchain.dev/tuning-deep-agents-different-models/)配套，适合作为 **个人项目/对照实验** 的基座。
  - 为什么重要(对我): 你可以用同一套任务集对比 **默认 harness vs 自定义 profile**，把「prompt 玄学」变成 **可回归配置**。
  - 明日动作(<=20min): fork 后只改 **1 个 profile 字段**（例如 `system_prompt_suffix`），跑 **3 题** tau2-style 手工用例看差异。
  - 估计复现成本: **中**（依赖模型与评测脚本准备）

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Building a new enterprise AI services company with Blackstone, Hellman & Friedman, and Goldman Sachs](https://www.anthropic.com/news/enterprise-ai-services-company)（Anthropic News，**2026-05-04**）
  - 机会信号: 明确描述 **Anthropic Applied AI** 与客户工程团队共建 **定制化 Claude 系统** 的交付形态，并把新公司纳入 **Claude Partner Network**（见正文）。
  - 风险信号: **重交付/重集成** 赛道会快速 **人才与流程同质化**；对 solo 而言，直接竞争大客户的 **SI 模式** 未必是最优切入点。
  - 我可执行的一步(<=30min): 用表格列出 **3 类**你最熟的垂直场景（workflow、合规、数据边界），每类写 **1 条**「**10 天内可交付**的 Claude 自动化切片」，对照文中 engagement 描述自检是否 **过宽**。

- [Accelerating the next phase of AI](https://openai.com/index/accelerating-the-next-phase-ai/)（OpenAI Company，页面用于理解产业资本节奏；与 Simon Willison 的引用链相关）
  - 机会信号: 产业侧对 **算力/资本/产品阶段** 的叙事会重塑 **B2B 采购与人才市场** 的预期（以页面为准）。
  - 风险信号: **估值与战略叙事** ≠ 你个人产品的 PMF；容易被宏观信息带偏节奏。
  - 我可执行的一步(<=30min): 只读 **与你相关的 1 个小节**，摘 **2 句**可转成「**我下周不做**」的决策边界（避免 FOMO 扩张范围）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**: 选 [LeetCode](https://leetcode.com/problemset/) **1 题**，用今日 Anthropic 文的口径写 **3 个探针**：(a) 边界输入 (b) 反例构造 (c) 「推翻自己第一次解法」的对照用例；提交前只跑这 3 个。

- **IELTS（≤1 条）**: 打开 [IELTS One Skill Retake](https://www.ielts.org/for-test-takers/ielts-one-skill-retake) 与考点页面，确认 **OSR 是否可用**（以官方条款为准），并把「**你下一次考试日期 → OSR 窗口**」写进日历备注（避免临期才发现不适用）。

## 6) 噪音过滤记录

- **`langgraph` `prebuilt==1.1.0a2` / `1.2.0a5` namespace 修复**：`0504.md` 已覆盖对应 release 叙事；本日改以 **`1.2.0a7` 新增量** 写入（**语义去重**）。
- **`langchain-classic==1.0.5` / `langchain-anthropic==1.4.3` / `langchain-openrouter==0.2.3`**：`0504.md` 已用 **同 URL**（**URL 去重**）。
- **`LlamaIndex v0.14.21`**：`0504.md` 已引用 **同一 release URL**（**URL 去重**）。
- **Simon Willison `2026-05-05` 的 YC/OpenAI 股权摘抄**：属于 **二级引用/市场八卦**，且对你主轨 **可执行增量低**（**drop**）。
- **YouTube 白名单频道**：未采用 **无精确 watch URL** 的条目（避免无法逐条核验）。
- **X 单帖**：未检索到 **可稳定引用的一手帖 URL**（**证据链不足**）。

## 7) 本周累计计数

- Agent: **59**（`0504.md` §7: 53 + 本日 §1: 6）
- Theory: **24**（`0504.md` §7: 22 + 本日 §2: 2）
- Career/Business: **13**（`0504.md` §7: 11 + 本日 §4: 2）
- LC: **8**（`0504.md` §7: 7 + 本日 §5: 1）
- IELTS: **8**（`0504.md` §7: 7 + 本日 §5: 1）

---

**非白名单占比**: 主链显式 `[OUTSIDE_WHITELIST]` **0 条（0%）**，**≤20%**。
