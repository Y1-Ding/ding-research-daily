# Daily Research - 2026-06-19 (21:30)

## 0) 今日3条结论

- **GLM thinking + constrained `tool_choice` 会在 reasoning→tool 边界崩溃**：**2026-06-19** 开 PR [#46149](https://github.com/vllm-project/vllm/pull/46149)——`DelegatingParser._apply_structural_tag` 硬编码 `reasoning=False`，xgrammar 语法不含 `</think>` 前缀，与 speculative decode 跨边界 token batch 冲突；named function / `"required"` + thinking ON → HTTP 500 或 mid-stream SSE error。
- **Sparse MLA（GLM-5.2 DSA）可用 DCP 把 KV 容量扩 ~7.3×**：**2026-06-19** 开 PR [#46145](https://github.com/vllm-project/vllm/pull/46145)——`num_kv_heads=1` 下 TP 全副本冗余；DCP=8 在 8×H200 上 KV tokens **2.54M**（DCP=1 仅 346K），支持 ~19 路并发 128K；核心算法是 **local top-K → global remap → all_gather → global top-K**。
- **Streaming parser engine「split 模式」在 reasoning→tool 交接处泄漏 U+FFFD**：**2026-06-19** maintainer [@bbrowning](https://github.com/bbrowning) 在 [#45919](https://github.com/vllm-project/vllm/pull/45919) 确认根因——`DelegatingParser` 中 `model_tokenizer.decode()` 在 reasoning parser 向 tool parser 交棒时产生替换字符；GLM-4.7-Flash 韩文 curl 可复现；统一 ParserEngine 模式或为 workaround。

## 1) Agent工程化（high-signal）

- [[Bugfix] Structural-tag grammar omits reasoning prefix, crashing forced tool_choice when thinking enabled（#46149，opened **2026-06-19**）](https://github.com/vllm-project/vllm/pull/46149)
  - 摘要: GLM-5.2 + `--tool-call-parser glm47 --reasoning-parser glm45 --enable-auto-tool-choice` + MTP speculative decode；`tool_choice` 为 named function 或 `"required"` 且 thinking ON 时，xgrammar FSM 在 `'.' + '</think>' + '<tool_call>'` 边界 reject tokens；根因 `_apply_structural_tag(..., reasoning=False)` 与 engine 假设不一致；修复传 `reasoning=thinking_enabled`；4-arm 矩阵从 2 fail → all pass；GLM-5.2-NVFP4 TP8 验证。
  - 为什么重要(对我): **agent 强制工具调用 + 推理模式** 是生产常见组合——grammar 与 parser 状态机必须 **同构** 模型输出拓扑，否则 eval 会误报「模型不跟 tool schema」。
  - 明日动作(<=20min): 写 **是/否** 用过 `tool_choice:"required"` + thinking（是/否）；若「是」，标是否遇 mid-stream tool 中断（是/否/未知）。

- [[DCP] Enable Decode Context Parallelism for sparse MLA (GLM-5.2 DSA)（#46145，opened **2026-06-19**）](https://github.com/vllm-project/vllm/pull/46145)
  - 摘要: `FlashMLASparseBackend` 新增 `can_return_lse_for_decode`；`indexer.py` / `sparse_attn_indexer.py` 实现 **DCP 下 global top-k**（local top-K → `global_pos = cp_rank + local_idx * dcp_ws` → all_gather → global top-K）；8×H200 + GLM-5.2-FP8 + MTP5 + CUDAGraph：DCP=8 纯 decode **150 tok/s**（DCP=1 为 394），但 128K 并发 **19 vs 2**；修复 MTP decode `ceildiv` 导致 ~400 token 后 repetition 的 bug。
  - 为什么重要(对我): **长上下文 agent  serving** 的瓶颈常是 KV 而非 FLOPs——sparse MLA + DCP 是 **容量/吞吐 trade-off 旋钮**（sweet spot DCP=2/4）。
  - 明日动作(<=20min): 写 **≤15 词**：你最长 agent context 需求（8K/128K/1M/未知）。

- [[Render] Add reasoning/tool parsing to /derender + fix byte-fallback FFFD（#45919，updated **2026-06-19**）](https://github.com/vllm-project/vllm/pull/45919)
  - 摘要: 合并 #45045 的 `build_chat_message()` 单一真相源；`/derender` 支持 parser-aware reasoning + tool call 解析；修复 byte-fallback U+FFFD；17 unit + e2e roundtrip tests；**2026-06-19** bbrowning 确认 chat completion streaming 的 U+FFFD 根因在 **DelegatingParser split 模式** `decode()` 泄漏，非本 PR `/derender` 路径；建议 GLM 走 unified `Glm47MoeParser` 或消除 decode 依赖。
  - 为什么重要(对我): **RL / logprob 训练管线** 用 `/derender` 时 reasoning/tool 解析必须与 chat 路径一致；streaming agent 的 CJK 乱码是 **parser 架构债**，影响多语言 eval。
  - 明日动作(<=20min): 写 **是/否** 经 vLLM streaming 处理 **非 ASCII** agent 输出（是/否）；若「是」，标是否见过 U+FFFD 替换符（是/否）。

- [[Frontend] [Parser] Port DeepSeek V4 to streaming parser engine framework（#45877，updated **2026-06-19**）](https://github.com/vllm-project/vllm/pull/45877)
  - 摘要: 用 declarative state machine `DeepSeekV4Parser` 替换旧 V32 subclass + V3 reasoning delegation；DSML tool call + `string="true"/"false"` arg 语义；**2491 tests passed**；**2026-06-19** 并入 V3.2 parser 后转 draft 再验证；live server 测试通过；parallel call wrapper unwrapping、partial param regex `<` 等 6 个 bugfix commits。
  - 为什么重要(对我): **ParserEngine 统一框架** 已成 vLLM agent serving 主路径——DeepSeek V4 接入意味着 DSML + reasoning 与 Gemma4/Qwen3/MiniMax 同构，便于 **跨模型 agent eval**。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 serve DeepSeek V3/V4（是/否/计划）。

- [[Frontend] Report cache usage in Anthropic /v1/messages API（#40912，updated **2026-06-19**）](https://github.com/vllm-project/vllm/pull/40912)
  - 摘要: 填充 `cache_read_input_tokens` / `cache_creation_input_tokens`（此前恒 `None`）；修正 `input_tokens = prompt_tokens - cached_tokens` 满足 Anthropic `total = input + cache_read + cache_creation`；opt-in `--enable-prompt-tokens-details`；streaming 故意与 vLLM OpenAI 一致——`message_start` 省略 cache 字段、`message_delta` 终端 chunk 填充；**13 tests passed**；Claude Code 实测 prefix hit **54600 cache_read**。
  - 为什么重要(对我): **agent 成本归因** 依赖 Anthropic usage 字段——Claude Code / 自研 agent 网关若读错 `input_tokens` 会 **低估 cache 节省**。
  - 明日动作(<=20min): 写 **是/否** 读 Anthropic response `usage.cache_read_input_tokens` 做计费（是/否/未知）。

- [[SpecDecode] Support DFlash with FlashInfer（#43081，updated **2026-06-19**）](https://github.com/vllm-project/vllm/pull/43081)
  - 摘要: DFlash 非因果 attention 走 FlashInfer `causal=False` prefill；RTX 4090 上 **DFlash + FlashInfer + FP8 KV** 为唯一可行 FP8 组合；GSM8K **0.844**、acceptance_len **3.69**、TPOT **2.49ms**；**2026-06-19** 修复 pre-commit + 避免 non-causal 选 TRTLLM；`verified` label；追踪 issue [#46105](https://github.com/vllm-project/vllm/issues/46105)。
  - 为什么重要(对我): **低成本 speculative decode**（DFlash 16 draft tokens/step）可在消费级 GPU + FP8 KV 落地——适合本地 agent 原型 **吞吐翻倍** 实验。
  - 明日动作(<=20min): 写 **≤10 词** GPU 型号（如 4090/A100/无）。

- [fix(langgraph): require `langgraph-api>=0.10.0` for `DeltaChannel`（#8043，ready **2026-06-17**）](https://github.com/langchain-ai/langgraph/pull/8043)
  - 摘要: `StateGraph.compile` 检测 `importlib.metadata.version("langgraph-api")`；`<0.10.0`（含 `0.10.0rc1`）且图含 DeltaChannel → **compile-time RuntimeError** 提示升级；`langgraph-api` 未安装时 no-op（本地执行）；E2E：`0.9.0` fail、`0.10.0` pass。
  - 为什么重要(对我): **DeltaChannel agent 自托管** 升级 langgraph 但不升 API server 会 **运行时 silent fail**——compile guard 把失败前移到 deploy 阶段。
  - 明日动作(<=20min): 写 **≤15 词**：你 LangGraph 部署 `langgraph-api` 版本（<0.10/≥0.10/未知）。

- [[Bugfix][CI] Retry cached HF tokenizer load after transport failures（#44820，updated **2026-06-19**）](https://github.com/vllm-project/vllm/pull/44820)
  - 摘要: CI / 冷启动时 HuggingFace tokenizer 下载偶发 transport failure 导致 **整个 serving 启动失败**；对 cached load 加重试；降低 **agent 推理节点弹性部署** 的 flaky 启动率。
  - 为什么重要(对我): K8s 滚动升级 agent 栈时，tokenizer pull 失败 = **全 pod CrashLoop**——重试是生产必备韧性。
  - 明日动作(<=20min): 写 **是/否** vLLM 启动依赖 **在线 HF hub**（是/否/镜像）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Structural tag grammar 必须建模 reasoning phase（vLLM #46149，**2026-06-19**）](https://github.com/vllm-project/vllm/pull/46149)
  - 摘要: xgrammar `get_glm_4_7_structural_tag(reasoning=False)` 仅含 tool-call suffix；thinking ON 时模型 emit `reasoning + </think> + <tool_call>`；speculative decode 单 batch 跨边界 → FSM reject。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) constrained decoding grammar 是 **输出空间子集** 还是 **解析状态机**？(2) reasoning 与 tool 是 **串行 phase** 还是可交错？(3) speculative decode 如何 **原子化 phase 边界**？
  - 明日动作(<=20min): 画 **1 条** reasoning→tool 的 token 序列（≤10 token 占位符）。

- [DCP sparse MLA 的 exact global top-k 证明（vLLM #46145，**2026-06-19**）](https://github.com/vllm-project/vllm/pull/46145)
  - 摘要: 全局 top-K 中任一 token 必在其 shard 的 local top-K 内（否则被 shard 内更高分 token 挤出）；indexer Q replicated、KV shard disjoint → LSE merge = global softmax；comm 成本 ~0.1ms NVLink。
  - 为什么重要(对我): **Sparse attention + 分布式 KV** 的核心是 **global index 一致性**——DSA indexer 与 dense DCP 的 LSE reduce 可复用，sparse 额外 all_gather candidates。
  - 明日动作(<=20min): 写 **≤20 词**：你理解的 sparse attention 是 **固定 pattern 还是 learned index**（固定/learned/未知）。

- [DelegatingParser split vs unified 与 tokenizer.decode 边界（#45919 + bbrowning，**2026-06-19**）](https://github.com/vllm-project/vllm/pull/45919)
  - 摘要: split 模式 reasoning parser → tool parser 交棒时 `decode()` 产生 U+FFFD；unified `Glm47MoeParser` 可规避；根因影响所有 split 新 engine parser 的非 ASCII 流。
  - 为什么重要(对我): **Agent streaming 原理**：parser 应在 **token id 空间** 交接，而非 **decoded string 空间**——decode 非单射（byte fallback、多字节字符）。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 输出解析在 **token 还是 string 层**（token/string/混合/未知）。

- [MLARoPE + KV cache cat manual fusion pass（vLLM #45573，updated **2026-06-19**）](https://github.com/vllm-project/vllm/pull/45573)
  - 摘要: 将 `MLARoPEKVCacheCatFusionPass` 从 compile pass 迁到 manual fusion；DeepSeek-V2-Lite bench：fuse on avg **2.246s** vs baseline fuse off **2.260s**；**228 unittests passed**；closes #43504。
  - 为什么重要(对我): **MLA（Multi-head Latent Attention）** 的 RoPE+KV write 融合减少 kernel launch——长上下文 decode 的 **memory bandwidth** 优化路径。
  - 明日动作(<=20min): 写 **≤15 词**：你用的模型是否 MLA/DSA 族（是/否/未知）。

## 3) 热门项目/可试个人项目

- [Datasette Apps: Host custom HTML applications inside Datasette（Simon Willison Blog，**2026-06-18**）](https://simonwillison.net/2026/Jun/18/datasette-apps/)
  - 摘要: 新插件 `datasette-apps`：在 Datasette 内托管 **sandbox iframe** HTML+JS 应用；`sandbox` + CSP 锁外域；`postMessage`/`MessageChannel` 协议跑 **allow-list SQL**；stored queries 支持受控 write；含 **copy-paste prompt** 让 LLM 生成 app；Claude Fable 5 安全审计发现 CSP allow-list 提权漏洞已修（`apps-set-csp` 权限）。
  - 为什么重要(对我): **低成本 data+agent UI**：SQLite + sandboxed frontend + LLM codegen = 个人 **RAG dashboard / 内部分析工具** 原型，无需全栈 deploy。
  - 明日动作(<=20min): 试 [agent.datasette.io](https://agent.datasette.io) demo；写 **≤10 词** 是否适合你的数据场景（是/否）。
  - 估计复现成本: **低**（pip install + 单 SQLite；无 GPU）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [AI demands more engineering discipline. Not less（Charity Majors 引述，Simon Willison Blog，**2026-06-17**）](https://simonwillison.net/2026/Jun/17/glm-52/)
  - 机会信号: Charity Majors 指出 **2025 年代码生成经济学反转**——代码从「珍贵资产」变「可丢弃可再生」；但 AI 时代需要 **更强工程纪律**（测试、review、架构），而非更少；对 **AI solo company** 意味着 **demo→production 鸿沟** 靠流程而非更多 prompt 填补。
  - 风险信号: 「vibe code 即可出货」叙事掩盖 **可维护性/安全/合规** 成本；Datasette Apps 的 Fable 5 安全审计即例证——**AI 加速开发 ≠ AI 替代安全 review**。
  - 我可执行的一步(<=30min): 读 [Microsoft MVP checklist](https://simonwillison.net/2026/Jun/18/datasette-apps/) 赞助链接或自建 **agent 上线前 5 项检查清单**（测试/权限/回滚/监控/成本）；写 **≤30 词** 你栈最弱一环。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 385. Mini Parser](https://leetcode.com/problems/mini-parser/)：递归下降解析嵌套 `NestedInteger`——对照 vLLM **ParserEngine declarative state machine**（#45877 DeepSeek V4）。明日动作：手写 `deserialize` 后写 **≤15 词**：`[` 出现时栈/递归如何处理（≤10 词）。

- **IELTS（≤1 条）**：[IELTS.org — Listening Sample Task (PDF)](https://s3.eu-west-2.amazonaws.com/ielts-web-static/production/Sample-tests/ielts-listening-sample-task-2023.pdf)：官方 **Listening Section 1** 样题（填空+选择）。明日动作：听 **Q1–5**（≤20 min），写 **≤15 词** 错因（拼写/数字/同音/语速 选 1）。

## 6) 噪音过滤记录

- **vLLM #45823 KV offload defer、#44602 inline system、#45444 SWA mask、#45093 encoder cache、#44725 count_tokens、#45950 Rust /v1/models、#45757 CPU eviction**：`0618.md` **URL/语义去重** → 不重复展开。
- **LangGraph #8053 nested subgraph checkpoint、#8109 delta-channel-dump**：`0618.md` 已覆盖 → 丢弃。
- **vLLM #46151/#46150 MoE FP8 LoRA / MTP revert drafts**：无 merge、回归风险向 → 丢弃。
- **vLLM #45470 DeepSeek V4 hardware-agnostic draft**：无 **0619** 新进展 → 丢弃。
- **vLLM #46154 CPU GQA segfault、#46156 tokenizer pickling**：kernel/边缘 bug，agent 通用信号弱 → 丢弃。
- **OpenAI Sora / audio model 博客**：非 agent 工程主轨 → 丢弃。
- **Simon June 19 pelican sightings**：无技术信号 → 丢弃。
- **`microsoft/autogen` / `openai/evals` / `run-llama/llama_index`**：无 **2026-06-19** 新鲜高信号 merge → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取原帖 → 不收录。

## 7) 本周累计计数

- Agent: **330**（`0618.md` §7: 322 + 本日 §1: 8）
- Theory: **138**（`0618.md` §7: 134 + 本日 §2: 4）
- Career/Business: **69**（`0618.md` §7: 68 + 本日 §4: 1）
- LC: **51**（`0618.md` §7: 50 + 本日 §5: 1）
- IELTS: **51**（`0618.md` §7: 50 + 本日 §5: 1）
