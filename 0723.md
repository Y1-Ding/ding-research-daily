# Daily Research - 2026-07-23 (21:30)

## 0) 今日3条结论

- **SummarizationMiddleware 在 summary 失败时仍删全历史**（LangChain #39000, **2026-07-23**）：429/timeout 时 `_create_summary` 把 error string 当合法 summary，随后 `RemoveMessage(REMOVE_ALL_MESSAGES)` —— 压测/限流场景的最差失败模式；middleware 层 eval 必须测「summary 异常 → history 保留」。
- **multimodal Memory token 低估可达 8–60x**（LlamaIndex #22441, **2026-07-23**）：flat default（image 256）vs block 级 `aestimate_tokens()`；sliding window / summary 会过早截断 multimodal agent 上下文 —— token 预算应走 per-block 估计而非类型常量。
- **vLLM 无界队列让 TTFT 隐性欠债**（vLLM #49445, updated **2026-07-23**）：engine 接受请求后 waiting queue 无上限；`max_num_queued_reqs` / `max_num_queued_tokens` 可在 IPC 链最早拒载 —— agent eval batch 压测应记录 queue depth 与 TTFT 关联。

## 1) Agent工程化（high-signal）

- [fix(core): empty stream success + deferred tool_calls for args=""（LangChain PR #39037, updated **2026-07-23**）](https://github.com/langchain-ai/langchain/pull/39037)
  - 摘要: 修复两 bug：(1) `RunnableWithFallbacks.stream()` 用 bare `next` peek 首 chunk，合法空 upstream stream 触发 `StopIteration` 被当失败 → fallback 或 `RuntimeError`；(2) `AIMessageChunk.init_tool_calls` 把 `args=""` 当缺失，首 SSE fragment 即 materialize `tool_calls` 为 `{}`，后续 streaming args 无法合并。
  - 为什么重要(对我): **评测 + skill 落地**——空 stream 假失败 = eval harness 误触发 fallback；premature `{}` tool_call = multi-turn tool eval 假阳性/invoke 崩溃。
  - 明日动作(<=20min): 写 **是/否**：你 streaming eval 是否测过空 stream + `args=""` 首 fragment（是/否/未知）。

- [fix: preserve conversation history when summary generation fails（LangChain PR #39000, updated **2026-07-23**）](https://github.com/langchain-ai/langchain/pull/39000)
  - 摘要: `SummarizationMiddleware` 在 summary model 429/timeout 时：原先 catch 异常返回 `"Error generating summary: ..."` 字符串，随后仍发 `RemoveMessage(id=REMOVE_ALL_MESSAGES)` 替换全对话。Fix：异常时返回 `None`，`before_model` 跳过 summary 替换，保留原始 history。
  - 为什么重要(对我): **token 优化 + 评测**——summary middleware 是长对话 agent 标配；限流场景删历史 = 不可恢复数据丢失 + eval 不可复现。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否用 summarization middleware（是/否/未知）。

- [test(core): add regression tests for EvaluationResult.feedback_config key preservation（LangChain PR #39038, updated **2026-07-23**）](https://github.com/langchain-ai/langchain/pull/39038)
  - 摘要: `EvaluationResult.feedback_config` 存任意 metadata（如 `{threshold: 1.0}`）时，Pydantic `Union[FeedbackConfig, dict]` 在旧版 langsmith 会静默丢弃未声明 key。Fix 已在 `make_evaluation_result` 实现（post-`__init__` 赋值），本 PR 补 regression tests pin 行为。Closes #31802。
  - 为什么重要(对我): **评测**——eval harness 用 `feedback_config` 挂 threshold/rubric version 时 silent key loss = 门禁阈值漂移无告警。
  - 明日动作(<=20min): 写 **是/否**：你 eval result 是否用 `feedback_config` 存 metadata（是/否/未知）。

- [Fix/memory multimodal token estimate（LlamaIndex PR #22441, updated **2026-07-23**）](https://github.com/run-llama/llama_index/pull/22441)
  - 摘要: `Memory._estimate_token_count()` 改为 delegate 各 block 的 `aestimate_tokens()`，替代 flat per-type default（image/audio/video 256, document 2048）——后者对 multimodal 内容 undercount **8–60x**。Fixes #22430，含新单元测试。
  - 为什么重要(对我): **token 优化**——undercount → memory window/summary 过早截断 → multimodal agent 丢关键视觉上下文；collect 层应记录 estimate vs actual token 偏差。
  - 明日动作(<=20min): 写 **是/否**：你 memory 层是否用 flat per-type token default（是/否/未知）。

- [feat(sdk-py): support v2 stream format in join_stream（LangGraph PR #8416, updated **2026-07-23**）](https://github.com/langchain-ai/langgraph/pull/8416)
  - 摘要: `RunsClient.stream()` 已支持 `version="v2"` typed stream parts，但 `join_stream()`（background run replay）仅 v1。Fix：async/sync `join_stream` 新增 `version` 参数，mirror `stream()` 行为，默认 `v1` 不变。
  - 为什么重要(对我): **skill 落地 + 评测**——background agent run replay 是 eval/monitor 常见路径；v1-only join 阻塞 v2 migration = trace 解析不一致。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `join_stream` replay background runs（是/否/未知）。

- [fix: guard LLM response against empty choices（LlamaIndex PR #21708, updated **2026-07-23**）](https://github.com/run-llama/llama_index/pull/21708)
  - 摘要: 修复 16 处 unguarded `response.choices[0]` 访问（OpenAI/AI21/GigaChat/Friendli/Azure Inference/Mistralai 等 integration），LLM 返回空 `choices` 时不再 `IndexError`/`AttributeError`。Fixes #21337。
  - 为什么重要(对我): **评测**——rate limit / provider glitch 返回空 choices = eval worker 崩溃而非可重试错误；harness 应区分 empty response vs model refusal。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否 catch 空 `choices` 并重试（是/否/未知）。

- [[Core] Add max_num_queued_reqs and max_num_queued_tokens for queue size management（vLLM PR #49445, updated **2026-07-23**）](https://github.com/vllm-project/vllm/pull/49445)
  - 摘要: vLLM 默认 **unbounded request queue**——请求被接受后 waiting queue 饱和时仍静默承担不可预测 TTFT。Fix：`--max-num-queued-reqs` 和 `--max-num-queued-tokens` 在 `AsyncLLM.add_request` 同步检查，超限时尽早拒载，让 LB 可 retry 到更轻实例。
  - 为什么重要(对我): **token 优化 + 评测**——agent eval batch 压测若不设 queue cap，TTFT 长尾被当 model 慢而非 infra 欠债；应记录 queue depth 与 per-request token 预算。
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 是否设 queue/token cap（是/否/未知）。

- [fix: improve pipeline compatibility with PgBouncer and SSL connections（LangGraph PR #8418, updated **2026-07-23**）](https://github.com/langchain-ai/langgraph/pull/8418)
  - 摘要: Supabase/PgBouncer+SSL 场景启用 pipeline mode 会 `SSL connection has been closed unexpectedly`；默认 `pipeline=False` 已规避，但文档未警告。Fix：在 `from_conn_string` docstring 加 PgBouncer+SSL 不兼容警告。Closes #5675（承接 #8396 pipeline reference 修复链）。
  - 为什么重要(对我): **skill 落地**——LangGraph checkpoint 用 Postgres 是 production agent 常见部署；踩坑 = agent state 随机断连 = eval/prod 不可复现。
  - 明日动作(<=20min): 写 **是/否**：你 checkpoint 是否走 PgBouncer（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Spec Decode][V1] Warm Eagle and DFlash/DSpark spec-decode Triton kernels at startup（vLLM PR #48804, updated **2026-07-23**）](https://github.com/vllm-project/vllm/pull/48804)
  - 摘要: Triton 对 runtime value=1 的 integer arg 和 `tl.constexpr` 做 compile-time specialization，每种组合产独立 cubin；无 warmup 时首请求每种 shape 付 JIT latency spike。Fix：新增 `vllm/model_executor/warmup/eagle_spec_decode_warmup.py` 等模块，枚举 spec-decode Triton kernel 全部参数组合并在 startup 预热。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：spec-decode 是 agent serving 降 latency 核心路径；首 token JIT spike = multi-turn eval 冷启动噪声；warmup 枚举 = 理解 Triton specialization 与 attention kernel 编译成本。
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 是否做 kernel warmup（是/否/未知）。

- [[Online quantization] Add online MXFP4 quantization support（vLLM PR #49347, updated **2026-07-23**）](https://github.com/vllm-project/vllm/pull/49347)
  - 摘要: 支持 `--quantization mxfp4` 对 bf16/fp16 模型做 online MXFP4 linear/MOE 量化；无 flashinfer/aiter 时走 Triton fallback `downcast_to_mxfp`。让未预量化模型可直接降 memory footprint serving agent 长上下文。
  - 为什么重要(对我): **Attention + token 优化**：MXFP4 降 weight memory = 更大 batch/KV budget；online quant 路径需单独 eval 精度退化对 tool-calling 的影响。
  - 明日动作(<=20min): 写 **≤15 词**：你是否测过 quant 后 tool-call 准确率（是/否/未知）。

- [[Bugfix][ROCm][CI] Fix qk_norm_rope_kvcache fusion test for packed KV layout（vLLM PR #49590, updated **2026-07-23**）](https://github.com/vllm-project/vllm/pull/49590)
  - 摘要: KV cache 已统一为 4D packed layout `(num_blocks, num_kv_heads, block_size, 2*head_size)`（#44455），但 fusion test 仍用旧 5D layout + 5-element `kv_stride_order`，ROCm CI 上 `IndexError`。Fix：对齐新 layout 的 stride order 参数化。
  - 为什么重要(对我): **Attention 机制**：QK-norm + RoPE + KV cache fusion 是 prefill/decode 热路径；layout 迁移若 test 不跟 = silent correctness regression on attention output。
  - 明日动作(<=20min): 写 **≤15 词**：你关注 KV cache layout 变更吗（是/否/未知）。

## 3) 热门项目/可试个人项目

- [darkrishabh/agent-skills-eval（GitHub, last push **2026-07-15**）](https://github.com/darkrishabh/agent-skills-eval)
  - 摘要: [agentskills.io](https://agentskills.io) 标准 skill 的 test runner：每个 eval 跑 `with_skill` vs `without_skill` 双轨，judge model 按 assertion 打分，支持 tool-call assertions + JSONL diff + static HTML report。TypeScript SDK + CLI，OpenAI-compatible provider。
  - 为什么重要(对我): **skill 落地 + 评测杠杆**——直接复用到你的 skill A/B eval pipeline：写 `evals/evals.json` → 跑 baseline vs skill lift → 有 receipts 而非 vibes。
  - 明日动作(<=20min): clone 仓库，读 README 的 `evals.json` schema；写 1 条你可映射的 assertion 样例（≤10 行）。
  - 估计复现成本: **低**（读 schema + 写 1 eval case，≤30min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Growing a fully-autonomous business to $500k/mo in 3 months（Indie Hackers, Polsia）](https://www.indiehackers.com/post/tech/growing-a-fully-autonomus-business-to-a-500k-mo-in-3-months-diZ8gkqMHm0CvEsc7Pfo)
  - 机会信号: 单人 founder + **零员工** 用 AI agent worker queue（Node.js + BullMQ + Redis）并行跑 marketing/support/ops/ads；$49/mo base + task credits 变现；**2025-12-15 上线 day-one revenue → 3 个月近 $500k/mo** —— 证明「agent 编排层」可替代早期 hire，eval/approval gate 是杠杆点。
  - 风险信号: 案例自述难独立验证；全自主 claim 可能过度营销；无公开 eval harness / incident 复盘；国内出海路径（支付/合规/流量）不同。
  - 我可执行的一步(<=30min): 写 **≤8 行**：你当前 1 个 agent 工作流中，哪 1 步可拆成「approval gate + eval 门禁」而非全人工执行。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 3513 Number of Unique XOR Triplets I](https://leetcode.com/problems/number-of-unique-xor-triplets-i/)（**Day 23, Jul 2026, Medium**）：给定数组，求不同 `(i,j,k)` 三元组 XOR 值种类数。明日动作：理解 XOR 前缀性质；写 **≤15 词**：bitmask 状态与 agent 离散状态空间的类比。

- **IELTS（≤1 条）**：[IELTS Ready — British Council 官方免费备考（含 AI Mini Mock）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-ready)：50 分钟 Mini Mock + AI 估分。明日动作：完成 **Listening 1 节 Mini Mock**；写 **≤15 词** 错因类型（拼写/连读/同义替换 选 1）。

## 6) 噪音过滤记录

- **AutoGen #7991/#7957/#7958、#7984–7987**：`0722.md` 已收录同族 cancel/truncation/orphan 修复 → 不重复。
- **LangGraph #7273 message IDs**：`0721.md` 已收录 → 不重复（07-23 仅 rebase 更新）。
- **vLLM #48295 llama3_json/jamba tool parser**：`0717–0719.md` 已收录 → 不重复。
- **vLLM #49294/#47629/#48123**：`0722.md` 已收录 → 不重复。
- **LangChain #39008/#39012、LlamaIndex #22419/#21800**：`0722.md` 已收录 → 不重复。
- **Cassie07/AgentSkill_Survey、Simon Willison Jul 21 Cat/Thariq**：`0722.md` 已收录 → 不重复。
- **openai/evals #1657/#1694**：`0720–0721.md` 待观察 → 不重复。
- **LlamaIndex #22360 reasoning_options structured predict**：偏 OpenAI Responses API 边缘 case，非今日 focus → 待观察。
- **LlamaIndex #21796 TokenTextSplitter logging fix**：logging 内部 TypeError，无 eval 动作 → 待观察。
- **LangChain #39036 model profiles refresh**：catalog 更新，无直接 eval/skill 动作 → 待观察。
- **vLLM #49587 doc gen、#49347 cleanup TODO**：非用户面信号 → 待观察。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-23** 原帖 URL → 不收录。
- **Simon Willison Jul 23 PyPI 14-day upload policy**（[blog.pypi.org](https://blog.pypi.org/posts/2026-07-22-releases-now-reject-new-files-after-14-days/)）：高价值 supply-chain 信号但非 agent eval 主轨 → 记入待观察（skill 发布应用新版本而非补传旧 release wheel）。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Jul 23 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **590**（`0722.md` §7: 582 + 本日 §1: 8）
- Theory: **242**（`0722.md` §7: 239 + 本日 §2: 3）
- Career/Business: **102**（`0722.md` §7: 101 + 本日 §4: 1）
- LC: **84**（`0722.md` §7: 83 + 本日 §5: 1）
- IELTS: **84**（`0722.md` §7: 83 + 本日 §5: 1）
