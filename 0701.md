# Daily Research - 2026-07-01 (21:30)

## 0) 今日3条结论

- **LangGraph Cloud 长 tool call 在 ~180s 被静默重放（#7417，updated 2026-07-01）**：`BG_JOB_HEARTBEAT=120` + sweep 机制使运行中的 subagent `ainvoke()` 被当作可重试任务重新 dispatch，原调用与 duplicate 均成功 → skill 落地若依赖长时 tool，eval 与账单会出现 2–3× 隐性成本，且 trace 无 preceding `model` 节点。
- **`durability="sync"` checkpoint 与 pending writes 仍缺原子事务边界（#8234，opened 2026-07-01）**：即便 #8050/#8055 已 merge，crash recovery 仍可能将 partial superstep writes 与错误 checkpoint 配对 → 生产 agent 产出 silent corruption，L3 eval 门禁无法捕获（应用不 fail，只输出错误结果）。
- **Attention 头可 RL 涌现 DFS 搜索机制（arXiv 2606.00183）**：两 head transformer 在稀疏 terminal reward 下分阶段学会「track visited actions」+「detect failure → backtrack」；depth-1/2 curriculum 训练后可泛化到更深树 → 为 agent planning eval 提供「head specialization」可观测假说。

## 1) Agent工程化（high-signal）

- [Long tool calls (~180s+) silently re-executed from checkpoint on LangGraph Cloud（LangGraph #7417 updated **2026-07-01**）](https://github.com/langchain-ai/langgraph/issues/7417)
  - 摘要: DeepAgents `SubAgentMiddleware` 的 `task()` tool 内 `subagent.ainvoke()` 常需 3–10 分钟；约 180s 后 trace 出现 duplicate run——相同 tool args hash、独立 `tools` 节点但**无 preceding `model` 节点**；原调用与 duplicate 均成功完成。根因指向 `langgraph-api` 中 `BG_JOB_HEARTBEAT=120`（硬编码不可配）+ `Runs.Sweep()` 将 `CancelledError` 视为可重试并 re-dispatch。
  - 为什么重要(对我): **token 优化 + eval**——长时 subagent 是 skill 编排常见模式；静默重放使 cost/latency eval 失真，且 duplicate 结果可能污染 downstream state。
  - 明日动作(<=20min): 写 **≤15 词**：你最长 tool call 是否 >3min（是/否/未知）。

- [fix: durability='sync' checkpoint ordering unenforced — post-crash recovery can restore inconsistent state（LangGraph #8234 opened **2026-07-01**）](https://github.com/langchain-ai/langgraph/issues/8234)
  - 摘要: `durability="sync"` 下 `put_writes()` 与 checkpoint persist 无保证顺序/原子性；crash recovery 可能将 partial superstep writes 恢复到错误 checkpoint。已有 #8050/#8055 仍不足——checkpoint 成功但 pending write future 失败、分布式 postgres 部署 partial state、无共享 DB transaction 边界。
  - 为什么重要(对我): **skill 落地 + eval**——sync durability 是生产长会话 agent 的默认期望；silent state corruption 使 L2 trajectory match 与 prod 行为分叉且极难 debug。
  - 明日动作(<=20min): 写 **是/否**：你 LangGraph 是否配置 `durability="sync"`（是/否/未知）。

- [Collaboration: Trust-gated checkpoints and governance nodes for LangGraph（LangGraph #7303 updated **2026-07-01**）](https://github.com/langchain-ai/langgraph/issues/7303)
  - 摘要: Microsoft [Agent Governance Toolkit](https://github.com/microsoft/agent-governance-toolkit) 提供 `langgraph-trust` 适配器（PyPI 已 ship）：trust-gated checkpoint nodes、governance conditional edges、ContentQualityEvaluator 维度评分驱动分支。Issue **2026-07-01** 仍有 activity，讨论 factual verification 作为 conditional edge signal。
  - 为什么重要(对我): **skill 落地 + governance eval**——与 openai/evals healthcare 三态决策、你 F/C 规则引擎同构；trust-gated checkpoint 是 skill 审批链的可复用 primitive。
  - 明日动作(<=20min): 写 **≤15 词**：你 agent 是否有 trust/quality gate 节点（有/无/仅 prompt）。

- [Clarify FactExtractionMemoryBlock condense prompt to return a full snapshot（LlamaIndex #22213 opened **2026-07-01**）](https://github.com/run-llama/llama_index/pull/22213)
  - 摘要: `DEFAULT_FACT_CONDENSE_PROMPT` 歧义导致 LLM 返回 incremental facts 而非 full snapshot；condense 路径 `self.facts = condensed` 全量替换，模型若只返回少量 fact 会 gut/wipe 已存记忆（#21103）。Fix：明确 prompt 要求返回完整去重 snapshot，移除两条 incremental 暗示指令。
  - 为什么重要(对我): **token 优化 + eval**——memory condense 是长 trace token 削减杠杆；prompt 歧义导致 silent memory loss，L2 eval 会出现「突然遗忘」假阴性。
  - 明日动作(<=20min): 写 **是/否**：你 memory condense 是否全量替换（是/否/未知）。

- [langchain stream_events v3 final response tokens not streamed in real-time, all arrive at once（LangChain #38484 updated **2026-07-01**）](https://github.com/langchain-ai/langchain/issues/38484)
  - 摘要: `stream_events` v3 在 agent 执行中，final response tokens 不实时流出，而是在执行结束时一次性到达；影响实时 observability 与 streaming UX。含最小可复现示例，**2026-07-01** 仍 open。
  - 为什么重要(对我): **eval harness**——collect→test→eval 若依赖 stream_events 做实时 token 计数/中断，会得到错误时序；与 token 优化监控直接相关。
  - 明日动作(<=20min): 写 **≤15 词**：你是否用 `stream_events` v3 做 token 监控（用/不用/未知）。

- [Add deterministic middleware orchestration with priority, conditional activation, and safe parallel execution（LangChain #37972 updated **2026-07-01**）](https://github.com/langchain-ai/langchain/issues/37972)
  - 摘要: Feature request：middleware 栈缺 first-class orchestration——(1) priority-based ordering、(2) runtime context 条件激活、(3) read-only middleware 安全并行 + deterministic merge。当前可 stack 但无生产级编排契约。
  - 为什么重要(对我): **skill 落地**——skill 常以 middleware 形态注入（guardrail、prompt transform、tool handler）；无 priority/conditional/parallel 契约使多 skill 组合不可预测，per-skill eval 难隔离。
  - 明日动作(<=20min): 写 **≤15 词**：你 middleware 是否 >3 层（是/否/未知）。

- [with_structured_output is not supported when reasoning effort is used（LangChain #38561 updated **2026-07-01**）](https://github.com/langchain-ai/langchain/issues/38561)
  - 摘要: `ChatOpenAI` 同时启用 `reasoning_effort` 与 `with_structured_output` 时报错/不兼容；含最小复现。**2026-07-01** 仍 open，影响 reasoning model 上的 structured eval grading。
  - 为什么重要(对我): **eval**——reasoning model + structured output 是 L2 exact-match grading 常见组合；不兼容迫使 eval 回退到 regex parse，降低可靠性。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否混用 reasoning + structured output（是/否/未知）。

- [Self-Compacting Language Model Agents（arXiv 2606.23525，submitted **2026-06-22**）](https://arxiv.org/abs/2606.23525)
  - 摘要: SelfCompact scaffold：compaction tool + lightweight rubric 让模型自主决定何时/如何压缩 context（sub-task resolved → fire；mid-derivation → suppress）。7 模型 × 6 benchmark：匹配或超越 fixed-interval summarization，token cost 降 30–70%，math +18.1pp、agentic search +5–9pp。揭示 meta-cognitive gap：模型 alone 不知 context rotting，rubric 可关闭。
  - 为什么重要(对我): **token 优化**——比固定阈值 compaction 更贴合 trajectory 结构；可直接挂 skill 层做 adaptive context 管理，eval 应对比 fixed vs rubric-gated compaction。
  - 明日动作(<=20min): 写 **≤15 词**：你 context 压缩是固定阈值还是事件触发（固定/事件/无）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Agentic Transformers Provably Learn to Search via Reinforcement Learning（arXiv 2606.00183，submitted **2026-05-29**）](https://arxiv.org/abs/2606.00183)
  - 摘要: 随机 k-ary 树环境，agentic transformer 仅观察 trajectory history + sparse terminal reward。构造两 head transformer 实现 randomized DFS：head-1 track visited actions，head-2 detect failure → backtrack。Policy gradient + depth-wise curriculum 使相同机制从 RL 分阶段涌现，无 expert demo；depth-1/2 训练后泛化到更深树。imbalanced goal 下 discount 产生 ranked DFS。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) head specialization 能否在真实 agent trace 中用 attention pattern 检测？(2) DFS normal form 与 ReAct/tool-loop 的对应关系？(3) depth curriculum 对你的 L1→L3 eval 分层有何启示？
  - 明日动作(<=20min): 写 **≤10 词**：你 eval 是否分层难度 curriculum（是/否/未知）。

- [Architecture-Aware Reinforcement Learning Makes Sliding-Window Attention Competitive in Math Reasoning（arXiv 2606.11634，submitted **2026-06-10**）](https://arxiv.org/abs/2606.11634)
  - 摘要: SWARR 两阶段：(1) SA→SWA conversion + SFT（无需重预训练）；(2) on-policy RL 在 SWA 约束下优化 self-generated trajectories。假设 SFT 数据含 SA 专属 long-range dependency 导致 SWA 落后；RL 使 trajectory 适配 SWA，math reasoning benchmark 大幅缩小 SWA vs SA gap，保留线性复杂度 attention 效率。
  - 为什么重要(对我): **Talk ≥3 turns**：(1) data-architecture mismatch 对你的 skill prompt 长度/结构意味着什么？(2) on-policy RL 适配 vs SFT 蒸馏哪个更适合 agent skill？(3) SWA 线性复杂度对长 trace serving 的 token 节省边界？
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 是否用 sliding-window attention（是/否/未知）。

## 3) 热门项目/可试个人项目

- [simonw/shot-scraper — `shot-scraper video` command（v1.10，**2026-06-30** release）](https://github.com/simonw/shot-scraper)
  - 摘要: 新 `shot-scraper video` 命令接受 `storyboard.yml` 定义 Playwright 自动化场景并录制 demo 视频；支持 `--mp4`、`--auth` cookie 注入、cursor 可见。Simon Willison 用 GPT-5.5 从 `--help` 输出自主生成 storyboard 并录制 Datasette bulk-insert demo；`--help` 设计类似内嵌 `SKILL.md`。
  - 为什么重要(对我): **eval 可见性杠杆**——agent 产出除 trace 外可加 video demo 作为 L3 人工审查证据；storyboard YAML 可纳入 collect→test→eval 流水线。
  - 明日动作(<=20min): `uvx shot-scraper video --help` 读 schema；写 **≤15 词** 你 agent 产出是否有可视化 demo（有/无/仅 log）。
  - 估计复现成本: **低**（`uvx shot-scraper`，无 GPU；需 Playwright browser）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [🚧 LangGraph v1 roadmap – feedback wanted!（LangGraph #4973 updated **2026-07-01**）](https://github.com/langchain-ai/langgraph/issues/4973)
  - 机会信号: **框架定义权窗口**——LangGraph 正征集 v1 `StateGraph` API 反馈（confusing parts、boilerplate、missing features）；早期贡献者可影响 agent orchestration 标准，与 composable skill primitives 方向对齐。
  - 风险信号: v1 可能 breaking change 已有 pipeline；反馈若无具体 use case 难被采纳；社区 issue 噪音大。
  - 我可执行的一步(<=30min): 读 issue + 评论；写 **≤30 词** 你最需要的一个 v1 feature（如 trust-gated node 一等公民）并评估是否发 comment。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 119 Pascal's Triangle II](https://leetcode.com/problems/pascals-triangle-ii/)：返回第 rowIndex 行；O(rowIndex) 空间从底向上单行递推。明日动作：手写 O(rowIndex) 空间版（≤20min）；写 **≤15 词**：递推行 vs agent 状态逐层展开有何类比。

- **IELTS（≤1 条）**：[IELTS Speaking — Describe a time you helped someone（British Council 样题）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-practice-tests/speaking)：Part 2 用「帮同事 debug agent eval」脱敏练 2 分钟陈述（when/what/how/result）。明日动作：录音 2 分钟自评 coherence（≤20min）；写 **≤15 词** 弱项（fluency/词汇/结构 选 1）。

## 6) 噪音过滤记录

- **GraphInterrupt #8217**：`0630.md` 已收录；**2026-07-01** 无 update → 不重复。
- **SkillsBench（arXiv 2602.12670）**：`0630.md` 已收录 → 不重复。
- **Keyless Attention（arXiv 2606.21848）**：`0630.md` 已收录 → 不重复。
- **healthcare_ai_governance eval #1682**：`0630.md` 已收录；**2026-07-01** 仅 metadata touch → 不重复细节。
- **fix(agent) model_to_tools_destinations #38569**：`0630.md` 已收录 merged → 不重复。
- **langchain-ai/agentevals**：`0630.md` 已收录 → 不重复。
- **Anthropic Claude Agent SDK blog**：`0630.md` 已收录 → 不重复。
- **TokenPilot（arXiv 2606.17016）**：与 SelfCompact 主题重叠且非 whitelist 首发；本日已收 SelfCompact → 丢弃。
- **context-ledger（wiztek-llc）**：非 whitelist repo，与 SelfCompact 压缩主题重叠 → 丢弃。
- **LlamaIndex #22209 Dakera memory**：integration proposal，无 merge → 信号不足。
- **vLLM #47288/#45877**：Elastic EP / DeepSeek V4 parser，与当前 focus 弱相关 → 丢弃。
- **openai/evals #1685**：空 issue spam → 丢弃。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-01** 原帖 → 不收录。
- **@gefei55 / @ianneo_ai 职业内容**：第三方聚合无法验证原帖 → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + arXiv 当日高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **434**（`0630.md` §7: 426 + 本日 §1: 8）
- Theory: **179**（`0630.md` §7: 177 + 本日 §2: 2）
- Career/Business: **81**（`0630.md` §7: 80 + 本日 §4: 1）
- LC: **63**（`0630.md` §7: 62 + 本日 §5: 1）
- IELTS: **63**（`0630.md` §7: 62 + 本日 §5: 1）
