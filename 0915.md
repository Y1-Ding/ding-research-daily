# Daily Research - 2026-09-15 (21:30)

## 0) 今日3条结论

- **vLLM Responses `/v1/responses/compact` 落地**（#56970, created **2026-09-15**）：长对话 agent 可在 serving 层做 **server-side compaction**（返回 `response.compaction` + `encrypted_content`）→ 对接你关注的 **token 优化 + compaction 可审计 trace**（对照 0913 Simon compaction 讨论），eval 须测 compact 前后 tool/code 指针是否保留。
- **多 MCP session 的 Responses 清理须「只注册一次 callback」**（#56988, created **2026-09-15**）：否则 duplicate cleanup / 对已关闭 session 再调用 → **skill/MCP agent harness** 的 lifecycle gold；须测 2+ MCP tools 并行调用后 session 各收到恰好 1 次 cleanup。
- **Triton Attention metadata 必须从本 KV group 的 `kv_cache_spec` 取 head 几何**（#56996, created **2026-09-15**）：混用 sliding/full attention（如 Gemma 4）时 model-global head 数会错选 2D/3D launch grid → **heterogeneous Attention serving eval** 的 geometry-gold。

## 1) Agent工程化（high-signal）

- [[Frontend] Add Responses API compaction endpoint（vLLM PR #56970, created **2026-09-15**）](https://github.com/vllm-project/vllm/pull/56970)
  - 摘要: 新增 `POST /v1/responses/compact`，对 Responses 形态的多轮 `input`（含已完成 assistant message）产出 `object: "response.compaction"` 与 `encrypted_content`（示例前缀 `vllm-compaction-v1:`）。作者在 GLM-5.3-Flash + Codex `wire_api: "responses"` 场景下验证 curl 流程。
  - 为什么重要(对我): **token 优化 + skill 落地**——serving 层 compaction 是 long-horizon agent 的 context 杠杆；须与「pre-compact trace 可检索」产品要求一起纳入 eval（衔接 0913 compaction checklist）。
  - 明日动作(<=20min): 写 **是/否**：你的 agent stack 是否计划调用 server-side compaction（是/否/未知）。

- [Propagate async model execution failures（vLLM PR #57009, created **2026-09-15**）](https://github.com/vllm-project/vllm/pull/57009)
  - 摘要: 修复 async 路径：`execute_model()` 异常被存入 execution future，但 `sample_tokens()` 返回 `EMPTY_MODEL_RUNNER_OUTPUT` 时 engine 未 `result()` execution future → 请求静默缺输出直至超时。Fix：在取 sampling 结果前观察 `exec_model_fut`（避免与 Ray 共享 future 双重 resolve）。
  - 为什么重要(对我): **评测 + serving**——agent eval 若依赖 vLLM V1 分 RPC，须将「forward 失败」与「空采样」区分，避免假超时污染 latency/cost 指标。
  - 明日动作(<=20min): 写 **是/否**：你的 eval 是否解析 vLLM missing-output / timeout 日志做根因分类（是/否/未知）。

- [[Bugfix][Responses] Clean up MCP tool sessions once before closing them（vLLM PR #56988, created **2026-09-15**）](https://github.com/vllm-project/vllm/pull/56988)
  - 摘要: 多 MCP session 时每个 session 各注册一次 cleanup callback，导致对同一 tool 重复 cleanup 或对已关闭 session 再调用。Fix：sessions 打开后 **只注册一次** callback，在关闭前统一 cleanup；覆盖 `ParsableContext` / `HarmonyContext`、未使用 tool、重复初始化、部分初始化失败等用例（59 passed）。
  - 为什么重要(对我): **skill 落地（MCP）+ 评测**——MCP agent 的 session 生命周期是 tool eval 的 teardown-gold。
  - 明日动作(<=20min): 写 **是/否**：你的 MCP agent eval 是否断言每个 called tool 恰好 1 次 cleanup（是/否/未知）。

- [Fix #20386: Deterministic tool I/O pre/post-processing for agents（LlamaIndex PR #22621, updated **2026-09-15**）](https://github.com/run-llama/llama_index/pull/22621)
  - 摘要: `FunctionTool` 增加 `pre_processor` / `post_processor`（sync+async 对），在 `__call__` 路径上对参数与 `ToolOutput` 做确定性变换；动机含 MCP 场景下规范化 tool I/O（`function_tool.py` +183/-231）。
  - 为什么重要(对我): **skill 落地 + 评测**——middleware 层可做 PII 脱敏、schema 规范化、eval 可复现的 tool I/O gold。
  - 明日动作(<=20min): 列 **1 个** 你现有 tool 适合 post_processor 的规则（≤15 词）。

- [fix(core): avoid duplicate first-hop triplets in property graph retrieval（LlamaIndex PR #22972, updated **2026-09-15**）](https://github.com/run-llama/llama_index/pull/22972)
  - 摘要: `SimplePropertyGraphStore.get_rel_map()` 将首跳 triplets 纳入 visited，避免下一步重复追加占满 `limit`、挤掉更深证据（例：`depth=3, limit=3` 链 `A→B→C→D` 现返回 3 条不同边而非重复 `A→B`）。
  - 为什么重要(对我): **RAG/agent 评测**——property graph retrieval 的 depth/limit gold；须测 limit 边界下证据深度不被首跳重复污染。
  - 明日动作(<=20min): 写 **是/否**：你的 graph RAG eval 是否含 `limit` 边界用例（是/否/未知）。

- [[Feature] Per-row candidate IDs for prefill token scoring（vLLM PR #56984, created **2026-09-15**）](https://github.com/vllm-project/vllm/pull/56984)
  - 摘要: M2 of #56860：`prompt_logprob_token_ids` 变为 `[num_rows, num_ids]` ndarray（`-1` padding），每行候选 ID 独立；OPD teacher 对学生 `R` token 的 top-k 可从 `[R, |union|]` 降为 `[R, k]`。相对 nested list，`SamplingParams.verify` 等路径显著加速（文中表：4000×1024 场景 verify 235ms→30ms）。
  - 为什么重要(对我): **评测 + token 成本**——on-policy distillation / logprob eval 的 wire format 与性能 gold。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `prompt_logprob_token_ids` 做 OPD/蒸馏 eval（是/否/未知）。

- [[Spec Decode][Model] Support DFlash2 draft models with GLM-5.3-Flash（vLLM PR #56983, created **2026-09-15**）](https://github.com/vllm-project/vllm/pull/56983)
  - 摘要: 使 `zai-org/GLM-5.3-Flash` + `incoai/GLM-5.3-Flash-DFlash2`（block-diffusion drafter）可在 NVIDIA 上启动 serving；修复 EAGLE3 接口、KV init 等串联 blocker；报告相对 MTP 的吞吐 sweep（不改 MTP 默认）。
  - 为什么重要(对我): **token 优化（spec decode）+ 评测**——agent 主力模型若走 GLM-5.3 + DFlash2，须单独 benchmark acceptance vs MTP。
  - 明日动作(<=20min): 写 **是/否**：你是否在 serving eval 矩阵中含 DFlash2 draft（是/否/未知）。

- [Rethinking skills and prompts for GPT-6 Astra（OpenAI Developers Blog, **2026-09-11**）](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)
  - 摘要: 随 GPT-6 Astra 能力上升，**skills、`AGENTS.md`、任务 prompt 宜减负**：避免 bloated context；模型更谨慎时需 `AGENTS.md` 明确「可安全推进」的工作流（如本地测试套件）。与 Agents API / Codex harness 同一产品代际。
  - 为什么重要(对我): **skill 落地 + token 优化**——强模型代际切换时须回归测试「删指令后 pass rate 是否升」而非堆 prompt。
  - 明日动作(<=20min): 从你 `AGENTS.md`/rules 删 **1 条** 可验证冗余指令，记录删前删后同一 L2 case 结果。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Bugfix][Attention] Source Triton metadata builder geometry from kv_cache_spec（vLLM PR #56996, created **2026-09-15**）](https://github.com/vllm-project/vllm/pull/56996)
  - 摘要: `TritonAttentionMetadataBuilder` 原从 model-global 取 `num_heads_kv`/`headdim`，与 per-group `kv_cache_spec` 不一致；`unified_attention` grid 用 tensor 真实 head 数，而 2D/3D 阈值用错误 head 数 → Gemma 4 等 heterogeneous head 模型性能/正确性风险。对齐 FlashAttention builder 从 spec 读取。
  - 为什么重要(对我): **Talk ≥3 turns + mixed Attention**——多 KV group 几何是 long-context agent serving 的 attention-backend gold。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 heterogeneous head + TRITON_ATTN（是/否/未知）。

- [[Bugfix][Spec Decode] Implement get_top_tokens() on the generic MTP drafter（vLLM PR #56995, created **2026-09-15**）](https://github.com/vllm-project/vllm/pull/56995)
  - 摘要: `use_local_argmax_reduction` 开启时 `DeepSeekMTP` 等 generic MTP drafter 缺 `get_top_tokens()` 导致引擎启动失败；补转发方法，与现有 `compute_logits` 对称。影响 `deepseek_v3`/`deepseek_v32`/`glm_moe_dsa` 等多条 MTP 路径。
  - 为什么重要(对我): **Talk ≥3 turns + draft logits/Attention**——TP 分片 argmax reduction 是 spec-decode eval 的启动-gold。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 `use_local_argmax_reduction` + DeepSeek MTP（是/否/未知）。

- [[Bugfix][GLM-5.3-Flash] Keep batch x heads out of gridDim.z in fused recurrent KDA/GDN kernels（vLLM PR #56974, created **2026-09-15**）](https://github.com/vllm-project/vllm/pull/56974)
  - 摘要: DP（TP=1/rank）下 `grid = (NK, NV, N*HV)` 在 1024 seq × 64 heads 时 `N*HV=65536` 超过 `gridDim.z` 上限 → CUDA graph capture 启动崩溃；将 `N*HV` 并入 `gridDim.x` 并重映射 `program_id`（同步 AMD/flash-linear-attention 副本）。
  - 为什么重要(对我): **Talk ≥3 turns + linear/recurrent Attention**——KDA/GDN 与标准 MHA 不同的 launch 约束是 hybrid model eval 的 DP-gold。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 GLM-5.3 + DP>1（是/否/未知）。

- [[Bugfix][KV Cache] Prevent scheduler wedge from Mamba admission overcount（vLLM PR #57000, created **2026-09-15**）](https://github.com/vllm-project/vllm/pull/57000)
  - 摘要: hybrid Mamba + `mamba_cache_mode=align` + 外部 KV connector：远程 KV 加载完成后重入 admission 时 `blocks_allocated=True` 跳过物理块计算，`num_new_blocks` 按逻辑块膨胀（例：759k token 请求误报需 495×3 Mamba blocks，实际约 9）→ 调度器永久 deferred、吞吐归零。
  - 为什么重要(对我): **Talk ≥3 turns + KV/Mamba state**——长上下文 agent + Mooncake 类 offload 的 scheduler-gold。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 Mamba align + external KV connector（是/否/未知）。

- [[Perf][DSv4.1] Reuse prefill main-KV gathers across decoder groups（vLLM PR #56986, created **2026-09-15**）](https://github.com/vllm-project/vllm/pull/56986)
  - 摘要: DeepSeek-V4.1 四层 CSA2 group 共享 main-KV 源却每层重复 dequant/gather；opt-in BF16 buffer（0–512 MiB 预算）首层 gather、后续层复用同一 region（各层仍刷新 SWA）。门控：单请求 FlashMLA prefill、无 DBO/PCP/DCP/SP 等。
  - 为什么重要(对我): **Talk ≥3 turns + KV/Attention prefill**——跨层 KV 复用是超长 prompt agent 的 prefill-cost gold。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 DeepSeek-V4.1 级长 prefill agent（是/否/未知）。

## 3) 热门项目/可试个人项目

- [langchain-ai/langchain-skills — Agent skills for LangChain/LangGraph/Deep Agents（~1224 stars, API updated **2026-09-15**）](https://github.com/langchain-ai/langchain-skills)
  - 摘要: MIT；21 个 skills（eval-engineering、langgraph-fundamentals 等）；`npx skills add langchain-ai/langchain-skills --skill <name>`；与 OpenAI 9/11「减负 skills」形成对照——可抽样对比你 Cursor rules 体积。
  - 为什么重要(对我): **skill 落地**——Harbor/eval-engineering 路径可映射你 map_prompt_test 流水线。
  - 明日动作(<=20min): 对比 `eval-engineering/SKILL.md` 与你 playbook 的 **1 个** 字段差异（≤20 词）。
  - 估计复现成本: **低**（只读 + 本地 dry-run，~30min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Introducing the Agents API（OpenAI Blog, **2026-09-10**）](https://openai.com/index/introducing-the-agents-api/)
  - 机会信号: **Codex harness 托管为 Agents API**（任务/模型/工具/环境一次调用）；OpenAI 托管 sandbox 或 BYO/VPC → **agent harness 即产品** 验证「个人公司」可卖 vertical workflow + eval，而非裸 API。
  - 风险信号: token+工具计费仍绑定 OpenAI；harness 开源但托管层锁仓；与 0604 LangChain「model neutrality」叙事共振——须保留可迁移 eval/task 定义。
  - 我可执行的一步(<=30min): 写 **3 条** 你现有 agent eval 中可迁到「harness 无关」的 artifact（task.toml / verifier / gold trace 各举 1）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[Maximum Number of Non-overlapping Palindrome Substrings](https://leetcode.com/problems/maximum-number-of-non-overlapping-palindrome-substrings/)（**2026-09-15 Daily Challenge**，LeetCode GraphQL `activeDailyCodingChallengeQuestion`，Hard）：区间 DP + 回文预处理；与 agent eval「非重叠子轨迹/子 span 最大化覆盖」同构。明日动作：完成 Hard 一题或读题写状态转移草图；**≤15 词**「非重叠 palindrome 子串」对你 trace 切片意味着什么。

- **IELTS（≤1 条）**：[IELTS Listening practice tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/listening)：0914 为 Reading，今日 **Listening**（选 1 套，~30min）。明日动作：完成 1 套 Listening；记录 **≥2 个**失误类型（拼写/数字/同义替换）；**≤15 词**你最弱的 Listening 题型。

## 6) 噪音过滤记录

- **test(mcp): allow Windows local path error（LangChain #40454）**：Windows CI 测试放宽 → 过滤。
- **docs/chore（#40453/#40452/#23066 Pushary 文档链）**：文档/社区链接 → 弱于当日 MCP/compaction/Attention 主题 → 过滤。
- **ROCm/XPU/CI/模型支持杂项（#57003–#57007 等）**：非当前 focus → 过滤。
- **OpenAI Data agent / put-data-to-work（2026-09-10）**：偏 ChatGPT Work 分析产品 → 弱于 Agents API harness 信号 → 过滤。
- **0909–0914 已收录项**：LangGraph #8868/#8916/#8918、LlamaIndex #23050/#23011、vLLM #56831/#56825/#56822、Simon Sep-12 compaction、eval-engineering 博文等 → semantic duplicate，不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-15** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub + OpenAI Developers ≥8 条主信号，无需 skill 补充。

## 7) 本周累计计数

- Agent: **985**（`0914.md` §7: 977 + 本日 §1: 8）
- Theory: **453**（`0914.md` §7: 448 + 本日 §2: 5）
- Career/Business: **147**（`0914.md` §7: 146 + 本日 §4: 1）
- LC: **128**（`0914.md` §7: 127 + 本日 §5: 1）
- IELTS: **128**（`0914.md` §7: 127 + 本日 §5: 1）
