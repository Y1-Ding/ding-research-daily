# Daily Research - 2026-06-01 (21:30)

> 采集口径：优先 **X_WHITELIST / YOUTUBE_WHITELIST / GITHUB_WHITELIST / BLOG_WHITELIST / CAREER_BUSINESS_WHITELIST**；评分 `relevance(40)+actionability(25)+credibility(20)+freshness(15)`，正文主段 **≥70**（职业池 **≥65** 且具可执行信号）。与 `0531.md` 及更早文件做 **URL 去重** + **语义去重**。本日 **`[OUTSIDE_WHITELIST]` 0 条** → 主链 **0%（≤20%）**。  
> **自动化提示（给编排器）**：Cron 触发时区若仍为 UTC，请将等价目标对齐 **Asia/Shanghai 每日 21:30**；失败 **重试 1 次**；连续 **2** 个日历日失败再发告警（本仓库文件不承载告警状态机）。

## 0) 今日3条结论

- **vLLM 多层 KV offload 把「调度步结束」从事件轮询里拆出来**：**2026-06-01** 打开的 [#44206](https://github.com/vllm-project/vllm/pull/44206) 为 `OffloadingManager` / `SecondaryTierManager` 增加 **`on_schedule_end()`**，在 `OffloadingConnectorScheduler.build_connector_meta()` 每步末尾统一处理 finished jobs、批量 promotion 与 per-step gate 重置——长会话 agent 的 **KV 层级状态机** 更可推理。
- **浏览器内 ASGI agent 栈可行**：Simon Willison（**2026-05-30**）用 **Claude Opus 4.8 + Claude Code for web** 在 **Pyodide + Service Worker** 下跑通 **FastAPI** 与 **Datasette 1.0a31** 演示，修复旧 Web Worker 方案无法执行 `<script>` 的硬伤——**零后端** 的 RAG/数据 agent 原型成本骤降。
- **「agent 生产力」正在分化为纪律问题，而非工具问题**：**2026-05-30/31** 连续两篇 Simon 链式讨论（Chad Whitacre 退网叙事、David Wilson「取消订阅」）指向同一张力：**低摩擦生成** 带来 **项目堆积/注意力耗散**；对你意味着 agent 工程要显式设计 **WIP 上限与可维护性门禁**，而不只追 token 吞吐。

## 1) Agent工程化（high-signal）

- [[KV Offload] Add `on_schedule_end()` hook（`vllm` PR #44206，**Opened 2026-06-01**）](https://github.com/vllm-project/vllm/pull/44206)
  - 摘要: 在 **KV connector / 多层 offload** 路径新增 **`on_schedule_end()`** 生命周期钩子，由 `OffloadingConnectorScheduler.build_connector_meta()` 在 **每个 scheduler step 结束** 调用；将 **finished jobs 处理、batched promotions 刷新、per-step gate 重置** 与 **`take_events()` 的纯事件观测** 分离。标签 **`kv-connector` / `v1`**，与 `0530.md`–`0531.md` 的 **TieringOffloadingSpec / objectstore 二级层** 叙事衔接。
  - 为什么重要(对我): 托管/自托管 **长程 agent** 若用 vLLM KV offload，**步边界语义** 直接影响 **checkpoint 恢复与 tail latency**；此 PR 让「何时提交 offload 副作用」有 **单一调用点**，便于写 **集成测试与可观测 span**。
  - 明日动作(<=20min): 若你用 vLLM KV connector，画 **1 条** 时序：`schedule step` → `on_schedule_end` → `take_events`（各 ≤8 词）；标你方是否 **缺 step 级 hook**（是/否/未知）。

- [fix(llms-openai): exclude sampling params from OpenAIResponses when reasoning active（`llama_index` PR #21843，**Opened 2026-06-01**）](https://github.com/run-llama/llama_index/pull/21843)
  - 摘要: **OpenAI Responses + reasoning** 时，`top_p` / `temperature` / `presence_penalty` / `frequency_penalty` 会触发 **400**；旧逻辑在合并 `additional_kwargs` 与 runtime `kwargs` **之前** pop 采样参数，导致用户从侧路传入的参数 **回流**。修复将 pop 移到 **两次 merge 之后**，并用模块级常量集中管理；**reasoning 关闭时 payload 不变**。含 **class / additional_kwargs / runtime kwargs** 三路径单测，Closes #20459。
  - 为什么重要(对我): **RAG / 结构化 agent** 若走 **Responses + thinking/reasoning**，参数卫生是 **稳定性** 问题而非调参问题；与 `0531.md` 的 **thinking_delta 通道** 并读，可统一 **「推理态 API 契约」** checklist。
  - 明日动作(<=20min): 在你方 **1 条** OpenAI Responses 调用链，列出 **是否在 reasoning 开启时仍传 temperature/top_p**（有/无）；若有，记文件路径待升级 llama-index。

- [Research: Running Python ASGI apps in the browser via Pyodide + a service worker（Simon Willison Blog，**2026-05-30**）](https://simonwillison.net/2026/May/30/pyodide-asgi-browser/)
  - 摘要: **Datasette Lite** 原用 Web Worker 拦截导航并跑 Python，但 **`<script>` 不执行** 破坏插件生态。作者让 **Claude Opus 4.8（Claude Code for web）** 探索 **Service Worker + Pyodide ASGI**：同源 **`/app/`** 请求由 SW 交给 Python ASGI 栈处理，**静态资源外无需后端**；文内称已跑通 **FastAPI 演示** 与 **Datasette 1.0a31** 演示（链接见正文）。
  - 为什么重要(对我): **边缘/离线数据 agent**（SQLite + 工具）可 **整包 WASM 部署**；对你方 **demo / 内网无外网** 场景，是 **LangGraph 本地图 + 浏览器 UI** 的低成本组合路径。
  - 明日动作(<=20min): 打开文内 **任一 demo**，用中文写 **≤25 词**：标 **SW 拦截路径前缀** 与你方若复现时的 **静态/动态分界**（各 ≤10 词）。

- [Release: datasette 1.0a32（Simon Willison Blog，**2026-05-31**）](https://simonwillison.net/2026/May/31/datasette/)
  - 摘要: **patch** 修复 `/db/-/execute-write` 上 **`INSERT ... RETURNING`** 与多处 **`base_url`** 问题；作者注明这些问题在 **2026-05-30 Service Worker 实验** 中暴露——与上一篇 **Pyodide ASGI** 工作 **同日耦合**。
  - 为什么重要(对我): 若你把 **Datasette / SQLite agent** 作为 **轻量知识库 UI**，该 release 是 **浏览器写路径** 的 **回归信号**；宜在升级 **Datasette Lite** 前锁定 **写接口 smoke test**。
  - 明日动作(<=20min): 若你用 Datasette，列 **是否依赖 execute-write 或 RETURNING**（是/否）；若是，记下 **当前版本 <1.0a32?**（是/否）。

- [release(sdk-py): 0.4.1 + release(langgraph): 1.2.3（`langgraph` PR #7944/#7945，**仍 Open，Updated 2026-05-29**）](https://github.com/langchain-ai/langgraph/pull/7945)
  - 摘要: **#7944** 计划发 `langgraph-sdk` **0.4.1**；**#7945** 发 `langgraph` **1.2.3** 并将 sdk 约束升为 **`>=0.4.1,<0.5.0`**（**RemoteGraph v3** 硬依赖）。**2026-06-01** 复查：**仍未合并**——升级窗口继续，但 **无新版本号落地**。
  - 为什么重要(对我): 与 `0531.md` 相同结论仍成立：在合并前完成 **RemoteGraph v3 / stream_events(v3)** 的 **依赖矩阵** 与 **回滚方案**。
  - 明日动作(<=20min): `pip show langgraph langgraph-sdk` 记下版本；若生产用 RemoteGraph，写 **阻塞升级的唯一调用点**（≤15 词）。

## 2) 原理深化（Attention/Transformer/Agent）

- [ACE PR #42645 — 维护者要求先写 RFC（`vllm`，**Comment 2026-05-31**）](https://github.com/vllm-project/vllm/pull/42645)
  - 摘要: vLLM 维护者 **@AndreasKaratzas**（**2026-05-31 22:21 UTC**）在 [#42645](https://github.com/vllm-project/vllm/pull/42645) 留言：**应先提交 RFC** 再合并；并强调 RFC 应由人撰写、**不要用 AI 代写**（「bad RFC with human word flow > AI RFC」）。同日作者刚合入 **Phase 3 `model_runner` attention hook**（`0531.md` 已述）；维护者意见把讨论从 **实现细节** 拉回 **Attention 加权压缩的公开设计契约**。
  - 为什么重要(对我): **三回合追问链**：(1) 你的 context 压缩是 **产品特性** 还是 **推理引擎特性**？(2) **Mode 3 attention** 在 **FA2/FlashInfer** 无 softmax 时回退 BM25——eval 是否 **按后端分层报告**？(3) 若跟进 ACE，你是否愿意先写 **≤2 页 RFC**（含 **与 KV 级 H2O/Scissorhands 边界**）？
  - 明日动作(<=20min): 用中文写 **≤40 词**「若在你方 fork vLLM 做 tool 行级压缩，最小 RFC 目录」3 条（问题/非目标/评测各 ≤12 词）。

- [fix(llms-openai): reasoning 态下采样参数语义（`llama_index` PR #21843，**Opened 2026-06-01**）](https://github.com/run-llama/llama_index/pull/21843)
  - 摘要: **Reasoning 模型** 把 **采样超参** 与 **推理链生成** 视为 **互斥 API 契约**；合并顺序错误会让 **kwargs 侧路** 破坏该契约。虽为集成层修复，但触及 **「推理态 vs 生成态」** 在 Transformer 服务里的 **接口不变量**。
  - 为什么重要(对我): 与 **thinking_delta / answer delta 分流**（`0531.md` [#21812](https://github.com/run-llama/llama_index/pull/21812)）同一主题：**多通道输出** 需要 **多通道参数集**。
  - 明日动作(<=20min): 为你 **1 个** 模型写表：**reasoning on** 时允许参数集合 vs **off** 时集合（各 ≤3 个参数名）。

## 3) 热门项目/可试个人项目

- [Pyodide ASGI + Service Worker 演示（Simon Willison Blog，**2026-05-30**）](https://simonwillison.net/2026/May/30/pyodide-asgi-browser/)
  - 摘要: 文内提供 **浏览器内 FastAPI** 与 **Datasette 1.0a31** 演示链接；机制为 SW 拦截 **`/app/`** 下请求并转 ASGI。**Claude Code for web** 用于探索实现——属 **个人可 fork 的 agent UI 壳**。
  - 为什么重要(对我): **估计复现成本 低–中**：需熟悉 **Pyodide 打包** 与 SW 生命周期；无需 GPU。
  - 明日动作(<=20min): clone/fork 思路即可——用中文写 **≤20 词**：你若做 demo，第一个 endpoint 测什么（health? `/app/` 200?）。
  - 估计复现成本: **中**（前端 SW + Pyodide 工具链；逻辑已由作者验证）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [I Am Retiring from Tech to Live Offline（Simon Willison Blog → Chad Whitacre，**2026-05-30**）](https://simonwillison.net/2026/May/30/retiring-from-tech-to-live-offline/)
  - 机会信号: 资深 **开源/基础设施** 从业者（Chad Whitacre，**Open Source Endowment** 等）在 **2026-05** 选择 **「Neo-Amish / AI Amish」**——保留有限现代便利但 **主动退出互联网与 agentic 加速**；其 **2026-02** 文 [Spitting Out the Agentic Kool-Aid](https://openpath.quest/2026/spitting-out-the-agentic-kool-aid/) 记录 **Claude Code 连续 12h+ 会话** 后的 **认知/家庭成本**。对 **AI 个人公司** 启示：部分高信任 **维护者/顾问** 可能 **永久离开公共讨论**，**知识传递要趁窗口**。
  - 风险信号: 若你的商业模式假设 **开源领袖持续公开布道**，此类退出会 **抬高获客与信任成本**；同时反映 **burnout** 在核心开发者中 **非边缘现象**。
  - 我可执行的一步(<=30min): 列出你依赖的 **3 位** 开源/AI 意见领袖；各写 **若 6 个月内沉默，你的备选信息源 1 个**（博客/RSS/邮件列表/GitHub releases 四选一）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：LeetCode Explore [`Linked List`](https://leetcode.com/explore/learn/card/linked-list/) 中 **Reverse Linked List**：把「**迭代三指针（prev/curr/next）**」映射为 agent **「工具调用链上就地改写状态而不丢尾指针」**。明日动作：用中文写 **≤30 词**：为你 **1 个** LangGraph 状态字段标 **prev/curr/next 各对应什么**（各 ≤8 词）。

- **IELTS（≤1 条）**：浏览 [IELTS 官方 — Speaking test format](https://ielts.org/take-a-test/test-format/speaking) 中 **Part 2（cue card 1 分钟准备 + 2 分钟陈述）**。明日动作：用中文写 **≤35 词**：把 **Part 2 的「提示卡结构」** 与你 **英文技术方案 2 分钟 elevator pitch** 的 **1 个** 共用提纲（自定）。

## 6) 噪音过滤记录

- **`https://github.com/vllm-project/vllm/pull/42645`（Phase 3 wiring 正文）**：`0531.md` §1/§2 **语义去重**；本日仅保留 **2026-05-31 RFC 门禁评论** 新信号。
- **`https://github.com/vllm-project/vllm/pull/41968`**、**`https://github.com/run-llama/llama_index/pull/21754`**、**`#21812`**、**`#21820`**：与 `0531.md` §1 **URL 去重**。
- **`https://simonwillison.net/2026/May/31/`（run-rate revenue 摘录）**：`0531.md` §4 **URL/语义去重**。
- **`https://www.anthropic.com/engineering/how-we-contain-claude`**：与 `0527.md`–`0531.md` **去重**（Simon 5/30 转述不重复收录）。
- **`https://openai.com/index/trustworthy-third-party-evaluations-foundations/`**、**`https://openai.com/index/building-self-improving-tax-agents-with-codex/`**、**`https://openai.com/index/strengthening-societal-resilience-with-rosalind-biodefense/`**：与 `0528.md`–`0530.md` **去重**。
- **David Wilson 原文 URL**：Simon 链式帖 [the-solution-might-be-cancelling-my-ai-subscription](https://simonwillison.net/2026/May/31/the-solution-might-be-cancelling-my-ai-subscription/) 为 **`(via)` 外链**，本环境 **未稳定解析到 Wilson 一手域名** → 不单独收录，结论已并入 §0（来源仍为 **Simon Willison Blog**）。
- **X_WHITELIST / YOUTUBE_WHITELIST / Bilibili 当日帖文**：无法稳定抓取原帖 URL/正文 → 不收录。
- **`https://github.com/openai/evals/commits/main`**：最近实质 commit **2026-04-14 量级** → 丢弃。
- **`https://github.com/microsoft/autogen`**：`merged:>2026-05-31` **0 条** → 无高信号。

## 7) 本周累计计数

- Agent: **208**（`0531.md` §7: 203 + 本日 §1: 5）
- Theory: **81**（`0531.md` §7: 79 + 本日 §2: 2）
- Career/Business: **50**（`0531.md` §7: 49 + 本日 §4: 1）
- LC: **34**（`0531.md` §7: 33 + 本日 §5: 1）
- IELTS: **34**（`0531.md` §7: 33 + 本日 §5: 1）
