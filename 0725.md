# Daily Research - 2026-07-25 (21:30)

## 0) 今日3条结论

- **vLLM 新增 `/v1/responses/input_tokens` 预检端点**（vLLM #49794, **2026-07-25**）：复用与生成请求相同的预处理管线（chat template / tools / history），零推理开销返回 token 计数 → agent gateway 可在 dispatch 前做成本/长度门禁，避免「发出去才发现超预算」。
- **GLM-5.2 owner-sharded PCP history 可达 4× KV 容量**（vLLM #49741, **2026-07-25**）：历史 cache page 单 owner + CUDA VMM peer view，避免每层全量历史 KV 复制 → 长上下文 sparse attention 的容量瓶颈从「每 rank 全量 replica」转向「逻辑页分片 + peer 寻址」。
- **AutoGen 取消 in-flight tool call 会永久挂死 stream**（AutoGen #7991 / #7956, **2026-07-25**）：`asyncio.gather` 遇 `CancelledError` 时未执行 `stream_queue.put_nowait(None)` → `on_messages_stream` 永不结束；fix 用 `finally` 保证 sentinel 发出 → skill eval harness 的 cancel/timeout 路径必须测「中断后 stream 是否终止」。

## 1) Agent工程化（high-signal）

- [[Feature] Add `/v1/responses/input_tokens` endpoint for preflight token counting（vLLM PR #49794, updated **2026-07-25**）](https://github.com/vllm-project/vllm/pull/49794)
  - 摘要: 新增 OpenAI 兼容 `POST /v1/responses/input_tokens`；`ResponsesCountTokensRequest` 继承 `ResponsesRequest`，接受与生成相同的 payload（含 `temperature`/`stream`/`max_output_tokens` 等字段无需剥离）；复用 `_make_request` / `_make_request_with_harmony` 预处理，支持 chat template、tool definitions、`previous_response_id` 历史。Fixes #49533。
  - 为什么重要(对我): **token 优化 + 评测**——agent orchestrator 在 dispatch 前可精确计数（含 tools schema 膨胀），把「超 max_tokens 才失败」前移到 preflight 拒载；eval batch 可按预估 token 分 shard。
  - 明日动作(<=20min): 写 **是/否**：你 agent gateway 是否在发请求前做 token 预检（是/否/未知）。

- [fix/issue 7956 assistant cancellation hang（AutoGen PR #7991, updated **2026-07-25**）](https://github.com/microsoft/autogen/pull/7991)
  - 摘要: `CancellationToken.cancel()` 在 tool call in-flight 时，`StaticWorkbench.call_tool` 的 `except Exception` 捕不到 `CancelledError`（Python 3.8+ 继承 `BaseException`）；更严重的是 `_execute_tool_calls` 中 `asyncio.gather` 被取消后跳过 `stream_queue.put_nowait(None)` → `AssistantAgent.on_messages_stream` 永久挂起。Fix：`finally` 块保证 sentinel 发出。Fixes #7956。
  - 为什么重要(对我): **skill 落地 + 评测**——eval harness 的 timeout/cancel 是标配；stream 不终止 = worker 泄漏 + 假「仍在运行」指标。
  - 明日动作(<=20min): 写 **是/否**：你 streaming eval 是否在 timeout 后验证 stream 已关闭（是/否/未知）。

- [fix(sdk-py): unblock active subscriptions on AsyncThreadStream.close()（LangGraph PR #8437, updated **2026-07-25**）](https://github.com/langchain-ai/langgraph/pull/8437)
  - 摘要: `AsyncThreadStream.close()` 直接 cancel fanout task，未先向 active subscription 推送 terminal sentinel → consumer 在空队列上 suspend 永不返回（`TimeoutError`）。Fix：close 前调用 `_signal_paused()`，对齐 `SyncThreadStream.close()` 先关 controller 的行为。Fixes #8429；含 `test_close_unblocks_active_subscription_iterator` 回归测试。
  - 为什么重要(对我): **skill 落地 + 评测**——LangGraph SDK stream replay / monitor 路径若 close 不 unblock iterator，eval worker 池会积累僵尸 consumer。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `AsyncThreadStream` 做 background run 订阅（是/否/未知）。

- [fix(langgraph): honor per-node cache_policy=None with set_node_defaults（LangGraph PR #7996, updated **2026-07-25**）](https://github.com/langchain-ai/langgraph/pull/7996)
  - 摘要: `StateGraph.set_node_defaults(cache_policy=...)` 后，节点 `add_node(cache_policy=None)` 无法显式禁用缓存——`None` 被当作「继承默认」而非「opt-out」。Fix：用 `MISSING` sentinel + `cache_policy_opt_out` flag 区分 omitted vs explicit `None`；补 default/override/opt-out 三类回归测试。
  - 为什么重要(对我): **skill 落地**——子 agent / 敏感 tool 节点需显式跳过 cache；opt-out 失效 = eval 看到缓存命中、prod 看到重算，A/B 不可比。
  - 明日动作(<=20min): 写 **是/否**：你 graph 是否有节点需要显式 `cache_policy=None`（是/否/未知）。

- [[Bugfix] Count reasoning tokens when the start token is absent（vLLM PR #49743, updated **2026-07-25**）](https://github.com/vllm-project/vllm/pull/49743)
  - 摘要: `BaseThinkingReasoningParser.count_reasoning_tokens()` 仅靠 start token（如 ``）递增 depth；DeepSeek-R1 等模型把 start token 注入 prompt，输出直接从 reasoning content 开始 → depth 恒为 0，`reasoning_tokens` usage 字段报告 0（但 `extract_reasoning` 正确）。Fix：输出开头隐式打开 reasoning span。
  - 为什么重要(对我): **评测 + token 优化**——reasoning model eval 若 usage 中 reasoning_tokens=0，cost attribution 和 token budget 门禁全失真。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否记录 `reasoning_tokens` usage 字段（是/否/未知）。

- [[Bugfix] Reject unsupported forced tool choice in Responses API（vLLM PR #49785, updated **2026-07-25**）](https://github.com/vllm-project/vllm/pull/49785)
  - 摘要: 非 Harmony Responses API 路径上，parser 不支持 `required`/named tool choice 时原先静默降级为 `auto` → agent 以为强制调 tool 实际未强制。Fix：parser 声明不支持时显式 reject；补 supported/unsupported parser 组合单测。Fixes #49712。
  - 为什么重要(对我): **评测**——tool-calling eval 若 tool_choice 被静默忽略，pass rate 与真实行为脱节。
  - 明日动作(<=20min): 写 **是/否**：你 eval 是否测 `tool_choice=required` 路径（是/否/未知）。

- [feat(parser): add xLAM tool parser to Rust frontend（vLLM PR #49799, updated **2026-07-25**）](https://github.com/vllm-project/vllm/pull/49799)
  - 摘要: `vllm-parser` Rust 前端实现 `XlamToolParser`，与 Python `xlam_tool_parser.py` 功能对齐：streaming `winnow` 状态机处理 bare JSON array + `[TOOL_CALLS]`/````json`/`<tool_call>` 前缀；保留 `` 为可见文本；19 个单测覆盖 multi-byte chunk boundary、dangling sentinel、CRLF code fence。Fixes #44280。
  - 为什么重要(对我): **skill 落地**——tool parser 在 Rust 前端 = serving 层更低延迟 streaming tool-call 解析；eval 需对齐 Python/Rust 双端输出。
  - 明日动作(<=20min): 写 **是/否**：你 serving 是否用 xLAM 系模型的 tool parser（是/否/未知）。

- [feat(evals): add timeout and retry controls（deepagents PR #4893, merged **2026-07-24**）](https://github.com/langchain-ai/deepagents/pull/4893)
  - 摘要: unified eval workflow 新增 `agent_timeout_multiplier` + `n_retries` dispatch 输入（prep 阶段校验）；Harbor 侧新增 `--retry-if-reward-below <float>`——仅当 primary reward 低于阈值（或缺失）时重试 errored trial；`AgentTimeoutError` 等默认 non-retryable 异常保持不重试。
  - 为什么重要(对我): **评测杠杆**——reward-gated retry = 避免 timeout 后盲目重跑烧钱；可直接映射到你的 collect→eval shard 重试策略。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval retry 是否按 reward 阈值门控（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Feature][GLM-5.2] Add owner-sharded PCP history for up to 4x KV capacity（vLLM PR #49741, updated **2026-07-25**）](https://github.com/vllm-project/vllm/pull/49741)
  - 摘要: PCP 要求每 rank attend 完整历史 Main KV + Indexer K，全量 replica 限制上下文长度。本 PR 为 GLM-5.2 sparse attention 增加 opt-in owner-sharded history：`owner_rank = p % pcp_size`，CUDA VMM peer view 暴露稳定 rank-major 视图；fuse owner 选择 + FP8 quant + persistent cache store + release/acquire fence；纯 prefill 用 bounded transient local working set。`VLLM_USE_PCP_OWNER_HISTORY=1` 启用。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：长上下文 agent 的 KV 容量核心在「历史页如何分片与寻址」；owner-shard 把 replica 成本从 O(pcp_size × history) 降到 O(history/pcp_size) per rank。
  - 明日动作(<=20min): 写 **≤15 词**：你长上下文 eval 是否测 KV 容量上限（是/否/未知）。

- [[Core] Exchange only sampled final rows after PCP prefill（vLLM PR #49756, updated **2026-07-25**）](https://github.com/vllm-project/vllm/pull/49756)
  - 摘要: PCP 在 final layer 后原先重建全部 scheduled rows，但 sampling 只需每 request 一个 final row。Fix：pure decode 无通信（已 replicated）；prefill/mixed batch 只 pack/exchange compact final-row buffer；prompt-logprob step 保留 dense restore。自动选 NVLink multicast symmetric memory，不支持时 fallback PCP collective。无 feature flag。
  - 为什么重要(对我): **Attention + token 优化**：PCP prefill 后 hidden-state 交换是 attention 输出到 logits 的瓶颈；compact final-row = 理解「采样只需末行」对通信量的影响。
  - 明日动作(<=20min): 写 **≤15 词**：你 PCP serving 是否 profiling 过 final-layer 通信（是/否/未知）。

- [[Kernel][SM100] Add a CuTeDSL fused query kernel（vLLM PR #49792, updated **2026-07-25**）](https://github.com/vllm-project/vllm/pull/49792)
  - 摘要: 为 sparse attention 的 fused query preprocessing 增加 SM100 CuTeDSL 实现；Triton 仍为 fallback，仅 supported dtype/shape 组合走 CuTeDSL。从 revert 的 #48597 中拆出的 focused PR；sparse physical-index storage 由 #49678 单独处理。Draft，待 SM100 compile + numerical parity 验证。
  - 为什么重要(对我): **Attention 机制**：sparse attention 的 query preprocessing 是 prefill 热路径；CuTeDSL vs Triton fallback 选型 = 理解 SM100 上 attention 前处理的 kernel 编译与 specialization 成本。
  - 明日动作(<=20min): 写 **≤15 词**：你关注 sparse attention query kernel 路径吗（是/否/未知）。

- [[Spec Decode][Perf] Optimize MTP draft decoding（vLLM PR #49793, updated **2026-07-25**）](https://github.com/vllm-project/vllm/pull/49793)
  - 摘要: MTP speculative-decoding 优化：跨 Model Runner V2 draft steps 共享 TopK state；greedy draft token 走 model local-argmax；末层 tensor-parallel all-reduce 与 RMSNorm fuse；FlashInfer workspace 不支持时 safe unfused fallback。从 revert 的 #48597 拆出。Draft，待 multi-rank SM100 验证。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：spec-decode 降 decode latency = agent multi-turn 响应速度核心；MTP TopK 共享 + fused norm/AR = 理解 draft path 的 attention 后处理瓶颈。
  - 明日动作(<=20min): 写 **≤15 词**：你 multi-turn eval 是否测 spec-decode acceptance rate（是/否/未知）。

- [fix(mooncake): reject duplicate transfer_id to prevent KV-cache leak（vLLM PR #49796, updated **2026-07-25**）](https://github.com/vllm-project/vllm/pull/49796)
  - 摘要: 客户端复用 `transfer_id` 跨多个 `do_remote_decode` 请求 → producer `reqs_need_send` 元数据被覆盖，被覆盖请求的 GPU KV blocks 永久泄漏直至进程重启。Fix 双层防御：(1) scheduler 拒绝已 active 的 transfer_id；(2) worker 检测 overwrite 并释放旧 blocks。含 duplicate/aborted/reused ID 回归测试。
  - 为什么重要(对我): **Attention/KV cache**：P/D disaggregation 下 KV transfer 是长上下文 agent 的基础设施；transfer_id 碰撞 = silent GPU memory 泄漏，eval 长跑后 OOM 难复现。
  - 明日动作(<=20min): 写 **≤15 词**：你 disaggregated serving 是否监控 KV block 泄漏（是/否/未知）。

## 3) 热门项目/可试个人项目

- [langchain-ai/deepagents — Harbor `--retry-if-reward-below` reward-gated retry（PR #4893, merged **2026-07-24**）](https://github.com/langchain-ai/deepagents/pull/4893)
  - 摘要: unified eval 集成 Harbor 新 flag `--retry-if-reward-below`：errored trial 仅当 primary reward 低于阈值（或缺失）才重试；与 exception include/exclude 策略叠加；`AgentTimeoutError` 保持 non-retryable。含 `agent_timeout_multiplier` / `n_retries` prep 校验。
  - 为什么重要(对我): **评测杠杆**——比盲目 `n_retries=3` 更省钱；可直接写 10 行 wrapper 映射你的 L2 eval 门禁。
  - 明日动作(<=20min): 读 PR body 中 Harbor `RetryConfig` 字段；写 1 条你可用的 retry 条件（≤10 词）。
  - 估计复现成本: **低**（读 schema + 配 1 个 threshold，≤30min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [I stopped running benchmarks on my agents. Here's what replaced them in a 5-agent shop.（Indie Hackers, Rapid Claw）](https://www.indiehackers.com/post/i-stopped-running-benchmarks-on-my-agents-heres-what-replaced-them-in-a-5-agent-shop-07f18272bf)
  - 机会信号: 5-agent 生产小团队用 **trace observability 替代 leaderboard benchmark**：loop rate 9%→3%（强制「done looks like X」验收契约）；cost per task ↓40%（cap tool-call depth + 杀 unbounded reflect）；silent stall 从每周数次→每月约 1 次（heartbeat + no-progress alert）。公开 benchmark 可被 hack，生产信号是 loop rate / cost-per-task / stall rate。
  - 风险信号: 低 4 位数 MRR、<25 付费客户，规模极小；季度 benchmark 仍作 sniff test；metrics 无可复现开源 harness；国内合规/支付路径不同。
  - 我可执行的一步(<=30min): 写 **≤8 行**：你当前 1 个 agent 工作流，哪 3 个 observability 指标（loop rate / cost-per-task / stall）最值得先接。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 3515 Shortest Path in a Weighted Tree](https://leetcode.com/problems/shortest-path-in-a-weighted-tree/)（**Day 25, Jul 2026, Hard**；承接 0724 的 3514 II）：带权树动态边权更新 + 根到节点最短路径查询。明日动作：Euler tour + 线段树模板写 1 次；写 **≤15 词**：lazy propagation 与 agent 状态增量更新的类比。

- **IELTS（≤1 条）**：[IELTS Ready — British Council 官方免费备考（含 AI Mini Mock）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-ready)：50 分钟 Mini Mock + AI 估分。明日动作：完成 **Listening 1 节 Mini Mock**；写 **≤15 词** 错因类型（拼写/连读/同义替换 选 1）。

## 6) 噪音过滤记录

- **LangChain #38863 AddableDict silent merge**：`0716.md` 已收录 → 07-25 仅 rebase，不重复。
- **LlamaIndex #22419 SlidingWindowRateLimiter**：`0722–0723.md` 已收录 → 不重复。
- **vLLM #49719 ZoomKV、#49704 KVBlockZeroer、#49667 mm IPC**：`0724.md` 已收录 → 不重复。
- **LangGraph #8428 Runtime.merge falsy、LangChain #39049 model_kwargs**：`0724.md` 已收录 → 不重复。
- **Anthropic Claude Opus 5（Jul 24 blog）**：高价值但非 07-25 新鲜信号 + 偏模型发布 → 待观察。
- **OpenAI Presence / HF cyberattack / Simon Willison Jul 21–22**：`0722–0724.md` 已收录或待观察 → 不重复。
- **LangChain #39050 model profiles refresh、LangGraph #8435–8440 deps bump**：内部 tooling/catalog → 待观察。
- **LlamaIndex #22457–22460 docs typo**：纯文档 → 过滤。
- **vLLM #49806–49808 docs typo/grammar**：纯文档 → 过滤。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-25** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Jul 25 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **606**（`0724.md` §7: 598 + 本日 §1: 8）
- Theory: **250**（`0724.md` §7: 245 + 本日 §2: 5）
- Career/Business: **104**（`0724.md` §7: 103 + 本日 §4: 1）
- LC: **86**（`0724.md` §7: 85 + 本日 §5: 1）
- IELTS: **86**（`0724.md` §7: 85 + 本日 §5: 1）
