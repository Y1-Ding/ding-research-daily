# Daily Research - 2026-09-09 (21:30)

## 0) 今日3条结论

- **AutoGen `ChatCompletionCache` 未把 `tool_choice` 纳入 cache key**（#8213, created **2026-09-09**）：`tool_choice="required"` 与 `"none"` 可命中同一 cached response → **agent eval 须把 tool policy 维度写入 cache key gold，否则 negative path 与 retry eval 全部失效。**
- **LangGraph `stream_mode="messages"` 可过滤 state key**（#8868, created **2026-09-09**）：`configurable["__pregel_stream_messages_keys"]` 限制 streaming 扫描范围 → internal reasoning channel 不再泄漏 → **skill 落地 + 评测须区分 public stream 与 checkpoint 完整 state。**
- **vLLM BailingMoeV2 `rope_theta` 未合并进 `rope_parameters`**（#56066, created **2026-09-09**）：Transformers v5 下 fallback `base=10000` 而真实 theta 远大于此 → 首 token 即 repetition loop → **Attention/RoPE eval 须断言 config bridge 与 spectrum alignment，不能只看 model load success。**

## 1) Agent工程化（high-signal）

- [feat(langgraph): restrict stream_mode="messages" to specific state keys（LangGraph PR #8868, created **2026-09-09**）](https://github.com/langchain-ai/langgraph/pull/8868)
  - 摘要: Closes #6798。`stream_mode="messages"` 原扫描 state 中全部 `BaseMessage`，internal reasoning channel（如 `agent_messages`）无 server-side 过滤即泄漏；新增 `configurable["__pregel_stream_messages_keys"]`，设值时仅扫描 listed keys，unset 时行为不变（附 backwards-compat test）。
  - 为什么重要(对我): **skill 落地 + 评测**——streaming visibility gate 是 agent platform eval 的 privacy/reproducibility 维度；eval 须断言 internal channel 不出现在 client stream。
  - 明日动作(<=20min): 写 **是/否**：你的 agent eval 是否断言 reasoning channel 不在 public message stream（是/否/未知）。

- [fix(checkpoint): make RedisCache key encoding injective（LangGraph PR #8869, created **2026-09-09**）](https://github.com/langchain-ai/langgraph/pull/8869)
  - 摘要: Fixes #8851。`RedisCache._make_key` 用 `:` join namespace 与 key 无 escaping → `(("a","b"),"k")` 与 `(("a:b",),"k")` 映射同一 Redis key → 后者 `set()` 覆盖前者、`clear()` 歧义；Fix percent-encode 各 segment 使 encoding injective 且 round-trippable。
  - 为什么重要(对我): **评测 + skill 落地**——cache key collision 是 distributed agent eval 的 silent data corruption gate；namespace 设计须可 injectivity 断言。
  - 明日动作(<=20min): 写 **是/否**：你的 agent cache eval 是否测 namespace collision（是/否/未知）。

- [fix(checkpoint-postgres): reject stale shallow checkpoints（LangGraph PR #8862, created **2026-09-08**）](https://github.com/langchain-ai/langgraph/pull/8862)
  - 摘要: Fixes #8810。cancelled `durability="exit"` run 可在 replacement run 已写入更新 state 后仍完成 older shallow checkpoint write → thread rollback；Fix 比较 stored vs incoming checkpoint ID，reject older write，blob 与 pending writes 不变。
  - 为什么重要(对我): **评测**——concurrent/cancelled run 的 checkpoint ordering 是 agent platform eval 的 race-condition gold；须测 stale write rejection 而非只测 happy path。
  - 明日动作(<=20min): 写 **是/否**：你的 agent eval 是否测 cancelled run 的 stale checkpoint rejection（是/否/未知）。

- [perf(langgraph): check only the newly finished future in on_done（LangGraph PR #8860, created **2026-09-08**）](https://github.com/langchain-ai/langgraph/pull/8860)
  - 摘要: Fixes #8859。`on_done` 用 `self.should_stop(self.done)` 扫描 accumulated set → k-th completion 扫描 k futures → **O(tasks²)** per superstep；Fix 仅 check newly finished future 并 remember positive result → O(n)。
  - 为什么重要(对我): **token 优化 + 评测**——superstep scheduling 复杂度直接影响 multi-node agent latency；eval 须标 node count scaling 而非只测 small graph。
  - 明日动作(<=20min): 写 **≤15 词**：你的 agent graph 最大 parallel node 数是多少（数字/未知）。

- [fix(deepseek): preserve reasoning content in request payloads（LangChain PR #40322, created **2026-09-09**）](https://github.com/langchain-ai/langchain/pull/40322)
  - 摘要: `ChatDeepSeek` 将 provider reasoning 存于 `AIMessage.additional_kwargs`，但 inherited OpenAI serializer 在 message 回传时 omit `reasoning_content`；Fix `_get_request_payload` 为 assistant messages restore 该 field，preserve DeepSeek reasoning-message contract across tool-call turns。
  - 为什么重要(对我): **skill 落地 + 评测**——multi-turn reasoning model 的 payload round-trip 是 agent eval gold；missing field 会导致 reasoning chain 断裂。
  - 明日动作(<=20min): 写 **是/否**：你的 DeepSeek agent eval 是否测 reasoning_content round-trip（是/否/未知）。

- [fix(agents): resolve environment handling and complete output truncation in ShellToolMiddleware（LangChain PR #40319, created **2026-09-09**）](https://github.com/langchain-ai/langchain/pull/40319)
  - 摘要: Fixes #40252（finalize #40253 系列）。`ShellToolMiddleware` 补齐 env mapping type normalization 与 output truncation（line count + byte limit）；persistent shell session 的 env 与 large output 处理完整。
  - 为什么重要(对我): **skill 落地 + token 优化**——shell tool output truncation 是 agent token budget gate；eval 须测 truncated output 的 trace 完整性与 env 类型约束。
  - 明日动作(<=20min): 写 **是/否**：你的 shell skill eval 是否断言 output truncation boundary（是/否/未知）。

- [fix(core): preserve Anthropic non-standard block input（LangChain PR #40321, created **2026-09-09**）](https://github.com/langchain-ai/langchain/pull/40321)
  - 摘要: Anthropic content-block translation 在构造 non-standard block 时移除原 dict 的 `index` → 读 `content_blocks` 会 mutate `AIMessage.content` → 后续 read 见不同 data；Fix copy unknown provider block before moving index，conversion repeatable。
  - 为什么重要(对我): **评测**——message content mutation 是 agent eval reproducibility gate；trace replay 与 gold 断言会因 silent mutation 脱节。
  - 明日动作(<=20min): 写 **是/否**：你的 agent eval 是否测 message content immutability across block conversion（是/否/未知）。

- [fix(cache): include tool_choice in ChatCompletionCache key（AutoGen PR #8213, created **2026-09-09**）](https://github.com/microsoft/autogen/pull/8213)
  - 摘要: `ChatCompletionCache` cache key 未含 `tool_choice` → 同 messages/tools 但不同 tool policy 的请求 reuse 同一 cached response（`required` vs `none` 互撞，`create_stream` 同理）；对 agent 尤其严重因 `tool_choice` 改变 model control flow。
  - 为什么重要(对我): **评测**——LLM cache key 维度遗漏是 agent eval 的 false-pass gate；须把 tool policy 纳入 cache isolation gold。
  - 明日动作(<=20min): 写 **是/否**：你的 agent LLM cache eval 是否含 tool_choice 维度（是/否/未知）。

- [docs: add btp-guard AST security gating cookbook recipe（AutoGen PR #8214, created **2026-09-09**）](https://github.com/microsoft/autogen/pull/8214)
  - 摘要: Closes #8162。Cookbook notebook 演示 AutoGen code execution 的 sub-50µs pre-flight AST invariant gating（`btp-guard`）；含 allowed execution 与 intercepted violation 示例，并标注 static inspection 安全边界。
  - 为什么重要(对我): **skill 落地**——code execution skill 的 AST gating 是 agent platform security gate 模板；eval 须测 violation interception 与 allowed path 分离。
  - 明日动作(<=20min): 读 cookbook 前 30 行，写 **1 条** AST gating 对你 code skill eval 的启发（≤20 词）。

- [fix(core): delete removed docs in UPSERTS_AND_DELETE even when hashes collide（LlamaIndex PR #23009, created **2026-09-09**）](https://github.com/run-llama/llama_index/pull/23009)
  - 摘要: `IngestionPipeline._handle_upserts` delete pass 通过 `get_all_document_hashes().values()` 枚举 stored docs → hash-keyed mapping 使 content 相同 doc collapse 为单 `doc_id` → 源端删除的 doc 在 hash collision 时 silent keep in docstore/vector store。
  - 为什么重要(对我): **评测 + RAG skill**——ingestion delete correctness 是 RAG agent eval gold；hash collision 场景须单独 gold path。
  - 明日动作(<=20min): 写 **是/否**：你的 RAG ingestion eval 是否测 hash-collision delete（是/否/未知）。

## 2) 原理深化（Attention/Transformer/Agent）

- [[Core][Model] Unify XD-RoPE into M-RoPE and derive the channel count（vLLM PR #56078, created **2026-09-09**）](https://github.com/vllm-project/vllm/pull/56078)
  - 摘要: XD-RoPE 为 native HunYuan-VL 添加，已迁移至 Transformers backend；无 `SupportsXDRoPE` 实现 → `uses_xdrope_dim` 恒 0 且 XD-RoPE branch unreachable；Transformers upstream 亦 collapse `xdrope_section` → `mrope_section`；Fix unify 为 M-RoPE 并从 config derive channel count。
  - 为什么重要(对我): **Talk ≥3 turns + RoPE/M-RoPE**——multimodal position encoding 的 section/channel 语义是 attention eval 的 config gate；须标 M-RoPE section 与 position id 对齐。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 multimodal M-RoPE model serving（是/否/未知）。

- [[Bugfix] Merge rope_theta into rope_parameters for BailingMoeV2（vLLM PR #56066, created **2026-09-09**）](https://github.com/vllm-project/vllm/pull/56066)
  - 摘要: BailingMoeV2 直传 `config.rope_parameters` 至 `get_rope` 未 bridge top-level `rope_theta`（V3 有）；Transformers v5 下 `rope_parameters` 不含 `rope_theta` → fallback `base=10000` 而真实 theta 远大于此 → RoPE spectrum 全 misalign → 首 token repetition loop；Fix 添加 `_build_rope_parameters` merge top-level theta 与 scaling override。
  - 为什么重要(对我): **Talk ≥3 turns + RoPE spectrum**——`rope_theta` bridge 是 MoE model attention 的 config correctness gate；load success ≠ spectrum correct。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 BailingMoe 系列 serving（是/否/未知）。

- [Skip padded rows in grouped MoE routing（vLLM PR #56079, created **2026-09-09**）](https://github.com/vllm-project/vllm/pull/56079)
  - 摘要: Propagate persistent padding mask through GPU model runners → sequence-parallel / DP / DBO / CUDA-graph padding rows 在 expert dispatch 前 invalidate；避免 padded token 进入 grouped MoE routing。
  - 为什么重要(对我): **Talk ≥3 turns + MoE routing attention stack**——padding mask propagation 是 distributed MoE eval 的 correctness gate；须标 padded row exclusion 而非假设 clean batch。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 grouped MoE + CUDA graph serving（是/否/未知）。

- [[Perf][Frontend] Defer reasoning usage recounts for non-continuous chat streams（vLLM PR #56067, created **2026-09-09**）](https://github.com/vllm-project/vllm/pull/56067)
  - 摘要: Chat streaming 每 delta 从 full generated history recount reasoning tokens，continuous usage disabled 时 Qwen parser with thinking disabled 仍反复 decode+parse 全 history；Fix 每 nonempty choice count once before final usage；continuous/forced usage 仍 per-delta；parser rules / sampling / model computation 不变。
  - 为什么重要(对我): **Talk ≥3 turns + token 优化**——streaming reasoning token accounting 是 agent serving eval 的 overhead gate；须分 continuous vs non-continuous usage profile。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 Qwen reasoning model streaming serving（是/否/未知）。

- [[CI][ROCm][Disagg] Enable breakable CUDA graphs for MiniMax-M3-MXFP8 decode（vLLM PR #56075, created **2026-09-09**）](https://github.com/vllm-project/vllm/pull/56075)
  - 摘要: Follow-up #54782。MiniMax-M3-MXFP8 decode 在 ROCm disagg CI fail：`piecewise CUDA graphs unavailable ... Set VLLM_USE_BREAKABLE_CUDAGRAPH=1`；prefill eager / decode piecewise graphs；Fix 在 `models.yaml` 设 `VLLM_USE_BREAKABLE_CUDAGRAPH: "1"` 使 bring-up 与 GSM8K pass。
  - 为什么重要(对我): **Talk ≥3 turns + decode attention kernel**——prefill/decode graph mode split 是 disaggregated serving eval 的 capability gate；须标 breakable graph requirement。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 disaggregated prefill/decode serving（是/否/未知）。

- [[Bugfix][Core] Reject batch-sharded sampling with LoRA at startup（vLLM PR #56074, created **2026-09-09**）](https://github.com/vllm-project/vllm/pull/56074)
  - 摘要: Fixes #56068。`compute_logits_local` models（llama, qwen3_5, deepseek_v4-nvidia, minimax_m3-nvidia）skip TP logits gather → `lm_head` LoRA delta 只能在 gather 后 apply → `--enable-lora` + `--enable-batch-sharded-sampling` 首 sampling step crash `NotImplementedError`；Fix startup reject with clear reason。
  - 为什么重要(对我): **Talk ≥3 turns + LoRA attention path**——logits gather 与 LoRA apply 顺序是 serving config compatibility gate；eval 须在 startup 标 unsupported combo。
  - 明日动作(<=20min): 写 **≤15 词**：你是否 LoRA + batch-sharded sampling（是/否/未知）。

## 3) 热门项目/可试个人项目

- [achimala/dream-loop — closed-loop visual agent skill（created **2026-09-07**, ~413 stars **2026-09-09** GitHub API）](https://github.com/achimala/dream-loop)
  - 摘要: Agent skill：image gen 生成 target screenshot → build toward target → subagent critic 比较 live vs target → loop until satisfied → 可选 re-dream better target；支持 Three.js/Blender；测试于 GPT-6 Astra in Codex；`npx skills add achimala/dream-loop`。
  - 为什么重要(对我): **skill 落地 + 评测**——「target → build → critic → loop」是可复用的 agent skill eval pipeline 模板；critic satisfaction 是可断言 gold。
  - 明日动作(<=20min): 读 README loop 5 步，写 **1 条** critic loop 对你 agent skill eval 设计的启发（≤20 词）。
  - 估计复现成本: **低**（clone + 读 loop spec + 写 1 条接点，~15min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Reducing cost and improving performance with Claude Platform（Anthropic Blog, **2026-09-08**）](https://claude.com/blog/reducing-cost-and-improving-performance-with-claude-platform)
  - 机会信号: Anthropic 发布 `/claude-api cost-optimize` + `prompt-audit` + `hillclimb` 三件套 → prompt cache hit rate / anti-pattern removal / effort calibration 可 **14–73% 降本且性能不降**（LegalBench -58%, tau2-bench -73%, SWE-bench -55%）；Fable 5.1 cache read $0.25/MTok（-75%）→ agentic workload 成本杠杆明确；**AI solo 公司可用 eval-driven cost audit 作为 signature 方法论**。
  - 风险信号: benchmark 结果依赖特定 task shape；hillclimb 过拟合 train set 风险（文中有 held-out test 但需自备 eval）；prompt cache TTL 5min 对 long-running agent 易 miss。
  - 我可执行的一步(<=30min): 读 blog §「Prompt cache」4 条 practical tips，写 **2 条** 对你 agent token budget 设计的接点（各 ≤15 词）。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[3639. Count Commas in Range II](https://leetcode.com/problems/count-commas-in-range-ii/)（**2026-09-09 Daily Challenge**，verified via LeetCode GraphQL `activeDailyCodingChallengeQuestion`，承接 0908 的 4245 Count Commas in Range Easy，今日 **Medium**「区间 `[L,R]` 逗号计数 + 数位 DP / 贡献分解升级」）：关键洞察——Easy 版 `[1,n]` 可 O(log n) 按位区间贡献；Medium 版需处理任意 `[L,R]` 边界与 leading zero；与 agent eval「区间聚合 + 边界 case gold」同构。明日动作：手写 `[L,R]` 版贡献分解；写 **≤15 词**：区间边界对你 eval 分段聚合意味着什么。

- **IELTS（≤1 条）**：[IELTS Writing Practice Tests（British Council）](https://takeielts.britishcouncil.org/take-ielts/prepare/free-ielts-english-practice-tests/ielts-on-computer/practice-tests/writing)：0908 已轮换 Listening，今日换 **Writing**（选 Task 1 Academic 图表描述或 General Task 1 书信，限时 20min）。明日动作：完成 1 篇 Task 1；记录 **≥2 个** 失误类型（时态 / 数据描述 / 段落结构 / 字数）；写 **≤15 词** 你最弱的 Writing 维度。

## 6) 噪音过滤记录

- **fix(openai): use unique OAuth token temporary files（#40326）**：OAuth infra fix，与 focus 弱相关 → 过滤。
- **fix(core): preserve empty Google GenAI image URLs（#40325）**：edge case fix → 过滤。
- **fix(langchain-classic): preserve chain input mappings（#40324）**：legacy chain fix → 过滤。
- **fix(langchain): run shell cleanup outside the event loop（#40323）**：0908 已收录 #40253 系列 partial fix → semantic duplicate with #40319 → 过滤。
- **fix(instrumentation): use None sentinel to fix mutable default args（#23007）**：instrumentation infra fix，信号弱于 #23009 → 过滤。
- **Add llama-index-vector-stores-infino（#23008）**：新 integration vendor → 过滤。
- **chore(deps/docs/model-profiles PRs（LangGraph #8867, LangChain #40317/#40318 等）**：deps/docs refresh → 过滤。
- **Refactor cpu offloading / Nanbeige model add / MoRI upgrade / docs PRs（vLLM #56076/#56071/#56073/#56072 等）**：infra/model add → 过滤。
- **[Bugfix][Frontend] Preserve metadata slots across modalities（#56069）**：multimodal metadata alignment，信号弱于 RoPE/MoE focus → 过滤。
- **0908 已收录项**：LangGraph governance 批 #8845–#8849、LlamaIndex MCP #22992/#22998、AutoGen concurrent speakers #8206/#8207、DeepSeek async JSONDecodeError #40282、vLLM MTP decode ordering #55898 等 → semantic duplicate，不重复。
- **Simon Willison GPT-6 Astra / rogue agent wikis 系列（Sep 3–5）**：0906–0908 已收录 → 不重复。
- **Anthropic Fable 5.1 launch / FLT formalization（Sep 1–4）**：0907 已收录 → 不重复。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-09-09** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **931**（`0908.md` §7: 921 + 本日 §1: 10）
- Theory: **425**（`0908.md` §7: 419 + 本日 §2: 6）
- Career/Business: **140**（`0908.md` §7: 139 + 本日 §4: 1）
- LC: **122**（`0908.md` §7: 121 + 本日 §5: 1）
- IELTS: **122**（`0908.md` §7: 121 + 本日 §5: 1）
