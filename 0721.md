# Daily Research - 2026-07-21 (21:30)

## 0) 今日3条结论

- **LangGraph Postgres pipeline 生命周期 bug 链在 #8396 收束**：`AsyncPostgresSaver` 在 `__init__` 存储外部 `AsyncPipeline` 引用，pipeline 上下文退出后仍用已关闭连接 → `SSL connection has been closed unexpectedly`；与 0719–0729 #8386/#8385 同属 checkpoint 集成陷阱，eval harness 应统一 `from_conn_string` 生命周期并在 CI 测 pipeline 路径。
- **skill eval 的「自修正偏差」需要 executor/grader 结构分离**：AEVAL 2607.16345 证明同一 agent 既执行又评分会把「调试能力」误记为「skill 质量」——first-attempt grading + 独立 grader 才能把 100% pass 还原为可审计 FAIL；你的 L2/L3 应显式记录 self-correction 次数。
- **vLLM PCP+MLA 被 CI 自动 revert（#49196）**：GLM-5.2 GSM8K 在 TP2+PCP2+EP 仅 0.52、TP1+PCP4+EP 仅 0.15（阈值 0.82）→ 长上下文 agent 的 sparse MLA + context parallel 路径存在 correctness bug；#49294 用 `-inf` LSE mask 修复空 context chunk merge，部署栈应记录 PCP 回退链。

## 1) Agent工程化（high-signal）

- [fix: remove stored pipeline reference to prevent SSL connection closed errors（LangGraph PR #8396, updated **2026-07-21**）](https://github.com/langchain-ai/langgraph/pull/8396)
  - 摘要: `AsyncPostgresSaver.__init__` 接受并存储外部 `AsyncPipeline` 引用，该引用在 `from_conn_string` 的 pipeline 上下文退出后仍被 saver 持有；后续 `setup()`/`put()` 等操作触发 `psycopg.OperationalError: SSL connection has been closed unexpectedly`。Fix：移除 `pipe` 参数与存储，pipeline 仅在需要的方法内通过 `async with self.conn.pipeline():` 内部管理。
  - 为什么重要(对我): **skill 落地 + eval**——与 0720 #8386（pipeline 上下文内 setup）构成完整修复链；multi-turn checkpoint 若在 pipeline 外单独调 saver 方法，应 grep 是否仍传外部 pipe。
  - 明日动作(<=20min): 写 **是/否**：你 Postgres checkpoint 是否在 `from_conn_string` 外单独调 `setup()`/`put()`（是/否/未知）。

- [fix: ToolNode interrupt propagation and related audit fixes（LangGraph PR #8395, updated **2026-07-21**）](https://github.com/langchain-ai/langgraph/pull/8395)
  - 摘要: `wrap_tool_call` 路径下 `ToolNode` 吞掉 interrupt（`GraphBubbleUp` 被转成 error ToolMessage 而非向上传播）；同 PR 还修 retry budget 多策略、CLI telemetry hang、config aliasing、Postgres pending-sends migration、checkpoint/serde 类型漂移等。Fix：`GraphBubbleUp` 正确传播，human-in-the-loop interrupt 不再被静默转为 tool error。
  - 为什么重要(对我): **skill 落地 + 评测**——skill agent 依赖 interrupt 做审批门禁；interrupt 被当 tool error = eval 把「用户取消」记成「工具失败」= 假阴性/假阳性混杂。
  - 明日动作(<=20min): 写 **是/否**：你 agent 是否用 `interrupt()` + `ToolNode`（是/否/未知）。

- [fix(langgraph): preserve message IDs and additional_kwargs in add_messages format="langchain-openai"（LangGraph PR #7273, updated **2026-07-21**）](https://github.com/langchain-ai/langgraph/pull/7273)
  - 摘要: `add_messages(format="langchain-openai")` 的 `_format_messages()` 两处丢数据：(1) `convert_to_openai_messages()` 未传 `include_id=True` → message ID 全丢；(2) 非标准 `additional_kwargs`（UI widget、metadata）在 OpenAI 格式 round-trip 中不可恢复。Fix：`include_id=True` + 按 ID 保存/恢复 `additional_kwargs`。
  - 为什么重要(对我): **skill 落地 + eval**——multi-turn skill eval 依赖 message ID 做 trace 对齐与 delta replay；ID 丢失 = collect 层无法关联同一 turn 的 tool call 与 response。
  - 明日动作(<=20min): 写 **是/否**：你 LangGraph state 是否用 `format="langchain-openai"`（是/否/未知）。

- [fix(pregel): deduplicate in-flight PUSH child tasks on parent retry（LangGraph PR #8392, updated **2026-07-20**）](https://github.com/langchain-ai/langgraph/pull/8392)
  - 摘要: `_runner.py` 中 in-flight PUSH child dedup 比较 `PregelExecutableTask` 对象与 string ID（`t == next_task.id`）恒为 `False`；父任务 retry 时已在跑的 nested PUSH child 被重复调度。Fix：改为 `t.id == next_task.id`（sync/async 双路径）。
  - 为什么重要(对我): **评测可靠性**——parallel skill eval worker 在父节点 retry 时可能双跑同一子 agent = 重复 side effect + token 翻倍；harness 应断言 child task ID 唯一。
  - 明日动作(<=20min): 写 **是/否**：你 graph 是否有 parent retry + nested PUSH child（是/否/未知）。

- [fix(langchain): handle OpenAI phased responses in ProviderStrategyBinding parsing（LangChain PR #38997, updated **2026-07-21**）](https://github.com/langchain-ai/langchain/pull/38997)
  - 摘要: OpenAI phased response 含多个 text block（`commentary` + `final_answer`），`_extract_text_content_from_message` 拼接全部 block 产生无效 JSON。Fix：逐 block 从后往前尝试 JSON parse，仅 `final_answer` block 含 structured output 时正确命中；兼容其他模型的 split-JSON 响应。
  - 为什么重要(对我): **评测 + skill 落地**——structured output judge / rubric 解析是 L2 eval 常见路径；phased response 拼接 = silent parse failure = eval 假阴性。
  - 明日动作(<=20min): 写 **是/否**：你 judge 是否用 OpenAI structured output / phased response（是/否/未知）。

- [fix(core): clear usage metadata callback on with-block exceptions（LangChain PR #38993, updated **2026-07-21**）](https://github.com/langchain-ai/langchain/pull/38993)
  - 摘要: `get_usage_metadata_callback` 的 `ContextVar` 仅在 `yield` 后清理；`with` 块抛异常时 cleanup 被跳过，后续 `llm.invoke()` 仍累加到前一个 callback → token 计数静默错误。Fix：`try/finally` + `ContextVar.reset(token)`，对齐 `tracing_v2_enabled` 模式。
  - 为什么重要(对我): **token 优化 + eval**——eval harness 在异常路径后继续跑下一 case 时，token 预算统计会污染；CI 应测「with 块异常后 callback 已清零」。
  - 明日动作(<=20min): 写 **是/否**：你是否用 `get_usage_metadata_callback`（是/否/未知）。

- [fix(core): coerce list-to-str and str-to-list field types in PydanticOutputParser（LangChain PR #38996, updated **2026-07-21**）](https://github.com/langchain-ai/langchain/pull/38996)
  - 摘要: 小模型常把 `str` 字段 emit 成 `list`（或反之），Pydantic `model_validate()` 直接 `ValidationError` → 不透明的 `OutputParserException`。Fix：validation 失败后 `_coerce_field_types()` 保守转换（`list[str]`→`str` 用 newline join、单元素 list unwrap scalar），仍失败则抛原 `ValidationError`。
  - 为什么重要(对我): **评测**——judge/rubric 用 `PydanticOutputParser` 时，小模型 schema 违规是高频 failure mode；coercion 可降低 eval 噪声但不掩盖真错误。
  - 明日动作(<=20min): 写 1 条你域内「模型输出 list 但 schema 要 str」的真实样例（≤10 行）。

- [AEVAL: From Anecdotal to Deterministic Testing for Agentic Skill Workflows（arXiv **2607.16345**）](https://arxiv.org/abs/2607.16345)
  - 摘要: CI 集成的 skill 确定性测试框架：skill 声明 `eval.config` → change-triggered 自动执行 → 结构化 evidence-grounded 质量信号。核心：**executor/grader 结构分离**防止 self-correction bias（agent 静默修补 skill 后自评 100% pass）；first-attempt grading + self-correction tracking + tiered fix suggestions（LV1 因果修复 / LV2 质量改进）作为 inline MR comment。
  - 为什么重要(对我): **skill 落地 + 评测**——与 0719 SkillCoach（过程四维 rubric）、0720 2606.17819（auto task 合成）互补：AEVAL 解决「CI 怎么跑 skill 变更」；你的 playbook 可 fork executor/grader 分离 + first-attempt 规则进 collect 层。
  - 明日动作(<=20min): 写 1 条你可映射的「executor 与 grader 必须分离」场景（≤10 行）。

## 2) 原理深化（Attention/Transformer/Agent）

- [Revert "[Core] Add MRV2 virtual-batch PCP for MLA"（vLLM PR #49196, updated **2026-07-21**）](https://github.com/vllm-project/vllm/pull/49196)
  - 摘要: CI 自动 revert #46570：GLM-5.2 GSM8K 在 prefill-context-parallel 下 accuracy 崩溃——`TP2+PCP2+EP` 仅 0.5224、`TP1+PCP4+EP` 仅 0.1486（阈值 0.82）；PCP 度越高越差，指向 PCP+MLA 新路径 correctness bug。Revert 为 #46570 的精确逆操作（39 files）。
  - 为什么重要(对我): **Talk ≥3 turns + Attention**：sparse MLA + PCP 是长上下文 multi-turn agent 的 attention 算子层；此 revert 说明「新 attention 并行路径未过 GSM8K 回归」——部署栈应记录 PCP 启用/回退链，eval bench 应含 PCP 维度。
  - 明日动作(<=20min): 写 **≤15 词**：你 serving 是否启用 context parallel（是/否/未知）。

- [[Bugfix][Attention] Ignore empty MLA context chunks during merge（vLLM PR #49294, updated **2026-07-21**）](https://github.com/vllm-project/vllm/pull/49294)
  - 摘要: 宽 PCP batch 将 cached context 切成 workspace-sized chunk 后，后续 chunk 可能对某些 prefill row 无 KV token；backend 仍返回有限 LSE → `merge_attn_states` 把未定义输出当有效 attention 污染 prefill 结果。Fix：零-context row 在 merge 前用 `-inf` LSE mask；单 in-place Triton kernel，B300 上 2K–131K balanced/skewed 均 ~12–13μs。
  - 为什么重要(对我): **Attention 机制**：#49196 revert 的直接修复方向；empty context chunk 是 PCP 分裂的结构性产物，attention merge correctness = 长上下文 agent 召回/生成质量。
  - 明日动作(<=20min): 写 **≤15 词**：你 eval 是否覆盖 PCP 分裂后的 attention merge（是/否/未知）。

- [[Feature] implement return indexer topk of sparse attention（vLLM PR #47279, updated **2026-07-21**）](https://github.com/vllm-project/vllm/pull/47279)
  - 摘要: 新增 `--enable-return-indexer-topk` flag，将 sparse attention indexer 层每 token 选中的 top-k KV slot 索引返回客户端（base64 npy，`shape=(num_tokens, num_indexer_layers, index_topk)`）。`SamplingParams.indexer_topk_prompt_start` 支持 multi-turn 场景跳过已返回 prefix 的 prompt token，避免 topk 重复。
  - 为什么重要(对我): **Attention + token 优化 + eval**：首次把 sparse attention 路由决策暴露为可观测信号；multi-turn agent eval 可测「哪层 indexer 选了哪些 KV」与召回/幻觉的相关性。
  - 明日动作(<=20min): 写 **≤15 词**：你是否需要 attention routing 可观测性（是/否/未知）。

## 3) 热门项目/可试个人项目

- [opensearch-project/agent-health（GitHub, Skills Evaluator）](https://github.com/opensearch-project/agent-health/blob/main/docs/SKILLS.md)
  - 摘要: AgentSkills/Claude Code skill 的 A/B 评测器：同一 prompt 跑 **with_skill** vs **without_skill** 双轨，对同一组 assertion 评分并报告 pass-rate/latency/token delta；可选 LLM 从 failure evidence 提议改进 `SKILL.md` 并 `auto` 应用（先 snapshot 到 `.bak`，校验原文 verbatim 匹配防 silent no-op）。
  - 为什么重要(对我): **skill 落地 + 评测杠杆**——比 0720 `aws-samples` 更贴近「skill 变更 → A/B → 可选 auto-fix」闭环；可取其 assertion 格式 + auto-apply guard 进 eval playbook。
  - 明日动作(<=20min): 读 `docs/SKILLS.md` 的 `evals/evals.json` 格式；对 1 个现有 skill 写 1 条 assertion（≤10 行）。
  - 估计复现成本: **低**（读文档 + 写 assertion，≤30min）。

## 4) 职业与商业探索池（AI个人公司/出海/海外工作）

- [Understand to participate（Simon Willison Blog, **2026-07-02**）](https://simonwillison.net/2026/Jul/2/)
  - 机会信号: Geoffrey Litt（AIE 2026）框架——与 coding agent 协作需「理解到能参与」的深度，避免 cognitive debt（理解漂移于实际代码）；「rich set of concepts」是持续参与 creative process 的前提 → **harness/评测/可观测性** 是降低 cognitive debt 的杠杆，适合出海 remote 的 signature。
  - 风险信号: 框架偏认知/协作哲学，非直接技术产出；需落地为「eval 让 agent 行为可审计 → 降低参与门槛」才有简历证据。
  - 我可执行的一步(<=30min): 写 **≤8 行**：你当前 1 个 agent 项目中，哪 1 个环节若加 trace/eval 可让你「参与」而非「旁观」。

## 5) 连续性维护（LC / IELTS）

- **LC（≤1 条）**：[LeetCode 673 Number of Longest Increasing Subsequence](https://leetcode.com/problems/number-of-longest-increasing-subsequence/)（**Day 21, Jul 2026, Medium**）：DP 求最长递增子序列个数——`lis[i]` 长度 + `fq[i]` 计数，同长度取 max 时累加 freq。明日动作：手写 DP 20 行；写 **≤15 词**：agent 多路径规划与 LIS 计数 DP 的类比。

- **IELTS（≤1 条）**：[IELTS Ready — British Council 官方免费备考（含 AI Mini Mock）](https://takeielts.britishcouncil.org/take-ielts/prepare/ielts-ready)：50 分钟 Mini Mock + AI 估分。明日动作：完成 **Speaking 或 Writing 1 节 Mini Mock**；写 **≤15 词** 错因类型（流利度/词汇/语法 选 1）。

## 6) 噪音过滤记录

- **LangGraph #8385/#8386/#8383、#8362 trace_inputs**：`0719–0720.md` 已收录 → 不重复（#8396 为 #8386 链最终收束）。
- **LangChain #38969 DeduplicateToolCalls、#38970 lazy transformers、LlamaIndex #22404/#22405**：`0720.md` 已收录 → 不重复。
- **SkillCoach 2607.01874、2606.17819、aws-samples skill-eval**：`0719–0720.md` 已收录 → 不重复。
- **vLLM #47629 TRITON_MLA_SPARSE、#49188/#49199**：`0719–0720.md` 已收录 → 今日 §2 改收 #49196/#49294/#47279。
- **Fingerprint 2607.06621、Legible 2607.04319/2607.08946、DSpark 2607.05147**：`0708–0720.md` 已收录 → 不重复。
- **LangGraph #8354 omit_expired**：`0718.md` 已收录 → 不重复。
- **LangGraph #8390 delta channel plain-value seeds**：重要但偏 checkpoint 边缘 case，今日 §1 已满 8 条 → 待观察。
- **LlamaIndex #22419 rate limiter、#22421 rerank fusion、#22418 refine early-stop**：RAG/token 相关但今日 §1 已满 → 待观察。
- **openai/evals #1657 atr_prompt_injection**：有趣但 16 样本 niche → 待观察。
- **Anthropic Demystifying evals / Infrastructure noise**：高价值但非 07-21 新鲜信号 → 记入待观察。
- **X / YouTube / Bilibili 白名单**：无法稳定抓取 **2026-07-21** 原帖 URL → 不收录。
- **news-aggregator-skill**：环境中未找到该 skill 文件 → 未调用；白名单 GitHub Jul 21 + arXiv 高信号 ≥8 条，无需 skill 补充。

## 7) 本周累计计数

- Agent: **574**（`0720.md` §7: 566 + 本日 §1: 8）
- Theory: **236**（`0720.md` §7: 233 + 本日 §2: 3）
- Career/Business: **100**（`0720.md` §7: 99 + 本日 §4: 1）
- LC: **82**（`0720.md` §7: 81 + 本日 §5: 1）
- IELTS: **82**（`0720.md` §7: 81 + 本日 §5: 1）
